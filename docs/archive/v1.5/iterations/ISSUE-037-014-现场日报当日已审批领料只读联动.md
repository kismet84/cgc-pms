# ISSUE-037-014 迭代记录

- 任务性质：能力新增
- 结果：Done，计入 `启动迭代-5` 第 1/5 条。
- 实现：日报详情批量读取同租户、同项目、同日期、`APPROVED` 且已出库的领料及物料，只返回最小非金额字段；前端只读展示。
- 验证：后端 10 tests、前端 6 tests、`pnpm type-check`、`git diff --check` 全部通过。
- 审查：独立 Reviewer PASS，无阻塞 finding；未执行真实浏览器视觉验收，列为非阻塞剩余风险。
- 回滚：移除日报详情 `requisitions` 聚合字段、日报侧 VO 和前端展示区；无数据迁移。

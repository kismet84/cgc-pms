# ISSUE-048-011 修正会计凭证页面陈旧生成规则说明验收报告

验收日期：2026-07-19
验收结论：**通过**
阻塞项：**0**
发布结论：**未提交、未推送、未发布生产。**

## 1. 目标与边界

- 将会计凭证页陈旧说明修正为当前事实：付款、回款凭证由权威业务写侧自动生成，手工生成入口未开放。
- 补充页面契约测试并执行真实只读页面复验。
- 不新增生成按钮、生成 API、科目映射、凭证策略、权限、迁移或业务数据，不改变复核、过账、冲销及会计期间状态机。

## 2. 实施结果

- 页面标题由“凭证生成入口暂未开放”收敛为“手工生成入口未开放”。
- 辅助说明改为“付款、回款凭证由权威业务写侧自动生成；当前仅开放查询、详情、过账和冲销”。
- 页面测试同时约束旧说明不存在、新事实存在、手工入口未开放，且源码不引用 `generateAccounting` 或 `/generate`。
- `A-01-ACCOUNTING-GENERATION-COPY` 已从唯一问题载体移除；A-01 剩余 9 个兼容性待确认子项。

## 3. 验收证据

| 验收门 | 结果 |
| --- | --- |
| Ready lint | 实施前通过；`ISSUE-048-011`，failureCategory=`none`；关闭为 Done 后不再作为 Ready 复跑 |
| 页面专项 | 1 个测试文件、5 项全部通过 |
| Legacy 类型检查 | `vue-tsc --noEmit` 通过 |
| 差异检查 | `git diff --check` 通过 |
| 运行态前置 | backend health 200、Legacy 5173 为 200、dev-login 为 302 |
| 真实页面 | `/accounting-entry` 显示新说明；旧说明计数 0；生成按钮计数 0 |
| 浏览器控制台 | warning/error 计数 0 |

浏览器只读访问会计凭证列表；未触发生成、复核、过账、重提或冲销。

## 4. 权限、数据与回滚

- 本轮未修改后端、API、路由、Store、权限码、数据库 migration 或历史会计事实。
- `accounting:add` 未扩大；正常付款、回款继续由权威业务写侧自动生成凭证。
- 回滚只需回退页面文案、对应测试和本轮治理回写；无数据恢复动作。

## 5. 收口守恒

- 本轮修复并复验：`A-01-ACCOUNTING-GENERATION-COPY`。
- 超出当前范围并正式承接：0。
- 证据不足或无明确价值而关闭：0。
- 新增后续项：0。
- 关闭后续项：1。
- 后续项净变化：-1。

结论：全部发现已处置，无悬空项；`ISSUE-048-011` 通过。

# ISSUE-040-038 投标成本受控删除入口与状态租户边界验收报告

结论：通过；阻塞：无。

## 范围与结果

- 投标成本页为 BIDDING 记录增加桌面与移动删除入口，仅对 `bid:delete` 或 ADMIN/SUPER_ADMIN 可见；确认文案包含目标项目名称和仅投标中可删边界。
- 前端只发送 `DELETE /bid-cost/{id}`，无 params、body、tenantId 或业务载荷；成功重新读取列表，失败显示后端原因并通过 rejected `onOk` 保留确认态。
- V153 的 MySQL/H2 迁移只注册 `bid:delete` BUTTON，并绑定既有 SUPER_ADMIN、ADMIN、COST_MANAGER；不复用 `bid:edit` 或 `bid:status`，未扩大状态迁移权限。

## 验收证据

- Ready lint、`git diff --check`、SQL 安全与双迁移静态核对通过。
- 后端：`BidCostControllerTest` 25项通过，覆盖未登录401、无权限403、显式 `bid:delete` 与管理员成功、权限迁移隔离、跨租户/不存在统一隐藏、WON/LOST 返回 `BID_STATUS_NOT_DELETABLE`。
- 前端：API 与页面专项2个文件共10项通过；`vue-tsc --noEmit`、目标 ESLint 通过。
- 审查：Controller 只授予独立 `bid:delete` 或管理员；Service 先按认证租户取目标，再限定 BIDDING；前端只有二次确认 `onOk` 才调用 DELETE，错误重新抛出以避免确认态伪关闭。最终 findings=无。
- 运行态：后端 health、原生前端5173与 dev-login 为200/200/302。Docker 前端容器内部正常但宿主5173转发超时，归类为环境前置；停止该前端容器并在同工作区隐藏启动原生 Vite 后稳定205秒。浏览器进入 `/bid-cost`，删除入口唯一，确认展示“投标测试项目”和状态边界，点击取消；后端浏览器阶段 DELETE 计数前后均为0，13:49后的控制台新增 error/warn=0。

## 治理收口

- 唯一台账移除 `A-01-BID-DELETE`，A-01 更新为有用户入口243、前端调用但无独立页面58、内部/集成/运维4、需补入口5、待废弃0、需要确认11，共321。
- 新增后续项：0；关闭后续项：1；后续项净变化：-1。
- 最小回滚：回退 V153、前端删除 API/确认入口、专项测试与治理回写；浏览器未写业务数据，无数据回滚。

剩余风险：浏览器只验证管理员打开与取消路径，真实成功、失败、租户和状态边界由后端集成与前端契约测试覆盖；中标/失标仍由既有唯一条目承接，不属于本轮新增悬空问题。

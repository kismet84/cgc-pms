# ISSUE-053-036 M7 登录与错误深链 V2 验收报告

> 日期：2026-07-27
>
> 基线：`master@07f0169da0a3c8ae3f6ee4de463dfe55da2e84de`
>
> 裁决：`PASS / ISSUE-053-036 Done / M7 CONTINUES`

## 1. 范围与结论

`/login`、`/403` 和 catch-all 404 三条 Legacy URL 已由真实 Clean-room V2 页面或受控确定性重定向承接，台账由 `24/63/0` 更新为 `21/66/0`。

- `/login` 继续使用同源 HttpOnly Cookie；凭据不进入存储、URL 或日志。
- `/403` 兼容路由确定性跳转 `/forbidden`，保留 query/hash；匿名访问仍先恢复会话并失败关闭到登录页。
- 未知深链只在会话恢复后渲染 404，不与缺权限 403 混同。
- 本切片零业务 API 写入、零后端改动、零数据库操作。

## 2. 实施证据

| 事项 | 证据 | 结果 |
| --- | --- | --- |
| V2 路由 | `frontend-admin-v2/src/router.ts` 的 `V2LegacyForbiddenRedirect` | 通过 |
| 台账接受映射 | `frontend-admin-v2/scripts/generate-route-ledger.mjs` 的三条白名单与 `assertM7GlobalV2Acceptance` | 通过 |
| unit | `pnpm test:unit` | 49 文件、366 项全通过；目标 router 16/16 |
| type | `pnpm type-check` | 通过 |
| lint | `pnpm lint:check` | 0 error；6 个既有 warning 均位于未修改的 `e2e/m1-shell.spec.ts:149-156` |
| clean-room | `pnpm check:boundary` | 176 个 V2 文件、16 个契约文件通过 |
| route ledger | `pnpm check:route-ledger` | 87 条命名路由，`21/66/0`，无漂移 |
| build | `pnpm build` | 228 modules，构建通过 |
| E2E | 独立端口 `5192` 执行 `playwright test e2e/auth.spec.ts` | 5/5 通过 |
| diff | `git diff --check` | 通过 |

认证 E2E 首次命中复用的旧 5174 Vite 服务，分类为 `environment_prerequisite`；切换独立端口后确认当前 `/403` 路由生效。随后发现认证规格未隔离业务 API，未登录后端 401 会按正确运行时策略清空会话，分类为 `ready_issue_config`；测试夹具改为认证 API 精确桩、其他业务 API 受控 500，最终 5/5 通过。未修改运行时会话策略。

## 3. 权限与安全复核

- 普通角色、无权限、匿名和通配权限样本均由现有 session/permission 守卫裁决。
- `/403` 未改为 public，避免匿名用户绕过会话恢复。
- 404 不发起业务请求；403 页面只显示被阻断的前端路径。
- 独立只读安全复核同意本切片通过；同时确认系统页面接入前必须补独立 `adminOnly` 角色门。

## 4. 剩余风险与下一门

- M7 尚余 21 条 `LEGACY_ONLY`；`ISSUE-053-037` 承接 profile/settings/help。
- `/approval/process` 和系统管理仍禁止接入，直到 router/navigation 的 adminOnly 门及普通角色负向测试通过。
- 组织、材料、字典和权限菜单存在权限码矩阵差异，必须在对应切片内先关闭。
- `/system/data` 只允许非生产 SUPER_ADMIN，且 M7 验收不执行真实清库。

## 5. 回滚与零悬空

- 回滚仅需移除 `/403` V2 兼容路由、三条接受映射及对应测试，再生成台账；Legacy 正式入口未变。
- 新增后续项：0；关闭后续项：0；后续项净变化：0。
- 独立复核发现的 4 个门已并入 M7 对应 Issue 验收，不另造 Backlog；无口头悬空项。

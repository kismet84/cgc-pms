# ISSUE-037-009 库存项人工补货目标量联动验收报告

## 结论

- Decision：通过。
- 阻塞：无。
- 上线边界：仅本地开发/测试环境完成验收；未发布、未 push。

## 实施边界

- V144 为 `mat_stock` 增加 nullable `replenishment_target_qty DECIMAL(18,4)`，不回填旧数据。
- 新设置接口一次原子维护安全库存与可空人工补货目标量；旧安全阈值接口保持兼容并保护目标关系。
- 复用 `inventory:stock:edit`、租户、启用仓库、项目范围和 `@Version`，不新增权限或策略表。
- 低库存 KPI 仍只由安全阈值触发；目标量仅决定采购申请预填数量。

## 验收证据

- Ready lint 与独立准入复核 PASS。
- TDD RED 后实现；`MatStockServiceTest,MatStockControllerTest` 共 53/53 通过。
- 前端库存专项 4/4、`pnpm type-check`、`git diff --check` 通过。
- 独立实现审查 PASS：原子关系、NULL 清空、乐观锁、权限范围、KPI、迁移、回退公式和旧接口均无阻塞缺陷。
- 重建前后端并稳定等待 180 秒；health gate 为 backend `UP`、frontend 200、dev-login 302。
- 真实 MySQL 的 V144 成功，既有库存目标量初始为 NULL。
- 真实 `PURCHASE_MANAGER`：安全阈值 10、目标量 60 保存后台账回读 60；KPI 前后均为 1；目标量 60 低于安全阈值 70 时返回 400；最终恢复安全阈值 10、目标量 NULL。

## 风险与回滚

- 非阻塞观察：可后续补新设置接口独立的只读权限拒绝、跨项目和模拟乐观锁冲突专项；当前共用授权路径、既有锁证据和真实角色闭环充分。
- 非目标：硬上限、供货周期、预测、自动下单、供应商策略、跨仓调拨和全量补货工作台。
- 回滚：回退应用接口与前端联动后移除 V144 新列；不改写 V142 或历史 migration。

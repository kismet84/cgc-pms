# ISSUE-053-003 Clean-room V2 八域应用壳与权限导航验收报告

## 结论

- 验收结论：通过。
- 阻塞项：0。
- 上线边界：仅完成本地 Clean-room V2 M1 应用壳切片；未发布生产、未切换正式入口、未修改 Legacy、后端、数据库、权限码或业务事实。
- 后续队列：下一串行 Ready 为 `ISSUE-053-004`；本轮新增后续项 0、关闭后续项 0、后续项净变化 0。

## 实施范围

- 新建 V2 八域导航目录：8 个一级业务域、30 个工作区、57 个独立 Tab 路径，沿用第50条主线的信息架构、URL 与权限码。
- 新建响应式应用壳：桌面固定侧栏、紧凑图标栏、移动抽屉、顶部项目/报告期控制、账户区、工作区标题和 Tab。
- 新建工作区状态：项目、报告期和项目/合同/结算对象上下文仅来自当前路由与后续权威选项；未知值、空选项、刷新和退出均 fail-close，不持久化陈旧上下文。
- V2 路由守卫按精确权限码或 `*` 通配权限过滤菜单、Tab 与深链；未登录进入登录页，无权路由进入受保护拒绝页。
- 壳层页面明确标记“业务页面尚未迁移”，不加载业务 API、不展示模拟经营数字、不把占位页登记成已迁移业务页。

## 权限与上下文复核

- 源码扫描未发现 `ADMIN`、`SUPER_ADMIN` 或角色名分支参与 V2 壳层可达性；系统管理等工作区仍使用现有 `system:*`、`workflow:*`、`audit:query` 权限码。
- 真实本地 ADMIN 会话显示八个一级业务域；模拟普通用户仅显示无权限要求的报表中心与其持有 `project:query` 的项目履约域。
- 模拟普通用户直达 `/contract/ledger` 被阻断；未登录直达保护路由返回登录；零业务权限用户只保留 Legacy 本就无权限码的报表中心。
- `/project/42/overview?projectId=unknown&period=2026-07` 可恢复对象上下文，但未知项目和报告期不成为有效选项；刷新结果一致。
- 退出调用既有会话缓存清理器，项目、报告期和对象上下文全部清空。

## 自动化证据

- Ready lint：通过，内容哈希 `7d746255bbf8c23933a5833cc644e284056295a177923585b6f7244d6d899b9e`。
- `pnpm test:unit`：9 文件、34 项通过。
- `pnpm type-check`：通过。
- `pnpm lint:check`：通过，0 error、0 warning。
- `pnpm check:boundary`：通过，V2 与共享契约无 Legacy UI 越界。
- `pnpm check:route-ledger`：通过，87 个 Legacy 命名路由、73 个视图引用、65 个独立视图无漂移。
- `pnpm build`：通过，81 个模块完成生产构建。
- 模拟 Edge 权限专项：4 项通过，覆盖未登录、ADMIN 登录、普通有权/无权深链和零权限样本。
- 本地实时 Edge 专项：5 项通过，覆盖未登录、ADMIN 会话恢复/退出、未知身份、登录响应式及三视口应用壳。
- `git diff --check`：通过。

## 浏览器与视觉核对

- 视觉源：用户选定的新版经营驾驶舱截图；未生成第二套视觉方向。
- 信息架构：左侧八域导航、顶部项目/报告期/账户区、内容工作区与 Tab 层级与选定基线一致。
- 色彩与表面：继续使用高明度浅蓝画布、白色导航/内容表面、品牌蓝激活态、细边框和轻阴影。
- 密度与排版：桌面保持紧凑导航、短标题和高信息密度；占位内容只表达迁移边界与上下文状态。
- 响应式：1440 展开域与当前工作区，1024 收敛为图标栏，390 使用可关闭抽屉；三视口均无横向溢出或遮挡。
- 交互状态：当前域、工作区和 Tab 有独立激活态；空项目/报告期为禁用控件，移动抽屉具备遮罩和明确关闭入口。
- 文案差异：视觉基线中的真实经营指标、待办、搜索和通知数据未复制；替换为“业务页面尚未迁移”与空上下文说明，避免空壳冒充 M2 业务交付。
- 有意偏差：1024 使用只显示域标识的紧凑栏，390 使用抽屉；原因是保障内容宽度和 44px 触控目标，不改变八域信息架构。
- 保真度裁决：应用壳结构、品牌层级、导航节奏与响应式方向达到高保真；真实驾驶舱内容保真不在本 Issue 范围，留待 M2。
- 控制台：应用 warning/error 与 pageerror 为 0；浏览器默认 favicon 404 为既有无价值噪声，已按明确规则过滤，不创建 backlog。

## 路由迁移台账裁决

- `docs/ui-v2/route-migration-ledger.md` 继续由 Legacy 路由源自动生成并通过漂移检查。
- 本 Issue 只建立承载壳和占位路由，不交付真实业务视图，因此 86 个 `LEGACY_ONLY` 与驾驶舱 `V2_SOURCE_AVAILABLE` 状态保持准确；未提前写成 `V2_ACCEPTED`。

## 失败分类与修复

- 初始空白页：`environment_prerequisite`，Vite 依赖优化缓存引用已失效的共享契约文件；重建 V2 开发容器并完成后端/前端 180 秒稳定门后恢复。
- 紧凑栏文字碎片：`quality_or_security` 中的真实响应式缺陷；隐藏紧凑模式工作区明细并复验三视口。
- Vite 样式未刷新：`environment_prerequisite`，Windows 挂载文件已更新但容器仍返回旧 transform；仅重启 V2 容器并完成 180 秒稳定门后复验。
- 首轮 E2E 红灯：`ready_issue_config`，userinfo fixture 多包了一层 `userInfo` 且标题选择器不唯一；按真实契约修正后通过。
- `demo.manager` 实时登录失败：`environment_prerequisite`，当前 dev `sys_user` 只存在 admin，接口返回 `USER_NOT_FOUND`；未为验收写入用户数据，普通有权/无权改由受控浏览器 fixture 验证。
- favicon 404：环境静态噪声，与应用壳目标无关，关闭且不制造后续项。

## 回滚

- 回退 V2 壳、导航目录、工作区状态、壳层路由、测试和治理回写即可。
- 保留 `ISSUE-053-001` 设计系统与 `ISSUE-053-002` 安全会话；Legacy 正式入口、后端与数据库无需回滚。

# ISSUE-053-018：M4合同台账与全生命周期V2验收报告

> 2026-07-22 用户复验修订：合同台账不再直出英文类型/状态枚举，移除面向实现的说明文字；分页改为“共N条—上一页—第N页—下一页”并右对齐。浏览器计算样式复验：页面标题`21px/700`、区块标题`15px/600`、分页`12px`。

> 2026-07-22 列表复验修订：合同卡片改为服务端分页表格；参照 Advanced Tables v5 Preset Views 增加“全部/草稿/审批中/履约中/已结算/已终止”开发者预设视图。切换视图清除旧搜索与筛选、写入可见 URL 查询条件并重新请求服务端，不做当前页伪排序。单测`177/177`、合同 E2E `2/2`及 live 浏览器交互通过。

> 2026-07-23 布局批注修订：删除顶部标题/筛选卡；“新建合同”移入合同列表标题栏；五项KPI合并为单卡片五列。浏览器复验搜索控件`0`、KPI单元`5`、五列等宽、新建按钮归属列表标题栏、页面横向溢出`false`、控制台错误`0`。

日期：2026-07-22

结论：**通过**。阻塞项：**0**。五条合同路由达到真实`V2_ACCEPTED`，台账为`63/24/0`。本结论不授权019～023、提交、push、PR、正式入口切换、Legacy退役或生产发布。

## 1. 实施范围

- `/contract`保留query/hash确定性重定向；台账、新建、详情和编辑四态由`ContractPage.vue`承接。
- 页面覆盖服务端分页、KPI、筛选、合同头、清单、付款条款、审批历史、复合原子保存、提交和删除；权威金额继续使用十进制字符串。
- 后端补齐子资源父合同项目范围、复合更新字段白名单、创建字段清洗、强制version/CAS、合同头与聚合金额/日期对账、generic提交状态门、重提业务权限及handler租户/状态条件。
- 复合保存继续以`contract:add/edit`作为聚合权限；独立子资源端点仍使用既有细粒度权限。附件、数据库、Legacy和其他M4路由未进入本轮。

## 2. 复核与根修

- 独立安全复核发现省略version可绕过CAS、旧写端点可绕过金额/日期校验，定级P1；本轮强制更新/提交version、限制generic合同提交、统一旧写入口头部校验并补负向测试。
- 安全复核P2指出合同重提仅受`workflow:resubmit`约束；本轮同时要求`contract:submit`，无权重提保持原状态。
- 独立测试复核指出Abort、陈旧响应、404/422、提交/删除失败和状态动作矩阵不足；本轮补齐列表/详情竞争、真实AbortError静默、404/422/500、写接口403 fail-close、失败无假成功、非草稿及缺权限动作隐藏测试。
- 用户复验发现`PROJECT_MANAGER`演示角色缺少`contract:query`，且只读台账无条件请求`partner:query`；已在幂等演示数据源补查询授权，并令合作方候选仅在新建/编辑模式加载。项目经理现可读取12条合同，写动作仍未扩大。
- 浏览器live验收使用本地5174运行态；管理身份加载12条真实合同，五路由无Shell占位，1440/1024/390三视口稳定后无横向溢出，控制台error为0。用户复验修正后保留`demo.manager`身份与`/v2/contract/ledger`页面。

## 3. 验证证据

| 验证 | 结果 |
| --- | --- |
| Ready lint | 通过；hash=`a75cc802aba45728f2258634b60594624dcb44812d89ec7e103f91499585b146` |
| 后端合同/工作流专项 | 6类107项通过；0失败、0错误 |
| `pnpm type-check:contracts` | 通过 |
| V2单测 | 28文件159项通过 |
| 完整演示项目验真 | 通过；`project_manager_contract_query=1` |
| `pnpm type-check` | 通过 |
| `pnpm lint:check` | 通过 |
| `pnpm check:boundary` | 通过；117个V2文件、13个契约文件 |
| 路由台账生成/校验 | 通过；87路由、73视图条目、65唯一视图，`63/24/0` |
| `pnpm build` | 通过；162模块 |
| Playwright目标E2E | 2/2通过；五路由、三视口、axe、权限fail-close |
| live浏览器 | 管理员与项目经理均读取12条真实合同；无权限提示；控制台error=0 |
| `git diff --check` | 通过 |

## 4. 范围与回滚

- 执行基线：`master`，初始HEAD `b6e46f9e37424f3f70889ccffb86bc2d662f6922`；当前为包含017与018的未提交工作区。
- 未修改数据库迁移、Legacy页面、部署、CI、正式入口或生产环境；仅向本地`cgc_pms_demo_v2`及其幂等数据源补`PROJECT_MANAGER -> contract:query`，并重启本地`cgc-pms-frontend-v2-dev`刷新代码挂载。
- 回滚只需回退合同V2页面/路由、共享契约/薄服务、精确后端根修、测试、五条台账状态及治理回写，并删除演示库角色菜单关系`520000000000008649`；无结构迁移回滚。

## 5. 零悬空收口

- 本轮修复并复验：启动核验全部后端缺口；安全复核2个P1与1个P2；测试复核2个P1与1个P2；用户发现的项目经理合同查询授权与只读多余请求。
- 超出范围并正式承接：0。
- 证据不足或无明确价值而关闭：付款条款实际日期早于计划日期的泛化限制建议无明确缺陷证据，保持现有实现，不新增backlog。
- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- 悬空项：0。

`ISSUE-053-019～023`是既定阶段计划，不是本轮新增发现；当前仍为Planned，须逐条补为Ready并获得明确实施授权。

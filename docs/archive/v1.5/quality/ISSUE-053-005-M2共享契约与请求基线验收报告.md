# ISSUE-053-005 M2 共享契约与请求基线验收报告

## 结论

- 验收结论：通过；M2 本地实施金丝雀放行 `ISSUE-053-006`。
- 阻塞项：0。
- 上线边界：仅本地 Clean-room V2 请求底座；未发布、未切换正式入口，未修改后端、数据库、Legacy、权限码或业务数据。
- 后续项：新增 0、关闭 0、净变化 0；本轮发现均已修复或有依据关闭，无悬空问题。

## 实施范围

- 共享契约补齐八角色 Dashboard DTO、权限码、端点映射、分页结果和项目摘要；稳定字段逐项核对当前 Java Controller/VO。
- 新增 Dashboard 与项目薄服务，只负责 URL、query、method 与 AbortSignal；无权角色在发请求前拒绝，项目级角色无项目时 fail-close。
- `period=YYYY-MM` 只向支持月份的五类角色发出 `month`；非法值清空，管理层端点不携带项目或月份。
- workspace 仅在 `ADMIN`、`SUPER_ADMIN`、通配或 `project:query` 权限下加载 `/projects`；分页合并仍服从后端租户与数据范围，不在前端扩大范围。
- 项目加载、角色切换和清理使用 AbortController 与 generation 隔离；取消请求不触发网络错误通知，陈旧响应不能覆盖最新状态。

## 自动化证据

- `ready-lint.ps1 -IssueTitle ISSUE-053-005`：Ready 状态下通过。
- `pnpm run type-check:contracts`：通过。
- `pnpm run test:unit`：12 文件、47 项通过。
- `pnpm run type-check`：通过。
- `pnpm run lint:check`：通过。
- `pnpm run check:boundary`：通过，扫描 63 个 V2 文件与 5 个共享契约文件。
- `pnpm run build`：生产构建通过；主 V2 chunk 52.61 kB，gzip 17.96 kB。
- `TEST_JWT_SECRET=<test-only-32-byte-value> .\\mvnw.cmd "-Dtest=DashboardControllerTest,PmProjectControllerTest" test`：39 项通过，0 失败、0 错误。
- 首次后端测试因当前测试环境 JWT 密钥仅 144 位而启动失败，分类为 `environment_prerequisite`；按 `application-local.yml` 的 `TEST_JWT_SECRET` 契约注入一次性测试密钥后通过，未修改代码或配置。
- `git diff --check`：通过。

## 权限与数据范围裁决

| 场景 | 行为 | 结论 |
| --- | --- | --- |
| 无角色权限 | 前端拒绝且 fetch 次数为 0 | 通过 |
| 项目级角色无项目 | 不请求 Dashboard，不回退租户全量 | 通过 |
| 管理层角色 | 只请求 management，不传 project/month | 通过 |
| 非法报告期 | 不传 month | 通过 |
| 支持月份角色 | 合法 `YYYY-MM` 原样传递 | 通过 |
| 项目列表 | 分页读取后端 `/projects` 返回记录 | 通过 |
| 陈旧项目响应 | Abort + generation 双重隔离，不回写 | 通过 |

## 发现与处置

- 后端目标测试初次失败：确认根因是本机测试密钥强度不足，本轮按测试环境契约修复运行前置并复验。
- `/projects` 的具体 SELF/DEPT/CUSTOM 过滤仍由现有后端数据范围实现决定；本 Issue 不复制、不放宽、不绕过后端过滤，因此无新增数据越权路径。
- 未发现需转后续的独立缺口；真实页面、视觉与角色组合属于已登记 `ISSUE-053-006`，不是本轮悬空项。

## 回滚

- 回退 M2 共享契约、Dashboard/项目薄服务、workspace 小范围接线和目标测试。
- M1 公共壳、安全会话与 Legacy 正式入口保持不变；无迁移或业务数据恢复要求。

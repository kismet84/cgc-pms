# ISSUE-040-037 投标成本受控修改入口与状态租户边界验收报告

结论：通过；阻塞：无。

## 范围与结果

- 投标成本页为 BIDDING 记录增加桌面与移动编辑入口，仅对 `bid:edit` 或 ADMIN/SUPER_ADMIN 可见；表单只提交 trim 后名称和可选备注。
- 后端改用专用更新 DTO，路径 ID 为唯一目标，客户端提交的 tenantId、projectId、bidStatus、金额及审计字段不能进入更新实体。
- V152 的 MySQL/H2 迁移只注册 `bid:edit` 并绑定既有 SUPER_ADMIN、ADMIN、COST_MANAGER；审查中将既有中标/失标端点解耦为 `bid:status`，避免编辑权限扩大为状态迁移权限。

## 验收证据

- Ready lint、`git diff --check`、迁移静态核对通过。
- 后端：`BidCostControllerTest` 22项通过，覆盖401/403、`bid:edit` 合法修改、输入校验、跨租户/不存在统一隐藏、非 BIDDING 拒绝、恶意字段不可覆盖，以及 `bid:edit` 不能调用中标/失标。
- 实际 SQL 证据：受控修改仅更新 `bid_project_name`、`remark` 及框架审计字段，不写 tenant_id、project_id 或 bid_status。
- 前端：API 与页面专项2个文件共8项通过；`vue-tsc --noEmit`、目标 ESLint 通过。
- 运行态：8080 health、5173 与 dev-login 可达；浏览器进入 `/bid-cost`，编辑入口唯一，弹窗回填“投标测试项目”并点击取消；后端运行日志投标 PUT 计数前后均为0，本轮控制台新增 error/warn=0。
- 审查：首次识别状态端点复用 `bid:edit` 的当前 diff 权限扩散风险；本轮改为 `bid:status` 并补测试后复验通过，最终 findings=无。

## 治理收口

- 唯一台账移除 `A-01-BID-UPDATE`，A-01 更新为有用户入口242、前端调用但无独立页面58、内部/集成/运维4、需补入口6、待废弃0、需要确认11，共321。
- 既有 `A-01-BID-WON`、`A-01-BID-LOST` 的验收权限同步校正为 `bid:status`，属于现有条目事实更新，不新增后续项。
- 新增后续项：0；关闭后续项：1；后续项净变化：-1。
- 最小回滚：回退 V152、更新 DTO、编辑 API/页面、状态权限解耦、专项测试与治理回写；浏览器未写业务数据，无数据回滚。

剩余风险：浏览器只验证管理员打开与取消路径，真实成功、失败、租户和状态边界由后端集成测试覆盖；删除与状态动作仍由既有唯一条目承接，不属于本轮新增悬空问题。

# 第55条主线支线：业务编码自动编号治理验收报告

验收日期：2026-07-28
稳定键：`BUSINESS-CODE-AUTOMATION-GOVERNANCE`
结论：`通过 / SAME_HEAD_CI_PASS`

## 1. 验收范围

- 22 项内部业务编码：服务端生成、客户端输入不作为事实、更新不改码。
- 8 项人工语义字段：供应商报价、图纸、合同清单、预算版本、目标成本版本、图纸版本、WBS任务、计量期间保持人工输入。
- 格式：`前缀-yyyyMMdd-三位序号`。
- 唯一粒度复用现有数据库约束；仓库通过 MySQL/H2 `V235` 新增租户+项目唯一约束。
- 查询包含软删历史，编号不复用；唯一冲突最多重试 3 次。

## 2. 实施结果

| 业务域 | 自动编码 |
| --- | --- |
| 库存/供应商 | `WH/SRC/SRT` |
| 进度 | `SCH/SPD/SCA` |
| 质量 | `QPL/QIN/QCO` |
| 技术 | `TSC/TRV/RFI/TDS/TAR` |
| 收尾 | `PC/SA/FA/WAR/DEF/ATR` |
| 成本 | `CFT/CCA` |

共享实现为 `BusinessCodeGenerator` 编译期规则白名单；业务服务负责事务、写后回读和冲突重试。V2 创建页不再要求人工填写上述22项，接口契约允许省略编码字段。

## 3. 验证证据

- 后端清理重编译：`mvnw.cmd -q clean -DskipTests compile`，通过。
- 共享及分域：11个测试类55项，通过，0失败、0错误；覆盖22项生成回读、软删不复用、租户/项目隔离、999耗尽、唯一冲突换号重试、仓库数据范围及不可篡改字段。
- H2 Flyway兼容：3项，通过；验证新建、升级均到`V235`及仓库唯一约束。
- 后端本地合计：12个测试类58项，通过，0失败、0错误。
- 一次性MySQL：`FlywayMySqlSmokeTest`、`BaselineMySqlSmokeTest`、`BaselineMySqlUpgradeTest`各1项，通过；验证新建、基线及升级均到`V235`。
- V2 unit：55个测试文件、398项，通过。
- `pnpm type-check:contracts`：通过。
- `pnpm type-check`：通过。
- 目标 Vue ESLint：通过。
- `pnpm build`：通过。
- V2边界、路由台账、设计系统、完整ESLint及包体检查：通过。
- V2迁移浏览器门禁：91项通过、1项条件跳过；仓库创建请求不含编码并回读服务端生成的`WH-002`。
- 推送同SHA CI：`ba1e1175fd69926d2360fe2cb5a3bcf9df0f807c`，run `30320636917`，13个必需作业全部成功，`verify-pre-pr-ci.ps1`判定`PASS`。
- `git diff --check`：通过。

首次目标集成测试因并行 Maven 共享 `backend/target` 读取到半写入 class 失败，分类为 `environment_prerequisite`；清理可重建产物并串行复跑后关闭。首轮远端`frontend-v2-gate`暴露仓库浏览器用例仍尝试填写已禁用编码，分类为`quality_or_security`；用例改为验证客户端不提交编码并回读服务端值，本地完整迁移门禁复验关闭。

## 4. 裁决与回滚

- 功能验收：通过。
- 功能阻塞项：0；Git交付门：同SHA CI已通过。
- 未实施发布/部署：不在本任务范围。
- 回滚：回退业务服务、DTO/契约和V2页面改动；`V235` 若已应用不得修改历史迁移，应新增后续迁移撤销约束。

## 5. 零悬空

- 新增后续项：0。
- 关闭后续项：1，`BUSINESS-CODE-AUTOMATION-GOVERNANCE`。
- 后续项净变化：`-1`。
- 无未承接遗留项。

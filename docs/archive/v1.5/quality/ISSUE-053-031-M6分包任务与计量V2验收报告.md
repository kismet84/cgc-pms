# ISSUE-053-031 M6分包任务与计量V2验收报告

## 1. 裁决

- 结论：通过。代码根因、绑定测试、全门、最终运行态与独立终审均通过。
- 范围：分包根路由、任务与计量Clean-room V2；任务/计量CRUD、计量清单、附件、提交审批，以及必要的服务端范围、金额、并发和原子性根修。
- 基线：`master`，`d5b93242fa64b429f4658cb48d5c4090e1e59578`；保留工作区既有未提交成果。
- 路由：`/subcontract`受控重定向到`/subcontract/task`；任务与计量页真实V2承接；台账达到`LEGACY_ONLY=37 / V2_ACCEPTED=50 / V2_SOURCE_AVAILABLE=0`。
- Git/环境：未commit、push、PR、切正式入口、退役Legacy、连接生产或执行生产写入。

## 2. 实施结果

- 扩展分包稳定契约与V2薄服务，新增任务/计量工作区；复用既有请求核心、公共壳、项目/合同候选、合同清单及业务附件能力。
- 任务覆盖列表、详情、新增、编辑、删除；计量覆盖列表、详情、新增、编辑、删除、清单维护、附件与提交。
- 新增/编辑/清单/附件/提交成功后重新读取服务端任务、计量、明细、金额、状态和附件；Abort与generation隔离快速筛选和陈旧响应。
- 项目、合同、合作方、关联任务和合同清单候选来自服务端范围；原始主键不作为可见主信息，业务编号进入详情，枚举显示中文。

## 3. 服务端权威与安全根修

- `SubTaskService`与`SubMeasureService`校验当前租户、项目访问、分包合同、合同合作方和关联任务；已被计量引用的任务上下文冻结，引用任务不可删除。
- 计量创建/更新忽略客户端金额、审批状态和业务编号；清单保存按稳定ID顺序锁定合同清单项，由服务端合同数量、单价、已批准累计量重算本期、累计和金额字符串。
- 提交前重新锁定并校验当前合同清单事实；陈旧单价、数量、金额或累计值失败关闭。提交与工作流回调使用租户+状态CAS；审批通过后才生成成本。
- `SUBCONTRACT`附件读取使用`subcontract:measure:query`，直接上传/删除使用`subcontract:measure:edit`；删除计量时，父对象`measure:delete`授权完成后按租户和精确业务绑定级联删除附件，不错误追加`measure:edit`。
- 删除计量在同一事务内清理明细、附件元数据和主记录；对象存储可用时复用`FileService`清理物理文件，禁用时保持本地测试可启动。

## 4. 验证证据

| 验证 | 结果 |
| --- | --- |
| Ready lint | 启动前通过，hash=`0250cfab05b05ff6a5d296bf2d45fa009ce612fe1a264b146f20ed3b40fe6721`；最终范围、命令与实施结果在转Done前通过，hash=`ff2adef3112bb2a5170a0cbd0f6ccf9aa5c1863001cd878e0b26f6fff4ad4db3` |
| 后端专项 | 7类、105项；失败0、错误0、跳过0 |
| 后端权限补充 | 3类、23项；失败0、错误0、跳过0 |
| 后端构建 | 停止本地backend后，隔离Docker容器`mvn -DskipTests package`生成当前fat JAR；重启后健康 |
| V2定向 / 全量单测 | 目标文件3项、全量45文件334项通过；受影响设计/路由门4文件82项通过 |
| 契约类型 / V2类型 | 通过 |
| ESLint | 通过，0错误、0警告 |
| Clean-room边界 | 164个V2文件、16个契约文件通过 |
| 路由台账 | 87路由、73视图、65唯一视图；`37/50/0` |
| V2构建 | 通过 |
| 目标Playwright | 连续两轮10项通过；缺权限零业务流量、受控重定向、任务写后读取、计量清单/附件/提交单次写及读取失败警示成立 |
| 三视口 / axe / console | `1440×900`、`1024×768`、`390×844`无页面横向溢出；axe serious/critical 0；console error/warn 0 |
| 真实运行态 | 当前fat JAR启动后backend与frontend-v2均healthy；`http://localhost:8080/api/actuator/health`=`UP`；V2实际落点`http://localhost:5174/v2/subcontract/measure` |
| `git diff --check` | 通过；仅既有M5文件行尾转换提示，无空白错误 |
| 独立复核 | 通过；阻塞0，五项根因及绑定测试、当前运行态、范围和治理一致性均确认 |

浏览器输出目录：`C:\Users\summade87114\AppData\Local\Temp\cgc-pms-issue-053-031`。应用内浏览器完成真实数据、任务→计量、详情弹窗和console验收；其截图接口连续超时，按既有技能允许回退到本地Playwright生成三视口截图。

## 5. 失败分类与收口

- `tool_config`：宿主无`mvn`；改用仓库既有Maven Docker运行。
- `environment_prerequisite`：复用dev容器测试时MySQL环境覆盖H2驱动；改用隔离Maven容器。
- `quality_or_security`：最小存储配置下`FileService`条件Bean缺失导致上下文失败；改为可选注入并保持生产物理文件清理。
- `quality_or_security`：旧计量Controller夹具的主表金额与明细不一致，被新增金额完整性门正确拒绝；修正夹具后专项通过。
- `tool_config`：首次前端命令误在仓库根目录执行，无`package.json`且未改文件；切换`frontend-admin-v2`后全门通过。
- `environment_prerequisite`：backend重启25秒时仍在启动；再等待20秒后健康。
- `test_or_fixture`：Playwright在弹窗入场动画期间点击自定义下拉，元素不稳定；测试等待`transform:none`后交互，并收紧双状态断言，连续两轮通过。
- `test_or_fixture`：新任务上下文门禁暴露旧前置任务夹具缺少同合同上下文，且事务测试清理顺序违反自引用外键；修正夹具与先后删除顺序后105项通过。
- `tool_config`：`test:unit`脚本已固定`tests/unit`，追加裸关键字误纳同名E2E；Ready命令改为精确Vitest文件，全量单测另行通过。
- `quality_or_security`：全量V2门禁发现页面级项目选择器、原生确认框、详情表可访问区域和内部实现文案；复用公共工作区与`V2ConfirmDialog`并补齐设计契约，82项受影响门禁与334项全量单测通过。
- `environment_prerequisite`：Windows绑定挂载锁定运行中JAR导致首次repackage失败；停止backend后用一次性Maven容器重打fat JAR，恢复backend和frontend-v2 healthy。
- `environment_prerequisite`：Docker内Vite未接收Windows文件事件，首次E2E命中旧页面；重启精确`frontend-v2`服务后两轮10项通过。
- 本轮修复并复验：上述全部实现、权限、范围、金额、CAS、附件、原子性及测试稳定性问题。
- 超出范围并正式承接：0；证据不足或无明确价值而关闭：无需新增数据库migration、客户端账本、第二请求层、状态库、表格库或通用财务框架。
- 新增后续项0、关闭后续项0、后续项净变化0；悬空项0。

## 6. 回滚

回退031契约/薄服务/页面、分包与附件最小后端根修、路由/导航/台账、测试及本报告/治理回写即可。无数据库migration、生产、正式入口或Legacy影响。

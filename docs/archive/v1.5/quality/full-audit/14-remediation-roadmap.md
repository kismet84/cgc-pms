# 全量审计整改路线图

## R0：生产门禁（P0，串行）

1. `SEC-001`：识别目标环境与 Secret 载体；新凭据生效、旧凭据失效；双人脱敏复核；验证失败立即回滚。
2. `FILE-001`：冻结对象清单；逐对象真实 AV 复扫；失败项隔离；输出对象级结果与审计记录。
3. `REL-001`：绑定同一制品 SHA；生产等价 Flyway；真实八角色与跨租户/项目负例；金额/文件；备份恢复；健康与回滚演练。

验收：三项全部通过才能从“禁止上线”重新裁决。任何一项失败即停止，不做部分放行。

## R1：权威载体修正（P2，1 个小提交）

- 更新 `blocked-issues.md`：关闭过期 `ISSUE-037-021` 当前阻塞描述，引用 PR #358 与 post-merge run。
- 更新 `project-map.md` 的 CI 状态。
- 更新 README 至 B215 + V216—V218，并补 V2 5174 入口。

验收：文件间 SHA、CI、迁移版本一致；无重复 Issue。

## R2：质量与观测（P2，可拆两项）

- Prometheus 建立机器认证或受控内部入口，Secret 外置，加入真实 scrape 测试。
- 前端中央错误上报完成脱敏、版本/环境标签、source map、采样与告警闭环。
- 按租户拒绝、金额边界、状态机异常、补偿回滚提升分支覆盖，并设不低于当前值的防回退门限。

## R3：维护债务（P3）

- 清理 25 个 Legacy lint warning。
- 小步拆分 `PayApplicationService`，保持 API、事务与审计语义不变。
- 替换废弃 API/测试注解；修正 dev MySQL 配置文件权限。

## 继续迁移（P1）

- `BIZ-001`：按业务价值逐切片迁移，禁止宣布 V2 全量完成。
- `DATA-001`：只在生产等价副本与外部消费者签认后单独退役历史表。

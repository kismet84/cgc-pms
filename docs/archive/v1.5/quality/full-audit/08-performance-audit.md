# 全量审计：性能

## 结论

**基本通过。评分 78/100。** 当前自动化未显示性能回退，但生产容量证据缺失。

## 证据

- Dashboard 性能测试纳入后端全量验证；既有指标记录 SQL 数 17，处于门限内。
- 前端两个应用 bundle-size gate 均通过；V2 最大 JS 111.33 KiB，Legacy 最大 457.14 KiB。
- 生产 Hikari `maximum-pool-size=50`、连接超时 30 秒、max lifetime 可配置。
- 列表与驾驶舱 API 使用项目范围、月份与分页/有限列表约束，未发现本轮新增全表暴露路径。

## 限制

- 未识别目标环境、生产数据量、SLO 与流量模型，故未执行负载、峰值、慢 SQL、缓存命中及容量测试。
- `REL-001` 关闭时必须补目标环境基线：P95/P99、错误率、连接池、锁等待、GC、CPU/内存与关键 SQL。

# 第55条主线支线：资金支出闭环S0标准冻结验收报告

## 1. 裁决

- 日期：2026-07-26。
- 事实基线：`master@6b134b423a3f3f770b8a4e45cb1456ce3484ea91`；数据库基线B215，增量V216—V223。
- S0裁决：`通过`。
- 业务闭环裁决：`P0_BLOCKED`；`PPCL-01`—`PPCL-11` 尚未实施。
- 生产裁决：`不通过、禁止上线`。
- 授权范围：仅标准、事实矩阵、阶段范围、退出门和治理回写；未修改业务代码、migration、数据库或运行环境，未执行Git分支、commit、push或PR操作。

## 2. 冻结结果

### 2.1 唯一载体

- 全局父项仅 `PROJECT-PAYMENT-CLOSED-LOOP`。
- `PPCL-01`—`PPCL-11` 仅为父项内部验收子键，不复制进 `current-issues.json`。
- `current-issues.json` 保持14项；第55条活动事项保持18项。
- 历史 `P0-1`—`P0-11` 只映射到当前11个子键，不重复立项。
- 历史 `PROD-GATE-001` 映射 `REL-TARGET-SHA-REVALIDATION`；`PROD-GATE-002` 因真实外部平台未选定且超出本支线范围，不进入当前执行队列。

### 2.2 13节点事实

| 节点 | S0裁决 |
| --- | --- |
| Project | 核心实现 |
| ProjectBudget | 部分实现；合同分配入口和消费时点未闭合 |
| Contract | 部分实现；创建/提交前置与删除保护不足 |
| Expense | 核心实现；提交时已占用预算 |
| Settlement | 核心实现 |
| PaymentApplication | 部分实现；DIRECT权限和重提同步不足 |
| Approval | 通用重提已实现；PAY_REQUEST业务同步缺口 |
| PayRecord | 核心事实较强；当前过早消费预算 |
| CashJournal | 部分实现；归档未消费预算，附件关系不足 |
| Invoice | 分配已实现；已分配PENDING仍可删除 |
| Voucher | 自动生成DRAFT；缺现金归档后过账门 |
| Dashboard | 已实现但执行率、现金流、利润口径不正确 |
| Trace | 仅三类入口，逐跳租户/项目校验不足 |

### 2.3 金额与状态

| 事件 | EXPENSE来源 | SETTLEMENT、SUB_MEASURE、DIRECT来源 |
| --- | --- | --- |
| 付款申请提交 | 只冻结可转付款额，不二次占用 | PAY_REQUEST占用 |
| 驳回/撤回 | 不释放底层费用预算 | 释放本轮PAY_REQUEST占用 |
| 重提 | 重新冻结可转付款额 | 新round重新占用一次 |
| PayRecord SUCCESS | 更新实付事实，预算仍保持占用 | 更新实付事实，预算仍保持占用 |
| CashJournal ARCHIVED | 一次性转为消耗 | 一次性转为消耗 |

归档前取消、归档后冲销、CashJournal REOPEN及已过账凭证处理必须在S2按既有 `PPCL-03` 闭合，禁止手工改账。

### 2.4 关系

采用最小 `payment_document_link`：

`id, tenant_id, cash_journal_id, file_id, document_type, created_by, created_at`

`cash_journal_id` 与 `file_id` 均非空并限制删除；同租户文件唯一。付款经CashJournal唯一PayRecord反查，发票继续使用现有 `invoice_payment_allocation`。不新增三个可空外键或第二套多态附件关系。

## 3. 11个子键退出门

| 子键 | 阶段 | S0冻结目标 |
| --- | --- | --- |
| `PPCL-01` | S1 | PAY_REQUEST原实例新round重提、状态与来源型预算同步 |
| `PPCL-02` | S2 | 合同预算日常入口、分配守恒和付款双重门 |
| `PPCL-03` | S2 | SUCCESS不消费；归档消费；取消、重开、冲销和凭证门 |
| `PPCL-04` | S1 | DIRECT独立服务端权限、原因和审计 |
| `PPCL-05` | S1 | 合同前置、writeback二次门与财务事实删除保护 |
| `PPCL-06` | S3 | 已分配或已核验发票不可删除 |
| `PPCL-07` | S3 | 付款证据类型、CLEAN、同租户、同日记和不可变 |
| `PPCL-08` | S3 | 仅补残余关系、同租户一致性和迁移预检 |
| `PPCL-09` | S4 | Dashboard固定样本与权威事实对账 |
| `PPCL-10` | S4 | 十类入口、逐跳校验和缺链显式失败 |
| `PPCL-11` | S4 | 真实后端FLOW-001、负向、并发、迁移、权限与同SHA CI |

## 4. 验证

- 两路独立只读复核覆盖13节点、金额时点、权限、租户、关系与11个子键。
- 当前标准、支线计划、第55条总计划、Current Focus、项目地图及索引状态已对齐。
- 文档引用、稳定键守恒、JSON状态源、Markdown静态契约和 `git diff --check` 作为S0退出验证。
- 未运行后端、前端或浏览器测试；S0未修改运行代码，历史测试和脏工作区测试不作为当前同HEAD验收证据。

## 5. 零悬空与剩余风险

| 指标 | 数量 |
| --- | ---: |
| 本轮新增后续项 | 0 |
| 本轮关闭后续项 | 0 |
| 后续项净变化 | 0 |
| 重复项 | 0 |
| 无载体悬空项 | 0 |

剩余风险全部由既有 `PPCL-01`—`PPCL-11` 承接。S1需另行授权；数据库、Git、目标环境和生产分别需要独立授权。S0文档回滚只需恢复本轮文档差异，不涉及数据恢复。

# 第52-53条主线 DQ-005、DQ-009 应用闭环与三工作树整合验收报告

验收日期：2026-07-19
验收结论：**通过（本地集成范围）**
发布结论：**未授权提交、推送或生产发布，本报告不构成生产上线批准。**

## 1. 范围与边界

- 目标工作树：`D:\projects-test\cgc-pms`，分支 `codex/mainline-53-ui-v2-m0`。
- 来源工作树：`D:\projects-test\cgc-pms-demo-data-v2`（第52条演示数据 V2）与 `D:\projects-test\cgc-pms-dict-refactor`（第53条字典治理）。
- DQ-005：质量处罚、返工成本与合作方评价强制关联合同，并校验租户、项目和责任合作方关系。
- DQ-009：关闭范围限定为 V216 `SYSTEM_GOVERNED_CODES` 九类核心字典和正式工作流状态机；不把未来新增业务字段无限纳入本次承诺。
- 整合方式：以当前 UI V2 工作树承接三个工作树的文件并集；来源工作树保留，不删除、不重写，不执行 Git 提交或推送。

## 2. 实施结果

### 2.1 DQ-005

- 前端标签和表单校验改为关联合同必选。
- 合同候选同时满足当前项目、当前租户，并且责任合作方是合同甲方或乙方。
- DTO 使用非空约束；Service 拒绝空合同、跨项目、跨租户和非合同相对方。
- 集成测试覆盖合法合同、空合同、跨项目、跨租户、非合同相对方五类路径。
- V2 数据校验结果：`quality_contract_trace=1`、`quality_contract_trace_invalid=0`。

### 2.2 DQ-009

- V216 规范项目、合同、合作方、成本九类核心字典，禁止租户影子覆盖并补齐 `WRITE_OFF`。
- 普通已认证业务角色可读取权威字典；项目、合同、合作方写入调用后端有效值校验。
- 核心字典值禁止删除或停用，字典变更纳入操作审计；权威读取不使用进程内陈旧缓存。
- 项目审批状态使用 `approval_status`；项目类型、项目状态、合同类型、合同状态、合作方类型、成本类型/来源/状态统一消费权威定义。
- 合同双方按业务关系选择当前租户合作方，禁止甲乙方相同；不再使用不存在的 `PARTY_A/PARTY_B` 类型。
- V217 增加合同项目/合作方租户一致性复合外键与甲乙方不同约束。
- 工作流演示模板按业务类型和节点语义绑定；实例、任务、节点与审批模式均覆盖正式状态。

### 2.3 三工作树整合

- 三个工作树起点 HEAD 均为 `3bd76a9c82f6c1e7b2eca813784c4b74d77b88b8`。
- 字典工作树的 53 个变更文件已进入目标工作树；同名 `SysDictDataControllerTest` 保留目标工作树更新后的字典类型 ID 断言。
- 演示数据包、字段覆盖清单、V216/V217、Legacy UI 字典消费、Clean-room V2 隔离底座均可从目标工作树单独构建和运行。
- `deploy/docker-compose.dev.yml` 的后端 JDBC 数据库名改为跟随 `MYSQL_DATABASE`，字段覆盖可按文档切换到隔离库。

## 3. 验收证据

| 验收门 | 命令或入口 | 结果 |
| --- | --- | --- |
| 后端全量 | `backend\mvnw.cmd -q verify` | 249 套件、2044 项，0 failure、0 error、3 skip；JaCoCo 门通过 |
| 后端专项 | DQ-005、字典、V216/V217、合同引用专项测试 | 通过；H2 新库与 V180 升级链均到 V217 |
| Legacy 前端全量 | `pnpm test:unit` | 131 文件、732 项全部通过 |
| Legacy 类型和构建 | `pnpm type-check`、`pnpm build` | 通过；4641 模块完成生产构建 |
| Clean-room V2 | 类型检查、契约检查、单测、边界、路由台账、构建 | 4 文件 7 项通过；15 个 V2 文件与 4 个契约文件边界通过；87 路由台账通过；构建通过 |
| 演示业务数据 | `verify.ps1 -Environment demo -Database cgc_pms_demo_v2` | 通过；权威字典、金额、工作流、信用代码、质量合同追溯错误指标均为 0 |
| 表单字段覆盖 | `verify-coverage.ps1 ... -ApiBaseUrl http://127.0.0.1:8080/api` | 617 表单项、591 绑定字段、37 API 回读；166 目标表非空；374 外键无孤儿 |
| 运行态稳定门 | 180 秒稳定观察 | 后端与 V2 均 `healthy`、0 重启 |
| HTTP | `http://127.0.0.1:8080/api/actuator/health`、`http://127.0.0.1:5173/`、`http://127.0.0.1:5174/v2/health` | 全部 HTTP 200 |

## 4. 运行态迁移说明

本地 `cgc_pms_demo_v2` 曾于 2026-07-19 01:40 应用 V216 早期草稿，启动时出现 Flyway 校验和不一致。三工作树当前 V216 文件 SHA-256 完全一致。处理仅限 loopback MySQL、受保护的 demo 库，并且仅在旧校验和精确匹配时更新一行 Flyway 历史；随后由应用正常执行 V217。正式库未修改。当前 V216、V217 均为成功状态。

新建数据库会直接执行当前 V216/V217；H2 新库和历史升级链测试均已证明。若其他非本地数据库曾人工执行未冻结的 V216 草稿，必须单独核对，不得复制本次 demo 修复命令。

## 5. 回滚与剩余风险

- 应用回滚：按文件撤销本轮 DQ-005/DQ-009 与测试改动；不要修改已应用迁移文件。
- 数据库回滚：V217 为约束增强，不提供自动降级；需要回滚时必须先评估合同跨租户引用风险并使用新增迁移处理。
- 运行态回切：将 `MYSQL_DATABASE` 恢复为 `cgc_pms` 后重建 backend，并刷新 frontend 代理连接。
- 剩余风险：生产环境迁移、真实低权限角色浏览器矩阵和远端 CI 未在本轮执行，原因是未授权生产/推送；不影响本地集成通过，但仍是发布门禁。

## 6. 收口守恒

- 本轮修复并复验：DQ-005、DQ-009、3 个过期后端测试契约、1 个过期前端静态断言、H2 基线迁移夹具冲突、开发 Compose 切库缺口。
- 超出当前范围并正式承接：0。
- 证据不足或无明确价值而关闭：0。
- 新增后续项：0。
- 关闭后续项：2（DQ-005、DQ-009）。
- 后续项净变化：-2。

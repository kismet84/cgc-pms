# ISSUE-053-013 M3质量安全整改闭环验收报告

## 1. 结论

- 验收结论：通过。
- 阻塞状态：无。
- 上线结论：本地开发/演示验收通过；未切换正式入口，未提交、推送、创建PR或发布生产，不构成生产上线批准。
- 路由结果：`/quality-safety`为真实Clean-room V2页面；台账`LEGACY_ONLY=70 / V2_ACCEPTED=17 / V2_SOURCE_AVAILABLE=0`。
- 数据库：无迁移、无结构变更；补充内容仅为可幂等重放的dev/demo测试身份、权限、阶段状态和附件元数据。

## 2. 实现与安全边界

- 共享契约、薄服务和页面覆盖质量计划、检查、问题、整改、复检、后果、反向trace和阶段附件；写后重新读取后端权威状态。
- 查询、计划、检查、整改、复检、后果六类权限分离。查询账号无写按钮且页面访问零写请求，后端403保持最终边界。
- 修复附件授权根因：`RECTIFICATION_EVIDENCE`仅接受整改权限，`REINSPECTION_EVIDENCE`仅接受复检权限；检查/问题证据保持检查维护权限。
- 演示包新增六个分权账号、ACTIVE计划、DRAFT/SUBMITTED检查、RECTIFYING/PENDING_REINSPECTION/CLOSED问题、REJECTED/SUBMITTED整改及四类CLEAN附件；验真拒绝动作越权和缺失阶段。
- 非目标保持：未迁移技术管理/项目收尾，未新增依赖、数据库迁移、全局Store、Legacy UI复用、正式入口或生产操作。

## 3. 验证证据

| 层级 | 范围 | 结果 |
| --- | --- | --- |
| Ready | `ready-lint.ps1 ... ISSUE-053-013` | 通过 |
| 后端 | `QualitySafetyClosedLoopIntegrationTest` | 5项通过；含整改/复检附件交叉授权负向 |
| Demo | `load.ps1 -Environment demo -Database cgc_pms_demo_v2` | 通过；21阶段、6账号、5动作权限、0越权、6项目成员、6阶段行、4附件类型 |
| V2契约/类型 | `type-check:contracts`、`type-check` | 通过 |
| V2全量单测 | `test:unit` | 24文件119项通过 |
| 静态边界 | `lint:check`、`check:boundary`、`git diff --check` | 通过；102个V2文件、10个契约文件 |
| 路由/构建 | `check:route-ledger`、`build`、`check:bundle-size` | 通过；87路由，质量页面JS 26.83kB，16个JS资产满足预算 |
| Live E2E | `V2_LIVE_QUALITY=1 playwright test e2e/m3-quality-safety.spec.ts` | 7项通过；三视口无横向溢出，axe无serious/critical，六账号动作分离，查询账号零写请求 |

## 4. 失败分类与闭环

- 首轮live仍显示旧占位：源码与容器挂载正确，Vite持有旧路由模块；分类`environment_prerequisite`，刷新V2容器后进入真实页面。
- 真实数据触发页面500：计划卡片错误引用检查循环变量；分类`quality_or_security`，把检查证据动作移回检查卡片，专项及live回归通过。
- 动作角色读请求403：demo角色误用后端明确fail-close的`CUSTOM`范围；分类`test_data`，改为租户内`ALL`并保留项目成员事实，动作权限仍严格单一，越权计数0。
- 整改角色无正向动作：缺`RECTIFYING`阶段样本；分类`test_data`，补充代表性问题并把验真阶段行提高到6。
- Windows bind mount未拾取Vue修复：分类`environment_prerequisite`，第二次刷新V2容器后7项live全绿。未达到同问题3次修复停止条件。

## 5. 回滚与后续

- 最小回滚：单路由恢复`LEGACY_ONLY`，回退质量契约/服务/页面、路由生成器、专项测试、附件授权修复及`M3_DOMAIN_PERMISSION_DATA`演示阶段；不删除既有业务质量事实或附件。
- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- `ISSUE-053-014～016`是M3既定串行实施任务，不是本轮发现项；014须补为Ready后实施。

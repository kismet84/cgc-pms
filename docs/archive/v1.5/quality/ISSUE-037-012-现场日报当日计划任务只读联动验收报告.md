# ISSUE-037-012 现场日报当日计划任务只读联动验收报告

## 结论

- Decision：通过。
- 阻塞：无。
- 上线边界：代码与自动化测试通过；未执行真实浏览器验收，未发布、未 push。

## 实施边界

- 日报详情在既有项目访问校验后，只读查询同租户、同项目且计划日期闭区间覆盖日报日期的分包任务。
- 返回任务编号、名称、作业区域、计划开始/结束、状态和字符串进度。
- 不返回合同金额、供应商信息，不提供任务新建、编辑、删除或进度写入。
- 未新增表、migration、权限或排程能力。

## 验收证据

- Ready lint PASS；TDD RED 命中后端缺少 `plannedTasks`/查询依赖与前端缺少只读区域。
- 后端 `SiteDailyLogServiceTest,SiteDailyLogControllerTest`：8/8 通过；查询 SQL 约束覆盖 tenant/project、非空计划日期与闭区间参数。
- 前端日报专项：4/4 通过；`vue-tsc --noEmit` 通过。
- `git diff --check` 通过；仅有 Windows 行尾提示。
- E 审查：查询发生在日报项目访问校验之后，最小字段无合同/供应商敏感数据，且无任务写调用。

## 图谱检索证据

- CodeGraph：定位 `SubTask`、`SubTaskService`、日报详情调用与 blast radius；编辑期自动同步因文件锁停用，归类为 `tool_config`。
- `codebase-memory-mcp`：只读确认 `SubTask` 具有 tenant/project、计划日期、区域、状态与进度，且任务服务写边界独立。
- 当前源码由直接 diff、Java 编译、Spring/H2 控制器测试与前端类型检查交叉核验。

## 风险与回滚

- 非阻塞：尚未以真实角色/真实 WBS 数据做浏览器展示验收；自动化证据足以支持代码级通过。
- 完整计划平台、多前置、关键路径、资源负载仍为非目标，已保留在项目地图语义中。
- 回滚：移除日报计划任务 VO、查询和前端只读表格；无数据迁移。

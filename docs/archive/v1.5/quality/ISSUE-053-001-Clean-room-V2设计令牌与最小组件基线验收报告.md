# ISSUE-053-001 Clean-room V2 设计令牌与最小组件基线验收报告

日期：2026-07-19
结论：**通过**
阻塞项：0
非阻塞悬空项：0

## 1. 范围与结果

- 在 `frontend-admin-v2` 内建立集中设计令牌：颜色、排版、4px 基准间距、圆角、阴影、层级、动效、控件高度和响应式尺寸。
- 建立 Button、Input、Select、Card、Badge、Alert、Skeleton、Dialog、Stack、Cluster、Grid 最小无业务组件集及独立预览。
- 建立组件状态、键盘行为、布局原语和令牌集中性的单元测试。
- 未修改 Legacy、后端、数据库、服务、状态、路由、正式入口或共享 UI 依赖；未引入真实业务 API、写操作或模拟经营数据。

## 2. 视觉源与复核

唯一视觉源 SHA-256：`BA1A7CC8391868F255AC7208753BD03AD2157E37018AEBF182E573B3D30D783B`。稳定锚点为 `design-qa.md` 与 commit `4aaef899ad2624c36580745f7b69c900839831ab`。

| 比对项 | 结论 |
| --- | --- |
| 配色 | 真白面板、冷浅灰画布、深蓝文字、主蓝与红/橙/绿语义色一致 |
| 排版 | 中文无衬线、紧凑标题层级、次级说明与数字对齐规则一致 |
| 几何 | 轻边框、8px 主卡片圆角、低强度阴影和紧凑控件一致 |
| 间距 | 4px 基准及 8/12/16/24/32 常用层级一致，无散落核心视觉值 |
| 状态与响应式 | primary/secondary/text/danger、禁用、加载、错误、focus 明确；390px 单列且无横向溢出 |

视觉源不含需复用的业务位图，因此未生成或引入图片资产。

## 3. 自动化门禁

| 门禁 | 结果 |
| --- | --- |
| Ready lint | 通过；Issue 哈希 `b3f34f85983dd5fa1cb1291c7a83e6472a8db1dae4c3693ef3254e1e4fecc376` |
| `pnpm test:unit` | 通过；5 文件、14 项测试 |
| `pnpm type-check` | 通过 |
| `pnpm lint:check` | 通过 |
| `pnpm check:boundary` | 通过；33 个 V2 文件、4 个契约文件扫描 |
| `pnpm build` | 通过；32 个模块转换 |
| `git diff --check` | 通过 |

## 4. 运行态与交互验收

- 访问 URL：`http://localhost:5174/v2/src/components/preview/index.html`。
- 后端健康：`http://localhost:8080/api/actuator/health` 返回 200；V2 预览返回 200。
- 1440、1024、390 三视口均无横向溢出；390px 下 13 个可见表单/按钮控件高度均为 44px。
- Dialog 打开后焦点进入 `role=dialog` 面板；Escape 关闭后焦点恢复到触发按钮。
- 控制台 warning/error 为 0。
- 初次预览命中 Vite 旧 CSS 转换缓存；仅重启本地 `cgc-pms-frontend-v2-dev` 后当前令牌生效。分类：环境前置类，非代码质量缺陷。

## 5. Reviewer 结论

- 令牌来自已选视觉源，组件范围满足 M1 后续登录与应用壳复用需求，无过度抽象。
- 无 Legacy import、无业务功能偷跑、无正式入口变更。
- 组件 API 保留原生语义和可访问名称；禁用、加载、错误、键盘焦点及减少动效有明确基线。
- M2 真实驾驶舱、认证会话、权限导航、403/404 和完整无障碍退出门均未提前实施。

## 6. 问题处置与收口

- 本轮修复并复验：Vite 旧转换缓存导致预览未加载当前 CSS；刷新 V2 本地运行态后复验通过。
- 超出当前范围并正式承接：0。
- 证据不足或无明确价值而关闭：0。
- 新增后续项：0。
- 关闭后续项：1（`ISSUE-053-001`）。
- 后续项净变化：-1。

最小回滚：回退 V2 令牌、组件、测试、预览与本轮文档回写；M0 健康页、Legacy、后端和数据保持不变。

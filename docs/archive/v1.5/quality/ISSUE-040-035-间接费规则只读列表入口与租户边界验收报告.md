# ISSUE-040-035 间接费规则只读列表入口与租户边界验收报告

结论：通过；阻塞：无。

## 范围与结果

- 成本台账页增加 `overhead:query` 或 ADMIN/SUPER_ADMIN 可见的规则只读弹窗，展示成本科目 ID、分摊依据、分摊周期和状态，支持服务端分页。
- 前端精确调用 `GET /overhead-allocation/rules`，请求参数仅含 pageNo/pageSize，不含 tenantId、body 或写操作；加载和失败清空旧记录及总数，请求序号阻止较早响应覆盖新分页结果。
- 后端生产代码未改动。既有接口权限和 Service 认证租户过滤保持不变，未放宽间接费执行权限，未引入规则新建、修改或删除能力。

## 验收证据

- Ready lint：通过。
- 后端：`OverheadAllocationControllerTest,TenantBoundaryTask2Test` 共21项通过，覆盖未登录401、无权限403、`overhead:query`/管理员成功及认证租户边界。
- 前端：`CostLedgerProduction.test.ts` 13项通过；`vue-tsc --noEmit`、目标 ESLint、`git diff --check` 通过。
- 运行态：8080 health、5173 与 dev-login 可达；前端重启后完成180秒稳定观察。真实浏览器进入 `/cost/ledger`，确认“查看间接费规则”入口唯一，弹窗只读、空态正常，无新增/编辑/删除动作，控制台 error/warn=0。
- 审查：复核权限可见性、租户参数不可注入、请求竞态、旧数据清理和允许路径，结论 `PASS`、findings=无。

## 治理收口

- 唯一台账移除 `A-01-OVERHEAD-LIST`，A-01 更新为有用户入口240、前端调用但无独立页面58、内部/集成/运维4、需补入口8、待废弃0、需要确认11，共321。
- 新增后续项：0；关闭后续项：1；后续项净变化：-1。
- 最小回滚：回退本 Issue 的前端规则类型/API/弹窗、专项测试与治理回写；不涉及数据库或业务数据回滚。

剩余风险：本地规则表为空，仅以真实空态和自动化分页契约验收；非空行展示由前端类型、表格列契约和后端模拟记录覆盖。证据充分，不形成后续项。

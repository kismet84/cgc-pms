# ISSUE-053-021 M4成本台账、核对与动态利润V2验收报告

## 裁决

- 结论：通过。
- 阻塞项：0。
- 上线结论：仅证明本地dev/test切片验收通过；不代表正式入口切换、Legacy退役或生产发布。
- 路由台账：`LEGACY_ONLY=52 / V2_ACCEPTED=35 / V2_SOURCE_AVAILABLE=0`。

## 范围与结果

- `/cost`确定性重定向`/cost/ledger`并保留query/hash；成本台账、成本核对、动态利润三页由独立Clean-room V2页面承接。
- 台账使用服务端分页、汇总、详情与报告期窗口；详情查询零写。汇总刷新使用独立`cost:summary:refresh`权限和审计，同日去重，历史日期不可变。
- 动态利润覆盖预测创建/更新/确认、纠偏创建/更新/提交、驳回后同实例重提、关闭和trace；通用工作流对纠偏提交/重提写入前失败关闭。
- 预测、纠偏写动作强制version/CAS；创建预测锁活动目标版本，纠偏新建/更新/关闭统一forecast→action锁序，防合计越界与`CONTROLLED + 开放纠偏`竞态。
- 纠偏责任人创建、更新与提交前均验证为项目有效成员；金额和所有Map ID稳定字符串传输。

## 验证证据

- Ready lint：通过。
- 后端目标命令通过；独立补测覆盖真实驳回重提、通用零写、confirm/submit/close双线程CAS、close/create不变量、普通动作权限、只读403、项目外范围及台账page/summary/detail范围。
- 前端：33个测试文件208项通过；契约类型、V2类型、ESLint、Clean-room边界、路由台账和构建通过。
- Playwright：4项通过；三页三视口、无横向溢出、axe serious/critical为0、台账大ID详情零写、刷新/确认冲突回刷及显式无权限403零业务请求。
- demo verify：`passed=true`；V221仅登记汇总刷新权限并完成成本角色矩阵。
- `git diff --check`：通过。

## 独立复核与补修

- 安全复核发现纠偏驳回无法重提、最终关闭与并发新建状态竞争两项P1；已新增专用工作流入口和generic fail-close、统一forecast锁序，并以真实工作流与并发不变量测试关闭。
- 测试复核发现失败态/陈旧响应、并发CAS、HTTP权限/项目范围及三页三视口证据缺口；已补正式Ready测试载体、500/404/409/422、Abort/generation、普通/只读权限、项目外范围及浏览器矩阵并复验关闭。

## 边界与收口

- 未迁移M7成本科目中心，未切正式入口，未连接生产。
- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- 悬空项：0。
- 最小回滚：回退成本三页/四路由、共享契约增量、精确快照/CAS/工作流根修、V221、测试及四条台账状态；017～020、Legacy和业务数据保持不变。

# ISSUE-053-006 M2 八角色新版经营驾驶舱验收报告

## 结论

- 验收结论：通过。原“七个项目级角色缺真实有数证据”阻塞已解除。
- 数据边界：驾驶舱范围仅变更本地 `cgc_pms_demo_v2` 与可重放演示包；后续用户授权的统一编号任务新增 V218 和演示包第19阶段。未连接生产库，未修改权限码或金额公式。成本分解复用现有 `5401` 权威科目树，不新建科目。
- 产品边界：成本和财务使用权威时间序列；其他角色在无权威时间序列时展示 API 已返回的“经营动态”，不伪造图表。
- 上线边界：本地 Clean-room V2 通过；未切换正式入口，未发布生产。

## 实施范围

- 新增演示包第16阶段 `ROLE_DASHBOARD_DATA`、第17阶段 `DASHBOARD_RISK_LEVELS`、第18阶段 `COST_BREAKDOWN_DATA` 与后续统一编号第19阶段 `STANDARDIZE_BUSINESS_CODES`；前三阶段分别补齐角色业务事实、八角色“高/中/低/其他”四档筛选数据，以及成本经理两级科目分解数据，第19阶段只规范内部业务编号。
- 成本分解使用“合同履约成本”父级与“招投标及前期费用、采购阶段成本、施工阶段成本、项目间接费用”四个子级；目标、实际、动态、偏差四项父级金额均等于子级合计，并同时覆盖正负偏差。
- 财务经理新增“合同资金分解”：合同为一级、付款记录为二级，展示合同金额、累计支付、审批中、已批未付、剩余额度和支付比例；直接复用现有合同、付款申请与付款记录，不新增接口、权限或演示事实。
- 保留既有成本七期趋势、财务支付闭环和管理层汇总；“全部预警”只作为聚合筛选，不计作风险等级。
- 非趋势角色工作区改为紧凑的经营动态列表，直接消费当前角色 API 列表字段；无数据时显示明确空态。
- 新增单元、后端服务测试与 live E2E，覆盖项目选择、角色端点、关键指标、风险映射、32个角色—等级组合、总工经营动态，以及成本科目权限、真实接口和两级展开/收起。

## 验收证据

- 演示库：`verify.ps1` 通过，`completed_stage=19`、内部业务编号不合规数0；成本分解5行、1个父级、4个子级、成本角色权限及四项父子金额差额0均通过。
- 真实浏览器：`m2-dashboard-live.spec.ts` 7/7 通过；成本分解1父4子，财务分解4个合同，演示服务合同展开5条付款记录，展开/收起均通过。
- V2 单测：14 文件/69 项通过；成本科目与合同资金两类展开/收起均有组件测试。
- 后端：`DashboardFinanceManagementServiceTest` 6/6 通过，覆盖合同资金口径、超合同额、管理层严重度及财务全部项目SELF数据范围。
- `type-check:contracts`、`type-check`、`lint:check`、`check:boundary`、`check:route-ledger`、`build`、`check:bundle-size`和 `git diff --check` 全部通过。
- 运行态：后端健康且连接 `cgc_pms_demo_v2`；V2 容器刷新后动态路由与当前源码一致。

## 发现项与收口

- 原阻塞 `environment_prerequisite`：本轮修复并复验，演示库及八角色 live E2E 通过。
- V2 前端容器保留旧路由模块：运行态刷新后解除，不是业务代码缺陷。
- Playwright 文字定位命中重复节点：收窄到 `#cost-trend`，目标用例和全文件复验通过。
- 原两档风险语义不足：统一为“高/中/低/其他”，八角色按权威字段映射，浏览器32个角色—等级组合复验通过。
- 原生 `select` 在当前浏览器无法展开：改为无依赖的页面内 `details` 下拉，真实点击展开、选项可见、选择后收起及列表刷新均通过。
- 风险表产生 Axe `scrollable-region-focusable`：补充键盘焦点后，1440/1024/390 三视口 serious/critical 为0。
- 成本科目分解此前无演示数据：本轮新增可重放第18阶段并复验，复用现有权威科目，不制造平行科目树。
- 财务合同明细会放大原“全部项目”未过滤数据范围风险：本轮在共享入口复用 `ProjectAccessChecker` 修复并通过SELF范围负向测试。
- 新增后续项 0，关闭后续项 1，后续项净变化 -1；无悬空项。

## 回滚

- 前端：回退经营动态派生函数、成本/财务分解页面分支和对应测试；成本/财务趋势不受影响。
- 演示数据：在新的本地 demo 空库重建前17阶段即可移除本次成本分解样本；不对已有数据库执行盲删。

# ISSUE-053-022 M4项目预算与产值计量V2验收报告

## 裁决

- 结论：通过。
- 阻塞项：0。
- 上线结论：仅证明本地dev/test切片验收通过；不代表正式入口切换、Legacy退役或生产发布。
- 路由台账：`LEGACY_ONLY=50 / V2_ACCEPTED=37 / V2_SOURCE_AVAILABLE=0`。

## 范围与结果

- `/budget`、`/production-measurement`由独立Clean-room V2页面承接；公共壳项目/报告期切换产生真实服务端`projectId/startDate/endDate`查询。
- 预算覆盖列表、详情、创建、编辑、明细、可用额、删除、提交及驳回后同实例重提；所有写动作强制version/CAS，明细写后重新读取服务端权威可用额。
- 产值覆盖计量期间、来源、计量单、内部审批、业主申报、核定/退回及结算追溯；金额和ID保持十进制字符串，数量与累计金额由服务端计算。
- 附件门禁按租户、业务类型、业务ID、文档类型和`CLEAN`状态查询真实`sys_file`；伪客户端计数、跨对象文件、失败上传及缺文件均不能推进状态。
- 文件上传/删除与计量提交/核定统一采用业务对象行锁；确定性双事务证明状态冻结后文件变更等待并拒绝，先删除文件则提交/核定失败且无工作流或结算副作用。

## 验证证据

- Ready lint：通过。
- 后端正式目标命令：10类110项通过，Failures/Errors/Skipped均为0；覆盖预算8个、计量13个HTTP endpoint权限矩阵，预算4项、计量9项并发、真实文件负例及当前三参文件授权契约。M4跨切片35类410项同样零失败/错误/跳过。
- 前端：35个测试文件222项通过；契约类型、V2类型、ESLint、Clean-room边界、路由台账和构建通过；M4跨切片E2E 16/16通过（本Issue目标5/5）。
- Playwright：5项通过；三视口、axe serious/critical为0、显式403零业务请求、预算CRUD/lines/availability CAS闭环、公共项目/报告期切换、超大ID/DecimalString及附件上传成立。
- demo verify：运行库`cgc_pms_demo_v2`已迁移至V221且`passed=true`；预算与产值普通角色权限、计量单、业主申报及金额差指标通过。
- `git diff --check`：通过。

## 独立复核与补修

- 测试复核发现报告期后端未绑定、HTTP权限矩阵、关键并发及预算CRUD/422证据缺口；已补服务端日期过滤、全endpoint真实MockMvc矩阵、预算/计量并发和完整页面/E2E闭环。
- 安全复核发现文件阶段校验与状态迁移TOCTOU；已增加业务对象`FOR UPDATE`锁序，并以确定性双事务等待与反向顺序测试关闭。
- 最终独立安全与测试复核均通过，无P0/P1。

## 边界与收口

- 未新增数据库迁移，未迁移M7成本科目中心，未切正式入口，未连接生产。
- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- 悬空项：0。

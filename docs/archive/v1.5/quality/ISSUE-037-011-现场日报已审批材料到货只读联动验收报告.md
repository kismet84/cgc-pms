# ISSUE-037-011 现场日报已审批材料到货只读联动验收报告

## 结论

- Decision：通过。
- 阻塞：无。
- 上线边界：代码与自动化测试通过；未执行真实运行态浏览器验收，未发布、未 push。

## 实施边界

- 日报详情在既有项目访问校验后，只读查询同租户、同项目、同日期且 `APPROVED` 的材料验收单与明细。
- 批量装配验收单号、供应商、物料、实收数量和合格数量；数量以字符串保留数据库精度。
- 前端查看详情时加载详情 API，展示只读表格或空态。
- 未新增表、权限或写接口，未修改验收审批、库存入库和成本生成。

## 验收证据

- Ready lint PASS；TDD RED 分别命中后端缺少 `deliveries`/聚合依赖与前端缺少只读区域。
- 后端 `SiteDailyLogServiceTest,SiteDailyLogControllerTest`：7/7 通过，覆盖项目访问、跨租户隐藏、空列表、查询参数与数量精度。
- 前端日报专项：3/3 通过；`vue-tsc --noEmit` 通过。
- `git diff --check` 通过；仅出现 Windows 行尾提示，无 whitespace error。
- E 审查：所有查询均绑定当前 tenant；验收单绑定日报 project/date/APPROVED；明细、物料、供应商使用批量租户查询；未新增任何写调用。

## 图谱检索证据

- CodeGraph：定位 `SiteDailyLogService`、`MatReceiptService`、`MaterialReceiptWorkflowHandler` 与前端日报详情流，确认审批通过后才写 `APPROVED` 并触发库存/成本。
- `codebase-memory-mcp`：只读复核后端模块边界和路由聚类；首次使用错误 project 名返回 `tool_config`，改用索引名 `D-projects-test-cgc-pms` 后成功。
- 编辑后 CodeGraph 自动同步因文件锁停用，归类 `tool_config`；当前源码由 diff、编译和目标测试交叉核验。

## 风险与回滚

- 非阻塞：尚未在真实本地服务中构造已审批验收数据做浏览器验收；当前由 Spring/H2 控制器测试、服务查询约束和前端静态专项支撑通过。
- 剩余产品边界：材料消耗、安装量、设备和生产率继续后置，已保留在 Current Focus/项目地图语义中。
- 回滚：移除日报到货 VO、详情聚合和前端展示；无数据迁移。

# ISSUE-053-012 M3项目计划与现场日报验收报告

## 1. 结论

- 验收结论：通过。
- 阻塞状态：无。
- 上线结论：本地开发/演示验收通过；未切换正式入口，未提交、推送、创建PR或发布生产，不构成生产上线批准。
- 路由结果：`/project-schedule`、`/site/daily-log`均为真实Clean-room V2页面；路由台账为`LEGACY_ONLY=71 / V2_ACCEPTED=16 / V2_SOURCE_AVAILABLE=0`。
- 数据库：无迁移、无结构变更。验收用身份、计划、WBS、日报及附件记录均已精确回滚，复核计数为0。

## 2. 实现与边界

- V2新增计划、WBS、月周计划、快照、纠偏、追溯、现场日报、质量安全摘要及`SITE_DAILY_LOG`附件契约、薄服务和页面。
- 请求核心支持原生`FormData`：不执行JSON序列化，不手设`Content-Type`，保留same-origin、CSRF和既有401恢复。
- 写侧使用服务端并发事实：WBS/期间项携带`expectedVersion`，日报编辑携带`expectedUpdatedAt`；失败后重读权威详情。
- 权限边界：计划查询、维护、提交、进度、纠偏及日报查询/编辑分离；缺质量安全权限时零请求且不阻断正文；按钮隐藏不替代后端权限。
- 后端最小兼容修复：快照写权限改为`schedule:progress`；计划、期间、纠偏及日报提交加行锁和状态重验；Legacy同步携带并发令牌。
- 非目标保持：未新增依赖、全局Store、数据库迁移、正式入口、生产连接或前端业务权威推导。

## 3. 自动化证据

| 层级 | 命令/范围 | 结果 |
| --- | --- | --- |
| Ready | `ready-lint.ps1 ... -IssueTitle ISSUE-053-012` | 通过，风险等级高 |
| 后端 | `ProjectScheduleClosedLoopIntegrationTest, ProjectScheduleControllerPermissionTest, SiteDailyLogServiceTest, SiteDailyLogControllerTest, BusinessObjectAuthorizerTest` | 34项通过；含陈旧WBS/期间项、期间并发提交唯一工作流、纠偏审批仅生成一份修订、日报时间令牌、重复提交副作用隔离及附件授权 |
| Legacy | 项目计划API、现场日报页面目标测试 | 2文件10项通过 |
| Legacy类型 | `frontend-admin pnpm type-check` | 通过 |
| V2契约 | `pnpm type-check:contracts` | 通过 |
| V2单测 | `pnpm test:unit` | 21文件98项通过；含FormData、CSRF、并发令牌、混合后端字段归一化及真实trace数组映射 |
| V2静态 | `pnpm type-check`、`pnpm lint:check`、`pnpm check:boundary` | 通过；Clean-room扫描93个V2文件、9个契约文件 |
| 路由 | `pnpm check:route-ledger` | 87路由，台账`71/16/0` |
| 构建 | `pnpm build`、`pnpm check:bundle-size` | 通过；9个JS资产满足预算 |
| Live E2E | `V2_LIVE_DELIVERY=1 playwright test e2e/m3-delivery.spec.ts` | 3项通过；两路由无占位、1440/1024/390无横向溢出、axe无serious/critical、真实分权按钮与零写请求 |

受控身份清理后再次执行同一live文件：路由与三视口2项通过，真实分权项因环境变量/身份已移除按设计跳过；不覆盖清理前3项通过证据。

## 4. 本地运行态与真实权限/写侧证据

- 刷新后端及V2 dev容器；`http://localhost:8080/api/actuator/health`返回200/UP，`http://localhost:5174/v2/health`返回200。
- 临时`demo.schedule-readonly`仅含`project:query,schedule:query`：计划GET 200；快照POST 403/`AUTH_FORBIDDEN`；浏览器不显示新建基线或维护WBS按钮，页面访问未产生写请求。
- `demo.manager`在ACTIVE项目创建受控草稿计划/WBS；首次写成功，旧`expectedVersion=0`再次写返回400/`PROJECT_SCHEDULE_VERSION_CONFLICT`，权威version保持1。
- `demo.production`创建、更新并提交受控日报；旧`expectedUpdatedAt`写返回400/`SITE_DAILY_LOG_VERSION_CONFLICT`，最终状态`SUBMITTED`。
- 附件闭环：DRAFT上传200、列表1条、删除200；日报提交200；提交后再次上传返回400/`SITE_DAILY_LOG_SUBMITTED_IMMUTABLE`；最终活动附件计数0。
- 精确回滚后：受控计划、WBS、2份日报、附件记录、临时用户和临时角色计数均为0。未删除范围外登录审计。

## 5. 失败分类与修复记录

- 首次V2构建失败：Vue模板内多语句点击表达式无法解析。分类`quality_or_security`，改为具名`resetFilters()`后构建通过。
- 首次后端扩展目标失败：H2保留微秒而API令牌精度为秒，造成新鲜日报令牌被误判陈旧。分类`quality_or_security`，比较双方秒精度并保留单调更新时间后，Controller目标及33项组合回归通过。
- 首次Live E2E仍显示Shell占位。源码与生产构建正确，V2 dev server持有旧路由模块；分类`environment_prerequisite`，按运行态规范刷新V2容器后真实页面通过。
- 首次追溯契约假设了不存在的摘要字段。独立复核后改为完整保留后端`trace()`七组真实数组，展示层再计算计数和最近项，并增加契约单测。
- 独立测试复核发现纠偏审批回调先生成修订、后条件更新的并发窗口。改为先锁纠偏行并重验状态，校验条件更新结果；双线程回归证明只生成一份修订且预警仅处理一次。
- 修复后独立测试复核结论：通过，剩余finding为0；正式报告、Ready/Done、台账及Blocked解除状态一致。

## 6. 回滚与后续

- 最小代码回滚：两路由恢复`LEGACY_ONLY`，回退delivery契约/服务/页面、FormData增量、路由生成器及后端/Legacy兼容修复；无数据库结构回滚。
- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- `ISSUE-053-013～016`属于M3既定串行计划，不是本轮发现项；013须按当前证据另行补为Ready后再实施。

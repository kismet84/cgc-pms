# 分包计量与结算收付二级页面 UI 统一重构验收报告

## 验收结论

- 结论：通过
- 阻塞：无
- 上线属性：前端视觉与布局重构，未改动 API、权限判断、路由或业务写入逻辑
- 验收基准：以项目列表页为硬参考，不以“达到列表页基线”代替视觉统一
- 验收范围：分包任务、分包计量、结算台账、付款申请、资金日记账、会计凭证、发票管理共 7 个二级页面

## 范围与变更

7 个页面全部按项目列表的同一视觉骨架收口：

- 页头仅保留面包屑，删除 H1 与说明文案。
- 主栏统一为“分段 KPI 条 → 两层筛选区 → 表格卡片”的信息顺序。
- 表格工具栏统一为左侧标题与数量、右侧列设置/刷新/新建等页面操作。
- 右侧分析栏统一使用 `--lg-rail-width` 设计令牌，宽度 240px、高度 703px、顶部 56px，与项目列表一致。
- 删除 KPI 内不属于项目列表视觉语言的装饰性渐变圆形。
- 付款申请去除额外顶部间距；资金日记账筛选区由纵向堆叠改为紧凑网格。
- 保留各页面原有业务字段、权限控制和空状态，不新增虚假业务入口。

## 验收证据

### 自动化验证

- 受影响域回归：13 个测试文件、72 个用例全部通过。
- 新增跨页面视觉结构防回退测试，覆盖无 H1、KPI/筛选/表格顺序、分析栏壳层与 240px 宽度令牌。
- ESLint：所有本轮变更页面与测试文件检查通过。
- TypeScript：`pnpm type-check` 通过。
- 生产构建：`pnpm build` 通过，共处理 4572 个模块。
- 差异卫生：`git diff --check` 通过。

### 内置浏览器验收

- 每次前端运行态刷新后均等待 180 秒；最终健康门禁为后端 200、前端 200、开发登录 302。
- 内置浏览器外层窗口宽度实测 1707px，满足不低于 1600px 的要求；同一窗口内完成项目列表与 7 个目标页复验。
- 项目列表与 7 个页面的左栏均为 822px，右栏均为 240px，分析栏顶部均为 56px、高度均为 703px。
- KPI 高度为 108–110px；筛选区高度为 126–147px，均保持项目列表的紧凑两层节奏。
- 7 个页面均为 H1=0、装饰性 KPI 卡片=0、页面水平溢出=false、工具栏左右分区=true。
- 分包任务、资金日记账、发票管理的查询与重置操作复验通过。
- 浏览器错误日志：0 条。
- 最终视觉对照板：`.agent-runtime/ui-qa-20260714/all-pages-final-v3/seven-pages-contact-sheet.png`（本地临时验收产物，不进入版本管理）。
- 结算表格顶部蓝色强调线批注修复后，计算样式仅保留全局卡片约 1px 的浅灰边框；宽屏截图复验无蓝线，证据位于 `.agent-runtime/ui-qa-20260714/settlement-line-fix/`。

## 发现项收口

- 早期仅按“列表页基线”判断，未满足用户要求的视觉统一：本轮修正判断口径，7 页全部重构并复验。
- 5 个页面分析栏仍为 336px，与项目列表 240px 不一致：本轮统一为设计令牌并复验。
- 分包任务/分包计量 KPI 过高且带装饰渐变：本轮移除页面特例并复验。
- 付款申请顶部偏移、资金日记账筛选区过高：本轮修复并复验。
- 结算表格卡片独有的 3px 主色顶部边框：本轮删除并增加防回退测试，宽屏复验通过。
- 新增后续项：0
- 关闭后续项：0
- 后续项净变化：0

## 剩余风险

无已知阻塞风险。本轮使用开发环境当前数据完成桌面端视觉与核心筛选交互验收；正式环境高数据量下的性能与分页行为仍由常规上线回归覆盖。

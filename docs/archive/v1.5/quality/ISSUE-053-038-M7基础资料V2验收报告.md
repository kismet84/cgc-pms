# ISSUE-053-038 M7 基础资料 V2 验收报告

> 日期：2026-07-27
>
> 基线：`master@07f0169da0a3c8ae3f6ee4de463dfe55da2e84de`
>
> 裁决：`PASS / ISSUE-053-038 Done / M7 CONTINUES`

## 1. 范围与结论

`/partner`、`/org`、`/material`、`/material/dictionary` 已由 Clean-room V2 页面或确定性重定向承接，台账由 `18/69/0` 更新为 `14/73/0`。

- 合作方、公司、部门、岗位、材料和材料分类均复用既有 API；列表、筛选、分页和状态展示不建立前端平行主数据。
- `/material` 保留 query/hash 并重定向到 `/material/dictionary`；路由和导航权限分别使用既有 `partner:query`、`org:list`、`material:dict:list`。
- 写操作继续按 add/edit/delete 精确权限显示；保存、状态变更和删除后刷新最新记录。
- Legacy 正式入口、Legacy 页面、数据库结构和权限码均未变。

## 2. 根因整改与安全证据

| 事项 | 整改 | 证据 |
| --- | --- | --- |
| 合作方敏感列表字段 | 分页结果不投影联系电话和银行账号；详情接口仍按查询权限返回编辑所需字段 | `MdPartnerServiceTest` 列表/详情正反例 |
| 组织租户与树一致性 | 部门强制当前租户；公司、父部门必须同租户同公司；更新拒绝自环和后代环 | `OrgServiceTest` 跨公司父级、循环和当前租户测试 |
| 组织引用保护 | 公司、部门和岗位删除前检查下级、岗位、用户岗位及现有业务外键引用 | `OrgServiceTest` 三层删除负向 |
| 材料输入与状态 | 材料编码创建后不可修改；编码、名称、状态、分类和税率边界由服务层校验；启停仅更新状态列 | `MdMaterialServiceTest` 正负例及非状态字段不变断言 |
| 采购材料引用 | 采购申请和采购订单拒绝停用、缺失或跨租户材料；同名停用材料不能绕过 | `PurchaseRequestServiceTest`、`MatPurchaseOrderServiceTest` |
| 权限对齐 | 材料列表和详情统一使用 `material:dict:list`；组织 V2 查询只调用 `org:list` 接口；管理员角色与后端写权限保持一致 | `MdMaterialControllerSecurityTest` 的读写/CSRF 正负例、router unit、route ledger |
| 前端事实边界 | 金额/税率保留字符串；创建接口数字 ID 先规范化再读回；无本地 KPI、缓存或日志；变更后重新请求 | V2 unit、设计门、E2E |

## 3. 验证

| 门禁 | 命令/范围 | 结果 |
| --- | --- | --- |
| 后端编译 | `mvnw.cmd -DskipTests compile` | 749 个源文件，`BUILD SUCCESS` |
| 后端定向测试 | partner、org、material、purchase request/order | 106 项，0 失败，`BUILD SUCCESS` |
| V2 目标 unit | master-data + router | 24/24 |
| V2 全量 unit | `pnpm test:unit` | 51 文件、378 项全通过 |
| type/contracts | `pnpm type-check`、`pnpm type-check:contracts` | 通过 |
| lint | `pnpm lint:check` | 0 error；6 个既有 warning 均在未修改的 `e2e/m1-shell.spec.ts:149-156` |
| clean-room | `pnpm check:boundary` | 186 个 V2 文件、16 个契约文件通过 |
| route ledger | `pnpm check:route-ledger` | 87 条命名路由，`14/73/0`，无漂移 |
| build | `pnpm build` | 242 modules，构建通过 |
| E2E | 独立端口 5176，partner/org/material 深链与重定向 | 1/1 通过 |
| diff | `git diff --check` | 通过；仅提示两个既有 CRLF 工作区文件后续由 Git 规范化 |

失败处理遵循唯一分类契约：命令引号和测试输出聚合问题归 `tool_invocation`；旧 5174 Vite 实例被复用归 `environment_prerequisite`；测试夹具、设计门、选择器和模板编译缺陷归 `quality_or_security`。均修正根因后做最小复验，再完成全量 Gate B；未放宽门禁。

## 4. 剩余风险与下一门

- M7 尚余 14 条 `LEGACY_ONLY`；`ISSUE-053-039` 是下一唯一 Ready，承接成本科目中心五条路由。
- `BIZ-001/M7` 的改密令牌撤销、多会话策略及后续 adminOnly 门不由本切片替代。
- 正式入口、Legacy 退役、commit、push、PR、目标环境和生产均未授权。

## 5. 回滚与零悬空

- 回滚仅移除三张基础资料 V2 页面、服务、四条路由映射、测试和台账接受项，并恢复本切片后端根修；Legacy 正式入口未变，无数据库 migration。
- 新增后续项：0；关闭后续项：0；后续项净变化：0。
- 本轮发现的权限、租户、状态、引用和敏感字段问题均已修复并复验；无口头悬空项。

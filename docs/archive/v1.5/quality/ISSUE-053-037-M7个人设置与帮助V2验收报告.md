# ISSUE-053-037 M7 个人、设置与帮助 V2 验收报告

> 日期：2026-07-27
>
> 基线：`master@07f0169da0a3c8ae3f6ee4de463dfe55da2e84de`
>
> 裁决：`PASS / ISSUE-053-037 Done / M7 CONTINUES`

## 1. 范围与结论

`/profile`、`/settings`、`/help` 已由 Clean-room V2 账号中心承接，台账由 `21/66/0` 更新为 `18/69/0`。

- 三条路由只要求登录，不挂仓库中不存在的 `profile:query/settings:query/help:query`，普通用户可访问，匿名深链仍失败关闭到登录页。
- 资料只提交姓名、手机号、邮箱和头像；保存后重新读取当前用户。密码请求只含旧密码和新密码，成功后清空三个输入框。
- 偏好接口只接受四个公开键及明确类型/枚举，写入后重新 GET；内部通知订阅偏好仍由服务层保留。
- 帮助页只保留可由当前实现证明的导航、键盘、Cookie 和管理员支持说明，未复制 Legacy 的占位电话、版本、备份周期或未实现快捷键。

## 2. 根因整改与安全证据

| 事项 | 整改 | 证据 |
| --- | --- | --- |
| 资料并发覆盖 | `ProfileService` 改为仅更新四个白名单列，并按当前用户与租户重新查询 | SQL 日志只出现 `real_name/phone/email/avatar`；保护字段前后值测试一致 |
| 资料输入边界 | 姓名非空/100 字、电话 50 字、邮箱格式/128 字、头像地址 500 字 | `ProfileControllerTest` 正负例 |
| 偏好任意 JSON | Controller 拒绝未知键、错误类型和非法枚举；响应只投影四个 UI 键 | `PreferenceControllerTest` |
| 租户与用户隔离 | profile 和 preferences 均使用当前 JWT 的 userId/tenantId | 跨用户、跨租户负向测试 |
| CSRF | Cookie JWT 写请求继续要求双提交 CSRF token | profile/preferences 缺 token 均为 403 |
| 敏感日志 | 偏好 JSON 解析失败不再记录原文；密码日志继续掩码 | 定向测试日志与静态差异 |

## 3. 验证

| 门禁 | 命令/范围 | 结果 |
| --- | --- | --- |
| 后端定向测试 | `mvnw.cmd -Dtest=ProfileControllerTest,PreferenceControllerTest test` | 27 项，0 失败，`BUILD SUCCESS` |
| V2 目标 unit | account + router | 22/22 |
| V2 全量 unit | `pnpm test:unit` | 50 文件、372 项全通过 |
| type/build | `pnpm build` | 232 modules，构建通过 |
| lint | `pnpm lint:check` | 0 error；6 个既有 warning 均在未修改的 `e2e/m1-shell.spec.ts:149-156` |
| clean-room | `pnpm check:boundary` | 180 个 V2 文件、16 个契约文件通过 |
| route ledger | `pnpm check:route-ledger` | 87 条命名路由，`18/69/0`，无漂移 |
| E2E | 独立端口 5194，auth + account | 6/6 通过 |
| diff | `git diff --check` | 通过 |

失败处理均遵循唯一分类契约：两次 Maven 参数转义错误归 `tool_invocation`；测试断言、格式、UI 设计门失败归 `quality_or_security`；独立复核与主线程并发写同一 Maven `target` 导致一次 class 暂缺，归 `environment_prerequisite`。停止并发后由主线程串行终态复跑 27 项全通过。未通过放宽门禁获得绿灯。

## 4. 剩余风险与下一门

- M7 尚余 18 条 `LEGACY_ONLY`；`ISSUE-053-038` 是下一唯一 Ready，承接合作方、组织、材料与材料字典。
- 改密后当前 JWT/refresh token 的撤销策略未在本切片扩展；不影响本地页面迁移裁决，但不得据此宣称密码安全闭环或生产通过。该项作为既有 `BIZ-001` 的 M7 退出子门承接。
- `/approval/process` 与系统管理仍受独立 `adminOnly` 门阻断。
- 正式入口、Legacy 退役、commit、push、PR、目标环境和生产均未授权。

## 5. 回滚与零悬空

- 回滚仅移除账号中心页面/服务、三条 V2 路由与账号入口，恢复资料/偏好根修前代码，并重新生成台账；Legacy 正式入口未变，无数据库 migration。
- 新增后续项：1；关闭后续项：0；后续项净变化：+1。
- 新增项未创建新稳定键，已写入既有 `BIZ-001/M7` 退出门；验收为改密后当前 access/refresh token 均不可继续使用，并明确多会话策略。无口头悬空项。

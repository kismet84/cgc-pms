# ISSUE-053-017：M4商务契约、金额与权限金丝雀验收报告

日期：2026-07-22

结论：**通过**。阻塞项：**0**。017金丝雀关闭；不代表任何M4页面已迁移，不授权018、提交、push、PR、正式入口切换、Legacy退役或生产发布。

## 1. 实施范围

- `packages/frontend-contracts/src/commercial.ts`冻结合同、变更、投标成本、目标成本、成本台账/汇总/控制、预算和产值计量稳定DTO、九项查询权限、API与金额字段。
- `frontend-admin-v2/src/services/commercial.ts`实现合同台账、合同详情、成本汇总历史三条GET薄服务；query编码、空筛选省略、ID校验和AbortSignal透传成立。
- 目标成本VO、成本控制和产值计量API出口按精确金额键输出`BigDecimal.toPlainString()`；未启用全仓BigDecimal序列化。
- 合同、变更、已绑定投标成本、目标成本、成本台账、预算及成本纠偏更新按当前租户和服务端项目可见范围失败关闭；未绑定项目的标前投标成本保持租户级可见。

## 2. 安全与金额裁决

- 独立安全复核发现成本纠偏更新缺项目范围守卫，定级P1；本轮在写库前补`requireProject`，并证明无权用户收到`PROJECT_ACCESS_DENIED`且数据库原值不变。复核后无P0/P1。
- 独立测试复核发现三条金丝雀缺真实HTTP金额类型证明；本轮补合同列表/详情与成本汇总历史HTTP回归，大于`2^53`、0、负数均返回JSON字符串。
- 前端契约无权威金额`number`，目标服务无`Number`、`parseFloat`或金额算术；税率、数量和利润率继续作为明确非金额边界。
- 查询权限固定为九项既有权限码；五条成本科目路由仍归M7，没有进入本契约或路由迁移。

## 3. 验证证据

| 验证 | 结果 |
| --- | --- |
| Ready lint | 通过；hash=`4748336fccb0beb6e1bfe81a292cda0f2f9d4d62f8177452f3cbd5e86af355ba` |
| 后端定向 | 10类124项通过；0失败、0错误 |
| 纠偏越权修复回归 | `TargetCostDynamicProfitClosedLoopIntegrationTest` 3/3通过 |
| 真实HTTP金额边界 | 合同与成本汇总Controller 20/20通过 |
| `pnpm type-check:contracts` | 通过 |
| V2单测 | 27文件144项通过 |
| `pnpm type-check` | 通过 |
| `pnpm lint:check` | 通过 |
| `pnpm check:boundary` | 通过；114个V2文件、13个契约文件 |
| `pnpm build` | 通过 |
| `git diff --check` | 通过 |

首次以未加引号的PowerShell Maven属性调用产生`.skip=true`生命周期错误，分类为`tool_invocation`；使用原参数加PowerShell字符串引用后通过。早期两个新增金额断言选错字段/聚合位置，分类为`ready_issue_config/测试契约`；修正后相关测试与最终组合回归通过。

## 4. 范围与回滚

- 执行基线：`master`，初始HEAD `b6e46f9e37424f3f70889ccffb86bc2d662f6922`；当前为未提交工作区。
- 未修改页面、路由、路由台账、数据库迁移、Legacy、demo、部署、CI或生产环境；台账保持`68/19/0`。
- 回滚只需回退商务契约/薄服务、精确后端金额与项目范围修正、对应测试及治理回写；无数据库回滚。

## 5. 零悬空收口

- 本轮修复并复验：启动核验发现的金额JSON number与项目范围缺口；安全复核P1；测试复核HTTP金额边界缺口。
- 超出范围并正式承接：0。
- 证据不足或无明确价值而关闭：0。
- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- 悬空项：0。

`ISSUE-053-018～023`是既定阶段计划，不是本轮新增发现；当前仍为Planned。018须另行补为合格Ready并获得明确实施授权。

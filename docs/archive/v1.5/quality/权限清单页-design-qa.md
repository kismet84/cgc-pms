# 权限清单页设计 QA

## 验收范围

- 源视觉：`D:\projects-test\test\permission-list\permission-list-render.png`，桌面端 1536 × 1024。
- 最新业务修正：移除页内 H1“权限清单”、说明文案及“新建权限”入口；权限数据由后端菜单/角色接口提供。
- 实现截图：`../../.agent-runtime/permission-list-final.png`，桌面端 1536 × 1024，超级管理员、默认筛选、第一页、工作台目录展开。
- 全页对比：`../../.agent-runtime/permission-list-compare-final.png`，左侧为参考图，右侧为最终实现。
- 未单独生成局部对比图：本轮修正对象是整段页头及右侧创建入口，最终全页截图已完整展示该区域被移除，不需要额外裁剪才能判断。

## 对比结论

- 布局：保留面包屑、四项概览、筛选区、权限类型页签、操作区、树形表格与分页；移除页头后，概览区直接承接面包屑，桌面首屏信息更紧凑。
- 字体与间距：延续项目现有字体栈、字号层级和控件节奏；表格使用固定可视高度与内部滚动，分页在 1536 × 1024 首屏可见。
- 颜色与资产：继续使用项目 Ant Design 图标与现有蓝/绿/橙状态色，未引入额外图片或第三方视觉资产。
- 文案：页内 H1、说明文案、“新建权限”按钮和创建弹窗均不存在；侧边导航与面包屑中的“权限清单”保留，作为定位信息。
- 数据：概览统计、权限行、角色、状态及接口路径均来自当前后端返回；参考图中的静态数量和时间仅作视觉示例，不覆盖真实数据。

## 交互与运行态

- 页面地址：`http://localhost:5173/system/permissions`。
- 健康门禁：后端健康接口、前端首页、开发登录入口均返回 HTTP 200。
- DOM 核对：H1 数量 0；“新建权限”精确文本数量 0；说明文案数量 0；概览卡片 4；分页 1。
- 筛选回归：默认 20 行；搜索 `dashboard:view` 后 1 行；重置后恢复 20 行。
- 浏览器日志：未发现 error 级日志；仅有 Vite 连接记录及既有 ECharts 提示，与本页改动无关。

## 问题历史

- P2（已修复）：初版 20 行表格把分页推到桌面首屏之外。已改为表体内部滚动并固定表头，复验截图中分页可见。
- 最新业务修正（已完成）：删除 H1、说明文案及前端新建权限能力，避免出现与后端权限来源冲突的创建入口。
- 当前无未处理的 P0、P1 或 P2 设计问题。

final result: passed

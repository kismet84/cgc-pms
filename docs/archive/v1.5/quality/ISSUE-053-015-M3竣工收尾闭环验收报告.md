# ISSUE-053-015 M3竣工收尾闭环验收报告

## 1. 结论

- 验收结论：通过；无阻塞。
- 本地开发/演示验收通过；未切正式入口，未提交、推送、创建PR或发布生产。
- `/project-closeout`为真实Clean-room V2页面；台账`LEGACY_ONLY=68 / V2_ACCEPTED=19 / V2_SOURCE_AVAILABLE=0`。
- 无数据库迁移或结构变更；补充可幂等重放的dev/demo身份、权限、阶段、合法关闭链和附件元数据。

## 2. 实现与边界

- 契约、薄服务和页面覆盖发起、分项验收、竣工验收、最终结算、尾款核验、质保、缺陷整改/复验、档案移交、关闭及trace。
- 查询与十类动作权限严格分离；结算绑定采用明确ID，不读取收入域候选，不扩大`revenue:operations:query`。
- ID统一为字符串，金额保持字符串；动作只消费后端状态，写成功或拒绝后回读overview/trace，不在前端推导“可关闭”。
- 分项验收、竣工验收和档案对象创建成功但附件上传失败时保留业务ID；重试仅上传附件，不重复业务POST。
- 服务端补强质保/缺陷责任人边界：必须是同租户启用用户及目标项目有效成员；同租户非项目成员明确拒绝。

## 3. 验证证据

| 层级 | 范围 | 结果 |
| --- | --- | --- |
| Ready | `ready-lint.ps1 ... ISSUE-053-015` | 通过 |
| 后端 | `ProjectCloseoutClosedLoopIntegrationTest` | 2项通过；含合法成员正向与非项目成员拒绝 |
| Demo | `load.ps1`与`verify.ps1` | 通过；21阶段、11账号、10动作、0越授、11成员、10阶段行、5附件类型、1合法关闭链、0非法责任人 |
| V2专项 | 收尾域、路由与导航单测 | 3文件14项通过；收尾专项5项 |
| V2全量 | `test:unit` | 26文件128项通过 |
| 类型/静态 | contracts、Vue、Lint、Clean-room、`git diff --check` | 通过；110个V2文件、12个契约文件 |
| 路由/构建 | 台账、build、bundle size | 通过；87路由，收尾页面JS 33.90kB，18个JS资产满足预算 |
| Live E2E | `verify-m3-live.ps1`中的收尾组 | 12项通过；三视口、axe、控制台、查询零写、10阶段动作真实账号 |

## 4. 发现与闭环

- 原CLOSED演示事实缺最终结算引用，结算状态、应收类型、未收余额与质保金额互相矛盾；已替换为`RECEIVABLE_CREATED/FINAL`结算、REGULAR与RETENTION两类足额回款、合法质保释放及完整CLEAN附件链。
- 质保和缺陷命令原先只写入`responsibleUserId`，未验证项目成员边界；已增加服务端校验和正负集成回归。
- 既有M3装载checkpoint会跳过已完成阶段内的新数据；阶段版本最终提升到10，可在同一唯一checkpoint上幂等升级，重载与验真通过。
- 初次全量Lint发现新页面5个未使用符号；移除后全量Lint通过。未达到同问题3次修复或4小时停止条件。

## 5. 回滚与后续

- 最小回滚：单路由恢复`LEGACY_ONLY`，回退收尾契约/服务/页面、路由生成器、服务端责任人校验、测试及M3收尾demo增量；不得删除既有验收、结算、回款、质保、缺陷、档案或关闭事实。
- 新增后续项0；关闭后续项0；后续项净变化0。
- `ISSUE-053-016`为既定M3全量退出门，不是本轮发现项。

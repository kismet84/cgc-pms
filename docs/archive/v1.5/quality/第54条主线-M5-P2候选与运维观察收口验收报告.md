# 第54条主线 M5：P2候选与运维观察收口验收报告

## 1. 验收结论

**通过。** A-06、A-07转`FROZEN`并补齐最小路线；A-09、A-10维持`FROZEN`并补齐与现有能力去重、单一外部目标等前置。旧node_modules ACL观察按“长期隔离且构建不引用”关闭；Trivy冷缓存继续`OBSERVATION`，不降低供应链扫描标准。

## 2. P2产品候选裁决

| 问题 | 裁决 | 重新准入前置 |
| --- | --- | --- |
| A-06报表中心平台化 | `FROZEN` | 单一报表、同步路径失败/超时证据、口径、权限、SLA、保留期 |
| A-07真实通知渠道 | `FROZEN` | 单一渠道与事件、供应商/Secret、同意与脱敏、重试和费用上限 |
| A-09总工程师技术域 | 维持`FROZEN` | 真实对象与角色，并与现有技术管理P0闭环完成去重 |
| A-10移动端与外部系统 | 维持`FROZEN` | 单一目标、沙箱、契约、数据所有权、安全、对账和停用 |

四项均禁止平台先行或多域捆绑；原问题仍是唯一Candidate载体。

## 3. 运维观察裁决

### 3.1 旧node_modules ACL残留

- 既有正式审计证据确认损坏依赖已隔离至`.codex-autopilot/runs/frontend-node_modules-acl-broken-20260713`，当前`frontend-admin/node_modules`已重建并可用。
- 隔离目录被忽略，不进入版本管理；当前构建只检查工作前端目录的`node_modules`，没有引用隔离路径。
- `git clean -fdn`未枚举该被忽略隔离目录，只显示本主线新增正式文档。
- 验收标准允许“确认长期隔离且构建不引用”；该分支已满足。为避免提权或误删用户环境，本轮不执行删除，问题关闭。

### 3.2 Trivy Java DB冷缓存

- CI已有UTC日期缓存键与历史缓存回退；既往供应链扫描成功。
- 本主线未取得同SHA冷缓存job，不能声称首次下载时延已消失。
- 问题继续`OBSERVATION`；只有获CI缓存治理授权后执行一次受控冷缓存运行。禁止跳过Java DB、改第三方未验证镜像或降低HIGH/CRITICAL门禁。

## 4. 收口统计

- 新增后续项：0。
- 关闭后续项：1（OPS-NODE-MODULES-ACL）。
- 后续项净变化：-1。
- 正式保留：A-06、A-07、A-09、A-10、OPS-TRIVY-COLD-CACHE共5项。
- 悬空项：0。

本阶段未删除文件、未修改CI、未触发远端job、未改业务代码或运行态。

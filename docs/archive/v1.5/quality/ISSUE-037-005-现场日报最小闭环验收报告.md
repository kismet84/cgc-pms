# ISSUE-037-005 现场日报最小闭环验收报告

## 裁决

- 结论：通过。
- 阻塞：无。
- 上线边界：本地开发环境通过；不发布生产、不连接生产库、`autoPush=false`。
- 能力口径：只完成项目/日期、施工内容、问题/延误、次日计划、草稿提交与附件，不代表现场管理平台完成。

## 正式交付物

- `site_daily_log` 表及 MySQL/H2 V141 migration，项目/日期唯一。
- 日报列表、详情、创建、修改、原子提交 API；日报本体无删除接口。
- `SITE_DAILY_LOG` 文件业务授权：查询与编辑权限分离，提交后附件只读。
- `/site/daily-log` 页面、项目/日期/状态筛选、草稿表单、提交与附件交互。

## 验收证据

| 项目 | 结果 |
| --- | --- |
| Ready lint / 门审 | lint 通过；4 个初审阻塞修订后终审通过 |
| 后端完整专项 | 4 类 32/32 通过 |
| 并发补修专项 | 控制器/服务/附件授权 19/19 通过 |
| 前端页面与路由 | 2 文件 33/33 通过 |
| 类型与静态检查 | `vue-tsc --noEmit`、`git diff --check` 通过 |
| 独立实现审查 | 初审发现状态竞态；原子 DRAFT 条件更新与行锁补修后复审通过 |
| Migration / 运行态 | MySQL 成功应用 V141；重建后稳定等待超过 180 秒；health/dev-login 通过 |
| 真实 API 闭环 | 草稿创建/修改、PNG 附件上传/列表/删除、提交成功；提交后正文更新和附件上传均被 400 拒绝；测试日报已清理 |

## 失败分类与修正

1. Ready 初审发现提交态附件读写口径、authority、验证命令和删除唯一语义冲突；取消日报本体删除并明确 query/edit 边界后通过。
2. 实现首轮测试缺少 Mockito 静态导入，归类为测试代码编译问题；补齐后通过。
3. 独立审查发现修改/提交与附件/提交竞态；正文改为 `status=DRAFT` 原子条件更新，附件写用 `SELECT ... FOR UPDATE`，复审通过。
4. 运行态首个 `.md` 附件被允许类型门禁拒绝；改用 PNG 样本后闭环通过，未修改文件安全策略。

## 剩余风险

- 生产经理当前 `data_scope=ALL` 是既有角色口径；若要求仅项目成员可见，应另拆数据范围治理。
- 人员、设备、材料、天气、定位、移动离线、质量安全与统计驾驶舱均未实施。
- 本会话缺少浏览器控制运行时，页面行为由 33/33 前端测试、类型检查、真实 API 和运行态入口共同裁决，不声称真实 DOM 点击验收。

# ISSUE-053-044 M8 全量自动化与代表性数据对账验收报告

> 日期：2026-07-27
> 分支：`master`
> 基线：`2908dd70b336a522b69640f2883a722ac1e9a5a6`
> 环境：本地 dev/test/demo
> 裁决：`PASSED`

## 1. 结论

M8 全量自动化、真实浏览器和代表性数据对账通过。V2、后端、迁移门和 23 阶段 Demo 均达到计划门槛；多角色、项目、审批、库存、金额和会计事实均由服务端返回或数据库回读证明。未发现跨租户、跨项目、越权、金额、库存、审批或会计守恒缺陷。

本报告不构成正式入口切换、Legacy 退役、目标环境验收或生产发布证据。

## 2. 自动化证据

| 验证 | 结果 |
| --- | --- |
| V2 unit | 55 files，397/397 通过 |
| Design system | 3 files，77/77 通过 |
| V2 type / lint | `type-check`、`lint:check` 通过 |
| 后端全量 | 275 suites，2324 tests，0 failure，0 error，3 skipped；`BUILD SUCCESS` |
| 全站 migration browser gate | 22 specs，91/91 通过 |
| 真实页面 E2E 第一组 | auth、M2 approval/dashboard、M3 delivery/projects，47/47 通过 |
| 真实页面 E2E 第二组 | shell、M3 closeout/quality-safety/technical，31/31 通过 |
| 真实页面 E2E 合计 | 78/78 通过 |
| Demo | package v2、dev、23/23 阶段装载与 verify 通过 |

## 3. 服务端事实对账

| 对账项 | 服务端回读结果 |
| --- | --- |
| 合法合作方 | 7 |
| 验收角色账户 | 8 |
| 工作流状态实例/配对 | 40 |
| 工作流动作权限 | 48 |
| 库存 | requested=100，ordered=100，received=100，issued=20，stock=80，差额=0 |
| 会计与发票分摊 | 差额=0 |
| 项目成员只读账户 | 账户=1，查询权限=2，写权限=0，项目成员关系=1 |
| E2E 临时项目清理 | `M3验收项目-*`剩余=0 |

## 4. 本轮根因修复

- Demo 种子补齐当前 schema 必填的投标、合作方和预算编码，并按当前角色主键写入验收账户。
- 新增最小只读项目成员验收账户；仅有 `project:query` 与 `project:member:list`，写权限保持 0。
- 真实 E2E 更新为当前页面和服务端事实；项目删除等待 DELETE 终态并再次查询服务端，避免测试提前退出留下数据。
- `V2Dialog` 去除重复 banner 语义；Dashboard 弹窗只在自身 panel 放开必要 overflow，修复下拉裁切。

## 5. 失败分类与处置

| 现象 | 分类 | 处置 |
| --- | --- | --- |
| 初次 Demo verify 无验收数据 | `environment_prerequisite` | 执行受控 23 阶段 loader 后复验 |
| Demo 种子与当前 schema/角色主键漂移 | `quality_or_security` | 最小修复种子并全量重载、回读 |
| E2E 仍使用旧页面/旧角色/旧数据假设 | `quality_or_security` | 改为当前 UI 与服务端权威事实 |
| 错用不存在的 lint 命令、loader 缺参数 | `tool_invocation` | 改用仓库标准命令和完整参数 |
| Dashboard 修改后 Vite 仍提供旧模块 | `environment_prerequisite` | 仅重建本地 `frontend-v2` 容器后复验 |

## 6. 收口

- 本轮修复并复验：上述 Demo、E2E、可访问性和弹窗问题。
- 超出范围并正式承接：0。
- 证据不足或无明确价值而关闭：0。
- 新增后续项：0；关闭后续项：0；后续项净变化：0；无载体悬空项：0。

`ISSUE-053-044`通过，`ISSUE-053-045`进入 Ready。未 commit、push、PR、切正式入口、退役 Legacy 或操作生产。

# ISSUE-037-006 库存项安全库存阈值与补货建议联动验收报告

## 结论

- Decision：通过。
- 阻塞：无。
- 上线边界：仅通过本地 dev/test 验收；未发布生产、未连接生产数据库、未 push。
- 完成范围：按租户内仓库+物料库存项维护安全阈值，统一低库存 KPI、当前预警和采购申请预填数量。

## 实施范围

- V142 MySQL/H2 为 `mat_stock` 增加 `safety_stock_qty DECIMAL(18,4) NOT NULL DEFAULT 10.0000`。
- 新增 `inventory:stock:edit`，仅新增授予正式 `PURCHASE_MANAGER`；ADMIN/SUPER_ADMIN 由控制器角色兜底。
- 阈值更新按 stock→warehouse→project 执行租户、启用仓库和 `ProjectAccessChecker` 校验，并复用 `MatStock.@Version` 乐观锁。
- 库存台账显示并维护阈值；KPI、当前预警和补货建议使用同一阈值，补货数量固定输出四位小数。
- 零库存继续不计入低库存；未实现最大库存、供货周期、预测、全量补货工作台、跨仓调拨或自动下单。

## 验收证据

- Ready lint：`ISSUE-037-006` pass；独立准入与最终实现复核均 PASS。
- 后端：`MatStockServiceTest,MatStockControllerTest` 共 48/48 通过，覆盖默认阈值、动态 KPI、负数/精度、跨租户、无项目访问、禁用仓库、伪造 ID、采购经理可写和只读拒绝。
- 前端：`stock-production.test.ts` 4/4；`pnpm type-check` 通过。
- 静态：`git diff --check` 通过；SQL 安全扫描未新增命中。
- 运行态：重建后稳定等待 180 秒；backend health 200、frontend 200、dev-login 302 并跟随至页面 200。
- 真实 API（SUPER_ADMIN）：库存量 42，阈值 `43 → 0` 时低库存 KPI `2 → 1`，差值精确为 1；随后恢复原阈值 10。
- 真实 API（PURCHASE_MANAGER）：`demo_alert_purchase` 具备 `inventory:stock:edit`，成功更新阈值 `10 → 11`，随后恢复为 10。

## 风险与回滚

- 非阻塞：后续可增加阈值更新与出入库真实并发专项；当前 `@Version + updateById` 冲突返回 `STOCK_CONCURRENT_CONFLICT`。
- 回滚：回退应用后移除 V142 新增角色权限、菜单按钮和 `safety_stock_qty` 列；不改写历史 migration。
- 测试数据：真实 API 仅临时修改既有本地 dev 阈值，已恢复原值，无残留业务数据。

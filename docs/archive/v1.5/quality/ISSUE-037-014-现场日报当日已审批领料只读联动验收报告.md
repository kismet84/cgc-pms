# ISSUE-037-014 现场日报当日已审批领料只读联动验收报告

## 结论

- 结论：通过
- 阻塞：无
- 上线范围：本报告仅裁决 Ready Issue 的代码与自动化验证，不包含生产发布。

## 实现与边界

- 日报详情在项目访问校验后，按同租户、同项目、同日期、`APPROVED` 且 `stock_out_flag = 1` 聚合领料单。
- 领料单、领料明细和物料各一次批量读取；无命中返回空列表。
- 返回字段仅包含领料单 ID/编号、明细 ID、物料 ID/名称/单位、领料数量和使用部位；数量使用 `toPlainString()`。
- 前端仅在日报查看模式展示领料明细和空态，无写入入口，无“已安装”语义。
- 未修改数据库 migration、领料/库存/成本模块、权限模型或状态机。

## 验收证据

- `TEST_JWT_SECRET=<test-only-strong-secret>; .\mvnw.cmd "-Dtest=SiteDailyLogServiceTest,SiteDailyLogControllerTest" test`：通过，10 tests，0 failures，0 errors。
- `pnpm test:unit src/pages/site/__tests__/daily-log.test.ts`：通过，6 tests。
- `pnpm type-check`：通过。
- `git diff --check`：通过。
- 首次后端执行中，服务层 7 tests 已通过，控制器测试因默认测试 JWT 密钥仅 144 bit 而启动失败；归类为环境前置后，注入仅用于测试的合规密钥复跑通过。

## 独立复核

- 阻塞 findings：无。
- 非阻塞 findings：无。
- 复核结论：通过。
- 复核覆盖：查询隔离与状态条件、访问校验时序、批量读取、敏感字段与数量精度、前端只读语义和修改路径边界。

## 剩余风险与回滚

- 剩余风险：无阻塞风险；本轮未执行真实浏览器人工视觉验收。
- 回滚：移除日报详情的 `requisitions` 聚合字段、日报侧 VO 和前端展示区；不涉及数据回滚。

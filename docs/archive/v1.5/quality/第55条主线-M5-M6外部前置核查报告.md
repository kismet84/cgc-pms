# 第55条主线 M5/M6：本机隔离验证与外部前置核查报告

核查日期：2026-07-27

核查结论：`LOCAL_STAGING_PASS / TARGET_ENV_ABSENT / PRODUCTION_BLOCKED`

## 1. 核查范围

- `DATA-001/ISSUE-049-001`
- `AUDIT-PROMETHEUS-SCRAPE-AUTH/OBS-002`
- `REL-CREDENTIAL-ROTATION`
- `REL-FILE-RESCAN`
- `REL-TARGET-SHA-REVALIDATION`
- M6同SHA、目标环境、备份恢复与上线裁决

本报告在现有本机开发栈外只创建一次性测试库、测试账号和恢复库，完成可安全执行的 M5/M6 验证；不连接生产、不读取或记录 Secret 明文、不发布、不写生产业务数据。

## 2. 本机隔离验证

| 验证 | 结果 |
| --- | --- |
| M6只读、租户、付款闭环、金额守恒 | `M6ReadOnlyCanaryTest`、`TenantIsolationTest`、`PaymentApplicationClosedLoopIntegrationTest`、`PaymentFinancialConsistencyTest`共35项通过 |
| JWT与改密 | `AuthControllerTest`、`ProfileControllerTest`按污染顺序36项通过；覆盖旧凭据版本拒绝和密码路径 |
| 后端全量 | Maven `verify`共275套件、2324项，0失败、0错误、3跳过，`BUILD SUCCESS` |
| MySQL/Flyway | 一次性库、库级临时账号、TLS `REQUIRED`；`FlywayMySqlSmokeTest`与`BaselineMySqlSmokeTest` 2/2通过，当前版本V234 |
| 备份恢复 | 现有脚本完成开发库只读备份和两次唯一恢复库演练；表数`200/200`、Flyway`234/234`、项目`16/16`，恢复脚本健康检查实测`200 tables PASS` |
| 文件扫描 | 现有ClamAV真实INSTREAM：干净内容`OK`、EICAR `FOUND`、不可达端口失败关闭 |
| 主线治理 | `test-mainline-owner-flow.ps1 -Profile HighRisk`通过 |
| 清理 | 两个恢复库、一个基线库和临时账号均删除并复查残留0；现有开发库和容器未修改 |

本轮发现并修复四项交付门遗漏：

1. M6只读金丝雀仍使用V85已删除的`userId=1`，改为现存测试用户`999`。
2. Profile测试夹具恢复管理员时漏恢复用户名，导致前序认证测试污染；补齐用户名恢复。
3. Windows恢复脚本健康检查错误使用反斜杠转义SQL引号，导致恢复成功后误报WARNING；修正并用第二个恢复库复验。
4. 主线编号契约只排除M阶段计划，误把既有“支线”计划当成重复主计划；补齐子主线例外并保持重复主计划拦截。

以上只增强测试和恢复验证，不改变生产业务逻辑、migration或现有数据。

## 3. 外部前置矩阵

| 稳定键 | 当前缺失证据 | 允许启动条件 | 当前裁决 |
| --- | --- | --- | --- |
| `DATA-001/ISSUE-049-001` | 生产等价脱敏副本；应用、BI、报表、ETL无消费者签认 | 明确副本来源、脱敏规则、消费者清单、恢复演练与独立DROP migration授权 | `BLOCKED` |
| `AUDIT-PROMETHEUS-SCRAPE-AUTH` | 目标网络边界、机器身份方案、Secret载体、轮换责任人 | 选定机器认证，完成无/错凭据拒绝、普通用户拒绝、认证抓取、旧凭据失效和非公网UI验证 | `NEEDS_CONFIRMATION` |
| `REL-CREDENTIAL-ROTATION` | 可识别生产环境、Secret载体、双人复核角色 | 首次正式部署前绑定同一版本执行新凭据生效、旧凭据失效、功能验证和脱敏复核 | `RELEASE_GATE` |
| `REL-FILE-RESCAN` | 生产业务表与对象存储守恒清单、存量复扫执行器、隔离处置权限 | 首次正式部署前逐对象真实扫描；感染、不确定、扫描器不可用均fail-close | `RELEASE_GATE` |
| `REL-TARGET-SHA-REVALIDATION` | 可识别生产环境、真实角色、发布与回滚窗口 | 最终HEAD通过required checks；首次部署时继续完成目标迁移、租户/项目/金额/文件正负样本及恢复演练 | `RELEASE_GATE` |

## 4. 当前事实

- 用户确认当前没有可识别生产环境；因此不再索取服务器、账号或Secret，把三项生产门登记为首次正式部署前置。
- 当前实现只有新上传真实扫描与下载`CLEAN`门，没有存量对象复扫执行器、对象/数据库守恒编排、隔离与重试审计；本机ClamAV通过不能关闭`REL-FILE-RESCAN`。
- 本机隔离证据不能替代生产环境凭据轮换、真实角色、生产对象和发布回滚证据。
- `codex/mainline-55-m8-release-gates`的最终HEAD仍须通过完整同SHA门禁；任何后续提交都会使旧CI证据失效。
- PR与合并未获单独授权；生产就绪裁决保持“不通过、禁止上线”。

## 5. 首次正式部署前置

当前无需用户提供未知环境信息。未来真正建立生产环境并准备首次部署时，必须补齐：

1. 识别生产环境及其网络、数据库、对象存储和发布入口，并分别授权允许的验证或写入动作。
2. 提供Secret载体和轮换责任人，不提供Secret明文。
3. 提供生产等价脱敏副本及消费者签认责任人。
4. 指定存量对象清单来源、扫描器和隔离处置责任。
5. 冻结待发布SHA，完成同SHA CI、备份恢复、真实角色、回滚及双人复核。

缺任一项不得上线；本机验证结果只能复用为准备证据。

## 6. 零悬空

- 新增后续项：0
- 关闭后续项：0
- 后续项净变化：0
- 当前活动事项：15
- 无载体悬空项：0

# 第55条主线支线：六项产品候选事实复核与决策门治理任务计划书

**Goal:** 对当前 HEAD 的 `A-02/A-03/A-04/A-09/A-06/A-07` 六项产品候选逐项完成事实复核、决策门治理、合格任务实施判断、验证与零悬空收口；缺少不可替代业务事实的事项保持原稳定键和状态，不制造 Ready。

**Architecture:** 复用现有库存、日报、进度、报表、站内通知和 M3 技术管理事实链，以 `docs/backlog/current-issues.json` 为唯一活动状态源。本支线执行键 `SIX-CANDIDATE-DECISION-GATE-CLOSEOUT` 不是新增全局 Issue；只有真实样本、权威来源、量化价值、权限边界、最小验收和回滚同时成立时才生成一个独立 Ready。当前六项均未过门，因此最小方案是治理回写，不新增业务代码、数据模型、外部集成或平台抽象。

计划日期：2026-07-28

计划状态：`CLOSED / REVIEW_PASS / 0_READY / 0_BUSINESS_IMPLEMENTED / 1_TEST_FIXTURE_FIXED`

编制基线：`master@0c1ad645f38f1f7c3ed9d47598720bf18fb1a91c`

支线执行键：`SIX-CANDIDATE-DECISION-GATE-CLOSEOUT`

## 1. 范围与非目标

范围：

1. 按 `A-02 → A-03 → A-04 → A-09 → A-06 → A-07` 串行复核。
2. 每项核对已实现能力、真正缺口、用户价值、权威来源、角色/租户/项目/对象范围、外部依赖、最小闭环和禁止扩建范围。
3. 每项只允许进入“形成 Ready 并实施”“保持 FROZEN/NEEDS_CONFIRMATION”“有据关闭”之一。
4. 同步主计划、计划索引、问题源、Current Focus、项目地图和正式验收报告。

非目标：

- 明确排除 `A-10`；不修改其状态、候选契约、来源引用或移动端/外部系统范围。
- 不把演示数据、测试夹具、历史均值、占位 sender、同步 CSV 或既有 M3 对象推断成新需求证据。
- 不建设预测平台、现场管理平台、通用排程、BI、消息中台或重复技术管理域。
- 不修改生产业务代码、数据库、Ready、AutoPilot 控制面、部署或生产环境。
- 本支线不授权 commit、push、PR 或合并。

## 2. 当前 HEAD 决策表

| 顺序 | 稳定键 | 已实现能力 | 决策门缺失事实 | 本轮裁决 |
| ---: | --- | --- | --- | --- |
| 1 | `A-02` | 自然日供应商提前期、同项目跨仓余量、审批在途、原子调拨、30/90自然日净领料 | 权威工作日历、明确需求对象、完整周期真实样本、误差阈值、量化收益 | 保持 `NEEDS_CONFIRMATION` |
| 2 | `A-03` | 日报草稿/提交、人工天气/人数、材料/计划/质量安全只读联动 | 单一高频角色、一个权威人员/设备事实源、真实样本、隐私与保存规则 | 保持 `FROZEN` |
| 3 | `A-04` | 版本计划、单前置 FS、项目隔离、环与状态门禁、并发控制 | 现有单前置无法表达的真实样本、改期成本、最小关系模型与迁移规则 | 保持 `FROZEN` |
| 4 | `A-09` | 方案、图纸/版本、会审、RFI回复复核、交底、施工依据、归档与 Trace | M3 无法承接的真实 Submittal 或非 RFI 重大技术问题及独立责任链 | 保持 `FROZEN` |
| 5 | `A-06` | 权限过滤报表目录、多个同步 CSV 和业务驾驶舱 | 单一报表、同步超时/超量证据、数据规模、SLA、下载保留责任 | 保持 `FROZEN` |
| 6 | `A-07` | 租户/用户隔离站内通知、订阅收件人、发送记录、重复站内通知抑制 | 一个真实渠道与事件、供应商/沙箱、Secret、同意、费用、SLA | 保持 `FROZEN` |

六项均有潜在用户价值，但当前没有一项同时满足 Ready 前置；也没有证据证明其剩余范围已被完整覆盖或无独立价值，因此不关闭稳定键。

## 3. 解冻责任与最小验收

| 稳定键 | 外部事实责任来源 | 解冻所需最小事实 | 通过后允许的最小闭环 |
| --- | --- | --- | --- |
| `A-02` | 采购/物资负责人、项目计划责任人 | 一个日历权威源、一种需求对象、一个完整周期样本、误差和收益门槛 | 只读建议→人工覆盖→采购申请预填→服务端回读；不自动下单 |
| `A-03` | 一线生产负责人、对应事实系统责任人、隐私责任人 | 一个高频角色、一个权威源、真实使用样本、权限/保存/审计规则 | 单一只读联动或最小录入链；提交日报不可被反向覆盖 |
| `A-04` | 项目计划负责人 | 一个单前置无法表达的真实项目、量化改期痛点、关系和基线规则 | 样本所需的最小依赖模型、DAG环检测、项目隔离、CAS与回滚 |
| `A-09` | 总工程师、技术提交方和审查方 | 一个真实 Submittal 或非RFI事件，含状态、责任、时限、附件、审批 | 优先扩展 M3；仅在对象语义确实不同后增加最小对象 |
| `A-06` | 报表业务口径负责人、数据安全责任人 | 一个同步失败报表、规模/耗时样本、目标SLA、权限和保留期 | 创建任务→服务端生成→状态→授权下载→到期删除→审计 |
| `A-07` | 业务事件负责人、IT/安全/合规、供应商责任人 | 一个渠道/事件、账号或沙箱、Secret载体、同意、费用和SLA | 幂等发送→回执→有限重试→人工失败处置→停用→审计 |

## 4. 阶段、允许范围与退出门

| 阶段 | 结果 | 退出条件 |
| --- | --- | --- |
| S0 当前事实冻结 | `PASSED` | 当前 HEAD、六个稳定键、相关代码/迁移/测试和历史验收可复读 |
| S1 产品决策门 | `PASSED / 0_READY` | 六项逐项完成三选一裁决；无猜测性解冻 |
| S2 合格任务实施 | `NOT_APPLICABLE / TEST_FIXTURE_FIXED` | 无候选通过 Ready 前置；修复A-04既有回归的测试身份隔离，不改业务逻辑 |
| S3 治理一致回写 | `PASSED` | 问题源、主计划、索引、Current Focus、项目地图和报告一致 |
| S4 验证与 Reviewer | `PASSED` | Standard计划门、Ready lint、JSON/引用/差异检查和独立复核通过 |

允许修改：

- `docs/archive/v1.5/plans/第55条主线-支线-六项产品候选事实复核与决策门治理任务计划书-2026-07-28.md`
- `docs/archive/v1.5/quality/第55条主线-支线-六项产品候选事实复核与决策门治理验收报告-2026-07-28.md`
- `docs/backlog/current-issues.json`
- `docs/backlog/current-focus.md`
- `docs/archive/v1.5/plans/README.md`
- `docs/archive/v1.5/quality/README.md`
- `docs/archive/v1.5/plans/第55条主线-全量审计整改与生产就绪收口任务计划书.md`
- `docs/product-intelligence/project-map.md`
- `backend/src/test/java/com/cgcpms/subcontract/SubTaskControllerTest.java`

禁止修改：

- `backend/src/main/**`、其他后端测试、`frontend-admin/**`、`frontend-admin-v2/**`、数据库迁移和运行环境。
- `docs/backlog/ready-issues.md`、`A-10` 对象、发布门和生产事实。

## 5. 验收、失败分类与证据

验收命令：

1. `pwsh -NoProfile -File scripts/codex-autopilot/test-mainline-owner-flow.ps1 -PlanPath "docs/archive/v1.5/plans/第55条主线-支线-六项产品候选事实复核与决策门治理任务计划书-2026-07-28.md" -Profile Standard`
2. 点源 `scripts/codex-autopilot/autopilot-ready.ps1` 后解析 `docs/backlog/ready-issues.md`，确认当前 Ready 为 0。
3. 解析 `docs/backlog/current-issues.json`，确认六项状态守恒、活动项仍为12且 `A-10` 内容不变。
4. 检查本计划、报告和索引引用均存在。
5. 运行六域既有专项测试，证明引用的当前能力仍在当前 HEAD 成立；`SubTaskControllerTest`必须可独立及组合执行。
6. `git diff --check`。
7. 独立 Reviewer 只读复核最终 diff。

失败分类：工具参数或脚本调用错误归 `tool_invocation`；依赖、构建产物或本地测试前置缺失归 `environment_prerequisite`；代码、测试、权限、状态或文档契约失败归 `quality_or_security`；证据不足归 `unknown`，补证据前不得强行裁决。

## 6. 风险与回滚

| 风险 | 控制 | 回滚 |
| --- | --- | --- |
| 用既有能力冒充高级需求已完成 | 明确“已实现能力”和“剩余候选”对象边界 | 回退本支线文档和六项新增来源引用 |
| 用测试夹具制造真实业务样本 | 真实样本必须来自相应业务责任人并可回放 | 保持原状态，不生成 Ready |
| 候选平台化膨胀 | 每次只允许一个样本、一个对象、一个最小闭环 | 删除未过门的 Ready/实现，不迁移数据 |
| 外部渠道或隐私越界 | 缺账号、Secret、同意、保留规则即 fail-close | 保持 FROZEN；不产生外部副作用 |
| A-10 被误纳入 | 校验其 JSON 对象在修改前后语义一致 | 恢复该对象原文 |
| A-04回归依赖其他测试预置用户 | 复用`JwtHttpTestTokenFactory`确保受控用户与凭据版本一致 | 回退单个测试文件，不影响业务数据 |

本轮仅治理文档、来源引用和一个测试身份夹具；无生产业务代码、数据库或外部状态回滚。

## 7. 零悬空

- 实际形成 Ready：0；实际业务实施：0；修复既有测试夹具：1。
- 新增后续项：0；关闭后续项：0；净变化：0。
- 重复项：0；无载体悬空项：0。
- 六项均由原稳定键继续正式承接，解冻责任和验收标准已明确。
- `A-10` 排除且不变。

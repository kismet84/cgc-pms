# 第26条主线 M6 B8 特殊审批实例留存台账

报告日期：2026-07-08  
报告类型：隔离测试记录留存台账 / B8 剩余风险归档  
报告边界：本台账仅复用 `docs/quality` 既有正式验收报告中的已见证据，目的是为 B8 专用审批实例和对应业务记录建立正式留存口径，避免后续误判为脏数据、误删或重复治理；未修改代码、配置、测试源码、运行环境或 Git 状态。  
报告路径：`D:\projects-test\cgc-pms\docs\quality\mainline-26-m6-b8-isolated-test-instance-ledger-2026-07-08.md`

## 1. 总体结论

- 结论：**通过归档**
- 阻塞/非阻塞：**非阻塞**
- 归档目的：将 M6.5 剩余风险中的 B8 特殊审批实例/业务记录明确登记为“隔离测试留存记录”，后续不得按普通脏数据或漏清理问题处理。

依据：

1. `docs/quality/mainline-26-m6-4-b8-final-browser-verification-2026-07-08.md` 已确认本轮专用领料申请已创建、提交并完成最终审批，且库存净额已回补。
2. `docs/quality/mainline-26-m6-blocker-remediation-acceptance-2026-07-08.md` 已将该记录定性为“无法物理删除、需隔离留痕”的非阻塞剩余风险，并明确建议保留可检索台账。

## 2. 留存对象

### 2.1 业务记录

| 项目 | 值 |
| --- | --- |
| 业务类型 | 专用领料申请 / 已审批业务记录 |
| 业务 ID | `2074661504812953602` |
| 业务编号 | `REQ-20260708-002` |
| 最终业务状态 | `approvalStatus=APPROVED` |
| 其他已见状态 | `stock_out_flag=1` |

### 2.2 工作流实例

| 项目 | 值 |
| --- | --- |
| workflow instance ID | `2074661549155135489` |
| 最终实例状态 | `instanceStatus=APPROVED` |
| 说明 | 该实例对应上述专用领料申请，属于 B8 最终补测产生的隔离测试审批实例 |

## 3. 隔离标识与证据

### 3.1 已登记的隔离标识

| 标识类别 | 已见证据 |
| --- | --- |
| 明细隔离前缀 | `M6-B8-1783472755924` |
| 明细字段 | `useLocation=M6-B8-1783472755924`，`batchNo=M6-B8-1783472755924` |
| 库存有效提交流水 | `sourceType=M6-B8-1783472755924-retry` |
| 审批自动出库回补流水 | `sourceType=M6-B8-1783472755924-approval-stock-rollback` |
| 业务关联出库流水 | `sourceType=MAT_REQUISITION`，`sourceId=2074661504812953602` |

说明：

1. 领料编号由系统自动生成，不能依赖编号前缀作为唯一隔离依据。
2. 后续识别该留存记录时，应优先组合使用 `业务 ID + workflow instance ID + 明细隔离前缀 + sourceType`，不要只靠单一字段模糊判断。

### 3.2 库存回补与最终净额证据

| 项目 | 值 |
| --- | --- |
| 最终库存回补证据 | `available_qty=42.0000` |
| 补测中有效入库结果 | `availableQty=42.0100` |
| 反向回滚结果 | `availableQty=42.0000` |
| 审批自动出库后回补结果 | `availableQty=42.0000` |

结论：本轮 B8 专用实例虽保留，但库存净额已按正式验收报告回补，不应再被作为“库存未回滚”问题重复上报。

### 3.3 证据目录

正式证据目录：

`C:\Users\L1597\AppData\Local\Temp\cgc-pms-mainline-26-m6-4-b8-final-2026-07-08`

说明：本台账只登记证据目录位置，不把临时截图、日志或缓存复制进 `docs/quality`。

## 4. 留存原因与删除边界

1. 该专用审批实例和对应业务记录已进入最终审批通过状态，正式报告已明确：**无法通过常规 API 物理删除**。
2. 该记录属于为关闭 B8 而创建的隔离测试样本，不属于常规业务脏数据，也不应被后续“数据清理”“审批待办收口”或“库存异常排查”流程顺手删除。
3. 若未来需要处理该记录，必须先明确处理目标是“合规清理历史留存测试记录”，并重新论证：
   - 是否存在安全的物理删除路径；
   - 是否会破坏审批/库存审计链路；
   - 是否需要同步处理业务表、工作流实例表与库存流水。
4. 在未形成新的正式裁决前，本台账默认结论是：**保留，不删除**。

## 5. 后续查询原则

后续如需定位或解释该记录，优先按以下顺序检索：

1. 直接按业务 ID `2074661504812953602` 查询业务主记录。
2. 直接按 workflow instance ID `2074661549155135489` 查询审批实例。
3. 按明细隔离前缀 `M6-B8-1783472755924` 查询 `useLocation`、`batchNo`。
4. 按库存流水 `sourceType` 检索：
   - `M6-B8-1783472755924-retry`
   - `M6-B8-1783472755924-approval-stock-rollback`
   - `MAT_REQUISITION` + `sourceId=2074661504812953602`

查询口径：

- 若查询结果命中上述 ID/前缀/流水，并且状态为 `instanceStatus=APPROVED`、`approvalStatus=APPROVED`，应优先判断为“已登记的 B8 隔离测试留存记录”。
- 不得仅因该记录仍存在于业务表、审批实例表或库存流水中，就直接判定为数据污染或漏清理。

## 6. 清理与误报处理原则

### 6.1 清理原则

1. 本台账登记对象默认不纳入常规清理任务。
2. 常规 API 无法物理删除，不得让运维、测试或业务同学以页面操作反复尝试“手工删掉”。
3. 若后续专项要求治理历史测试留存，必须单列任务并先出正式方案，再决定是否触达该记录。

### 6.2 误报处理原则

出现以下场景时，应按“已知留存记录”处理，而不是直接报缺陷：

1. 审批或领料查询中再次看到业务 ID `2074661504812953602`。
2. 工作流实例查询中再次看到 instance ID `2074661549155135489`。
3. 库存流水中看到本台账登记的 `sourceType`。
4. 核对库存时发现该链路最终净额已经回到 `available_qty=42.0000`。

建议处理动作：

1. 先引用本台账与 `mainline-26-m6-4-b8-final-browser-verification-2026-07-08.md`。
2. 再确认是否出现与本台账不一致的新事实，例如状态被改写、库存净额不再为 `42.0000`。
3. 只有出现新的偏差事实时，才应升级为新增问题；否则按“留存已备案”关闭误报。

## 7. 关联正式报告

1. `D:\projects-test\cgc-pms\docs\quality\mainline-26-m6-4-b8-final-browser-verification-2026-07-08.md`
2. `D:\projects-test\cgc-pms\docs\quality\mainline-26-m6-blocker-remediation-acceptance-2026-07-08.md`
3. `D:\projects-test\cgc-pms\docs\quality\mainline-26-m6-3-4-browser-verification-2026-07-08.md`

## 8. 需要确认项

- 需要确认：正式审批意见中的 comment 文本原值未在本次可读正式报告中逐字展开，本台账仅能确认“审批意见前缀已用于隔离”，不能补写其具体内容。

## 9. 最终结论

- B8 特殊审批实例/业务记录留存台账：**已归档**
- 是否满足“避免后续误判为脏数据或漏清理”目标：**满足**
- 当前处理口径：**保留为隔离测试记录，不纳入常规清理；后续命中时按本台账解释，不按新增脏数据处理**

# 第 13 条主线收口报告（2026-07-03）

## 1. 结论

- 结论：通过
- 阻塞 / 非阻塞：非阻塞
- 当前判断：可进入上线准备

第 13 条计划要求的 `M1-M5` 已完成闭环。当前结论基于本周证据，不沿用旧测试数量、旧审计结论或旧“已完成”表述。核心依据是：M1 门禁通过、M2 历史阻断项复核通过、M3 核心 E2E 通过、M4 文档索引校准完成、M5 决策包已形成。

## 2. 依据

### 2.1 M1：质量门禁复核与证据固化

1. 后端 verify 通过：`144` 个 surefire XML suite，`1397 tests`，`0 failures`，`0 errors`，`0 skipped`。
2. 前端 type-check 通过。
3. 前端 build 通过：`4411 modules transformed`。
4. 前端 unit 通过：`54 files`、`266 tests`。

### 2.2 M2：历史阻断项逐条重判

1. 大多数历史阻断项已关闭。
2. `deploy/.env.example` 的 `ENC(` 残留已复核关闭。
3. 前端完整门禁“需要确认”已被 M1 当前证据覆盖为通过。

### 2.3 M3：核心业务链路最小回归

执行命令：

```powershell
pnpm exec playwright test e2e/login.spec.ts e2e/dashboard.spec.ts e2e/contract.spec.ts e2e/cost-regression.spec.ts e2e/cost-reconciliation.spec.ts e2e/payment-invoice.spec.ts e2e/approval.spec.ts e2e/inventory.spec.ts e2e/procurement.spec.ts e2e/variation.spec.ts --reporter=list
```

执行结果：

1. `36 tests`
2. `36 passed`
3. 覆盖登录、驾驶舱、合同、成本、付款、审批、库存、采购、签证主链路

### 2.4 M4：文档索引校准

1. `docs/README.md` 已新增第 13 条当前有效结论入口。
2. 历史审计和历史验收报告保留，但明确为历史记录，不再冒充当前放行依据。

### 2.5 M5：上线准备决策包

1. 已形成当前统一结论。
2. 已列出非阻塞剩余风险。
3. 已同步执行产物和未来计划状态。

## 3. M3 期间修复摘要

本节只保留对结论有意义的摘要，不展开逐行修复明细：

1. 修复登录单测密码复杂度测试数据。
2. 修复后端测试断言和限流隔离问题。
3. E2E 认证态补齐 `sessionStorage`，并新增 / 调整 auth helper。
4. 修复 `dashboard`、`cost`、`contract`、`payment`、`approval`、`inventory`、`procurement`、`variation`、`login` 的 E2E 选择器、数据前提或稳定断言。
5. 审批 E2E 已通过 API 创建并提交可重复合同审批样本，再在 UI 侧完成同意 / 驳回，并回读后端验证状态与记录。

## 4. 通过 / 不通过 / 阻塞 / 非阻塞判断

| 项目 | 结论 | 依据 |
| --- | --- | --- |
| 是否通过 | 通过 | `M1-M5` 均已完成且有当前证据 |
| 是否阻塞 | 非阻塞 | 当前剩余问题不影响“是否进入上线准备”的判断 |
| 是否可进入上线准备 | 可进入 | 质量门禁、历史 blocker、核心链路、文档索引均已闭环 |

## 5. 剩余风险

1. 当前工作区未提交改动较多，`git status --short` 为 `53` 条，提交边界和最终合入批次仍需单独收敛。
2. 项目经理标签切换后，当前环境未返回内容区数据；当前归类为非阻塞，但上线前仍建议补确认。
3. 当前没有独立 `project.spec.ts`，项目链路仍缺单独规格化 E2E。
4. 部分真实写操作深度仍可继续增强，当前主链路已覆盖最小闭环，但未覆盖所有深水区异常路径。

## 6. 不纳入本次收口

1. 业务代码继续扩面改造
2. 全量真实写路径深度补齐
3. 中长期报表、移动端、通知运营化建设
4. 工作区未提交改动的提交 / 合并 / 推送动作

## 7. 主负责人建议

1. 按“通过 / 非阻塞 / 可进入上线准备”执行后续排期。
2. 上线准备阶段优先处理提交边界收敛、项目链路补确认和真实写操作深度增强。
3. 后续若新增数据库、认证授权、生产配置类高风险改动，应重新开当周验证，不沿用本报告直接放行。

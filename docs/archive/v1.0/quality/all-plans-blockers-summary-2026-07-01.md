# 全部计划阻塞项汇总报告（截至 2026-07-01）

## 结论

当前 `docs/plans` 目录内 13 份计划/专项中：

1. **已确认仍有当前阻塞的主线**：无全局现行阻塞。`第10条主线-权限字典治理与历史权限码治理裁决任务计划书` 的第二版首包 `notification:list` 已实施并归档通过，旧的“第二版阻塞、不得进入 migration”口径已失效；10A 全量历史权限码治理仍未完成，但剩余候选只存在单项准入阻塞，不能批量推进。
2. **已关闭或已按首包收口、当前无主负责人层面阻塞的计划**：`第8条`、`10B 首包`、`10C 首包`、`10D 首包`。
3. **文档中存在条件性阻塞规则，但当前未命中或已绕开**：`10B/10C/10D` 的主计划和首包计划。
4. **已完成核实并确认关闭的旧主线**：`第3条`、`第5条`、`第6条`、`第7条` 均已有归档或等价收口证据，当前不再属于“未核实状态”。

本报告按三类信息分别记录：

- `当前已确认阻塞`：结合本线程已知进展，当前确实拦住后续执行或实现的事项。
- `文档预设阻塞条件`：计划书里写明“若发生则阻塞”的条件，但本线程未确认已触发。
- `未核实事项`：本线程没有重新验真，只保留挂账。
- `工作区残余风险`：V113/V114/V115 MySQL/H2 migration 与 `MigrationIntegrityTest` 已通过 verifier，但仍需纳入后续提交范围，最终验收要以提交后的定向检查为准。

## 汇总表

| 计划 | 当前状态 | 当前已确认阻塞 | 文档预设阻塞条件 | 未核实事项 |
| --- | --- | --- | --- | --- |
| 第3条主线-生产经理时效文案增强 | 已收口 | 无 | 计划书未抽出单独阻塞章节；需按任务验收结果判定通过/不通过、阻塞/非阻塞 | 已由 `production-timing-and-approval-business-entry-closure-2026-07-01.md` 收口为“后续增强 / 非阻塞” |
| 第5条主线-审批详情业务入口增强 | 已关闭 | 无 | 文档明确该主线是“后续增强，不是阻塞修复”；未见单独阻塞项章节 | 已由 `approval-business-entry-acceptance-2026-07-01.md` 归档关闭；采购/分包按 ID 深链已拆到第 6 条 |
| 第6条主线-审批业务单据深链定位增强 | 已关闭 | 无 | 负向权限样本不是硬门禁；若没有现成账号/数据组合，可由安全静态结论兜底 | 已由 `approval-business-entry-deeplink-acceptance-2026-07-01.md` 归档关闭；剩余仅非阻塞观察项 |
| 第7条主线-审批中心生产化闭环增强 | 已关闭 | 无 | 权限感知隐藏入口不是本主线阻塞项；跨租户和复杂 `data_scope` 可在无样本时由安全静态复核兜底 | 已由 `approval-center-production-closure-2026-07-01.md` 与补充收口 `approval-center-production-risk-closure-2026-07-01.md` 关闭 |
| 第8条主线-审批中心权限账号矩阵与权限感知入口优化 | 已收口 | 无 | 文档明确第二租户、复杂 `data_scope`、项目级强隔离浏览器硬验不作为本线阻塞 | 本线程未重新打开执行证据，但主线程上下文已确认第8条已关闭 |
| 第9条主线-权限配置中心与入口注册体系 | 已关闭 | 无 | 不通过条件：权限清单与运行态权限集明显不一致、三类业务映射分叉、`workflow-only` 泄露目标业务字段、admin 三类入口回退、扩项为可编辑平台/全站注册/付款申请接入 | 已由 `approval-center-permission-registry-closure-2026-07-01.md` 归档关闭；计划书状态需同步 |
| 第10条主线（10A）-权限字典治理与历史权限码治理裁决 | 阶段性收口（V113/V114/V115 已关闭） | 无全局现行阻塞；剩余候选保留单项准入阻塞 | 计划书仍保留高危项逐项确认、不得批量清理其他历史权限码等边界 | `alert:edit` 曾阻塞首包；第二版候选样本 `notification:list` 已实施并归档通过，现为非阻塞收口 |
| 第10B专项-项目级强隔离主计划 | 主计划已收口 | 无 | 文档不通过条件：未裁决隔离语义就直接实现；把复杂 `data_scope`、第二租户、全业务模块一次性并入；只补 seed 不确认后端隔离语义 | 主计划已收口，但未核实后续是否又有新扩面诉求 |
| 第10B专项-项目级强隔离最小实现包 | 首包已收口 | 无主负责人层面阻塞 | 文档写明的执行期阻塞：现有 demo 数据无法形成同租户有权限/无权限最小样本；合同详情链路触达面超出最小包边界；运行态刷新后无法形成最小跨租户负向证据 | 首包已关闭，但合同列表/采购/分包/付款等是否另开包未核实 |
| 第10C专项-可编辑权限平台主计划 | 主计划已收口 | 无 | 文档不通过条件：未补审计、防自我提权、高危权限保护就进入实现；直接开放权限码本体编辑；新增权限事实源；把 10B/10D 混包 | 主计划已收口，但未核实后续是否有人提出权限码本体编辑诉求 |
| 第10C专项-角色菜单绑定编辑增强最小实现包 | 首包已收口 | 无主负责人层面阻塞 | 文档写明的执行期阻塞：现有 `SysRoleService.assignMenus` 无法承接三条硬门禁；最小审计快照表不足以支撑恢复路径；前端无法承接后端拒绝语义 | 首包已关闭，但 `ROLE_MENU_SELF_EDIT_FORBIDDEN` 是否补运行态硬证未核实为新包 |
| 第10D专项-全站入口注册体系主计划 | 主计划已收口 | 无 | 文档不通过条件：未盘点就建框架；新增入口注册表；把 registry 当授权边界；一次性接入全站业务；顺手接入付款申请或更多类型且没有账号矩阵与负向验收 | 主计划已收口；未核实后续是否有人再提数据库化/插件化诉求 |
| 第10D专项-全站入口注册体系首批实现包 | 首包已收口 | 无主负责人层面阻塞 | 文档写明的执行期阻塞：需要新增后端授权聚合/目标业务权限预判；需要入口注册表、migration、seed 或数据库化 registry；默认接入付款申请或更多业务；改变第8/9条安全基线；把 registry 当真实授权边界或聚合目标业务详情字段 | 首包已关闭，但付款申请和更多业务类型后续是否另开包未核实 |

## 当前已确认阻塞项

无全局现行阻塞，但 `invoice:list`、`inventory:transaction:list`、`dashboard:view`、`project:list` 仍分别保留单项准入阻塞或观察结论，不能批量推进。

### 1. 第10条主线（10A）历史阻塞已收口

依据：`docs/plans/第10条主线-权限字典治理与历史权限码治理裁决任务计划书.md`

10A 曾经历两段历史过程：先是 `alert:edit` 首包样本不通过，后续重新选择第二版候选样本 `notification:list`。该第二版样本已实施并归档通过，因此不再构成当前阻塞项。当前仍需保留的只有全量历史权限码治理边界：不能批量清理其他历史权限码，高危项仍需逐项裁决；本轮 V113/V114/V115 已收口，剩余候选继续按单项准入结论挂账。

### 2. 高危需要确认项整体阻塞进入普通修复

同样属于 10A 范围内的阻塞门禁：

1. `alert:edit` 重复。
2. `contract:submit` 重复。
3. `未绑定 submit 权限 6 条`。
4. `project:delete`、`system:menu:list`、`system:role:list`、`system:user:list` 等 DB-only 高危项。
5. A 集合中的高危授权点候选。

这些项目前状态不是“修复中阻塞”，而是：**没有完成进一步确认前，阻塞其进入普通修复包**。

## 文档预设阻塞条件

以下计划当前并未确认命中阻塞，但计划书已经写明，一旦触发应直接按阻塞处理：

### 10B 首包

1. 无法形成同租户有权限/无权限最小样本。
2. 合同详情链路触达面超出最小包边界。
3. 无法形成最小跨租户负向证据。

### 10C 首包

1. `SysRoleService.assignMenus` 无法承接三条硬门禁。
2. 最小审计快照表不足以支撑恢复路径。
3. 前端无法承接后端拒绝语义。

### 10D 首包

1. 需要新增后端授权聚合或目标业务权限预判接口。
2. 需要新增入口注册表、migration、seed 或数据库化 registry。
3. 需要默认接入付款申请或更多业务类型。
4. 实现会改变第 8 / 第 9 条审批入口安全基线。
5. 把 registry 当作真实授权边界，或让 workflow 详情聚合目标业务详情字段。

### 10B / 10C / 10D 主计划共性

1. 主计划都已阶段性收口。
2. 若后续扩面时跳过边界裁决，直接进入实现，应按各自计划书中的“不通过条件”处理。

## 未核实但需挂账的阻塞/风险项

以下事项本线程未重新验真，但作为总负责人需要挂账：

### 第3条主线

1. 已核实存在观察项收口报告：`production-timing-and-approval-business-entry-closure-2026-07-01.md`。
2. 当前结论不是“未执行”，而是 GAP-004 已被裁决为后续增强 / 非阻塞。
3. 该主线自身是否另有独立实现归档，当前未见单独质量归档文件，故保留“已收口但非独立实现关闭”的说明。

### 第5条主线

1. 已核实归档关闭：`approval-business-entry-acceptance-2026-07-01.md`。
2. 采购申请、分包计量只跳列表的限制不再是“未核实”，而是第 5 条已知 MVP 边界。

### 第6条主线

1. 已核实归档关闭：`approval-business-entry-deeplink-acceptance-2026-07-01.md`。
2. 负向权限样本、项目级数据范围和 query 清理策略保留为非阻塞剩余风险。

### 第7条主线

1. 已核实归档关闭：`approval-center-production-closure-2026-07-01.md`。
2. 剩余风险补充关闭：`approval-center-production-risk-closure-2026-07-01.md`。
3. 跨租户和复杂 `data_scope` 已明确不回灌为第 7 条阻塞。

### 第9条主线

1. 实际执行状态与计划书状态存在脱节：主线程上下文中已收口，但文档仍为 `正式版候选`。
2. 未核实是否需要补文档状态同步。

## 建议动作

### P0：需要单独立包或补文档同步

1. 同步第 9 条和 10A 计划书状态，避免后续误判为仍处于旧口径。
2. 记录工作区残余验收风险：V113/V114/V115 MySQL-H2 迁移与 `MigrationIntegrityTest` 已通过 verifier，但仍需纳入后续提交范围，最终验收要以提交后的定向检查为准。

### P1：建议继续核实的旧主线

1. 第3条

原因：第 5 / 6 / 7 条已找到关闭归档或补充收口证据；第 3 条已找到观察项收口证据，但是否需要单独实现归档仍可继续整理。

## 主负责人结论

1. **当前已确认阻塞后续实现推进的事项已清零；10A 第二版首包 `notification:list` 已实施并归档通过，不再是当前 blocker。**
2. **10B / 10C / 10D 当前没有主负责人层面的现行阻塞，但都保留了清晰的执行期阻塞条件。**
3. **第5/6/7 条已完成核实并可从“未核实状态”移出；第3条已完成观察项收口核实。**
4. **若需要继续清零“全部计划”的残余风险，下一步应让 verifier 对 V113/V114/V115 MySQL/H2 migration 与 `MigrationIntegrityTest` 做定向确认，而不是继续按旧阻塞口径回填 10A。**

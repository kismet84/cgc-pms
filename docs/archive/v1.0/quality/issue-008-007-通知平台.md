# ISSUE-008-007：通知平台 最小可行回归

完成日期：2026-07-09

## 目标

- 基于现有架构补齐“通知平台”的一轮最小可验收能力或回归断言。
- 不扩大为完整平台化改造，不连接生产环境。
- 不新增 migration，不接入邮件、短信、企业微信、钉钉等外部渠道。

## 修改范围

- `backend/src/main/java/com/cgcpms/alert/notification/AlertNotificationDispatcher.java`
  - 对订阅渠道做 `trim + upper-case` 归一化后再匹配发送器，避免 `in_app`、` IN_APP ` 这类配置被静默跳过。
- `backend/src/test/java/com/cgcpms/alert/notification/AlertNotificationDispatcherTest.java`
  - 新增通知平台分发单测，证明订阅渠道存在大小写和空白差异时仍会发送站内通知，并写入发送记录。

## 验收证据

- `cd backend; .\mvnw.cmd "-Dtest=AlertNotificationDispatcherTest" test`
  - RED：首次运行失败，原因是 `inAppSender.send(...)` 未被调用，证明旧逻辑会静默跳过 ` in_app `。
  - GREEN：实现归一化后通过，`Tests run: 1, Failures: 0, Errors: 0, Skipped: 0`。
- `cd backend; .\mvnw.cmd test`
  - 未通过；Surefire 报告汇总 `1545` 个测试、`11` 个 failures、`29` 个 errors、`1` 个 skipped。
  - 失败类集中在既有 `DashboardChiefEngineerServiceTest`、`InvoiceValidationTest`、`MigrationSoftDeleteBehaviorTest`、`Phase2FullChainIntegrationTest`、`Phase4IntegrationTest`、`PurchaseRequestServiceTest`、`ContractRevenueServiceTest` 和旧 `workflow` 测试类；新增通知平台分发测试不在失败列表。
- `cd frontend-admin; pnpm type-check`
  - 通过，`vue-tsc --noEmit` exit 0。
- `git diff --check`
  - 通过，仅有换行符转换提示。

## 结论

结论：通过（本 Issue 目标通过；后端全量仍有既有无关红灯）。

阻塞：无。

剩余风险：
- 本轮只修正现有站内通知分发渠道匹配，不新增通知平台表结构和外部渠道能力。
- 邮件、短信、企业微信、钉钉仍保持现有占位/未配置语义。
- 后端全量测试仍有既有无关失败，需由对应 Ready Issue 分别治理。

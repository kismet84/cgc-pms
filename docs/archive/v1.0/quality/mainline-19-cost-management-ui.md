# 主线19：成本管理模块UI优化质量报告

## 最终结论

- 结论：通过
- 阻塞：否
- 可上线：是

## 变更范围

- `frontend-admin/src/pages/cost/ledger.vue`
- `frontend-admin/src/pages/cost/summary.vue`
- `frontend-admin/src/pages/cost-target/index.vue`
- `frontend-admin/src/pages/cost-target/edit.vue`

## 目标范围

在不更换设计体系、不新增依赖、不重构后端的前提下，按现有灰白企业后台风格统一重设计成本管理模块核心页面，沿用已收敛的列表页骨架，覆盖：

- 成本列表
- 项目成本明细核对
- 成本目标列表
- 成本目标编辑/详情

并保持完整交互。

## 验收证据

### 1. 构建与类型检查

- `pnpm -C frontend-admin type-check`：通过
- `pnpm -C frontend-admin build`：通过

### 2. 成本列表 `/cost/ledger`

- 桌面端通过：筛选区、KPI 区、主表格区、右侧分析栏骨架统一
- 移动端通过：卡片化展示可用，无明显横向溢出
- 交互通过：
  - 筛选“材料费”后，记录数与 KPI 同步变化
  - “重置”按钮可清空筛选输入
- 控制台/API：未见关键错误

### 3. 项目成本明细核对 `/cost/summary`

- 桌面端通过：项目选择前后状态清晰，明细与结论布局统一
- 移动端通过：主信息、核对结果、分析区可读
- 交互通过：
  - 项目选择后可正常展示成本目标、合同锁定、实际成本、动态成本、偏差结论
- 控制台/API：未见关键错误

### 4. 成本目标列表 `/cost-target/index`

- 桌面端通过：筛选区、KPI 区、表格工具栏、右侧分析栏骨架统一
- 移动端通过：空态、列表态结构可读
- 初始问题：
  - “新建成本目标”按钮点击后仍停留在 `/cost-target/index`
  - 根因不是磁盘代码错误，而是前端运行态陈旧
- 修复动作：
  - 列表页创建入口改为跳转 `/cost-target/create`
  - 执行 `python scripts/rebuild.py frontend` 刷新前端运行态
- 复验结果：
  - 点击“新建成本目标”后，真实浏览器跳转到 `/cost-target/create`

### 5. 成本目标创建页 `/cost-target/create`

- 页面结构通过：基础信息、成本科目、金额明细、审批与备注、提交检查、填写说明完整
- 交互通过：
  - “添加科目”后，科目明细由 `0 条` 增至 `1 条`

### 6. 成本目标详情/编辑链路

为补齐真实运行态证据，使用演示数据完成终验：

- 演示数据：
  - `targetId=2073598438153322497`
  - 版本号：`M19UI9-API-1783219367829`
  - 版本名称：`M19 UI9 API终验演示 1783219367829`
  - 项目：`2072948013658456065`
  - 科目：`900011 / 投标费用`
  - 明细金额：`1000`

终验结果：

- 详情：通过
  - 从列表行操作进入“查看详情”
  - 弹窗显示“成本目标详情”、版本号、保存后的版本名称、1 条科目明细、金额合计 `1,000.00`
- 编辑：通过
  - 直接访问 `/cost-target/2073598438153322497/edit`
  - 页面显示“编辑成本目标”，原版本号、项目、科目明细正常回显
- 保存：通过
  - 修改版本名称为 `M19 UI9 保存确认 1783219674314`
  - 保存后列表与详情同步回显
  - `updatedTime=2026-07-05 10:48:13`
- 提交审批：通过
  - 点击“提交审批”后出现确认弹窗
  - 确认后列表显示“审批中”
  - API 回显 `approvalStatus=APPROVING`
- 离开拦截：通过
  - 有未保存修改时点击侧边栏离开
  - 出现“未保存的更改 / 当前成本目标有未保存的修改 / 继续编辑 / 确定离开”

### 7. 控制台与关键 API

- 应用控制台：未见 `error/warn`
- 关键 API：
  - `GET /api/auth/userinfo`：200
  - `GET /api/projects?pageNo=1&pageSize=1000`：200
  - `GET /api/cost-targets?pageNo=1&pageSize=20`：200
  - `GET /api/cost-subjects/tree`：200
  - 创建、详情、保存后详情、提交后详情：成功返回 `code=0`

## 阻塞项

- 无

## 非阻塞项

- 终验使用了 1 条演示成本目标数据：
  - `targetId=2073598438153322497`
  - 如需保持演示环境整洁，可后续手工删除

## 风险复盘

- 本轮唯一真实阻塞不是代码逻辑，而是前端运行态陈旧
- 磁盘代码已切换为 `router.push('/cost-target/create')`，但浏览器初次仍执行旧弹窗逻辑
- 通过 `python scripts/rebuild.py frontend` 刷新前端运行态后，阻塞解除
- 结论：该类前端 UI 主线在裁决前必须包含“真实浏览器 + 当前运行态”证据，不能只看磁盘代码

## 模型分档复盘

- 实际需要升档的任务：
  - 真实浏览器终验与最终裁决，适合 `gpt-5.5 / medium`
- 证明可以降档的任务：
  - 前端运行态刷新与复验，适合 `gpt-5.4 / low`
- 本轮发生的重新分档：
  - 从“实现型问题”切换为“运维型 stale runtime 处理”后，已降档处理

## 残留说明

- 本轮已执行前端刷新命令：
  - `python scripts/rebuild.py frontend`
- 未保留额外测试进程、临时构建检查任务或浏览器验收残留为后续阻塞项


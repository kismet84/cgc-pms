# 第 21 条主线 P0-1：权限逐码裁决报告（2026-07-05）

## 1. 结论

- 结论：不通过
- 阻塞 / 非阻塞：阻塞
- 当前判断：第 21 条主线 `P0-1` 当前不得收口，必须先完成最小修复和专项治理后再复核

本报告基于主线程已给定的 `P0-1` 权限审计裁决结论整理归档，不新增代码、测试、运行态或 Git 侧证据推导。

同时明确：

1. 第 21 条主线 `P0-1` 当前为阻塞项。
2. 第 15 至第 20 条主线既有“通过 / 非阻塞”或已归档结论保持有效，不因本次 `P0-1` 裁决被回退。

## 2. 裁决表摘要

| 权限码 / 类别 | 当前裁决 | 处置要求 | 阻塞性 |
| --- | --- | --- | --- |
| `invoice:list` | 需最小修复 | 补齐最小权限修正后复核 | 阻塞 |
| `inventory:transaction:list` | 不通过 | 做最小修复后复核 | 阻塞 |
| `dashboard:view` | 需专项治理 | 不接受局部打补丁，需专项收口 | 阻塞 |
| `project:list` | 需最小修复前复核 | 先完成最小修复，再做复核裁决 | 阻塞 |
| `submit` 权限 | 不通过 | 需最小修复并补边界复核 | 阻塞 |
| `authority` 缺口 `84` 个 | 不通过 | 需专项治理，统一补齐缺口 | 阻塞 |
| 高危 `DB-only` | 不通过 | 需专项治理，不得按非阻塞遗留处理 | 阻塞 |

## 3. 分项裁决

### 3.1 `invoice:list`

- 裁决：需最小修复
- 归档判断：
  1. 当前口径不足以直接判定通过。
  2. 允许按最小修复路径推进，但修复完成前不得视为已收口。
  3. 修复后必须重新复核，不接受直接沿用旧结论。

### 3.2 `inventory:transaction:list`

- 裁决：不通过
- 归档判断：
  1. 当前存在明确缺口，不能按观察项或低风险遗留处理。
  2. 只接受最小修复方案，不接受绕开主问题的表面兜底。
  3. 修复完成后必须重新进入裁决复核。

### 3.3 `dashboard:view`

- 裁决：需专项治理
- 归档判断：
  1. 当前问题不适合按单点补丁收口。
  2. 需作为专项治理项独立推进，统一梳理权限口径和实际边界。
  3. 专项未完成前，不能给出通过或非阻塞结论。

### 3.4 `project:list`

- 裁决：需最小修复前复核
- 归档判断：
  1. 当前不能直接确认通过。
  2. 需先完成最小修复动作，再进入正式复核。
  3. 修复前不允许按历史可用状态继续放行。

### 3.5 `submit` 权限

- 裁决：不通过
- 归档判断：
  1. `submit` 属于关键动作权限，不接受模糊口径放行。
  2. 当前结论已达不通过标准，需修复后再做边界复核。
  3. 修复前不得作为可上线遗留项处理。

### 3.6 `authority` 缺口 `84` 个

- 裁决：不通过
- 归档判断：
  1. 当前缺口数量已超出零散修补范畴。
  2. 需按专项治理统一盘点、补齐和复核。
  3. 在缺口系统性收敛前，不得认定权限基线达标。

### 3.7 高危 `DB-only`

- 裁决：不通过
- 归档判断：
  1. `DB-only` 高危问题不能按普通遗留项降级。
  2. 必须专项治理，并给出清晰整改闭环。
  3. 未完成治理前，保持阻塞结论。

## 4. 主线影响判断

| 主线 | 当前判断 | 说明 |
| --- | --- | --- |
| 第 21 条主线 `P0-1` | 阻塞 | 存在多个权限码不通过、专项治理项和高危 `DB-only` 问题，当前不得收口 |
| 第 15 条主线 | 非阻塞 | 保持既有归档结论，不因本次 `P0-1` 回退 |
| 第 16 条主线 | 非阻塞 | 保持既有归档结论，不因本次 `P0-1` 回退 |
| 第 17 条主线 | 非阻塞 | 保持既有归档结论，不因本次 `P0-1` 回退 |
| 第 18 条主线 | 非阻塞 | 保持既有归档结论，不因本次 `P0-1` 回退 |
| 第 19 条主线 | 非阻塞 | 保持既有归档结论，不因本次 `P0-1` 回退 |
| 第 20 条主线 | 非阻塞 | 保持既有归档结论，不因本次 `P0-1` 回退 |

## 5. 通过 / 不通过 / 阻塞 / 非阻塞判断

| 项目 | 结论 | 依据 |
| --- | --- | --- |
| 是否通过 | 不通过 | `invoice:list`、`inventory:transaction:list`、`project:list`、`submit` 存在需修复或不通过结论，且 `dashboard:view`、`authority` 缺口 `84` 个、高危 `DB-only` 需专项治理 |
| 是否阻塞 | 阻塞 | 当前存在专项治理项和高危问题，已超过“非阻塞遗留”可接受范围 |
| 是否可关闭第 21 条主线 `P0-1` | 不可关闭 | 修复、专项治理和复核动作均未完成前，不具备收口条件 |
| 是否回退第 15-20 条历史主线 | 不回退 | 本次报告仅裁决第 21 条主线 `P0-1`，不推翻既有已归档主线结论 |

## 6. 最小执行建议

### 6.1 最小修复项

1. `invoice:list`
2. `inventory:transaction:list`
3. `project:list`
4. `submit`

执行要求：

1. 只做最小修复，不扩展无关重构。
2. 修复后逐项复核，不得批量默认通过。

### 6.2 专项治理项

1. `dashboard:view`
2. `authority` 缺口 `84` 个
3. 高危 `DB-only`

执行要求：

1. 需专项收口，不接受零散补丁替代专项治理。
2. 专项完成后需输出新的正式裁决依据。

## 7. 风险点

1. 当前问题集中在权限边界本身，若误按非阻塞处理，存在越权、漏权或错误放行风险。
2. `authority` 大量缺口和高危 `DB-only` 问题说明当前不适合用零星修补替代系统治理。
3. 若在未完成复核前强行收口第 21 条主线，会导致正式归档结论与实际权限状态不一致。

## 8. 归档边界

1. 本报告只归档主线程已给定的 `P0-1` 裁决结论。
2. 本报告不包含新的代码审计、测试执行、运行态验证或上线放行结论。
3. 本报告的核心用途是形成正式 `docs/quality` 裁决记录，供后续最小修复、专项治理和复核闭环使用。

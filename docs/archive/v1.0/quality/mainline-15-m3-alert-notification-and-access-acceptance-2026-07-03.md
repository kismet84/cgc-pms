# 第 15 条主线 M3：预警中心通知与权限深化验收报告（2026-07-03）

## 1. 结论

- 结论：通过
- 阻塞 / 非阻塞：非阻塞
- 当前判断：第 15 条主线 `M3` 通知与权限深化已完成当前阶段收口

本结论基于 2026-07-03 当前工作区代码、既有测试结果，以及已补齐的容器 / 接口 / 真实浏览器复验证据；外部渠道 `EMAIL / WECHAT / SMS` 仍不按正式打通口径验收。

## 2. 依据

### 2.1 通知闭环与发送记录

已知证据：

1. 站内通知已落地。
2. 发送记录已落地。
3. 多渠道派发骨架已落地。
4. 外部渠道 `EMAIL / WECHAT / SMS` 当前仍为占位接入，不得按已正式打通口径验收。

归档判断：

1. `M3` 已从“页面被动查看”推进到“具备主动通知链路”。
2. 当前可确认的是站内通知闭环和统一派发骨架。
3. 当前不可确认的是外部正式发送能力，因此只记为占位能力。

### 2.2 权限隔离与入口补齐

已知证据：

1. `AlertAccessScopeResolver` 已实现角色域与项目范围隔离。
2. `V123__grant_project_manager_alert_list_menu.sql` 已补项目经理入口。
3. `AlertEvaluationServiceTest` 权限相关整类已通过，含 `TA11f2`。
4. `AlertControllerTest` 已覆盖订阅接口与项目经理入口正例，后端控制器测试已通过。

归档判断：

1. 当前权限口径已不是 `M1` 的“默认筛选”弱约束，而是后端角色域与项目范围的真实隔离。
2. 项目经理入口已补齐，`PM` 不再因为菜单缺口被排除在 `M3` 验收之外。
3. 当前后端权限结论来自代码与测试证据，不来自本轮浏览器复验。

### 2.3 订阅策略与接收人计算

已知证据：

1. 已存在 `AlertSubscriptionService`。
2. 已提供 `GET /alerts/subscription` 与 `PUT /alerts/subscription`。
3. 已形成“角色默认订阅 + 用户覆盖”收敛口径。
4. 接收人已从单一项目经理升级为“项目内活跃成员集合 + 域权限 + 有效订阅过滤”。

归档判断：

1. `M3` 已具备最小可解释订阅策略，不再只是文档口径。
2. 接收人计算已从单点责任人模型升级为项目内成员集合模型。
3. 订阅范围当前受域权限和有效订阅过滤约束，未见“通过订阅扩权”的归档证据。

### 2.4 前后端验证结果

前端已知验证：

1. `pnpm type-check`：通过。
2. `pnpm vitest run src/pages/alert/__tests__/index.test.ts`：先因 mock 初始化失败，后经 `vi.hoisted(...)` 修复并通过。

后端已知验证：

1. `./mvnw -q "-Dtest=AlertEvaluationServiceTest,AlertControllerTest" test`：通过。

运行态补充复验：

1. 前一轮运行态阻塞根因已确认：宿主机 `backend/target/cgc-pms-backend.jar` 未包含 `AlertSubscriptionService.class`，导致 `/api/alerts/subscription` 在运行态返回 `500`；问题性质为运行产物未刷新，不属于代码缺陷未修复。
2. 已通过最小运维刷新 `python scripts/rebuild.py backend` 解决，并在等待约 `180` 秒后复验通过：
3. `/api/actuator/health => {"status":"UP"}`。
4. `GET /api/alerts/subscription => 200, code=0`。
5. `GET /api/alerts?pageNum=1&pageSize=1 => 200, code=0, total=9`。
6. 真实浏览器复验账号：`demo_alert_purchase / admin123`。
7. 访问 `http://localhost:5173/alert` 后，“通知订阅”弹窗可正常加载。
8. 浏览器网络侧复验：`GET /api/alerts/subscription => 200 OK`、`PUT /api/alerts/subscription => 200 OK`。
9. 点击“确定”后弹窗关闭、页面稳定、保存成功。
10. 回填结果仅见 `domains=["PURCHASE"]`、`channels=["IN_APP"]`，未见通过订阅修改放大权限范围。
11. 截图证据：
12. `D:\projects-test\cgc-pms\output\playwright\m3-alert-subscription-after-save-stable-20260703-205910.png`
13. `D:\projects-test\cgc-pms\output\playwright\m3-alert-subscription-dialog-reopen-20260703-205910.png`

归档判断：

1. 当前代码层和测试层证据支持 `M3` 已完成最小通知、订阅和权限深化实现。
2. 运行态阻塞已被最小运维刷新解除，当前容器、接口与真实浏览器证据可以支撑 `M3` 运行态通过结论。
3. 当前运行态结论不延展到外部渠道正式发送，`EMAIL / WECHAT / SMS` 仍只按占位能力归档。

### 2.5 本轮证据边界

本轮明确不纳入以下结论：

1. 不认定 `EMAIL / WECHAT / SMS` 已正式打通，因为当前只有占位骨架。
2. 不把一次后端运行产物刷新，扩写为长期稳定性、压测结果或多环境一致性结论。
3. 不把当前浏览器复验扩写为所有角色、所有通知域、所有渠道均已覆盖完毕。

清理口径：

1. 本轮运行态证据已纳入正式验收，不再沿用“未启动 Playwright / 未做容器复验”的旧口径。
2. 当前报告只归档已发生的最小运维刷新与复验结果，不补造额外环境操作。
3. 外部渠道仍按占位能力保留，不因本轮运行态通过而改写。

## 3. 与计划书逐项对照

对照 [第15条主线-M3-预警中心通知与权限深化任务计划书-2026-07-03.md](/../plans/第15条主线-M3-预警中心通知与权限深化任务计划书-2026-07-03.md)：

1. 预警生成或状态流转已能触发站内通知：通过
2. 通知接收对象具备最小可解释计算口径：通过
3. 系统已形成统一派发接口和渠道占位，不再只有页面内列表：通过
4. 用户只能看到自身权限范围内的预警，且后端接口已做真实校验：通过
5. 详情查看、状态更新、批量处理不能绕过权限边界：通过
6. 已形成角色默认订阅 + 用户覆盖的最小订阅策略：通过
7. 订阅范围不能突破权限范围：通过
8. 后端相关测试、前端静态验证通过：通过
9. 真实浏览器与接口证据已覆盖通知、订阅和权限场景：通过
10. 质量归档报告完成，并明确哪些外部渠道仍为占位接入：通过

说明：

1. 第 9 项已由容器 / 接口 / 真实浏览器补充复验覆盖。
2. 本轮解除的是运行产物未刷新的阻塞，不是把外部渠道占位能力改写成已正式打通。

## 4. 通过 / 不通过 / 阻塞 / 非阻塞判断

| 项目 | 结论 | 依据 |
| --- | --- | --- |
| 是否通过 | 通过 | 站内通知、发送记录、多渠道骨架、强权限隔离、订阅策略、项目经理入口和前后端定向验证均已形成闭环证据 |
| 是否阻塞 | 非阻塞 | 运行产物未刷新导致的接口 `500` 已通过最小运维刷新解除；当前未补的是外部渠道正式打通，这不阻塞 `M3` 当前归档 |
| 是否可关闭第 15 条主线 `M3` | 可关闭 | 当前证据已覆盖 `M3` 的核心实现、容器 / 接口运行态和真实浏览器订阅保存链路，并已明确占位能力边界 |

## 5. 已完成能力

1. 站内通知最小闭环已落地。
2. 发送记录已落地，可支撑后续追踪。
3. 多渠道统一派发骨架已落地。
4. 后端权限隔离已从默认筛选升级为角色域与项目范围真实校验。
5. 项目经理入口已补齐。
6. 订阅能力已具备 `GET /alerts/subscription` 与 `PUT /alerts/subscription`。
7. 已形成“角色默认订阅 + 用户覆盖”的最小订阅策略。
8. 接收人计算已升级为“项目内活跃成员集合 + 域权限 + 有效订阅过滤”。
9. 前端类型检查已通过。
10. 前端告警页单测已通过。
11. 后端 `AlertEvaluationServiceTest` 与 `AlertControllerTest` 已通过。
12. 宿主机后端运行产物刷新后，`/api/actuator/health`、`GET /api/alerts/subscription`、`GET /api/alerts?pageNum=1&pageSize=1` 已完成运行态复验。
13. 真实浏览器下订阅弹窗加载、保存、关闭、重开均稳定，且未见订阅越权放大。

## 6. 占位能力

以下能力当前只能按“占位 / 骨架已留”归档，不得按“正式打通”表述：

1. `EMAIL`
2. `WECHAT`
3. `SMS`

当前口径：

1. 已有多渠道扩展位。
2. 已有统一派发方向。
3. 未见真实外部发送成功证据、凭据配置证据或运行态回执证据。

## 7. 剩余风险

当前无新增阻塞性剩余风险。

保留风险与空白：

1. 当前真实浏览器复验集中在采购域订阅保存链路；若后续扩大验收范围，仍需补更多角色、更多域和更多异常分支覆盖。
2. 外部渠道仍为占位接入，后续若进入正式打通，需单独补配置、发送回执与失败回退验收。
3. 若后续继续扩展订阅维度，必须坚持“订阅不能扩权”边界，不得突破当前权限模型。

## 8. 主负责人建议

1. 按“通过 / 非阻塞”关闭第 15 条主线 `M3` 当前归档。
2. 后续若需要更强运行态背书，应在当前已通过基础上补更多角色 / 域 / 异常路径复验，不必回退为“未覆盖运行态”口径。
3. 外部渠道后续推进顺序应保持为：先补真实配置与发送回执，再做单渠道正式打通验收，不要把占位骨架写成平台完成。
4. 后续扩展通知或订阅时，继续复用当前站内通知、统一派发接口和 `AlertAccessScopeResolver` / `AlertSubscriptionService` 口径，不要平行再造一套消息或权限体系。

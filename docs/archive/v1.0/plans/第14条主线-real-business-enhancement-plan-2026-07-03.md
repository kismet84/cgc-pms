# 主线 14：真实业务增强计划（2026-07-03）

## 1. 结论

- 主线编号：14
- 主线名称：真实业务增强
- 当前判断：主线 14 已正式关闭
- 当前定位：
  - 不是继续泛化补测试
  - 而是把现有“最小可验证闭环”推进到“真实业务样本、真实口径、真实回读闭环”

## 2. 启动依据

- 主线 13 已完成工程侧可继续推进的非阻塞剩余项收口。
- 当前唯一保留未关闭项是：
  - `项目经理标签内容区数据确认`
- 当前仓库已经具备：
  - 独立 `project.spec.ts`
  - 合同回读增强
  - 库存真实写入后台账回读
  - 合同失败场景
- 因此下一条主线不应再停留在“测试是否存在”，而应进入“真实业务增强”。

## 3. 主线目标

主线 14 目标分为 4 个方向：

1. 项目经理驾驶舱真实数据闭环
2. 付款链路真实创建与回读增强
3. 现有业务样本向真实业务口径收敛
4. 现场确认结果正式回填与结论固化

## 4. 阶段拆分

### M1：项目经理驾驶舱真实确认

- 目标：
  - 拿到 `项目经理标签内容区` 的真实环境结论
- 范围：
  - 权限
  - 项目样本
  - `/dashboard/project-manager` 接口真实返回
  - 空数据口径
- 通过标准：
  - 能明确落入以下之一：
    - 正常空数据
    - 口径缺解释
    - 前端渲染问题
    - 后端 / 数据问题
- 当前结果摘要：
  - 当前结论：通过
  - 当前归类：前端渲染问题已修复并复验通过
  - 阻塞性：非阻塞
  - 依据：
    - `DashboardPmView.vue` 对可能为 `null` 的明细列表直接读 `.length` 的问题已定位并修复
    - `pnpm test:unit -- src/pages/dashboard/__tests__/DashboardPmView.test.ts` 通过
    - `pnpm test:unit -- src/pages/dashboard/__tests__/DashboardDataLoading.test.ts` 通过
    - `pnpm type-check` 通过
    - `pnpm exec playwright test e2e/dashboard.spec.ts -g "should switch between role tabs and verify content changes" --reporter=line` 结果 `1 passed`
    - 真实页面点击“项目经理”后内容区可见，出现“待办任务”“待审批”“滞后项目”“临期合同”，且可见真实项目内容“成本驾驶舱演示项目 / XM-20260628-001 / 进行中”
  - 排除项：
    - 不是正常空数据
    - 不是口径缺解释
    - 不是后端 / 数据异常
  - 结果文档：
    - [`docs/quality/mainline-14-m1-pm-dashboard-confirmation-2026-07-03.md`](../quality/mainline-14-m1-pm-dashboard-confirmation-2026-07-03.md)

### M2：付款链路真实业务增强

- 目标：
  - 补齐付款申请真实创建后回读状态
- 范围：
  - 优先付款申请，不优先重复扩写发票链路
- 通过标准：
  - 至少存在 1 条付款真实写后回读闭环
- 当前结果摘要：
  - 当前结论：通过
  - 阻塞性：非阻塞
  - 依据：
    - `frontend-admin/e2e/payment-invoice.spec.ts` 已新增“API 真实创建付款申请 -> 付款列表回读状态”的闭环
    - 定向验证与整份 spec 验证均通过
  - 结果判断：
    - 付款页“新建申请”前端表单缺少 `applyCode` 的问题已修复，并完成复验
    - `frontend-admin/src/pages/payment/__tests__/apply-code-form.test.ts` 通过
    - `pnpm type-check` 通过
    - `pnpm exec playwright test e2e/payment-applycode.spec.ts --reporter=line` 结果 `1 passed`
    - 页面级复验通过
    - `applyCode` 缺失问题已关闭
  - 结果文档：
    - [`docs/quality/mainline-14-m2-payment-enhancement-2026-07-03.md`](../quality/mainline-14-m2-payment-enhancement-2026-07-03.md)

### M3：业务样本真实性增强

- 目标：
  - 让当前 E2E 样本更贴近真实业务口径，而不只是技术可跑通
- 范围：
  - 合同
  - 库存
  - 付款
  - 驾驶舱样本关联性
- 通过标准：
  - 新增样本不再只是孤立技术数据，而能解释来源、状态、回读结果
- 当前结果摘要：
  - 当前结论：阶段通过
  - 阻塞性：对主线 14 关闭非阻塞
  - 依据：
    - 合同样本已具备来源、保存后回读结果与失败场景保护
    - 库存样本已具备真实写入后的台账回读结果
    - 付款样本已具备 API 真实创建后的列表回读状态
  - 当前判断：
    - 合同、库存、付款样本已不再只是孤立技术样本，已能解释来源、状态、回读结果
  - 剩余风险：
    - 付款链路后端最终落库 `applyCode` 口径已确认，UI 输入值会在显式传值时保留，空值时自动生成

### M4：结论回写与收口

- 目标：
  - 把主线 14 的真实业务增强结果回写到计划和质量文档
- 通过标准：
  - `docs/未来开发计划.md` 与主线 14 结论一致
  - 已明确关闭项、保留项和仍需现场确认项
- 当前结果摘要：
  - 当前结论：已关闭
  - 阻塞性：非阻塞
  - 已完成回写：
    - M1 真实确认结果已固化
    - M2 付款链路增强结果已固化
    - M3 阶段结论已回写到本计划
    - `docs/未来开发计划.md` 已完成项目经理驾驶舱问题回写，并同步移除付款前端创建链路缺陷待办
  - 当前保留项：
    - 无

## 5. 执行顺序

1. 先做 `M1 项目经理驾驶舱真实确认`
2. 再做 `M2 付款链路真实业务增强`
3. 再做 `M3 业务样本真实性增强`
4. 最后做 `M4 结论回写与收口`

## 6. 派工口径

### 任务 A：项目经理驾驶舱真实确认

- 执行类型：现场 / 环境确认
- 建议角色：QA 验证执行人
- `model`：`gpt-5.4-mini`
- `thinking`：`low`
- 输出：
  - 权限 / 项目 / 接口 / 口径确认记录

### 任务 B：付款链路真实增强

- 执行类型：工程执行
- 建议角色：前端测试工程执行子智能体
- `model`：`gpt-5.4`
- `thinking`：`medium`
- 输出：
  - 付款申请真实写后回读增强
  - 针对性验证结果

### 任务 C：业务样本真实性增强

- 执行类型：工程执行
- 建议角色：前端测试工程执行子智能体 / 后端验证执行子智能体
- `model`：`gpt-5.4`
- `thinking`：`medium`
- 输出：
  - 更贴近真实业务口径的样本与回读证据

## 7. 风险点

- 真实环境风险：
  - 驾驶舱结论仍依赖现场环境，不可由代码推断替代
- 数据口径风险：
  - 如果只补自动化，不补业务解释，主线 14 会失焦
- 环境差异风险：
  - 付款、驾驶舱、审批一旦进入真实样本，环境差异会比主线 13 更明显
- 边界风险：
  - 不应把主线 14 重新做成“大而全全链路测试计划”

## 8. 通过 / 不通过判断

### 通过

- 项目经理驾驶舱有真实环境结论
- 付款链路新增真实写后回读闭环
- 文档能区分：
  - 已完成的真实业务增强
  - 仍待现场确认的项
  - 新发现的问题归属

### 不通过

- 继续停留在“测试存在即可”
- 没有真实环境结论
- 没有付款真实回读闭环
- 业务样本仍只是技术样本，无法解释业务口径

## 9. 主负责人结论

- 当前建议：主线 14 已正式关闭，项目经理驾驶舱问题与付款前端 `applyCode` 缺失问题均已修复并复验通过，不再保留相关待办
- 当前优先级：高于继续泛化补更多页面冒烟
- 当前最小可行入口：
  - 付款链路后端最终落库 `applyCode` 口径已确认并完成回写，无需继续跟踪

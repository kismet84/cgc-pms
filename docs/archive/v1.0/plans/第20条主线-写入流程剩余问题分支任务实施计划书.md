# 第20条主线-写入流程剩余问题分支任务实施计划书

## 一、结论

- 当前第20条主线写入流程验收结论：**不通过 / 阻塞**。
- 本计划仅针对真实浏览器验收中已暴露的**写入闭环剩余问题**制定后续分支任务，不扩大到“第20条主线字典总方案”整体重做。
- 执行目标是以最小可行方式修复可明确归因的问题，恢复“编辑保存关闭弹窗 / 草稿字段正确回填 / 提交审批状态真实流转 / 新建路径可形成可测草稿”的闭环能力。

## 二、证据范围

### 2.1 必引验收证据

- `D:\projects-test\cgc-pms\output\playwright-write-regression\write-regression-report.json`
- `D:\projects-test\cgc-pms\output\playwright-write-regression-retry\write-regression-retry-report.json`
- `D:\projects-test\cgc-pms\output\playwright-write-regression-final\final-inconclusive-retake-report.json`
- `D:\projects-test\cgc-pms\docs\plans\第20条主线-硬编码常量关联字典服务实施方案.md`

### 2.2 关键截图目录

- `D:\projects-test\cgc-pms\output\playwright-write-regression\screenshots`
- `D:\projects-test\cgc-pms\output\playwright-write-regression-retry\screenshots`
- `D:\projects-test\cgc-pms\output\playwright-write-regression-final\screenshots`

### 2.3 已收敛的验收事实

1. 采购申请：最终复测已能稳定打开行操作和编辑弹窗，但保存后弹窗不关闭；接口详情中 `items=null`，前端编辑弹窗明细未正常回填，未到达提交审批。
2. 领料申请：最终复测已能稳定打开行操作和编辑弹窗，但保存后弹窗不关闭；接口详情中 `items=null`，编辑弹窗明细行材料字段空、必填字段未回填，未到达提交审批。
3. 付款申请：复测已实际进入 DRAFT 编辑弹窗并保存，弹窗不关闭；申请编号、申请金额、申请原因、付款依据为空，未到达提交审批。
4. 分包计量：最终复测已完成编辑保存并触发提交审批确认，但提交后接口仍为 `approvalStatus=DRAFT` / `status=DRAFT`，浏览器 console 出现 `400 (Bad Request)`，审批状态未流转。
5. 采购订单 / 成本目标 / 签证：当前主要阻塞不是“已证实的提交审批业务缺陷”，而是无可提交草稿或 UI 新增路径受下拉、必填明细、前置数据阻塞，导致无法形成稳定可验收样本。
6. 系统角色：真实页面与源码口径一致，无新增角色入口，仅有编辑权限能力；若产品仍要求“新增角色”写入闭环，应单独立需求，不应混入本轮写入缺陷修复。
7. 回归脚本方面，重试与最终复测已经证明临时脚本中的稳健行操作策略有效，但目前仍停留在 `output/` 临时脚本层，不宜长期作为正式验收基线。

## 三、任务边界

### 3.1 纳入本计划

- 采购申请、领料申请、付款申请、分包计量的明确写入闭环缺陷修复。
- 采购订单、成本目标、签证的“可创建 / 可保留 / 可查询草稿”前置路径补齐。
- 系统角色页能力边界的产品口径确认。
- 写入流程回归脚本的正式化沉淀计划。

### 3.2 不纳入本计划

- 不重写第20条主线字典总方案。
- 不在本计划中直接要求改造全部常量、全部字典映射、全部审批页面。
- 不把系统用户首轮 500 误判为业务缺陷；复测已证明该问题来自测试脚本错误请求路径。

## 四、阻塞分级

### 4.1 阻塞项

- `20-W1` 采购申请编辑保存闭环修复
- `20-W2` 领料申请编辑保存闭环修复
- `20-W3` 付款申请草稿数据与编辑保存闭环修复
- `20-W4` 分包计量提交审批状态流转修复

### 4.2 验收前置阻塞

- `20-W5` 采购订单 / 成本目标 / 签证写入前置数据与 UI 新增可测性补齐

### 4.3 产品口径阻塞 / 待确认

- `20-W6` 系统角色能力边界确认

### 4.4 非阻塞但建议纳入收口

- `20-W7` 写入流程回归脚本固化

## 五、执行顺序

1. 先执行 `20-W1`、`20-W2`、`20-W3`、`20-W4`，恢复已证实缺陷的核心写入闭环。
2. 再执行 `20-W5`，补齐采购订单、成本目标、签证的可测前置样本与 UI 新建路径。
3. `20-W6` 先由产品 / 主线程给出口径裁决，再决定是否另开需求，不与本轮缺陷实现并行混做。
4. 最后执行 `20-W7`，把已验证有效的临时复测策略沉淀为正式回归资产。

## 六、分支任务清单

### 20-W1 采购申请编辑保存闭环修复

- 目标：修复采购申请 DRAFT 草稿编辑保存后弹窗不关闭、明细未回填、无法继续提交审批的问题。
- 范围：
  - 仅限采购申请编辑页 / 保存接口 / 明细回填链路。
  - 重点核对 `PR-DEMO-WF-MINE-RUN-001` 同类 DRAFT 草稿的详情回填与保存闭环。
- 建议执行角色：后端实现子智能体 + 前端实现子智能体联动，必要时再补真实验收子智能体。
- `model=gpt-5.5`
- `thinking=high`
- `reason=涉及前后端明细回填、保存口径和审批前置数据一致性，需要排查 items 为空的根因并避免只修表象。`
- 禁止事项：
  - 不扩大到采购订单、领料、付款等其他页面一起重构。
  - 不借机重做第20主线字典总方案。
- 验收标准：
  - 编辑 `PR-DEMO-WF-MINE-RUN-001` 类 DRAFT 草稿时，弹窗可正常打开。
  - 明细 `items` 正常回填，至少 1 行物料字段完整可见。
  - 点击保存后弹窗关闭，列表刷新后草稿仍可再次打开。
  - 保存后可继续执行“提交审批”路径，不因缺失明细或隐藏校验阻断。
- 风险点：
  - 详情接口返回 `items=null` 可能是后端聚合缺失，也可能是前端编辑态未兼容空数组。
  - 若保存逻辑依赖隐藏必填字段，容易修成“假关闭、真未保存”。

### 20-W2 领料申请编辑保存闭环修复

- 目标：修复领料申请 DRAFT 草稿编辑保存后弹窗不关闭、明细材料字段空、必填字段未回填、无法继续提交审批的问题。
- 范围：
  - 仅限领料申请编辑页 / 保存接口 / 明细回填链路。
  - 重点核对 `REQ-DEMO-REAL-004` 同类 DRAFT 草稿。
- 建议执行角色：后端实现子智能体 + 前端实现子智能体联动，完成后交真实验收子智能体。
- `model=gpt-5.5`
- `thinking=high`
- `reason=与采购申请类似，但领料单还涉及仓库、领料人、材料行金额计算等必填回填链路，数据完整性风险更高。`
- 禁止事项：
  - 不扩展到库存出入库、仓库管理等旁支模块。
  - 不以“自动补默认值”替代真实字段回填核对。
- 验收标准：
  - 编辑 `REQ-DEMO-REAL-004` 类 DRAFT 草稿时，材料名称、单位、数量、金额、使用部位等关键字段可见。
  - 点击保存后弹窗关闭且数据保留。
  - 可继续执行“提交审批”操作，不因空明细或隐藏校验失败而中断。
- 风险点：
  - `items=null` 与 `partnerId/partnerName` 为空可能同时存在，需区分哪些字段允许为空，哪些字段必须补齐。
  - 金额重算与行数据回填若口径不一致，容易产生保存后金额漂移。

### 20-W3 付款申请草稿数据与编辑保存闭环修复

- 目标：修复付款申请 DRAFT 编辑弹窗打开后关键字段为空、保存不闭环、无法提交审批的问题。
- 范围：
  - 仅限付款申请编辑页、详情回填、保存校验、付款依据行展示。
  - 重点核对 `PAY-20260703-006` 同类 DRAFT 草稿。
- 建议执行角色：后端实现子智能体 + 前端实现子智能体联动。
- `model=gpt-5.5`
- `thinking=high`
- `reason=付款申请涉及金额、编号、原因、付款依据等核心业务字段，属于数据一致性和表单恢复能力问题，不能只修前端展示。`
- 禁止事项：
  - 不把问题简化为“弹窗关闭”单点修补。
  - 不扩大到发票、付款台账等其他页面。
- 验收标准：
  - 编辑 DRAFT 时申请编号、申请金额、申请原因、付款依据均正常回填。
  - 点击保存后弹窗关闭且再次打开仍保留修改值。
  - 可继续执行提交审批，不再因字段空值或依据行缺失阻断。
- 风险点：
  - 草稿本身可能已存脏数据，需要区分“历史样本脏数据”与“当前保存逻辑不写入”。
  - 付款依据为空可能来自关联来源单据未落库，排查范围不可只停留在前端。

### 20-W4 分包计量提交审批状态流转修复

- 目标：修复分包计量编辑保存后虽可点击提交审批，但提交后 API 仍停留 `DRAFT` 的状态流转问题。
- 范围：
  - 仅限分包计量提交审批链路、提交接口、审批状态落库和前端触发参数。
  - 重点核对 `SM-DEMO-WF-MINE-WDR-001` 同类 DRAFT 草稿。
- 建议执行角色：后端实现子智能体主责，前端实现子智能体配合，完成后交真实验收子智能体。
- `model=gpt-5.5`
- `thinking=high`
- `reason=这是审批状态机与接口契约问题，已具备稳定复现证据，需重点排查后端状态迁移、参数校验和 400 根因。`
- 禁止事项：
  - 不以“前端提示成功”替代真实 API 状态流转。
  - 不跳过接口 / 数据库层验证，只看页面文案。
- 验收标准：
  - 编辑保存后数据仍可正常回读。
  - 点击提交审批后接口返回成功，浏览器 console 不再出现对应 400。
  - 复查详情与列表时 `approvalStatus` 不再为 `DRAFT`，进入审批中或等价状态。
  - 审批中心能看到该单据进入真实待办链路。
- 风险点：
  - 可能存在“保存成功”和“提交审批失败”分属两套接口的情况，需分别核对。
  - 若审批实例创建失败但业务单状态未回滚，可能导致脏中间态。

### 20-W5 采购订单 / 成本目标 / 签证写入前置数据与 UI 新增可测性补齐

- 目标：先补齐可复用测试样本或修正表单新建路径，使采购订单、成本目标、签证具备“可创建草稿、可查询、可继续验收”的前置条件。
- 范围：
  - 采购订单：解决新建订单时下拉选择不稳定、未形成 DRAFT 草稿的问题。
  - 成本目标：解决成本科目明细与总额对齐、保存后未形成草稿的问题。
  - 签证：解决合同 / 变更类型 / 明细必填链路导致无法形成草稿的问题。
- 建议执行角色：前端实现子智能体 + 后端实现子智能体按页面拆分；必要时补一名运维 / 数据准备型子智能体负责样本准备。
- `model=gpt-5.4`
- `thinking=medium`
- `reason=本任务核心是补齐可测路径与前置样本，不是已证实的审批流业务缺陷；需要控制投入，优先恢复验收通道。`
- 禁止事项：
  - 不把三页一起做大重构。
  - 不在本任务中要求一次性解决所有写入页的新增体验问题。
- 验收标准：
  - 采购订单、成本目标、签证三页至少各形成 1 条可查询 DRAFT 草稿或等价可重复验收样本。
  - 样本创建过程不依赖临时手工数据库改数。
  - 主线程可据此再次安排真实浏览器“编辑保存 -> 提交审批”闭环验收。
- 风险点：
  - 可能是前置主数据缺失，不是页面本身缺陷；需先区分是数据准备问题还是表单实现问题。
  - 若通过 API 造样本绕过 UI，需同步说明该样本是否只用于验收前置，不得误写成“UI 已修复”。

### 20-W6 系统角色能力边界确认

- 目标：确认系统角色页本轮是否仅验证“编辑权限”能力，还是产品确实要求新增角色入口；若要求新增，则单独立需求，不混入本轮写入阻塞。
- 范围：
  - 仅处理产品口径与验收口径，不改业务代码。
  - 基于真实页面与源码已知事实：当前无新增角色入口，仅有编辑权限入口。
- 建议执行角色：产品口径复核 / 审计归档型子智能体。
- `model=gpt-5.4`
- `thinking=medium`
- `reason=这是口径裁决任务，不是实现任务，需要基于页面事实给出纳入或剔除的结论。`
- 禁止事项：
  - 不直接派生成“补一个新增角色按钮”实现任务，除非产品明确确认。
  - 不把页面设计差异误写成系统缺陷。
- 验收标准：
  - 主线程获得明确书面结论：本轮只保留编辑权限验证，或另立新增角色需求。
  - 若另立需求，需从本轮写入阻塞清单中剔除。
- 风险点：
  - 若口径不先收敛，后续验收会反复把“无新增入口”误报为缺陷。

### 20-W7 写入流程回归脚本固化

- 目标：把本轮临时复测中已证明有效的稳健行操作、草稿定位、截图落盘、API 复核策略沉淀为正式 e2e 或验收脚本方案。
- 范围：
  - 仅定义后续固化任务，不要求在本计划阶段立即改代码。
  - 重点沉淀采购申请、领料申请、付款申请、分包计量四类高价值闭环脚本模板。
- 建议执行角色：测试实现子智能体 / 验收脚本维护子智能体。
- `model=gpt-5.4`
- `thinking=medium`
- `reason=属于测试资产整理与稳健性固化，复杂度中等，但不应滥用高推理。`
- 禁止事项：
  - 不在本计划要求立即重写整套 Playwright 框架。
  - 不把 `output/` 临时脚本直接当正式长期基线而不整理。
- 验收标准：
  - 形成正式任务单，明确哪些临时策略要沉淀、哪些证据产物需要保留、哪些脚本应迁入正式目录。
  - 后续回归可复用统一的“行操作打开 -> 编辑保存 -> 提交审批 -> API 复核 -> 截图落盘”基线。
- 风险点：
  - 若不固化，本轮已解决的自动化稳定性会在下轮验收中再次退化。
  - 若过早固化到错误页面契约，后续维护成本会抬高。

## 七、建议派工顺序与角色类型

1. 实现型任务：
   - `20-W1`
   - `20-W2`
   - `20-W3`
   - `20-W4`
   - `20-W5`
2. 审计 / 口径型任务：
   - `20-W6`
3. 验收 / 测试资产型任务：
   - `20-W7`

## 八、阶段性验收要求

### 8.1 第一阶段验收

- 对 `20-W1` 至 `20-W4` 做真实浏览器复测。
- 结论要求明确到单页面：通过 / 不通过、阻塞 / 非阻塞、剩余证据。

### 8.2 第二阶段验收

- 对 `20-W5` 验证是否已形成可复用 DRAFT 草稿样本。
- 若仍无法形成样本，不得直接推进整条主线收口。

### 8.3 收口前要求

- `20-W6` 产品口径已裁决。
- `20-W7` 至少形成正式任务单或纳入下一阶段计划，避免临时脚本证据失效。

## 九、最终判定口径

- 在 `20-W1` 至 `20-W4` 未全部通过前，第20条主线写入流程不得判定为已收口。
- 在 `20-W5` 未补齐前，采购订单、成本目标、签证三页仍属于“验收通道未打通”，不能宣称已完成写入闭环验证。
- `20-W6` 未裁决前，系统角色页不得继续作为泛化阻塞项反复计入实现缺陷。
- `20-W7` 虽非阻塞，但建议纳入最终收口，否则后续复验成本会重复放大。

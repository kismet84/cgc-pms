# Blocked Issues

## 当前阻塞任务

### ISSUE-032-005：M3 财务真实角色缺失导致真实角色抽样无法完成

失败分类：环境前置类

阻塞原因：

- `docs/quality/mainline-32-m3-real-role-sampling-2026-07-09.md` 已裁决第32主线 M3 `不通过 / 前置阻塞`。
- 本地 dev 数据库未发现任何绑定 `FINANCE` 角色的非超管用户，当前缺少可用于真实财务角色抽样的登录账号。
- 第32主线 M3 明确禁止使用超管 `dev-login` 或其他非财务账号冒充财务角色体验，因此不能补做财务驾驶舱与相关入口抽样。

WIP 暂存：

- stash hash：不适用
- stash message：不适用
- 恢复条件：补充或确认一个 `FINANCE` 非超管真实账号后，按 M3 口径重做财务角色抽样。
- 未完成验收项：`/dashboard` 财务标签、`/api/dashboard/finance`、付款/发票/结算相关入口真实财务角色抽样。

解除条件：

- 在 dev/test 运行态新增或确认一个绑定 `FINANCE` 角色的非超管账号。
- 使用该账号完成第32主线 M3 财务角色浏览器与 API 抽样，并更新正式质量报告。
- 根据复验结论同步 backlog 状态；复验通过前，不得把 M3 宣告为通过 / 非阻塞。

## 记录格式

### ISSUE-xxx：任务标题

失败分类：命令调用问题 / 环境前置类 / Ready Issue 配置问题 / 真实代码质量或安全问题

阻塞原因：

- 待补充

WIP 暂存：

- stash hash：待补充
- stash message：待补充
- 恢复条件：待补充
- 未完成验收项：待补充

解除条件：

- 待补充

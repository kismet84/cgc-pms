# Blocked Issues

## v1.5 当前阻塞任务

### PI-2026-07-11-02 供应商履约档案准入阻塞

- Issue：供应商履约档案 Candidate 的数据质量前置。
- 失败分类：产品前置 / 数据口径不足，不是业务代码失败。
- 阻塞原因：当前 `supplierScores()` 只按采购订单 `deliveryDate` 和当前未完成状态计算逾期；已完成但迟交的订单不会计入逾期，也没有实际收货时点、质量、价格、退货等稳定口径，不能称为综合履约评分。
- 已完成证据：已核对 `DashboardMaterialRoleService.supplierScores()` 的订单范围、分组、逾期判断、排序和前 5 名截断逻辑；现有输出本质是“当前未完成订单按期率”。
- 解除条件：明确第一版是仅交付维度还是综合履约；若为交付维度，定义计划交期、实际完成/收货时点、部分收货和跨期口径，并确认现有表能提供稳定数据；若为综合履约，先确认质量、价格和退货数据源。
- 未完成验收项：真实供应商跨项目/跨期样本、迟交后已完成样本、部分收货样本、租户与项目范围、前端档案入口。
- 安全恢复方式：完成口径决策和数据源核验后，刷新产品情报并重新建立 Candidate；在此之前不得拆成 Ready 或修改业务代码。

v1.0 阻塞快照见 [历史记录](../archive/v1.0/backlog-snapshot/blocked-issues.md)。

## 记录格式

每条阻塞记录必须包含：Issue、失败分类、阻塞原因、已完成证据、解除条件、未完成验收项和安全恢复方式。

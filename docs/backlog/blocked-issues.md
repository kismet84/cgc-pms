# Blocked Issues

## 当前阻塞任务

### ISSUE-005-001：付款与发票列表页生产化补强

阻塞原因：

- 2026-07-08 本轮代码实现、测试、`type-check`、`build`、`git diff --check` 已完成。
- 未通过浏览器验收的前端实现已作为 WIP 暂存，未提交入库。
- 本地前端 `5173` 可启动，但后端 `http://localhost:8080` 连接被拒绝。
- 内置浏览器访问首页时出现 `Request failed with status code 500`；访问 `dev-login` 路径时被 `net::ERR_BLOCKED_BY_CLIENT` 拦截，无法完成付款/发票页面真实浏览器验收。

解除条件：

- 恢复本地后端 `8080` 运行态。
- 提供可用的 `dev-login` 或等价登录验收路径。
- 恢复已暂存的 ISSUE-005-001 前端 WIP。
- 在真实运行态下补做付款页与发票页的筛选回显、重置、刷新保持、空态、错误态和重试入口验收。

## 记录格式

### ISSUE-xxx：任务标题

阻塞原因：

- 待补充

解除条件：

- 待补充

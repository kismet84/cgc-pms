# Current Focus

## 当前阶段

用户于 2026-07-10 触发新的 `启动迭代-3`。`ISSUE-008-032` 已完成并计入第 `1/3` 条；当前 `enabled=true`、`stop=false`、`pause=false`、checkpoint=`go`，允许继续串行执行 `ISSUE-008-033`。

上一轮 `启动迭代-10` 的 `10/10` 历史保持不变：依次完成 `ISSUE-008-012`、`ISSUE-008-013`、`ISSUE-008-014`、`ISSUE-008-015`、`ISSUE-008-016`、`ISSUE-008-017`、`ISSUE-008-018`、`ISSUE-008-019`、`ISSUE-008-020`、`ISSUE-008-021`；这批结果仍以 `done-issues.md` 与对应质量报告为准，不纳入新一轮计数。

本轮优先从 `ad-hoc-plan.md` 高优先级 Candidate 补入 3 条合格 Ready。`ISSUE-008-032` 后端接口无前端入口治理已收口；后续 `ISSUE-008-033` WBS 现有日期派生延期风险可见性、`ISSUE-008-034` 供应商交期表现摘要与采购订单下钻均不新增 migration、不连接生产，按 `033 -> 034` 串行执行。

## 当前 Ready 状态

- 当前有 2 条合格 Ready：`ISSUE-008-033`、`ISSUE-008-034`；下一条为 `ISSUE-008-033`。
- 新一轮计数：`1/3`，剩余 `2` 个实施型额度；拆单轮不计数。
- `blocked-issues.md` 当前为空，不存在必须先解阻的当前 focus 前置项。
- 依赖顺序：`ISSUE-008-033 -> ISSUE-008-034`；`ISSUE-008-033` 当前可实施。
- 并行边界：两条共享前端页面与 backlog 归档边界，统一串行。
- `ISSUE-008-032` 治理结果：50 个 Controller 中 39 个已有入口、4 个合法复用、1 个 Prometheus `/actuator` 运维专用、6 个高置信缺口；项目成员入口已修复，复用 `/system/users`，后端项目范围继续由 `ProjectAccessChecker` 约束。
- `ISSUE-008-032` 非阻塞后续：`/accounting-entry`、`/audit-logs`、`/bid-cost`、`/overhead-allocation`、`/contract-revenue` 5 个接口无现成承载页；add/edit/delete 权限正向显示测试尚未补齐。
- 其他非阻塞观察：WBS 前置任务依赖需要新业务模型，本轮明确不做；供应商履约仅覆盖采购订单交期摘要，不是综合档案。

## 本次 READY_SPLIT 记录（启动迭代-3，2026-07-10）

- 新增 Ready：`ISSUE-008-032`、`ISSUE-008-033`、`ISSUE-008-034`。
- 串行顺序：先治理后端业务接口与前端入口映射基线，再补 WBS 派生延期提示，最后补供应商交期表现下钻。
- 候选取舍：WBS Candidate 中“前置任务依赖”因现有 `sub_task` 无 dependency/predecessor 字段而剔除，只保留无需持久化的延期风险可见性；供应商档案限定复用现有 `supplierScores` 和采购订单 `partnerId` 查询，不新增档案表。
- 未选：采购补货建议需要新增库存阈值/补货规则模型；现场日报属于 P3 冻结范围；询价/比价/定标/黑名单、完整 WBS 依赖网络均需要 migration 或更大业务裁决。
- 计数口径：拆单轮当时只更新 backlog/state，不计入实施型完成数并初始化为 `0/3`；当前已由 `ISSUE-008-032` 推进为 `1/3`。

## 本轮最小总验收（10/10）

- 已完成：`ISSUE-008-022`、`ISSUE-008-024`、`ISSUE-008-025`、`ISSUE-008-023`、`ISSUE-008-026`、`ISSUE-008-027`、`ISSUE-008-028`、`ISSUE-008-029`、`ISSUE-008-030`、`ISSUE-008-031`。
- 阻塞：无。
- 非阻塞观察：外部真实通知渠道不在本轮目标内；`ISSUE-008-030`、`ISSUE-008-031` 未执行浏览器 E2E。
- 当前 focus：本轮已达到 `启动迭代-10` 上限，停止下一任务派发；后续是否继续须由新的迭代触发重新裁决。
- 收口口径：本轮完成的是 10 项局部实现或回归证明，不代表相关能力域整体平台化完成。

## 本轮候选域对比与选择结论

- `WBS / 甘特图`：`ISSUE-008-023` 与 `ISSUE-008-026` 已完成归档；已补组件级真实渲染与降级态验收，不新增 schedule 表或拖拽排程。
- `供应商评分 / 采购增强`：`ISSUE-008-025` 与 `ISSUE-008-027` 已完成归档；已补供应商交期表现排名可见性与组件级真实渲染证据，不扩展综合评分。
- `报表中心 / 通知平台`：`ISSUE-008-030` 已收口报表目录异常态、重试入口与权限过滤；`ISSUE-008-031` 已以 no-op 回归证明收口通知占位渠道发送记录、跳过原因与前端可见性。
- `规则治理中心`：M2/M3 已完成去重、阈值等核心后端生效回归；继续拆规则设计器/执行日志/效果分析需要新增模型或更大范围，当前不选。
- `询价 / 比价 / 定标 / 黑名单 / 补货建议`：长期计划存在，但当前需要新增业务模型或 migration，且数据、权限和回滚口径未完成裁决，本轮不选。
- `P3、财务生产集成、BIM、AI、总工程师技术域`：仍属冻结或生产前置范围，本轮禁止选择。

## 本次 READY_SPLIT 记录（2026-07-10）

- 新增 Ready：`ISSUE-008-030`、`ISSUE-008-031`。
- 选择理由：两项均来自 P2 报表中心 / 通知平台，复用现有页面、接口与测试入口，不需要新增 migration，不连接生产环境，能在本地 dev/test 闭环。
- 未选范围：规则治理设计器 / 执行日志 / 效果分析、WBS 完整 schedule、供应商询价 / 比价 / 定标 / 黑名单 / 补货建议均涉及新增模型、migration 或更大业务裁决，本轮不选。
- 计数口径：拆单轮只更新 backlog/state/iteration，不计入实施型完成数；`iterationCompleted=8`、`remainingIterations=2` 保持不变。

## 上一轮最小总验收（历史保留）

- 上一轮已完成 `10/10` 个实施型 Ready Issue，并在达到上限后停止继续派发；这代表“上一轮串行缺口收口完成”，不代表相关能力域整体平台化完成。
- 上一轮成果与证据继续保留在 `done-issues.md`、`docs/quality/issue-008-012~021-*` 与 `docs/iterations/iteration-2026-07-10-report.md`。
- 新一轮从 `0/10` 独立计数，不重算、不覆盖上一轮结果。

## 第32条主线 M5 分层冻结

- A 类能力项：报表中心、规则治理中心、通知平台、WBS / 进度计划 / 甘特图、供应商评分 / 采购增强，当前口径统一视为“已完成局部最小回归或已有局部入口，不等于完整平台化完成”。
- A 类能力项后续若要进入 Ready，只能围绕明确的平台化缺口或专项治理缺口拆题，不得把既有最小回归结果重复包装成“整个平台已完成”。
- B 类能力项：`P3` 现场日报 / 移动端、财务集成 / 月结锁账、技术域 / 总工程师驾驶舱、BIM / IFC、AI 辅助，统一视为“长期储备 / 默认冻结”。
- B 类能力项默认不得进入当前 Ready 执行；只有用户明确解除冻结，并同时补齐业务前置、数据前置、验收口径和风险边界后，项目总负责人才能单独裁决是否拆题。

## 第32条主线 M6 UI 与质量遗留复验顺序

- M6 六类遗留统一拆成两条基线：`UI/体验基线`（UI 一致性、可访问性、登录页品牌、`440px` 移动端布局）和 `自动化/门禁基线`（覆盖率、E2E CI）。
- M6 默认顺序是：先完成 `ISSUE-032-007`，再执行 `ISSUE-032-008`；两者都以“复验和分类”为目标，不以“整改完成”为目标。
- 在 `ISSUE-032-007/008` 形成当前基线前，不得把旧截图、旧覆盖率数字或旧 E2E 结论直接当成当前已验证事实。
- 在 M6 基线复验完成前，不得直接发起全站 UI 重构、移动端专项实现或覆盖率 / E2E 硬门禁整改。

## 任务来源

- 当前执行队列：`docs/backlog/ready-issues.md`
- 临时计划书：`docs/backlog/ad-hoc-plan.md`
- 长期总任务池：`docs/backlog/cgc-pms-production-enhancement-plan.md`
- Ready 队列少于 3 条或用户要求切换方向时，先读临时计划书 `docs/backlog/ad-hoc-plan.md`，再读长期计划书 `docs/backlog/cgc-pms-production-enhancement-plan.md`；只允许由项目总负责人结合两类候选来源、`done/ready/blocked` 现状、当前项目状态、用户最新约束、风险和依赖关系补货，目标形成 3–5 条合格 Ready；若已有未执行 Ready，补货后总数不得超过 5。3–5 条不代表允许并行，存在关联仍按串行处理；无法形成至少 3 条时必须记录具体原因。补货轮只更新 backlog，不直接修改业务代码。

## 动态候选范围与选择原则

- 候选任务默认先来自临时计划书，再来自长期总任务池，不再固定绑定 `EPIC-004~007` 样例范围。
- 由项目总负责人根据当前项目状态动态判断优先级，优先选择依赖清晰、风险可控、可在本地测试环境收口的任务。
- 选题依据至少包括：临时计划书、长期总任务池、`docs/backlog/ready-issues.md`、`docs/backlog/done-issues.md`、`docs/backlog/blocked-issues.md`、当前项目状态、用户最新约束、任务风险与前置依赖。
- 默认跳过：已 Done 的任务、Blocked 且未解除阻塞的任务、超出当前禁止范围的任务、需要生产环境/生产数据库连接的任务、未满足下方“阶段切换条件”的数据库 migration 任务。
- 当用户明确要求串行且只写 1 条 Ready 时，优先锁定单一平台化缺口题，不保留并行候选。

## 阶段切换条件

当当前 Ready 队列中生产准入类回归任务已清空，且 `blocked-issues.md` 没有未解除阻塞项时，AutoPilot 可以从长期计划的下一阶段拆分生产增强类 Ready Issue。

允许进入下一阶段的候选范围：

- P2 报表中心、规则治理中心、通知平台、WBS / 进度计划 / 甘特图、供应商评分 / 采购增强。
- 上述 P2 能力即使已有最小回归或局部入口，也只代表具备继续治理的基础，不代表完整平台化完成。
- 任务必须能在本地 dev/test 环境闭环，不依赖生产环境、生产数据库或外部生产平台。
- 每次补货目标形成 3–5 条合格 Ready；若已有未执行 Ready，补货后总数不得超过 5。每个 Ready Issue 必须写清允许修改范围、禁止事项、验证命令、归档报告和是否需要新增 migration；3–5 条不代表并行，存在关联仍串行，无法形成至少 3 条时必须记录具体原因。
- 如果涉及数据库结构，只允许新增版本化 Flyway migration，不得修改已应用 migration；Ready Issue 必须包含表/字段最小范围、回滚口径和本地验证命令。
- 若新增 migration 同时触达权限、安全、租户隔离、金额口径、审批状态机或生产集成，默认不自动执行，先进入人工裁决。

## 当前禁止执行

- 总工程师相关任务：若长期计划中仍标为冻结或缺少基础域，默认不选
- BIM 相关任务：若长期计划中仍标为冻结或缺少基础域，默认不选
- AI 相关任务：若长期计划中仍标为冻结或缺少基础域，默认不选
- `P3` 现场日报 / 移动端：默认不选，除非用户明确解除长期储备冻结并补齐移动端/H5/PWA 验收前置
- 财务生产集成：默认不选，除非用户明确解除
- 月结锁账：默认不选，除非用户明确解除长期储备冻结并补齐财务口径、期间关闭规则和回滚口径
- 生产发布：默认不选，除非用户明确解除
- 生产数据库连接：默认不选，除非用户明确解除
- 数据库 migration：默认不选；满足“阶段切换条件”且仅新增版本化 Flyway migration 时，可作为生产增强类 Ready Issue 执行

## 每轮上限

- 每轮最多处理 3 个 Ready Issue；仅限完全无关联、无任何代码关联的任务并行，不能证明完全无关联时按串行处理
- 每轮最多修改 20 个文件
- 无 Ready Issue 不写代码

## 自动合并边界

允许：

- backend/**
- frontend-admin/**
- deploy/**
- docs/backlog/**
- docs/iterations/**
- docs/quality/**

禁止：

- 仓库外文件删除
- .git 删除
- 用户目录删除
- 生产环境变更

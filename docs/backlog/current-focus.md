# Current Focus

## 当前阶段

`ISSUE-008-020：规则治理中心平台化缺口-M2：阈值/窗口/严重度配置生效回归` 已于 2026-07-10 完成正式收口，并计入 `启动迭代-10` 的第 9 个实施型 Ready Issue；当前 Ready 队列已空，待主线程/A 基于长期任务池重新裁决下一条串行 Ready。

`ISSUE-008-019：通知平台平台化缺口-M5：并发重复分发幂等与发送记录一致性回归` 已于 2026-07-10 完成正式收口，并计入 `启动迭代-10` 的第 8 个实施型 Ready Issue。

`ISSUE-008-018：通知平台平台化缺口-M4：同告警重复通知抑制与站内信频控回归` 已于 2026-07-10 完成正式收口，并计入 `启动迭代-10` 的第 7 个实施型 Ready Issue。

`ISSUE-008-017：通知平台平台化缺口-M3：占位渠道可见性与发送记录语义回归` 已于 2026-07-10 完成正式收口，并计入 `启动迭代-10` 的第 6 个实施型 Ready Issue。

`ISSUE-008-016：通知平台平台化缺口-M2：状态变更通知与订阅偏好一致性回归` 已于 2026-07-10 完成正式收口，并计入 `启动迭代-10` 的第 5 个实施型 Ready Issue。

`ISSUE-008-015：报表中心平台化缺口-M6：导出审计留痕与目录一致性回归` 已于 2026-07-10 完成正式收口，并计入 `启动迭代-10` 的第 4 个实施型 Ready Issue。

`ISSUE-008-012：报表中心平台化缺口-M3：导出能力映射与元数据一致性回归` 已于 2026-07-10 完成正式收口，并计入 `启动迭代-10` 的第 1 个实施型 Ready Issue。

`ISSUE-008-013：报表中心平台化缺口-M4：导出入口与筛选快照一致性回归` 已于 2026-07-10 完成正式收口，并计入 `启动迭代-10` 的第 2 个实施型 Ready Issue。

`ISSUE-008-014：报表中心平台化缺口-M5：跨页全量导出与筛选语义一致性回归` 已于 2026-07-10 完成正式收口，并计入 `启动迭代-10` 的第 3 个实施型 Ready Issue。

截至 2026-07-10，`ISSUE-032-001`、`ISSUE-008-011`、`ISSUE-008-012`、`ISSUE-008-013`、`ISSUE-008-014`、`ISSUE-008-015`、`ISSUE-008-016`、`ISSUE-008-017`、`ISSUE-008-018`、`ISSUE-008-019` 与 `ISSUE-008-020` 均已完成正式收口；当前待主线程/A 基于长期任务池重新拆下一条串行 Ready。

## 当前 Ready 状态

- 当前无可直接启动的合格 Ready Issue。
- 选题依据：
- `blocked-issues.md` 当前为空，不存在必须先解阻的前置项。
- `docs/quality/issue-008-020-rule-governance-config-effectiveness.md` 已完成正式归档，当前规则配置字段真实生效边界已进入 `Done`。
- 长期计划 `8.2 规则治理中心` 仍存在“规则阈值 / 执行周期 / 严重度覆盖”等平台化缺口，而 `ISSUE-008-006` 只正式验证了 `enabled=0` 的最小启停语义，尚未把 `threshold_ratio`、`window_days`、`severity_override` 做成当前可复验事实。
- `ISSUE-008-020` 已完成正式收口，并已从 Ready 队列移除。
- `ISSUE-008-019` 已完成正式收口，并已从 Ready 队列移除。
- `ISSUE-008-018` 已完成正式收口，并已从 Ready 队列移除。
- `ISSUE-008-017` 已完成正式收口，并已从 Ready 队列移除。
- `ISSUE-008-015` 已完成正式收口，并已从 Ready 队列移除。
- `ISSUE-008-015` 收口后，报表中心当前一段明确缺口已推进到“导出审计留痕”这一层；后续若继续报表中心，需重新基于新的剩余缺口拆题，不直接续跑。
- `ISSUE-008-014：报表中心平台化缺口-M5：跨页全量导出与筛选语义一致性回归` 已完成正式收口，并已从 Ready 队列移除。
- `ISSUE-008-013：报表中心平台化缺口-M4：导出入口与筛选快照一致性回归` 已完成正式收口，并已从 Ready 队列移除。
- `ISSUE-008-012：报表中心平台化缺口-M3：导出能力映射与元数据一致性回归` 已完成正式收口，并已从 Ready 队列移除。
- `ISSUE-008-011：报表中心平台化缺口-M2：目录权限过滤与入口一致性回归` 已完成正式收口，并已从 Ready 队列移除。
- `ISSUE-032-001` 已从 Ready 队列移除，并以 `docs/quality/issue-032-001-workflow-business-type-registration-closeout.md` 完成正式归档。
- `ISSUE-008-014` 已完成“跨页全量导出仍受当前筛选语义约束”的最小闭环，`ISSUE-008-015` 已继续补齐“导出审计留痕”最小闭环；这仍不等于报表中心平台化整体完成。
- 长期计划 `8.3 通知平台` 当前已完成 M2-M5 一段串行推进；后续若继续通知平台，需重新基于新的剩余缺口拆题，不直接沿用当前 Ready。
- 规则治理中心、WBS / 甘特图、供应商评分 / 采购增强与通知平台后续平台化题本轮仍未证明完全无关联；下一条 P2 题仍需主线程/A 重新裁决，不并行直接续跑。

## 第32条主线 M5 分层冻结

- A 类能力项：报表中心、规则治理中心、通知平台、WBS / 进度计划 / 甘特图、供应商评分 / 采购增强，当前口径统一视为“已完成局部最小回归或已有局部入口，不等于完整平台化完成”。
- A 类能力项后续若要进入 Ready，只能围绕明确的平台化缺口或专项治理缺口拆题，不得把既有最小回归结果重复包装成“整个平台已完成”。
- B 类能力项：`P3` 现场日报 / 移动端、财务集成 / 月结锁账、技术域 / 总工程师驾驶舱、BIM / IFC、AI 辅助，统一视为“长期储备 / 默认冻结”。
- B 类能力项默认不得进入当前 Ready 执行；只有用户明确解除冻结，并同时补齐业务前置、数据前置、验收口径和风险边界后，项目总负责人才能单独裁决是否拆题。

## 第32条主线 M6 UI 与质量遗留复验顺序

- M6 六类遗留统一拆成两条基线：`UI/体验基线`（UI 一致性、可访问性、登录页品牌、`440px` 移动端布局）和 `自动化/门禁基线`（覆盖率、E2E CI）。
- M6 默认顺序是：先完成 `ISSUE-032-007`，再执行 `ISSUE-032-008`；两者都以“复验和分类”为目标，不以“整改完成”为目标。
- 在 `ISSUE-032-007/008` 形成当前基线前，不得把旧截图、旧覆盖率数字或旧 E2E 结论直接当成当前已验证事实。
- 在 M6 基线复验完成前，不得直接发起全站 UI 重构、移动端专项实现或覆盖率 / E2E 硬门禁整改。

## 任务来源

- 长期总任务池：`docs/backlog/cgc-pms-production-enhancement-plan.md`
- 当前执行队列：`docs/backlog/ready-issues.md`
- 当前执行队列没有合格 Ready Issue 时，只允许由项目总负责人结合长期总任务池、`done/ready/blocked` 现状、当前项目状态、用户最新约束、风险和依赖关系，拆出最多 5 个一轮可执行 Ready Issue；该轮只更新 backlog，不直接修改业务代码；后续执行允许最多 3 个完全无关联、无任何代码关联的 Ready Issue 并行，不能证明完全无关联时按串行处理。

## 动态候选范围与选择原则

- 候选任务默认来自长期总任务池，不再固定绑定 `EPIC-004~007` 样例范围。
- 由项目总负责人根据当前项目状态动态判断优先级，优先选择依赖清晰、风险可控、可在本地测试环境收口的任务。
- 选题依据至少包括：长期总任务池、`docs/backlog/ready-issues.md`、`docs/backlog/done-issues.md`、`docs/backlog/blocked-issues.md`、当前项目状态、用户最新约束、任务风险与前置依赖。
- 默认跳过：已 Done 的任务、Blocked 且未解除阻塞的任务、超出当前禁止范围的任务、需要生产环境/生产数据库连接的任务、未满足下方“阶段切换条件”的数据库 migration 任务。
- 当用户明确要求串行且只写 1 条 Ready 时，优先锁定单一平台化缺口题，不保留并行候选。

## 阶段切换条件

当当前 Ready 队列中生产准入类回归任务已清空，且 `blocked-issues.md` 没有未解除阻塞项时，AutoPilot 可以从长期计划的下一阶段拆分生产增强类 Ready Issue。

允许进入下一阶段的候选范围：

- P2 报表中心、规则治理中心、通知平台、WBS / 进度计划 / 甘特图、供应商评分 / 采购增强。
- 上述 P2 能力即使已有最小回归或局部入口，也只代表具备继续治理的基础，不代表完整平台化完成。
- 任务必须能在本地 dev/test 环境闭环，不依赖生产环境、生产数据库或外部生产平台。
- 第一轮只拆出最多 5 个 Ready Issue；每个 Ready Issue 必须写清允许修改范围、禁止事项、验证命令、归档报告和是否需要新增 migration。
- 如果涉及数据库结构，只允许新增版本化 Flyway migration，不得修改已应用 migration；Ready Issue 必须包含表/字段最小范围、回滚口径和本地验证命令。
- 若新增 migration 同时触达权限、安全、租户隔离、金额口径、审批状态机或生产集成，默认不自动执行，先进入人工裁决。

## 当前禁止执行

- 总工程师相关任务：若长期计划中仍标为冻结或缺少基础域，默认不选
- BIM 相关任务：若长期计划中仍标为冻结或缺少基础域，默认不选
- AI 相关任务：若长期计划中仍标为冻结或缺少基础域，默认不选
- `P3` 现场日报 / 移动端：默认不选，除非用户明确解除长期储备冻结并补齐移动端/H5/PWA 验收前置
- 财务生产集成：默认不选，除非用户明确解除
- 月结锁账：默认不选，除非用户明确解除长期储备冻结并补齐财务口径、期间关闭规则和回滚口径
- 生产发布：默认不选，除非用户明确解除
- 生产数据库连接：默认不选，除非用户明确解除
- 数据库 migration：默认不选；满足“阶段切换条件”且仅新增版本化 Flyway migration 时，可作为生产增强类 Ready Issue 执行

## 每轮上限

- 每轮最多处理 3 个 Ready Issue；仅限完全无关联、无任何代码关联的任务并行，不能证明完全无关联时按串行处理
- 每轮最多修改 20 个文件
- 无 Ready Issue 不写代码

## 自动合并边界

允许：

- backend/**
- frontend-admin/**
- deploy/**
- docs/backlog/**
- docs/iterations/**
- docs/quality/**

禁止：

- 仓库外文件删除
- .git 删除
- 用户目录删除
- 生产环境变更

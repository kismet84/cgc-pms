# 历史开发记录

> 记录 CGC-PMS 项目从工程启动到当前的完整开发历程。
> 编写日期：2026-06-20
> 最近更新：2026-06-24

---

## 项目概述

CGC-PMS 是面向建筑工程总包项目的全过程管理系统，覆盖项目、合同、采购、库存、分包、成本、付款、结算、发票、审批、预警、驾驶舱、组织和系统管理等业务领域。

技术栈：Spring Boot 3.3 + Vue 3 + TypeScript + MySQL 8.0 + MyBatis-Plus + Flyway + Redis + MinIO + Docker。

---

## 开发时间线

### 第 0 阶段：工程启动（2026-06-08 ~ 06-10）

**目标**：把设计文档变成可运行的系统骨架。

| 日期 | 里程碑 |
|------|--------|
| 06-08 | Git 仓库建立，monorepo 目录结构定型（backend / frontend-admin / deploy / docs） |
| 06-08 | 技术栈冻结：Java 21 + Spring Boot 3.3 + Vue 3 + Vite + Ant Design Vue + MyBatis-Plus |
| 06-08 | Docker Compose 开发环境就绪（MySQL 8.0 + Redis 7 + MinIO） |
| 06-09 | Spring Boot 工程初始化，统一响应结构、全局异常处理、Swagger 就绪 |
| 06-09 | Vue 3 工程初始化，Layout、登录页、路由框架就绪 |
| 06-09 | Flyway 迁移脚本 V1~V8，覆盖系统表、项目表、合同表、审批表、成本表、文件表 |
| 06-10 | 登录鉴权（JWT + RBAC + Refresh Token）完成 |
| 06-10 | 项目和合作方 CRUD 完成（后端 + 前端） |

**交付物**：可登录的后台骨架，MySQL 一键初始化，Swagger 可访问。

---

### 第 1 阶段：审批引擎 + 合同中心（2026-06-10 ~ 06-11）

**目标**：跑通审批引擎 POC，打通第一条合同业务闭环。

| 日期 | 里程碑 |
|------|--------|
| 06-10 | 审批引擎核心实现：提交、同意、驳回、撤回、转办、加签、会签、或签 |
| 06-10 | 审批并发控制：`taskVersion` 乐观锁 + `idempotencyKey` 幂等机制 |
| 06-10 | `availableActions` 后端返回可用操作，前端动态渲染按钮 |
| 06-10 | 合同台账完整页面：筛选、分页、KPI 卡片、列设置、VxeTable 表格 |
| 06-10 | 新建合同 4 步分步表单：基本信息 → 清单 → 付款条件 → 提交审核 |
| 06-10 | 文件上传系统（MinIO + 预签名 URL + 业务附件关联） |
| 06-11 | 合同提交审批 + 审批回调（`ContractWorkflowHandler`）打通 |
| 06-11 | `isCritical` 事务一致性机制：回调异常触发 `@Transactional` 回滚 |
| 06-11 | 合同锁定成本生成（`CostGenerationService.generateLockedCost`）+ 幂等机制 |
| 06-11 | 集成测试 11 用例，H2 + MySQL 双环境验证 |

**交付物**：审批引擎就绪，合同从草稿到审批通过到成本生成的完整闭环可跑通。

---

### 第 2 阶段：成本归集与资金闭环（2026-06-11 ~ 06-14）

**目标**：建立多来源成本归集体系，打通付款与资金链路。

| 日期 | 里程碑 |
|------|--------|
| 06-11 | `CostGenerationStrategy` 策略模式落地：合同、收货、分包计量、变更签证 5 种策略 |
| 06-12 | 材料验收模块 + 验收成本生成 |
| 06-12 | 分包计量模块 + 计量成本生成 |
| 06-12 | 变更签证模块 + 变更成本调整 |
| 06-12 | 付款申请模块 + 付款审批 + 财务回写（`PayRecordService.writeback`） |
| 06-12 | 付款依据关联（验收 → 付款、计量 → 付款、签证 → 付款） |
| 06-13 | 成本汇总：项目级目标成本 vs 实际成本 vs 动态成本 |
| 06-14 | 成本溯源：`sourceType` + `sourceId` + `sourceItemId` 三级追溯 |
| 06-14 | 第2阶段测试报告 |

**交付物**：多来源成本可自动归集，付款申请可追溯业务依据，成本台账反查来源。

---

### 第 3 阶段：成本分析与合同深化（2026-06-14 ~ 06-16）

**目标**：深化成本管理能力，完善合同全生命周期。

| 日期 | 里程碑 |
|------|--------|
| 06-14 | 成本科目树管理（一级/二级/三级科目） |
| 06-15 | 目标成本版本管理 + 目标成本审批 |
| 06-15 | 动态成本分析（已发生 + 预计待发生） |
| 06-15 | 成本偏差预警（动态成本 vs 目标成本） |
| 06-16 | 合同变更台账 + 变更金额联动 |
| 06-16 | 结算管理模块 + 结算审批 |
| 06-16 | 合同状态机完善（DRAFT → APPROVING → PERFORMING → SETTLED） |
| 06-16 | 第3阶段测试报告 |

**交付物**：成本科目体系与目标成本管理就绪，合同变更与结算闭环打通。

---

### 第 4 阶段：组织架构与库存发票集成（2026-06-16 ~ 06-18）

**目标**：补齐组织、库存、发票、通知等横向支撑模块。

| 日期 | 里程碑 |
|------|--------|
| 06-16 | 组织架构模块（公司 → 部门 → 岗位） |
| 06-17 | 项目成员管理 + 用户组织关联 |
| 06-17 | 库存管理：仓库、库存台账、出入库流水、采购申请 |
| 06-17 | 发票管理：创建、登记、核验 |
| 06-18 | 站内消息通知 + SSE 实时推送 |
| 06-18 | 审批抄送（`wf_cc`） |
| 06-18 | 驾驶舱 5 角色（PM/商务/成本/财务/管理层） |
| 06-18 | 预警规则引擎 + 定时批处理评估 |
| 06-18 | Flyway V33~V39（7 个迁移脚本）+ 菜单权限种子数据 |

**交付物**：组织/库存/发票/通知模块就绪，驾驶舱与预警系统上线。

---

### 审计修复与质量加固（2026-06-12 ~ 06-20）

穿插在各阶段之间的持续质量改进：

| 日期 | 工作 |
|------|------|
| 06-11 | 安全审计：方法级 RBAC 全覆盖、Refresh Token 黑名单、文件上传校验、密钥脱敏 |
| 06-12 | 第一次全量代码审查 + 审查问题修复 |
| 06-12 | 第二次全量代码审查（二次验证） |
| 06-12 | P3 中期改进：H2 schema 漂移修复、通知 bizId、测试隔离、SSE 异常处理 |
| 06-13 | 全面项目审查（6 路并行：安全/代码质量/架构/前端/数据库/配置部署） |
| 06-13 | 5 个阻断级问题修复（角色零权限、SSL 配置、SSE 缓冲、输入校验、Mass Assignment） |
| 06-15 | 全量审计报告 + 修复 |
| 06-17 | 全量系统代码审查 |
| 06-18 | 技术债务审计 + 修复 |
| 06-19 | 列表页统一改造 |
| 06-20 | 深度审计修复（task-029 完整闭环） |
| 06-21 | 库存台账表格优化、ESLint 全量清零（67→0）、build.py 构建脚本 |

### 第 6 阶段：主链路验收与技术债务清零（2026-06-22）

**目标**：逐链路验证 5 条业务闭闭环，修复发现的问题，清零 4 项技术债务。

| 日期 | 里程碑 |
|------|--------|
| 06-22 | 5 条业务主链路逐文件验证（项目→合同→审批、采购→验收→入库→出库、分包→计量→成本、付款→回写→发票、结算→归档），60+ 检查点 |
| 06-22 | **P0 修复**：验收→入库自动联动 — `MaterialReceiptWorkflowHandler.onApproved()` 注入 `MatStockService`，遍历合格明细自动调用 `stockIn(receipt.getWarehouseId(), materialId, qualifiedQty, "MAT_RECEIPT", receiptId)` |
| 06-22 | **P0 修复**：库存流水来源追溯 — `stockIn/stockOut` 新增 `(sourceType, sourceId)` 重载，`StockTransactionDTO` 新增可选字段，Controller 透传 |
| 06-22 | **P1 修复**：验收页添加仓库选择下拉框，`onMounted` 加载启用仓库列表 |
| 06-22 | **P2 修复**：`SettlementStatusConstants` 常量类（3 组状态常量），`StlSettlement.settlementStatus` 注释修正 |
| 06-22 | **技术债务 #1**：Nginx 代理目标可配置化 — `nginx.conf` 硬编码 `backend:8080` → `${BACKEND_URL}` 环境变量 |
| 06-22 | **技术债务 #2**：`notification.ts` baseURL 逻辑重复 — 改为复用 Axios 实例 `service.defaults.baseURL` |
| 06-22 | **技术债务 #3**：H2/MySQL Flyway 差异修复 — 补 V85 到 H2（V75/V88 已有 Java 迁移；V89 为 MySQL 专属种子数据修复） |
| 06-22 | **技术债务 #4**：前端 `any` 类型清零 — 31→0 处（20 处 `SelectOption` 类型、4 处 `CallbackDataParams`、4 处 `Record<string, unknown>`、3 处其他具体类型），新增 `types/ui.ts` |
| 06-22 | **误报排除**：accounting 模块 8 文件全量审查，无 `@Deprecated` 代码，从债务清单移除 |
| 06-22 | `未来开发计划.md` 更新 — 优先级路线图、版本规划、技术债务清零 |
| 06-22 | `历史开发记录.md` 更新 — 第 6 阶段记录 |

**交付物**：5 条链路全部闭环，10 个问题修复（6 业务 + 4 债务），前端 any 类型清零，H2/MySQL schema 对齐。

---

### 第 7 阶段：质量加固与全量测试覆盖提升（2026-06-23）

**目标**：大幅提升后端测试覆盖率，消除 H2 全量测试数据污染，推进安全加固全部落地，前端大文件拆分 + `any` 类型清零。

| 日期 | 里程碑 |
|------|--------|
| 06-23 | **Phase A: Controller 测试收口** — 18 个新测试文件 (~180 方法)，全部零覆盖 Controller 收口完成，消除所有零覆盖包（17→0） |
| 06-23 | **Phase B: Workflow Handler 测试** — 10 个新测试文件 (~59 方法)，覆盖审批生命周期（提交→同意→驳回→撤回→转办→会签→或签→加签） |
| 06-23 | **Phase C: Service 层补充** — 6 个文件增强 (~32 方法)：`MatPurchaseOrderServiceTest`(新)、`SubMeasureServiceTest`(新)、`ContractRevenueServiceTest`(扩展)、`OverheadAllocationServiceTest`(扩展)、`StlSettlementServiceTest`(扩展)、`MatReceiptServiceTest`(修复) |
| 06-23 | **后端测试覆盖率** — 68%→73% Instruction, 49%→53% Branch（932→1198 tests, +266 测试方法, +36 测试文件, 零覆盖包 17→0） |
| 06-23 | **Surefire 优化** — `forkCount` 1C→1, `reuseForks` true, `parallel=classes`, `threadCount=2`；解决"太多 OpenJDK Platform binary"和 CPU 多核满载问题 |
| 06-23 | **H2 全量测试数据污染清零** — 修复 `WorkflowQueryServiceTest` 分页断言污染（按 taskId 精确匹配替代硬编码 total=1）+ `CtContractPaymentTermServiceTest` H2 表锁超时（`Db.saveBatch` → 逐条 `mapper.insert`）；全量套件 1198/1198 PASS (0 failures, 0 errors) |
| 06-23 | **安全加固全部落地** — `scripts/check-sql-safety.ps1` 静态扫描 6 种注入模式，本地 PASS；`RateLimitAspect` 多维限流（IP/USER/TENANT/IP_AND_ACCOUNT）+ 账号锁定保护（R-27）；`FileTypeValidator` 扩展名+MIME+魔术字节三元校验；`@AuditedOperation` 操作审计（6 Controller 18 端点已标注） |
| 06-23 | **前端大文件拆分** — 6 页面全部分拆：org(1333→576)、dashboard(1231→335)、stock(900→295)、receipt(807→257)、invoice(841→263)、contractLedger(802→270)，均 ≤500 行；`org/index.vue` 进一步优化 576→473 行（提取 `OrgMetricStrip` 组件） |
| 06-23 | **`StlSettlementService` 拆分** — 744 行拆为 QueryService(412行) + WriteService(258行) + Assembler(83行) + 门面(100行)，Controller 直接注入新服务，门面已标注 `@Deprecated` |
| 06-23 | **前端 `any` 类型清零** — 31→0 处全部替换为具体类型（`SelectOption`、`CallbackDataParams`、`Record<string, unknown>` 等） |
| 06-23 | **结算状态常量化** — 新建 `SettlementStatusConstants`（生命周期/审批/定案三组），3 文件 12 处字面量全部替换 |
| 06-23 | **`StlSettlement.settlementStatus` 注释修正** — 「结算最终归档状态」→「结算定案状态」，注明与项目归档（`archiveFlag`）的区别 |

**交付物**：后端测试覆盖 73/53（+36 文件 +266 测试），H2 全量 1198/1198 PASS，安全加固 4 项全部落地（SQL 门禁/多维限流/文件校验/操作审计），前端 6 模块大文件拆分 + any 类型清零，结算服务拆分 + 状态常量化。

---

### 2026-06-24：开发计划审核与状态纠偏（早间）

| 日期 | 里程碑 |
|------|--------|
| 06-24 | `未来开发计划.md` 全面审核 — 逐项核验计划声明与代码库现状的一致性，发现 7 处偏差并修订 |
| 06-24 | **CI 状态纠正** — `GitHub Actions 自动化测试` 从 "P1 待办" 移至 "✅ 已完成"（`.github/workflows/ci.yml` 已覆盖 backend-test/type-check/frontend-build/E2E） |
| 06-24 | **SQL 门禁状态修正** — 从 "✅ 已完成 接入 CI" 修正为 "✅ 脚本完成 / ⚠️ CI 待接入"（脚本本地 PASS，但 CI workflow 未添加扫描步骤） |
| 06-24 | **JaCoCo 门禁风险标注** — 计划中当前覆盖率 73/53 与 `pom.xml` JaCoCo check 阈值 80/70 存在冲突，`mvn verify` 可能在 CI 中失败 |
| 06-24 | **前端覆盖率数据纠正** — 计划中写 6.7% 更正为 9.79%（与 `vitest.config.ts` 基线一致） |
| 06-24 | **`StlSettlementService` 拆分** — 从 "近期 P1" 移除（已完成），新增技术债务项 "门面退役（迁移剩余调用方后移除 deprecated 门面）" |
| 06-24 | **Actuator/Prometheus 拆分** — 原 "Spring Boot Actuator + Prometheus P1" 拆为 "Actuator 基础监控 ✅ 已完成" + "Prometheus 指标导出 P1" |
| 06-24 | `历史开发记录.md` 更新 — 第 7 阶段 + 2026-06-24 记录，项目数据刷新 |

---

### 全系统 UI 重构 (2026-06-24)

**目标**：以 `docs/00-UI-Design-Baselines-and-Code-Specifications.md` 为唯一视觉基准，统一全系统 66 个页面/组件的视觉风格。保留 `lg-*` 作为布局和组件类名体系。

| 日期 | 里程碑 |
|------|--------|
| 06-24 | **Phase A: 设计基准与 Token 统一** — 全局 CSS Token 覆盖：字体体系（6 级字号）、间距体系（4 级）、品牌渐变（`--brand-gradient-logo` / `--brand-gradient-avatar`）；`BasicLayoutShell` 5 处硬编码→CSS 变量；`SidebarMenu` 选中态 `#edf5ff`→`var(--primary-soft)`；新建 `docs/quality/ui-refactor-checklist.md`（66 页面迁移状态追踪） |
| 06-24 | **Phase B: 全局布局与导航统一** — `BasicLayoutAsync` 4 处硬编码颜色→CSS 变量（logo 渐变、avatar 渐变、hamburger 色）；页面包装结构统一使用 `lg-page` / `app-page` |
| 06-24 | **Phase C: 列表页/台账页统一** — 搜索栏增强 `lg-search-bar`（40px 统一高度 + flex-wrap 多行筛选 + 扩展筛选 slot）；工具栏增强 `lg-toolbar`（34px 统一按钮 + 左右分组）；表格统一 `lg-table-wrap`（Ant Design 全局覆盖 + `lg-tag` 通用标签类 4 变体 + vxe-table 边框/行高/hover 统一） |
| 06-24 | **Phase D: KPI 与分析面板统一** — 项目列表页 KPI `project-stats-grid`→`lg-kpi-strip`（5 卡片 `lg-kpi-card` + `is-warn` 风险态）；分析面板 `project-widget`→`lg-panel` + `lg-type-list`；删除 103 行重复自定义 CSS；全模块 KPI/分析面板已对齐 `lg-*` 体系 |
| 06-24 | **Phase E: 表单/弹窗统一** — 新建 `lg-modal-form` 全局 CSS 模块（125 行）：统一 Modal header/body/footer 内边距、确认/取消按钮顺序（`row-reverse`）、表单栅格 80px 标签右对齐、Input/Select/InputNumber 统一 32px 高度 + `.is-vertical` 变体 + `.lg-form-grid` 双列栅格；7 个弹窗文件迁移（system/users、system/dict、system/roles/PermissionModal、partner、material/dictionary、org/CompanyModal、org/DepartmentModal、org/PositionModal） |
| 06-24 | **Phase F: 文案统一** — 新建 `frontend-admin/src/constants/uiText.ts`（30 个中文常量：空状态/操作/按钮/提示/分页 5 大类）；`request.ts` 英文化中（`'Request failed'`→`'操作失败，请稍后重试'`、`'Network error'`→`'网络异常，请检查连接'`） |
| 06-24 | **Phase G: 组件抽象** — 新建 5 个 `lg-*` 通用组件：`LgPageHeader`（面包屑+标题+操作区）、`LgSearchBar`（关键词+查询/重置+扩展 slot）、`LgToolbar`（左主操作+右辅助 slot）、`LgKpiCard`（label/value/unit/warn/bar/hint props）、`LgEmptyState`（统一空状态 `'暂无数据'`），全部 TypeScript + `<script setup>` + 使用现有 `lg-*` CSS 类 |
| 06-24 | **Phase H: 质量门禁** — 新建 `e2e/ui-refactor-smoke.spec.ts`（7 核心页面渲染验证：项目列表/合同台账/成本台账/付款申请/库存台账/审批中心/系统用户）；`package.json` 添加 `test:e2e:ui` 脚本；最终验证：`pnpm build` ✅（14s, 0 错误）、`vitest` ✅（38 文件/174 测试）、Playwright E2E ✅（7/7, 5.8s） |

**统计**：修改 23 个文件，新建 11 个文件，删除 ~300 行重复 CSS，零 TypeScript 错误，零测试回归。

---

### 2026-06-24：CI 风险解除与运维基础设施就绪（凌晨）

| 日期 | 里程碑 |
|------|--------|
| 06-24 | **JaCoCo 基线确认** — 实际覆盖率 79.8%/57.6% 超 CI 阈值 73%/53%，`mvn verify` 不阻塞 merge |
| 06-24 | **SQL Safety 已接入 CI** — 确认 `ci.yml` 第 167-173 行已有 `sql-safety-scan` job，6 种注入模式覆盖 |
| 06-24 | **SQL 安全规范文档** — `docs/11-安全规范.md` 补充豁免机制与误报处理说明 |
| 06-24 | **Prometheus 指标导出就绪** — `micrometer-registry-prometheus` 已在 `pom.xml`，dev/prod 已配置 endpoint 暴露 |
| 06-24 | **监控基础设施** — `deploy/monitoring/prometheus.yml` + `docker-compose.monitoring.yml` 新增，运维手册补充指标清单 |
| 06-24 | **数据库备份恢复** — `scripts/mysql-backup.ps1`（全量备份+保留策略）+ `scripts/mysql-restore.ps1`（恢复+健康检查），`deploy/.env.example` 补充备份变量 |
| 06-24 | `docs/10-部署运维手册.md` 补充 Prometheus 指标和备份恢复章节 |

**交付物**：4 项运维基础设施全部落地（SQL 门禁 CI 接入确认 / Prometheus 指标导出 / 监控配置 / 数据库备份恢复）。

---

### 第 8 阶段：第二批优先功能包（2026-06-24）

**目标**：一次性落地 3 个 P1/P2 功能需求，消除未来计划中的近期待办项。

| 日期 | 里程碑 |
|------|--------|
| 06-24 | **Phase 1-4：分包计量与任务显式关联** — `sub_measure` 新增 `sub_task_id`（可空），Flyway V93 双环境迁移；`SubMeasureService` 创建/更新增加 5 级校验链（存在性→租户→项目→合同→合作方）；查询回填任务展示字段；`SubMeasureServiceTest` 新增 7 个验证用例；前端计量页新增关联分包任务选择器 + 联动过滤 |
| 06-24 | **Phase 1-A3：发票强制关联付款记录** — `InvoiceService.create()` 新增 `payRecordId` 非空校验 + `PayRecord` 引用完整性校验；前端 `InvoiceFormModal` 付款记录从"可选(allow-clear)"改为"必选(required)"；新增 RED 测试 12/13；E2E 更新适配 |
| 06-24 | **Phase 2-A1：领料申请模块（全栈）** — Flyway V94 双环境迁移（`mat_requisition` + `mat_requisition_item` 双表）；后端 8 文件：Entity/VO/Mapper/Service/Assembler/Controller/Handler；`MaterialRequisitionWorkflowHandler.onApproved()` 审批通过自动调用 `MatStockService.stockOut("MAT_REQUISITION", ...)` 出库；前端 7 文件：Type/API/Vue/Composables；路由 `/inventory/material-requisition` + 导航菜单 |
| 06-24 | **Phase 3-A2：项目自动归档通知** — `ProjectArchiveNotifier`：`@Async` 异步检测 4 条件（合同已结算+付款已付清+结算已定案+审批流完结），满足时发送站内通知；`SettlementWorkflowHandler.onApproved()` 末尾触发异步检查；设计边界：仅通知不自动归档 |
| 06-24 | **Phase 4：全量回归测试修复** — 发票 `payRecordId` 强制关联导致 8 个 Controller/Validation 测试回归：`InvoiceControllerTest`/`InvoiceValidationTest` 增加 `@BeforeEach` 种子 PayRecord + JSON body 补 payRecordId；修复后 1262→1270 全量 PASS |

**提交**：`e0d73a69` / `b2ccf97c` / `d496320b` + `04c7e820` + `f1a70319` / `756b96b3` / `6bc860a5` → `acd4e78d`

**交付物**：3 个功能模块全栈交付（分包任务关联 / 领料申请 / 项目归档通知），发票强制关联，8 个回归测试修复。未来计划中 3 项近期待办消除。

---

### UI 视觉方案微调（2026-06-24）

| 日期 | 里程碑 |
|------|--------|
| 06-24 | **参考页视觉方案调整** — 搜索栏卡片化、风险 KPI 左红边框、侧边栏激活态右蓝竖条、表格圆角 8px |
| 06-24 | **项目与组织页视觉复刻** — `global.css` 全局样式增强、`org/index.vue` 布局优化、`OrgMetricStrip` 组件样式刷新、`project/index.vue` 列表页视觉升级 |

---

### 第 5 阶段：收尾与清理（2026-06-21）

| 日期 | 里程碑 |
|------|--------|
| 06-21 | ESLint 全量清零（68→0 errors, 255→0 warnings, 30 files） |
| 06-21 | 库存台账表格优化（搜索栏增强、列设置下拉、中文来源类型映射、详情 Drawer、列排序） |
| 06-21 | 前端单测从 136→154 覆盖（ApprovalWorkList 正则修复、新增测试） |
| 06-21 | 添加 `rebuild.py` 构建脚本 |
| 06-21 | 排除 CLAUDE.md/AGENTS.md/.agent-runtime/.github/.serena/docs/agents 至 .gitignore |
| 06-21 | 远程仓库清理（删除 3 个过期 codex/* 分支、移除历史遗留非项目文件） |

---

## 各阶段交付物汇总

| 阶段 | 后端文件 | 前端文件 | 迁移脚本 | 测试用例 |
|------|----------|----------|----------|----------|
| 第 0 阶段 | ~30 | ~15 | V1~V8 | — |
| 第 1 阶段 | ~50 | ~35 | V9 | 11 |
| 第 2 阶段 | ~80 | ~45 | V10~V25 | 50+ |
| 第 3 阶段 | ~60 | ~40 | V26~V32 | 80+ |
| 第 4 阶段 | ~50 | ~35 | V33~V43 | 174 |
| 审计修复 | ~30 | ~20 | V44~V47+ | 206 |
| 收尾清理 | — | ~50 | — | 154（前端 35/35） |
| 链路验收+债务清零 | ~10 | ~15 | V85(H2) | 14 后端库存测试 |
| 第 7 阶段（测试+安全+拆分） | ~36 测试文件 | ~6 大文件拆分 | — | +266 后端 + 1,119→1,198 全量 PASS |
| 计划审核与纠偏 | — | — | — | — |
| CI 风险解除+运维基础 | — | — | — | — |
| 第 8 阶段（功能包） | ~10 | ~10 | V93~V94 | +8 回归修复 |
| UI 视觉微调 | — | ~5 | — | — |

---

## 关键架构决策

1. **模块化单体**：业务模块多、事务边界紧，选择模块化单体而非微服务。后续团队规模扩大后再考虑拆分。
2. **审批引擎自研**：从零构建审批引擎而非集成第三方，因为业务审批与成本生成、状态联动紧密耦合。
3. **MyBatis-Plus + Flyway**：放弃 JPA/Hibernate，选择 MyBatis-Plus 以保留 SQL 可控性；Flyway 管理所有 DDL/DML 变更。
4. **雪花 ID + JSON 字符串**：全局 `ASSIGN_ID` 生成 19 位 ID，JSON 序列化为字符串避免前端精度丢失。
5. **策略模式成本生成**：`CostGenerationStrategy` 接口解耦各业务模块的成本生成逻辑，新增来源只需实现策略即可。
6. **`isCritical` 事务一致性**：审批回调通过 `isCritical()` 控制异常是否回滚审批事务，保证审批状态与业务状态强一致。

---

## 项目数据（截至 2026-06-24）

| 维度 | 数据 |
|------|------|
| 后端 Java 文件 | ~315+ |
| 前端 TypeScript/Vue 文件 | ~185+ |
| Flyway 迁移版本 | MySQL V1~V94，H2 V1~V94 |
| 后端测试 | 1,270 tests / 0 failures (79.8% instruction / 57.6% branch) |
| 前端单元测试 | 174 tests / 37 文件 (9.79% lines, CI 基线 9%) |
| E2E 测试 | 84 tests / 17 spec 文件 |
| ESLint | 0 errors / 0 warnings |
| TypeScript `any` 类型 | 0 |
| SQL 注入扫描 | CI 已集成（6 种模式，零命中） |
| 后端测试文件 | 124 个 |
| 安全加固 | 4 项全部落地（SQL 门禁 CI 接入/多维限流/文件校验/操作审计） |
| 前端大文件 | 6 模块全部分拆，均 ≤500 行 |
| UI 设计系统 | `lg-*` 体系覆盖全 66 页面，CSS Token 固化，品牌渐变可配置 |
| Docker 容器 | 5 个（MySQL / Redis / MinIO / Backend / Frontend） |
| 主业务链路 | 5 条全部闭环 |
| 新增业务模块 | 领料申请（`requisition`）、项目归档通知（`ProjectArchiveNotifier`） |
| 运维基础设施 | 数据库备份恢复脚本、Prometheus 监控配置、SQL 安全规范文档 |

---

> 历史归档文档见 `archive/` 目录，包含各阶段详细审计报告、测试报告、开发计划和验收文档。

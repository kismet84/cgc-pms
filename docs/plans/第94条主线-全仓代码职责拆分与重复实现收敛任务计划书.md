# 第94条主线：全仓代码职责拆分与重复实现收敛

**Goal:** 以当前干净 `master` 为事实基线，在不改变路由、API、权限、租户、金额、状态机、事务、数据库和用户可见结果的前提下，分批拆解已证实的多路由页面、God Service、重复政策源和循环依赖，合并或删除高置信透明包装与等价 helper，使主要变更单元恢复为单一业务职责、可独立测试、可逐批回滚。 **Architecture:** 保留模块化单体、Vue SFC、现有 Router/Pinia、Spring Controller/Service/Mapper、领域事务、Flyway、文件生命周期、工作流状态机、前端契约包和既有 façade/barrel；采用“当前 Codemap 与行为基线 → 公共契约稳定 → 只读查询/纯逻辑先拆 → 单路由/单聚合金丝雀 → 高风险权限与事务策略后拆 → 全链验真”的最小路径。禁止建设微服务、通用状态框架、Query Bus、通用规则引擎、第二权限/金额/文件事实系统或新第三方依赖；禁止把千行页面原样搬进千行 composable，也禁止按行数机械拆文件。

> 编制日期：2026-08-13
>
> 计划状态：`AUTHORIZED / G0_IN_PROGRESS / GIT_DELIVERY_AUTHORIZED`
>
> 编制基线：`master@09b7e59bf72cfcf9681ba4257386089fc0743503`，与 `origin/master` 一致，工作区干净
>
> 环境边界：仅本地 dev/test/demo；不存在 prod、生产或目标环境
>
> 当前授权：用户已明确授权实施第94条、研判并解决阻塞后继续，以及全部任务收口后的任务自有改动 commit、受保护 push/PR/合并、post-merge 验真与已合并源分支清理；不授权非本地环境、生产数据库、强推、绕过保护、Tag 或 Release
>
> 唯一整改、状态与验收载体：本计划的 `M94-F01～M94-F18`；`docs/backlog/current-issues.json` 不复制结构型子项

## 1. 基线、去重与计划裁决

### 1.1 当前事实基线

1. 当前仓库已恢复为干净 `master@09b7e59b`，与 `origin/master` 一致；本地、远端分支和现有 worktree 中均无旧第94条主线载体。
2. `docs/plans/` 当前最高正式编号为第93条；未发现“全仓代码职责拆分与重复实现收敛”的同主题计划、质量报告或活动 Issue，因此本计划可使用第94条编号。
3. 当前可执行源码、测试和脚本约 1,739 个、315,411 行，其中超过 1,000 行的文件 41 个；主要分布如下：

| 范围 | 文件数 | 物理行 | 千行级文件 |
| --- | ---: | ---: | ---: |
| `backend/src/main/java` | 866 | 93,784 | 6 |
| `backend/src/test/java` | 369 | 96,298 | 7 |
| `frontend-admin-v2/src` | 131 | 62,790 | 26 |
| `frontend-admin-v2/tests` | 79 | 18,203 | 0 |
| `packages/frontend-contracts/src` | 17 | 5,325 | 1 |
| `scripts` | 157 | 21,840 | 1 |
| `tools` | 32 | 1,966 | 0 |

4. 规模只用于定位热点，不作为拆分判据。Vue SFC 将 `<script setup>`、`<template>`、`<style scoped>` 放在同一文件完全合规；只有文件同时承担多个路由、多个独立状态机或跨域事务时才进入拆分范围。
5. 编制时 `node scripts/codemap/generate-codemap.mjs --verify` 成功，但 lock 的 `generation_base_commit` 仍为 `6e8dfafb8b7d`、`input_scope_dirty=true` 且 `documentation` stale，不能作为当前 HEAD 的代码修改依据。实施前必须在干净隔离 worktree 重生成三件套并重新回答调用方、影响面、测试覆盖三问。
6. `docs/backlog/current-focus.md` 尚未登记第93条已完成事实。本次只同步焦点索引，不把该文档变成第二整改状态源。

### 1.2 与既有主线去重

| 既有主线 | 已有事实 | 第94条边界 |
| --- | --- | --- |
| 第88条 | 已删除死页面、占位通知渠道和 renderer spike，并收口 Flyway 半门禁 | 不重开已删除资产，不扫描或清理受保护历史目录 |
| 第90条 | 资金与成本字段权威源、状态、迁移和退役仍整体未授权 | 第94条只做行为等价结构拆分；不得实施字段、迁移、金额口径、双读、退役或状态语义；重叠文件两主线必须串行 |
| 第91条 | 已完成查询性能、Dashboard 服务边界、中性展示 helper、assembler 显式 ID，以及采购三个私有 application 的第一步 | 复用这些结果；第94条只承接尚存的 2,734 行采购多路由页面及其他剩余职责，不重复报功 |
| 第92条 | 已完成可靠性、安全与验证治理整改 | 不重建备份、覆盖率、CORS/CSRF、MySQL 或 Codemap 治理能力 |
| 第93条 | 已完成审计字段防篡改与同根写入边界整改 | 不改其 DTO、字段只读、迁移或审计语义，除非第94条移动代码时需要等价适配 |

### 1.3 拆分、合并和保留原则

1. 按路由、业务状态机、事务边界和变更频率拆；不按固定行数切片。
2. 先保留原 façade、barrel、URL 和公开方法，再迁移内部实现；调用方稳定后才删除兼容层。
3. 只合并语义和失败行为完全相同的代码；同名但粒度不同的 DTO、金额字段和状态不得合并。
4. 优先删除透明包装、重复映射和无行为 helper；不得为“统一风格”新增工厂、基类或通用框架。
5. 查询、组装、纯算法先拆；权限、金额、库存、文件副作用和写状态机最后拆，并保持原事务和锁序。
6. 每个批次必须能独立回滚，禁止跨批次同时修改同一个 SFC、Service、Mapper、契约文件或测试夹具。

## 2. 发现项登记与目标裁决

下列条目是计划内结构发现，不是 `current-issues.json` 的产品 Backlog。优先级表示实施顺序和风险，不代表当前存在生产故障。

| ID | 优先级 | 当前证据 | 目标裁决 |
| --- | --- | --- | --- |
| `M94-F01` | P0/治理 | Codemap 基线落后当前 HEAD；焦点索引缺第93条 | G0 重生成 Codemap，冻结文件所有权；计划期同步索引 |
| `M94-F02` | P0/契约 | `commercial.ts`、`supply-chain.ts`、`finance.ts` 契约聚合多个领域；`system-management.ts` 重复定义 `PageResult` | 按领域拆内部文件，保留原 barrel；完全相同类型只留一份，不同投影明确改名 |
| `M94-F03` | P1/前端 | `navigation/catalog.ts` 约 708 行数据与选择器混合；`router.ts` 约 613 行同时维护组件映射、导航路由、上下文路由与重定向 | 拆 types、分域 catalog、access selector、route registry；Router 与导航目录保持两个职责，不合成巨型注册表 |
| `M94-F04` | P0/供应链 | `PurchaseExecutionPage.vue` 2,734 行，被申请、订单、验收三个路由复用 | 拆 `PurchaseRequestWorkspace`、`PurchaseOrderWorkspace`、`MaterialReceiptWorkspace`；共用详情、附件与候选组件；保留三套事务/回滚语义 |
| `M94-F05` | P1/财务 | `ReceivablesWorkspacePage.vue` 2,035 行承载付款、费用、收入、发票四个路由；`FinanceControlWorkspacePage.vue` 承载六个路由 | 按路由拆工作台，共用候选、附件和 Trace；第91条查询性能已关闭，本项只拆呈现/协调层，不改金额、来源和冲正语义 |
| `M94-F06` | P0/合同 | `ContractPage.vue` 1,891 行同时承载台账、新建、详情、编辑 | 拆 ledger/detail/editor，继续拆预算、清单、付款条款、审批时间线；保留 URL、版本、历史候选和金额校验 |
| `M94-F07` | P0/成本 | `CostSubjectPage.vue` 1,961 行被 taxonomy/rules/scope/trace 四个路由复用；后端 `CostSubjectV2Service` 1,346 行混合三类聚合 | 前端四路由独立；后端拆 mapping、bid transfer、finance allocation 和只读 resolver；与第90条严格串行且不改字段/金额/迁移语义 |
| `M94-F08` | P1/权限 | `AccessControlPage.vue` 1,678 行被用户、角色、权限三个路由复用 | 拆三路由页面；权限树构造与选中算法抽纯模块；所有授权变化保持 fail-close |
| `M94-F09` | P1/现场 | `QualitySafetyPage.vue` 1,738 行混合计划、检查、整改、复检、后果和离线草稿同步 | 先抽离线同步应用服务，再拆生命周期面板；第91条真分页已关闭，本项不改 API 分页或查询预算 |
| `M94-F10` | P1/壳与文档 | `AppShell.vue` 1,694 行混合导航、通知、通讯未读、移动端和演示切号；`DocumentCanvas.vue` 1,706 行混合几何、交互、布局与面板 | Shell 拆视觉区和通知 composable；Canvas 先抽纯几何/校验/layout engine，再拆字段库、画布、属性面板，保留 façade |
| `M94-F11` | P1/页面 | Project、Variation、DailyLog、Schedule、Closeout、Technical、Requisition、Inventory、Supplier、Measurement、Subcontract 等仍有多阶段工作区 | 按业务触达逐页拆；没有独立路由/状态机价值的页面保留，不为行数制造组件 |
| `M94-F12` | P0/安全 | Workflow Engine 与 AccessValidator 各维护一份逐项相同的业务类型→提交权限 switch | 建立唯一 `WorkflowSubmissionPolicy` 注册表，统一提交权限和专用入口政策；`ProjectAccessChecker` 已由第91条收敛，保持唯一项目范围入口，只补等价回归、不再拆分 |
| `M94-F13` | P1/文件授权 | `BusinessObjectAuthorizer` 1,121 行、17 个数据依赖，集中处理多领域对象查询、权限、状态和文件阶段；当前无已确认授权缺陷 | 保留唯一 fail-close façade；先建立全类型 parity 测试，再按 4～6 个领域拆 `FileAccessPolicy`，启动时检测未知、缺失和重复策略 |
| `M94-F14` | P1/事务服务 | `CtContractService`、`CashJournalService`、`MatStockService`、`QualitySafetyService` 同时混合 query、assembler 和多个写生命周期 | 先拆 query/assembler，再按聚合拆 command/lifecycle；合同复合保存、现金锁序、库存移动、质量后果继续保持原子事务 |
| `M94-F15` | P2/付款 | `PaymentTraceService` 994 行、27 个依赖；`PayApplicationService` 同时含 query、command、basis 和 validator | 第91条 PaymentTrace 查询性能已关闭；只在独立价值成立时保留顶层只读快照事务并拆 resolver/loader/validator，PayApplication 拆 query/command/assembler；不实施第90条字段收敛 |
| `M94-F16` | P0/文件 | `FileService` 通过 `ObjectProvider<ProjectFileService>` 依赖 ProjectFile，而 ProjectFile 又直接依赖 FileService | 先抽不反向依赖 FileService 的最小 `ProjectFileProjection` port，再拆存储、上传、读取、删除和目录/预览/历史导入职责；保持同事务索引、MinIO 补偿、after-commit 删除和失败重试 |
| `M94-F17` | P1/测试与样式 | 大型单测按多个领域堆叠；`components.css` 1,481 行；AutoPilot compatibility test 1,161 行 | 测试按行为拆并在相邻目录共享 fixture；CSS 按组件族拆并保留导入顺序；脚本测试按场景拆，不改控制面行为 |
| `M94-F18` | P2/去重 | `CostBudgetPage.vue` 是 16 行透明包装；Partner 详情重复；本地 `PageResult`、部分错误 helper、`nvl/nullToZero`、采购文档事件生成代码重复 | 逐项用引用和等价测试确认后删除/合并；透明 application 只有在无事务编排价值时删除，不撤销第91条有效边界 |

## 3. 目标架构与文件边界

### 3.1 前端目标

```text
router / navigation catalog
  -> route workspace page
     -> domain panels / editors / dialogs
        -> page-private application use case
           -> focused service client
              -> frontend-contracts domain types
```

1. 一个 route workspace 只拥有一个主要业务对象或一个紧密状态机；共享页面骨架可以复用，但不得以 `mode` 参数重新聚合六个不相关路由。
2. route page 负责路由参数、权限、加载与子组件编排；领域 panel 负责局部交互；application 用例只保留跨多个 API 的事务补偿、幂等或回滚编排。
3. service 文件按 API bounded context 拆分，原领域入口继续 re-export，避免一次性修改所有调用方。
4. 完全相同 API 类型归契约包；页面私有 view model 留在页面附近；不同投影禁止合成大量 optional 字段的“万能 DTO”。
5. 样式保持现有 token 和类名；本主线不做视觉重设计。

### 3.2 后端目标

```text
Controller
  -> stable facade / command service / query service
     -> domain validator / assembler / policy
        -> Mapper / external adapter
```

1. Controller 继续表达同一 REST 资源；拆 Service 不要求同步拆 Controller。
2. query/assembler 可以独立；写命令以业务事务为边界。禁止把一个原子写流程拆成多个独立事务。
3. 原 façade 在迁移期保留公开签名并委托新服务；调用方迁移完成、兼容测试通过后才删除无价值包装。
4. 工作流政策必须只有一个权威注册/解析入口；`ProjectAccessChecker` 继续作为第91条已验收的唯一项目范围入口，不新增平行 resolver；未知类型、缺失策略和重复注册全部失败关闭。
5. Mapper、DTO/VO、单实现安全/外部适配接口按框架和契约边界保留，不按文件短小合并。

### 3.3 明确保留项

- 所有已应用 Flyway migration、MySQL/H2 baseline、legacy migration 和版本化演示数据包。
- `WorkflowSubmitService`、`WorkflowApprovalService`、`WorkflowTaskService`、`WorkflowWithdrawService` 的独立事务状态机。
- `VirusScanner`、`DocumentRenderer`、`BidAwardProjectCreator`、`FileLifecycleGateway` 等安全、外部适配或跨域 port。
- 小型 MyBatis mapper、独立写入 DTO/VO、Vue 403/404/500 页面和 Capacitor Android 壳。
- `V2Stack` 与 `V2Cluster` 的轴向语义、`services/request.ts` 的请求协议、`fieldDrafts.ts` 的 IndexedDB repository。
- Dashboard façade、AutoPilot 兼容入口脚本、desktop launcher 单入口，以及需要版本可复现性的 v1/v2 demo 文件；没有新证据不删。

## 4. 范围与非目标

### 4.1 范围

1. `M94-F01～F18` 的当前 HEAD 复核、行为冻结、结构整改、定向验证与零悬空裁决。
2. 前端页面、布局、组件、服务、契约、路由目录、样式和对应单元/浏览器测试。
3. 后端项目范围、工作流政策、文件授权、合同、成本、付款、现金、库存、质量、文件服务及对应测试。
4. 测试 fixture、结构契约和必要的静态边界检查；只增加能防止本次边界回退的最小门禁。
5. Codemap 三件套、计划索引、当前焦点和实施后正式质量报告。

### 4.2 非目标

- 不改变产品功能、页面视觉语言、URL、导航可见性、API wire contract、权限码、错误码或响应金额。
- 不修改数据库表、字段、索引、迁移或演示数据；默认结论为 `G2_NO_MIGRATION`。
- 不实施第90条 `M90-F01～F12` 的字段、状态、双读、退役、迁移或金额权威工作。
- 不建设微服务、共享数据库之外的新存储、消息队列、通用事件总线、全局前端状态框架或通用 CRUD/DTO 生成器。
- 不把每个方法拆成一个类，不为单实现纯内部逻辑创建 speculative interface，不设置机械 LOC 强制门。
- 不删除测试换取通过，不降低权限、金额、租户、状态机、覆盖率、CI 或浏览器门禁。
- 不读取、扫描、修改或清理根规则禁止的历史/私有目录；不清理其他 worktree。
- 不连接非本地环境，不执行生产、Tag、Release、部署或发布裁决。
- 未另获授权前不创建/切换分支，不 commit、push、建 PR 或合并。

## 5. 不变量与结构完成标准

### 5.1 业务不变量

1. 所有现有 URL、深链、重定向、路由 meta、移动能力和权限可见性保持不变。
2. API 方法、路径、请求/响应字段、错误码和兼容历史值行为保持不变。
3. 租户、项目、角色、数据范围、文件权限和工作流专用入口全部 fail-close；拒绝顺序不静默放宽。
4. 金额精度、舍入、守恒、预算占用/释放、付款/冲销、库存移动平均价和成本事实不变。
5. 乐观锁、显式行锁、幂等键、状态转换、after-commit 事件和外部对象补偿顺序不变。
6. 离线草稿、同步重试、附件续传和历史候选“可读但不可重新选择”契约不变。

### 5.2 结构完成标准

1. 同一 Vue 页面不再由三个以上语义独立路由共享一个巨型 `mode` 状态机；确有共享价值的只保留壳和小型共享 panel。
2. 主要 route page 只负责编排，不再同时包含多个独立保存/提交/冲正状态机；超过 1,000 行但仍内聚的文件必须有保留裁决，不以数字判失败。
3. 高风险 Service 的 query、assembler 与 command/lifecycle 依赖分离；公开 façade 和事务入口清晰可审计。
4. 工作流业务类型→提交权限/专用入口政策只有一个权威注册表；项目范围继续只由现有 `ProjectAccessChecker` 提供，禁止产生第二实现。
5. `FileService` 与 `ProjectFileService` 不再形成双向依赖；文件索引、对象存储和 after-commit 语义保持一致。
6. 公共契约无重复定义；不同领域投影名称清晰；原公共 import path 在迁移期可用。
7. 每个被拆生产边界都有对应拆分后的 focused test；大型测试不存在依赖文件内执行顺序的隐式共享状态。
8. 不新增第三方运行或开发依赖；重复生产代码净减少，新增文件仅用于真实职责边界。

## 6. G0～G5 阶段门禁

### G0：稳定基线、所有权与 Codemap

1. 另获实施和 Git 分支授权后，在干净 `origin/master` 上建立隔离 worktree；记录 branch、HEAD、ahead/behind、status、worktree 占用和回滚点。
2. 重生成 `docs/codemap/codemap.html/json/lock` 并运行 generator self-test 与 `--verify`；对每个待改模块回答调用方、影响面和测试覆盖。
3. 重新统计热点、依赖数、路由复用和公开方法；每个 `M94-Fxx` 只能确认为“实施”“有据关闭”或“与第90条冲突而保持未启动”，不得沿用本计划的旧行数代替当前证据。
4. 建立文件所有权表。数据库、共享契约、同一 SFC/Service、Codemap 和 Git 始终由主线程串行写入。
5. 确认第90条未并行启动；若已启动，暂停所有重叠的 cost/payment/cashbook/finance 文件。
6. 记录本地 backend/frontend URL、JDBC/Flyway、演示库和关键开关；G0 不修改运行态或数据。

**G0 通过条件：** 当前 SHA、干净隔离工作树、Codemap 三问、18 项当前裁决、文件所有权、第90条冲突矩阵、测试与回滚入口齐全；否则禁止改代码。

### G1：行为契约与先失败保护

1. 为每个多路由页面冻结 URL、权限、查询参数、加载请求、动作可见性、成功/失败提示和深链行为。
2. 为金额、库存、付款、文件、权限和工作流服务冻结公开方法、事务注解、锁序、幂等键、错误码和 DB 回读不变量。
3. 对工作流权限重复和文件策略注册建立等价矩阵；把第91条 `ProjectAccessChecker` 的 Java/SQL scope 测试作为受保护回归基线，不计划拆分；未知、缺失、重复政策先有失败测试。
4. 对 barrel/import、API type 和历史候选建立 TypeScript 契约；先证明旧 import path 可继续编译。
5. 对拟删除包装、重复 helper 和 event listener 做引用、行为和异常语义检查；无法证明完全等价则保留。
6. 为每个批次定义一个最小金丝雀和精确回滚文件集。

**G1 通过条件：** 所有实际实施项先有可运行保护；权限、金额、状态、事务和兼容契约无空白；否则不得进入代码迁移。

### G2：数据、迁移与持久化不变

1. 本主线 migration diff 必须为空；任何 schema/字段/索引需求转交第90条或另行裁决，不得夹带实施。
2. 对受影响的合同、成本、付款、现金、库存、质量和文件流程，在 H2 与本地 MySQL 记录迁移前后相同输入的状态、金额、来源和行数守恒。
3. 对拒绝路径验证跨租户、错项目、错权限和非法状态均不产生 DB/MinIO 副作用。
4. 文件服务拆分必须证明上传失败补偿、删除 after-commit、预览任务和项目目录投影一致。
5. 所有测试记录使用任务专用精确 ID，并记录恢复/清理方式；禁止数据库重置。

**G2 通过条件：** `NO_MIGRATION`、H2/MySQL 结果等价、数据库与对象存储无残留副作用；否则回滚当前批次。

### G3：分批结构实施

1. 固定执行 `M0 → M1 → M2A～M2E → M3 → M4 → M5 → M6 → M7 → M8`，每批通过后才进入下一批。
2. 先移动类型、纯逻辑、query 和 assembler；再移动写命令。原 façade/barrel 先委托，调用方迁移完成后才删除透明层。
3. Spring 事务方法移动到新 bean 时必须保持代理调用有效；禁止因 self-invocation 或拆 bean 让 `@Transactional` 失效。
4. 同一事务内的锁、验证、写入、事件和补偿不得跨批次拆开。
5. 每次只拆一个路由或一个聚合，运行定向测试、静态检查和 diff 审查；首次失败按统一分类处理。

**G3 通过条件：** 18 项逐项完成或有据关闭，结构目标满足，所有定向/模块测试通过，无临时兼容层和无载体 TODO。

### G4：本地运行态与真实浏览器

1. 按运行态 Skill 核对当前实施 SHA、URL、listener、Docker mount、backend、JDBC/Flyway 和 `/api/actuator/health`。
2. 逐个拆分路由验证直接 URL、刷新、前进/后退、查询参数、列表、详情、编辑、提交/审批/冲正等实际用户流程。
3. 验证桌面和移动视口的 DOM、焦点、键盘、弹窗、通知、离线恢复和 console；不得用源码或单测替代。
4. 对权限、金额、库存、文件和工作流分别执行至少一条成功链和一条失败关闭链，并回读 DB/对象事实。
5. 若浏览器连接旧 worktree、旧端口、旧容器或错误数据源，分类为环境前置并停止验收。

**G4 通过条件：** 当前 SHA 的真实 API、DOM、交互、网络、console 和服务端回读同时通过。

### G5：全量验证、治理与零悬空

1. 运行全部受影响模块回归、后端完整测试/构建、前端契约/单测/类型/lint/build、browser contract、相关本地 live 和安全门禁。
2. 重生成并验证 Codemap；重新回答最终 diff 的调用方、影响面和测试覆盖。
3. 逐项回写 `M94-F01～F18`：本轮修复复验、超出范围且正式承接、或证据不足/无价值而关闭。
4. 创建正式质量报告 `docs/quality/2026-08-13-issue-094-全仓代码职责拆分与重复实现收敛.md`，同步计划、质量、焦点索引；只有产生跨主线且唯一可验收问题时才更新 `current-issues.json`。
5. 输出本地通过/不通过、阻塞/非阻塞、依据、剩余风险、恢复条件以及新增/关闭/净变化；不得给出生产或上线通过结论。
6. Git 交付仍需用户明确授权；获得授权后才执行同 SHA CI、受保护 PR/合并和 post-merge 验真，禁止直接推送 `master`。

**G5 通过条件：** G0～G4 当前 SHA 证据齐全，18 项全部有唯一裁决，Codemap/计划/报告一致，无无载体遗留项。

## 7. 实施批次与文件所有权

| 批次 | 内容 | 主要文件范围 | 最小验证 | 禁止扩展 |
| --- | --- | --- | --- | --- |
| `M0` | 基线、Codemap、契约矩阵 | `docs/codemap/*`、本计划、只读调用/测试表 | generator self-test、generate、verify、Git/worktree | 不改业务代码 |
| `M1` | 低风险类型与去重金丝雀 | `packages/frontend-contracts/src/**`、focused service barrel、`CostBudgetPage.vue`、Partner detail、等价 helper/tests | contracts type-check、focused unit、route contract、build | 不改业务字段或 API；不删有编排价值 application |
| `M2A` | 合同读路由金丝雀 | `ContractPage.vue`、新 ledger/detail/editor 子页面与 tests | 合同 route/unit、type、build、ledger 浏览器 | 先只抽只读 ledger；不改金额/预算写入 |
| `M2B` | 采购三路由 | `PurchaseExecutionPage.vue`、现有 purchase application、focused components/tests | 申请/订单/验收单测、失败回滚、浏览器三路由 | 不合并订单与验收事务 |
| `M2C` | 成本科目与权限三/四路由 | `CostSubjectPage.vue`、`AccessControlPage.vue`、focused services/tests | 四成本路由、三权限路由、权限负例 | 与第90条串行；不改权限结果 |
| `M2D` | 财务十路由 | Receivables、FinanceControl、finance services/contracts/tests | 金额守恒、冲正、Trace、十路由浏览器 | 不改第90条字段/状态语义 |
| `M2E` | 质量安全离线闭环 | `QualitySafetyPage.vue`、draft sync、panels/tests | 离线恢复、幂等、附件、状态闭环 | 不新建通用离线框架 |
| `M3` | 壳、Canvas 与剩余页面 | AppShell、Dashboard、DocumentCanvas/Template、P1/P2 页面 | shell/communication、canvas、页面 focused tests、浏览器 | 不重设计视觉；无独立状态机页面可保留 |
| `M4` | 工作流政策单一源 | Workflow policy/validator/engine、ProjectAccessChecker 回归 tests | permission 等价、专用入口、既有 scope/跨租户负例 | 不修改 ProjectAccessChecker 实现；不合并四个 workflow 状态机服务 |
| `M5` | 文件循环与基础设施 | FileService、ProjectFileService、projection port、preview/import/reconcile tests | MinIO 补偿、同事务投影、after-commit、目录/预览/对账 | 不重开第92条可靠性，不新建第二文件目录或任务系统 |
| `M6` | 文件授权策略 | BusinessObjectAuthorizer、FileAccessPolicy、FileService tests | 全业务类型矩阵、未知/重复策略、锁/状态负例 | façade 保留；策略按领域分组，不一类型一类 |
| `M7` | 后端聚合拆分 | CostSubject、Contract、CashJournal、Stock、Quality、Payment/Trace 及 tests | 事务、并发、金额、库存、状态、查询快照 | 一个批次只迁移一个聚合；不重开第91条性能项；与第90条重叠串行 |
| `M8` | 测试、样式、脚本与收口 | 大型 tests、相邻 fixtures、components CSS、AutoPilot test、Codemap/docs | 全量门禁、结构契约、HighRisk plan profile | 不改控制面行为，不建全仓万能 fixture |

批次内仍按“一次一个 route/aggregate”串行。前后端可并行只读取证；共享契约、数据库、Codemap、同文件写入、正式验收与 Git 由主线程串行。

## 8. 金丝雀与退出条件

### 8.1 金丝雀

1. `M1`：先删除 `CostBudgetPage.vue` 透明包装并保持 `/cost-budget` 的标题、权限、查询参数和 DOM；失败则恢复 wrapper，不影响其他页面。
2. `M2A`：只抽合同台账只读路由，详情/编辑仍走旧 façade；列表 URL、分页、权限和浏览器通过后再拆写路径。
3. `M4`：先让唯一 Workflow policy registry 只提供当前两个重复 permission switch 的数据，保持旧入口委托；全部业务类型矩阵等价后再收敛专用入口策略。`ProjectAccessChecker` 只跑回归，不拆代码。
4. `M5`：先以 projection port 解开 File/ProjectFile 单个依赖方向，保留全部原调用结果；上传、删除、预览和恢复证据通过后再拆内部职责。
5. `M6`：只抽一个低耦合文件领域 policy，BusinessObjectAuthorizer façade 继续总入口；负向授权矩阵通过后再扩域。
6. `M7`：每个后端服务先抽只读 query/assembler；写状态机在查询拆分稳定后单独处理。

### 8.2 退出与保留条件

- 拆分后仅把同等复杂度移入一个巨型 composable/helper：撤回，重新按业务边界设计。
- 需要改变 API、字段、金额、迁移或状态才能完成结构拆分：停止并转交第90条或另行请求授权。
- 一个候选没有独立变更频率、测试边界或复用价值：有据保留，不拆。
- 抽象只有一个透明实现且不承担安全、外部适配、事务或测试 seam：删除抽象或保留原实现，不继续扩层。
- 结构门禁显著增加维护成本却不能捕获本轮回退：删除该门禁，不制造永久治理负担。

## 9. 验证矩阵

| 验收项 | 必须证明 | 失败判定 |
| --- | --- | --- |
| `A-01` 基线与所有权 | 当前 SHA、干净隔离工作树、Codemap 当前、文件单写入者 | stale map、未知 dirty hunk、与第90条并发占用 |
| `A-02` 路由与导航 | 所有原 URL、深链、重定向、meta、查询参数和移动能力不变 | 任一路由失效、组件模式串线、导航可见性变化 |
| `A-03` 前端契约 | 原公共 import path 编译；API payload/response 不变；历史候选可读禁选 | 肥 DTO 合并、字段可空性放宽、旧 import 断裂 |
| `A-04` 权限与租户 | 第91条项目范围基线不变；工作流 permission、文件策略等价；未知/重复/跨租户失败关闭 | 新建平行项目范围实现、任一放权、拒绝顺序漂移或对象存在性泄漏 |
| `A-05` 金额与状态 | 合同、预算、付款、现金、库存、成本、质量状态与金额守恒 | 舍入、锁序、冲正、幂等、状态转换变化 |
| `A-06` 事务与副作用 | `@Transactional` 代理有效；DB、事件、MinIO 和 after-commit 顺序不变 | 半事务、重复事件、孤儿对象或补偿丢失 |
| `A-07` 离线与文件 | 草稿恢复、重试、附件和项目目录/预览结果等价 | 重复写、草稿丢失、附件不可达 |
| `A-08` 测试结构 | focused tests 独立运行；完整链 smoke 保留；无隐式顺序依赖 | 只移动测试、不覆盖新边界，或删除有效场景 |
| `A-09` 真实浏览器 | 当前 SHA 的 URL、DOM、操作、网络、响应式、console 证据 | 只有源码、build 或旧运行态证据 |
| `A-10` 数据与迁移 | migration diff 为空；H2/MySQL 及对象事实守恒 | 夹带 schema/字段/数据修复或重置数据库 |
| `A-11` 结构净收益 | 重复政策源归一、循环依赖消失、主要多路由状态机拆开；依赖新增 0 | 文件数增加但职责/测试/变更面未改善 |
| `A-12` 零悬空 | 18 项全部有唯一裁决；新增/关闭/净变化可复算 | 任一确认问题以“以后再说”悬空 |

实施期基础命令在 G0 按当前工具能力锁定，建议最小入口：

```powershell
node scripts/codemap/test-generate-codemap.mjs
node scripts/codemap/generate-codemap.mjs
node scripts/codemap/generate-codemap.mjs --verify

pnpm --dir frontend-admin-v2 run type-check:contracts
pnpm --dir frontend-admin-v2 run lint:check
pnpm --dir frontend-admin-v2 run test:unit
pnpm --dir frontend-admin-v2 run build
pnpm --dir frontend-admin-v2 run test:e2e:contract

Push-Location backend
.\mvnw.cmd clean test
Pop-Location
```

具体领域测试、MySQL 门禁和 live/browser 入口由各批次按当前 Codemap 三问补齐；不得把上述通用命令当作权限、金额、事务或浏览器专项证据。

## 10. 失败分类

| 分类 | 示例 | 动作 |
| --- | --- | --- |
| `tool_config` | PowerShell、pnpm、Maven、Playwright 参数或入口错误 | 修正一次参数或切权威入口，不判产品缺陷 |
| `environment_prerequisite` | 端口占用、容器未运行、mount/JDBC/URL 不是任务基线 | 停止 G4，恢复本地前置后重新核对 |
| `test_or_fixture` | 拆分后 mock 路径、mount helper、测试数据或执行顺序失效 | 修复测试边界，不放宽生产不变量 |
| `product_bug` | 路由行为、金额、库存、状态、文件或合法流程发生变化 | 阻塞当前批次，回到最小根因修复 |
| `quality_or_security` | 权限放宽、跨租户泄漏、事务失效、锁序改变、未知策略放行 | 立即回滚金丝雀，独立只读复核后重开 |
| `ready_issue_config` | 与第90条所有权冲突、计划范围漂移、候选无证据却扩张 | 停止代码，先修载体、范围和所有权 |
| `external_dependency` | 要求非本地环境、生产数据、Secret 或外部系统 | 按 local-only 有据关闭，除非用户先修改规则 |

## 11. 风险、恢复矩阵与回滚

| 风险/失败点 | 立即动作 | 恢复方式 | 重开条件 |
| --- | --- | --- | --- |
| Codemap/文件所有权漂移 | 停在 G0 | 在隔离 worktree 重生成，重新冻结路径 | map verify 与三问齐全 |
| 路由拆分破坏深链/缓存 | 停止该 route 后续拆分 | 恢复原 route component 和 façade；保留失败测试 | URL、query、DOM、返回导航全部通过 |
| barrel/type 拆分形成循环 import | 停止契约迁移 | 恢复原 barrel，缩小类型所有权 | type-check、build、运行时 import 均通过 |
| Spring 拆 bean 导致事务失效 | 立即回滚该聚合 | 恢复原事务入口；保留 assembler/query 拆分中安全部分 | 事务代理、回滚、并发和 DB 回读通过 |
| 权限/项目范围不等价 | 回滚整个政策金丝雀 | façade 切回旧实现，保留矩阵测试 | 全业务类型正负矩阵与独立复核通过 |
| 金额/库存/冲销守恒失败 | 停止 M7 | 恢复原 command/lifecycle 文件，不迁移数据 | H2/MySQL、并发、金额和状态守恒通过 |
| 文件对象或索引不一致 | 停止 M5，保留对象证据 | 恢复原双向调用；精确清理任务对象 | 补偿、after-commit、目录和预览回读通过 |
| 第90条启动或范围冲突 | 暂停重叠批次 | 只保留无重叠已验证成果，不 cherry-pick 混合修改 | 两计划文件所有权和顺序重新签认 |
| 拆分净收益不足 | 关闭该候选 | 保留原内聚文件，不制造抽象 | 出现真实独立变更/测试边界才重开 |

所有回滚只撤销第94条任务自有文件和任务专用测试数据；禁止整树 reset、修改已应用 migration、覆盖共享成果、删除他人 worktree 或清理未证明归属的数据。

## 12. 最终验收标准

1. `M94-F01～F18` 全部以当前实现 SHA 重新取证并完成唯一裁决，无重复 Issue 或无载体遗留。
2. 六个 P0 多路由页面按真实业务路由/状态机拆分；剩余大页面均有“拆分”或“保持内聚”的证据裁决。
3. 前端契约和 service 按领域归属，原公共 import path 兼容；重复 `PageResult` 等等价类型消失，不产生万能 DTO。
4. 工作流提交政策只有一个权威定义；`ProjectAccessChecker` 保持第91条唯一范围入口；BusinessObjectAuthorizer 保持唯一 fail-close 入口并按领域策略分工。
5. FileService/ProjectFileService 循环消失；合同、成本、付款、现金、库存、质量服务的 query/assembler 与写状态机边界清晰。
6. 事务、锁序、幂等、金额、租户、权限、状态、文件副作用和离线恢复全部保持原结果。
7. 已确认透明包装与完全等价重复被删除或合并；无法证明等价者有据保留；新依赖为 0。
8. focused tests、模块回归、后端完整验证、前端 contracts/unit/type/lint/build、browser contract 和实际受影响 live/browser 流程通过。
9. 本地实际 URL、backend SHA、JDBC/Flyway、API、DB/对象回读、DOM/交互/网络和 console 证据绑定同一实现 SHA。
10. migration diff 为空；无数据库重置、非本地环境或生产操作。
11. Codemap 三件套重生成并 verify；计划、质量、焦点索引与最终代码一致。
12. 正式收口输出通过/不通过、阻塞/非阻塞、剩余风险、恢复条件和新增/关闭/净变化；不宣称生产可发布。

## 13. 当前状态与后续授权

- 当前完成：第94条编号、基线、去重关系、18 项发现载体、阶段门、批次、验收和恢复方案已编制。
- 当前未完成：G0 当前 Codemap、实施 worktree、行为基线、任何代码整改、运行态/浏览器验收、质量报告和 Git 交付。
- 当前阻塞：无外部阻塞；实施与受保护 Git 交付已授权，当前状态为 `G0_IN_PROGRESS`。
- 正式 Issue：新增 0、关闭 0、净变化 `0`；18 项由本计划内部唯一承接，不修改 `current-issues.json`。
- 实施第一动作：重新核对 branch/status/worktree，在干净隔离工作树重生成 Codemap，完成 G0；不得直接从 M1 开始改代码。
- Git、PR、合并与清理：已授权在全部 G0～G5 收口后按受保护路线执行；禁止提前推送、直接推送 `master`、强推、绕过保护、Tag、Release 或生产发布。

# 第95条主线：示例库全业务流程阻塞修复与浏览器闭环

**Goal:** 在当前本地示例库中修复已证实的跨模块契约、权限、状态机和页面阻塞，通过内置浏览器按正式业务入口完成可合法执行的采购、库存、分包、质量、技术、计量、收入、合同与项目收尾全流程，并以服务端权威事实、自动化回归和受保护 Git 交付完成收口。 **Architecture:** 复用现有 Controller/Service/Mapper、Vue 工作台、工作流、附件、库存、合同和收尾状态机；只补缺失字段、业务专属候选接口、现有命令入口及最小页面动作，不新增平行账本、旁路 API、客户端权威状态或第三方依赖。所有业务写入走正式页面/API，数据库仅做只读取证；未来业务事实不得靠直改数据库伪造。

> 编制日期：2026-08-15
>
> 计划状态：`COMPLETE / G0-G5_PASSED / GIT_DELIVERY_MERGED / POST_MERGE_VERIFIED`
>
> 代码基线：`codex/mainline-95-full-business-flow-closeout@dc4d72bebfbb5e131938c95d0eaa4bd73750d883`，与启动时 `origin/master` 同步；任务改动均保留在源分支
>
> 运行基线：仅本地 dev/test/demo；backend `http://127.0.0.1:8080`、frontend `http://127.0.0.1:5173`，实际 JDBC 库 `cgc_pms_demo_ui_20260728`
>
> 示例项目：`2084515692153524225 / XM-20260804-001`
>
> 授权：本地代码、文档、测试与示例库正式业务操作；任务自有改动 commit、push、PR、受保护合并、post-merge 验真和已合并源分支清理。不授权生产、非本地环境、强推、管理员绕过、Tag 或 Release
>
> 唯一发现与状态载体：本计划 `M95-F01～M95-F12`；不在 `current-issues.json` 复制子项

## 1. G0 基线

1. 仓库默认分支为 `master`；任务改动已安全挂到 `codex/mainline-95-full-business-flow-closeout`，未覆盖或清理任何既有文件。
2. backend/frontend 健康且均返回 HTTP 200；MySQL、Redis、MinIO、ClamAV 为本地开发容器。
3. 启动时项目为 `ACTIVE/APPROVED`，三项 WBS 分别为 `10%/IN_PROGRESS`、`0%/NOT_STARTED`、`0%/NOT_STARTED`；收尾前置包含 WBS、质量验收、分包、库存、采购付款与合同终局。
4. 通过正式 UI/API 消除门禁后，项目 `XM-20260804-001` 已自然到达 `CLOSED`，收尾 `PC-20260814-001` 为 `CLOSED`，三份合同均为 `SETTLED`。
5. 采购付款 `PAY-20260815-001` 为 `APPROVED/PAID`、实付 9,560；现金日记账 `CJ-20260815-001` 为 `OUT/ARCHIVED`；示例库业务表未直改。
6. `docs/codemap/codemap.json` 已覆盖施工履约、供应链、质量、合同、前端 V2 与共享契约调用边界；代码冻结后已重生成三件套并 `--verify`。

## 2. 范围与发现项

| ID | 范围 | 完成标准 |
| --- | --- | --- |
| M95-F01 | 产值计量 WBS | 每条计量来源显式选择并提交有效 WBS；专用 form-options 不依赖计划模块权限；弹窗无溢出 |
| M95-F02 | 分包任务 WBS | 新建/编辑任务必须提交当前项目 ACTIVE 基线 WBS；历史失效值只读禁选 |
| M95-F03 | 质量检查 WBS | QUALITY 检查必须选择有效 WBS；SAFETY 继续允许空 WBS；服务端按租户/项目校验 |
| M95-F04 | 技术与收尾质量事实 | 技术验收归档和分项验收只接受同 WBS 的 QUALITY/PASS；SAFETY/PASS 不得绕过 |
| M95-F05 | 采购草稿处置 | 仅 DRAFT/DRAFT 订单可删除；父单、明细、附件原子软删，页面精确确认并权威回读 |
| M95-F06 | 合同履约结清 | 页面复用既有 settle API，传当前 version；服务端事实不足时拒绝且页面不假成功 |
| M95-F07 | 收入附件契约 | 销项发票、回款命令提交 `attachmentCount=1`，与单文件 UI 和后端校验一致 |
| M95-F08 | 页面与显示可靠性 | 纳秒时间安全格式化、无界候选改按需查询、方向不符动作禁用、toast 与弹窗层级/布局正确 |
| M95-F09 | 角色与迁移 | 恢复 V294～V296 历史迁移，V297 最小补齐 TECHNICAL_LEAD 交付权限；MySQL/H2 同步 |
| M95-F10 | 示例库业务闭环 | 通过正式 UI 消除真实门禁，完成收尾、合同终局、质保/档案等可达终态；不得直改数据库造事实 |
| M95-F11 | G0～G5 验证 | 定向与全量后端、前端、迁移、浏览器 contract、内置浏览器 DOM/视觉/控制台与服务端回读通过 |
| M95-F12 | 受保护交付 | task-owned commit、同 SHA Push CI、pre-PR verifier、PR checks、merge、post-merge 与分支清理完整 |

## 3. 非目标

- 不连接或规划生产、非本地目标环境，不部署、不发版、不创建 Tag/Release。
- 不修改已应用 migration；历史版本只恢复与已应用 checksum 精确一致的原文件，新权限使用新版本。
- 不降低权限、租户、金额、库存、附件、WBS、合同或收尾服务端校验，不新增管理绕过。
- 不为演示完成而直接更新业务表、回填未来日报、伪造验收、付款、回款或缺陷事实。
- 不扩大到与本任务无关的重构、视觉重设计或依赖升级。

## 4. G0～G5 门禁

### G0 基线锁定

- 分支、HEAD、83 项承接改动、运行 URL、JDBC、项目、租户、门禁和回滚边界已记录。
- 当前 Codemap 能回答调用方、影响面与测试覆盖；最终冻结后重生成。

### G1 契约完整

- 每个发现项具备前端类型、服务调用、后端权限/校验、失败语义和至少一条自动化保护。
- 历史值可读禁选；未知项目、跨租户、无权限和状态漂移全部 fail-close。

### G2 数据与迁移

- 原 V294 按已应用 checksum 精确恢复；新增 V295～V299；MySQL/H2 fresh/upgrade/checksum 通过。
- 示例库操作前后以只读 SQL 回读数量、金额、状态、来源和租户；禁止直接业务表写入。

### G3 服务端闭环

- 采购删除、WBS 绑定、库存守恒、合同结清、质量事实、附件计数和收尾门禁均以服务端状态为准。
- 任何提前操作失败后数据不变、页面重新加载权威状态。

### G4 运行态与内置浏览器

- 健康、JDBC、最终路由和关键 API 成功；内置浏览器完成真实交互。
- 每阶段检查 URL、有效 DOM、无框架 overlay、相关 console error/warn 为 0、截图无裁切/遮挡/横向溢出。

### G5 正式收口

- 全量后端、前端、browser contract、迁移、安全、Codemap 与 diff 门禁通过。
- 质量报告记录通过/不通过、业务事实、剩余风险、回滚、后续项新增/关闭/净变化。
- exact-SHA Push CI、PR、受保护合并和 post-merge 证据完整后才清理源分支。

## 5. 风险、恢复与回滚

| 风险 | 控制 | 恢复/回滚 |
| --- | --- | --- |
| 共享脏工作区误提交 | 源分支隔离；显式路径 staging；提交前审查全部 staged diff | 停止 Git，保留工作区和源分支；不 reset/checkout 用户改动 |
| 示例业务状态不可逆 | 每步先读权威状态；只走现有正式动作；金额/库存/合同串行 | 失败后依服务端事务回滚与页面权威回读；不手工修表 |
| 未来计划事实被伪造 | 允许合法重排计划或等待真实条件；禁止直写未来进度/验收 | 无合法 UI 路径则保持阻塞并正式裁决不通过 |
| 迁移 checksum 漂移 | 历史文件按已应用 checksum 恢复；新授权单独 V297 | checksum 不一致立即停止，不 repair、不改已应用文件 |
| CI/PR 失败 | 按统一失败分类整改后做一次等价复验 | 保留提交与分支；禁止管理员绕过、强推或删分支 |

## 6. 验收口径

1. 所有代码发现项 `M95-F01～F09` 均本轮修复并复验或有据关闭。
2. 示例库每项业务写入都有页面交互、API/DOM 成功反馈和数据库只读回读；数量、金额和状态守恒。
3. 项目收尾若完成，必须由现有状态机自然到达；若受时间或真实资金事实阻塞，最终裁决必须为不通过，不以测试数据旁路冒充完成。
4. 最终交付绑定精确 source SHA、Push CI、PR、merge SHA、post-merge verifier、`origin/master` 同步及源分支删除证明。

## 7. 本地实施与验收结果

1. `M95-F01～F09` 全部修复并复验；WBS、QUALITY/PASS、采购草稿删除、合同结清、附件计数、候选查询、时间格式、权限与页面布局均由前后端契约保护。
2. `M95-F10` 已通过内置浏览器和正式业务入口完成：库存守恒、三 WBS 完成、三条同 WBS 质量验收、分项/竣工验收、业主结算、尾款/质保金回收、档案移交、合同结清与项目关闭。
3. 后端全量 372 suites/2,862 tests、关键定向 151 tests、前端 unit 641 tests、browser contract 98 tests、MySQL fresh/upgrade 各 1 test、contracts/Vue type-check、build、lint、运行部署契约和 Codemap 均通过。
4. 内置浏览器验证关键页面 DOM、视觉与操作；成员弹窗布局已整理，console warning/error 为 0；服务端只读回读确认项目、收尾、合同、付款与现金日记账终态。
5. `M95-F11～F12` 已完成：source `96a771deda41185747685ba88bd15ef980c56aea` 的 Push CI `31853877532` 与 PR CI `31854492617` 全绿；PR #448 受保护合并为 `ea211af1d73b9e1c0c7e4ae39fd797e19d83ea4b`，post-merge `31855142538` 与 verifier 通过，源分支已清理。

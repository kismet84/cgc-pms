# 第55条主线 M0：事实刷新与治理唯一化任务计划书

**Goal:** 将第55条基线中的25项唯一事项绑定到当前 `master@536584c8372b554604103d9a4f6ca8fc7c89fa4d` 与实际工作区，关闭已有充分新证据的事项，刷新仍成立事项的真实范围，保持重复项和无载体悬空项均为0。

**Architecture:** 继续以 `docs/backlog/current-issues.json` 保存现行问题，第55条总计划只编排其余唯一事项；本文件记录阶段边界，不建立第二套问题系统。优先复用当前源码、正式验收报告和既有测试，只做治理回写，不修改业务实现、数据库、运行环境或发布配置。

计划状态：`M0 已验收`

计划日期：2026-07-26

## 1. 执行快照

- 分支：`master`
- HEAD：`536584c8372b554604103d9a4f6ca8fc7c89fa4d`
- 启动时工作区已有7处脏改动，全部按既有成果保留。
- 本阶段允许：事实检索、目标/全量测试、静态验证、治理文档和唯一状态源回写。
- 本阶段禁止：业务代码、数据库、运行环境、分支切换、commit、push、PR、正式入口、Legacy退役和生产操作。

## 2. 范围与非目标

范围：

1. 复核25项的当前状态、来源、前置、下一动作和验收条件。
2. 复核 `current-issues.json`、Blocked、Current Focus、项目地图、全量审计台账和第55条矩阵是否冲突。
3. 对可能已由新代码关闭的现金预测并发项执行源码、正式报告和目标测试三重验证。
4. 刷新财务定时对账、JaCoCo、Legacy lint、前端错误上报、付款服务、废弃API和dev MySQL配置事实。
5. 回写阶段结论及新增/关闭/净变化统计。

非目标：

- 不实施 M1 及后续阶段问题。
- 不因覆盖率提高、全量测试通过或本地证据解除任何生产发布门。
- 不修改第53条 M7/M8、AutoPilot控制面、Frozen产品候选或目标环境状态。

## 3. 阶段门

1. 当前 HEAD、分支和脏工作区已冻结。
2. 25项逐项有结论；状态只能是保持、刷新后保持或有据关闭。
3. 关闭项必须同时具备当前源码、正式承接证据和可运行验证。
4. Current Focus、项目地图、总计划和现行问题源同步。
5. `git diff --check`、JSON解析、引用存在和25项守恒通过。

## 4. 验证

```powershell
backend\mvnw.cmd -C -Dtest=ProjectCashForecastConcurrencyTest test
backend\mvnw.cmd -C verify
Set-Location frontend-admin
pnpm lint:check
Set-Location ..
git diff --check
```

验收报告：`docs/quality/第55条主线-M0-事实刷新与治理唯一化验收报告.md`。

## 5. 风险与回滚

- 脏工作区归属：只做精确补丁，不覆盖或清理既有差异。
- 历史证据漂移：旧报告只作来源；当前源码和本轮验证优先。
- 状态误关：证据不完整时保持 OPEN、NEEDS_CONFIRMATION、FROZEN、OBSERVATION、BLOCKED 或 RELEASE_GATE。
- 回滚：仅撤销本阶段治理文档和状态源差异；不涉及业务数据、迁移、运行环境或远端。

## 6. 阶段出口

M0通过只允许进入 M1 规划，不自动启动 M1。任一 `RELEASE_GATE` 未解除时，生产裁决继续为“不通过、禁止上线”。

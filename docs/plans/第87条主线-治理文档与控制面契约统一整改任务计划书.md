# 第87条主线：治理文档与控制面契约统一整改

**Goal:** 关闭手册、Prompt、门禁、流程与技术规范之间已证实的八组漂移，使根授权、环境边界、失败分类、前端事实、运行等待、AutoPilot Git 行为、长任务 CI 和历史边界可由现有自动化复验。 **Architecture:** 复用根 `AGENTS.md`、现有专项 Skill、`current-issues.json`、现有 AutoPilot 配置/脚本及 policy suite；只改现有事实源、投影和断言，不新增治理框架、状态库、required context 或第三方依赖。

> 编制日期：2026-08-10
> 计划状态：`IMPLEMENTED / G0-G5_LOCAL_PASSED / GIT_DELIVERY_AUTHORIZED / UNIFIED_WITH_MAINLINE_88 / CONTROL_PLANE_CANARY_REQUIRED`
> 唯一载体：本计划
> 基线：`master@28ca2aef30f876deb8120b858fea9d248ff08528`，实施前工作区 clean，Codemap 校验通过
> 环境：仅本地 dev/test/demo；本任务不连接数据库、不启动运行态，tenant、演示数据、URL 和数据重置开关均 `N/A`
> 授权：用户已授权与第88条统一执行受保护 Git 交付；不授权生产、目标环境、强推、保护绕过、Tag 或 Release

## 1. 范围与裁决

| ID | 已证实问题 | 本轮最小整改 | 验收 |
| --- | --- | --- | --- |
| D1 | AutoPilot `autoMerge=true` 可绕过明确 Git 授权 | 默认关闭；commit/merge 分权且每次运行显式授权，缺失即 fail-close | 负向测试证明 HEAD/index 不变 |
| D2 | 本地环境规则与生产/发布文档、Backlog 投影冲突 | `current-issues.json` 保持唯一状态源；生产内容标为冻结参考，投影改为不适用/非阻塞 | JSON、Markdown 和根规则一致 |
| D3 | 验收 Prompt 复制过时三分类 | 删除副本，只引用 CI Skill 七分类；增加环境范围与“上线不适用” | policy 静态检查通过 |
| D4 | 前端规范、用户手册与实际依赖、请求层、目录、路由漂移 | 按 `package.json`、`request.ts`、当前导航修正文档 | 无已知 Axios/Ant/Vxe/ECharts/旧路由断言 |
| D5 | long-task-gate 功能测试不在合并前 required CI | 复用现有 required job 执行 Node 测试并由 policy suite 锁定 | Node 15 项和 workflow 契约通过 |
| D6 | `waitSeconds` 被实现为固定睡眠 | 改为最大超时，轮询就绪并可提前返回 | 相关静态/行为测试通过 |
| D7 | 插件直调 helper 仍能 commit | helper 永久 preview-only，真实提交只走授权控制面 | 无非 DryRun 提交分支 |
| D8 | 当前项目地图混入使用“当前”措辞的历史快照 | 加强不可作为现状判断的历史围栏并去当前化 | 历史/现行边界明确 |

## 2. 非目标与不变量

- 不改变业务逻辑、数据库、前后端运行态、Git 历史或分支保护。
- 不新增第二套分类、Issue 状态、授权服务、CI required context 或等待框架。
- 仓库持久配置不能代表用户 Git 授权；commit 与 merge 授权不能互相推导。
- `current-issues.json` 独占 Issue 状态、分类和 blocking；Markdown 只作人读投影。
- 生产/目标环境继续不存在且不进入计划、验证或阻塞项。

## 3. 实施阶段与 G0～G5

| 门禁 | 证据 | 状态 |
| --- | --- | --- |
| G0 基线 | 分支、clean 状态、HEAD、worktree、Codemap、环境与授权边界已记录 | `PASS` |
| G1 契约 | D1～D8 均有唯一事实源、文件范围、负向边界和验收 | `PASS` |
| G2 数据 | 无数据库或数据迁移；确认无数据写入 | `N/A` |
| G3 实现 | 文档、控制面与 CI 最小整改完成，未扩大业务范围 | `PASS` |
| G4 验证 | policy suite、AutoPilot 契约、long-task Node 测试、链接/JSON/Codemap/diff 检查通过 | `PASS` |
| G5 收口 | 当前 diff、风险、回滚、Git 权限和零悬空统计齐全 | `PASS_LOCAL` |

## 4. 文件边界

- 文档：`docs/backlog/**`、`docs/prompt/**`、相关 `docs/standards/**`、`docs/manuals/end-user-manual.md`、`docs/product-intelligence/project-map.md`。
- 控制面：相关 `scripts/codex-autopilot/**`、插件 preview helper、`.github/workflows/ci.yml`、运行刷新 Skill。
- 计划与状态导航：本文件、`docs/plans/README.md`、`docs/backlog/current-focus.md`。
- 只有上述行为边界的现有测试可扩展；不创建平行测试框架。

## 5. 验证、风险与回滚

- 最小验证：JSON/Markdown 引用、`git diff --check`、Codemap verify、long-task Node 测试、AutoPilot policy/owner/control-plane 相关测试。
- 风险：授权参数若被持久化或默认继承，仍可能形成越权；必须用一次运行输入和负向测试封闭。
- 风险：CI job 名变化会影响保护；本轮复用现有 required job，不新增 context。
- 回滚：仅回退本计划所属文件差异；无数据库、运行态、外部系统或 Git 历史恢复。
- 控制面指纹会随行为性文件变化；未经用户明确 `启动迭代-1`，不得进入新指纹 AutoPilot 金丝雀或多任务运行。

## 6. 收口记录

- 实现：D1～D8 全部落地；独立复核发现的 Git 未分类变异命令、早期历史快照、管理员手册范围和文档中心入口四处遗漏均在原 D1/D2/D8 内修复并复验。Codemap 三件套已按当前工作树刷新。
- Git 授权：中心包装器仅放行明确只读命令；`add/commit`、`merge`、branch/worktree/switch/checkout 分别要求本轮显式授权。未知命令和调用方 Git 全局选项默认拒绝；`reset --hard`、`update-ref`、`tag`、`-C ... commit` 负向测试证明 HEAD、index、refs 不变。
- 文档统一：Issue 投影、Prompt 七分类引用、local-only 环境、前端依赖/请求/路由、管理员与终端手册、运行等待、运维冻结参考及项目地图历史围栏均与当前事实源一致。
- CI：复用既有 required jobs；Windows 控制面契约进入 `desktop-launcher`，long-task 15 项进入 `sql-safety-scan`，未新增 required context。
- 验证：HighRisk policy suite `7/7`；主线程控制面关键测试 `10/10`；long-task `15/15`；worker 追加的 context/recovery/stall/final-scope/repair/runner 回归全部通过；workflow contract、PowerShell 语法、JSON、104 个非禁区本地链接、Codemap verify、`git diff --check` 全部通过。
- 失败分类：首次计划门禁缺“阶段”和 Codemap 快照陈旧归 `quality_or_security`，均修复后通过；测试 fixture 与自定义检查器构造错误归 `tool_invocation`，未放宽生产逻辑。
- 控制面：新指纹 `6c05a15be18bd3fb168754edb51502d75035ef3103d49f0ce00d9b8704a2fac0`，策略版本 `2`。未执行真实金丝雀；进入 N>1 或无界运行前，仍须用户明确发送 `启动迭代-1`。
- 回滚：仅回退本计划列出的任务自有差异和同步生成的 Codemap 三件套；无数据库、运行态、外部系统或 Git 历史恢复。
- Git 交付：已纳入 `codex/mainline-87-88-closeout` 统一交付；当前仅声明本地验证通过，远端 CI、PR、合并与清理以实际完成证据为准。
- 零悬空：原八组发现本轮修复八组；最终复核新增四处同范围遗漏并全部修复。新增后续项 `0`、关闭后续项 `0`、后续项净变化 `0`；无无载体遗留项。

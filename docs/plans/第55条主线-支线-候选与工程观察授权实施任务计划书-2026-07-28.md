# 第55条主线支线：候选与工程观察授权实施任务计划书

**Goal:** 在不虚构业务事实、不降低安全门禁的前提下，对证据化收口后保留的 7 项产品候选与 2 项工程观察实施当前可安全完成的最小工作，并为尚缺外部证据的事项保留唯一验收入口。

**Architecture:** 沿用 `docs/backlog/current-issues.json` 的 9 个稳定键。CI 仅增加原生 `workflow_dispatch` 布尔输入；冷缓存运行在 GitHub 托管的新 job 工作区跳过共享缓存恢复，不删除、覆盖或新建共享缓存。产品候选与 AutoPilot 效率观察继续使用既有事实源和决策门，不新增平台、重复 Issue 或推测性业务代码。

计划日期：2026-07-28
计划状态：`S1-S2_PASSED / S3_CANARY_VALIDATED / 0_READY`
支线执行键：`CANDIDATE-OBSERVATION-AUTHORIZED-IMPLEMENTATION`

## 1. 范围与非目标

- 范围：`A-02/A-03/A-04/A-06/A-07/A-09/A-10`、`OPS-TRIVY-COLD-CACHE`、`AUTO-IMPROVEMENT-45AD2EFE82162775`。
- 本轮实施：为 `OPS-TRIVY-COLD-CACHE` 增加一次性隔离冷缓存触发能力及契约测试。
- 不实现缺少权威业务样本的预测、日报、排程、报表、外部通知、技术对象或系统集成。
- 不修改 AutoPilot 评分、权重、调度、状态机或环境；已收到完整触发词 `启动迭代-1`，本轮只执行单Issue金丝雀。
- 已授权金丝雀分支、commit、push、手工CI与受保护合并；不删除 Actions 缓存、部署或操作生产。

## 2. 当前 HEAD 裁决

| 稳定键 | 当前裁决 | 缺失前置 |
| --- | --- | --- |
| `A-02` | 保持 `NEEDS_CONFIRMATION` | 权威工作日历、预测对象、误差与收益 |
| `A-03` | 保持 `FROZEN` | 单一现场角色、权威人员/设备事实、隐私边界 |
| `A-04` | 保持 `FROZEN` | 现有单前置模型无法表达的真实样本 |
| `A-06` | 保持 `FROZEN` | 明确报表、同步超时或超限、SLA |
| `A-07` | 保持 `FROZEN` | 首个渠道/事件、供应商、Secret、同意与费用边界 |
| `A-09` | 保持 `FROZEN` | M3 无法表达的真实 Submittal 或重大技术问题 |
| `A-10` | 保持 `FROZEN` | 单一外部系统、沙箱、接口契约与数据责任 |
| `OPS-TRIVY-COLD-CACHE` | `VERIFIED_RESOLVED` | 同SHA push与手工冷缓存CI均通过 |
| `AUTO-IMPROVEMENT-45AD2EFE82162775` | 保持 `OBSERVATION` | 下一完整回顾周期及可复现同类根因 |

## 3. 实施阶段

- [x] S0：复核 9 项当前 HEAD 事实、现有能力和缺失前置。
- [x] S1：增加 `trivyColdCache` 手工布尔输入；开启时跳过唯一共享缓存恢复，扫描前在新 job 工作区创建空目录。
- [x] S1：保留普通 push/PR 与默认手工运行的 UTC 日缓存、历史回退、Trivy `0.65.0`、`HIGH,CRITICAL`、失败退出和 10 分钟扫描超时。
- [x] S1：补充工作流契约测试，阻止隔离冷缓存路径重新引入历史回退。
- [x] S2：实施提交`b19d954c0d6434878025508c60c75e3970d39e09`的push CI 13/13通过；同SHA手工触发`trivyColdCache=true`，缓存恢复为`skipped`，官方漏洞库与Java DB重新下载，扫描及全部13项job通过，安全job用时53秒。
- [x] S3：用户已明确发送 `启动迭代-1`，生成 `ISSUE-055-001` 并进入单Issue金丝雀。
- [ ] S3：累计下一完整周期后再裁决效率观察。
- [x] S4：同步主计划、计划索引、活动问题源、Current Focus、项目地图和阶段验收报告。

## 4. 验收、风险、失败分类与回滚

- 本地退出门：工作流契约、控制面指纹自测、Codex 执行策略、主线计划 Standard 门和 `git diff --check` 全部通过。
- 远端退出门：隔离冷缓存 job 在现有时限内完成官方 Java DB 下载与 `HIGH/CRITICAL` 扫描；失败不得降级或绕过扫描。
- 风险：冷缓存下载可能超时或受外部网络影响；仅影响该次手工 CI，不改变业务代码和默认缓存。
- 控制面指纹由 `a6bc70e858d84b7c57972be39e322e215a6b3b978312b0635be0317cf805ae6c` 变为 `741040cf34e8cb397d24b706e8e7f362794374ced48c9f667283f0bbe573e1e5`；进入 N>1 或无界执行前必须完成用户明确启动的单 Issue 金丝雀。
- 远端下载、缓存或超时失败按 CI 唯一失败分类契约分诊，不自动判为业务缺陷。
- 回滚：删除 `trivyColdCache` 输入和缓存恢复条件，恢复单一路径；共享缓存未删除，可继续使用。

## 5. 零悬空

- 新增后续项：0；沿用8个稳定键。
- 关闭后续项：1；净变化：-1。
- 无载体悬空项：0。
- 下一完整周期尚未完成，由`AUTO-IMPROVEMENT-45AD2EFE82162775`承接；7项产品候选的业务前置仍记录于`current-issues.json`。

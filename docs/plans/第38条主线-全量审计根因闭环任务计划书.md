# 第38条主线：全量审计根因闭环任务计划书

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. 本计划按仓库规则由主线程直接执行，不派子智能体。

**Goal:** 修复 `docs/quality/code-audit-2026-07-12-full-develop-1.5.md` 中全部阻塞项与明确测试缺口，并以同口径全量门禁形成可上线裁决证据。

**Architecture:** 保持生产限流、权限、金额公式和页面功能边界不变，只在测试配置、陈旧契约、固定 SQL 安全说明、E2E 状态判定和供应链扫描覆盖处做最小根因修复。前后端修改分别以 RED/GREEN 证据推进，最后统一执行后端、前端、MySQL、依赖安全、E2E、GitHub 分支保护和 `git diff --check` 验收；不发布生产、不连接生产库、不自动 push。

**Tech Stack:** Java 21、Spring Boot、JUnit 5、Mockito、Maven Wrapper、Vue 3、TypeScript、Vitest、ESLint、pnpm 11、Playwright Chromium、PowerShell、Docker、Trivy、GitHub CLI。

## Global Constraints

- 当前工作分支必须为 `develop/1.5`，每次变更前执行 `git branch --show-current` 与 `git status --short`。
- 不修改已应用 Flyway migration，不连接生产数据库，不删除仓库外文件。
- 不关闭生产安全能力，不放宽 SQL 扫描规则，不删除有效权限或金额断言。
- `autoPush=false`：不自动 push；当前授权不包含 Git commit，计划中的提交步骤只记录建议边界，不执行。
- 前端依赖目录仅在绝对路径确认、清理预览和 ACL 根因复核后重建。
- GitHub 外部变更仅限 master 的 `enforce_admins` 与 `required_conversation_resolution`，变更前后都保存只读 API 摘要。

---

### Task 1: 固化后端限流测试隔离

**Files:**
- Create: `backend/src/test/java/com/cgcpms/common/filter/GlobalWriteRateLimitTestDefaultsTest.java`
- Modify: `backend/src/test/resources/application.yml`
- Modify: `backend/src/test/java/com/cgcpms/common/filter/GlobalWriteRateLimitFilterTest.java`

**Interfaces:**
- Consumes: `GlobalWriteRateLimitProperties.isEnabled()` 与现有 `GlobalWriteRateLimitFilterTest`。
- Produces: 普通测试默认 `enabled=false`，限流专项测试显式 `enabled=true`。

- [ ] **Step 1: 写默认测试配置 RED 用例**

```java
@SpringBootTest
@ActiveProfiles("local")
class GlobalWriteRateLimitTestDefaultsTest {
    @Autowired GlobalWriteRateLimitProperties properties;

    @Test
    void globalWriteRateLimitIsDisabledForUnrelatedIntegrationTests() {
        assertFalse(properties.isEnabled());
    }
}
```

- [ ] **Step 2: 运行 RED**

Run: `cd backend; .\mvnw.cmd '-Dtest=GlobalWriteRateLimitTestDefaultsTest' test`

Expected: FAIL，当前测试 classpath 继承 `enabled=true`。

- [ ] **Step 3: 写最小配置修复**

在 `backend/src/test/resources/application.yml` 增加：

```yaml
rate-limit:
  global-write:
    enabled: false
```

在 `GlobalWriteRateLimitFilterTest` 的 `@SpringBootTest(properties = {...})` 增加：

```java
"rate-limit.global-write.enabled=true"
```

- [ ] **Step 4: 运行 GREEN 与污染回归**

Run:

```powershell
cd backend
.\mvnw.cmd '-Dtest=GlobalWriteRateLimitTestDefaultsTest,GlobalWriteRateLimitFilterTest,SubTaskControllerTest,PreferenceControllerTest,SysDictDataControllerTest,SysDictTypeControllerTest' test
```

Expected: 限流默认与专项测试通过，控制器测试无 429。

---

### Task 2: 修正权限契约与 SQL 安全门禁

**Files:**
- Modify: `backend/src/test/java/com/cgcpms/cashbook/FundAccountMapperTest.java`
- Modify: `backend/src/main/java/com/cgcpms/site/service/SiteDailyLogService.java`
- Modify: `backend/src/main/java/com/cgcpms/subcontract/service/SubTaskService.java`

**Interfaces:**
- Consumes: `countMenus(List<String>)`、`countRolePermissions(Long, List<String>)` 和 SQL 安全 marker 白名单。
- Produces: 权限码语义断言与两处 `fixed-sql-fragment` 说明。

- [ ] **Step 1: 复现权限测试与 SQL 扫描 RED**

Run:

```powershell
cd backend
.\mvnw.cmd '-Dtest=FundAccountMapperTest' test
cd ..
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/check-sql-safety.ps1
```

Expected: 菜单 ID 956/957/958 断言失败；SQL 脚本命中两处固定片段。

- [ ] **Step 2: 删除陈旧数字 ID 断言，保留权限码边界**

保留并强化以下语义：

```java
assertEquals(0, countMenus(List.of("file:upload", "file:query", "file:delete")));
assertEquals(0, countRolePermissions(6L,
        List.of("cashbook:account:manage", "file:upload", "file:query", "file:delete")));
```

删除：

```java
assertEquals(0, countMenuIds(List.of(956L, 957L, 958L)));
```

- [ ] **Step 3: 为固定服务端片段加精确 marker**

```java
query.apply("1 = 0"); // SQL-SAFETY: fixed-sql-fragment
wrapper.apply("1 = 0"); // SQL-SAFETY: fixed-sql-fragment
```

- [ ] **Step 4: 运行 GREEN**

Run:

```powershell
cd backend
.\mvnw.cmd '-Dtest=FundAccountMapperTest,TenantBoundaryTask2Test' test
cd ..
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/check-sql-safety.ps1
```

Expected: 权限/租户边界测试与 SQL 扫描全部通过。

---

### Task 3: 补齐结算金额快照直接测试

**Files:**
- Create: `backend/src/test/java/com/cgcpms/settlement/StlSettlementWriteServiceAmountTest.java`

**Interfaces:**
- Consumes: `StlSettlementWriteService.create(StlSettlement)`、`StlSettlementQueryService.sumVarOrderConfirmed/sumSubMeasureApproved/sumPaidAmount`。
- Produces: 公开创建入口的金额公式和 tenant/contract 参数回归保护。

- [ ] **Step 1: 写金额回归测试**

使用 Mockito 构造五个依赖，设置合同当前金额 `1000.00`、变更 `100.00`、计量 `200.00`、已付 `300.00`、扣款 `50.00`，调用 `create` 后断言：

```java
assertEquals(new BigDecimal("1000.00"), settlement.getContractAmount());
assertEquals(new BigDecimal("100.00"), settlement.getChangeAmount());
assertEquals(new BigDecimal("200.00"), settlement.getMeasuredAmount());
assertEquals(new BigDecimal("300.00"), settlement.getPaidAmount());
assertEquals(new BigDecimal("50.00"), settlement.getDeductionAmount());
assertEquals(new BigDecimal("1250.00"), settlement.getFinalAmount());
assertEquals(new BigDecimal("62.50"), settlement.getWarrantyAmount());
assertEquals(new BigDecimal("887.50"), settlement.getUnpaidAmount());
verify(queryService).sumVarOrderConfirmed(TENANT_ID, CONTRACT_ID);
verify(queryService).sumSubMeasureApproved(TENANT_ID, CONTRACT_ID);
verify(queryService).sumPaidAmount(TENANT_ID, CONTRACT_ID);
```

- [ ] **Step 2: 运行测试并确认测试装配正确**

Run: `cd backend; .\mvnw.cmd '-Dtest=StlSettlementWriteServiceAmountTest' test`

Expected: 测试通过；若暴露金额公式或参数错误，则只修复被该用例证明的根因后重跑。

- [ ] **Step 3: 运行现有结算回归**

Run: `cd backend; .\mvnw.cmd '-Dtest=StlSettlementWriteServiceAmountTest,StlSettlementServiceTest,StlSettlementControllerMockMvcTest' test`

Expected: 全部通过。

---

### Task 4: 修复前端 lint 与三个陈旧契约

**Files:**
- Modify: `frontend-admin/src/pages/inventory/stock.vue`
- Modify: `frontend-admin/src/layouts/components/__tests__/SidebarMenu.test.ts`
- Modify: `frontend-admin/src/pages/contract/__tests__/ContractLedgerPage-ui-consistency.test.ts`
- Modify: `frontend-admin/src/pages/dashboard/__tests__/DashboardReferenceFidelity.test.ts`

**Interfaces:**
- Consumes: `navigationItems`、`buildDateColumn`、采购驾驶舱拆分交期指标。
- Produces: 与当前产品实现一致的语义测试和 0 lint error。

- [ ] **Step 1: 运行四项 RED/错误复现**

Run:

```powershell
docker exec cgc-pms-frontend-dev pnpm exec vitest run src/layouts/components/__tests__/SidebarMenu.test.ts src/pages/contract/__tests__/ContractLedgerPage-ui-consistency.test.ts src/pages/dashboard/__tests__/DashboardReferenceFidelity.test.ts
docker exec cgc-pms-frontend-dev pnpm exec eslint src/pages/inventory/stock.vue
```

Expected: 3 个测试失败，库存页面报告 2 个未使用变量。

- [ ] **Step 2: 写最小契约修复**

```typescript
// stock.vue：从 useStockLedger 解构中删除
fetchKpi,
fetchWarehouses,

// SidebarMenu.test.ts：项目经营期望末尾增加
'现场日报',

// ContractLedgerPage-ui-consistency.test.ts
expect(composableSource).toContain("buildDateColumn('signedDate', '签订日期'")

// DashboardReferenceFidelity.test.ts
expect(purchaseViewSource).toContain('lateCompletedCount')
expect(purchaseViewSource).toContain('overdueIncompleteCount')
```

删除对组件必须包含 `overdueOrderCount` 的重复展示要求；后端/类型兼容字段不删除。

- [ ] **Step 3: 运行 GREEN 与前端静态门禁**

Run:

```powershell
docker exec cgc-pms-frontend-dev pnpm exec vitest run src/layouts/components/__tests__/SidebarMenu.test.ts src/pages/contract/__tests__/ContractLedgerPage-ui-consistency.test.ts src/pages/dashboard/__tests__/DashboardReferenceFidelity.test.ts
docker exec cgc-pms-frontend-dev pnpm lint:check
docker exec cgc-pms-frontend-dev pnpm type-check
```

Expected: 相关测试通过、lint 0 error、type-check 通过。

---

### Task 5: 重建主机依赖并修复付款页 E2E 状态契约

**Files:**
- Regenerate only: `frontend-admin/node_modules/`
- Modify: `frontend-admin/e2e/ui-refactor-smoke.spec.ts`

**Interfaces:**
- Consumes: 锁文件 pnpm 11.0.9、Playwright 1.55.x、付款页 `.payment-head-digest`、`.vxe-table`、`.payment-list-feedback`。
- Produces: 主机可执行 ESLint/Playwright 与兼容空数据的付款页 smoke。

- [ ] **Step 1: 安全核对并预览依赖目录**

Run:

```powershell
$target = (Resolve-Path frontend-admin/node_modules).Path
if ($target -ne 'D:\projects-test\cgc-pms\frontend-admin\node_modules') { throw "unexpected target: $target" }
git clean -fdn frontend-admin/node_modules
Get-Content frontend-admin/node_modules/eslint/bin/eslint.js -TotalCount 1
```

Expected: 绝对路径匹配；读取因 ACL 拒绝而失败，确认重建对象是可再生成依赖。

- [ ] **Step 2: 重建依赖并安装浏览器**

使用 PowerShell 原生 `Remove-Item -LiteralPath $target -Recurse -Force`，随后：

```powershell
cd frontend-admin
corepack pnpm install --frozen-lockfile
corepack pnpm exec playwright install chromium
```

Expected: 安装成功，ESLint 入口可读，Playwright Chromium 可执行文件存在。

- [ ] **Step 3: 先复现付款 E2E RED**

Run: `cd frontend-admin; corepack pnpm exec playwright test e2e/ui-refactor-smoke.spec.ts -g '付款申请页'`

Expected: 在空数据时因强制要求表格而失败。

- [ ] **Step 4: 改为明确的双状态断言**

```typescript
await expect(page.locator('.payment-head-digest')).toBeVisible()
const tableOrFeedback = page.locator('.vxe-table, .payment-list-feedback')
await expect(tableOrFeedback.first()).toBeVisible({ timeout: 5000 })
```

保留 `.lg-page` 可见断言，移除“表格必须存在”和可选 KPI 日志分支。

- [ ] **Step 5: 运行付款用例和完整 UI smoke**

Run:

```powershell
cd frontend-admin
corepack pnpm exec playwright test e2e/ui-refactor-smoke.spec.ts -g '付款申请页'
corepack pnpm test:e2e:ui
```

Expected: 付款用例与完整 smoke 全部通过。

---

### Task 6: 对构建后 fat JAR 增加供应链漏洞扫描

**Files:**
- Modify: `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: `artifacts/backend/cgc-pms-backend.jar` 与 `aquasec/trivy:0.65.0`。
- Produces: 构建产物 HIGH/CRITICAL library 漏洞阻断步骤，保留 SBOM/attestation。

- [ ] **Step 1: 建立 workflow RED 静态断言**

Run:

```powershell
$ci = Get-Content -Raw .github/workflows/ci.yml
if ($ci -notmatch 'Scan built backend jar') { throw 'missing built-jar vulnerability scan' }
```

Expected: FAIL。

- [ ] **Step 2: 在下载 backend JAR 后增加扫描步骤**

```yaml
      - name: Scan built backend jar
        run: |
          docker run --rm -v "$PWD/artifacts/backend:/workspace:ro" aquasec/trivy:0.65.0 \
            fs --scanners vuln --vuln-type library --severity HIGH,CRITICAL \
            --exit-code 1 --format table --timeout 10m /workspace/cgc-pms-backend.jar
```

- [ ] **Step 3: 验证 YAML、静态契约和本地 JAR 扫描**

Run:

```powershell
cd backend
.\mvnw.cmd -q -DskipTests package
cd ..
docker run --rm -v "${PWD}/backend/target:/workspace:ro" aquasec/trivy:0.65.0 fs --scanners vuln --vuln-type library --severity HIGH,CRITICAL --exit-code 1 --format table --timeout 10m /workspace/cgc-pms-backend.jar
```

并用可用 YAML 解析器读取 `.github/workflows/ci.yml`；Expected: 语法通过且 0 HIGH/CRITICAL。

---

### Task 7: 加固 master 分支保护

**Files:**
- External state only: GitHub repository `kismet84/cgc-pms`, branch `master`

**Interfaces:**
- Consumes: 当前 11 个 required checks 与 branch protection API。
- Produces: `enforce_admins=true`、`required_conversation_resolution=true`，其他保护不回退。

- [ ] **Step 1: 保存变更前摘要**

Run: `gh api repos/kismet84/cgc-pms/branches/master/protection`

Expected: strict=true，11 个 contexts，enforce_admins=false，conversation resolution=false。

- [ ] **Step 2: 启用两项保护**

Run:

```powershell
gh api --method POST repos/kismet84/cgc-pms/branches/master/protection/enforce_admins
gh api --method PUT repos/kismet84/cgc-pms/branches/master/protection/required_conversation_resolution
```

- [ ] **Step 3: 重新读取并逐项核验**

Run: `gh api repos/kismet84/cgc-pms/branches/master/protection`

Expected: 两项 enabled=true，11 个 required checks、strict、force-push/deletion 保护保持原值。

---

### Task 8: 执行同口径全量验证

**Files:**
- No source changes unless a verification failure exposes a new root cause.

**Interfaces:**
- Consumes: Tasks 1-7 的全部产物。
- Produces: 可支撑正式上线裁决的完整命令证据。

- [ ] **Step 1: 后端全量门禁**

Run: `cd backend; .\mvnw.cmd -q verify`

Expected: 0 failures、0 errors。

- [ ] **Step 2: 前端全量门禁**

Run:

```powershell
cd frontend-admin
corepack pnpm lint:check
corepack pnpm type-check
corepack pnpm build
corepack pnpm check:bundle-size
corepack pnpm test:coverage
corepack pnpm audit --audit-level high --registry=https://registry.npmjs.org
corepack pnpm test:e2e:ui
```

Expected: 全部 exit 0。

- [ ] **Step 3: 数据库、安全与供应链门禁**

执行 SQL 安全脚本、MySQL 8 Flyway 临时容器烟测、源码目录与 fat JAR Trivy HIGH/CRITICAL 扫描；所有失败先按工具配置、环境前置、真实质量分类并复验。

- [ ] **Step 4: 运行态与浏览器验收**

检查 8080 health、5173 首页、dev-login；使用内置浏览器从 dashboard 点击进入一个核心业务页并检查控制台。若 WebView 仍无法附着，以已通过的 Playwright E2E 作为页面自动化主证据，并把 WebView 明确归为外部工具限制，不冒充产品失败。

- [ ] **Step 5: Git 完整性**

Run:

```powershell
git diff --check
git status --short
git clean -fdn
```

Expected: diff check 通过；只出现正式交付物和本任务确认的修改；临时产物仅预览不盲删。

---

### Task 9: 更新正式审计结论与项目地图

**Files:**
- Modify: `docs/quality/code-audit-2026-07-12-full-develop-1.5.md`
- Modify: `docs/product-intelligence/project-map.md`
- Modify only if remaining risk exists: `docs/backlog/blocked-issues.md` or `docs/backlog/current-focus.md`

**Interfaces:**
- Consumes: Task 8 的新鲜全量验证证据和 Task 7 的 GitHub API 结果。
- Produces: 逐项解除记录、通过/不通过、阻塞/非阻塞、是否可上线裁决。

- [ ] **Step 1: 对照原报告逐项核验 B-01 至 B-07 和五类环境/工具问题**

每项必须关联直接命令或当前 API 证据，不用“未发现问题”替代通过证明。

- [ ] **Step 2: 更新报告和项目地图**

报告记录正式交付物、验收证据、临时产物、git 状态、结论、阻塞和剩余风险。若仍有未解除风险，同步写入 backlog；若全部解除，明确说明无剩余阻塞。

- [ ] **Step 3: 最终文档与 diff 复验**

Run: `git diff --check; git status --short`

Expected: 通过，且未提交、未 push。

---

## 提交边界

当前用户授权覆盖代码、测试、依赖环境和 GitHub 分支保护修复，但未明确授权 Git commit 或 push。因此本计划执行完成后只回报可提交文件集合；如用户后续明确授权，再按最小逻辑单元提交。绝不自动 push。

# 第98条主线：全量审计发现整改与本地就绪收口

**Goal:** 对 `master@8ff20a3c10bcad015883db59f600b19af7a4a729` 全量审计登记的 11 项风险逐项修复、验证或基于当前事实关闭，使安全、监控、文件错误边界、部署配置、审计可观测性、依赖、备份恢复、性能、架构和格式门禁在本地环境零悬空收口。 **Architecture:** 保持现有模块化单体、JWT 业务认证、Spring Security、Prometheus、Docker Compose、Flyway 和 Vue 预览链；以模板双层隔离、独立机器抓取身份、稳定错误契约、持久日志卷、单一数据库名参数和审计失败指标完成最小根因修复。不因文件行数机械拆分，不引入新依赖、公共 API、数据库 Schema 或第二套审计系统。

> 编制日期：2026-08-19
>
> 代码基线：`master@8ff20a3c10bcad015883db59f600b19af7a4a729`
>
> 工作区边界：保留审计开始前 6 个已修改文件、1 个未跟踪文件及本轮审计文档；仅修改本任务直接需要的代码、配置、测试、计划、Codemap 与质量报告
>
> 环境边界：仅本地 dev/test/demo；禁止连接、测试或声称非本地环境验收
>
> Git 边界：不授权 branch、commit、push、PR、合并、Tag、Release 或清理

## 1. 范围与裁决

| ID | 风险 | 本轮裁决目标 |
| --- | --- | --- |
| SEC-001 | 单据模板事件属性与无 sandbox 预览 | 服务端拒绝事件处理属性；两处预览 iframe 强制空 sandbox；后端、前端和真实浏览器回归 |
| OPS-001 | Prometheus 无可持续机器认证 | 独立 Basic 机器身份仅可访问 Prometheus endpoint；Secret 文件载体；无/错/JWT 凭据拒绝；UI 仅监听本机 |
| API-001 | 对象存储异常文本外泄 | 对外固定错误码和消息；详细异常只进脱敏日志；回归验证 SDK 文本不外泄 |
| OBS-001 | 生产文件日志未持久化 | Compose 将 `/var/log/cgc-pms` 挂载到独立持久卷并纳入配置契约 |
| DEPLOY-001 | MySQL 数据库名双源 | JDBC URL 与 `MYSQL_DATABASE` 使用同一参数；自定义名称配置契约通过 |
| OBS-002 | 通用操作审计失败不可量化 | 增加 attempt/success/failure 指标及失败告警；保留最佳努力语义，关键财务审计继续使用既有强一致链 |
| DEP-001 | 后端依赖扫描缺本次证据 | 执行仓库固定 Trivy 扫描；HIGH/CRITICAL 门禁通过或按统一分类整改 |
| DR-001 | 备份恢复演练缺本次证据 | 在隔离本地容器执行既有恢复演练；不触碰当前开发数据库 |
| PERF-001 | 当前版本性能证据不足 | 执行现有查询预算、并发与本地基准；仅裁决本地可重复门槛 |
| ARCH-001 | 千行文件维护风险 | 对照第94条当前成果复核职责；只修已确认的多职责根因，单纯行数告警关闭 |
| STYLE-001 | 前端 Prettier 告警 | 仅格式化实际告警文件，保持语义不变，lint 零告警 |

## 2. 非目标

- 不改公共 API、DTO、路由、权限业务模型、金额口径、状态机或数据库 Schema。
- 不建设通用消息总线、持久审计 outbox、外部日志平台、微服务或新监控平台。
- 不以生产、目标环境、发布或同 SHA CI 作为本轮本地完成证据。
- 不覆盖或回退审计前用户已有改动；重叠文件只做目标化补丁。

## 3. 门禁

- G0：分支、HEAD、脏文件、内容指纹、Codemap 三问、本地数据源、租户、运行 URL 与功能开关冻结。
- G1：安全、认证、错误契约、配置和可观测性设计与负向测试完成。
- G2：后端定向测试、依赖扫描、备份恢复、性能/并发门禁通过。
- G3：前端单测、lint、type-check、build 与部署/治理静态契约通过。
- G4：现有本地运行态经最小刷新后，健康、真实 URL、模板安全 DOM 和 console 验收通过。
- G5：Codemap、全量门禁、质量报告、风险登记、Backlog 去重和零悬空统计完成；Git 交付明确未授权。

## 4. 风险与回滚

1. 安全链回滚需同时回退服务端事件属性校验、预览 sandbox 与测试，禁止只回退一层。
2. 机器认证若配置缺失必须失败关闭 Prometheus endpoint，不能退回普通用户 JWT 或匿名抓取。
3. Compose 变更只影响后续容器创建；回滚恢复原配置即可，持久卷不自动删除。
4. 格式化只限 lint 报告文件；出现语义差异时逐文件回退本轮格式化，不触碰审计前脏文件。

## 5. G0 事实记录

- 当前分支 `master`，HEAD `8ff20a3c10bcad015883db59f600b19af7a4a729`；仅一个工作树。
- 本地容器：backend `127.0.0.1:8080`、frontend `127.0.0.1:5173`、MySQL `127.0.0.1:3307`、Redis、MinIO、ClamAV 均已启动；后端健康。
- 当前本地运行态使用 dev profile、租户 `0`、dev-login；本轮不重置数据，不改变业务功能开关。
- Codemap 已用于确认 `document-template-platform`、`file-document`、`security-auth`、`observability`、`prod-config-gate` 的调用方、影响面和覆盖测试；代码修改前重生成三件套，收口时再次验证。

## 6. 收口状态

本地 G0～G5 已完成。原 11 项中 7 项修复并复验、3 项补证关闭、1 项无明确价值关闭；整改中新发现 `OBS-003/P2` 已同轮修复。当前开放风险 0，新增/关闭/净变化后续项为 0/1/-1。Git 交付未授权，因此无远端同 SHA CI、PR、合并或发布声明。

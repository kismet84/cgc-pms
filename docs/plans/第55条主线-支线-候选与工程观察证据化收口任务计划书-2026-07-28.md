# 第55条主线支线：候选与工程观察证据化收口任务计划书

**Goal:** 对第55条主线现有 7 项产品候选、2 项 AutoPilot 效率观察和 1 项 Trivy 冷缓存观察完成证据纠偏与逐项裁决；关闭已被正式 v2 评分模型替代的 v1 观察，其余事项保持原稳定键和决策门，不把观察直接改成实现任务。

**Architecture:** 复用 `docs/backlog/current-issues.json`、第55条 M4 报告、AutoPilot v2 正式启用证据和 CI 扫描门禁。`CANDIDATE-OBSERVATION-EVIDENCE-CLOSEOUT` 仅是支线执行键，不是新增全局 Issue；禁止新建候选平台、重复问题系统、控制面试验或批量解冻。

计划日期：2026-07-28
计划状态：`CLOSED / REVIEW_PASS / 1_CLOSED / 0_READY`
支线执行键：`CANDIDATE-OBSERVATION-EVIDENCE-CLOSEOUT`
优先级：P1
主责：项目总负责人

## 1. 范围与计数

本支线复核以下 10 个既有稳定键：

- 产品候选：`A-02/A-03/A-04/A-06/A-07/A-09/A-10`。
- 工程观察：`OPS-TRIVY-COLD-CACHE`、`AUTO-IMPROVEMENT-7EFDFA50553C2152`、`AUTO-IMPROVEMENT-45AD2EFE82162775`。

结果：

- `AUTO-IMPROVEMENT-7EFDFA50553C2152` 关闭并从活动问题源移除。
- 其余 9 项状态不变；未形成 Ready。
- `current-issues.json` 从 14 项降为 13 项。
- 本支线新增全局事项 0、关闭 1、净变化 `-1`、重复 0、悬空 0。

非目标：

- 不实现产品候选，不自动下单，不扩建日报/WBS/报表/通知/技术/集成平台。
- 不修改 AutoPilot 评分、权重、调度、状态机、指纹或运行环境。
- 不清理 GitHub Actions 缓存，不降低或跳过 Trivy 扫描。
- 不实施上线、部署或生产事项。

## 2. 最终裁决

| 稳定键 | 裁决 | 当前证据 |
| --- | --- | --- |
| `A-02` | 保持 `NEEDS_CONFIRMATION` | 已有提前期、在途、调拨和净领料；仍无权威工作日历、预测对象、误差基线和量化收益 |
| `A-03` | 保持 `FROZEN` | 日报及质量安全联动已存在；仍无单一角色、痛点、频次和权威源 |
| `A-04` | 保持 `FROZEN` | 单前置 FS 已成立；无现有模型无法表达的真实多前置样本 |
| `A-06` | 保持 `FROZEN` | 同步 CSV 和驾驶舱已存在；无同步失败、数据量或 SLA 证据 |
| `A-07` | 保持 `FROZEN` | 站内通知已闭环；无渠道、事件、供应商、Secret、同意和 SLA 边界 |
| `A-09` | 保持 `FROZEN` | M3 已覆盖方案、会审、RFI、交底和归档；无 M3 不能承接的真实样本 |
| `A-10` | 保持 `FROZEN` | 财务端点与 outbox 基础存在；无已批准目标系统、契约和沙箱 |
| `OPS-TRIVY-COLD-CACHE` | 保持 `OBSERVATION` | 近期成功运行使用已有缓存；本轮未获授权删除共享 Actions 缓存 |
| `AUTO-IMPROVEMENT-7EFDFA50553C2152` | `CLOSED_SUPERSEDED` | v1 `cycleEfficiency` 仅有 1 个历史样本；正式 v2 已改用 `taskExecutionEfficiency`，新任务不再生成 v1 正式分，历史不回算 |
| `AUTO-IMPROVEMENT-45AD2EFE82162775` | 保持 `OBSERVATION` | 唯一正式周期 27 个样本均值 6.11、中位数 10；无下一完整周期或稳定同类根因 |

## 3. 阶段结果

| 阶段 | 结果 | 退出证据 |
| --- | --- | --- |
| S0 事实冻结与去重 | `PASSED` | 10/10 键、状态、证据和唯一载体可复读 |
| S1 AutoPilot 纠偏 | `PASSED` | v1/v2 边界明确；关闭 1 个不可再产生新样本的 v1 观察 |
| S2 Trivy 冷缓存验证 | `SKIPPED` | 需要破坏共享缓存状态；保留观察，不改门禁 |
| S3 产品候选前置 | `PASSED / 0_READY` | 7 项均缺真实样本或量化价值，状态守恒 |
| S4 状态同步与收口 | `PASSED` | 活动源移除已关闭键，主计划与报告同步 |

## 4. 裁决门

1. 产品候选只有真实样本、权威事实源、量化价值、权限/租户边界、最小验收和回滚齐全时才可形成一个 Ready。
2. `A-02` 即使解锁也只输出只读建议，不自动创建订单。
3. v1 历史评分保持不可变；关闭观察不删除、不重算历史事实。
4. v2 低分须先形成稳定同类根因；控制面行为变化仍需新指纹与用户明确的单 Issue 金丝雀。
5. Trivy 保持 `HIGH,CRITICAL` 和失败退出；冷缓存验证须另获共享缓存治理授权。

## 5. 风险、验收与回滚

- 10 项有且仅有一个裁决；`current-issues.json` 不新增键。
- v1/v2 不混算，历史评分不回算。
- 7 项产品候选保持原决策门，0 项无证据解锁。
- Trivy 未改缓存、阈值或 workflow。
- 若 v1 后续重新成为正式 active 版本，则回退 `CLOSED_SUPERSEDED` 为观察并重新取得完整周期，不复用旧单样本。
- 失败分类：Trivy 冷缓存验证属于 `infrastructure_or_environment` 且需破坏共享缓存，本轮按授权跳过；其余事项无执行失败。

## 6. 零悬空收口

- 新增后续项：0。
- 关闭后续项：1。
- 后续项净变化：`-1`。
- 当前保留：9。
- 重复立项：0。
- 无载体悬空项：0。

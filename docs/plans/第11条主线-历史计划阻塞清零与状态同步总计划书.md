# 第 11 条主线：历史计划阻塞清零与状态同步总计划书

## 计划状态

正式版。

本主线不是业务功能实现包，而是主负责人层面的总协调收口主线。目标不是新增功能，而是把现有多条主线/专项中的阻塞、条件性阻塞、未核实状态和文档状态漂移统一收口，避免后续继续在过期计划或错误状态上分派。

## 一、制定依据

本计划基于以下已存在材料和主线程最新裁决：

1. `docs/quality/all-plans-blockers-summary-2026-07-01.md`
2. `docs/plans/第10条主线-权限字典治理与历史权限码治理裁决任务计划书.md`
3. `docs/plans/第10A主线-权限字典治理与历史权限码治理首批最小修复包任务计划书.md`
4. 已关闭或阶段性收口的第 8 / 9 / 10B / 10C / 10D 主线与首包结论
5. 主线程最新裁决：`alert:edit` 不通过，10A 首包停止在只读裁决阶段
6. 10A 本轮点状治理最新事实：`V113 notification:list`、`V114 partner:list`、`V115 contract:list` 已通过并推送 GitHub，commit 为 `e0d7a32c3ad4c3e3f0f584e3073e49b67a8ddea9`

## 二、主负责人纠偏结论

对现有阻塞报告做如下纠偏：

1. `all-plans-blockers-summary-2026-07-01.md` 中“10A 已定义最小修复包”这一事实已经部分过期。
2. 最新事实不是“10A 没有可执行首包”，而是：
   - 10A 确实尝试过首包定义；
   - `alert:edit` 首样本已被安全侧判定为不通过；
   - 第二版低风险样本 `notification:list` 已实施并归档通过，第二版首包可关闭。
3. 也就是说，当前真实状态已经从：
   - “仍在等待重新选样本或暂停”
   变为：
   - “第二版首包已收口，10A 全量历史权限码治理仍未完成，但不再被该样本阻塞”。
4. 第 3 / 5 / 6 / 7 条当前仍不是“已确认阻塞”，而是“状态未核实”。
5. 第 9 条和 10A 的文档状态漂移本身已成为协作阻力，需单独清理。

## 三、主线目标

第 11 条只做四件事：

1. 同步阻塞事实：
   - 让阻塞报告与 10A 最新裁决一致。
2. 核实旧主线状态：
   - 第 3 条
   - 第 5 条
   - 第 6 条
   - 第 7 条
3. 同步计划书状态：
   - 第 9 条
   - 第 10 条 / 10A
4. 为 10A 重新决定后续路径：
   - 记录第二版首包 `notification:list` 已实施并归档通过
   - 关闭第二版首包，不把它外推成 10A 全量完成

## 四、明确不纳入范围

1. 不在本主线内直接修任何业务代码。
2. 不在本主线内直接执行 migration。
3. 不在本主线内继续推进 10B / 10C / 10D 的后续扩面。
4. 不借“状态核实”顺手做新需求。
5. 不把付款申请接入、第二租户、复杂 `data_scope`、全站入口平台化、权限码本体编辑并入本线。

## 五、当前总判断

### 已确认阻塞

1. 10A 作为全量治理主线当前不构成单样本阻塞。
2. 历史上曾阻塞的首包样本已收口；当前仅保留全量未完与逐码裁决边界。

### 条件性阻塞

以下主线当前没有现行阻塞，但仍保留执行期阻塞条件：

1. 10B 首包
2. 10C 首包
3. 10D 首包

### 未核实状态

1. 第 3 条
2. 第 5 条
3. 第 6 条
4. 第 7 条

### 文档状态漂移

1. 第 9 条实际已收口，但计划书仍是 `正式版候选`
2. 第 10 条 / 10A 当前事实已变化，需要同步“第二版首包已实施并归档通过”的状态

## 当前执行进度

### 已完成

1. 已核对并纠偏阻塞报告 `docs/quality/all-plans-blockers-summary-2026-07-01.md`：
   - 将 10A 的阻塞原因从“无首包”更新为“第二版首包 `notification:list` 已实施并归档通过，首包已收口”
   - 将第 5 / 6 / 7 条从“未核实”移出
   - 将第 3 条更新为“观察项已收口，非独立实现关闭”
2. 已完成旧主线状态核实：
   - 第 5 条：`approval-business-entry-acceptance-2026-07-01.md`
   - 第 6 条：`approval-business-entry-deeplink-acceptance-2026-07-01.md`
   - 第 7 条：`approval-center-production-closure-2026-07-01.md`
   - 第 7 条剩余风险：`approval-center-production-risk-closure-2026-07-01.md`
   - 第 3 条观察项收口：`production-timing-and-approval-business-entry-closure-2026-07-01.md`
3. 已同步文档状态：
   - 第 9 条计划书已改为正式版并补当前关闭口径
   - 第 10 条 / 10A 已补“第二版首包已实施并归档通过”的最新状态
   - `第10A主线-权限字典治理与历史权限码治理首批最小修复包任务计划书.md` 已改为不通过
4. 10A 本轮后续点状治理已收口：
   - `V113 notification:list`、`V114 partner:list`、`V115 contract:list` 已通过并推送 GitHub
   - 仍保留 `invoice:list`、`inventory:transaction:list`、`dashboard:view`、`project:list` 的逐码裁决边界，不回灌为第 11 条阻塞

### 当前状态

1. 第 11 条小包 1、2、3 已完成。
2. 小包 4 已收敛出当前唯一正确结论：10A 第二版首包已收口，且本轮 `V113/V114/V115` 三个点状包已完成；后续只需继续逐码裁决 `invoice:list`、`inventory:transaction:list`、`dashboard:view`、`project:list` 等剩余候选，不再沿用“重新选样本或暂停”的旧口径。
3. 当前第 11 条已达到主线程收口条件。

## 六、串行小包

### 小包 1：阻塞报告纠偏

执行角色：

- 代码质量审计员
- 安全审计员

目标：

1. 将 `all-plans-blockers-summary-2026-07-01.md` 与 10A 最新裁决对齐。
2. 明确写出：
   - 10A 第二版首包已实施并归档通过
   - 不再把该首包写成当前阻塞点

验收：

1. 阻塞报告不再使用过期口径。
2. 不修改业务代码。

### 小包 2：旧主线状态核实

执行角色：

- 代码质量审计员
- 测试验收员
- 安全审计员

核实对象：

1. 第 3 条主线
2. 第 5 条主线
3. 第 6 条主线
4. 第 7 条主线

目标：

1. 判清每条主线是：
   - 已关闭
   - 已完成但未归档
   - 有非阻塞遗留
   - 仍存在阻塞
2. 不靠计划书猜状态，只接受：
   - 归档文档
   - 运行态证据
   - 测试/验收证据
   - 已有主线程结论

验收：

1. 四条主线分别给出明确状态。
2. 若证据不足，必须写“未核实”，不得冒充完成。

### 小包 3：文档状态同步

执行角色：

- 代码质量审计员

目标：

1. 同步第 9 条计划书状态，使其与实际收口一致。
2. 同步第 10 条 / 10A 计划口径，使其反映“第二版首包已实施并归档通过”的最新事实。
3. 必要时补一份“主线状态对照表”归档。

验收：

1. 计划书状态与主线程实际结论一致。
2. 不新增新的执行范围。

### 小包 4：10A 后续路径裁决

执行角色：

- 产品/需求负责人
- 后端设计师 / 后端架构助手
- 数据库与迁移负责人
- 安全审计员
- 代码质量审计员
- 主负责人裁决

目标：

在 `alert:edit` 样本不通过后，后续已经完成第二版首包收口，当前只保留两种边界：

1. 继续对其他历史权限码逐码裁决；
2. 不把 `notification:list` 的收口结果外推成 10A 全量完成。

补充说明：

1. 本轮 `V113 notification:list`、`V114 partner:list`、`V115 contract:list` 已通过并推送 GitHub，说明 10A 的后续收口已进入点状治理阶段。
2. `invoice:list`、`inventory:transaction:list`、`dashboard:view`、`project:list` 均未进入本轮 migration，原因分别为非管理角色绑定、测试/菜单预期、dashboard 回退风险、项目边界风险。
3. 以上剩余候选只作为逐码裁决边界保留，不回灌为第 11 条阻塞，也不代表 10A 全量完成。

验收：

1. 必须明确后续逐码裁决项为何仍需保留。
2. 不得把第二版首包收口结果外推成批量清理。

## 七、通过 / 不通过标准

### 通过条件

1. 阻塞报告已完成纠偏。
2. 第 3 / 5 / 6 / 7 条状态已核实或被明确标记为“仍未核实”。
3. 第 9 条、10A 计划状态与最新事实同步。
4. 10A 后续路径已有清晰裁决：
   - 新样本
   - 或暂停
   - 以及本轮 `V113/V114/V115` 三个点状包已完成，剩余候选仅做逐码裁决

### 不通过条件

1. 继续沿用过期阻塞口径。
2. 把“未核实”写成“已完成”。
3. 在没有重新裁决的情况下继续推进 10A 修复实现。
4. 借第 11 条主线扩出新的业务实现包。

## 八、当前执行建议

当前不再建议继续拆更多动作。第 11 条已完成其协调收口目标，下一步应由主负责人单独裁决：10A 是继续重选样本，还是暂停。

## 九、主负责人裁决

1. 第 11 条主线成立。
2. 这是一条协调收口主线，不是功能实现主线。
3. 当前第一优先级不是写代码，而是把“阻塞事实、计划状态、旧主线状态”对齐。
4. 在第 11 条完成前，不建议继续为 10A 强行开新的实现包。

## 当前关闭口径

第 11 条已完成当前计划书范围内的全部任务，即：

1. 阻塞报告已完成纠偏。
2. 第 3 / 5 / 6 / 7 条状态已完成核实。
3. 第 9 条与 10A 文档状态已同步到最新事实。
4. 10A 当前后续路径已收敛为：第二版首包已收口，本轮 `V113/V114/V115` 三个点状包已完成，后续仅剩其他历史权限码逐码裁决。

第 11 条主线本身可按“通过 / 非阻塞”关闭。后续若继续推进，只应另开：

1. `10A 其他历史权限码逐码裁决包`
2. 或 `10A 全量收口归档包`

**Goal:** 在不改变既有业务语义、接口契约、权限模型、数据库结构和运行环境的前提下，针对当前扫描出的 21 个超过 1000 行的代码文件制定分阶段拆分计划；后端生产代码优先消除超 1000 行风险，前端已审/已重建 UI 页面以视觉无变化为硬门槛，若视觉不变与 1000 行目标冲突，允许阶段性行数豁免并记录理由、剩余风险和后续拆分条件；测试类超长项仍要么拆分，要么有明确保留理由。 **Architecture:** 继续沿用仓库现有技术栈与目录结构，前端保持 Vue 3 + TypeScript + Ant Design Vue + 既有 `lg-*` 体系，后端保持 Spring Boot 既有服务分层与测试组织方式；本主线只做最小可行拆分，不引入新框架、新依赖、新代码生成器，不借题发挥重做业务逻辑，只允许在原模块内按门面化、私有组件化、组合逻辑下沉、测试夹具/断言拆分、样式分层等方式外科手术式收敛。

# 第24条主线-代码大文件拆分任务计划书

## 一、结论与执行建议

- 建议通过：通过，当前已完成计划内拆分与归档收口。
- 当前性质：结构治理型主线已完成执行，现阶段只剩提交集成和可选浏览器截图级视觉复验。
- 最终口径：原始 21 个候选超长代码文件均已完成拆分、收敛验收，或被确认为非代码锁文件排除对象；`frontend-admin/pnpm-lock.yaml` 不纳入代码拆分范围。
- `frontend-admin/src/assets/styles/global.css` 已完成顺序保持式最小拆分，并通过独立验收，结论为 `PASS/非阻塞`，不再是阶段性豁免、待裁决或 `M5` 条件门控项。

## 二、当前收口结果

### 2.1 最终收口状态

| 类别 | 结论 |
| --- | --- |
| 后端超长生产代码 | 已完成拆分或收敛，不再存在计划内待裁决项 |
| 前端超长页面/视图 | 已完成拆分并通过复验或独立验收 |
| 共享样式 `global.css` | 已完成顺序保持式拆分并独立验收 `PASS/非阻塞` |
| 锁文件 `frontend-admin/pnpm-lock.yaml` | 非代码文件，排除出本主线拆分范围 |

### 2.2 `global.css` 最终拆分结果

- 当前 `frontend-admin/src/assets/styles/global.css` 为 9 行，仅保留顺序 `@import` 入口。
- 同目录新增 8 个分片文件，且均小于 1000 行：
- `global-app-redesign.css`：906 行
- `global-base.css`：541 行
- `global-list-dashboard.css`：413 行
- `global-list-layout.css`：515 行
- `global-list-table.css`：556 行
- `global-modal-form.css`：247 行
- `global-non-list-pages.css`：478 行
- `global-typography-modal.css`：369 行

## 三、阶段执行状态

### 3.1 已完成并通过复验/独立验收

- `M1 DashboardService 门面化和角色服务拆分`：已完成并通过复验。
- `M1b DashboardServiceTest 拆分`：已完成并通过复验。
- `M2 前端列表页统一拆分`：已完成并通过复验或独立验收。
- `M3 Dashboard/Cost 视图拆分`：已完成并通过复验或独立验收。
- `M4 P2 页面顺手拆`：已完成并纳入最终收口。
- `M5 global.css 分层`：已完成顺序保持式最小拆分，并通过独立验收 `PASS/非阻塞`。
- `M6 最终验收归档`：当前文档即为收口归档结果。

### 3.2 已完成收口项说明

- `cost/summary.vue`：已完成主线程只读核对与独立验收，结论 `PASS/非阻塞`。
- `cost-target/index.vue`：已完成最小拆分与独立验收，结论 `PASS/非阻塞`。
- `global.css`：已从“共享样式高风险待裁决项”转为“已完成并独立验收通过项”。

## 四、`global.css` 验收证据

- 文本等价证据：按 `@import` 顺序拼接 8 个分片后，与 `HEAD` 中拆分前的 `global.css` 完全一致，结论 `same=True`。
- 类型检查证据：`pnpm type-check` 通过。
- 构建证据：`pnpm build` 通过。
- 差异洁净证据：`git diff --check` 通过。
- 验收结论：`global.css` 顺序保持式最小拆分成立，当前结论为 `PASS/非阻塞`。

## 五、视觉结论与剩余风险

- 视觉结论：本次未执行 13 页浏览器截图级复验，但基于“按顺序拼接文本完全等价 + 构建通过 + 未改选择器/声明”可判定当前视觉无变化结论成立，且该项不构成阻塞。
- 剩余风险：仅保留“可选浏览器截图级视觉复验”作为非阻塞增强项，不再保留“缺证据前不做强拆”“阶段性豁免”“不能推进 `M5`”等旧口径。
- 风险等级：低。当前不存在阻塞本主线收口的代码拆分遗留项。

## 六、最终验收标准达成情况

1. 计划内 21 个候选超长代码文件均已完成拆分、收敛验收，或确认为非代码锁文件排除对象。
2. `global.css` 已不再承载全部样式体量，主入口仅保留 9 行顺序 `@import`。
3. 各分片文件均小于 1000 行，且未引入新框架、新依赖或样式体系迁移。
4. 本主线相关前端拆分项已覆盖独立验收所需的最小必要证据，其中 `global.css` 已具备文本等价、类型检查、构建和 diff 校验四类证据。
5. 当前剩余事项仅为提交集成和可选浏览器截图级视觉复验，不影响“通过/非阻塞”结论。

## 七、后续执行顺序

1. 维持本主线当前“通过/非阻塞”收口结论，不再把 `global.css` 作为待执行或待裁决项重开。
2. 后续如需增强证据，可补做 13 页浏览器截图级视觉复验，但该动作属于可选增强，不是本主线通过前置条件。
3. 工程层面仅剩提交集成，无需再扩展新的拆分范围。

## 八、模型分配经验

- 后端核心服务拆分与复杂复验升档是合理的。
- 前端页面拆分与计划归档使用 `gpt-5.4 / medium` 足够。
- `global.css` 收口事实一旦由文本等价、构建与差异检查共同锁定，就不应继续维持阶段性豁免口径。

## 九、收口判定

- 通过/不通过：通过。
- 阻塞/非阻塞：非阻塞。
- 依据：21 个候选超长代码文件已完成拆分或排除；`global.css` 已完成顺序保持式最小拆分，分片均小于 1000 行，并通过 `same=True`、`pnpm type-check`、`pnpm build`、`git diff --check` 验收。
- 剩余风险：未做 13 页浏览器截图级视觉复验，但该项仅作为非阻塞增强项记录。
- 最终结论：第24条主线已达到可收口状态，后续仅剩提交集成和可选视觉复验。

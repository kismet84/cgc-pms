# 第56条主线：全新项目业务数据 UI 录入任务计划书

**Goal:** 在独立本地数据库中，先建立8个驾驶舱角色和1个普通员工角色，每类角色通过真实表单创建3个单角色用户，共27个用户；再通过 CGC-PMS Clean-room V2 内置浏览器的真实表单入口，新建15条相互独立、跨域引用完整的工程业务链。除角色固定9条、用户固定27条外，每个纳入范围的业务创建表单最终各有15条本轮新记录；所有文本值不含“测试”或“示例”，所有业务日期随机分布在 `2026-01-01～2026-07-28` 且满足业务因果，不引用、修改或补写旧数据。

**Architecture:** 新建并独占本地数据库 `cgc_pms_demo_ui_20260728`，通过现有 Flyway 空库基线和受控管理员完成初始化；后端、Redis database 和 MinIO bucket 使用独立本地配置，不连接或复制 `cgc_pms`、`cgc_pms_demo`、`cgc_pms_demo_v2` 的业务数据。复用当前 V2 表单、服务端自动编号、现有权限、状态机、租户/项目隔离和业务校验，不增加业务种子脚本、批量写接口、前端自动填充代码或第二套数据模型。以 15 个新项目为 15 条纵向业务链，每条链只引用本轮浏览器提交后回显的新对象 ID；主数据、项目、合同、预算、进度、供应链、分包、资金、现场、质量和技术按依赖顺序串行创建。除 M0 空库初始化外，浏览器是唯一业务数据写入口，API、SQL和数据库工具只允许只读验真。

计划日期：2026-07-28

计划状态：`IN_PROGRESS / M0_M1_ROLE_USER_IMPLEMENTED / BUSINESS_DATA_NOT_STARTED`

计划基线：`master@0c1ad645f38f1f7c3ed9d47598720bf18fb1a91c`

执行键：`FRESH-BUSINESS-DATASET-UI-ENTRY`

## 1. 当前边界

### 1.1 已知事实

- 当前 backend、MySQL 和 frontend-v2 最近一次验真均为 `healthy`，正式执行前必须重新检查。
- 当前工作区已有其他任务未提交文件，本计划不得覆盖、清理或提交这些成果。
- 前序交互已在旧运行库通过 UI 创建“滨江科创中心一期建设工程”1条项目记录；该记录不迁移、不计数、不修改，正式实施时在独立新库通过 UI 重新创建业务链 `01`。
- 用户已授权先实施独立运行态、9类角色和27个用户；未授权继续创建其他主数据或业务单据。

### 1.2 “不要旧数据”的判定

1. 独立新库初始化完成时业务事实表必须为空；不删除、不修改、不补写旧库现存记录。
2. 新记录的所有外键必须来自本轮 `01～15` 新对象台账。
3. 下拉候选出现旧记录时，只允许选择本轮已记录的新名称与回显 ID；找不到则该步骤 fail-close。
4. 新数据通过项目名称、业务主题、日期和回显 ID 形成独立集合，不使用“测试”“示例”“demo”“sample”等隔离词污染业务字段。
5. 验收时同时证明“新记录数量正确”和“未引用旧 ID”，不能只看页面总数增长。

### 1.3 非目标

- 不开发批量导入、种子数据、浏览器脚本、写接口或数据库脚本。
- 不新建 MySQL 容器或复制旧库；复用本地 MySQL 实例中的独立数据库和最小权限账号。
- 不修改业务规则、权限、自动编号、状态机、页面结构和既有数据。
- 不处理上线、部署、生产环境、真实账号、真实客户或真实个人信息。
- 不把查询、导出、刷新和状态动作伪装成“创建表单”计数。

### 1.4 风险

| 风险 | 处置与退出条件 |
| --- | --- |
| 下拉框混入旧对象，导致新链引用旧 ID | 每次选择后记录回显 ID；只允许命中新对象台账，否则暂停该链 |
| 表单需上游状态但状态机不允许推进 | 先完成依赖链和页面回读；无法通过真实 UI 推进则登记为受阻，不绕过服务端 |
| 随机日期破坏业务因果 | 先生成并冻结日期账本，录入前校验顺序，提交后回读 |
| 自动编号冲突或未生成 | 以服务端回显为准；未生成、重复或允许人工覆盖时停止该表单并分类 |
| 15×多表单录入造成漏项、重复或跨链串号 | 使用链号、表单清单和新 ID 台账逐项勾验，每阶段完成后再进入下阶段 |
| 本地运行态或旧 Vite 进程导致页面与当前代码不一致 | M0 重新验真 backend、frontend-v2、代理与页面版本；不一致先刷新运行态 |
| 后端、Redis或MinIO仍指向旧运行资源 | M0 核对数据库名、Redis database 和 bucket；任一未隔离则禁止浏览器写入 |
| 回滚能力不足 | 只执行页面支持且可精确定位的删除/作废；无法安全回滚时停止后续写入 |

## 2. 数据集设计

### 2.1 十五条纵向业务链

| 链号 | 项目名称 | 项目类型 | 计划周期 |
| --- | --- | --- | --- |
| 01 | 滨江科创中心一期建设工程 | 施工总承包 | 2026-01-08～2026-07-24 |
| 02 | 城北综合物流园仓储工程 | 施工总承包 | 2026-01-12～2026-07-17 |
| 03 | 东湖人才公寓建设工程 | 施工总承包 | 2026-01-19～2026-07-28 |
| 04 | 南站商务区机电安装工程 | 专业分包 | 2026-02-02～2026-06-26 |
| 05 | 西城医院幕墙专业分包工程 | 专业分包 | 2026-02-09～2026-07-10 |
| 06 | 高新产业园主体结构劳务工程 | 劳务分包 | 2026-01-15～2026-06-12 |
| 07 | 临港冷链基地钢材采购项目 | 材料采购 | 2026-02-16～2026-05-29 |
| 08 | 新城学校装饰装修工程 | 专业分包 | 2026-03-02～2026-07-15 |
| 09 | 河东污水处理厂土建劳务工程 | 劳务分包 | 2026-01-26～2026-06-30 |
| 10 | 轨道车辆段商品混凝土采购项目 | 材料采购 | 2026-02-23～2026-05-22 |
| 11 | 机场保税仓钢结构专业分包工程 | 专业分包 | 2026-03-09～2026-07-24 |
| 12 | 云谷研发总部主体总承包工程 | 施工总承包 | 2026-01-05～2026-07-27 |
| 13 | 滨海会展中心改造工程 | 施工总承包 | 2026-03-16～2026-06-19 |
| 14 | 北岸安置房二期建设工程 | 施工总承包 | 2026-01-13～2026-07-21 |
| 15 | 智慧能源产业基地建设工程 | 施工总承包 | 2026-02-05～2026-07-27 |

项目地址、建设单位、监理单位、设计单位、合作方、人员、材料、合同主题和施工部位使用与项目地域、类型一致的合成业务名称，不使用真实个人敏感信息。

### 2.2 随机日期契约

1. 执行前一次性生成 15 条日期账本，采用固定随机种子，保证可重放但输出日期分散。
2. 所有用户可填写或服务端生成的业务日期总体分布在 `2026-01-01～2026-07-28`；不得早于2026年1月、晚于2026年7月或把同一表单15条全部写为执行当天。
3. 每条链必须满足：
   - 项目立项 ≤ 合同签订 ≤ 合同生效 ≤ 计划开工 < 计划完工；
   - 采购申请 < 招采截止 ≤ 采购订单 < 计划到货 ≤ 收货；
   - WBS计划开始 ≤ 日报日期 ≤ WBS计划完成；
   - 检查计划 ≤ 检查日期 ≤ 整改期限；
   - 计量期间结束 ≤ 结算日期 ≤ 付款申请日期；
   - 发票日期与付款、应收事实顺序符合对应业务规则。
4. 周末、月末、季度末和常规工作日混合分布；需要工作日约束的表单只选工作日。
5. 所有随机日期先写入执行账本再录表单，禁止边填边猜。

### 2.3 角色与用户矩阵

| 驾驶舱/身份 | 角色编码 | 驾驶舱权限 | 用户名 | 数量 |
| --- | --- | --- | --- | ---: |
| 项目经理 | `PROJECT_MANAGER` | `dashboard:project-manager:view` | `ui26.pm01～ui26.pm03` | 3 |
| 商务经理 | `COMMERCIAL_MANAGER` | `dashboard:business-manager:view` | `ui26.bm01～ui26.bm03` | 3 |
| 成本经理 | `COST_MANAGER` | `dashboard:cost-manager:view` | `ui26.cost01～ui26.cost03` | 3 |
| 采购经理 | `PURCHASE_MANAGER` | `dashboard:purchase-manager:view` | `ui26.pur01～ui26.pur03` | 3 |
| 生产经理 | `PRODUCTION_MANAGER` | `dashboard:production-manager:view` | `ui26.prod01～ui26.prod03` | 3 |
| 总工程师 | `CHIEF_ENGINEER` | `dashboard:chief-engineer:view` | `ui26.chief01～ui26.chief03` | 3 |
| 财务经理 | `FINANCE` | `dashboard:finance:view` | `ui26.fin01～ui26.fin03` | 3 |
| 管理层 | `MANAGEMENT_EXECUTIVE` | `dashboard:management:view` | `ui26.mgmt01～ui26.mgmt03` | 3 |
| 普通员工 | `COMMON_USER` | 无驾驶舱权限 | `ui26.staff01～ui26.staff03` | 3 |

独立空库基线已包含除 `MANAGEMENT_EXECUTIVE` 外的8个目标角色。M1复用并通过真实UI配置这些系统白名单角色，仅通过UI新增 `MANAGEMENT_EXECUTIVE`；基线角色不是旧业务数据，不迁移任何旧库角色绑定或旧账号。

1. 每个用户只绑定一个上述业务角色，不绑定 `ADMIN`、`SUPER_ADMIN` 或通配权限。
2. 角色权限按驾驶舱与实际业务职责最小赋权；正常审批人只增加 `workflow:approve`、`workflow:reject`，申请人按业务增加创建/提交及 `workflow:withdraw`、`workflow:resubmit`。
3. 首轮不授予 `workflow:transfer`、`workflow:add-sign`；仅在对应动作进入验收范围时追加。
4. 15个项目按3名同角色用户轮换责任；申请人与审批人必须是不同账号。
5. 工作流节点必须通过真实 UI 配置并回读实际 `approverId` 或项目角色解析结果；禁止仅凭角色名称假定路由成功。

## 3. 表单范围

### 3.1 每类目标的新建表单

| 阶段 | 表单/对象 | 每类目标 | 关键新依赖 |
| --- | --- | ---: | --- |
| M1 | 目标业务角色 | 9 | 复用并配置8个空库基线角色；新增管理层角色 |
| M1 | 本地业务用户 | 27 | 每角色3人；每人单角色 |
| M1 | 组织部门 | 15 | 新公司根节点 |
| M1 | 组织岗位 | 15 | 新部门 |
| M1 | 合作方 | 15 | 无旧外键 |
| M1 | 材料字典 | 15 | 新分类或表单允许的内置枚举 |
| M1 | 成本科目 | 15 | 本轮新根/子科目 |
| M2 | 项目 | 15 | 新业务责任信息 |
| M2 | 项目成员 | 15 | 新项目+新用户 |
| M2 | 项目仓库 | 15 | 新项目 |
| M3 | 合同 | 15 | 新项目+新合作方 |
| M3 | 合同清单 | 15组 | 新合同+新材料/科目 |
| M3 | 合同付款条款 | 15组 | 新合同 |
| M3 | 目标成本版本 | 15 | 新项目+新科目 |
| M3 | 项目预算 | 15 | 新项目+新科目 |
| M3 | 基线计划 | 15 | 新项目 |
| M3 | WBS任务 | 15组 | 新计划+新项目成员 |
| M4 | 采购申请 | 15 | 新项目+新材料 |
| M4 | 招采事件 | 15 | 新采购申请 |
| M4 | 采购订单 | 15 | 新项目+新供应商+新申请 |
| M4 | 收货验收 | 15 | 新订单+新仓库 |
| M4 | 领料申请 | 15 | 新仓库+新库存+新合同 |
| M5 | 分包任务 | 15 | 新项目+新分包合同/单位 |
| M5 | 分包计量 | 15 | 新任务+新合同 |
| M5 | 分包结算 | 15 | 新计量 |
| M5 | 付款申请 | 15 | 新项目+新合同+新合作方 |
| M5 | 费用单 | 15 | 新预算+新科目+新合作方 |
| M5 | 业主结算/应收 | 15 | 新项目+新业主合同 |
| M5 | 进项/销项发票 | 各15 | 新付款/应收事实 |
| M6 | 产值计量 | 15 | 新项目+新合同+新WBS |
| M6 | 现场日报 | 15 | 新项目+新计划 |
| M6 | 质量检查计划 | 15 | 新项目 |
| M6 | 质量检查记录 | 15 | 新检查计划 |
| M6 | 质量问题及整改 | 各15 | 新检查记录 |
| M6 | 技术方案 | 15 | 新项目 |
| M6 | 图纸/会审 | 各15 | 新技术方案 |
| M6 | RFI/回复 | 各15 | 新图纸/会审 |
| M6 | 技术交底 | 15 | 新方案+新项目成员 |
| M6 | 施工依据 | 15 | 新合法技术对象 |
| M6 | 现场签证变更 | 15 | 新项目+新合同+新现场事实 |
| M6 | 项目收尾对象 | 15 | 对应链达到合法收尾前置 |

“15组”表示每条业务链至少一组，组内明细数量依表单最小闭环确定；不得为凑数复制同一明细。

### 3.2 不计入“每个表单15条”

- 查询、筛选、导出、刷新、查看详情。
- 提交、审批、驳回、撤回、归档、反记账、关闭、重开等状态动作；它们仅作为下游准入前置执行。
- 菜单、权限项、流程定义和数据维护等控制面配置；角色仅按2.3矩阵创建9条，不适用15条规则。
- 密码、Secret、外部渠道账号、银行真实凭据、真实个人手机号/证件。
- 无独立业务记录的编辑弹窗，以及需要真实外部回执但本地无合法来源的表单。

若正式盘点发现新的 V2 业务创建表单，必须在 M0 写入冻结清单并按同一标准补足15条；不得口头遗漏。

## 4. 阶段与退出门

| 阶段 | 工作 | 退出门 |
| --- | --- | --- |
| M0 隔离与范围冻结 | 创建并迁移独立空库；配置独立后端、Redis database和MinIO bucket；浏览器只读盘点全部 V2 创建入口；冻结表单清单、必填字段、状态前置、日期账本和新ID账本格式 | 当前数据库名为`cgc_pms_demo_ui_20260728`；业务事实表为空；旧资源未连接；表单清单唯一；运行态健康 |
| M1 角色用户 | 通过 UI 配置9类目标角色、最小权限和27个单角色用户；逐账号登录回读身份 | 目标角色9；用户27；每角色3；超级管理员绑定0；全部回显ID已记录 |
| M2 新项目 | 在独立新库创建15项目、成员和仓库；链01也重新创建 | 三类各15；无旧用户/项目引用 |
| M3 商务基线 | 创建合同、清单、条款、目标成本、预算、基线计划和WBS | 金额守恒；日期合法；必要状态动作完成 |
| M4 供应链 | 采购申请→招采→订单→收货→库存→领料 | 数量守恒；收货不超订单；领料不超服务端库存 |
| M5 分包资金 | 分包任务→计量→结算；付款/费用/应收/发票 | 合同、预算、累计金额和发票口径通过服务端门禁 |
| M6 履约现场 | 产值、日报、质量、技术、签证和合法收尾 | 每个下游对象均可追溯到同链上游 |
| M7 验收收口 | UI逐类回读；API/DB只读对账；生成正式验收报告 | 每类15；禁词0；旧ID引用0；悬空0 |

每一阶段只允许在上一阶段通过后继续。首个表单先执行1条金丝雀，回读正确后再补齐剩余14条。

## 5. 浏览器实施规则

1. 只使用用户指定的内置浏览器和真实可见表单入口。
2. 不通过 `fetch`、控制台脚本、后端接口、SQL、migration或seed脚本创建数据。
3. 浏览器自动化只做与人工等价的点击、选择、填入和提交；每次提交后读取成功提示、列表或详情回显。
4. 自动编号字段保持只读，由服务端生成；外部发票号、报价号等业务字段按新日期账本生成且保持唯一。
5. 每次下拉选择必须用可见名称确认新对象，不手填数据库 ID。
6. 表单首次失败先分类；修复环境或输入后仅复验该条，不盲目重提。
7. 批次中断时记录最后成功的链号、表单、回显ID和下一步；恢复前先回读，防止重复创建。

## 6. 验收矩阵

| 验收项 | 通过标准 |
| --- | --- |
| 新数据隔离 | 所有新外键均属于本轮ID账本；旧ID引用数0 |
| 表单数量 | 9个目标角色全部有效、用户恰有27条且每角色3人；冻结清单内其他业务创建表单各有15条本轮记录 |
| 文本质量 | 新记录字段中“测试/示例/demo/sample”命中0 |
| 日期质量 | 所有业务日期位于`2026-01-01～2026-07-28`；每类至少覆盖5个不同自然日；不全部等于执行日；全链因果校验通过 |
| 工程真实性 | 名称、地点、单位、部位、金额、数量、税率和状态组合符合对应工程场景 |
| 金额一致性 | 合同、预算、目标成本、计量、结算、付款和发票累计关系通过服务端校验 |
| 库存一致性 | 申请、订单、收货、入库、领用数量以服务端事实为准且可回读 |
| 权限与租户 | 所有对象属于当前本地租户；27个用户均为单业务角色；`ADMIN`/`SUPER_ADMIN`绑定0；跨项目/跨租户引用0 |
| 审批身份 | 申请人与审批人账号不同；任务实际指派给当前审批用户；至少保留提交人、审批人和审计日志三方一致证据 |
| UI证据 | 每阶段至少保留列表计数、代表性详情和最终落点；浏览器控制台无新增错误 |
| 工作区 | 不覆盖原有未提交文件；不产生未授权代码、Git或生产变更 |

最终报告写入 `docs/quality/第56条主线-全新项目业务数据UI录入验收报告-2026-07-28.md`。

## 7. 失败分类、暂停与恢复

- Docker、端口、数据库、前端代理、数据未加载：`environment_prerequisite`，恢复后复验。
- 浏览器参数、定位器或交互调用错误：`tool_invocation`，刷新页面状态后修正一次。
- 表单校验、权限、状态、金额、库存或日期契约失败：`quality_or_security`，不得绕过。
- 入口或证据未召回：`retrieval_gap`，使用允许的只读代码/图谱查证。
- 缺少合法上游对象或无法确定业务含义：`unknown`，暂停该表单并继续不依赖的链；不得引用旧数据填空。

## 8. 回滚

1. M1～M3金丝雀只保留草稿；确认下游可用后再执行提交/审批动作。
2. 草稿记录优先通过对应 UI 删除；删除前核对精确名称、链号和依赖。
3. 已进入状态机的记录只使用业务支持的撤回、作废、冲销或关闭动作，不直接删库。
4. 若某类表单无安全回滚入口，在首条金丝雀提交前暂停并记录为阻塞。
5. 禁止删除旧数据、删除命名卷、重置旧数据库或直接清理旧表；独立新库需废弃时另行确认精确目标和恢复边界。

## 9. 文件与授权范围

计划阶段允许修改：

- `docs/plans/第56条主线-全新项目业务数据UI录入任务计划书-2026-07-28.md`
- `docs/plans/README.md`

计划阶段禁止：

- 继续创建、编辑、删除或审批业务数据。
- 修改业务代码、测试、配置、migration或运行环境。
- commit、push、PR、合并、发布或生产操作。

当前授权只覆盖本地独立数据库及配套运行资源初始化、9类角色、27个用户的浏览器录入和必要只读验真；其他主数据和业务单据仍需用户再次明确授权。授权不包含 Git 或生产操作。

## 10. 零悬空收口

- 每个失败表单必须归入：本轮修复并复验、正式阻塞并写明前置、或证据证明不属于业务创建表单而关闭。
- 新增后续项、关闭后续项和净变化在验收报告中统计。
- 9个目标角色未全部有效、用户不是27条、任一角色不是3用户、其他纳入表单少于15条、存在旧ID引用、禁词命中、日期集中当天或无载体失败项，最终结论均为“不通过”。

# 执行交接清单

## [SKIPPED] InvoiceValidationTest 3个失败 (Phase 4)
- **时间**：2026-06-24T12:50:00+08:00
- **Phase**：Phase 4 — 全量回归与修复
- **失败测试**：
  1. `shouldIgnoreTenantIdFromRequestBody` — 期望 200，返回 400（缺 payRecordId）
  2. `shouldFilterByInvoiceNoPartialMatch` — 同上
  3. `shouldFilterByVerifyStatus` — 同上
- **根因**：3 个测试的 JSON body 缺少 `payRecordId` 字段。种子 PayRecord 已通过 `@BeforeEach` 插入（90001L），但 JSON 未引用它
- **尝试修复**：已添加 `@BeforeEach` 种子数据插入，补齐 `SEED_PAY_RECORD_ID` 常量和 `PayRecordMapper` 注入；3 个 JSON body 已改为 `.formatted(SEED_PAY_RECORD_ID)` 模板
- **建议人工介入**：执行 `./mvnw test -Dtest=InvoiceValidationTest -pl .` 验证修复是否生效

## [IMPLICIT-DEP] 测试修复依赖种子数据
- **任务组**: InvoiceControllerTest / InvoiceValidationTest 与 H2 seed data
- **依赖关系**: 控制器测试依赖 `@BeforeAll initData()` 中的 `payRecordService.writeback()` 生成种子付款记录，但这在 H2 local profile 中依赖失败
- **处理**: InvoiceControllerTest 已通过（使用 `writeback` 生成），InvoiceValidationTest 需要额外修复

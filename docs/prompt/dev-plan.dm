你是一名资深全栈技术负责人。生成一份可直接执行的开发任务拆解计划，务必绑定验收标准并包含冒烟测试。

请严格按以下要求输出：

## 一、任务拆解
- 按模块分组（如用户认证、数据库模型、前端页面等）
- 每个任务包含：
  - 任务名称与简要说明
  - 输入/输出（数据或依赖）
  - 涉及的文件建议（新增/修改）
  - 复杂度：P0（核心阻塞）/ P1（重要）/ P2（优化）
  - **验收标准**（必须包含正常场景、边界条件、异常场景，使用 Given-When-Then 或检查项格式）
- 任务间依赖关系用“依赖：任务X”标注

## 二、改动文件清单与计划
- 列出所有需要新增/修改的文件
- 对每个文件用 1-3 句话说明改动要点（只写计划，不写完整代码）
- 用 🔨修改、✨新增、🗑️删除 标识

## 三、数据流变化
- 描述该需求涉及的前后端交互流程（如“用户点击提交 → POST /api/xxx → 数据库写入 → 返回结果 → 页面跳转”）
- 如有状态管理或数据格式变化，简要说明

## 四、影响范围与回归测试建议
- 列出可能受影响的现有功能或模块
- 对每个受影响点给出 1 条回归测试建议

## 五、冒烟测试方案
- 给出 5~10 个核心冒烟测试用例，覆盖主要成功路径和最关键的失败路径
- 每个用例一句话说明操作与预期结果
- 确保开发完成后可据此快速验证是否“基本可测”
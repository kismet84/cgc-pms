# Iteration Report - 2026-07-11

## 启动迭代-3（新轮次）

### ISSUE-008-035 项目成员项目级访问范围治理

- 收口：通过、非阻塞；计入本轮第 `1/3` 条。
- 修复：项目成员查询与写操作统一复用 `ProjectAccessChecker`，保留既有同租户项目存在性校验。
- 验收：TDD RED 命中缺失构造依赖；GREEN 定向测试 11/11，`git diff --check` 通过。
- 审查：未新增权限码、数据范围枚举、migration 或第二套鉴权抽象。
- 后续：串行进入 `ISSUE-008-036`。

---

## 第34条主线资金日记账与付款附件闭环

目标：完成计划 M1–M7 的本地实现、独立审查、全量门禁、真实角色页面验收与正式归档。

### 任务状态

- `ISSUE-034-001` 是唯一实际进入 Ready 的条目。
- 计划内 `ISSUE-034-002` 至 `ISSUE-034-006` 未单独进入 Ready；本轮按第34主线统一验收口径完成 M1–M7，不补造逐条 Ready 流转历史。
- Ready 已移除 034-001，Done 新增 `MAINLINE-034` 统一完成记录，current-focus 已转为“无合格 Ready，等待下一真实焦点”。

### 实施与修复摘要

1. 新增资金账户、日记账、只追加变更日志及 MySQL/H2 `V136`，复用既有租户、软删、权限与审计模式。
2. 完成手工收支、付款成功自动待归档、来源幂等、事务回滚、动态余额与导出安全。
3. 完成附件硬门禁、归档不可变、红冲净额、超级管理员受控撤销/重新归档及完整快照。
4. 完成 24 小时 FINANCE 预警、合格接收人、数据库锁域去重、归档自动关闭与页面跳转。
5. 完成单页资金日记账、账户管理、付款关联、权限路由、银行账号掩码与 440px 布局。
6. 独立审查发现并闭环文件删除事务顺序、多实例预警去重、付款成功后关联查询误报、期初日期边界、详情账户信息和移动布局问题。

### 验收证据

- 后端全量：`1678` tests，`0` failures，`0` errors，`1` skipped。
- 前端目标：6 个文件，`78/78` tests 通过。
- `pnpm type-check`、`pnpm build`：通过。
- 独立审查：Critical=`0`，Important=`0`。
- 运行态：backend、frontend、dev-login health gate 通过；权威挂载恢复后稳定等待 180 秒复验为 healthy。
- 浏览器：FINANCE、query-only、no-permission 三类真实角色权限边界及 440px 实际视口通过；临时角色、用户和绑定零残留。
- `git diff --check`：文档尾随空格修复后通过。

### 环境前置恢复偏差

运行态验收为恢复权威本地挂载，除计划内 backend/frontend 刷新外额外重建了本地 MySQL、Redis、MinIO。未执行测试数据删除或重置，未连接生产；稳定等待 180 秒后服务恢复 healthy。该事项分类为环境前置恢复，不构成业务代码失败。

### 收口结论

结论：通过

阻塞：无

是否自动合并：否

是否提交：否，由主线程后续选择性 local commit

是否推送：否，`autoPush=false`

是否上线：否，本轮未执行生产发布

非阻塞剩余风险：

1. afterCommit 删除 MinIO 失败可能留下不可访问孤儿对象，纳入 current-focus 运维观察。
2. 跨租户浏览器样本缺失，但后端自动化覆盖租户隔离；不影响本地验收。
3. 生产账户期初复核与超级管理员真实受控修正演练属于上线前人工清单。

正式报告：`docs/quality/mainline-34-cash-journal-payment-attachment-closeout-acceptance.md`。

### 模型分配经验

- 金额、并发、权限、数据一致性和正式裁决需要高档模型；多实例预警问题出现后升档合理。
- 固定运行态命令、台账和报告整理可降档，不应与安全审查统一配置。
- 前端正式裁决需要真实 mount/浏览器证据，纯源码正则不足。

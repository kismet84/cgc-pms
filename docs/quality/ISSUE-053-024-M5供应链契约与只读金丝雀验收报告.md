# ISSUE-053-024 M5供应链契约与只读金丝雀验收报告

## 1. 裁决

- 结论：通过。
- 范围：M5共享API/DTO/查询权限/十进制字符串契约、V2只读服务、仓库/库存/采购三条GET零写金丝雀。
- 基线：`master`，`d5b93242fa64b429f4658cb48d5c4090e1e59578`，含工作区既有未提交成果。
- 路由：未迁移，台账保持`LEGACY_ONLY=50 / V2_ACCEPTED=37 / V2_SOURCE_AVAILABLE=0`。
- Git/环境：未commit、push、PR、切正式入口、退役Legacy、连接生产或执行生产写入。

## 2. 实施结果

- 新增`packages/frontend-contracts/src/supply-chain.ts`，冻结8项查询权限、8组API入口、库存/采购/验收十进制字段和仓库、库存台账、采购订单、验收只读DTO。
- 新增`frontend-admin-v2/src/services/supply-chain.ts`，只复用既有`apiRequest`，提供仓库、库存台账、采购订单、验收四个GET薄服务；query和ID统一编码，AbortSignal原样透传。
- 新增前端金丝雀测试，覆盖成功、空态、403/404/500、空ID、十进制字符串、GET零body、AbortSignal、Clean-room边界和手工出入库禁用契约。
- 新增后端`SupplyChainReadOnlyCanaryTest`：真实MockMvc执行仓库、库存台账、采购订单GET，前后核对`mat_warehouse`、`mat_stock`、`mat_stock_txn`、`mat_purchase_order`数量与最新时间不变。

## 3. 权限与失败关闭

- Controller与B215均以`inventory:warehouse:list`作为仓库查询权威权限。
- Legacy路由/台账中的`inventory:warehouse:query`是存量前端漂移：金丝雀证明该权限访问仓库GET为403，`inventory:warehouse:list`为200。
- 024不修改Legacy或路由台账；后续仓库V2迁移必须使用`inventory:warehouse:list`，迁移验收时再更新V2导航与台账状态。
- `/inventory/stock/in`和`/inventory/stock/out`继续返回`MANUAL_STOCK_MOVEMENT_DISABLED`；共享API不暴露两端点。

## 4. 验证证据

| 验证 | 结果 |
| --- | --- |
| Ready lint | 通过 |
| 后端组合专项 | `55`项，失败`0`、错误`0`、跳过`0`；Maven `BUILD SUCCESS` |
| 查询零写金丝雀 | `3`项通过；三条GET及四张业务表前后快照一致 |
| V2单测 | `37`文件、`274`项通过 |
| 契约类型 / V2类型 | 通过 |
| ESLint | 通过，0错误、0警告 |
| Clean-room边界 | 143个V2文件、14个契约文件通过 |
| 路由台账检查 | 87路由、73视图、65唯一视图；状态仍为`50/37/0` |
| V2构建 | 193模块，成功 |
| `git diff --check` | 通过 |

## 5. 发现项收口

- 本轮修复并复验：仓库查询权限权威契约固定为`inventory:warehouse:list`，并增加旧权限403/新权限200自动化证据。
- 超出范围并正式承接：无新增项；Legacy权限漂移由既有M5仓库迁移`ISSUE-053-027`在路由切换时处理，不创建重复Backlog。
- 证据不足或无明确价值而关闭：无需新增后端业务修改、migration、页面、状态库、请求层或通用抽象。
- 新增后续项：0；关闭后续项：0；后续项净变化：0；悬空项：0。

## 6. 回滚

回退供应链共享契约、V2薄服务、前后端金丝雀测试及本报告/治理回写即可。无数据库、业务数据、路由状态或生产影响。

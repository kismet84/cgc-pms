# ISSUE-040-043 间接费规则受控删除入口与执行事实保护验收报告

## 验收结论

通过，非阻塞。间接费规则删除入口、`overhead:delete`/管理员权限、认证租户隐藏、执行事实保护、二次确认和取消不写入均具备证据；未连接生产、未修改 dev 业务数据、未 push。

## 实施结果

- Service 删除前读取认证租户目标，并以 tenantId+ruleId 统计 `overhead_allocation_run`；已有任一执行事实返回 `RULE_ALREADY_EXECUTED`，无引用时沿用软删除。
- 前端 API 只发送路径 id 和 DELETE 方法，无 body、params 或 tenantId。
- 规则表格增加权限控制的删除动作；确认文案包含科目 ID、分摊依据、执行事实不可删除及历史分摊不清理，失败不刷新/移除行。

## 验收证据

- 后端专项27项通过：覆盖401、403、显式权限、管理员、跨租户隐藏、无引用软删除、同租户存在 run 拒绝删除并保留规则。
- 前端专项20项通过：覆盖 DELETE 精确请求、权限入口、确认文案、取消路径、成功刷新和失败保留；类型检查与目标 ESLint 通过。
- 运行态：backend、frontend、dev-login 连续19个样本、180秒均正常，首尾为 200/200/302。
- 浏览器：dev 规则列表为空且 Ready 禁止造数；真实页面安全显示“暂无数据”并关闭，Docker 后端目标 DELETE 日志由0保持0，控制台 error=0、warn=0。确认弹窗与取消不 DELETE 由前端专项精确覆盖，属于环境数据前置下的最小等价验证。
- Ready lint、`git diff --check`、JSON 解析与允许/禁止路径核对通过。

## 风险与回滚

- 剩余风险：浏览器未在自然规则行点击删除，原因是 dev 数据为空且禁止为验收造业务数据；自动化、真实空态和 DELETE=0 已覆盖裁决必需边界，后续自然数据可复跑且不新增治理项。
- 最小回滚：回退 Service 引用检查、前端 API/动作、专项测试及治理回写；不需要数据库迁移或数据恢复。

## 问题收口

本轮新增后续项：0

本轮关闭后续项：1

本轮后续项净变化：-1

关闭唯一问题：`A-01-OVERHEAD-DELETE`。A-01 的明确需补入口叶子已清空，剩余11项均已有 `NEEDS_CONFIRMATION` 唯一承接。

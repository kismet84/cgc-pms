# 阶段 2：架构审计

## 结论

整体为前后端分离的模块化单体。安全、租户、审计、文件、工作流、财务和交付域边界可追踪；Codemap 可回答主要调用、影响和测试关系。未发现循环依赖导致的当前功能阻断。

## 已确认优势

- Web/API、认证、领域服务、持久化和交付配置层次清楚。
- 关键资金、工作流和文件链路有专用服务、事务及测试，不依赖前端校验维护一致性。
- 代码地图锁定当前脏输入快照，`node scripts/codemap/generate-codemap.mjs --verify` 退出 0。

## 整改复核（2026-08-20）

`ARCH-001` 已关闭，不再作为当前缺陷。第94条全仓职责复核已按调用、事务、锁、补偿与测试边界完成拆分：`AppShell`、`DocumentCanvas`、`BusinessObjectAuthorizer`、成本科目服务等高密度职责已抽离；付款申请、文件补偿与带锁编排因缺少安全切缝而保留门面。当前超长文件清单只证明体量，不能证明多职责根因；机械按行数拆分会扩大事务和回归风险。

后续仅在出现高频冲突、无法隔离测试或明确职责混杂证据时重新登记，不以行数阈值制造任务。依据：`docs/quality/2026-08-13-issue-094-全仓代码职责拆分与重复实现收敛.md`。

# ISSUE-040-027 系统角色删除管理员入口与安全边界验收报告

## 结论

- 最终结论：通过，但有1项已登记的非阻塞后续项。
- 阻塞：无。
- 复核：主线程在独立审查阶段对实现差异哈希 `06566e5890d1d5b7341f9295431c7fb010aa53fe09655ea0dc777e24b16f217d` 给出 `PASS`，findings=无；本轮未派子智能体，不声称存在独立 Reviewer 进程。
- 上线边界：仅在本地 dev/test 验收并本地提交；不连接生产数据库、不发布生产、不 push。

## 正式交付物

- 前端系统 API 新增类型化 `deleteRole(roleId)`，精确调用无 body、无 params 的 `DELETE /system/roles/{id}`。
- 既有 admin-only 角色页新增 ADMIN/SUPER_ADMIN 可见的“删除”危险操作；SYSTEM、ADMIN、SUPER_ADMIN 行不提供删除项，确认框展示角色名称与不可逆提示。
- 后端删除路径新增 SYSTEM、保留编码和 roleLevel<2 保护，用户绑定检查先于任何关系删除；合法未绑定自定义角色才在同一事务内删除角色菜单与目标角色。
- 失败路径保留列表并展示后端错误，成功路径刷新列表；既有角色列表、新建、详情和菜单授权行为保持不变。

## 自动化验收证据

- Ready lint：通过，Ready 内容哈希 `73cb8980fed8e938a54b275cafe839aea074a34d8521eaa52bad013ce2c7ab63`。
- 后端 `SysRoleControllerTest,SysRoleServiceTest`：48项通过，0失败、0错误、0跳过。
- 前端 API/页面专项：2个文件、38项通过。
- `pnpm type-check`、目标 ESLint、`git diff --check`：通过；ESLint 首次仅有10条格式化警告，本轮定向修复并复验为0告警。
- Controller 覆盖显式 `system:role:delete` 成功、无权限403、未登录401；既有 Service 用例继续覆盖跨租户 `ROLE_NOT_FOUND`。
- Service 新增证据覆盖 SYSTEM、ADMIN、roleLevel<2、用户绑定拒绝及全部关系无副作用；合法删除与事务回滚由同一专项回归覆盖。
- 前端专项覆盖 ADMIN/SUPER_ADMIN 可见、普通用户即使持删除权限仍不可见、确认前不调用、成功刷新、失败不刷新且不伪报成功。

## 运行态验收

- `http://localhost:8080/api/actuator/health`、`http://localhost:5173/` 和 dev-login 均返回200，dev-login 最终进入 `/dashboard`。
- 本地 dev 容器切换到当前 worktree 构建产物后，连续180秒的12次采样均为前后端健康。
- 使用 SUPER_ADMIN 会话进入 `/system/roles`，创建唯一角色 `AUTOPILOT_DELETE_20260715_0043`；确认前无删除请求，确认后提示“角色删除成功”并回读0条，测试角色无残留。
- SUPER_ADMIN 系统角色操作菜单仅有查看详情和编辑权限，删除项数量为0；浏览器控制台无 error/warn。
- CSS宽度1036px（命中 `max-width:1200px`）时，`.lg-grid` 从 flex 切为 block，但 `.lg-table-wrap` 实测高度为0，角色行被工具栏覆盖且操作按钮不可交互；宽屏桌面视口下表格高度649px，删除闭环正常。该既有响应式缺陷不阻断本 Ready 的宽屏管理员入口，已作为 `UI-ROLE-RESPONSIVE-TABLE-ZERO-HEIGHT` 正式登记。

## 权限与数据一致性复核

- 前端 admin-only 可见性没有替代后端授权：后端继续允许 ADMIN/SUPER_ADMIN 或显式 `system:role:delete`，无权限与未登录分别返回403/401。
- 删除前先核对当前租户目标，再核对系统/保留/高等级角色，最后检查 `sys_user_role`；任何拒绝均发生在角色菜单删除之前。
- 用户绑定存在时返回 `ROLE_IN_USE`，角色、用户关系和菜单关系均保持；目标不存在或跨租户统一 `ROLE_NOT_FOUND`。
- 成功路径要求角色删除计数为1，否则抛出 `ROLE_DELETE_FAILED` 并由事务回滚已执行的菜单关系删除。

## 发现项与后续统计

- 本轮修复并复验：角色删除管理员入口、破坏性确认、后端系统角色/用户绑定/原子性保护及对应测试。
- 超出当前范围并正式承接：`UI-ROLE-RESPONSIVE-TABLE-ZERO-HEIGHT`，P1、非阻塞；其修复可能影响全部 `lg-list-page`，本轮不扩大到全局样式。
- 证据不足或无明确价值而关闭：0。
- 新增后续项：1。
- 关闭后续项：1（`A-01-ROLE-DELETE`）。
- 后续项净变化：0。
- 新项已写入 `docs/backlog/current-issues.json`，包含复现证据、优先级、暂不处理原因和验收标准；未产生悬空问题，产品候选排序不变。

## 回滚

- 回退实施提交与收口提交即可移除前端删除 API、页面交互、Service 保护、测试与治理回写。
- 无 schema、migration 或生产数据变更；唯一浏览器测试角色已在本地 dev 库删除，无需数据回滚。

<!-- AUTOPILOT-FACTS:START -->
## AutoPilot 自动事实

- Issue：ISSUE-040-027
- Ready 哈希：73cb8980fed8e938a54b275cafe839aea074a34d8521eaa52bad013ce2c7ab63
- 实施提交：157ec52f7ac847ce7d53277b85ea442fd57a1e08
- 验证差异哈希：06566e5890d1d5b7341f9295431c7fb010aa53fe09655ea0dc777e24b16f217d
- Reviewer：required=True; decision=PASS（主线程独立审查阶段，无独立 Reviewer 进程）
- 后续项：added=1; closed=1; net=0
- 控制面指纹：ce13ccd0e4254c50f48304dfcae4518d81eaa7b7a26daaf932f0f3fb6ce8e131
<!-- AUTOPILOT-FACTS:END -->

# ISSUE-040-024 间接费执行分摊用户入口与金额安全边界验收报告

## 结论

- 最终结论：通过；独立 Reviewer 对绑定 diff `7a574f2d189e812f5f1885a2b2f6f14af5245cb45688a4ca9cf2596e7c580a2a` 给出 `PASS`，发现项 0。
- 阻塞：无。
- 上线边界：本报告仅证明本地 dev/test 实现与验收通过，不代表已发布生产；未连接生产库、未发布、未 push。
- 剩余风险：无悬空风险。生产部署时只允许由正式发布流程应用 V149；migration 回滚不得删除既有执行事实或成本。

## 交付范围

- 前端在既有成本台账增加按权限显示的月度执行入口、月末转换、影响提示、二次确认、成功/幂等反馈和失败保留。
- 后端以认证租户执行，拒绝非法日期、非月末、当前月和未来月份，只允许执行已完整结束月份；客户端 tenantId 不参与租户选择。
- 新增 `overhead_allocation_run` 实体、Mapper、执行结果与 V149 MySQL/H2 migration，以租户+规则+月份唯一键阻断串行和并发重复。
- 定时入口通过 overhead 专用 Mapper 的显式 `@InterceptorIgnore(tenantLine = "true")` 只读发现活跃租户，再逐租户绑定 `UserContext`；规则、项目、成本和执行事实查询写入仍受显式租户条件约束。
- 成本来源排除 `OVERHEAD_ALLOCATION`，保留 EQUAL、DIRECT_LABOR、CONTRACT_AMOUNT 语义，按分归集尾差；执行事实、成本写入和成本汇总刷新位于同一事务。
- 未实现规则列表/新建/修改/删除入口，未新增路由、菜单、角色授权、总账、结账、冲销、批量补算或历史重算。

## 验收证据

### 权限、租户与审计

- `OverheadAllocationControllerTest` 证明未登录 401、仅 `cost:ledger:query` 403、显式 `overhead:execute` 与 ADMIN/SUPER_ADMIN 成功；请求 tenantId 不覆盖认证租户。
- 服务测试证明规则、活跃项目、来源成本、执行事实和生成成本均受租户过滤；不同租户可独立执行且不会错误去重。
- `@AuditedOperation` 记录租户、期间、执行用户与成功标志；非法期间记录失败，不产生伪成功审计。
- 前端专项证明按钮仅对管理员或同时具备 `cost:ledger:query`、`overhead:execute` 的用户可见，API 不发送 tenantId；失败保留确认态，成功刷新成本台账。

### 金额、幂等、并发与回滚

- 两个活跃项目及多条规则覆盖可整除/不可整除金额、零金额和三种既有分摊依据；每条规则的分摊合计按分等于来源金额，来源明确排除既有分摊。
- 连续重复执行只保留一条执行事实和一组成本；数据库唯一键直接拒绝第二条相同租户/规则/月末事实。
- 当前月月末即使格式合法也会在创建执行事实前被拒绝，避免月中提前占用幂等键；随后执行已完整结束月份仍可正常生成成本。
- 专项补强从 `scheduledMonthlyAllocation` 公共入口证明空租户上下文仍能发现非 0 租户，其他租户不产生 run 或成本；不同月份、规则、项目和租户不会误去重，手工与定时并发复用同一门禁，只产生一组有效成本。
- 注入任一成本写入失败或成本汇总刷新失败时，执行事实和生成成本全部回滚。

### Migration

- `MigrationIntegrityTest`、`MigrationVersionUniquenessTest` 与 H2 Spring 测试启动证明 V149 文件完整、版本唯一并可在 H2 实际应用；未修改 V1～V148。
- 使用本机 `127.0.0.1:3307` dev MySQL 注入测试 profile 所需连接与测试 JWT secret 后，`FlywayMySqlSmokeTest` 1 项通过、0 跳过，证明 MySQL Flyway 上下文可实际启动并读取当前版本。
- V149 只新增执行事实表及其幂等唯一键，不修改既有 `cost_item` 通用来源唯一键。

### 浏览器

- health gate：8080 actuator、5173 首页均为 200，dev-login 最终进入目标页面；重建命令因本机会话缺少 compose secret/Jasypt 插值而归类为环境配置问题，复用既有 dev 容器重启并稳定等待 180 秒后复验通过。
- dev-login 以 SUPER_ADMIN 进入 `/cost/ledger`，入口可见；弹窗默认选择上月，显示 `2026-06-30`、影响说明和不可重复提示，必须二次确认。
- SUPER_ADMIN 首次执行生成 1 条成本、金额 `1.01` 并刷新台账；同一规则/月末再次执行显示“已执行，无需重复生成成本”，数据库仍只有 1 条成功 run 和 1 条分摊成本。
- 真实浏览器以临时普通用户进入 `/cost/ledger`；该角色具备成本台账及页面附属只读权限但不具备 `overhead:execute`，执行按钮 DOM 数量为 0，后端控制器专项同时证明直接调用返回 403。
- 用于真实浏览器回读的唯一规则、来源成本、run、生成成本、临时角色关系及本会话审计均在 dev 本机库精确清理；规则/run/测试成本/临时用户/临时角色计数均读回 0，受影响成本汇总已刷新并恢复原值。

## 命令结果

- H2 后端门禁将业务/控制器测试与 migration 测试隔离执行，均通过；MySQL smoke 使用 `127.0.0.1:3307` dev MySQL 单独执行为 1 项通过、0 跳过，并从 `flyway_schema_history` 读回 V149 `success=1` 与四列幂等唯一键。一次将 MySQL 数据源环境变量与 H2 profile 混合的组合命令导致驱动不匹配，归类为命令隔离配置问题，拆分后稳定复验通过。
- 补强后的控制器与服务专项合计 17 项通过、0 失败、0 错误、0 跳过。
- 前端专项：1 个文件、12 项通过；`pnpm type-check` 通过；限定 ESLint 通过。
- `pwsh -NoProfile -File scripts/check-sql-safety.ps1`：通过。
- `git diff --check`：通过。
- 浏览器验收数据仅在 dev、`127.0.0.1` 且 `.codex-autopilot/ALLOW_TEST_DATA_RESET` marker 存在时写入；验收后按固定 ID 精确清理并完成数据库零残留读回。

## 工具交叉核验

- CodeGraph 指向主仓库而非当前 worktree，无法覆盖 repair diff，归类为索引范围/召回不足；未据此作出存在性或安全裁决。
- `codebase-memory-mcp` 用于后端路由、服务、测试与跨层依赖补查；关键结论最终均以当前分支源码、专项测试、数据库唯一键和浏览器可见状态核验。

## 发现项与后续统计

- 本轮修复并复验：修复空租户定时线程被租户插件收窄为 `tenant_id=0` 的发现缺陷；修复当前月月末可在月中提前创建执行事实并占用幂等键的边界缺陷；补齐公共定时入口发现非 0 租户、其他租户隔离、不同月份/租户不误去重、定时/手工并发同门禁、MySQL V149 实际启动及真实浏览器权限/幂等证据。
- 超出范围并正式承接：0。
- 证据不足或无明确价值而关闭：0。
- 新增后续项：0。
- 关闭后续项：1（`A-01-OVERHEAD-EXECUTE`）。
- 后续项净变化：-1。
- `docs/backlog/blocked-issues.md` 无新增；迭代优先级未改变，故不修改 `evolution-decision.md`。

## 回滚

- 代码回滚：回退本 Issue 的成本台账入口、API 适配、执行事实代码、测试和 V149 文件。
- 已应用 migration：只停用新入口并保留执行事实及成本，不删除或改写历史数据；由正式数据库变更流程决定后续处置。
- 本地验收数据已清理，不存在需要额外回滚的临时规则、run 或成本。

<!-- AUTOPILOT-TASK-SCORE:BEGIN key=dc04a9efc209fb5c221ada982d5b0ff520c29ead9c5e9213e681a2ea017b05bd -->
## AutoPilot 任务评分

```json
{
  "schemaVersion": 2,
  "key": "dc04a9efc209fb5c221ada982d5b0ff520c29ead9c5e9213e681a2ea017b05bd",
  "issueId": "ISSUE-040-024",
  "implementationCommit": "cd725ca080d6691d18128ab23e0b6b301496e871",
  "scoringVersion": "autopilot-task-score/v2",
  "scoredAt": "2026-07-14T13:56:23.6440671+08:00",
  "total": 70,
  "dimensions": {
    "deliveryCorrectness": {
      "score": 35,
      "max": 35,
      "evidence": [
        "docs/quality/ISSUE-040-024-间接费执行分摊用户入口与金额安全边界验收报告.md"
      ]
    },
    "zeroDanglingIssues": {
      "score": 25,
      "max": 25,
      "evidence": [
        "docs/quality/ISSUE-040-024-间接费执行分摊用户入口与金额安全边界验收报告.md"
      ]
    },
    "firstPassAcceptance": {
      "score": 0,
      "max": 20,
      "evidence": [
        "docs/quality/ISSUE-040-024-间接费执行分摊用户入口与金额安全边界验收报告.md"
      ]
    },
    "taskExecutionEfficiency": {
      "score": 0,
      "max": 10,
      "evidence": [
        "docs/quality/ISSUE-040-024-间接费执行分摊用户入口与金额安全边界验收报告.md"
      ]
    },
    "stockIssueReduction": {
      "score": 10,
      "max": 10,
      "evidence": [
        "docs/quality/ISSUE-040-024-间接费执行分摊用户入口与金额安全边界验收报告.md"
      ]
    }
  },
  "hardGatesPassed": true,
  "followupNetChange": -1,
  "executionTiming": {
    "wallClockSeconds": 12277,
    "businessPhaseSeconds": 4324,
    "controlPlaneSeconds": 7953,
    "semanticProgressAt": "07/14/2026 11:41:21",
    "livenessSignalsExcluded": true
  },
  "sourceRefs": [
    "docs/quality/ISSUE-040-024-间接费执行分摊用户入口与金额安全边界验收报告.md"
  ],
  "shadow": false
}
```
<!-- AUTOPILOT-TASK-SCORE:END -->

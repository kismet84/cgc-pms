# ISSUE-040-034 工作流个人效率统计只读入口与数据范围验收报告

结论：通过；阻塞：无。

## 范围与结果

- 前端类型化调用 `GET /workflow/statistics/efficiency`，请求参数仅为关键词、业务类型、实例状态、起止时间和 `overdueHours=48`，不包含 userId、tenantId、body 或写操作。
- 现有审批工作台分析栏展示待办、逾期待办、已办记录、已处理任务和平均处理分钟数；加载与失败先清空旧统计，请求序号阻止较早响应覆盖新筛选结果。
- 后端生产代码未改动。既有 Controller 从 `UserContext` 获取当前 userId/tenantId，`WorkflowQueryService` 的任务、记录与实例查询均按两者及筛选条件收敛。

## 验收证据

- Ready lint：通过。
- 后端：`WorkflowQueryServiceTest` 32 项通过，覆盖用户、租户、筛选、逾期与平均处理时长口径。
- 前端：`ApprovalWorkList.test.ts` 13 项通过；`vue-tsc --noEmit`、目标 ESLint、`git diff --check` 通过。
- 运行态：8080 health、5173 与 dev-login 可达；前端重启后完成 180 秒稳定观察。真实浏览器从 dev-login 进入 `/approval/todo`，输入“不存在审批”触发筛选，URL 与空态同步，个人效率五项完整显示，无框架错误层，控制台无 error/warn。
- 审查修复：首次运行态截图发现窄栏标签截断，补齐网格标记与列宽后复验；绑定 diff 审查发现快速筛选响应竞态，加入 requestId 门禁并复跑前端裁决项。两项均已本轮关闭。

## 治理收口

- 唯一台账移除 `A-01-WORKFLOW-EFFICIENCY`，A-01 更新为有用户入口239、前端调用但无独立页面58、内部/集成/运维4、需补入口9、待废弃0、需要确认11，共321。
- 新增后续项：0；关闭后续项：1；后续项净变化：-1。
- 最小回滚：回退本 Issue 的前端 API 类型、审批分析栏、专项测试与治理回写；不涉及数据库或业务数据回滚。

剩余风险：仅验证 Chromium 桌面视口；效率栏在移动端按既有设计隐藏，不新增移动端入口。该行为属于既有非目标，未形成悬空问题。

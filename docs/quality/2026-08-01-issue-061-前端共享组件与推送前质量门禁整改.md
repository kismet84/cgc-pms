# 第61条主线质量报告：前端共享组件与推送前质量门禁整改

日期：2026-08-01

结论：本地实施与验证通过；正式同 SHA CI、commit、push、PR 未执行，不能判定为正式交付完成。

## 改动

- `frontend-admin-v2/src/components/V2Select.vue`：增加 `ariaLabelledby`，保留原生 select 的 label、required、disabled、错误描述和空选项契约。
- `frontend-admin-v2/tests/unit/design-system.test.ts`：补 visually-hidden label、disabled、required、error、ARIA 命名单测。
- `frontend-admin-v2/src/styles/tokens.css`：禁用控件文本改用现有 `--v2-color-text-secondary` 色值，消除响应式 Axe 对比度违规。
- `frontend-admin-v2/e2e/`：确定性 spec 对未处理 API 请求 fail-fast；付款、费用、发票、付款方式字典请求补最小响应。
- `frontend-admin-v2/scripts/run-push-quality-gate.mjs`、`frontend-admin-v2/package.json`：新增受影响变更检测和 `check:pre-push`，顺序执行 diff check、lint、type-check、unit、build、migration gate。
- `.githooks/pre-push`：版本化 hook；本地 `git config core.hooksPath .githooks` 已设置。
- `scripts/ci/test-workflow-contract.ps1`：增加本地门禁脚本、hook、package script 契约断言。

## 验证证据

| 检查 | 结果 |
| --- | --- |
| `pnpm test:unit` | 58 files / 398 tests passed |
| `pnpm type-check` | passed |
| `pnpm lint:check` | passed, 0 errors / 0 warnings |
| `pnpm build` | passed |
| `pnpm check:pre-push` | passed |
| `git hook run pre-push` | passed，真实 hook 入口执行同一门禁 |
| `pnpm test:e2e:migration-gate` | 23 deterministic specs；91 passed / 1 skipped |
| `scripts/ci/test-workflow-contract.ps1` | passed，13 CI jobs contract |
| `git diff --check` | passed |
| legacy listbox selector scan | 0 hit；`selectOption` 42 hits |

## 失败分类与修复

- `quality_or_security`：付款/收入确定性 spec 的字典请求未声明，触发 500；已补 4 个最小字典响应。
- `quality_or_security`：禁用刷新按钮颜色触发 serious Axe 对比度违规；已改用现有高对比次级文本色。
- `tool_invocation`：CI 契约初版把多行脚本数组当作单行文本匹配；拆分为独立契约字符串后复验通过。

## 剩余风险

本轮新增代码处于未提交工作区，当前 HEAD `e32013663d934572dbc130f74c6858b02812dcbd` 无法代表本轮差异；因此尚无同 SHA CI、提交、推送或 PR 证据。取得 Git 交付授权后，应在不改动本地差异的前提下提交并取得同 SHA CI，再更新本报告为正式收口结果。

零悬空统计：修复并复验 5 项；正式承接 1 项（同 SHA CI/Git 交付）；关闭 0 项；新增后续项 1、关闭后续项 0、净变化 +1。

# 第66条主线 M6：工作区归档与清理阶段报告

> 日期：2026-08-03
> 分支：`codex/mainline-66-rule-governance`
> HEAD：`1877031fcc0ee72cdea4d8d798dc6ed09c3482a3`
> 裁决：`G0-G4 PASS / G5 BLOCKED / REVERSIBLE_ISOLATION_ONLY / NO_PERMANENT_DELETE / LOCAL_ONLY`

## 1. 结论

M6 G0～G4通过，G5不通过。三个白名单目标共 `888,888,550` 字节（约847.7 MiB）逻辑内容已同卷移动到 `.agent-runtime/m6-quarantine-20260803-1538/`，原路径均不存在，隔离目标均存在且可按 manifest 精确回移。

本轮没有永久删除，实际释放空间记为0字节。同卷移动不释放磁盘分配；前后可用空间采样受并发进程影响减少 `8,249,344` 字节，不能把逻辑隔离量表述为释放量。

## 2. 实施与保留

| 目标 | 逻辑字节 | 结论 | 完整性证据 |
| --- | ---: | --- | --- |
| `desktop/node_modules` | 633,704,406 | 已隔离 | 回移后生成路径无关内容摘要，再隔离后摘要一致：`ed3a3f02...59d6f` |
| `.codex-autopilot/runs/frontend-node_modules-acl-broken-20260713` | 247,907,369 | 已隔离，禁止永久删除 | 23,665文件、3,046目录、字节数及0重解析点前后一致；内容读取被既有ACL拒绝，未提权、未改ACL |
| `.playwright-mcp` | 7,276,775 | 已隔离 | 回移后生成路径无关内容摘要，再隔离后摘要一致：`e37fbee3...a7250` |

清单与回滚事实位于 `.agent-runtime/m6-quarantine-20260803-1538/manifest.json`。首次离线验证失败产生的18文件、132,446字节临时树也保留在隔离区；未借机永久删除。

以下资产明确保留：Trivy缓存、Chrome 149、当前前端/契约/知识图谱依赖、`.codegraph`、`backend/target`、根及模块 `.agent-runtime`、数据库备份、`output/playwright`、AutoPilot状态/事件/closeout/开关/重置标记、普通 run、孤儿 worktree、仓库内旧 pnpm store。孤儿 worktree历史SHA证据不一致，旧store无立即删除必要性，均以“证据不足关闭”结束本轮。

归档候选也已逐项关闭：`design-qa.md` 因截图引用未稳定化而原位保留；`memory/**`、`CLAUDE.md` 因无获授权的仓库外私有目的地而原位保留；正式截图、数据库备份及认证状态继续原位保留；`x-to-markdown/**` 因缺用户价值裁决而原位保留。未执行仓库外归档。

## 3. 验证

| 验证 | 结果 |
| --- | --- |
| 当前V2离线安装 | 302包全部复用，下载0包，通过 |
| 旧V1 lockfile离线安装 | 393包全部复用，下载0包，通过 |
| HighRisk规则套件 | 7/7通过 |
| Mainline Owner Flow | 通过 |
| workflow contract | 13个required jobs，通过 |
| `pnpm --dir frontend-admin-v2 run type-check` | 通过 |
| `pnpm --dir frontend-admin-v2 exec playwright --version` | 1.61.1，可用 |
| pnpm store与当前依赖元数据 | 均指向 `D:\.pnpm-store\v11` |
| CodeGraph/知识图谱 | 数据库当日更新，相关进程仍运行，未中断 |
| AutoPilot | `LIMIT_REACHED`、enabled、启动标记、测试重置标记保持；`executorPid=null`、无run lock |
| `git diff --check` | 通过；隔离区未扩大Git差异 |
| 实际分配大小 | `BLOCKED`；ACL拒绝零访问metadata handle，pnpm hardlink不能由逻辑大小安全去重，manifest记录 `allocatedBytes=null`，禁止用逻辑大小推断 |

## 4. 失败分类

| 事件 | 分类 | 处理与最小复验 |
| --- | --- | --- |
| PowerShell管道、对象表达式及 `New-Item` 参数错误 | `tool_invocation` | 修正调用；对应清单、离线安装和状态读取各复验一次通过 |
| 递归删除临时验证树被策略阻止 | `tool_config` | 未绕过；临时树送回收站，残留启动树移入隔离区 |
| 初始metadata摘要迁移后不一致 | `tool_invocation` | 旧摘要不适合跨路径裁决；两个可读目标经受控回移/内容摘要/再隔离证明一致 |
| 损坏ACL目标内容哈希拒绝访问 | `quality_or_security` | 不提权、不改ACL、不永久删除；元数据清单前后一致并保持隔离 |

## 5. 风险、回滚与零悬空

- 回滚：逐项确认源路径不存在、隔离目标存在后，按 manifest 将目标回移到原绝对路径；任一冲突即停止。
- 剩余风险：损坏ACL目标无内容摘要，因此永久删除继续不通过；隔离区未定义观察期，也不得进入 Batch D。
- 后续项：M6新增1、关闭0、净变化+1，唯一载体为第66条计划。G5验收标准是取得不改ACL的实际分配大小证据，或由用户明确修改该验收标准；无载体遗留项为0。
- 正式裁决：可逆隔离通过；由于 `allocatedBytes` 证据缺失，G5不通过，不声明M6或扩展后的第66条主线完成。
- Git、生产、部署、数据库及仓库外归档均未执行。

# 第53条主线 M5 供应链与物资全量退出门验收报告

## 裁决

- 结论：通过；M5本地正式退出门满足。
- 阻塞项：0。
- 路由：10条目标路由全部由真实Clean-room V2承接或受控重定向。
- 台账：`LEGACY_ONLY=40 / V2_ACCEPTED=47 / V2_SOURCE_AVAILABLE=0`。
- 证据层级：本地源码与本地dev/test/demo；不等同同SHA CI或目标环境发布证据。
- 发布边界：未commit、push、PR、切正式入口、退役Legacy或操作生产。

## 业务闭环证据

- 供应商招采：事件、邀请、报价、评审、定标、合同、履约评价、退货、黑名单与trace使用服务端状态和七组细分权限。
- 采购执行：申请、订单、部分/完整验收保持来源、数量、预算、金额、已收、剩余及状态服务端权威。
- 仓储库存：仓库范围/CRUD、库存数量/价值、阈值、在途、历史净领料、来源流水及幂等调拨成立；通用手工出入库继续失败关闭。
- 领退料：申请、明细、审批、出库、退料确认/冲销及trace成立；退料绑定原领料明细与原出库流水。
- V2不建立平行库存账，不本地累计权威数量、价值或状态；写后统一重新读取。

## 10路由完成矩阵

| 路由 | V2承接 | 服务端权威事实 | 当前证据 |
| --- | --- | --- | --- |
| `/supplier-sourcing` | `SupplierSourcingPage` | 招采状态、报价金额、评审、定标、履约、退货、黑名单、trace | 台账`V2_ACCEPTED`；供应商单测与E2E |
| `/purchase` | 重定向`/purchase/order` | 不创建第二入口状态 | 路由单测与采购E2E |
| `/purchase/order` | `PurchaseExecutionPage` | 申请来源、合同/供应商、数量、单价、金额、审批、已收量 | 台账`V2_ACCEPTED`；采购单测与E2E |
| `/purchase/receipt` | `PurchaseExecutionPage` | 订单剩余量、部分/完整验收、质量数量、库存流水 | 台账`V2_ACCEPTED`；采购单测与E2E |
| `/inventory` | 重定向`/inventory/warehouse` | 不创建第二库存入口 | 路由单测与库存E2E |
| `/inventory/warehouse` | `InventoryWorkspacePage` | 项目范围、仓库状态、CRUD结果 | 台账`V2_ACCEPTED`；库存单测与E2E |
| `/inventory/stock` | `InventoryWorkspacePage` | 现存量、可用量、价值、成本、阈值、在途、历史净领料、调拨结果 | 台账`V2_ACCEPTED`；库存后端测试、单测与E2E |
| `/inventory/transaction` | `InventoryWorkspacePage` | 来源流水、出入方向、发生量、结存、来源对象 | 台账`V2_ACCEPTED`；只读权限与库存E2E |
| `/inventory/purchase-request` | `PurchaseExecutionPage` | 预算、申请数量/金额、审批与来源 | 台账`V2_ACCEPTED`；采购单测与E2E |
| `/inventory/material-requisition` | `RequisitionWorkspacePage` | 领料审批、出库、库存/成本流水、退料/冲销、原流水绑定 | 台账`V2_ACCEPTED`；领退料后端测试、单测与E2E |

矩阵逐项源自当前路由装配、`40/47/0`台账、服务适配、后端专项和四组Playwright；10路由均未落入`ShellPlaceholderPage`或Legacy组件。

## 自动化证据

- Ready lint：Ready阶段最终hash `14993bdadc9635ba61bc593fb7033c720405fd11aeff511fe1c800ee5c1d4b27`已通过；终态为Completed，复跑按契约返回`READY_CONTRACT_INVALID`，改做治理一致性验真。
- 后端全量：264类，2198项，0失败、0错误、3跳过，Maven `BUILD SUCCESS`。
- M5后端顺序复验：25类，244项，0失败、0错误、0跳过，Maven `BUILD SUCCESS`。
- V2单测：43文件，321项通过。
- M5 Playwright：公共壳加四组供应链39/39通过；覆盖权限失败关闭、服务端事实、负向错误、重复点击、三视口和axe。
- 契约/V2类型、ESLint、Clean-room边界、路由台账、生产构建与包体通过；边界扫描158个V2文件、14个契约文件，构建206模块、36个JS资产。
- demo verify：`cgc_pms_demo_v2`通过；库存、采购、领退料、金额和来源守恒指标通过。
- `git diff --check`通过。

## 失败分类

- `ready_issue_config`：029初稿使用无效任务性质`正式验收`；改为权威枚举`回归证明`后Ready lint通过。
- `ready_issue_config`：demo命令初指旧库`cgc_pms_demo`，该库无V218 `pay_record.record_code`；未迁移或修改数据库，改指现行本地demo库`cgc_pms_demo_v2`后通过。
- `command_config`：一次合并命令在`backend`目录调用仓库根Ready脚本，路径无效且测试未启动；拆分正确工作目录后通过。
- `lifecycle_expected`：029置为Done后再次调用仅接受Ready状态的lint，按契约返回“状态不是Ready”；执行前Ready hash已通过并固化，终态改做台账、治理一致性与`git diff --check`。
- `quality_or_security/DELIVERY_GATE_OMISSION`：M5 E2E首轮18项仍按旧详情入口、旧筛选结构和内联错误提示断言；同步当前业务契约后四组39/39通过。
- `quality_or_security/DELIVERY_GATE_OMISSION`：后端全量暴露新增必填业务编码未进入测试夹具及库存测试并行清理冲突；补齐业务编码、改为专用项目/仓库夹具后全量264类2198项与顺序25类244项通过。
- `quality_or_security/DELIVERY_GATE_OMISSION`：全V2动态门原未覆盖表单可见原始主键；扩门证明红灯后，项目、用户、合同、合作方、仓库、物料、成本科目等字段统一改为业务候选，数据库主键仅作内部值。
- `quality_or_security/DELIVERY_GATE_OMISSION`：候选选择器E2E在弹窗过渡动画终态前操作，产生5项不稳定失败；改为等待真实动画完成，未增加固定延时或降低断言，最终39/39通过。
- 上述均非业务缺陷、非产品回归；无未分类失败。

## 完成性复核补强

- 发现并关闭1项直接风险：库存卡片曾将十进制字符串转为`Number`并在前端推导告警色；现仅展示服务端原值与服务端KPI，不再推导库存状态。
- 补充422请求失败类型样本；403/404/409/422/500均有共享请求层或目标流程证据。
- 补充仓库、库存、来源流水、领料路由非Placeholder及精确权限单测。
- 补充普通角色缺少`requisition:submit`、`requisition:stock-out`、`requisition:return`时动作失败关闭的浏览器样本。
- 补强后全量类型、321项单测、Lint、边界、台账、生产构建与39/39 M5 E2E通过。

## 治理与零悬空收口

- `ISSUE-053-024～029`全部Done；M5无剩余Ready。
- 正式报告、M5计划、总计划、Ready、Current Focus、Done、项目地图与路由台账一致。
- 终态机器审计：目标路由10/10为`V2_ACCEPTED`，无Placeholder/Legacy承接，6份报告引用存在，台账`40/47/0`，029为Done，前端库存状态推导不存在，`git diff --check`通过。
- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- 悬空项：0。
- M6未启动；须另行完成产品决策、计划与Ready补货。

## 设计系统复审补强

- 全 V2 自动门禁现覆盖默认标题层级、重复 H1、动态分页、实现语言及可见原始数据库主键；供应链目录门禁覆盖内联详情、密集记录卡片、复合表头和裸状态。
- 8 个可见供应链页面运行态均为单 H1、21px、中文状态、无页面横向溢出；仓库使用表格，详情使用标准弹窗。
- 本轮修改前端、测试、设计基线和治理报告；后端仅修测试夹具隔离与新增必填编码，未改业务实现、未重启运行后端、未触碰数据库。

## 剩余边界与回滚

- 正式入口切换、Legacy退役、同SHA CI和目标环境发布仍未授权，也未由本报告证明。
- 回滚029需回退业务候选选择器、库存服务端事实展示、测试夹具隔离、动态门补强与治理回写；无数据库回滚。
- 若后续同SHA CI或目标环境证据失败，M5发布裁决为不通过；保持Legacy正式入口。

# ISSUE-053-006 M2 八角色新版经营驾驶舱验收报告

## 结论

- 验收结论：不通过，实施基本完成；缺少七个项目级角色的真实有数后端/浏览器证据。
- 阻塞项：1 个 `environment_prerequisite`。当前本地 MySQL 的 `pm_project` 为 0 行，唯一用户为 tenant 0 的 admin；真实 `/projects` 返回 `total=0`。
- 依赖裁决：`ISSUE-053-007` 不启动；必须先解除本 Issue 真实数据门禁。
- 上线边界：仅本地 Clean-room V2；未发布、未切换正式入口，未修改后端、数据库、Legacy、权限码或业务数据。
- 后续项：新增 0、关闭 0、净变化 0。未完成验收继续由唯一载体 `ISSUE-053-006` 承接，不另造重复 backlog。

## 已实施范围

- `/dashboard` 已由壳占位替换为八角色真实 API 容器；无权角色 Tab 不出现，只请求当前选中角色。
- 项目级角色无项目时 fail-close；管理层只调用后端 management 视图，不携带项目或月份。
- 页面本地状态以 AbortController 与 generation 隔离项目、报告期、角色和刷新导致的陈旧响应。
- 金额使用字符串分组和符号显示，不经 JavaScript 浮点运算改变精度；正数、负数、零、大额、空值和百分比由纯函数测试覆盖。
- 辅助健康分只按当前响应中的风险、逾期和待办计数展示，明确标注“非财务、结算或业务写入口径”。
- 成本经理提供原生 SVG 趋势和精确表格等价信息；成本分解独立请求、最多两级展开，失败不清空主驾驶舱。
- 快捷入口只暴露当前已达 `V2_ACCEPTED` 的页面内锚点；审批、预警、报表未验收前不跳转占位页。

## 已通过证据

- `pnpm test:unit`：14 文件、59 项通过。
- `pnpm type-check`、`pnpm lint:check`：通过。
- `pnpm check:boundary`：通过，扫描 67 个 V2 文件与 5 个共享契约文件。
- `pnpm build`：101 个模块完成生产构建；Dashboard 独立 JS 15.98 kB、CSS 6.26 kB。
- `pnpm check:bundle-size`：通过，6 个 JS 资产。
- `m1-shell.spec.ts`：模拟身份三视口 2 项通过，覆盖登录、权限、403/404、无横向溢出与 axe。
- `m2-dashboard-live.spec.ts`：本地真实 admin、真实 `/projects` 空结果、项目级 fail-close、真实 management API、1440/1024/390 与 axe 通过。
- `shell-live.spec.ts`：本地真实管理层 Dashboard 与公共壳三视口 1 项通过。
- 真实浏览器首次发现健康卡辅助文字 4.37:1、刷新按钮 4.12:1 两处 AA 对比度不足；均在 Dashboard 局部样式修复，最终 serious/critical 为 0。
- `git diff --check`：通过。

## 尚缺证据与解除条件

| 验收项 | 当前证据 | 裁决 |
| --- | --- | --- |
| 八角色权限与端点选择 | 契约/服务/单测覆盖八角色；浏览器可见八个有权 Tab | 代码通过 |
| 项目经理、商务、成本、采购、生产、总工、财务真实有数数据 | 本地无项目，前端按规则不发请求 | 阻塞 |
| 管理层真实数据 | `/dashboard/management` 返回 200，空数据展示通过 | 通过 |
| 无项目 fail-close | `/projects total=0`，Dashboard 项目端点请求为 0 | 通过 |
| 成本趋势和两级下钻真实数据 | 组件、纯函数和部分失败单测通过；无项目样本 | 阻塞 |
| 1440/1024/390 与无障碍 | 真实管理层 + 模拟项目经理通过；无横向溢出，axe serious/critical=0 | 通过 |

解除条件：提供现有非生产受控项目样本和具备对应 Dashboard 权限的身份，或另行明确授权建立不进入迁移、不污染生产的本地验收数据。解除后只读复验八端点、金额、趋势、风险、下钻和三视口；不得以静态 mock 替代真实有数证据。

## 失败分类与处置

- 首轮 live E2E 等待项目经理数据失败：真实 `/projects` 返回空，归类 `environment_prerequisite`；修正验收路径为验证 fail-close 与管理层，不把空库误判为页面代码失败。
- live 壳 1024 仍显示当前域工作区：旧测试期望隐藏，与公共壳定稿事实冲突，归类 `ready_issue_config`；更新断言并通过。
- 视觉检查两次发现对比度不足：归类 `quality_or_security`，均本轮修复并复验。

## 回滚

- 将 `/dashboard` 组件映射恢复为 `ShellPlaceholderPage`，回退 `src/pages/dashboard/**` 与对应测试。
- 保留 053-005 共享契约、请求基线、M1 公共壳与 Legacy 正式入口；无数据库或业务数据回滚要求。

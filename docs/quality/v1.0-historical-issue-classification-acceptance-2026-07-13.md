# 第40条主线：v1.0 历史问题分类治理验收报告

## 结论

- 结论：通过。
- 阻塞：非阻塞。
- 使用范围：v1.0 正式历史回溯和 v1.5 候选/复验输入；不能替代当前验证。
- 私有边界：`archive/v1.0/private/**` 未读取、未扫描、未入图。

## 正式交付

- 知识图谱 0.3.0 支持正式历史文档入图及 `historical/versionScope` 标记。
- `kg_search` 增加 `scope=current|historical|all`，默认排除历史资料。
- 建立 [v1.0 历史问题四态分类](../backlog/v1.0-historical-issue-classification.md)。
- 92 个唯一历史 Issue 按 v1.0 状态归档，256 条风险陈述归并为可跟踪问题域。

## 分类裁决

| 类别 | 裁决 |
| --- | --- |
| 已解决 | 92 个历史 Issue 有 Done/关闭证据；另有 5 个历史主题已被 v1.5 新鲜证据解除 |
| 仍适用 | 10 个产品/平台问题域保持 Candidate 或 Frozen，不自动实施 |
| 已过期 | 6 类旧分支、旧工作区、旧工具与旧批次记录退出当前跟踪 |
| 需要复验 | 8 个权限、租户、数据一致性、真实角色、外部集成和工具链风险域保守保留 |

## 验收要求

- Node Test Runner、MCP smoke、Schema 幂等、正式历史采集、搜索隔离、私有哨兵、依赖审计和 `git diff --check` 必须全部通过。
- current 搜索不得返回 `historical=true`；historical 搜索必须只返回 v1.0 历史节点。
- 254 个正式历史 Markdown 必须具有正文、CurrentVersion 和历史标记。

## 验收证据

| 项目 | 结果 |
| --- | --- |
| Node Test Runner | 18 tests，0 failure |
| Schema | 23 条语句应用成功 |
| 正式文档采集 | 369 个允许文件；其中历史文件 263、历史 Markdown 254 |
| 幂等复采 | 369 processed / 369 skipped / 0 added / 0 updated |
| 历史 Markdown 覆盖 | 254 Artifact / 254 正文 / 254 CURRENT_VERSION |
| 搜索隔离 | current 20 条中历史命中 0；historical 20 条中当前命中 0 |
| 私有封存 | `archive/v1.0/private/**` Artifact 数量 0 |
| 来源覆盖 | ArtifactVersion 缺失来源 0；Decision/Evidence 缺失来源 0 |
| 引用闭环 | 连续两轮 369 processed / 369 skipped / `unresolvedReferences=0` |
| 轻量引用节点 | 16 个 `referenceOnly` Artifact；正文数量 0 |
| 敏感测试值 | 0 |
| 依赖审计 | high 及以上漏洞 0 |
| `git diff --check` | 通过 |

## 剩余风险

- 四态清单按问题域归并，不把 256 条重复或过程性风险陈述逐条恢复为 backlog。
- “需要复验”尚未执行对应业务回归；只有进入具体产品目标或上线裁决时才按风险拆分。
- 历史全文增加图谱容量，但默认查询隔离避免其冒充当前事实。

以上均为非阻塞边界；后续任何复验结果必须回写分类清单和 Current Focus。

## 已关闭的索引风险

- 原 28 条未建 `REFERENCES` 边已闭环：仓库内存在且不在文档语料中的文件或目录使用轻量路径节点，不读取目标正文；16 个唯一轻量节点的正文数量为 0。
- 其中最后 3 条是 `.txt` 测试日志中的 `[值](类型)` 被误识别为 Markdown 链接；采集器现仅对 `.md` 抽取链接，并有回归测试保护。
- 连续两轮采集均为 `SUCCEEDED`，369 个文档全部幂等跳过，`unresolvedReferences=0`；`archive/v1.0/private/**` Artifact 数量仍为 0。

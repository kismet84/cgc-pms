# ISSUE-040-021：系统菜单详情管理员入口验收报告

## 结论

- 验收结论：通过。
- 阻塞性：非阻塞；本 Issue 的管理员入口、详情请求、加载/失败态、权限、租户隔离与敏感字段收敛均已闭环。
- 上线边界：代码具备进入后续集成/发布门禁的条件；本轮未连接生产、未修改生产数据、未发布、未 push，不能替代同一待发布 SHA 的正式上线验证。

## 来源、目标与范围

本 Issue 来源于 `docs/backlog/current-issues.json` 的 `A-01-MENU-DETAIL`，其 `sourceRefs` 指向 `docs/quality/ISSUE-037-019-后端接口无前端入口只读盘点与治理裁决验收报告.md` 中 `GET /system/menus/{id}`“需补用户入口”的静态裁决，并引用 `docs/product-intelligence/project-map.md` 与 `docs/product-intelligence/evolution-decision.md` 的 `PI-2026-07-13-02`。

范围仅是在既有 admin-only `/system/permissions` 页面增加完整菜单树目标选择、类型化 GET 调用和只读详情交互，并补控制器权限专项；不修改后端生产详情语义，不新增路由、权限码、角色、migration、列表、编辑、动态路由或完整菜单 CRUD。

## 实现结果

1. 页面仅向 ADMIN、SUPER_ADMIN 展示“查看详情”；普通用户无论无权限或仅持有 `system:menu:query` 都不会被前端单元测试伪装为页面可达，后端权限码授权仍独立有效。
2. 详情目标直接来自完整 `MenuTreeVO` 树，不依赖只展示有权限码节点的表格行；目录、菜单、按钮及无权限码节点均可选择。
3. `getMenuDetail` 复用既有 `SysMenuVO`，固定发送无请求体、无额外参数的 `GET /system/menus/{id}`，没有复制第二套详情类型。
4. 选择新目标时立即清空旧详情；请求成功仅展示名称、类型、父节点、路径、组件、权限码、图标、排序、状态和可见性，请求失败保留目标、显示可理解错误且不泄露上一次详情。请求序列保护避免较早响应覆盖较新目标。
5. 后端生产 Controller、Service、Entity、Mapper 和 schema 保持不变；既有授权、租户 fail-close 与响应字段白名单未放宽。

## 验收证据

- Ready lint：`ISSUE-040-021` 通过，无 error、无 warning。
- 后端专项：`SysMenuControllerTest,SysMenuServiceTest` 共43项，0 failure、0 error，Maven `BUILD SUCCESS`。详情覆盖 ADMIN、SUPER_ADMIN、显式 `system:menu:query` 返回200，无角色且无权限返回403，未登录返回401；既有服务专项覆盖跨租户和不存在目标统一 `MENU_NOT_FOUND`。
- 前端专项：2个测试文件、26项全部通过。详情覆盖 GET 无请求体/参数、ADMIN/SUPER_ADMIN 可见、普通用户与仅持查询权限不可见、完整树节点选择、单次请求、加载期清空旧数据、白名单展示和失败保留目标。
- 前端类型与静态质量：`vue-tsc --noEmit` 通过；4个目标前端文件 ESLint 0 error、0 warning；`git diff --check` 通过。
- 差异边界：改动仅位于 Ready `allowedPaths`；禁止修改的后端生产代码、`frontend-admin/src/types/system.ts`、路由、migration、部署与 CI 路径无差异。
- 浏览器验收：health gate 的 backend、frontend、dev-login 均返回200；从 dev-login 最终进入 `http://localhost:5173/system/permissions`，页面标题为“权限清单 - 建筑工程总包项目管理系统”。默认桌面视口点击“查看详情”，从完整树选中按钮节点“合同提交审批”后，页面展示类型“按钮”、权限码 `contract:submit`、状态“启用”、可见性“隐藏”等10个白名单字段；页面非空、无框架错误遮罩，控制台 error/warn 为0。
- 检索交叉核验：查询目的为确认前端入口、`SysMenuVO` 复用、后端详情授权和租户链路。CodeGraph 命中 `SysMenuController.getById`、`toVO` 与 `SysMenuVO`，但提示索引属于主工作树且前端召回不足；只读 `codebase-memory-mcp` 进一步命中 `SysMenuService.getById`、`SysMenuController.getById`、前端 `getMenuTree` 和服务隔离测试，随后以当前 worktree 的 `rg`、git diff 与专项测试交叉确认。该缺口归类为工具召回/隔离不足，没有误判为代码不存在。

## 首次失败分类、审查与复验

- 首次并行执行两个 pnpm 命令时同时触发同一 `node_modules` 依赖检查，出现 `ERR_PNPM_EBUSY` 符号链接竞争；分类为 `tool_config/命令并发问题`。串行 `pnpm install --frozen-lockfile` 恢复后，前端专项、类型检查与目标 ESLint 全部通过。
- 首次浏览器打开的5173容器绑定主工作树 `D:\projects-test\cgc-pms\frontend-admin`，未显示当前 issue worktree 改动；分类为环境前置。仓库刷新脚本又因本地 Compose 缺少 `JASYPT_ENCRYPTOR_PASSWORD` 在重启前失败，分类为 `tool_config`。随后临时停止旧前端容器，从当前 worktree 启动本机 Vite，稳定等待180秒并复验 health gate 后完成浏览器验收，收口时恢复原前端容器。
- 独立 Reviewer 首次复核发现阻塞性文档一致性问题：部分仍表述“当前”状态的 Current Focus、项目地图和迭代决策保留 226/22 旧口径，与本 Issue 的 227/21 守恒冲突。已统一更新这些现行口径，明确历史快照保持原值，并交由同一 Reviewer 二次复核。
- 同一独立 Reviewer 二次复核确认首次阻塞已完整关闭，现行口径统一为227/21，019/020历史快照未被误改；最终剩余阻塞0、非阻塞发现0、悬空问题0，裁决为通过、非阻塞。
- 代码风险审查未发现越权、租户绕过、敏感字段泄漏、旧数据残留或超范围生产改动。格式审查发现的19条 Prettier warning 已在允许文件内机械修正并复验，未留下口头建议或质量报告观察项。

## 风险审查与回滚

- 授权：前端 admin-only 与后端 ADMIN/SUPER_ADMIN 或 `system:menu:query` 分别留证；隐藏入口不替代后端403门禁。
- 数据与一致性：详情读取仍以当前租户为边界，不存在和跨租户目标统一 fail-close；本轮没有测试数据删除或重置。
- 敏感字段：前端类型与页面只消费 `SysMenuVO`；响应和页面不展示 `tenantId`、`deletedFlag`、审计字段、`remark` 或 `children`。
- 回滚：回退本 Issue 的前端详情 API、只读详情交互、对应测试、控制器权限测试和治理文档；无 schema 或生产数据回滚。

## 台账与非阻塞问题零悬空

- `A-01-MENU-DETAIL` 已从 `docs/backlog/current-issues.json` 移除，未保留 Done 后仍 OPEN 的重复状态。
- A-01 守恒：有用户入口227、前端调用但无独立页面58、内部/集成/运维4、需补入口21、待废弃0、需要确认11，共321。
- 本轮新增后续项：0。
- 本轮关闭后续项：1（`A-01-MENU-DETAIL`）。
- 后续项净变化：-1。
- 本轮发现的 pnpm 并发、旧工作树运行态和格式警告均已修复或恢复并复验；未发现需要另行承接且具备立项价值的剩余问题。

## 剩余风险

无本 Issue 范围内悬空风险。剩余21个 A-01 入口缺口和11个需要确认项均为 `docs/backlog/current-issues.json` 中既有唯一条目，不由本 Issue 新增或扩大；后续继续按各自 Ready、权限与数据边界推进。

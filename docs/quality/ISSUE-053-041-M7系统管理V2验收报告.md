# ISSUE-053-041 M7 系统管理 V2 验收报告

> 日期：2026-07-27
>
> 基线：`master@2908dd70b336a522b69640f2883a722ac1e9a5a6`
>
> 裁决：`PASS / ISSUE-053-041 Done / M7 CONTINUES`

## 1. 范围

`/system` 及用户、角色、权限、字典、审计、文档模板、数据维护 7 条子路由全部由 Clean-room V2 承接；`/system` 确定性重定向至 `/system/dict` 并保留 query/hash。

| 路由 | 读取门 | 角色门 | 写入边界 |
| --- | --- | --- | --- |
| `/system/users` | `system:user:query` | ADMIN/SUPER_ADMIN | 用户字段白名单、自操作保护、管理员连续性 |
| `/system/roles` | `system:role:query` | ADMIN/SUPER_ADMIN | 复用既有角色与权限 API |
| `/system/permissions` | `system:menu:query` | ADMIN/SUPER_ADMIN | 复用既有菜单权限事实 |
| `/system/dict` | `system:dict:list` | ADMIN/SUPER_ADMIN | 字典写后重新读取 |
| `/system/audit` | `audit:query` | 无 adminOnly | 只读；缺权限不请求 API |
| `/system/doc-templates` | `document:template:query` | ADMIN/SUPER_ADMIN | 编辑、发布沿用精确权限和版本契约 |
| `/system/data` | 无合成权限码 | SUPER_ADMIN | 非生产、精确确认；验收不执行清库 |

未新增权限码、数据库 migration、并行用户事实源或前端安全裁决。

## 2. 契约与安全证据

| 事项 | 结论 | 证据 |
| --- | --- | --- |
| 双门禁 | adminOnly 页面同时校验角色与精确权限；普通角色误配权限、管理员缺权限均在 API 前失败关闭 | navigation/router unit、目标 E2E |
| 用户更新白名单 | 普通更新只接受 realName、phone、email、avatar、orgId；忽略 username、tenantId、isAdmin、status、password 和审计字段 | `SysUserServiceTest` |
| 管理员连续性 | 禁止自停用、自删除；停用、删除和角色替换均按启用管理员事实加锁校验，已停用管理员不计入保障 | `SysUserServiceTest` |
| 全会话改密撤销 | access/refresh token 绑定当前密码凭据版本；改密后旧令牌不能继续访问或刷新，缺版本失败关闭 | `JwtUtilsTest`、`JwtAuthenticationFilterTest`、`AuthControllerTest` |
| 审计只读 | 页面不提供新增、修改、删除动作；缺 `audit:query` 时请求数为 0 | page unit、目标 E2E |
| 数据维护 | 后端仅非生产 profile、SUPER_ADMIN、精确确认码三门同时成立时执行；同连接恢复 FK 检查，部分失败显式报错 | `SystemControllerProfileTest` |
| 写后回读 | 用户、角色、字典和模板操作成功后重新读取服务端事实 | page/service unit |
| 台账 | 8 条路由全部接受，无 Legacy 或占位实现 | route ledger `0/87/0` |

## 3. 验证

| 门禁 | 结果 |
| --- | --- |
| 后端定向测试 | 7 个测试类，74 项，0 失败、0 错误、0 跳过 |
| 后端 package | `mvnw -q -DskipTests package` 通过 |
| V2 目标 unit | navigation、router、page、service 共 32/32 通过 |
| 设计系统门禁 | 3 文件、77 项全通过 |
| V2 全量 unit | 55 文件、397 项全通过 |
| type/contracts | `pnpm type-check`、`pnpm type-check:contracts` 通过 |
| lint | 0 error；本任务 warning 为 0；6 个既有 warning 位于未修改的 `e2e/m1-shell.spec.ts:149-156` |
| clean-room | 202 个 V2 文件、16 个契约文件通过 |
| route ledger | 87 条命名路由、73 个视图引用、65 个唯一视图，`0/87/0` |
| build/bundle | 266 modules；50 个 JS 资产；构建和包体门通过 |
| 目标 E2E | 当前源码独立端口 5184，4/4 通过；含角色/权限负向、审计只读、重定向、三视口、水平溢出与 serious/critical axe 0 |

E2E 使用 mock API 验证当前前端契约，不替代真实后端或目标环境证据；清库 API 请求数始终为 0。首轮发现的设计系统违规和已停用管理员连续性缺口归类 `quality_or_security`，均已根修并复验；无未分类失败。

## 4. 回滚与边界

- 回滚只移除本切片 V2 页面、服务、路由/导航元数据、测试和台账接受映射，并恢复本切片后端用户/令牌/数据维护根修；无数据库 migration 或数据恢复动作。
- 正式入口、Legacy 退役、commit、push、PR、目标环境和生产均未授权。
- 下一唯一 Ready 为 `ISSUE-053-042`，只执行 M7 全量退出门。
- 新增后续项：0；关闭后续项：0；后续项净变化：0。当前范围内无口头悬空项。

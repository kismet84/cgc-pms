# ISSUE-053-026：M5采购申请、订单与验收V2验收报告

## 1. 裁决

结论：通过。`/purchase`已受控重定向到`/purchase/order`；`/inventory/purchase-request`、`/purchase/order`、`/purchase/receipt`已由真实Clean-room V2页面承接，四路由均达到`V2_ACCEPTED`。路由台账由`49/38/0`更新为`45/42/0`。

阻塞项：0。非阻塞遗留：0。未授权且未执行commit、push、PR、正式入口切换、Legacy退役、生产发布或生产数据操作。

## 2. 范围与实现

- 共享契约新增采购申请、订单、验收的查询/命令/明细DTO及15项细分权限；ID、金额和数量保持稳定字符串。
- 薄服务复用`/purchase-requests`、`/purchase-orders`、`/receipts`既有列表、详情、明细、创建、提交和删除端点；查询支持AbortSignal，路径ID统一编码。
- 单个`PurchaseExecutionPage`按三条业务路由切换，覆盖服务端分页、详情、采购申请到订单来源、订单剩余数量候选、部分/完整验收及审批提交；页面不累计金额、在途或验收余量。
- 创建闭环要求当前普通角色同时具备对应`add/edit/delete`权限；单头创建后明细失败时，只删除本次新建草稿。任一权限缺失均不开放创建入口。
- 所有成功写入后重新读取列表和详情；超量验收保留服务端409，不自动重试，不乐观修改库存或验收数量。
- 未修改后端业务代码、数据库migration、Legacy、请求核心、状态库、依赖或其他M5路由。

## 3. 关键证据

| 门禁 | 结果 |
| --- | --- |
| Ready lint | 通过；hash `6a4295af57b1e1e7f6a473fe2e05c4ca77f2f444d73b6ff994eb75f2e580264a` |
| 后端专项 | 9类采购/验收/工作流测试共113项，0失败/0错误/0跳过，`BUILD SUCCESS` |
| V2单测 | 39文件、281项全部通过；覆盖权限、稳定字符串、路径编码、Abort、409和设计系统门 |
| 契约/V2类型 | 全部通过 |
| Lint / Clean-room边界 | 0 error；149个V2文件、14个契约文件通过 |
| 路由台账 | 87命名路由；`LEGACY_ONLY=45 / V2_ACCEPTED=42 / V2_SOURCE_AVAILABLE=0` |
| 构建/包体 | Vite生产构建与bundle size门通过；采购执行页面独立chunk生成 |
| 目标E2E | 9/9通过：查询权限403零业务流量、创建三权限失败关闭、申请/订单/部分与完整验收、订单剩余量读取、超量409精确回滚、双击去重、三视口和axe |
| 差异静态检查 | `git diff --check`通过 |

浏览器验收使用刷新后的本地V2容器`http://127.0.0.1:5174/v2/`；容器健康，页面HTTP 200。API由Playwright本地mock承载，未连接生产或写入真实业务数据。

## 4. 风险复核

- 租户/项目范围、合同/供应商一致性、预算、申请转订单、超量验收、库存写入、工作流状态和重复回调继续由既有后端113项专项裁决；本Issue未复制规则。
- 页面展示采购申请预计金额/预算行、订单金额/已收数量、验收订单量/累计量/剩余量，均来自服务端响应。
- 创建链只在可精确回滚时开放；409负向证明明细请求一次、草稿删除一次，无孤立新建单头。
- 三视口为1440×900、1024×768、390×844；无页面级横向溢出，axe serious/critical为0。
- loading、empty、error、Abort、陈旧响应隔离、分页和重复点击锁成立；HTTP业务码与消息由公共请求层保留。

## 5. 失败分类与处置

- 初次V2回归命中字体/表格/按钮/Dialog公共设计门：分类`implementation`；改用既有token与公共组件后复验通过。
- 初次Playwright命中5174旧Vite模块：分类`environment`；按运行态Skill重启`frontend-v2`并等待健康后复验。
- 验收记录同时含`receiptCode`与`orderCode`，初版联合类型判别顺序错误：分类`implementation`；改为先判`receiptCode`，目标E2E复验通过。
- 收口复核发现固定100条列表和订单明细ID手填证据偏弱：分类`product`；本轮补服务端分页及订单剩余数量候选，不新增前端数量计算。

## 6. 收口

- 本轮修复并复验：采购申请、订单、验收四路由V2闭环、权限失败关闭、服务端事实展示、失败精确回滚、路由台账及全量相关门禁。
- 超出范围并正式承接：`ISSUE-053-027`成为唯一Ready，承接仓库、库存与来源流水四路由；未自动启动。
- 证据不足或无明确价值而关闭：无。
- 新增后续项：0；关闭后续项：0；后续项净变化：0；无载体遗留项：0。

最小回滚：回退本Issue采购执行契约/服务扩展、V2页面、路由、台账、测试及治理回写；024～025、Legacy、数据库和既有业务数据保持不变。

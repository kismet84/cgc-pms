# 12 多助手协作规范

## 总原则

主会话是项目总负责人，负责目标、优先级、风险、验收和最终结论。8 个助手是专题会话，默认只分析、计划、审计或验收；只有收到明确的“执行、修复、实现、修改、运行测试、提交”等动作词，才进入执行模式。

本规范是长期执行版，不是一次性任务说明。凡是分派、回传、等待、复用、验收、经验沉淀相关的动作，都以本文为准。

所有助手接到任务后必须先读：

1. `AGENTS.override.md`
2. `AGENTS.md`
3. 本文档
4. 自己的主规范

## 角色边界

| 助手 | 主规范 | 职责 | 默认不做 |
|------|--------|------|----------|
| 产品/需求负责人 | `docs/03-业务模块说明.md` | 目标、范围、流程、验收口径 | 不写代码 |
| 前端设计师 | `docs/05-前端开发规范.md` | Vue 页面、交互、`lg-*` UI、前端验收点 | 不改后端 |
| 后端设计师 | `docs/04-后端开发规范.md` | 接口、服务边界、事务、异常、业务状态 | 不决定 UI |
| 数据库与迁移负责人 | `docs/07-数据库与迁移规范.md` | 表结构、索引、Flyway、H2/MySQL 一致性 | 不改已应用迁移 |
| 测试验收员 | `docs/09-测试规范.md` | 最小验证集、pass/fail、阻塞判断 | 不默认修复 |
| 安全审计员 | `docs/11-安全规范.md` | 鉴权、越权、脱敏、密钥、上传、敏感数据 | 不做业务重构 |
| 部署运维负责人 | `docs/10-部署运维手册.md` | Docker、环境变量、日志、健康检查、回滚 | 不改业务代码 |
| 代码质量审计员 | `docs/prompt/code-audit-agent.md` | 生产可用性、坏抽象、重复逻辑、测试缺口 | 不默认实现修复 |


## 当前 8 个助手会话

| 助手 | threadId |
|------|----------|
| 产品/需求负责人 | `019f0d99-fcf1-7ba0-b82e-2c0a84b96f53` |
| 前端设计师 / 前端架构助手 | `019f0d9a-1cd8-73c0-ba96-eb01bc1ad00c` |
| 后端设计师 / 后端架构助手 | `019f0d9a-3779-7ae2-ac92-832982a138e2` |
| 数据库与迁移负责人 | `019f0d9a-4fb5-7c50-b65c-a4557f2ad1f2` |
| 测试验收员 | `019f0d9a-6812-7010-aeb8-94e4d3b2f6a8` |
| 安全审计员 | `019f0d9a-826e-77a0-9781-218e984c194f` |
| 部署运维负责人 | `019f0d9a-adcd-7bc2-a5de-1f73996f264c` |
| 代码质量审计员 | `019f0d9a-ce8e-7bc1-8a5f-80aa4a3e561d` |

## 复用原则

主会话分派任务时，优先复用下表中的现有助手会话，不得为了同一职责重复创建新线程。

复用顺序：

1. 先在“当前 8 个助手会话”里找同职责线程。
2. 通过 `send_message_to_thread` 把新任务发给既有线程。
3. 只有在确实没有对应职责线程，或现有线程已经不可用、必须隔离上下文时，才允许新建线程。
4. 若只是同步规则、催回传、补充验收口径，优先使用 `gpt-5.4 + low`，并继续复用原线程。
5. 若同一助手已经承担该职责，禁止先新建再转发，避免出现重复会话、重复上下文和结论分叉。

复用判断标准：

- 产品/需求、前端、后端、数据库、测试、运维、安全、代码质量八类职责，默认都应复用既有线程。
- 同一职责下的后续任务，默认沿用原线程，除非任务范围已经明显分裂成独立阶段且需要隔离工作树。
- 需要回传主会话的任务，优先在原线程内完成并主动汇报，不要额外开“汇报专用线程”。
- 如果主会话已经记录了对应线程 ID，后续分派必须优先使用该 ID，不能依赖记忆或手工口述。

复用例外：

- 需要不同工作树、不同分支或不同环境隔离时，可以新建线程，但必须说明为什么不能复用原线程。
- 线程已长期失联、状态明显异常、或原线程任务已完成并归档时，可以重新开线程。
- 发生严重上下文污染时，可以先保留原线程，再新建线程，但必须在主会话里说明复用失败原因。

## 协作流程

1. 主会话拆任务，明确目标、输入、输出、验收标准。
2. 专题助手只在自己的边界内给结论和依据。
3. 跨模块冲突由主会话裁决。
4. 数据库迁移、CI 配置、共享前端组件、上线配置必须串行处理。
5. 所有服务均部署在 Docker；只要发生服务重启，后续检查、测试、页面验收或下一步操作前必须等待 3 分钟，待服务完全启动后再继续。
6. 在 PowerShell 中执行命令时，只要参数值包含逗号，尤其是 `-Dxxx=a,b` 这类 Maven/JVM 参数，必须整体加引号；`ParserError` 先按命令语法问题处理，不得直接判定为编译失败、单测失败或业务失败。
7. 专题助手完成任务后必须使用 `send_message_to_thread` 主动向主会话回传；如果当前线程没有该工具或调用失败，则在最终回复末尾输出固定的“给主负责人汇报”区块，作为降级转交载体。
8. 所有助手发现可复用的常见错误问题时，必须按本文档的“常见错误记录模板”追加到本文档，作为后续任务的默认经验库。
9. 助手输出回主会话后，由主会话给最终通过/不通过、阻塞/非阻塞结论。
10. 主会话只要收到专业助手的最新回传，就必须优先读取该回传并继续处理，不要重复输出固定等待话术；只有在明确没有新回传且确实需要等待时，才简短说明正在等待。
11. 主会话撰写新的任务计划书时，默认只能产出“草案”；只有在相关专业助手完成只读研判并主动回传后，主会话才允许将计划书升级为“正式版”或据此进入执行分派。

## 主会话处理规则

1. 主会话收到新回传后，处理顺序固定为：先读回传，再判断是否需要继续分派、督办、验收或裁决。
2. 主会话不把“等待”当成主要输出内容，不在没有新信息时反复播报同一句状态。
3. 任务下发后主会话不要试图主动读取会话。
4. 只有三种情况下主会话才允许主动读取线程或主动拉取结果：
   - 用户明确要求“拉取线程”或“拉取结果”。
   - 专业助手超过约定超时时间仍未回传。
   - 当前主线已阻塞，且必须确认线程状态才能继续。
5. 主会话给出的“通过/不通过”“阻塞/非阻塞”必须基于真实证据，不基于任务已发出或感觉已接近完成。
6. 若专业助手已经明确回传，主会话不得再用固定等待话术覆盖回传内容。
7. 若主会话触发第 4 条中的主动拉取，必须先说明触发原因；禁止静默读取线程。

## 升级到项目总负责人

出现以下情况必须升级：

- 任务跨越两个以上助手职责。
- 需要修改数据库迁移、认证授权、生产配置或 CI 门禁。
- 验收结论与实现报告冲突。
- 存在上线阻断风险、数据风险或回滚风险。
- 助手之间结论不一致。

## 调度规则

1. 主线程固定使用 `gpt-5.5 + medium`。
2. 主会话给任何专业助手分派任务时，必须在任务开头显式写明 `model=...`、`thinking=...`。
3. 不允许沿用旧线程配置，不允许省略，不允许事后解释；若分派时未写明 `model` / `thinking`，该次分派视为无效，必须补发。
### 模型分配

- `gpt-5.5`
  - 跨文件实现
  - 架构设计
  - 疑难排障
  - 安全审计
  - 复杂代码评审
- `gpt-5.4`
  - 常规开发
  - 常规测试
  - 普通后端/数据库分析
  - 运维排查
  - 产品口径整理

补充硬规则：

- 禁止使用 `gpt-5.4-mini`
- 禁止使用 `gpt-5.3-codex-spark`

### 推理强度分配

- `low`
  - 发通知
  - 同步规则
  - 请求回传
  - 简单状态确认
- `medium`
  - 单点分析
  - 普通验收
  - 轻量方案
  - 一两个文件范围的读查
- `high`
  - 跨文件实现
  - 多步骤测试
  - 深入代码审查
  - 数据契约设计
  - 中高复杂度排障
- `max` / `ultra`
  - 只用于关键架构裁决
  - 多模块联动疑难问题
  - 高不确定性根因分析
  - 默认不用，必须有明确必要性

### 8 位助手默认档位

| 助手 | 默认模型 | 默认推理强度 |
|------|----------|--------------|
| 产品/需求负责人 | `gpt-5.4` | `medium` |
| 前端设计师 / 前端架构助手 | `gpt-5.5` | `high` |
| 后端设计师 / 后端架构助手 | `gpt-5.5` | `high` |
| 数据库与迁移负责人 | `gpt-5.4` | `high` |
| 测试验收员 | `gpt-5.4` | `medium` |
| 安全审计员 | `gpt-5.5` | `high` |
| 部署运维负责人 | `gpt-5.4` | `low` |
| 代码质量审计员 | `gpt-5.5` | `high` |

### 动态升降档规则

- 只读分析：先用 `medium`
- 确认进入实现：升到 `high`
- 只是同步消息：降到 `gpt-5.4` + `low`
- 复验失败、根因不清：从 `medium` 升到 `high`
- 涉及架构、安全、跨模块阻塞：必要时升到 `max`
- 没有明确理由，不使用 `ultra`

部署运维负责人默认使用 `gpt-5.4 + low`。适用范围是机械性任务，例如 `docker restart`、前后端重建、前端 Vite 缓存清理、健康检查、端口检查和简单日志确认。以下场景必须升档：普通重启或重建失败一次，升到 `gpt-5.4 + medium`；涉及 Flyway 迁移、权限链路、数据风险、生产环境或回滚方案，升到 `gpt-5.4 + medium`；涉及运行态与源码失配、跨服务故障、缓存/HMR/构建产物根因分析，升到 `gpt-5.5 + medium`。

## 分派原则

1. 任务必须单一、具体、可验收，不能把多个职责、多个页面、多个环境塞给一个助手。
2. 分派任务时必须显式指定 `model` 和 `thinking`，不能依赖旧线程配置自动沿用。
3. 模型和推理强度按任务复杂度匹配，不能所有任务都默认高档位。
4. 优先复用现有线程，能用既有助手解决的事情，不新建会话。
5. 涉及产品、前端、后端、数据库、测试、运维、安全、代码质量时，要先拆职责再分派，不要把边界打穿。
6. 同步规则、催回传、状态确认这类轻量任务，优先使用 `gpt-5.4 + low`。
7. 跨模块联动、疑难排障、关键架构裁决，才升到 `high` 或 `max`。
8. 机械性运维任务优先使用 `gpt-5.4 + low`；失败一次或进入根因排查后再按动态升档规则处理。

### 计划书撰写规则

1. 主会话可以先写计划书草案，用于收敛范围、提出执行顺序和列出待研判问题。
2. 草案不能直接视为正式计划，更不能据此直接进入实现或验收。
3. 正式计划至少需要经过与任务直接相关的专业助手只读研判后才能定稿：
   - 产品/需求类主线：必须有产品/需求负责人回传。
   - 前端实现类主线：必须有前端设计师回传。
   - 测试/验收类主线：必须有测试验收员回传。
   - 涉及权限、数据边界、安全或运维的，还必须补相应助手回传。
4. 主会话在收到足够回传前，必须明确把文档标注为“草案”或在主会话说明其为草案，不得把草案口径当作正式基线对外分派实现。
5. 正式版计划与草案若有差异，主会话必须以专业助手回传为依据更新计划书，再启动执行分派。
6. 若用户明确要求“先出草案”，可直接写草案；但后续进入执行前仍需按本节完成专业助手研判。

### opencode 任务分配规则

`opencode` 只适合边界清楚、结果可验证、失败后容易回收的任务。

- 安排给 `opencode cli` 的任务，模型名称固定为 `jojocode/gpt-5.5`。
- `opencode cli` 任务必须串行执行，不得并行派发多个 `opencode` 任务。
- 主会话调用 `opencode cli` 后必须耐心等待任务完成，不要中途打断；默认超时时长为 30 分钟。
- 当前 shell 是 Windows PowerShell。
- 禁止使用 bash 的 export 前缀。
- 不要写 `export A=B; command`。
- 需要环境变量时用 `$env:A='B'; command`。
- git/mvn/pnpm 普通命令无需加任何 env 前缀，直接执行。
- 若 `opencode` 超过 30 分钟仍未完成，才允许判定为超时并要求输出或收集失败原因；不得在 30 分钟内因短暂无输出而中断。
- 任务必须窄：只给一个目标，不要同时塞多个角色、多个页面、多个环境。
- 输入必须明确：写清路径、页面、命令、验收标准。
- 输出必须可判定：优先要求 `通过/不通过`、`有没有`、`缺什么`、`最小建议`。
- 默认不交给它高风险改动：权限、迁移、上线节奏这类仍走主线程 + 专业助手链路。
- 优先交给它重复性工作：登录、复验、截图、查日志、找差异。
- 先只读，再修改：先让它查清楚，再决定要不要改。
- 任务结束要能收口：必须有结论、失败原因或下一步最小建议。

违例处理：

- 主线程未按本节写明 `model` / `thinking` 的任务分派，一律视为无效分派；必须按原任务目标补发完整分派，不得默认助手继续执行。
- 主线程未满足“主动拉取线程”触发条件时，不得以补看结果、顺手确认、避免等待等理由主动读取线程；已发生时应在主会话中明确说明触发原因和偏差。

适合交给 `opencode` 的任务样例：

```text
任务：先登录后检查 dashboard 四个页签是否可见
范围：http://localhost:5173/dashboard
目标：确认商务经理、项目经理、采购经理、生产经理都显示
验收：给出可见/不可见结论，说明是否需要重新登录
禁止：不要改代码，不要动后端
```

```text
任务：只排查采购经理页面为什么有空列表
范围：dashboard 采购经理页和相关接口
目标：找出空数据来自接口、默认项目还是前端过滤条件
验收：给出空态原因和最小修复建议
禁止：不要扩到总工程师，不要改无关页面
```

```text
任务：只验证 dashboard 相关测试和构建
范围：frontend-admin
目标：确认 unit test 和 build 是否通过
验收：给出通过/不通过和失败用例
禁止：不要顺手改业务代码
```

## 输出格式

每个助手的交付必须包含：

- 结论：建议/不建议，或通过/不通过。
- 依据：代码、文档、命令、日志、截图或 SQL。
- 风险：阻塞/非阻塞。
- 下一步：最小必要动作。

对不确定内容必须标注“需要确认”。

## 回传规则

1. 专题助手完成任务后，必须主动向主会话回传。
2. 首选 `send_message_to_thread`。
3. 如果当前线程没有 `send_message_to_thread` 或调用失败，则在最终回复末尾输出固定的“给主负责人汇报”区块。
4. 回传要尽量短，但必须包含：任务、结论、阻塞/非阻塞、依据、已修改/已验证、剩余风险、需要主负责人裁决。
5. 如果是只读分析或验收，回传里必须明确区分“已验证事实”和“推断结论”。
6. 主会话收到回传后，先读回传，不重复固定等待话术。

命令执行与报错分类补充：

- `PowerShell ParserError`：命令语法问题，先修正命令再重试。
- `mvn`、`pnpm`、`java`、`node` 等工具非零退出：工具执行失败。
- 测试框架断言失败或测试报告失败：真实测试失败。
- PowerShell 中最小安全写法示例：`./mvnw.cmd -q "-Dtest=TestA,TestB" test`

每次任务完成后，助手必须追加以下汇报区块：

```text
【给主负责人汇报】
助手：
任务：
结论：
阻塞/非阻塞：
依据：
已修改/已验证：
剩余风险：
需要主负责人裁决：
```

## 常见错误记录

触发条件：

- 同类错误在不同任务中重复出现。
- 错误已定位到明确原因，且具备可复用的规避或修正方法。
- 错误来自命令执行、环境、Docker、测试口径、常见页面验收偏差或协作流程。

记录要求：

- 记录必须短，直接写事实、原因和处理。
- 不记录一次性偶发噪音。
- 新记录追加在本节末尾，不改写历史记录。

常见错误记录模板：

```text
### [YYYY-MM-DD] [错误标题]
- 现象：
- 原因：
- 处理：
- 预防：
- 适用范围：
```

### [2026-06-28] PowerShell 中 Maven -Dtest 参数被逗号拆分
- 现象：执行 `./mvnw.cmd -q -Dtest=TestA,TestB test` 时，PowerShell 先报 `ParserError`，命令未真正进入 Maven。
- 原因：PowerShell 将逗号当作参数分隔或数组语法处理，未加引号的 `-Dtest=...,...` 会被错误拆分。
- 处理：将整个参数值加引号，例如 `./mvnw.cmd -q "-Dtest=TestA,TestB" test`。
- 预防：在 PowerShell 中，只要参数值包含逗号，尤其是 Maven/JVM `-Dxxx=a,b` 形式，统一整体加引号后再执行。
- 适用范围：Windows PowerShell、Codex/助手在 PowerShell 下执行 Maven、Java、Node 等带逗号参数的命令。

### [2026-06-28] 驾驶舱角色文案与接口语义错位
- 现象：页面把既有 `finance`、`mgmt` 角色显示为“生产经理”“安全经理”，但后端接口、权限码和 VO 仍分别承载财务驾驶舱、管理层驾驶舱数据。
- 原因：只改前端展示标签，未同步后端数据契约、权限命名和聚合口径。
- 处理：角色化重构前先审查 `DashboardRole`、权限码、接口路径、VO 和服务聚合来源，确认语义一致后再改页面。
- 预防：不得用改 tab 文案替代角色数据契约；生产、安全等新角色需要独立契约或明确复用映射说明。
- 适用范围：驾驶舱、权限驱动页面、角色化页面重构。

### [2026-06-28] 驾驶舱新增页签已进前端源码但运行页仍只显示旧角色
- 现象：`/dashboard` 源码已包含“采购经理”“生产经理”页签，但实际页面仍只显示“商务经理”“项目经理”。
- 原因：前端运行容器已加载最新源码，但登录返回的 `permissions` 未包含 `dashboard:purchase-manager:view`、`dashboard:production-manager:view`，且 `SUPER_ADMIN` 不会触发前端 `ADMIN` 全量角色分支。
- 处理：先确认 `5173` 是否来自当前 Docker 前端容器与最新源码挂载，再检查登录返回的 `userInfo.roles`、`userInfo.permissions` 是否已包含二阶段驾驶舱权限；缺权限时不要误判为前端未刷新。
- 预防：驾驶舱角色验收同时核对三项：前端源码 tab 顺序、运行中容器挂载、登录接口返回权限集合。
- 适用范围：驾驶舱、权限驱动 tab 页面、Docker 前端验收。

### [2026-06-29] 业务角色 `data_scope` 写数字会被权限逻辑隐式放大
- 现象：迁移里把 `sys_role.data_scope` 写成 `3` 这类数字值，角色仍能看到租户级项目数据。
- 原因：后端只显式识别 `SELF`、`DEPT`、`CUSTOM`，其他值会落入默认分支并按 `ALL` 处理。
- 处理：新角色种子不要再写数字数据范围，直接写明确字符串；需要租户级可见时写 `ALL`。
- 预防：新增 `sys_role` 种子时同步核对 `PmProjectService` / `ProjectAccessChecker` 的数据范围枚举，不要依赖历史隐式兼容值。
- 适用范围：角色种子、权限迁移、项目列表与驾驶舱项目权限。

### [2026-06-29] Docker dev 后端新增迁移只改源码不会自动生效
- 现象：`db/migration` 下已经有新 SQL，但重启 `cgc-pms-backend-dev` 后权限或种子数据仍不生效。
- 原因：dev 后端容器运行的是 `target/cgc-pms-backend.jar`；新增 migration 只写入源码，未先重打 JAR 时运行包里没有这份 SQL。
- 处理：先在后端容器内执行 `cd /app && MAVEN_CONFIG= mvn -q -DskipTests clean package`，再 `docker restart cgc-pms-backend-dev`，并等待 3 分钟后复验。
- 预防：凡是后端运行态涉及 Flyway、权限种子、配置或 Spring Bean，先确认当前 JAR 是否已重打，再谈重启是否有效。
- 适用范围：Docker dev 后端、Flyway 迁移、权限种子、运行态验收。

### [2026-06-29] Flyway 迁移改版本号后 target 残留旧 SQL 会继续冲突
- 现象：源码里已把迁移从 `V97` 改到 `V98`，但测试启动仍报 `Found more than one migration with version 97`。
- 原因：`backend/target/classes/db/migration*` 里还残留上一次编译生成的旧 `V97` SQL；未 `clean` 时，Flyway 仍会扫描到它。
- 处理：先执行 `clean` 清理 `target`，再重新 `test` / `package`，不要只跑增量编译。
- 预防：凡是重命名、顺延 Flyway 版本号或删除迁移文件后，下一次 Maven 命令统一用 `clean test` 或 `clean package`。
- 适用范围：Spring Boot + Flyway、本仓库后端测试、Docker dev JAR 重打。

### [2026-06-29] 二阶段驾驶舱页面空白不一定是接口错，多数是 demo 业务数据未成链
- 现象：采购经理、生产经理页签已出现，项目下拉也有值，但页面指标和列表全部为 0 或空数组。
- 原因：前端会自动选首个项目，后端也支持 `projectId` 为空时聚合全部 ACTIVE 项目；真正缺的是 `mat_purchase_request`、`mat_purchase_order`、`mat_receipt`、`mat_requisition`、`sub_measure`、`mat_stock` 这类业务示例数据。常见伴随问题是 H2/MySQL demo 种子链不对称，H2 有项目/合同基础数据，MySQL 只有仓库/物料或角色权限。
- 处理：先用 SQL 核对默认演示项目是否存在完整业务数据链；若只是空数据，优先补最小 demo 数据迁移，不先改查询语义或前端交互。
- 预防：新增角色化驾驶舱或业务看板时，权限种子、项目基础数据、业务单据 demo 数据必须一并设计，且 H2/MySQL 同步补齐。
- 适用范围：驾驶舱、演示环境、H2/MySQL 双迁移、依赖项目维度聚合的页面验收。

### [2026-06-29] 主会话收到回传后不要继续重复等待话术
- 现象：主会话在助手已经回传后，仍重复输出“我继续按协同规则等待，不主动去盯线程”之类固定话术。
- 原因：把“等待 30 分钟再查看”误读成持续状态播报，导致同一句话在多个回合里重复出现。
- 处理：主会话只在两种情况下发声，一是收到专业助手新回传后立即处理，二是确实超时且需要重新查看线程时再简短说明。
- 预防：当用户明确要求“安心等待”“不要监控线程”时，默认不做周期性等待播报；若有新回传，先读回传再继续处理。
- 适用范围：多助手协同、线程等待、主会话状态更新。

### [2026-06-29] 多助手协同正式版规则补强
- 现象：协同规则散落在回合消息里，存在复用原则不够显式、分派档位不够明确、主会话等待话术重复等问题。
- 原因：正式规则未统一收束到同一文档，导致协同动作依赖临时口头说明。
- 处理：明确主会话职责、角色复用原则、分派原则、回传规则、主会话处理规则、等待规则，并统一写入本文。
- 预防：后续所有协同相关约定先补本文，再在对话里补充临时约束。
- 适用范围：主线程分派、助手复用、回传督办、长期项目协作。

### [2026-07-01] 主会话新写计划书只能先视为草案
- 现象：主会话直接产出新的主线计划书并按正式版口径继续推进，但尚未经过相关专业助手只读研判。
- 原因：把“主负责人先收敛方向”误当成“正式计划已经成立”，跳过了产品、前端、测试等助手的计划层校准。
- 处理：新增“计划书撰写规则”，默认先写草案；收到相关专业助手回传后，主会话再把计划书升级为正式版并进入执行分派。
- 预防：凡是新主线 / 新增强包 / 新收口任务，先判断当前计划书是草案还是正式版；未完成专业助手研判前，不得把草案当作正式执行依据。
- 适用范围：主线任务计划书、增强包计划书、观察项收口计划书、执行前的任务拆解文档。

### [2026-06-29] Goal 自动续跑时重复输出等待状态
- 现象：执行 `goal` 任务时，主会话在没有新回传的情况下反复输出“仍在等待”“没有新回传”等状态。
- 原因：把 `goal` 自动续跑误当成每次都需要回复，而不是内部继续等待。
- 处理：无新事件时静默等待；只有收到回传、超时检查、出现阻塞或完成验收时才回复。
- 预防：等待型 `goal` 必须区分“目标仍在进行”和“需要对用户输出”。
- 适用范围：`goal` 自动续跑、多助手协同、等待专业助手回传的任务。

### [2026-06-29] Docker 前端 node_modules 卷里的 Vite 缓存导致运行态和源码失配
- 现象：`frontend-admin` 源码已更新，但 `http://localhost:5173/src/pages/dashboard/components/*.vue` 仍返回旧模块或另一版编译结果。
- 原因：前端 dev 容器挂载了独立的 `node_modules` 卷，`/app/node_modules/.vite` 与 `/app/node_modules/.vite-temp` 里的预构建产物会长期保留，普通 `docker restart` 不能保证清空。
- 处理：删除容器内 `/app/node_modules/.vite` 和 `/app/node_modules/.vite-temp`，再重启前端容器，让 Vite 重新生成依赖缓存和模块图。
- 预防：遇到“源码已改、5173 仍下发旧模块”时，先清 Vite 缓存卷，再谈浏览器缓存或页面逻辑。
- 适用范围：Docker dev 前端、Vite HMR、运行态与源码失配排查。


凡涉及驾驶舱摘要字段、摘要列展示、摘要相关验收与口径争议，产品、前端、后端、测试统一引用 [docs/13-驾驶舱摘要字段生成与展示规范.md](D:/projects-test/cgc-pms/docs/13-驾驶舱摘要字段生成与展示规范.md)，不在协作文档重复维护摘要正文。

### [2026-06-30] BusinessException 单参构造不会设置业务错误码
- 现象：测试期望抛出 `RESOURCE_NOT_FOUND`，但实际异常码为 `BUSINESS_ERROR`。
- 原因：`new BusinessException("RESOURCE_NOT_FOUND")` 调用的是 message 单参构造，错误码固定为 `BUSINESS_ERROR`。
- 处理：需要稳定错误码时使用双参构造，例如 `new BusinessException("RESOURCE_NOT_FOUND", "资源不存在")`。
- 预防：新增后端业务拒绝、权限拒绝、资源不存在等场景时，统一检查 `BusinessException` 构造方式。
- 适用范围：后端 Service/Controller 异常处理、权限/租户边界校验、定向测试断言。

### [2026-07-01] SpringBootTest 未切到 local 会误吃 MySQL 迁移链
- 现象：后端定向测试在 H2 启动前就报 `V1__init_system_tables.sql` 的 `SET NAMES utf8mb4` 语法错误。
- 原因：测试类未启用 `local` profile，继承了主配置里的默认 profile，Flyway 扫到了 MySQL `db/migration` 而不是 `db/migration-h2`。
- 处理：需要走 H2 迁移链的后端集成测试统一加 `@ActiveProfiles("local")`。
- 预防：凡是依赖 H2/Flyway demo seed 的 SpringBootTest，先确认 profile 为 `local`，不要依赖默认 profile。
- 适用范围：后端 SpringBootTest、Flyway H2 迁移、workflow/dashboard/demo seed 定向测试。

### [2026-07-01] 仅凭 remark 条件会误判 workflow demo seed 未落库
- 现象：抽查审批 demo seed 时，单看 `wf_instance.remark='V110审批中心权限矩阵workflow-only样本'` 的结果，就把 MySQL 运行态判断为“workflow-only 样本未落库”。
- 原因：把备注字段当成唯一验收口径，未同时核对固定业务编码、发起人和业务类型；终端字符显示异常时更容易放大误判。
- 处理：复验时至少同时核对 `CT-DEMO-WF-PERM-ONLY-001`、`PR-DEMO-WF-PERM-ONLY-001`、`SM-DEMO-WF-PERM-ONLY-001` 三条业务单据，以及 `demo_workflow_only` 发起的 `CONTRACT_APPROVAL`、`PURCHASE_REQUEST`、`SUB_MEASURE` 三条 `wf_instance`。
- 预防：审批 demo seed 不得只用备注字段做存在性判断，统一采用“业务编码 + 发起人/业务类型 + COUNT(*)”的组合抽查口径。
- 适用范围：审批中心 demo seed、workflow 验收 SQL、运行态 MySQL/H2 样本复验。

# 第53条主线 全 V2 公共上下文标准与门禁升格验收报告

> 日期：2026-07-25
>
> 分支：`master`
>
> 对象：`frontend-admin-v2`
>
> 裁决：通过；仅代表当前本地工作区实现和验证，不代表 Git 交付或生产发布

## 1. 结论

M4 公共项目/报告期合同已升格为全 V2 强制标准。唯一公共壳负责具体值、全部值、URL 和跨导航保留；全页面静态门禁止业务页用“请选择项目/请先选择项目”状态标题替代全部项目结果；业务域 E2E 继续使用真实 DTO 证明聚合结果。

M5 供应商招采和采购执行的直接缺陷已关闭：

- 供应商招采在“全部项目”下并发读取 workspace store 已授权项目并合并事件、履约评价和退货只读事实；创建、评价和退货写动作仍要求具体项目。
- 采购申请、订单和验收在“全部项目”下不发送 `projectId`，由既有可选项目查询返回授权聚合事实；写动作仍要求具体项目。

## 2. 门禁

- `tests/unit/global-context-contract.test.ts`：导航路径唯一；扫描全部 V2 页面，禁止项目缺失状态标题；锁定基线和浏览器门禁。
- `e2e/m1-global-context-contract.spec.ts`：验证具体项目、具体报告期、跨业务域保留、全部项目和全部报告期 URL 清理。
- `e2e/m5-supplier-sourcing.spec.ts`：新增全部项目供应商事实样本。
- `e2e/m5-purchase-receipt.spec.ts`：新增全部项目采购/订单/验收样本，并断言聚合请求不携带 `projectId`。
- `scripts/run-migration-ui-gate.mjs` 现有规则自动包含新增 `m1-*` 确定性浏览器门禁。

## 3. 验证

| 验证 | 结果 |
| --- | --- |
| 全 V2 单测 | 42 文件，294/294 通过 |
| 目标上下文、设计系统与 M5 单测 | 58/58 通过 |
| 公共壳上下文 E2E | 1/1 通过 |
| M5 公共壳与供应链真实 DTO E2E | 39/39 通过 |
| TypeScript | `pnpm run type-check` 通过 |
| ESLint | `pnpm run lint:check` 通过 |
| 生产构建 | `pnpm run build` 通过 |
| 差异格式 | `git diff --check` 通过 |
| 本地真实页 | 供应商招采“全部项目”无项目缺失死态；采购申请显示 5 条，项目/报告期均可选择并可恢复“全部” |

## 4. 运行态恢复

验证期间确认本地后端 JVM 于 20:14 启动后，其绑定的 `backend/target/cgc-pms-backend.jar` 在 20:44 被原位覆盖，旧 JVM 出现 `NoClassDefFoundError: ch/qos/logback/classic/spi/ThrowableProxy`；Actuator 仍为 UP，分类 `environment_prerequisite`。

已重启 `cgc-pms-backend-dev` 读取 20:44 新 JAR。应用启动后：

- `/api/auth/userinfo`、`/api/project-context/options`、`/api/dashboard/project-manager`、`/api/alerts` 均为 HTTP 200。
- 项目 10 条、仓库 3 条、采购申请 5 条。
- 应用完成启动后的 `NoClassDefFoundError` / `ThrowableProxy` 日志计数为 0。
- V2 前端因缓存旧模块另行重启，源码、容器和 Playwright 结果恢复一致。

## 5. 边界与收口

- 未修改后端业务代码、数据库、Flyway、权限、租户范围或状态机。
- 未执行 commit、push、PR、合并、发布或生产操作。
- 新增后续项：0；关闭后续项：1（M5 公共壳全部项目失效）；后续项净变化：-1；悬空项：0。

## 6. 供应链设计系统复审整改

本轮先扩门禁并证明红灯，再修页面。原 `design-system.test.ts` 48/48 假绿；扩展后稳定拦截默认 H1、重复主标题、动态分页、实现语言和卡片化密集记录。M5 专项门禁通过目录扫描自动覆盖 `src/pages/supply-chain/*.vue`，拦截内联详情、复合表头和裸状态插值。

整改结果：

- `V2PageState` 默认标题由 H1 降为 H2；403、404、500 和壳加载页显式声明 H1。
- 供应商、采购、仓库、库存、出入库和领退料统一单 H1、21px 标题；导航与页标题一致。
- 仓库卡片列表改为可横向滚动表格；采购移动端不再复制卡片列表。
- 招采追溯、采购详情和领退料链路改为标准详情弹窗；动作弹窗打开时关闭底层详情，消除遮罩叠层。
- 页大小统一 10；分页顺序统一为“上一页—第 N 页—下一页”。
- 状态、招采方式和流水类型改为中文标签；拆分总分/等级、数量/金额和审批/业务状态。
- 项目 ID 输入改为公共项目选择器；清除“服务端裁决/读取/事实”等实现语言。
- 表格行操作使用 44px 触控尺寸；表格保持受控横向滚动。

最终证据：

| 验证 | 结果 |
| --- | --- |
| 设计系统门禁 | 49/49 通过 |
| M5 供应链合同与设计门禁 | 12/12 通过 |
| 全 V2 单测 | 42 文件，294/294 通过 |
| ESLint | 通过 |
| TypeScript 与生产构建 | 206 模块，构建通过 |
| 公共壳与 M5 Playwright | 39/39 通过；三视口与 axe 覆盖 |
| 本地运行态 8 个可见页面 | 每页 1 个可见 H1、21px；无裸英文枚举、页面横向溢出、业务错误告警或控制台 warning/error |

失败分类：旧 5174 容器内容归 `environment_prerequisite`，刷新 Vite 后关闭；旧 E2E 文案选择器归 `ready_issue_config`；详情/动作弹窗遮罩叠层归 `quality_or_security` 并已修复复验。

本轮新增后续项 0、关闭后续项 0、后续项净变化 0、悬空项 0。

# 第50条主线：CGC-PMS V2 导航重构验收报告

日期：2026-07-18

结论：**通过**。阻塞项：**无**。上线属性：本地前端实现与验收通过；未提交、未推送，不构成生产发布授权。

## 1. 验收范围

- 固定八个一级业务域与二级工作区。
- 同对象/连续流程的路由化 Tab。
- 原 URL、route name、权限码、路由守卫与页面文件兼容。
- 全局账户工具与项目、合同、结算对象上下文导航。
- 第48条主线新增“业务单据模板”的 V2 归属。

不包含：业务页面重构、API/后端/数据库变更、新权限码、V2 候选路径改名、生产发布。

## 2. 需求逐项裁决

| 要求                                 | 证据                                                                     | 裁决 |
| ------------------------------------ | ------------------------------------------------------------------------ | ---- |
| 一级菜单固定为 8 项                  | navigation.ts 静态约束、navigation-v2.test.ts、真实页面八域 DOM          | 通过 |
| 二级菜单按业务工作区组织             | 30 个 NavigationWorkspace；侧栏只渲染工作区                              | 通过 |
| 同流程页面使用 Tab                   | 采购、库存、分包、合同成本、资金、访问控制等 54 个描述符                 | 通过 |
| Tab 不超过 5 个                      | navigation-v2.test.ts 遍历全部工作区                                     | 通过 |
| Tab 独立 URL、权限、刷新、历史、深链 | 既有 route + meta.permission；单测与采购运行态切换/返回/刷新             | 通过 |
| 旧路由继续可用                       | 未改旧 route path/name；router.test.ts 与真实旧 URL 通过                 | 通过 |
| 权限不按业务角色硬编码               | permission/adminOnly 统一由 useNavigationAccess 消费；既有管理员边界保留 | 通过 |
| 个人中心不占业务一级菜单             | 全局账户菜单真实显示个人资料、偏好设置、帮助、退出                       | 通过 |
| 详情/新增/编辑不污染侧栏             | 导航描述符仅列工作区 Tab；对象上下文单独渲染                             | 通过 |
| 不删除/移动页面                      | Git diff 无业务页面删除或移动                                            | 通过 |
| 第48条新增模板入口不丢失             | 系统管理 > 系统配置 Tab；路由权限一致性测试                              | 通过 |

## 3. 自动化证据

| 命令                     | 结果                                                |
| ------------------------ | --------------------------------------------------- |
| `pnpm exec vitest run`   | 129 个测试文件、727 项全部通过                      |
| `pnpm lint:check`        | 0 error；25 个 warning 均来自并行中的第48条主线文件 |
| `pnpm build`             | vue-tsc 与 Vite 生产构建通过                        |
| `pnpm check:bundle-size` | 通过；最大 JS chunk 457.14 KiB                      |
| `git diff --check`       | 收口阶段通过                                        |

首轮全量测试与 lint/build 并行时，两项 5 秒测试因资源竞争超时；改为独立运行后通过，分类为 `environment_prerequisite`，不是业务失败。两项旧导航源码断言已按 V2 结构更新。

既有全局分析栏测试曾禁止所有按钮，但库存分析栏已有受 `canTransfer` 权限约束的跨仓调拨专项入口。已将测试契约收窄为：其他分析栏保持纯信息，库存栏只允许已存在的受权调拨动作；未修改业务行为。

## 4. 运行态证据

- 刷新前 5173 仍加载旧七域包，分类为 `environment_prerequisite`；执行前端刷新并等待 180 秒后，前后端均返回 200。
- `/inventory/purchase-request`：八域侧栏、供应链与物资/采购执行、采购申请/采购订单/材料验收真实渲染。
- 点击采购订单后进入 `/purchase/order`；浏览器返回恢复 `/inventory/purchase-request` 和采购申请激活态。
- `/purchase/receipt?projectId=7` 刷新后保持材料验收激活态，query 保留；页面既有列表同步会移除无业务语义的任意 hash，本主线未新增 redirect/alias，也未改变该既有行为。
- `/project/1/overview`：项目对象导航显示项目总览、项目成员、编辑项目和返回项目列表。
- 全局账户菜单显示个人资料、偏好设置、帮助与支持、退出登录；业务侧栏无个人中心一级域。
- 验收浏览器控制台无 warning/error。

## 5. 风险、回滚与 Git

- 权限风险：未变更 route meta、ROUTE_PERMISSION_MAP、guard、按钮权限或后端授权；菜单/Tab 只是可见性层。
- 兼容风险：继续使用全部旧路径；V2 候选路径未启用，不存在 redirect 参数丢失风险。
- 回滚：仅回滚导航描述符、前端壳、工作区/对象导航及测试；不得回滚第48条主线新增模板路由和页面。
- Git：当前分支 `codex/document-generation`；工作区包含第48条主线并行未提交成果。第50条未提交、未推送。

## 6. 非阻塞问题零悬空

- 本轮修复并复验：旧七域运行包、两项旧源码断言、并发超时复验、库存分析栏过时测试契约、材料验收深链核对。
- 超出范围并正式承接：0。
- 证据不足或无明确价值而关闭：任意无业务语义 hash 的跨页面持久化，不作为本轮新缺陷；未来若启用 V2 redirect，按计划书第 5.3 节重新验收。
- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。

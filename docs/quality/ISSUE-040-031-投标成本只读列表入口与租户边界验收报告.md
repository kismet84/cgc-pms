# ISSUE-040-031 投标成本只读列表入口与租户边界验收报告

结论：通过。阻塞：无。上线裁决：本地验证范围内可上线，未执行生产发布或 push。

## 交付与边界

- 新增 `/bid-cost` 只读列表、投标类型与 GET API，支持关键字、状态、刷新和分页；请求不包含 tenantId、body 或写方法。
- 静态路由与项目经营侧栏均绑定 `bid:query`；MySQL/H2 V150 注册单一只读菜单，并为现有 SUPER_ADMIN、ADMIN、COST_MANAGER 角色按存在性绑定。
- 后端生产 Controller、Service、Mapper、实体和表结构未修改；未开放新建、详情、编辑、删除、中标、失标、金额或项目转化能力。

## 验收证据

- Ready lint：通过，无错误或告警；SQL 安全扫描：通过；`git diff --check`：通过。
- 后端：`BidCostControllerTest,BidCostServiceTest` 共24项通过，覆盖401、`bid:query` 正向、无权限403、跨租户记录隐藏、V150 只读菜单绑定及既有 CRUD/中标回归。
- 前端：API、页面和路由专项38项通过；`vue-tsc --noEmit` 与目标 ESLint 通过。API 精确为 `GET /bid-cost`，无 body 与 tenantId。
- 运行态：Issue 工作区独立启动 backend/frontend，连续180秒观察后 `8080/api/actuator/health`、`5173/`、dev-login 三项均返回200，dev-login 最终到达 dashboard。
- 浏览器：最终落点 `http://localhost:5173/bid-cost`，标题“投标成本 - 建筑工程总包项目管理系统”；侧栏、筛选、空态、刷新和分页可见，关键字搜索与重置成功，DOM 无投标写动作，控制台 error/warn=0。
- Reviewer：主线程对权限、租户、只读请求、双数据库迁移与绑定差异 `24559c75feb182aa449857c8cfed53227f550762e46de62b164a3428ab7207b8` 复核，结论 `PASS`，findings=无。

## 失败分类与修正

- 首次后端测试因本地 JWT 密钥仅144位被 JJWT 拒绝，分类为环境前置；注入测试进程专用安全长度密钥后进入真实用例。
- 首轮迁移断言为空，分类为 `ready_issue_config`：Ready 遗漏 H2 V150；补齐双数据库同版本迁移并更新允许范围后通过。
- 浏览器首验发现分页 total 为字符串导致 Vue 警告，属于当前 diff 真实质量问题；前端显式 `Number(...)` 归一化并补测试，最终控制台告警归零。

## 存量问题与后续项

- 依据唯一载体 `docs/backlog/current-issues.json` 及源报告 `docs/quality/ISSUE-037-019-后端接口无前端入口只读盘点与治理裁决验收报告.md`，`A-01-BID-LIST` 已满足验收并移除。
- A-01 守恒更新为：有用户入口236、前端调用但无独立页面58、内部/集成/运维4、需补入口12、待废弃0、需要确认11，共321。
- 新增后续项0；关闭后续项1；后续项净变化-1。未发现需正式承接的悬空问题，产品候选排序未改变。

剩余风险：生产数据库尚未执行 V150，正式发布仍需按既有备份、迁移和回滚流程操作；本轮不包含生产发布。

# 第 21 条主线 P0-1：权限治理阶段收口报告（2026-07-05）

## 1. 结论

- 结论：通过
- 阻塞 / 非阻塞：非阻塞
- 当前判断：`P0-1` 已完成本阶段最小闭环，可进入 `P0-2`，但不等于权限全量治理完成

本报告承接 [第 21 条主线 P0-1：权限逐码裁决报告（2026-07-05）](D:\projects-test\cgc-pms\docs\quality\mainline-21-p0-1-permission-adjudication-2026-07-05.md) 的阻塞裁决，记录 `P0-1a`、`P0-1b`、`P0-1c` 已完成的最小修复和专项收口事实，并明确哪些事项已被裁决为不纳入本轮全量治理。

同时明确：

1. 第 15 至第 20 条主线既有结论不回退，继续按原归档口径有效。
2. `P0-1` 本次收口仅代表阶段阻塞解除，不代表 `authority` 差集、角色菜单绑定和入口 registry 已做全量补齐。

## 2. 承接关系

初始裁决报告的阻塞点包括：

1. `invoice:list`
2. `inventory:transaction:list`
3. `project:list`
4. `submit` 权限绑定
5. `dashboard:view`
6. 高危 `DB-only`
7. `authority` 缺口 `84` 个

本次阶段收口的判断标准不是“把权限体系一次性补齐”，而是确认阻塞 `P0-1` 的最小修复包和定向专项是否已闭环，以及剩余项是否已经被重新裁决为后续专项范围，而不是继续阻塞进入 `P0-2`。

## 3. 已完成闭环

### 3.1 P0-1a：V128 最小修复包

已完成事实：

1. `invoice:list` 与 `project:list` 已清空旧码。
2. `requisition:submit` 已完成注册绑定。
3. `InventoryTransaction` 入口已改为 `inventory:transaction:list`。
4. 写按钮已改为受 `inventory:transaction:add` 控制。
5. 定向测试已通过。

归档判断：

1. 初始裁决中属于最小修复项的旧码和 submit 绑定问题，已按最小范围闭环。
2. 本包只处理阻塞 `P0-1` 的必要项，不扩展到无关权限重构。

### 3.2 P0-1b：dashboard 权限专项最小收口

已完成事实：

1. `dashboard` 路由已支持 `dashboard:view` 或任一 `dashboard:*:view`。
2. `dashboard:view-only` 不再展开全量 tab。
3. 无 `dashboard` 权限时，`/dashboard` 返回 `false`，用于阻断自重定向。
4. 定向测试已通过。

归档判断：

1. 初始裁决中 `dashboard:view` 的阻塞性已由本轮最小专项收口解除。
2. 当前通过标准是“权限边界不再错误放大且无自重定向绕过”，不是“dashboard 权限模型全面重构完成”。

### 3.3 P0-1c：高危 DB-only 四码门禁

已完成事实：

1. DB-only 四码门禁已通过。
2. 已新增 `V129`。
3. 仅清理 `204/project:delete`、`501/system:user:list`、`502/system:role:list`、`503/system:menu:list`。
4. 未改动 `sys_role_menu`。
5. 定向测试已通过。

归档判断：

1. 初始裁决中的高危 `DB-only` 阻塞项，已按最小必要范围闭环。
2. 本轮明确只处理四个高危残留码，不外推为全部历史 `DB-only` 治理完成。

## 4. 不纳入本轮全量治理

### 4.1 authority 84/82 差集

当前裁决：

1. `authority` `84/82` 差集不做本轮全量补齐。
2. 该问题已转入后续分模块专项、角色菜单绑定和入口 registry 范围。
3. 该问题不再作为 `P0-1` 继续阻塞进入 `P0-2` 的条件。

判断说明：

1. 本轮收口目标是解除 `P0-1` 阻塞，而不是一次性完成权限体系全量治理。
2. 对 `authority` 差集的处理口径已经从“当前阻塞项”调整为“后续专项项”，因此不应继续卡住阶段推进。

### 4.2 不回退第 15-20 条主线结论

当前裁决：

1. 第 15 至第 20 条主线既有“通过 / 非阻塞”或已归档结论继续有效。
2. 本次 `P0-1` 阶段收口只覆盖第 21 条主线权限治理范围。
3. 不因本轮权限最小闭环而追溯回退既有主线结论。

## 5. 通过 / 不通过 / 阻塞 / 非阻塞判断

| 项目 | 结论 | 依据 |
| --- | --- | --- |
| 是否通过 | 通过 | `P0-1a`、`P0-1b`、`P0-1c` 已完成最小闭环，且各自定向测试通过 |
| 是否阻塞 | 非阻塞 | 初始裁决中的最小修复项、dashboard 最小专项和高危 DB-only 四码门禁均已关闭 |
| 是否可进入 `P0-2` | 可进入 | `authority 84/82` 差集已明确转入后续专项，不再作为本阶段阻塞条件 |
| 是否等于权限全量治理完成 | 不等于 | 角色菜单绑定、入口 registry、分模块专项仍在后续范围内 |
| 是否回退第 15-20 条主线 | 不回退 | 本次仅解除第 21 条主线 `P0-1` 阶段阻塞，不重判既有归档主线 |

## 6. 剩余风险

1. `authority 84/82` 差集仍然存在，只是当前已被裁决为后续专项，而不是本轮阻塞项。
2. 角色菜单绑定与入口 registry 的全量口径仍需后续分模块治理，当前不能误读为已全面补齐。
3. 本报告只记录既有完成事实和阶段裁决，不新增全链路测试、运行态验证或上线放行推导。

## 7. 不纳入本次收口

1. `authority` 差集全量补齐
2. 角色菜单绑定全量清理
3. 入口 registry 全量收敛
4. 其他未被本轮明确纳入的历史权限码扩面治理
5. 第 15 至第 20 条主线历史结论重判

## 8. 收口建议

1. 按“通过 / 非阻塞 / 可进入 `P0-2`”执行下一阶段排期。
2. 后续如进入 `authority`、角色菜单绑定或入口 registry 专项，需单独建账，不沿用本报告直接判定全量权限治理完成。
3. 若后续专项发现新的越权、漏权或数据侧高风险，应重新开专项裁决，不回写本报告口径。

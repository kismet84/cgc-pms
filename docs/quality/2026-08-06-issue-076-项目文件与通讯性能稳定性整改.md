# 第76条主线：项目文件与通讯性能稳定性整改验收报告

> 验收日期：2026-08-06
> 实施基线：`master@ec1fe310081791ef8463490093b8bc411bcdc780`
> 当前裁决：`G0-G5_LOCAL_PASSED / ISSUE-076-001_CLOSED / GIT_DELIVERY_IN_PROGRESS`

## 1. 结论

第76条三个性能/竞态缺陷及计划索引漂移已在本地闭环。项目文件授权过滤下沉数据库，通讯附件查询数不再随消息条数增长，草稿清理只跳过明确的删除/已发送竞态；权限、租户、项目、附件事务和未知失败上抛边界保持不变。

## 2. G0～G5证据

- `G0`：锁定`master@ec1fe3100`、任务自有治理差异和本地运行态；`codemap.lock`过期，代码修改前已重生成三件套。
- `G1`：冻结数据库可见性谓词、精确`COUNT + LIMIT/OFFSET`、单页附件批量、有界ID、两类预期竞态和未知失败上抛不变量。
- `G2`：H2及本地MySQL现有表、索引和Flyway V284可承载；未新增或修改migration、依赖、缓存、锁平台。
- `G3`：文件`COUNT`与page共用同一`QueryParts`；允许来源只做一次权限能力判定并生成服务端常量`EXISTS`分支；附件按最多100条消息一次查询；草稿竞态按单条事务隔离。
- `G4`：`/api/actuator/health`为`UP`；dev-login、`/api/project-files`、会话与消息API均返回成功；文件中心及站内通讯页面DOM正常，通讯页2条消息，浏览器控制台error/warning均为0。
- `G5`：依赖门、定向/全量测试、真实MySQL并发、治理契约、JSON、代码地图、独立复核、风险/回滚和零悬空已齐全；Git合并与post-merge保持独立证据层。

## 3. 实施结果

- `FILE-PERF-01`：移除无界候选读取、BUSINESS逐条授权和全量ID `IN`；MANAGED/BUSINESS在数据库完成租户、项目范围、来源存在、来源项目一致及来源权限交集过滤。
- `COMM-ATTACH-01`：1、50、100条消息均固定为成员、消息、附件最多3次SQL；无附件返回空列表，附件保持`created_at,id`顺序。
- `COMM-DRAFT-01`：`COMMUNICATION_MESSAGE_NOT_FOUND`、`COMMUNICATION_MESSAGE_IMMUTABLE`记录后继续；未知业务异常、文件删除异常及运行异常仍失败退出。
- `GOV-01`：计划索引第73～75条改为PR #393已合并并完成post-merge，未重开第72条或REL项。

## 4. 自动化与运行态

- 编译：`mvnw -DskipTests compile`通过。
- 定向：最终52项通过；项目文件分页专项2项覆盖1万条、首页、第100/101页、精确ID、跨租户、删除、缺失、QS项目不一致及真实Flyway H2 SQL。
- 全量：325份Surefire报告，2543项，0失败，0错误，16跳过；JaCoCo报告及`cgc-pms-backend.jar`生成。
- 本地MySQL：`CommunicationMySqlConcurrencyTest` 2项通过，覆盖删除/已发送竞态和后续草稿继续处理。
- 依赖：Trivy Java依赖HIGH/CRITICAL为0；镜像库下载失败后改用官方数据库镜像预置缓存复验通过。
- 治理：HighRisk主线计划门、Codex任务执行策略、`current-issues.json`解析和`git diff --check`通过。
- 独立复核：初查发现`TECH_RFI_RESPONSE`不存在`deleted_flag`、QS来源/父对象项目一致性及测试尾页证据不足；本轮修正并复验，最终无P0～P3遗留。

## 5. 失败分类

- `code_defect`：独立复核发现的TECH RFI字段误用、QS项目关联不足和尾页断言不足，均已修复并复验。
- `environment_prerequisite`：真实MySQL首轮缺测试JWT配置；Trivy数据库下载两次EOF。使用本地测试配置及官方数据库镜像预置缓存后通过。
- `tool_config`：PowerShell中的`bash`命中缺失WSL，改用Git Bash。
- `tool_invocation`：浏览器等待条件和页面内沙箱fetch不受支持，改用`domcontentloaded`与宿主只读HTTP会话；浏览器DOM/console仍独立通过。
- 当前无`product_defect`、`test_fixture`、`infrastructure`、`flaky`或`test_regression`遗留。

## 6. 风险、回滚与零悬空

- 剩余风险：来源权限分支仍随既有业务类型增加而显式维护；这是fail-closed边界，不影响当前已登记类型。新增类型时须同时补谓词与测试。
- 回滚：只撤销第76条服务、测试、代码地图和治理差异；不修改已应用migration，不覆盖其他任务成果。文件分页异常时回退本批谓词并保持问题打开；通讯异常时分别回退批量组装或竞态分类，保留既有权限、行锁和事务边界。
- 项目仅有本地环境；非本地部署、生产数据库、容量及恢复证据不适用，也未发起。
- 计划周期新增1、关闭1、净变化`0`；本轮新增后续项0、关闭后续项1、净变化`-1`；无无载体遗留项。

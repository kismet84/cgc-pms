# 本地演示库业务数据清理验收报告

日期：2026-08-03

结论：通过

目标库：`127.0.0.1:3307/cgc_pms_demo_ui_20260728`

## G0 基线锁定

- 分支：`codex/mainline-66-rule-governance`。
- 工作区存在既有未提交修改；本次仅处理数据维护后端、前端、脚本、测试和本报告。
- 目标限定为本机 `dev/test/demo` 数据库；已确认 `.codex-autopilot/ALLOW_TEST_DATA_RESET`。
- 不包含 Git 提交、推送、PR、生产或远程数据库操作。
- 执行前重新识别 200 张基础表、2 个视图；策略为 25 张保留表、175 张清理表。

## G1 契约完整

- 唯一策略清单：`backend/src/main/resources/data-maintenance-table-policy.json`。
- 每张基础表必须且只能属于 `RETAIN` 或 `CLEAR`；未知表、缺失表、重复表均阻断。
- 视图不删除，仅识别并验证：`v_business_audit_event`、`v_reconciliation_issue`。
- 删除浏览器高危入口 `DELETE /system/clear-database`；保留超级管理员只读预览 `GET /system/data-maintenance/preview`。
- `/system/data` 仅展示数据库、策略指纹、阻塞原因、保留分组、清理计数、文件数、忽略视图和宿主机命令。

## G2 数据、备份与恢复

- 主清理运行：`D:\backups\cgc-pms\data-maintenance\20260803_191038\run-manifest.json`。
- 主数据库备份 SHA-256：`66946f327ae1cc2052e0bc742fd9fcdf350eaf66430f482b87de7ec43d11f581`。
- 最终审计归零运行：`D:\backups\cgc-pms\data-maintenance\20260803_192353\run-manifest.json`。
- 最终数据库备份 SHA-256：`76290594dffd9fa1aaaf5c0a4ec5d1fe6edf1984e2ae6ade18c119a150c5b99a`。
- 每次执行均先恢复到独立验证库并核对保留表摘要，再进入删除阶段。
- 首次主清理删除 16,936 条业务数据和 487 条文件元数据；精确对应 MinIO 对象删除后为 0。
- 最终回读：175 张清理表共 0 行、非零清理表 0 张、目标 MinIO 对象 0 个、2 个视图仍存在。

## G3 服务端与事务闭环

- `scripts/database/clear-business-data.ps1` 默认只预览；执行必须同时提供 `-Execute`、完整库名确认和预览指纹。
- 脚本校验本机地址、环境名称、marker、目标容器、数据库名和表策略完整性。
- 数据库清理使用单事务 `DELETE`；未使用 `TRUNCATE`。
- 文件删除失败时数据库不变并恢复对象；SQL 失败时事务回滚并恢复对象。
- 保留表执行前后逐表内容摘要一致；运行清单不记录凭据。

最终保留基线：

| 数据 | 行数 |
| --- | ---: |
| 有效用户 | 12 |
| 软删除用户 | 22 |
| 用户角色绑定 | 13 |
| 角色 | 13 |
| 菜单/权限 | 280 |
| 材料分类 / 材料 | 66 / 2,107 |
| 成本科目 | 84 |
| 审批模板 / 节点 | 27 / 71 |
| 字典分组 / 类型 / 数据 | 8 / 26 / 134 |
| 单据模板 / 版本 / 默认绑定 | 3 / 4 / 3 |

## G4 运行态与浏览器

- 后端健康：容器 `healthy`，`/api/actuator/health` 返回 `UP`。
- 前端健康：容器 `healthy`，`http://127.0.0.1:5173/` 返回 200。
- 数据维护页显示“只读预览”和策略指纹；旧确认码、确认复选框、清理按钮均不存在。
- 用户、角色、权限、字典、材料、成本科目、审批流程和单据模板页面可读取。
- 单据模板页面读取到采购申请模板 1 个、已发布版本 2 个。
- 项目、合同、采购申请、库存、财务页面为空；资金金额统一显示 `0.00`。
- 本轮浏览器验收控制台错误：0。
- 浏览器验收产生的 3 条审计记录已在最终运行中再次清理，最终数据库回读仍为 0。

## G5 自动化与收口

- PowerShell：解析、自检、预览、真实执行和回读通过。
- 后端聚焦测试：4/4 通过；`mvnw.cmd -DskipTests package` 通过。
- 前端聚焦单测：7/7 通过；类型检查、生产构建通过。
- 前端 lint：0 错误、9 条既有非本任务格式警告。
- 前端 E2E：`m7-system.spec.ts` 4/4 通过，覆盖权限拒绝、只读契约、无破坏性请求和三视口。

执行中发现并关闭：

1. MinIO 路径末尾 CR 导致首次备份失败；对象和数据库均未删除。已规范化路径并复验。
2. PowerShell 嵌套数组生成 `System.Object[]` SQL；事务在首条删除前失败，487 个对象已全部恢复，数据库未变。已扁平化 SQL 并加入自检。
3. Spring 多构造器无法选择注入构造器，导致后端启动失败。已显式标记 `ObjectMapper` 构造器并通过真实启动验证。

回滚条件：任一保留表摘要变化、清理表重新出现业务数据、文件恢复失败或目标库识别不一致。回滚来源为对应运行目录内已验证的 MySQL 备份和 MinIO 清单/对象备份。

非阻塞后续项：本机 `deploy/.env` 尚未配置 Compose 当前要求的 `MINIO_ACCESS_KEY` / `MINIO_SECRET_KEY`，导致 `python scripts/rebuild.py backend` 在重启阶段被 Compose 插值拒绝；本轮已用现有容器直接重启并完成健康及浏览器验收。优先级 P2；因凭据配置不属于本次数据清理范围而延期。验收标准：补齐本机非生产服务账号凭据后，配置命令可完成重启且后端健康为 `UP`。

新增后续项：1；关闭后续项：3；后续项净变化：-2；所有遗留项均已有正式载体。

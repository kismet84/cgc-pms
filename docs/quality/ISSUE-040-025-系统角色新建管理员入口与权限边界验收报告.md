# ISSUE-040-025 系统角色新建管理员入口与权限边界验收报告

## 结论

- 最终结论：通过。
- 阻塞：无。
- 独立复核：Reviewer 对实现差异哈希 `6bec1258709186652b431968716e2f8ec99c822d24c4bf812a2a2d6fa3f3ac59` 给出 `PASS`，findings=无。
- 上线边界：本轮只在本地 dev/test 验收并本地提交，不连接生产数据库、不发布生产、不 push。

## 正式交付物

- 前端系统 API 新增类型化角色创建调用，只发送 `roleCode`、`roleName`、`status`、`dataScope`，精确调用 `POST /system/roles`。
- 既有 admin-only 角色页新增 ADMIN/SUPER_ADMIN 可见的新建交互，覆盖必填/长度校验、失败保留表单、成功关闭并刷新列表；普通用户即使持有 `system:role:add` 也不显示入口。
- 后端创建服务清除客户端 ID，以认证上下文覆盖 tenantId，固定 `roleType=CUSTOM`、`roleLevel=2`，默认 `status=ENABLE`、`dataScope=SELF`；拒绝保留编码、SYSTEM、roleLevel 0/1 与非法枚举。
- 创建路径不写 `sys_role_menu`，后续菜单授权继续走既有独立入口及其高危权限、自角色、超级管理员保护。

## 自动化验收证据

- Ready lint：通过，Ready 内容哈希 `23cb54af682852e05e0ce6a9c8120b3791c352d848b8c8a2f99a0ca0a491e568`。
- 后端 `SysRoleControllerTest,SysRoleServiceTest`：38项通过，0失败、0错误、0跳过。
- 前端 API/页面专项：2个文件、26项通过。
- `pnpm type-check`、目标 ESLint、`git diff --check`：通过。
- 正负样本覆盖未登录401、无权限403、ADMIN/SUPER_ADMIN 与 `system:role:add` 合法授权、客户端 ID/tenantId 覆盖无效、跨租户不可见、保留编码/SYSTEM/roleLevel 0/1 拒绝、非法枚举拒绝、重复编码无部分写入及既有菜单授权回归。

## 运行态验收与清理

- `http://localhost:8080/api/actuator/health`、`http://localhost:5173/`、dev-login 三项 health gate 均返回200，最终进入 `/dashboard`。
- 从 dev-login 进入 `/system/roles`，创建唯一角色 `AUTOPILOT_CANARY_20260714_1655`；列表回读成功。
- 对相同编码重复提交时弹窗保持打开，角色编码与名称输入保留，证明失败不伪报成功且不清空表单。
- 本地 dev 数据源为 `jdbc:mysql://localhost:3307/cgc_pms`，且 `.codex-autopilot/ALLOW_TEST_DATA_RESET` 存在，满足清理三重门禁。
- 数据库读回：tenant_id=0、role_type=CUSTOM、status=ENABLE、data_scope=SELF、role_level=2、deleted_flag=0、角色数1、菜单关系数0。
- 按固定角色 ID `2076953624149557250` 精确删除1条角色、0条菜单关系；复查角色与菜单关系残留均为0，页面刷新后无该测试角色。

## Reviewer 与证据修复

- 首轮 Reviewer 发现验证摘要仍绑定旧恢复哈希 `318914...`，且复核差异混入上轮“不通过”的提前收口草稿；该问题归类为 `tool_config/证据绑定与阶段边界错误`，不是业务实现失败。
- 已撤出提前生成的报告和治理草稿，重新执行全部正式验证，以实现差异哈希 `6bec1258...` 重新生成 request、final.diff 与验证摘要；三者哈希一致。
- 第二轮独立 Reviewer 对同一 Issue、同一实现差异给出 `PASS`，findings=无；PASS 后才生成本正式报告和治理回写，避免用收口报告自证收口报告。

## 发现项与后续统计

- 本轮修复并复验：角色新建入口、后端安全收敛、前后端测试、浏览器与数据库边界、证据绑定与阶段边界错误。
- 超出当前范围并正式承接：0。
- 证据不足或无明确价值而关闭：0。
- 新增后续项：0
- 关闭后续项：1
- 后续项净变化：-1
- 关闭项为唯一存量问题 `A-01-ROLE-CREATE`；未产生悬空问题，产品候选排序不变。

## 回滚

- 回退实施提交与收口提交即可恢复前端创建 API、类型、角色页交互、后端创建收敛、测试和治理载体。
- 无 schema 或 migration；测试角色已精确清理，无数据回滚。

<!-- AUTOPILOT-FACTS:START -->
## AutoPilot 自动事实

- Issue：ISSUE-040-025
- Ready 哈希：23cb54af682852e05e0ce6a9c8120b3791c352d848b8c8a2f99a0ca0a491e568
- 实施提交：005fb9bcb7652217a0ec29a69f8258dd1f7c54b1
- 验证差异哈希：6bec1258709186652b431968716e2f8ec99c822d24c4bf812a2a2d6fa3f3ac59
- Evidence manifest：5fa43476cbe4b0920bd01bcfc89c6a2043a38675d55b9911739ce258ac7a1052
- Reviewer：required=True; decision=PASS
- 后续项：added=0; closed=1; net=-1
- 指标：{"closeoutDispatchCount":1,"contextBaseBuildCount":1,"contextDeltaBuildCount":2,"executorInvocationCount":1,"implementationDispatchCount":1,"inputTokens":null,"outputTokens":null,"phaseDurationsSeconds":{"CLOSING":0,"IMPLEMENTED":210,"IMPLEMENTING":1058,"REPAIRING":3514,"REVIEWED":17,"REVIEWING":339,"VALIDATED":53,"VALIDATING":248},"phaseRestartCount":0,"plannerCandidateRefs":[],"plannerInvocationCount":0,"repairDispatchCount":1,"reportProjectionCount":1,"reviewDispatchCount":2,"reviewerInvocationCount":2,"runResumeCount":0,"tokenUsageStatus":"not_available","totalTokens":null,"validationDispatchCount":2,"validationExecutedCount":2,"validationReusedCount":0,"wallClockSeconds":5439}
- 控制面指纹：3f041d9d6dec8fdbb6e1bf2f9238597edf6cc1174a5c79031a85dded6c1fc3bd
- PreCloseout Facts：a92fac5e31356d0340bd516d79e94bd8a43b3997d7d4da2a76e4c74600d0f0f3
<!-- AUTOPILOT-FACTS:END -->

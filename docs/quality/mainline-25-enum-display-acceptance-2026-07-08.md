# 第25条主线 M6 正式验收与收口报告（2026-07-08）

## 1. 总体结论

- 结论：通过
- 阻塞 / 非阻塞：非阻塞
- 是否建议主线程收口：建议收口
- 裁决口径：本报告只覆盖“全站用户可见业务枚举中文展示治理”主线范围内的正式验收归档，不新增代码、测试、运行态或 Git 动作。

依据：

1. 对计划书台账中的 39 个静态业务页面完成三次复扫后，重点页面用户可见业务枚举英文码均已清零。
2. 首轮与二轮新增问题 `ACCEPTED`、`PURCHASE` 已在第三次复扫中确认闭环。
3. 当前剩余事项仅为计划书已声明的非阻塞边界，不影响本主线“用户可见业务枚举不再直出英文”的收口结论。

## 2. 范围与非目标

### 2.1 本次归档范围

1. 计划书：`docs/plans/第25条主线-全站业务枚举中文展示治理任务计划书.md`
2. 首轮巡检：`.agent-runtime/reports/mainline-25-m1m2-enum-browser-audit.md`
3. 三次复扫：
   - `.agent-runtime/reports/mainline-25-m5-final-rescan-report.md`
   - `.agent-runtime/reports/mainline-25-m5-final-rescan-report-2.md`
   - `.agent-runtime/reports/mainline-25-m5-final-rescan-report-3.md`

### 2.2 非目标与排除口径

以下内容不纳入本报告“已治理完成”承诺：

1. 动态详情 / 编辑路由：如 `/contract/:id`、`/project/:projectId/overview`、`/approval/:instanceId` 等需要业务 ID 的页面。
2. 编号、模板编码、角色码、权限码、路径、技术日志、英文专名、测试数据片段。
3. 后端数据治理、数据库字典补数、全站 i18n 改造、无关英文文案清零。
4. `/cost/subject` 中 `CONTRACT_ASSET` 等成本科目编码是否保留中文并列展示后的编码，继续按产品确认项处理。

## 3. 阶段执行摘要

| 阶段 | 当前结论 | 阻塞性 | 证据摘要 |
| --- | --- | --- | --- |
| M1 路由清单建立 | 通过 | 非阻塞 | 已整理 39 个静态业务页、8 个动态详情/编辑路由，明确优先级和巡检台账 |
| M2 浏览器首轮巡检 | 不通过 | 阻塞 | 首轮确认 4 个 P0 页面存在业务枚举英文直出：`/alert`、`/purchase/order`、`/subcontract/measure`、`/inventory/purchase-request` |
| M3/M4 最小修复执行 | 已完成 | 非阻塞 | 修复围绕既有 label helper / fallback 收口，不扩展为 i18n 或后端重构 |
| M5 第一次复扫 | 不通过 | 阻塞 | 原 4 个确认缺陷页已闭环，但新增 `/purchase/receipt` 质量状态 `ACCEPTED` |
| M5 第二次复扫 | 不通过 | 阻塞 | `ACCEPTED` 已闭环，但新增 `/purchase/order` 订单类型 `PURCHASE` |
| M5 第三次复扫 | 通过 | 非阻塞 | 39 个静态业务页复扫完成，重点页面 0 命中，新增确认缺陷 0 个 |
| M6 正式归档 | 通过 | 非阻塞 | 本报告完成正式收口归档 |

## 4. 已修复问题清单

本主线最终闭环的问题如下：

1. `/alert`
   - KPI 卡片与告警表格中的 `OPEN`、`ARCHIVED`、`PROCESSED`、`LOW`、`MEDIUM`、`HIGH`
   - 当前第三次复扫证据：显示“待处理”“已归档”“低/中/高”等中文
2. `/purchase/order`
   - 订单状态中的 `IN_TRANSIT`、`APPROVED`、`PARTIAL`
   - 二次复扫新增的订单类型 `PURCHASE`
   - 当前第三次复扫证据：显示“运输中”“已审批”“部分入库”“采购订单”
3. `/subcontract/measure`
   - 状态中的 `REJECTED`、`APPROVED`
   - 当前第三次复扫证据：显示“已驳回”“已确认”“审批中”
4. `/inventory/purchase-request`
   - 业务状态 / 审批状态中的 `REJECTED`
   - 当前第三次复扫证据：显示“已驳回”“已通过”“已转PO”
5. `/purchase/receipt`
   - 第一次复扫新增的质量状态 `ACCEPTED`
   - 当前第三次复扫证据：显示“让步接收”“待检验”“部分合格”“合格”

## 5. 三次复扫证据摘要

### 5.1 首轮复扫结论

1. 原 M1/M2 4 个确认缺陷页目标英文码已全部消失。
2. 39 个静态业务页宽口径复扫新增发现 `/purchase/receipt` 的 `ACCEPTED`。
3. 结论：不通过 / 阻塞。

### 5.2 二次复扫结论

1. `/purchase/receipt` 的 `ACCEPTED` 已 0 命中，并显示中文“让步接收”。
2. 39 个静态业务页继续复扫时，新增发现 `/purchase/order` 订单类型 `PURCHASE`。
3. 结论：不通过 / 阻塞。

### 5.3 三次复扫结论

1. 39 个静态业务页全部可访问，复扫覆盖完成。
2. 5 个重点页面目标英文码均为 0 命中：
   - `/alert`
   - `/purchase/order`
   - `/subcontract/measure`
   - `/inventory/purchase-request`
   - `/purchase/receipt`
3. `/purchase/receipt` 的 `ACCEPTED` 后续新增问题已闭环。
4. `/purchase/order` 的 `PURCHASE` 后续新增问题已闭环。
5. 宽口径候选复核未发现新增确认缺陷。
6. 结论：通过 / 非阻塞。

### 5.4 关键收口指标

| 指标 | 结果 |
| --- | --- |
| 静态业务页复扫覆盖数 | 39 |
| 重点页面目标码命中 | 0 |
| `ACCEPTED` 后续问题 | 已闭环 |
| `PURCHASE` 后续问题 | 已闭环 |
| 第三次复扫新增确认问题 | 0 |

## 6. 剩余非阻塞项

1. 动态详情 / 编辑路由未纳入本轮 39 个静态业务页复扫，原因是需要真实业务 ID；建议后续在对应主线或联调窗口中做定点补验。
2. `/cost/subject` 的 `CONTRACT_ASSET`、`CONTRACT_LIABILITY`、`SETTLE` 继续按“成本科目编码并列中文名称”的产品确认项处理；当前不作为本主线阻塞缺陷。

## 7. 上线 / 继续推进建议

### 7.1 当前建议

1. 就第25条主线当前目标而言，建议主线程按“通过 / 非阻塞”收口。
2. 不建议把本主线继续扩写为全站 i18n、后端字典治理或“所有英文清零”专项。

### 7.2 后续建议

1. 若后续业务要求验收动态详情 / 编辑页，应另开补验任务，使用真实业务 ID 做定点复扫。
2. 若产品决定成本科目编码不应在 `/cost/subject` 可见展示，应以产品口径单独开小任务处理，不回退本报告当前通过结论。

## 8. 模型分配经验简记

1. M1 路由清单与 M6 归档使用 `gpt-5.4 + medium` 是合理的，任务以证据整理、边界表达和正式裁决文档为主。
2. M2/M5 浏览器巡检与复扫需要跨页面比对真实 DOM 文本、排除误报并形成阻塞判断，保持高一档验收模型更稳妥。
3. 本主线的阻塞并非来自复杂架构问题，而是来自“每轮复扫都可能出现新的用户可见枚举直出点”，因此验收阶段不能机械降档。

## 9. 归档说明

1. 本报告为正式验收 / 收口归档，只新增 `docs/quality` 下报告文件。
2. 本报告不回退既有改动，不修改业务代码、配置、脚本、运行环境，不提交 Git、不推送。
3. `.agent-runtime/reports/` 中的阶段材料继续保留为原始证据来源，本报告仅提炼正式裁决所需摘要。

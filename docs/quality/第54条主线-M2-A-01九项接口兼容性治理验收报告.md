# 第54条主线 M2：A-01九项接口兼容性治理验收报告

日期：2026-07-20
分支：`codex/mainline-54-25-issue-governance`
结论：**M2通过；A-01父项及9个叶子项关闭。不上线。**

## 1. 统一裁决

| 问题键 | 接口 | 裁决 | 关键依据 |
| --- | --- | --- | --- |
| A-01-CONTRACT-ITEM-CREATE | `POST /contracts/{id}/items` | 保留集成兼容API并修复 | 与批量保存同域；新增补齐草稿合同门禁 |
| A-01-CONTRACT-ITEM-UPDATE | `PUT /contracts/{id}/items/{itemId}` | 保留集成兼容API | 校验租户、合同归属、草稿状态和明细归属 |
| A-01-CONTRACT-ITEM-DELETE | `DELETE /contracts/{id}/items/{itemId}` | 保留集成兼容API | 校验租户、合同归属、草稿状态和明细归属 |
| A-01-CONTRACT-TERM-CREATE | `POST /contracts/{id}/payment-terms` | 保留集成兼容API并修复 | 与批量保存同域；新增补齐草稿合同门禁 |
| A-01-CONTRACT-TERM-UPDATE | `PUT /contracts/{id}/payment-terms/{termId}` | 保留集成兼容API | 校验租户、合同归属、草稿状态和条款归属 |
| A-01-CONTRACT-TERM-DELETE | `DELETE /contracts/{id}/payment-terms/{termId}` | 保留集成兼容API | 校验租户、合同归属、草稿状态和条款归属 |
| A-01-PAY-RECORD-DETAIL | `GET /pay-records/{id}` | 保留内部复用和集成查询 | `PaymentReversalService`复用；独立查询权限与跨租户拒绝已验证 |
| A-01-PROJECT-MEMBER-DETAIL | `GET /projects/{projectId}/members/{id}` | 保留安全详情查询 | 服务校验项目访问、租户、项目与成员归属；供粒度化编辑/集成使用 |
| A-01-SETTLEMENT-SOURCES | `GET /settlements/{id}/sources` | 保留结算追溯查询 | 兼容服务调用；按当前租户与合同过滤已确认变更、计量和成功付款 |

未检出前端同方法调用不等于接口可删除。现有Legacy合同编辑页继续使用批量全量保存；本阶段不新增重复单条编辑UI，也不删除可能存在外部客户端的兼容API。

## 2. 根因修复

- `CtContractItemService.create()`改为复用`requireDraftParentContract()`。
- `CtContractPaymentTermService.create()`改为复用`requireDraftParentContract()`。
- 两处新增各增加一个“已审批/非草稿合同拒绝新增”回归测试，错误码统一为`CONTRACT_NOT_EDITABLE`。
- 未新增接口、依赖、抽象、迁移或权限码。

## 3. 验证证据

```text
backend/mvnw.cmd -Dtest=CtContractItemServiceTest,CtContractPaymentTermServiceTest test
结果：35项通过，失败0，错误0。

backend/mvnw.cmd -Dtest=PayRecordControllerTest,TenantIsolationTest,ProjectMemberServiceTest,StlSettlementQueryServiceTest test
结果：55项通过，失败0，错误0。
```

覆盖：合同清单/条款CRUD、非草稿状态、跨租户付款详情、付款Controller权限契约、项目成员项目访问/租户/归属、结算来源过滤与不存在资源拒绝。

## 4. 台账收口

- A-01的9个叶子项均取得唯一裁决并完成必要修复、验证。
- A-01聚合父项随全部叶子项关闭。
- `docs/backlog/current-issues.json`移除父项及9个叶子项；历史裁决写入`docs/backlog/done-issues.md`。
- 本阶段新增后续项0，关闭后续项10，后续项净变化`-10`。

最终裁决：**通过。接口继续受现有权限、租户和业务状态门禁保护；不构成生产发布证据。**

# 第53条主线 M4 商务合约全量退出门验收报告

> Issue：`ISSUE-053-023`
>
> 日期：2026-07-24
>
> 验收基线：`master` / `d5b93242fa64b429f4658cb48d5c4090e1e59578` 加本 Issue 最终工作区差异
>
> 裁决：**通过**

## 1. 结论

M4 商务合约阶段通过本地 dev/test 全量退出门。18 条目标路由全部为真实 Clean-room V2 页面或受控确定性重定向，路由台账保持 `LEGACY_ONLY=50 / V2_ACCEPTED=37 / V2_SOURCE_AVAILABLE=0`；5 条成本科目路由继续归属 M7。

合同、变更/投标、目标成本、成本台账/汇总/动态利润、预算及产值计量的既有切片证据经后端全量、测试顺序、V2 全量静态门和五组 M4 Playwright 回归重新验证。未发现新的业务状态机、金额、权限、租户/项目范围、附件、CAS、幂等或并发缺陷。

本报告仅证明本地实现与测试验收通过，不等同于同 SHA CI、正式入口切换、Legacy 退役或生产发布。

## 2. 本轮根因修复

1. 后端全量首次执行在 `SysDictDataControllerTest` 加载上下文时发生 `Java heap space`，随后共享测试数据源关闭并引发 6 项 M4 级联错误。单独复验相关 23 项全部通过，确认不是业务逻辑失败。Surefire 单 fork 堆上限由 1 GB 调整为 2 GB；随后后端全量与顺序复验均通过。分类：`quality_or_security/DELIVERY_GATE_OMISSION`。
2. demo 验证器仍按已废弃的 `M52-PM-*`、`M52-OWNER-SUB-*` 编号统计，和当前 `PM-*`、`OMS-*` 标准编号不一致；同时用项目全部进度快照数量校验固定种子，受到合法追加快照干扰。验证器改为核对冻结业务编号和固定种子 ID，复验 `passed=true`。分类：`quality_or_security/DELIVERY_GATE_OMISSION`。
3. 初版 Ready demo 命令缺少必填 `Environment`、`Database` 参数，已修正并通过 Ready lint。分类：`ready_issue_config/READY_CONTRACT_INVALID`。

## 3. 路由退出门

| 范围 | 路由数 | 结果 |
| --- | ---: | --- |
| 合同 | 5 | `V2_ACCEPTED` |
| 变更与投标成本 | 3 | `V2_ACCEPTED` |
| 目标成本 | 4 | `V2_ACCEPTED` |
| 成本台账、汇总与动态利润 | 4 | `V2_ACCEPTED` |
| 预算与产值计量 | 2 | `V2_ACCEPTED` |
| 合计 | 18 | 全部通过 |

台账生成器确认 87 个命名路由、73 个 Legacy 视图引用、65 个独立 Legacy 页面；最终状态为 `50/37/0`。M4 路由无 `ShellPlaceholderPage`，成本科目中心 5 路由仍为 `LEGACY_ONLY`。

## 4. 验证证据

| 验证 | 结果 |
| --- | --- |
| Ready lint | 通过；高风险、回归证明契约有效 |
| 后端全量 `mvnw.cmd -C verify` | 263 suites、2173 tests、0 failures、0 errors、3 skipped；通过 |
| 后端反向顺序单线程复验 | 12 suites、159 tests、0 failures、0 errors、0 skipped；通过 |
| OOM 首错与 M4 级联目标复验 | 23 tests、0 failures、0 errors；通过 |
| V2 全量单测 | 36 files、264 tests；通过 |
| 契约类型、V2 类型、ESLint | 通过 |
| Clean-room 边界 | 141 个 V2 文件、13 个契约文件；通过 |
| 路由台账 | 87 routes、73 view entries、65 unique views；通过 |
| 生产构建、包体 | 构建通过；30 个 JS assets 通过包体门 |
| M4 Playwright | 5 组、16/16 通过；覆盖三视口、axe、403 零业务请求、CAS、项目/报告期与附件 |
| 本地运行态 | 后端 `8080`、V2 `5174` 均 HTTP 200；MySQL、Redis、MinIO 等目标容器健康 |
| demo verify | `cgc_pms_demo_v2`、`passed=true`；计量 3、业主申报 2、金额差 0 |
| `git diff --check` | 通过 |

## 5. 边界、风险与回滚

- 未修改数据库 migration、Legacy UI、正式入口、生产配置或生产数据。
- 本地受控 demo 包已在 `cgc_pms_demo_v2` 幂等重载；未触碰 `cgc_pms`。需要恢复时重新执行同一 load 脚本。
- 当前成果仍是未提交工作区；用户未授权 commit、push、PR 或发布。首次非 Draft PR 前仍需最终提交 SHA 的等价 CI。
- 回滚本 Issue 时仅恢复 `backend/pom.xml`、demo 验证器和治理文档；017～022 既有业务实现与 `50/37/0` 台账不变。
- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- 无未承接发现。

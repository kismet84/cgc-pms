# ISSUE-040-001 预警批量评估独立权限码修复验收报告

## 结论

- 结论：通过。
- 阻塞：非阻塞。
- 问题类型：真实权限边界缺口。
- 关闭范围：仅关闭 `alert:edit` 同时保护普通预警处理和租户级批量评估的共码历史债；第40条 V-01 其他权限注册表、菜单、入口和真实角色复验仍未完成。
- 是否可上线：需要确认；本地专项通过，但本轮未取得 MySQL 环境和同一待合并 SHA 的远端门禁证据。

## 根因与最小修复

- 当前 `AlertController` 的已读/状态处理与 `POST /alerts/batch-evaluate` 都使用 `alert:edit`。
- V40 菜单 767“标记已读”和 768“批量评估”也共用 `alert:edit`；历史第10A主线已指出高低风险动作不应共码，但当时未进入修复。
- V146 只把既有菜单 768 的权限码前向更新为 `alert:evaluate`，保留菜单 ID 与全部角色—菜单关系；菜单 767 继续为 `alert:edit`。
- `batch-evaluate` 的后端强制权限同步改为 `alert:evaluate`；普通已读、状态处理和管理员兼容语义不变。

## 证据链

| 证据 | 结果 |
| --- | --- |
| 知识图谱历史召回 | 命中第10A `alert:edit` 安全否决与当前 V-01 复验要求 |
| CodeGraph | 当前 5 个写端点共用 `alert:edit`，其中 `batchEvaluate` 会触发租户/项目预警生成 |
| `codebase-memory-mcp` | `alert:edit` 命中 5 个 Controller 方法、V40 两个菜单、前端统一管理权限；与 CodeGraph 一致 |
| `rg` 补查 | CodeGraph 未完整召回 SQL 中文名称和测试调用，补查确认菜单 767/768、V42 角色关联及现有测试；归类为工具召回不足 |
| 测试先行 | 修复前 21 tests 中 2 项按预期失败：注解仍为 `alert:edit`、V146 缺失 |
| 最终专项 | 22 tests，0 failure，0 error，0 skipped |
| H2 实际迁移 | Spring 测试上下文执行 V146 后，菜单 767=`alert:edit`、768=`alert:evaluate` |
| 权限负样本 | 仅持有 `alert:edit` 的普通用户请求 `batch-evaluate` 返回 403 |
| 权限正样本 | 持有 `alert:evaluate` 的普通用户及管理员请求返回 200 |
| 迁移契约 | MySQL/H2 V146 同号且版本唯一；只更新 id 768，不含 INSERT/DELETE，不修改 id 767 |
| SQL safety | `PASS` |
| `git diff --check` | 通过 |

## 环境与回滚

- `SPRING_DATASOURCE_URL` 未配置，MySQL Flyway smoke 未执行，分类为环境前置缺失，不伪装为 MySQL 实跑通过。
- V146 SQL 仅使用通用 `UPDATE ... WHERE`，并有 MySQL/H2 双文件契约；后续仍须由同一待合并 SHA 的 MySQL CI 门禁复验。
- 迁移应用前可回退本 Issue 文件；应用后不得修改 V146，需新增前向迁移恢复权限码并同步回退 Controller 注解。

## 剩余风险

- V-01 仍需继续复验其他权限码、菜单、审批入口和真实角色正负样本。
- 前端当前没有实际调用 `batchEvaluate` 的可见按钮；未来新增入口时必须使用 `alert:evaluate`，不能复用 `canManageAlerts/alert:edit`。

# ISSUE-053-004 Clean-room V2 响应式、无障碍与错误状态基线验收报告

## 结论

- 验收结论：通过。
- 阻塞项：0。
- M1 裁决：完整退出门通过；M2 真实驾驶舱未启动，须经产品决策与 Ready 补货。
- 上线边界：仅本地 Clean-room V2；未发布生产、未切换正式入口，未修改 Legacy、后端、数据库、权限码或业务事实。
- 后续项：新增 0、关闭 0、净变化 0；本轮发现均已修复或有依据关闭，无悬空问题。

## 实施范围

- 完成 1440 展开侧栏、1024 图标栏、390 移动抽屉；移动抽屉支持 Escape、Tab 循环、焦点恢复、背景隔离和页面滚动锁定。
- 增加跳转主内容、可见焦点、语义地标、可访问名称、移动端 44px 目标和 `prefers-reduced-motion`。
- 新增通用加载/空/错误状态组件、403、404、全局错误边界与路由加载壳；全局错误只显示安全指纹，不泄漏原始异常。
- 通知入口仅提供无数据占位和权限边界；不调用通知业务 API，不伪造数量、通知列表或写操作。
- `/no-access` 保留兼容重定向到 `/forbidden`；未登录、ADMIN、普通有权和无权身份继续消费既有会话与权限码契约。
- 用户中途单独授权取消“禁止主动调用 subagent”约束；`AGENTS.override.md` 增加主线程可按净收益自主调用 subagent 的规则。该治理变更不属于 ISSUE 原始范围，但有当前会话明确授权，且不改变产品运行态。

## 自动化证据

- Ready lint：在状态仍为 `Ready` 时通过，内容哈希 `38d873a0a37cb026f3fb4241e7e0984e26c5d79732cf7bc413e8dfaa30056559`。
- `pnpm test:unit`：10 文件、38 项通过。
- `pnpm type-check`：通过。
- `pnpm lint:check`：通过，0 error。
- `pnpm check:boundary`：通过，扫描 58 个 V2 文件与 4 个共享契约文件。
- `pnpm check:route-ledger`：通过，87 个 Legacy 命名路由、73 个视图引用、65 个独立视图无漂移。
- `pnpm exec playwright test e2e/m1-shell.spec.ts --reporter=list`：Edge/Chromium 2 项通过，覆盖三视口、四类身份、403/404、减少动效、axe、控制台和业务请求隔离。
- `V2_LIVE_SHELL=1 pnpm exec playwright test e2e/shell-live.spec.ts --reporter=list`：本地实时 Edge 1 项通过，三视口截图与无业务请求断言通过。
- `pnpm build`：92 个模块完成生产构建。
- V2 容器重启后稳定观察 280 秒，`/v2/dashboard` 返回 200。
- `git diff --check`：通过。

## 无障碍与身份矩阵

| 维度 | 证据 | 结论 |
| --- | --- | --- |
| 1440 / 1024 / 390 | 登录与壳无横向溢出；桌面、图标栏、移动抽屉路径可达 | 通过 |
| 键盘与焦点 | 跳转主内容、Tab 循环、Escape、路由后主内容聚焦、可见 outline、焦点恢复 | 通过 |
| 语义与名称 | main/nav/header/dialog/status/alert 地标与隐藏标签对应可访问名称 | 通过 |
| 减少动效 | `prefers-reduced-motion` 下非必要动画压缩 | 通过 |
| axe | 核心流程 serious/critical 违规为 0 | 通过 |
| 身份 | ADMIN、普通有权、无权、未登录结果与既有契约一致 | 通过 |
| 运行质量 | 控制台 warning/error 为 0；壳层业务 API 请求为 0 | 通过 |

## 浏览器、视觉与降级说明

- 已选概念源：用户选定经营驾驶舱原图，SHA-256 `BA1A7CC8391868F255AC7208753BD03AD2157E37018AEBF182E573B3D30D783B`；稳定锚点为 `design-qa.md` 与 commit `4aaef899ad2624c36580745f7b69c900839831ab`。
- Browser/IAB 已优先复用现有浏览器绑定；DOM 快照为空，截图调用返回 `v2M1Tab.playwright.screenshot is not a function`，分类为 `tool_invocation`，按 Ready 允许范围降级到仓库 Playwright + Edge 实浏览器。
- 对照点：冷灰蓝画布与白色表面、品牌蓝激活态、八域左导航密度、顶部项目/报告期/账户层级、细边框/轻阴影面板、桌面到移动的信息架构均保持。
- 1440 保留完整域与工作区；1024 有意收敛为图标栏；390 有意使用带遮罩抽屉和底部账户区，保障内容宽度、44px 触控目标与退出可达性。
- 基线上折真实指标、图表、待办与通知数据未复制；本轮以“业务页面尚未迁移”、空上下文和无通知占位替代，避免把 M1 壳冒充 M2 业务交付。
- 视觉裁决：M1 壳层结构、品牌、密度、层级、响应式与失败状态保真；真实驾驶舱内容保真不在本 Issue 范围。

## Reviewer 发现与处置

- 移动抽屉原账户操作隐藏：本轮增加固定底部账户区与“退出登录”，复验通过。
- 路由后主内容焦点不可见/时序不稳定：改用路由完成钩子聚焦并恢复可见 outline，自动化通过。
- 旧认证 E2E 仍断言 `/no-access`：更新为 `/forbidden`，兼容重定向继续保留。
- 全局 Suspense 单根警告：增加稳定根容器，控制台复验为 0。
- Reviewer 提到 `tbody` 选择器：当前源码实际为 `body`，无复现证据，按“证据不足关闭”，未制造 backlog。

## 回滚

- 回退 V2 响应式样式、状态组件、403/404/全局错误页、路由接线、测试和治理回写即可。
- 保留 `ISSUE-053-001`～`ISSUE-053-003` 已验收设计系统、安全会话与应用壳；Legacy 正式入口、后端和数据库无需回滚。

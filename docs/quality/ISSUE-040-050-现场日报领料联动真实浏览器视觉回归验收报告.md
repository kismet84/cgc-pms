# ISSUE-040-050 现场日报领料联动真实浏览器视觉回归验收报告

## 验收结论

- 结论：通过。
- 阻塞：无。
- 任务性质：回归证明。
- 范围：现有现场日报详情的已审批且已出库领料只读区、专用空态、长施工文本与桌面/窄视口可用性；未修改业务代码、权限、库存、数据模型或生产数据。

## 运行态与自动化证据

- backend health、5173 与 dev-login 三项 health gate 连续稳定采样183秒，18组采样全部通过；当前 Vite 指向 `ISSUE-040-050` worktree。
- `pnpm vitest run src/pages/site/__tests__/daily-log.test.ts`：1个文件、7项测试通过。
- `pnpm exec vue-tsc --noEmit`：通过。
- 浏览器控制台新增 error/warn：0。

## 真实浏览器证据

- 桌面视口1280×720通过 dev-login 进入现场日报页，目标详情中的140字施工内容完整保留并可滚动读取。
- 真实领料行显示 `REQ-M30-MAT-LEDGER-001`、`螺纹钢 HRB400 Φ12`、`30.0000`、`吨`、`第30主线成本台账领料演示`；表头仅为领料单号、物料、领料数量、单位、使用部位。
- 另一既有日报无命中时显示“当日暂无已审批且已出库领料”，没有用材料到货空态代替领料空态。
- 浏览器详情未出现价格、金额、合同、供应商或“已安装”字段/语义，领料事实未被解释为工程安装事实。
- 通过浏览器原生 viewport 能力设置480×800窄视口，`matchMedia('(width < 500px)')` 为 true；详情弹窗宽480px，内容区宽448px，左右边界位于视口内，标题、长文本、领料区和关闭按钮均可达。
- 临时 viewport override 已调用 reset；随后页面恢复1280×720默认视口。

## 测试数据边界与回收

- 验收夹具创建前确认环境为 dev/test、数据库主机为 `127.0.0.1`、`.codex-autopilot/ALLOW_TEST_DATA_RESET` 存在，三重门禁同时满足。
- 固定日报夹具仅用于验证长文本与真实领料匹配，首轮写入因 MySQL 客户端字符集未显式指定产生乱码；分类为测试数据工具配置，使用 `utf8mb4` 原位纠正后再开始正式浏览器验收，没有修改生产逻辑或放宽断言。
- 收口按固定日报 ID 与 tenant_id 精确删除；数据库复读剩余0条，浏览器刷新后只保留既有1条日报，测试数据零残留。

## 风险复核

- 本次只证明当前 dev 角色下既有详情组件的可见性、只读字段、空态、长文本和窄视口可操作性，不声明新增日报或库存能力。
- 数据匹配继续以同租户、同项目、同日报日期、APPROVED 且真实出库为后端既有边界；本轮没有绕过权限或改变查询条件。
- 未连接生产、未发布生产、未 push；剩余风险仅为未来组件改版后的常规视觉回归，需要由现有自动化与发布验收持续覆盖，本轮不制造泛化后续项。

## 后续项收口

- 本轮新增后续项：0
- 本轮关闭后续项：1
- 关闭问题键：OBS-DAILY-LOG-BROWSER
- 本轮后续项净变化：-1

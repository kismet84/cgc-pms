# ISSUE-053-019 M4变更签证与投标成本V2验收报告

> 2026-07-22 用户复验修订：变更签证与投标成本移除重复介绍和面向实现的说明文字，变更状态改为中文业务词；分页统一为“共N条—上一页—第N页—下一页”并右对齐。浏览器计算样式复验：页面标题`21px/700`、区块标题`15px/600`、分页`12px`。

**结论：通过（本地dev/test范围）。** 三条目标路由达到真实`V2_ACCEPTED`，路由台账为`LEGACY_ONLY=60 / V2_ACCEPTED=27 / V2_SOURCE_AVAILABLE=0`。未授权且未执行提交、push、正式入口切换、Legacy退役或生产发布；本报告不构成上线裁决。

## 实施范围

- `/variation`确定性保留query/hash并重定向到`/variation/order`；变更页面覆盖分页筛选、详情/编辑、明细、提交、业主申报/核定与追溯。
- `/bid-cost`覆盖分页筛选、详情、新增、编辑、删除、选择可见项目中标与未中标；成功后重新读取服务端事实，失败不伪改本地状态。
- 金额通过十进制字符串/BigDecimal传输和处理；业主核定后合同金额仅由服务端合同变更链更新。
- 后端补齐version/CAS、状态条件写、租户/项目/合同/合作方范围、投标费用租户条件、附件动作/阶段门禁及驳回重提；V219登记`variation:order:add/edit/delete/item:edit`并授权正式角色。

## 独立复核与处置

- 安全复核发现明细保存错误复用`variation:order:edit`：本轮新增`variation:order:item:edit`，同步Controller、前端、V219、demo与正负测试。
- 安全复核发现`VAR_ORDER`驳回后重提被通用状态校验拒绝：本轮为resubmit显式允许`REJECTED/WITHDRAWN`并增加状态回归。
- 测试复核指出CAS、投标租户冲销、附件阶段与角色矩阵证据不足：本轮补齐旧版本六类写动作、跨租户费用、附件权限/阶段、只读/无权角色及三视口E2E；未要求把同一通用HTTP错误映射在每个页面重复枚举测试。

## 验收证据

- Ready lint通过；迁移在`cgc_pms_demo_v2`本地dev库应用成功，项目经理变更权限`9`、商务经理变更/投标权限`14`，demo verify为`passed=true`。
- 后端变更、投标、合同变更、附件、工作流专项`169`项通过；新增CAS、幂等、跨租户、附件阶段和驳回重提负向证据。
- V2单测`30`文件、`176`项通过；契约类型、V2类型、Lint、Clean-room边界、路由台账与生产构建通过。
- Playwright目标E2E `2/2`通过：三视口无横向溢出、axe serious/critical为0、无控制台错误、只读动作隐藏、两路由无权身份403失败关闭。
- live浏览器：`demo.business`可访问变更与投标并取得业务动作；`demo.manager`可访问变更且访问投标失败关闭；明细专属权限刷新后可见，控制台无error。

## 风险与回滚

- 剩余风险：仅完成本地dev/test证据，未形成同SHA CI、目标环境、备份恢复或生产发布证据，因此不得声明可提Ready PR或可上线。
- 最小回滚：回退三路由页面/契约/服务、后端精确根修、V219权限与对应测试；保留017/018、M0～M3、Legacy和正式入口。

## 后续项

- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- 悬空项：0。`ISSUE-053-020～023`为既有计划切片，不是本轮新发现项，仍须逐条补为Ready并另获授权。

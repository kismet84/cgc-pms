# 第95条主线：示例库全业务流程阻塞修复与浏览器闭环报告

> 日期：2026-08-15
>
> 实施基线：`master@dc4d72bebfbb5e131938c95d0eaa4bd73750d883`
>
> 实施分支：`codex/mainline-95-full-business-flow-closeout`
>
> 当前状态：`PASS / G0-G5_PASSED / GIT_DELIVERY_MERGED / POST_MERGE_VERIFIED`
>
> 环境边界：仅本地 dev/test/demo；不构成生产、非本地环境、版本发布或上线裁决
>
> 计划载体：[第95条主线任务计划书](../plans/第95条主线-示例库全业务流程阻塞修复与浏览器闭环任务计划书.md)

## 1. 结论

第95条主线 12 项发现已完成本地实施与验收。产值、分包、质量检查补齐业务专属 WBS 契约；技术归档、分项验收和施工完成门只接受同 WBS 的 `QUALITY/SUBMITTED/PASS`；采购草稿可安全原子删除；合同履约可由页面调用既有服务端 settle 状态机；发票/回款附件计数、库存候选、时间格式、按钮语义、toast、弹窗层级与成员弹窗布局完成一致性修复。历史迁移 V294 以已应用 checksum 原文恢复，新变化使用 V295～V299。

当前示例项目已通过正式页面/API 自然到达 `CLOSED`。采购、收货、领退料、库存、分包、质量、技术、进度、计量、收入、付款、现金、收尾、质保、档案和三类合同终局形成可回读业务链；未直接修改业务表造事实。后端、前端、H2/MySQL、浏览器契约、内置浏览器视觉/DOM/console、运行部署契约和 Codemap 全绿。实现已完成 exact-SHA Push CI、独立 PR CI、受保护合并、post-merge 验真及源分支清理。

## 2. 12 项发现唯一裁决

| ID | 裁决 | 结果 |
| --- | --- | --- |
| `M95-F01` | 本轮修复并复验 | 计量行显式 WBS；measurement-owned form-options；宽弹窗及长内容无溢出 |
| `M95-F02` | 本轮修复并复验 | 分包任务 WBS 必填、按项目加载；失效历史值只读禁选 |
| `M95-F03` | 本轮修复并复验 | QUALITY 检查强制有效 WBS；SAFETY 保持可空；租户/项目 fail-close |
| `M95-F04` | 本轮修复并复验 | 技术归档、分项验收与 close gate 拒绝 SAFETY/PASS 和跨 WBS 事实 |
| `M95-F05` | 本轮修复并复验 | 仅 DRAFT/DRAFT 采购单可删；父单、明细、附件同事务软删 |
| `M95-F06` | 本轮修复并复验 | 合同详情按权限/状态显示结清动作，提交当前 version，失败权威回读 |
| `M95-F07` | 本轮修复并复验 | 销项发票与回款固定提交单文件 `attachmentCount=1` |
| `M95-F08` | 本轮修复并复验 | 纳秒时间格式、库存按需候选、方向动作、toast、弹窗层级与成员弹窗布局修复 |
| `M95-F09` | 本轮修复并复验 | 原 V294 checksum 精确恢复；V295～V299 完成角色、权限与周计划替换校准 |
| `M95-F10` | 本轮修复并复验 | 正式 UI/API 完成示例库完整业务链，项目自然关闭 |
| `M95-F11` | 本轮修复并复验 | 本地 G0～G4、迁移、contract、内置浏览器与权威回读全绿 |
| `M95-F12` | 本轮修复并复验 | exact-SHA Push CI、PR #448、受保护合并、post-merge 和源分支清理全部完成 |

## 3. 服务端权威业务事实

只读回读目标库 `cgc_pms_demo_ui_20260728`：

| 事实 | 终态 |
| --- | --- |
| 项目 `XM-20260804-001` | `CLOSED`，实际完工日 `2026-08-15` |
| 收尾 `PC-20260814-001` | `CLOSED` |
| 主合同 `CT-20260811-001` | `MAIN / SETTLED` |
| 采购合同 `CT-20260814-001` | `PURCHASE / SETTLED`，已付 9,560 |
| 分包合同 `CT-20260814-002` | `SUB / SETTLED`，已付 3,000 |
| 付款 `PAY-20260815-001` | `APPROVED / PAID`，实付 9,560 |
| 现金日记账 `CJ-20260815-001` | `OUT / ARCHIVED`，金额 9,560 |

材料链数量守恒：采购申请转单、采购订单审批并完成、验收合格入库 20、领料出库 8、退料入库 3、后续正式领料出库 15，最终库存归零。三项 WBS、三条同 WBS 质量验收、三项分项验收、竣工验收、业主结算、尾款、质保金、质保释放与档案移交均经页面和服务端状态机完成。

## 4. 自动化与运行态证据

| 验证 | 结果 |
| --- | --- |
| 后端全量 | 372 suites、2,862 tests，0 failures/errors、28 个显式环境开关 skipped |
| 后端定向回归 | 关键 17 classes、151 tests 与全量红灯修正 3 classes、8 tests 均通过 |
| 前端 unit | 89 files、641 tests，全部通过 |
| contracts type-check / Vue type-check / build | PASS；build 465 modules |
| lint / changed-files Prettier | 0 error；10 条既有范围外 warning；Prettier PASS |
| browser contract | 27 specs、98 expected，0 skipped/unexpected/flaky |
| MySQL fresh | `BaselineMySqlSmokeTest` 1/1，V299、215 表、bootstrap/权限/模板/约束通过 |
| MySQL upgrade | `BaselineMySqlUpgradeTest` 1/1，V180→V299、296 migrations validate 通过 |
| H2/权限/状态机定向 | 纳入后端 151 tests，全部通过 |
| 运行部署契约 | backend/frontend dev runtime、`Asia/Shanghai` 时区、健康检查通过 |
| 内置浏览器 | 真实 URL/DOM/操作、成员弹窗视觉、console warning/error 0 |
| Codemap | generate 与 `--verify` 通过 |

浏览器 contract 首轮唯一失败为申请页 fixture 缺 `/requisitions/material-options` GET 响应，分类 `ready_issue_config`；补齐契约后单项和全量 98/98 通过。MySQL fresh 首轮应用上下文使用 240-bit 测试 JWT 密钥，分类 `environment_prerequisite`；仅给测试进程提供合规本地密钥后同一 gate 通过，未降低生产安全默认值。后端首次全量发现 closeout 分页测试未区分项目成员候选、quality façade 快照漏新 form-options、cost 夹具仍写遗留 `FIN` 角色；均分类 `ready_issue_config`，同步契约/夹具后定向 8/8、全量 2,862 tests 通过。所有临时 MySQL 库均精确删除。

首轮 exact-SHA Push CI `31852658882` 的 15 个非失败 job 均成功；`backend-test-mysql` 因投标项目范围夹具仍写 V296 已禁止的遗留 `OTH` 角色失败，`reliability-contracts` 因 Codemap 在提交前脏工作树生成而 clean-snapshot 不一致失败。两项均分类 `ready_issue_config`：夹具改用合法 `PROJECT_MANAGER` 后 H2 2/2、独立 MySQL 2/2 通过；Codemap 在对应 clean 代码提交上重生成。修复后的 exact-SHA Push CI 另行取证，不复用失败 run。

## 5. 视觉与交互验收

- 产值计量弹窗使用宽版布局，来源、WBS、剩余量、数量和文件列在桌面视口无横向溢出，窄屏退化为单列。
- 项目成员“添加成员”弹窗中岗位名称改为独占整行，搜索、用户/角色、岗位、起止日期层次清楚；1512×1114 实际浏览器截图无裁切或遮挡。
- V2ActionMenu 上传入口保持原生 file label 行为；toast 与对话框层级不被 backdrop 覆盖。
- 页面最终 console warning/error 为 0；不存在框架错误 overlay。

## 6. 数据、权限与回滚边界

- 所有候选接口限定租户、项目、权限及 ACTIVE 基线；历史失效值只读禁选，提交继续由服务端复核。
- 金额、库存、合同、质量、付款、项目状态和附件事实均由服务端权威状态机决定；前端不复制 readiness 规则。
- 已应用 migration 未改写；V294 从恢复包取回并与示例库 checksum 匹配，新变化只落 V295～V299 的 MySQL/H2 对应版本。
- 代码回滚边界为启动基线 `dc4d72bebfbb5e131938c95d0eaa4bd73750d883` 与后续任务提交；禁止 reset `master` 或修改已应用 migration。
- 示例库业务终态是正式本地操作结果，不以 SQL 逆写回滚。需要重演时使用新的本地 demo/test 数据集，不破坏当前验收证据。

## 7. 受保护 Git 交付证据

| 证据 | 结果 |
| --- | --- |
| 实现源 HEAD | `96a771deda41185747685ba88bd15ef980c56aea` |
| Push CI | `31853877532`，push event，16 个 required jobs 全绿；pre-PR verifier `PASS` |
| PR | [#448](https://github.com/kismet84/cgc-pms/pull/448)，Ready、同 HEAD；独立 PR CI `31854492617` 全绿 |
| 复用裁决 | `scripts/ci/test-runtime-deployment-contract.ps1` 属控制面变化，Push CI 复用按设计 fail-close；未绕过独立 PR CI |
| 受保护合并 | squash merge `ea211af1d73b9e1c0c7e4ae39fd797e19d83ea4b` |
| tree 证明 | source/merge tree 均为 `44cf0e6a7ed5f95f843b53117f70704880d61151` |
| post-merge | run `31855142538` 成功；verifier `PASS / REUSED_PUSH_CI`，同时绑定 Push CI 与 PR CI |
| 清理 | `origin/master` 指向 merge SHA；实现源分支本地与远端均已删除 |

## 8. 零悬空统计

- 计划发现 12 项：本轮修复并复验 12 项，正式承接 0，证据不足关闭 0。
- 直接相关新增缺口：浏览器 fixture 1、MySQL 测试前置 1、成员弹窗视觉 1；均本轮处理并复验。
- 正式后续项新增 0、关闭 0、净变化 0；无无载体代码/业务遗留。
- Git 最终裁决：`PASS / GIT_DELIVERY_MERGED / POST_MERGE_VERIFIED`；不含生产部署、Tag、Release 或非本地环境结论。

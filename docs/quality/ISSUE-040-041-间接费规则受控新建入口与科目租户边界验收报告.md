# ISSUE-040-041 间接费规则受控新建入口与科目租户边界验收报告

## 结论

通过，非阻塞，可合入本地主线；不涉及生产发布或推送。

规则新建入口、白名单请求、认证租户、启用间接费科目、枚举和失败保留均取得直接证据。高风险复核结论 PASS，无权限放宽、跨租户引用或客户端身份/状态字段注入。

## 范围与证据

- 后端新增 Create DTO，Controller 只转交 costSubjectId、allocationBasis、allocationCycle；Service 固定租户与 ENABLE，并校验科目 tenant/status/subjectType/accountCategory。
- 前端在既有规则弹窗增加 `overhead:add`/管理员可见的新建表单，API 无 id、tenantId、status 或审计字段。
- Ready lint PASS，契约哈希 `09c81f2963b211d6ded927e85b3ff7863eba0dc2ac06d8d3b5abb82db3c11267`。
- 后端 Controller/Service 22项通过；前端 API/页面16项、类型检查、目标 ESLint 和 `git diff --check` 通过。
- 当前 Issue Vite 稳定超过180秒；浏览器打开规则列表与新建表单，确认默认直接人工/月度及服务端确定租户/状态警示后取消，POST `/overhead-allocation/rules` 前后均为0，控制台 error/warn=0。

## 边界复核

- 未登录401、仅查询权限403；显式 `overhead:add` 与管理员成功。
- DTO 拒绝缺失科目、EQUAL 非白名单依据和 YEARLY 非白名单周期；额外身份/状态字段未传入 Service。
- 跨租户、不存在、停用、非 OVERHEAD 科目均返回 `OVERHEAD_SUBJECT_INVALID`；合法规则归属认证租户并固定 ENABLE。
- 修改、删除、启停、执行算法、迁移和历史分摊事实未改变。

## 发现项处置

本轮新增后续项：0

本轮关闭后续项：1

本轮后续项净变化：-1

关闭明细：`A-01-OVERHEAD-CREATE`。无悬空发现项。

## 回滚与剩余风险

- 最小回滚：回退 DTO、科目校验、前端 API/表单、专项测试和治理回写；不删除既有规则或分摊事实。
- 剩余风险：浏览器遵守只取消门禁，真实创建由自动化覆盖；正式环境仍需执行既有同 SHA 发布门禁。

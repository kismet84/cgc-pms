# ISSUE-048-006 现场日报当日质量安全检查只读联动验收报告

结论：通过；阻塞：无；可合并：是；可推送：否（`autoPush=false`）。

## 范围与实现

- 新增 `GET /site-daily-logs/{id}/quality-safety`，管理员可访问；普通用户必须同时具备 `site:daily:query` 与 `quality:safety:query`。
- 服务端先按当前租户隐藏不存在或跨租户日报，再校验项目数据范围；项目和日期只从日报反查，客户端不能扩大查询范围。
- 只查询同租户、同项目、日报日期且状态为 `SUBMITTED` 的检查；问题按租户、项目和检查ID一次批量读取，无N+1。
- 返回字段仅含检查ID、编号、地点、结论、问题总数、高风险数和未关闭数；前端提供加载、空、失败和数据态，失败不阻断日报正文。
- 本项未新增迁移、字段或权限，未修改质量安全写侧，未写入验收数据。

## 验收证据

- 后端：`SiteDailyQualitySafetyServiceTest` 3/3、`SiteDailyLogControllerTest` 4/4通过；覆盖跨租户隐藏、项目校验、租户/项目/日期/提交状态约束、批量问题聚合、空结果短路、双权限交集和管理员边界。
- 前端：日报目标测试8/8通过，`vue-tsc --noEmit`通过；无质量权限不请求，有权限时展示最小字段，错误被局部消化。
- 构建与运行：后端可执行包构建成功；隔离后端以当前 V210 开发库启动，Flyway校验207条迁移并确认无需迁移，`/api/actuator/health` 返回200，新端点未认证访问返回401。
- 数据条件：当前本地开发库 `site_daily_log` 与 `qs_inspection_record` 均为0行。为遵守只读验收约束，没有创建日报或检查；因此详情数据态由服务与控制器测试确定性覆盖，真实运行态只验证启动、迁移基线、健康和路由保护。
- 静态门禁：`git diff --check`通过；修改均在Ready允许路径内，禁止的质量安全写侧、迁移、部署、脚本、插件、仓库规则和工作流均未修改。

## Reviewer 复核

- 结论：PASS；findings：无。
- 数据正确性：检查查询同时限定租户、项目、日报日期和 `SUBMITTED`；问题查询再次限定租户、项目和检查ID，高风险按 `HIGH`、未关闭按非 `CLOSED` 计数。
- 安全：跨租户日报在项目校验前统一隐藏；普通用户任一单权限均403，双权限和管理员才进入业务服务；返回对象不含问题正文、责任主体、合作方、附件、金额或整改详情。
- 性能：检查一次查询、问题一次批量查询；无检查直接返回空列表，不查询问题表；排序固定为检查编号、检查ID。
- 前端韧性：权限不足不发请求，旧请求用序号隔离，质量安全失败只显示局部警告，不关闭日报详情。

## 失败分类与恢复

- 首次后端测试失败为测试编译问题：捕获变量声明为 `Wrapper`，需转换为 `AbstractWrapper` 才能读取参数映射；修正测试后7/7通过，业务实现未返工。
- 首次前端测试失败为静态断言仍匹配单行早退；实现增加加载态复位后断言同步更新，8/8通过。
- 隔离运行容器前两次启动分别缺少命令和工作目录，分类为 `tool_config`；补齐 `java -jar` 与 `/app` 后启动成功。运行态验真完成后隔离后端和Vite进程均已精确停止。

## 治理收口

- `PI-2026-07-17-06` 已实施回写；唯一叶子 `A-03-QUALITY-SAFETY-DAILY-FACTS` 已关闭。
- 聚合项 `A-03` 继续只承接人员班组、设备、自动天气、定位、离线和统计，不再把质量安全列为缺口。
- 新增后续项：0
- 关闭后续项：1
- 后续项净变化：-1
- 最小回滚：回退独立端点、服务、VO、前端调用和展示；数据库与质量安全事实无需回滚。

剩余风险：本地开发库当前没有日报与检查样本，未做数据态页面截图；专项服务/控制器测试已覆盖相同数据与权限边界，不构成当前合并阻塞。生产发布、生产数据库和push均未执行。

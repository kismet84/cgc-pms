# 第78条主线：全审批业务单据可视化模板与完整详情字段验收报告

> 验收日期：2026-08-06
> 实施基线：`master@6caaf895d7996b48b778a503f81ec8031a39ea90`
> 当前裁决：`G0-G5_PASSED / ISSUE-078-001_CLOSED / GIT_DELIVERY_MERGED / POST_MERGE_VERIFIED`

## 1. 结论

第78条在本地 dev/test/demo 范围通过。当前租户启用的28种审批业务类型全部具备唯一完整详情 Provider、语义字段目录、示例数据和默认模板策略；`PAYMENT`仅保留为显式历史兼容类型。A4纵横向画布以`design_schema`为唯一持久事实，服务端统一编译受限HTML、字段清单和PDF；分包计量已用真实对象、明细集合、发布版本和默认绑定完成参考闭环。

## 2. G0～G5证据

- `G0`：任务分支、基线SHA、工作区归属、V287号段、Codemap、本地URL和数据库已锁定；未覆盖非任务改动。
- `G1`：28/28启用审批类型Provider就绪；重复注册和缺失注册fail-close；字段目录、示例快照、真实快照和默认模板策略由同一Provider提供。
- `G2`：V287只新增可空`design_schema`并放宽模板类型约束；H2 active/legacy、真实MySQL空库基线和V180→V287升级均通过；旧源码模板继续使用兼容路径。
- `G3`：画布校验、字段引用、集合上下文、服务端HTML编译、manifest推导、PDF分页和生成历史同源；60行集合跨页且表头重复，末行可见。
- `G4`：后端健康`UP`、5173返回200；浏览器28个类型均显示字段契约就绪；分包计量真实ID `520000000000002101`渲染`M52-SM-001`及“主体结构劳务”；保存重开恢复横向A4、边距、元素和表格；主页面按“业务模块—模板—HTML预览”三栏展示；编辑器具备组件库、画布网格、单一方向切换和共用画布区域预览；1810×1114桌面及700px单栏均无页面横向溢出，控制台错误0。
- `G5`：全量后端、前端、双数据库、构建、lint、Codemap、文档、回滚和零悬空均通过。

## 3. 实现与验证

- Provider：28/28启用审批类型就绪；Provider实现不使用Mapper、JDBC或实体反射，不暴露审批记录、内部主键和附件。
- 画布：A4纵横向、毫米坐标、边距、拖拽、缩放、层级、字段搜索、集合列、实时sandbox预览、保存回读和旧源码编辑兼容。
- 模板：`SUB_MEASURE_CANVAS_078` V1已发布并设为默认，契约`sub-measure.v1`，字段为`measure.code`和`items.name`。
- 后端：333份Surefire报告，2570项，0失败，0错误，18跳过；另行启用的真实MySQL空库与V180升级2项均通过。
- 前端：72个测试文件、485项全通过；`type-check`、`type-check:contracts`、`build`通过；`lint:check`为0错误、28个既有warning。
- 运行态：重建脚本成功；backend/frontend容器健康；本地演示库Flyway当前V287且`biz_document_template_version.design_schema`存在。
- 静态门禁：`git diff --check`通过；Codemap已登记业务单据模板平台及调用、持久化和测试边界。
- Git交付：实现SHA `766e8a60fb90805b515a5385ba679bf4e234fe14`的push CI `31100112881`、PR CI `31100974994`均13/13成功；PR #400合并为`master@08fa71d5d906564393daee40faeb5caf064b2334`；同SHA post-merge run `31101912189`成功；原实现分支本地和远端均已删除。

## 4. 失败分类与解决

- `product_defect`：画布编译器输出非XHTML头、集合表格绝对定位导致跨页重叠、空画布提前请求预览、成本纠偏别名大小写不稳定；均在服务端编译器、页面预览门和共享金额映射处修复并复验。
- `test_contract_mismatch`：V287后部分迁移断言仍为V286、测试Provider与付款Provider重复、付款安全字段预期仍包含已禁止敏感信息；同步契约后定向与全量测试通过。
- `tool_invocation`：首次全量Maven外层时限过短被终止；扩大时限后原样重跑509.7秒通过，不归因为产品失败。
- `environment_prerequisite`：单独执行Compose状态命令缺少本机MinIO变量；版本化重建脚本和Docker实际状态均正常，改用健康端点与容器健康证据完成验收。
- 当前无未解决`product_defect`、`test_regression`、`flaky`、权限缺口或数据一致性缺口。

## 5. 风险、回滚与零悬空

- 剩余风险：画布首版只支持单层集合表格和A4两种方向；这是明确产品边界，不是隐藏缺口。新增布局能力时扩展现有JSON契约和编译器，不引入第二渲染事实。
- 回滚：关闭画布入口即可恢复旧源码编辑；保留可空`design_schema`、模板版本、默认绑定和生成历史；数据库变化只允许新增前向migration。
- 项目仅有本地环境；未发起生产、目标环境或生产数据库操作。
- 计划周期新增1、关闭1、净变化`0`；本轮新增后续项0、关闭后续项0、后续项净变化0；无无载体遗留项。

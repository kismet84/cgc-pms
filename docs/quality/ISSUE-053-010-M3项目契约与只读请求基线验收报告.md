# ISSUE-053-010：M3项目契约与只读请求基线验收报告

日期：2026-07-21

结论：**通过**。阻塞项：**0**。M3金丝雀关闭；本报告不代表任何M3页面已迁移，不授权提交、push、PR、正式入口切换或生产发布。

## 1. 交付物

- `packages/frontend-contracts/src/project.ts`：项目、项目总览、成员DTO、查询类型和只读API路径。
- `frontend-admin-v2/src/services/projects.ts`：分页、详情、总览、成员薄服务；既有可见项目加载复用同一分页入口。
- `frontend-admin-v2/tests/unit/m3-project-request-baseline.test.ts`：编码、signal、端点、空ID和金额字符串回归。
- M3阶段计划、Ready、Current Focus、项目地图和迭代决策回写。

## 2. 契约与安全复核

- 已逐字段核对当前后端`PmProjectVO`、`ProjectOverviewVO`、`PmProjectMemberVO`和Legacy项目类型；总览金额继续保持字符串，未用JavaScript浮点生成权威金额。
- 只访问既有`GET /projects`、`GET /projects/{id}`、`GET /projects/{id}/overview`、`GET /projects/{projectId}/members`。
- ID使用`encodeURIComponent`；查询使用平台`URLSearchParams`，空字符串和非法非正分页值不发送。
- AbortSignal透传；未新增权限兜底、租户全量回退、缓存、Store、页面状态、依赖、后端或数据库变更。
- M3路由与迁移台账保持`LEGACY_ONLY`，无占位冒充完成。

## 3. 验证证据

| 验证 | 结果 |
| --- | --- |
| Ready lint | 通过；`ISSUE-053-010`，高风险，契约hash已生成 |
| `pnpm type-check:contracts` | 通过 |
| V2单测 | 19个文件、88项通过；目标新增4项 |
| `pnpm type-check` | 通过 |
| `pnpm lint:check` | 通过 |
| `pnpm check:boundary` | 通过；83个V2文件、8个契约文件 |
| `pnpm build` | 通过；最大JS资产约111.33kB |
| 路由台账与包体门 | 通过；87个路由、8个JS资产 |
| 主线owner flow HighRisk | 通过 |
| `git diff --check` | 通过 |

首次目标测试的空ID用例错误地按Promise拒绝断言同步fail-fast，分类为`ready_issue_config/测试契约`；修正断言后同套验证全绿，业务实现未因测试适配而放宽。
主线owner脚本首次从`frontend-admin-v2`工作目录调用导致相对路径失效，分类为`tool_invocation`；回到仓库根按原参数复验通过。

## 4. 范围与Git

- 分支：`codex/mainline-53-ui-v2-m3`。
- 未修改后端、Legacy、数据库、路由、M3页面、部署、CI或生产环境。
- 用户未跟踪目录`docs/quality/full-audit/`保持隔离，未读取、修改或清理。
- 未提交、未push、未创建PR。

## 5. 收口

- 本轮修复并复验：Ready状态/缩进格式；目标测试同步断言。
- 超出范围并正式承接：0。
- 证据不足或无价值而关闭：0。
- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- 悬空项：0。

下一阶段属于M3既定页面迁移，不是本轮新发现。补充下一条Ready前，必须核实项目列表写侧权限、状态机和真实demo身份；不得直接把本次只读底座标为`V2_ACCEPTED`。

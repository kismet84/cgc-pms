# 第53条主线 M2：工作台与新版驾驶舱验收报告

## 结论

- 验收结论：**通过**。`ISSUE-053-005～009`全部完成，M2 P0本地退出门关闭。
- 上线结论：**未上线，不授权上线**。正式入口仍为Legacy；整站切换、回滚演练、Legacy退役与生产发布属于后续阶段及独立授权门。
- 数据边界：仅使用本地dev/demo运行态与`127.0.0.1:3307/cgc_pms_demo_v2`，未连接生产、未新增迁移、未重置或删除业务数据。

## 交付范围

- 驾驶舱、报表目录、历史预警重定向、审批根入口、四类个人审批列表及实例详情共九个Legacy route name达到`V2_ACCEPTED`。
- `/approval`兼容重定向至`/approval/todo`；`/approval/:instanceId`兼容重定向至V2权威实例详情并保留query。流程模板`/approval/process`保持`LEGACY_ONLY`。
- 路由台账生成器以当前V2接线为接受事实，生成`LEGACY_ONLY=78 / V2_ACCEPTED=9 / V2_SOURCE_AVAILABLE=0`，检查模式防止手工漂移。
- 恢复经营健康“辅助判断、非财务/结算口径”提示；移动端审批详情标题恢复16px。
- 复用公共`V2Dialog`统一桌面端通知、审批详情与预警处置弹窗的卡片、文字和布局；按内容保留各业务结构，不强制圆环或百分比。审批详情和预警处置去除嵌套白卡，下拉菜单固定向下展开且不被弹窗面板裁切。未增加后端、数据库、依赖、业务状态或新页面。

## 验收证据

- Ready与静态门禁：Ready lint通过；Clean-room边界扫描82个V2文件、8个契约文件通过；路由台账check通过；主线owner flow与7项统一执行策略套件通过；`git diff --check`通过。
- V2自动化：18个单测文件、84项通过；Lint零错误零警告；类型检查与生产构建通过；8个JS资产均低于300KiB门禁。
- 后端性能：`DashboardPerformanceTest` 2项通过；5个活动项目SQL计数13，满足≤20门槛；空数据场景SQL计数1。
- 数据验真：完整demo包`passed=true`，20个阶段完成；8个角色账号、8组范围、40组角色×审批状态、48组角色×动作权限、8组预警查看/处置权限及金额守恒指标通过。
- 真实浏览器：最终同一diff下驾驶舱live 8/8（含弹窗下拉向下展开且无裁切）、审批与九路由live 12/12、M1壳四身份/403/404 2/2、live壳1/1，共23/23；既有health验收继续通过。覆盖1440、1024、390视口、axe serious/critical=0、无页面横向溢出、无新增控制台warning/error。
- 实际入口：后端`http://localhost:8080/api/actuator/health`、Legacy`http://localhost:5173/`、V2`http://localhost:5174/v2/`均返回200；admin dev-login最终落点`/v2/`。

## 首轮失败分类与处置

- `ready_issue_config`：053-009字段缩进导致Ready解析失败；修正载体格式和真实允许路径后lint通过。
- `quality_or_security`：台账生成器硬编码旧状态、审批根/详情Legacy路径缺失、移动端标题15px及经营健康口径提示缺失；均本轮修复并复验。
- `quality_or_security`（桌面弹窗）：审批详情与预警处置保留旧嵌套白卡，预警处置下拉被弹窗面板裁切；统一公共弹窗令牌和桌面布局，面板仅在选择器展开时释放裁切，验证菜单仍按组件契约向下展开。
- `quality_or_security`（测试契约）：预警统一为权威列表后，旧E2E仍按派生风险要求每角色四级数据；改为对照当前角色服务端权威记录逐项核验严重度，不降低产品门禁。日期断言改为核对权威记录编号和合法服务端日期。
- `environment_prerequisite`：源码变更后Vite HMR出现一次白页；只重启`frontend-v2`并等待healthy，随后同套live验证通过。

## 权限、数据与回滚

- 八角色只请求当前有权驾驶舱端点；权威预警继续受角色业务域、活动项目成员和报告期限制。审批列表/详情继续由后端按当前用户、租户与参与关系裁决，动作仍受`availableActions + 显式权限 + CSRF + 幂等键`约束。
- 金额继续使用服务端字符串口径；前端格式化不生成权威金额。真实验收未执行审批业务动作。
- 最小回滚：回退本Issue的兼容路由、路由测试/台账映射、E2E契约、视觉/口径修复、公共弹窗令牌及桌面布局和治理回写；M0/M1与既有M2页面、后端、数据库、Legacy保持不变。

## 发现项收口

- 本轮修复并复验：Ready契约格式、台账硬编码、审批兼容深链、权威预警E2E契约、动态日期断言、移动端标题、健康口径提示、公共弹窗桌面视觉、嵌套白卡、下拉向下展开与裁切、M1跨阶段壳断言及运行态HMR漂移。
- 超出范围并正式承接：0。
- 证据不足或无明确价值而关闭：0。
- 新增后续项：0。
- 关闭后续项：1（`ISSUE-053-009`）。
- 后续项净变化：-1；无悬空项。

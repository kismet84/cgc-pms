# 第73条主线：项目级统一文件中心与受控 Office 在线预览验收报告

> 验收日期：2026-08-05
> 实施基线：`origin/master@b718e50dfda0c0f5d682b85f4df3ec528e50aaf7`
> 实施分支：`codex/mainline-73-75-20260805`
> 当前裁决：`G0-G5_LOCAL_PASSED / ISSUE-073-001_CLOSED / GIT_DELIVERY_IN_PROGRESS`

## 1. 结论

项目文件中心、业务附件只读投影、服务端版本、权限交集、历史导入、对账、预览任务与派生物清理已实现并通过本地 G0～G5。原业务模块继续掌握业务证据维护权；中心不复制原文件。Git 同 SHA CI、PR、合并与 post-merge 继续独立取证。

## 2. 关键证据

- V282 MySQL/H2 建立目录、版本、分类、权限和持久转换任务；真实本地 MySQL 已迁移至 V284（V283/V284为第75条通讯及多租户RBAC正向补丁）。
- 通用附件上传与删除在原业务事务同步维护只读索引；投标逻辑名和版本链原样投影；来源权限与项目数据范围取交集。
- 历史分类由 `business_type + document_type` 冻结映射；普通附件和投标逻辑链合并为统一候选流，跨批按原日期和 `created_at,id` 稳定编号，导入前按项目日期整体预检999上限。
- PDF/图片直接短签；OOXML 持久排队。输出重新校验、计算 SHA-256、扫描后才标记 READY；FAILED 稳定回读，不由点击无限重试。
- Alpine、LibreOffice、Python和字体版本固定；转换容器无外网业务网络、无数据库或对象存储凭据，非 root、只读根文件系统、资源受限。
- Office本地运行证据：等价运行镜像`sha256:a766e1ad80d0d2d1620ac40b80758c0be534789676c6a78a6b8243aab2061c97`；宿主与容器`server.py` SHA-256均为`d04af7f488814a1c90968976f460a3119781c35ef1e43285dc320dd88e2d96f5`；用户`10001:10001`、只读根、`CAP_DROP=ALL`、1GB、1CPU，仅接入`internal=true`网络。健康回读`ok`，真实4905字节DOCX转换为10663字节PDF并返回`200`与`%PDF-`头。
- 后端完整 `clean verify` 通过：324份测试报告、2540项测试、0失败、0错误、15跳过，JaCoCo分支覆盖率60.237%；前端 contracts、类型、lint（0错误）、70文件469项单测、87路由台账和生产构建通过。
- 内置浏览器 `/project/files` 实际 URL、标题、筛选、新建入口、空态和分页可见，1280px 无横向溢出，页面错误0、控制台错误0。

## 3. 失败分类与处理

- `code_defect`：独立复核发现业务写删未同步、预览审计/FAILED/异步状态、输出哈希、历史分类与999预检、部分投标链补投影、删除与转换竞争孤儿清理缺口；均在共享根因处修复并补回归测试。
- `environment_prerequisite`：Docker BuildKit 首次提取 LibreOffice 层发生 I/O 错误；重启 Docker Desktop、删除仅任务构建器并保留卷后，改用同 digest/同包版本任务容器做可观测运行态验真。
- `tool_invocation`：PowerShell Maven `-Dtest` 逗号参数未加引号及并发执行两次 `clean` 干扰产物；改为引号参数、单进程完整 `clean verify` 后通过，不判业务缺陷。

## 4. 风险、回滚与零悬空

- 应用回滚时关闭文件中心/转换入口并回退应用；保留新增表和索引供前滚，不删除原文件或已应用 migration。
- 第73条不授权生产或非本地环境操作。本地实现、同 SHA CI、Git 合并分别取证。
- 计划周期新增1、关闭1、净变化`0`；独立复核发现并关闭历史排序、部分投标链补投影、预览删除竞争3项，净变化`0`；无无载体遗留项。

# 第63条主线质量报告：项目授权与文档一致性整改

日期：2026-08-03

结论：本地实施与验收通过；Git 交付已授权，生产、部署和发布不在本轮范围。

## 实施结果

- 采购申请、采购订单、材料验收的列表、详情、明细、删除和重提路径统一复用 `ProjectAccessChecker`。
- 未指定项目的列表仅查询可访问项目；空范围返回确定性空页。
- 删除和重提在工作流、预算或状态写入前完成项目授权校验。
- README、后端规范、当前状态已与 POM/Flyway/Git 事实对齐；无消费者的 `backend/dtree.txt` 已删除。

## 验证证据

| 检查 | 结果 |
| --- | --- |
| 三服务定向与联合矩阵 | passed；跨项目读写拒绝、空范围、正向身份均覆盖 |
| 后端全量 `verify` | 2419 passed / 0 failed / 3 skipped；覆盖率门禁通过 |
| 漂移扫描 | README、POM、后端规范、当前状态和计划索引中的已确认版本/状态漂移 0 命中 |
| 派生依赖快照 | `backend/dtree.txt` 不存在 |
| 治理契约 | `test-codex-task-execution-policy.ps1` passed |
| 本地运行态 | backend `UP`，frontend HTTP 200 |

## 失败分类与边界

- `quality_or_security`：独立复核曾误报验收重提缺门禁；回读调用链确认 `validateReceiptForSubmission()` 首行已校验项目权限，且早于任何写入，二次独立复核通过。
- 当前 `cgc_pms` 无项目业务样本，未为浏览器负向链伪造跨项目数据；该边界由服务层真实数据库集成测试覆盖。
- 未执行 commit、push、PR、生产连接或发布。

零悬空统计：本轮关闭 `ISSUE-063-001`、`ISSUE-063-004` 2 项；新增后续项 0，关闭后续项 2，净变化 -2；计划全周期净变化 0。

# ISSUE-037-003 驾驶舱项目数据范围统一收口验收报告

## 结论

- 结论：通过。
- 阻塞：无。
- 上线裁决：本报告只证明本地开发环境与目标安全回归通过，不授权生产发布。

## 交付范围

- 项目经理指定项目入口执行租户、dataScope 和 ACTIVE 状态校验；指定项目的滞后列表最多只包含自身。
- 项目经理全项目入口一次取得可见 ACTIVE projectIds，待办与审批通过 `WfTask.instanceId -> WfInstance.projectId` 过滤，到期合同直接按 projectIds 过滤。
- 管理驾驶舱的项目数、排名、金额汇总、全部待办、逾期待办和 HIGH 风险使用同一可见 ACTIVE projectIds。
- 空项目、非 ACTIVE、不可见、缺少项目关联和跨租户工作流实例/风险记录一律 fail-close；空可见项目返回结构完整的零/空结果。
- 只复用现有 `ProjectAccessChecker`，未修改角色/dataScope 语义、实体、数据库、API、前端或其他业务模块。

## 验收证据

- Ready lint：通过，风险等级高、任务性质缺口修复，无 errors/warnings。
- TDD：先观察到隐藏指定项目未拒绝、SELF 管理驾驶舱返回 4 个 ACTIVE 项目；修复后新增测试转绿。
- 审查补修：独立审查发现“可见但非 ACTIVE 的指定项目”仍可进入聚合，补充 COMPLETED 项目失败测试并在访问校验后返回 `PROJECT_ACCESS_DENIED`；最终复核通过。
- 后端目标回归：`DashboardProjectBusinessServiceTest,DashboardFinanceManagementServiceTest,DashboardPerformanceTest` 共 21 项，通过 21、失败 0、错误 0。
- 性能证据：管理驾驶舱 5 项目场景实测 17 条 SQL，满足当前测试门槛 `<=20`，保持批量查询而非按项目 N+1。
- 静态门禁：`git diff --check` 通过。
- 运行态：后端重建并等待 180 秒后，health 为 `UP`；backend、frontend、dev-login 均返回 200，dev-login 最终落到 `/dashboard`。
- 独立安全审查：指定项目、全项目、管理汇总、orphan/跨租户、空集合、数量/列表一致和批量查询均通过，无残余阻塞。

## 风险与回滚

- 历史任务或风险记录若缺少有效 ACTIVE projectId，将不再对受限用户显示；这是预期的 fail-close 行为。
- `DashboardPerformanceTest` 的显示名/输出仍写 `<=10`，实际断言门槛为 `<=20`；属于非阻塞测试文案债，已进入 Current Focus，不影响本次安全裁决。
- 安全回滚不得恢复租户全量：保留指定项目访问校验；若全项目聚合异常，应返回结构完整空结果或临时禁用入口。
- 不涉及数据库结构、存量数据、角色授权或 API 契约回滚。

# ISSUE-053-032 M6结算台账、详情与追溯V2验收报告

> 验收日期：2026-07-26
> 验收范围：本地 dev/test/demo
> 结论：通过
> 阻塞项：0

## 1. 裁决

- `/settlement`受控重定向至`/settlement/list`；列表与详情由真实Clean-room V2页面承接，Legacy导入为0。
- 路由台账达到`LEGACY_ONLY=34 / V2_ACCEPTED=53 / V2_SOURCE_AVAILABLE=0`。
- 结算新增、编辑、明细、删除、提交及来源、付款、成本、附件、审批追溯成立。
- 合同、计量、合同清单和金额均由服务端裁决；前端只展示字符串并在写后重读。
- 未新增数据库migration、前端账本、结算公式或依赖；未切正式入口、退役Legacy、commit、push、PR或发布生产。

## 2. 服务端权威与权限

- 新增与编辑只接受业务选择和扣款输入；项目归属由合同事实确定。
- 服务端提交前锁定结算、合同、签证、计量、付款和合同清单，并从同一锁定事实集重建数量、单价、金额；切换合同会清除旧合同明细后重建。
- 非分包、未审批或非履约合同在新建/编辑前失败关闭；负扣款、非法来源、跨项目/合同、重复结算和非法状态同样失败关闭。提交使用行锁与CAS，失败不留下部分事实。
- `settlement:query/add/edit/delete/submit`分别控制查询和动作；结算附件对象权限纳入文件上传、下载、预览和删除授权。
- 结算附件写入、删除与提交共用结算行锁；两种竞争顺序均等待后失败关闭，不会在提交附件计数后并发删证据。
- 删除权限不依赖编辑权限；删除由结算服务精确级联附件、明细和计量关联，并在主记录删除返回0时回滚全部前置变更。删除前释放合同和业务编号唯一键，重复新建、删除同一合同可恢复草稿成立。
- 业务编号作为详情入口；原始主键不作为页面主信息，状态、来源类型和公式版本映射为中文。

## 3. 受控示例数据与恢复

- 受控demo包增加结算合同清单、已审批计量来源、动作权限、有效审批人及一个无结算候选合同；装载阶段增至23。
- `load.ps1 -Environment demo -Database cgc_pms_demo_v2`与`verify.ps1`通过。
- 真实API在项目`520000000000009002`、合同`520000000000014201`完成`DRAFT → APPROVING → WITHDRAWN`；服务端回读已计量与最终结算均为`100000.00`。
- 附件按既有`OTHER`文档类型上传并通过病毒扫描；撤回后保留附件并直接删除结算。删除后结算、附件、明细和计量关联活动数均为0；候选合同列表前后均为0条活动结算，demo verify恢复为`settlement_draft_candidate=1`。
- `settlement_workflow_approver_invalid=0`；真实提交未再出现无效审批人。

## 4. 自动化证据

- 后端结算、文件权限、并发与事务故障注入专项：10类134/134通过。
- V2目标单测：8项通过；V2全量单测：47个文件、346项通过。
- 合同类型、V2类型、Lint、Clean-room边界、路由台账、构建、包体和`git diff --check`通过。
- Lint仅报告6条既有`e2e/m1-shell.spec.ts`格式警告，0错误。
- Playwright目标套件：5/5通过；含仅删除权限正样本，axe serious/critical为0，console error/warning为0。
- Ready lint通过，契约哈希：`6a4a765f55c63463d7bc90166e779f58bbaa44acd67452954616cd6e07f3c76e`。

## 5. 真实浏览器证据

- 当前fat JAR后端健康检查为`UP/200`，数据库为`cgc_pms_demo_v2`，V2 dev-login可用。
- 1440×900、1024×768、390×844三视口均无页面级横向溢出。
- 项目选择器可见且可用；列表、新增和详情使用真实后端数据。
- 详情显示业务编号和中文枚举，不显示原始主键主信息；金额无重复货币符号。

## 6. 失败分类与关闭

- `quality_or_security`：Vue多行事件表达式、重复货币符号和英文枚举均已修复并复验。
- `test_or_fixture`：Playwright弹窗动画等待、Vitest误收同名E2E、demo缺少合同清单与可恢复候选均已修复。
- `test_or_fixture`：demo结算审批节点引用无效用户；改为当前租户启用admin，并纳入verify失败关闭指标。
- `environment_prerequisite`：Windows Docker中的旧Vite watcher已重启并以当前代码复验。
- `tool_config`：本地pnpm覆盖未向V2安装树应用`brace-expansion`补丁；仅修复被忽略的生成安装树后完成Lint，未引入源码依赖。
- `tool_usage`：首次API写入未带XSRF头返回403；按真实会话契约重试通过，不属于业务缺陷。
- `expected_business_rejection`：已有合同重复创建结算被`STL_DUPLICATE_SETTLEMENT`正确拒绝。
- `quality_or_security`：前端上传使用未登记文档类型、删除草稿时软删除唯一键冲突；分别复用既有`OTHER`契约、在删除前释放唯一键后复验通过。
- `quality_or_security`：独立复核发现附件删除可与提交竞争、结算删除未自行级联附件且未检查主记录删除结果；统一结算行锁、精确附件级联、返回值校验及事务故障注入后关闭。

## 7. 零悬空与回滚

- 本轮修复并复验：全部032范围发现项。
- 超出范围并正式承接：0。
- 证据不足或无明确价值而关闭：0。
- 新增后续项0、关闭后续项0、后续项净变化0、悬空项0。
- 最小回滚：回退032页面、薄服务/契约、结算与文件权限根修、路由/导航/台账、测试、demo增量及治理回写；030～031、Legacy和数据库结构保持不变。

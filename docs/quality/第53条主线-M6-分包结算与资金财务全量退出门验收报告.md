# 第53条主线 M6 分包结算与资金财务全量退出门验收报告

## 裁决

- 结论：通过；M6本地正式退出门满足。
- 阻塞项：0。
- 路由：16条目标路由全部由真实Clean-room V2页面或受控重定向承接。
- 台账：`LEGACY_ONLY=24 / V2_ACCEPTED=63 / V2_SOURCE_AVAILABLE=0`。
- 证据层级：本地当前源码、dev/test/demo运行态；不等同同SHA CI或目标环境发布证据。
- 发布边界：未commit、push、PR、切正式入口、退役Legacy或操作生产。

## 业务闭环证据

- 分包任务与计量：任务、合同清单、计量、附件、提交、审批及金额均按服务端项目/合同/合作方范围裁决。
- 结算：计量、签证、付款、合同清单与结算快照在服务端锁内重算；附件、提交和删除保持原子性。
- 付款、费用、收入与发票：来源、可付余额、应收、核销、付款、回款、发票校验和项目范围保持服务端权威。
- 资金与会计：日记账、预测、凭证、会计期间、月结和反结账保持对象锁、CAS、统一锁序、借贷平衡及写后回读。
- V2不建立平行资金账，不本地计算权威金额、来源、状态或期间结论，不开放手工凭证生成入口。

## 16路由完成矩阵

| 路由 | V2承接 | 服务端权威事实 |
| --- | --- | --- |
| `/subcontract` | 重定向`/subcontract/task` | 不创建第二分包入口 |
| `/subcontract/task` | `SubcontractWorkspacePage` | 任务、项目/合同/合作方范围、状态 |
| `/subcontract/measure` | `SubcontractWorkspacePage` | 合同清单、计量数量/金额、附件、提交 |
| `/settlement` | 重定向`/settlement/list` | 不创建第二结算入口 |
| `/settlement/list` | `SettlementWorkspacePage` | KPI、结算快照、金额、状态、项目范围 |
| `/settlement/:id` | `SettlementWorkspacePage` | 来源、签证、付款、成本、附件与审批追溯 |
| `/payment` | 重定向`/payment/application` | 不创建第二付款入口 |
| `/payment/application` | `ReceivablesWorkspacePage` | 付款来源、可付余额、审批、支付与单据 |
| `/payment/expense` | `ReceivablesWorkspacePage` | 费用明细、审批、付款关联 |
| `/revenue` | `ReceivablesWorkspacePage` | 业主结算、应收、回款、核销与追溯 |
| `/invoice` | `ReceivablesWorkspacePage` | 发票识别、校验、业务关联和文件安全 |
| `/finance-operations` | `FinanceControlWorkspacePage` | 收付、对账、快照、计划与预警 |
| `/cash-journal` | `FinanceControlWorkspacePage` | 账户、日记账、归档、重开、冲销与互链 |
| `/cash-forecast` | `FinanceControlWorkspacePage` | 预测版本、缺口措施、审批和实际回写 |
| `/accounting-entry` | `FinanceControlWorkspacePage` | 凭证复核、过账、驳回、重提、冲销和借贷平衡 |
| `/financial-close` | `FinanceControlWorkspacePage` | 期间检查、对账、关账、反结账和月结追溯 |

矩阵逐项源自当前路由装配、`24/63/0`台账、后端全量、V2全量、四组Playwright、demo与内置浏览器；16路由均未落入`ShellPlaceholderPage`或Legacy组件。

## 自动化证据

- Ready lint：Ready阶段hash `ae0d7cb96cdfa987654995bb5215626607a5fea0e39162e014b1a9402121d658`通过；终态为Done，改做治理一致性验真。
- 后端全量：273类，2263项，0失败、0错误、3跳过，Maven `BUILD SUCCESS`；当前源码fat JAR `BUILD SUCCESS`。
- M6根修复验：034后端联合77/77、DTO 14/14、资金/月结根修15/15及锁序11/11；035污染源并行组合与生产计量/收入闭环通过。
- V2单测：48文件，351项通过。
- M6 Playwright：分包、结算、收付款/收入/发票、资金/会计四组，三视口合计12/12通过。
- 契约/V2类型通过；ESLint 0错误、6项既有M1 Prettier警告；边界扫描173个V2文件、16个契约文件。
- 路由台账生成与检查通过：87条路由、73个视图、65个唯一视图，精确`24/63/0`。
- 生产构建222模块、42个JS资产，包体检查通过。
- demo verify：`cgc_pms_demo_v2` 23阶段通过；数量、金额、来源、库存、结算、回款、发票和会计借贷差额均守恒。
- 当前fat JAR重启后，内置浏览器验收财务月结、资金运营和结算台账；演示项目选择器启用、真实数据可见，console error/warn为0。

## 失败分类与本轮根修

- 首轮后端全量为2263项、4失败、3错误；全部按统一失败分类处理，未降低断言、并行度或治理基线。
- `quality_or_security`：新增两处`owner_settlement SELECT * FOR UPDATE`令数据库治理计数从221升至223；改为显式列投影，保留行锁，计数恢复221。
- `test_or_fixture`：结算KPI路由测试仍mock五参数重载，Controller已调用七参数重载；同步当前契约后通过。
- `test_or_fixture`：结算服务测试与查询测试共享命名H2和合同30001，并行付款事实触发正确超付门；提交/附件并发用例改用独享分包合同，未调高金额或改变门禁顺序。
- `quality_or_security`：业主结算提交的工作流附件门读取持久化`attachment_count`，旧顺序在工作流后更新；现先以CAS持久化CLEAN附件数，再启动工作流并更新审批状态，失败整事务回滚。
- `test_or_fixture`：生产计量闭环缺少业主结算CLEAN附件及精确清理；补齐当前业务前置和清理范围。
- 根修后原失败类31/31、污染源并行组合通过，最终后端全量2263项通过；无未分类失败。

## 治理与零悬空收口

- `ISSUE-053-030～035`全部Done；M6无剩余Ready。
- 正式报告、M6计划、总计划、Ready、Current Focus、Done、项目地图与路由台账一致。
- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- 悬空项：0。
- M7未启动；本轮不自动创建M7 Ready。

## 剩余边界与回滚

- 正式入口切换、Legacy退役、同SHA CI和目标环境发布仍未授权，也未由本报告证明。
- 回滚035只回退本轮结算/收入测试隔离、业主结算附件提交顺序、显式列查询、KPI测试契约与治理回写；不改写030～034已验收业务事实或数据库结构。
- 若后续同SHA CI或目标环境证据失败，M6发布裁决为不通过；保持Legacy正式入口。

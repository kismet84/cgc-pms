# ISSUE-053-028 M5领料、出库与退料V2验收报告

## 2026-07-26复审裁决

- 结论：当前M5页面范围通过。
- 阻塞项：0。
- 路由：`/inventory/material-requisition`继续由Clean-room V2承接。
- 台账边界：该路由项保持`V2_ACCEPTED`；全局迁移计数正受M6在制变更影响，不作为本次M5页面裁决依据。
- 发布边界：未commit、push、切正式入口、退役Legacy或操作生产。

## 复审发现与本轮修复

- 列表补齐`dateFrom/dateTo`服务端筛选；报表周期按自然月形成含首尾日的闭区间，不再只改页面标签。
- 指定项目查询先执行项目访问校验；全部项目查询仅使用当前用户可访问项目ID，无可访问项目时返回空。
- 更新领用单同时校验原项目与目标项目，防止借编辑跨项目迁移业务数据。
- 全部项目口径显示项目列；仓库编码与名称分列，不再以混合文本代替结构化字段。
- 新建与编辑统一要求创建权限；详情、编辑器、退料与冲销均使用V2对话框和服务端状态，权限不足时失败关闭。
- 退料幂等命中后仍校验原领用项目访问权，并严格核对同一请求事实；不因复用幂等结果绕过项目边界。
- 领用、出库、退料、冲销后的库存数量、金额和状态只以服务端回读为准；未引入前端库存计算或平行台账。

## 示例链路补齐

- 浏览器复审发现：核心示例领用单`REQ-20260718-001`状态为已批准，但详情显示0条审批记录。
- 根因：核心领用单存在业务、明细和出库流水，缺少匹配`MATERIAL_REQUISITION + 业务ID`的工作流实例与审批记录。
- 已沿用现有demo种子模式补齐工作流实例、完成节点、已办任务和有效审批记录；审批发生于出库流水之前，字段使用真实项目、合同、金额、用途和意见。
- `CGC-COMPLETE-PROJECT`受控增量装载通过；`requisition_approval_trace=1`、`role_dashboard_requisition_trace=1`、`completed_stage=23`，总体验证`passed=true`。

## 验证证据

- 后端联合专项：库存、领用、退料、权限与幂等相关测试共88项通过，0失败、0错误、0跳过。
- V2类型检查通过；M5目标单测19项通过。
- M5目标Playwright：24项通过；覆盖项目/期间筛选、权限失败关闭、详情、出库、退料、冲销、重复请求、三视口与可访问性。
- 浏览器运行态：有效项目加`2026-07`期间返回0条，正确排除2025-04历史领用；全部项目、全部期间返回2条并显示项目、仓库编码和仓库名称；详情显示1条领用明细和1条库存流水；表格可聚焦；控制台无错误或警告。
- demo装载后的数据库契约验证确认核心已批准领用单具备1条有效审批轨迹。

## 全局门边界

- 全局`design-system.test.ts`仍有2项失败，均定位到在制M6 `ReceivablesWorkspacePage.vue`。
- 分类：`quality_or_security`；由已启动的`ISSUE-053-033`承接，不是M5领退料页面缺陷。本报告不据此宣称全仓全局门已绿。

## 零悬空收口

- 本轮M5发现：全部修复并复验。
- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- 无载体遗留项：0。

## 回滚

- 回退领退料查询范围、权限/幂等修复、V2页面、契约和对应测试。
- demo数据仅在本地`cgc_pms_demo_v2`增量装载；需要回滚时重建该本地demo库，不影响生产或其他数据库。

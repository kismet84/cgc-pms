# ISSUE-053-028 M5领料、出库与退料V2验收报告

## 裁决

- 结论：通过。
- 阻塞项：0。
- 路由：`/inventory/material-requisition`由真实Clean-room V2承接。
- 台账：`LEGACY_ONLY=40 / V2_ACCEPTED=47 / V2_SOURCE_AVAILABLE=0`。
- 发布边界：未commit、push、切正式入口、退役Legacy或操作生产。

## 实现证据

- `RequisitionWorkspacePage.vue`复用V2请求层、权限守卫和设计系统，覆盖服务端分页列表、详情、明细、创建、更新、删除、提交、审批后出库、退料确认/冲销及trace。
- 查询、新增、编辑、删除、提交、出库和退料按七组权限独立失败关闭；动作是否可用同时服从服务端状态。
- 领料数量、单价、金额、审批、出库、库存流水和退料结果仅展示写后服务端回读；前端不累计库存、成本或状态。
- 新建领料要求完整增改删权限；明细保存失败仅删除本次新建草稿，不触碰既有数据。
- 退料绑定原领料明细与原出库流水，提交唯一幂等键；冲销后重新读取详情和trace。
- 未新增后端业务代码、数据库迁移、依赖、平行库存账或通用手工出入库入口。

## 验证证据

- Ready lint：`ISSUE-053-028`通过，hash `9a6cd525eaccac3e3e5c677301a89a1037b07e16c0376b4f679cda72799f3474`。
- 后端领退料专项：3类，4项通过，0失败、0错误、0跳过。
- V2单测：41文件，288项通过。
- 目标E2E：7/7通过；覆盖权限失败关闭、服务端状态/数值、草稿精确回滚、出库、原流水绑定退料、冲销、重复点击、三视口和axe。
- 契约/V2类型、ESLint、Clean-room边界、台账检查、生产构建、包体与`git diff --check`通过；边界扫描155个V2文件、14个契约文件，构建35个JS资产。
- 本地运行态：`frontend-v2`容器刷新后`http://127.0.0.1:5174/v2/`返回200；未连接生产。

## 失败分类与修复

- 实现错误：模板内联点击表达式包含多语句，Vite编译失败；收敛为`openReverse()`方法，生产构建与目标E2E复验通过。
- 测试定位：同名按钮触发Playwright strict locator；限定首个目标后通过，未改业务语义。
- 验证门补充：`vue-tsc`未发现上述模板编译问题，故保留生产构建为必要验收门。

## 零悬空收口

- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- 悬空项：0。
- 下一串行项：`ISSUE-053-029` M5全量回归与正式收口；由主线正式承接，不属于028遗留缺陷。

## 回滚

- 回退领退料V2页面、契约/服务扩展、路由/台账和测试。
- 保留024～027、Legacy、数据库和既有业务数据；无需数据回滚。

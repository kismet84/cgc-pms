# 第75条主线：站内即时通讯系统验收报告

> 验收日期：2026-08-05
> 实施基线：`origin/master@b718e50dfda0c0f5d682b85f4df3ec528e50aaf7`
> 当前裁决：`G0-G5_LOCAL_PASSED / ISSUE-075-001_CLOSED / GIT_DELIVERY_IN_PROGRESS`

## 1. 结论

同租户启用用户的一对一、群聊、纯文本/附件、未读、SSE提醒、完整断线补拉及群成员治理已形成服务端权威闭环；系统通知中心保持独立。

## 2. 关键证据

- V283 MySQL/H2 建立会话、成员、消息三表及租户0 RBAC；V284以正向 migration 为所有已有启用租户补齐通讯菜单与角色授权，不修改已应用V283。成员加入/离开序列限定历史边界，关闭群禁止继续治理或发送。
- 发送采用草稿、附件、最终发送三步；服务端唯一键与并发回读保证幂等，前端重试状态绑定正文和文件指纹，载荷变化先取消旧草稿。
- SSE 每用户最多5连接、25秒心跳；事件只作提示，页面按100条循环补拉直至完整，并用 generation 阻止快速切换会话的乱序覆盖。
- 不存在、跨租户、非成员和附件越权统一返回 `404 RESOURCE_NOT_FOUND`，避免资源枚举。
- `COMM-01/05`：H2并发回归与真实本地MySQL并发测试均证明正反顺序创建只产生1个私聊会话和2个成员，并发发送序号为1/2；真实MySQL测试还暴露并修复了REPEATABLE READ旧快照回读根因。
- `COMM-02/04`：跨租户、非成员、加入前附件均按404隐藏；移除后重入只看到最新加入序号后的消息，另有重入后旧附件不可见的显式回归。
- `COMM-03/06`：群主/管理员/成员、关闭群、阅读游标与未读回读由服务测试和服务端锁/约束覆盖。
- `COMM-07/08`：5个附件成功、第6个拒绝、恶意扫描拒绝、已发送不可变、过期草稿只删自身附件；共享`FileServiceTest` 51项与`FileTypeValidatorTest` 17项覆盖20MB、伪MIME、恶意文件和存储补偿。拒绝路径不产生可见消息或SSE。
- `COMM-09/10`：SSE连接上限/心跳、REST完整补拉、纯文本渲染、RBAC、快速切换防乱序及顶栏未读请求身份保护均有自动化与浏览器证据。
- 后端完整 `clean verify` 通过：324份测试报告、2540项测试、0失败、0错误、15跳过，JaCoCo分支覆盖率60.237%；前端 contracts、类型、lint（0错误）、70文件469项单测、87路由台账和生产构建通过。
- 环境门开启后的真实本地MySQL专项机读报告：`CommunicationMySqlConcurrencyTest` 1项、0失败、0错误、0跳过；H2迁移与附件授权补充回归38项全绿，显式证明普通角色仅获view/send且无group-manage、重入后旧附件不可见。
- 内置浏览器实际创建私聊并发送“计划75浏览器验收消息 2026-08-05”，服务端回读为已发送；联系人、会话、消息编辑器和未读入口可见，页面错误0、控制台错误0。

## 3. 风险、回滚与零悬空

- 回滚时停用通讯入口和接口并回退应用；保留消息表供前滚，不删除附件对象或已应用 migration。
- 不包含在线状态、输入提示、逐条回执、WebSocket、消息队列或外部渠道。
- 失败分类：`code_defect`包括多租户RBAC漏授、MySQL REPEATABLE READ重复键后旧快照、快速会话切换响应串写和未读旧请求覆盖，均已修复并复验；`test_fixture`包括旧E2E未模拟新顶栏未读/SSE请求及MySQL基线仍硬编码V280/203表，分别补共享shell fixture和V284/208表预期后复验；`static_analysis_false_positive`为SQL扫描器命中Spring `@Scheduled`配置占位符，按既有同线白名单协议标明其非SQL语义；`tool_invocation`包括宿主健康检查旧端口和并发`clean`干扰，改用实际8080端口与单进程全量门后通过。
- 最新JAR本地容器健康`UP/healthy`，Flyway从V283成功正向迁移至V284；不代表非本地发布证据。
- 计划周期新增1、关闭1、净变化`0`；独立复核7项与真实MySQL并发根因均在本轮关闭，后续项净变化`0`；无无载体遗留项。

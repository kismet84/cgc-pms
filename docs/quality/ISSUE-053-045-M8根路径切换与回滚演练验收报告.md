# ISSUE-053-045 M8 根路径切换与回滚演练验收报告

> 日期：2026-07-27
> 分支：`master`
> 环境：本地 dev 运行态 + demo 数据
> 演练端点：`127.0.0.1:5180`
> 裁决：`PASSED`

## 1. 结论

同一 `frontend-v2-root` 镜像完成 `V2_ROOT → LEGACY_ROLLBACK → V2_RESTORE`。三阶段健康、dev-login、用户信息 API、SSE、静态资源和代表性深链均通过。演练使用独立 Compose project、固定 loopback 端口和外部 dev 网络；结束后演练容器全部删除，既有 5173、5174、8080 保持 200。

该结果仅证明本地切换与回滚机制，不构成正式入口、目标环境或生产切换。

## 2. 可重复入口

```powershell
.\scripts\m8\invoke-root-switch-rehearsal.ps1 -Port 5180
```

演练载体固定连接本地 `deploy_cgc-pms-dev-net`，不接受目标网络参数：

- `deploy/docker-compose.m8-rehearsal.yml`
- `deploy/m8/nginx.v2-root.conf`
- `deploy/m8/nginx.edge.conf.template`
- `scripts/m8/invoke-root-switch-rehearsal.ps1`

## 3. 阶段证据

| 阶段 | 前端身份 | 静态资源 | 深链 | 浏览器 | API | SSE |
| --- | --- | --- | --- | --- | --- | --- |
| `V2_ROOT` | `CGC-PMS V2` | `/assets/app-DjianX7L.js` | dashboard、project、approval、system users | 1/1 | `UP` | `text/event-stream` |
| `LEGACY_ROLLBACK` | `建筑工程总包项目管理系统` | `/@vite/client` | dashboard | HTTP恢复 | `UP` | `text/event-stream` |
| `V2_RESTORE` | `CGC-PMS V2` | `/assets/app-DjianX7L.js` | dashboard、project、approval、system users | 1/1 | `UP` | `text/event-stream` |

两次 V2 阶段资产路径一致，证明恢复使用同一根路径制品。每次 edge 切换均先通过 `/healthz` 和 `/api/actuator/health`，再执行登录、API、SSE、静态资源和深链验证；V2 两阶段另以真实 Chromium 完成四条深链直达、页面标题和刷新恢复，控制台/page error 为零。

## 4. 根因修复与失败分类

| 发现 | 分类 | 处置 |
| --- | --- | --- |
| V2 workspace 补丁路径仍依赖 Legacy 目录，Docker 冻结安装无法构建 | `quality_or_security` | V2 自带同一安全补丁，Dockerfile 只复制 V2 补丁目录；`--frozen-lockfile` 构建通过 |
| PowerShell 将 health JSON 作为字节数组返回 | `tool_invocation` | UTF-8 解码后再解析；代理 health 为 `UP` |

未修改生产 Compose、正式入口、后端业务、数据库或 Legacy 业务实现。

## 5. 清理与恢复

- 演练 Compose project 终态无容器。
- `127.0.0.1:5180` 终态不可达，符合自动清理预期。
- `127.0.0.1:5173/`、`127.0.0.1:5174/v2/login`、`127.0.0.1:8080/api/actuator/health` 终态均为 200。
- 失败时 `finally` 同样执行演练 project `down`；不会停止现有 dev project。

`ISSUE-053-045`通过，`ISSUE-053-046`进入 Ready。新增后续项0、关闭后续项0、后续项净变化0、悬空项0。

# ISSUE-048-009 系统用户编辑详情权威加载验收报告

结论：通过；阻塞：无；可合并：是；可推送：否（`autoPush=false`）。

## 范围与实现

- 前端新增 `getUserDetail(id)`，固定调用既有 `GET /system/users/{id}`；编辑动作等待详情成功后才打开弹窗。
- 表单只映射详情的用户名、姓名、手机、邮箱和角色ID；密码始终为空，不接收或回传租户与服务端其他字段。
- 详情加载失败只提示重试，不回退分页行快照；序列号保证快速切换只接受最后请求，点击新增使在途编辑请求失效。
- 后端生产代码、数据库结构、权限码、角色映射、租户隔离和用户更新事务均未修改。

## 验收证据

- 后端专项：`SysUserDetailControllerTest` 3/3与 `TenantIsolationTest#testCrossTenantUserRead` 1/1通过；覆盖管理员、`system:user:query`、无关权限403、响应无密码字段和跨租户 `USER_NOT_FOUND` 语义。
- 前端专项：3/3通过，覆盖详情GET、成功后开窗、无分页行兜底、密码空值、角色ID和新增失效序列。
- 工程门禁：TypeScript类型检查、目标ESLint零警告、生产构建、Ready lint、允许/禁止路径和 `git diff --check` 全部通过。
- 浏览器：隔离 Vite 与受控只读路由夹具验收当前实现；点击旧行 `stale-row-user` 的编辑动作后只观察到一次 `GET /api/system/users/1`，弹窗显示 `authoritative-user`、权威联系方式和项目经理角色，密码为空，旧行值未混入，控制台0条 warning/error。
- 浏览器验收未保存、分配角色、改变状态或发出任何业务写请求；权限与租户语义由Spring集成测试覆盖，不以前端夹具替代。

## Reviewer 复核

- 最终结论：PASS；无修复项，无数据正确性、权限、租户、敏感字段或前端时序阻塞。
- 权限与敏感字段：既有控制器仅允许管理员或 `system:user:query`；`SysUserVO` 无 `password`、密码哈希、令牌和租户字段。
- 租户边界：详情继续通过 `SysUserService.getById`和 MyBatis 租户条件查询，跨租户ID保持不可见；本轮未修改服务或实体。
- 前端时序：每次编辑先递增请求序号，完成时只接受当前序号；失败分支不执行表单映射或开窗。

## 风险与处置

- 用户详情端点为存量生产能力，本轮仅增加前端消费与边界回归；没有证据支持改权限、拆详情页或新增缓存，均按超范围关闭。
- 验收首次合并后端命令时遇到测试 JWT 密钥未继承，用显式测试密钥重跑后4/4通过；这是 `environment_config`，未触发业务代码修复。
- 最小回滚：回退前端详情API和编辑前加载逻辑，恢复分页行填表；后端与数据无需回滚。

## 治理收口

- `PI-2026-07-17-09` 已实施回写；唯一叶子 `A-01-SYSTEM-USER-DETAIL` 已从当前问题台账关闭，A-01剩余10项仍由原唯一载体承接。
- 本轮新增后续项：0
- 本轮关闭后续项：1
- 本轮后续项净变化：-1

剩余风险：浏览器使用受控只读响应稳定证明分页行与详情值不同时的表单行为；真实权限、租户和序列化边界由后端集成测试验证。生产发布、生产数据库和push均未执行。

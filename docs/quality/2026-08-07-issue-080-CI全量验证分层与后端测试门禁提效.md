# ISSUE-080 CI 全量验证分层与后端测试门禁提效质量报告

> 日期：2026-08-07
> 状态：`IMPLEMENTED / G0-G5_PASSED / GIT_DELIVERY_MERGED / POST_MERGE_VERIFIED`
> 环境：仅本地 dev/test/demo 与 GitHub Actions
> 基线：`master@4b4510054e88e0bb60068cc5b45962d0dbcd51bd`

## 裁决

G0～G5 通过，PR #408 已正常合并并完成同 SHA post-merge。未把 mock、局部测试、摘要 job、本地运行态或 push CI 当作 PR 独立证据。

## 变更与边界

- 37 个 Playwright spec 显式分为 contract 27、live 9、M8 special 1；选择器漂移与 contract skip 均 fail-close。
- 保留 `backend-test`、`e2e` 兼容 job id；完整后端与 12 类历史顺序敏感复验并行，后者禁用重复 JaCoCo。
- `e2e` 只运行 browser contract，不启动 backend、MySQL、Redis 或 MinIO；MySQL 五类专项仍由独立 job 负责。
- verifier evidence 固定为 14 项；`build-summary` 只展示。缺独立 reviewer，PR push reuse 不启用。
- 未新增依赖、schema、migration、第三方 CI、服务编排或非本地环境流程。
- M8 阻塞根因按现有能力最小修复：edge 响应头缓冲、legacy 构建缓存/超时、tab 内稳定 SSE clientId；通知与通讯五连接上限保持不变。

## 客观证据

| 门禁 | 结果 |
| --- | --- |
| 后端完整 | 基线连续 5 次通过；2577 测试，0 失败、0 错误、18 既有跳过，中位 437 秒；全部阻塞修复后最终 2580 测试、423.4 秒通过 |
| 后端覆盖 | instruction 78.61%、branch 60.57% |
| 顺序敏感 | 12 类、183 测试、0 失败、0 跳过；`jacoco.skip=true` |
| MySQL | 临时空库 15/15；migration、baseline、租户范围、付款并发、材料删除并发通过；0 临时对象残留 |
| 前端单测 | 72 文件、499 测试通过 |
| 浏览器 contract | 27 spec、98/98、0 skipped、0 unexpected、0 flaky、93.082 秒 |
| 前端静态 | lint 0 错误；类型检查、构建通过 |
| 运行态 | Health DOM/截图为当前 V2，frontend 与 API 代理均可达 |
| M8 | `127.0.0.1:5180` 三阶段通过；V2 root 与 restore 浏览器各 1/1，legacy rollback、API、SSE 均通过 |
| live | 9 个 spec 的隔离 `cgc_pms_demo_v2` 前置不存在；当前库未重置，试跑 79 项证据作废，不形成业务失败或通过裁决 |
| 治理 | workflow、pre-PR、post-merge、任务策略、HighRisk 主线与 Surefire summary 契约通过 |
| push CI | `8b1449f6f77feb9f15a63b9e59474e79c8fefe95`；run `31187798303`；14/14 required jobs，verifier PASS |
| PR CI | PR #408；独立 full run `31189007218`；14/14 required jobs |
| 合并 | `master@fbbb0592a5365c299b59eb3017d45d044d9051da`；post-merge run `31191656447` 成功 |
| 分支保护 | 14 required checks，`app_id=15368`，`strict=true`，`enforce_admins=true`；review requirement 与复用变量均缺失 |

## 五次远端性能裁决

连续成功 run：`31187798303`、`31188954522`、`31189007218`、`31190019114`、`31190699337`。

| 指标 | 样本秒数（升序） | 中位 | P90/最大 | 裁决 |
| --- | --- | ---: | ---: | --- |
| backend job | 382、509、527、579、650 | 527 | 650 | 未达 `<=500s` |
| backend verify step | 340、471、475、534、602 | 475 | 602 | 核心步骤低于 500s |
| order job | 82、87、119、156、217 | 119 | 217 | 与 full 并行且全绿 |
| browser job | 106、111、128、128、140 | 128 | 140 | 达到 `<=150s` |
| browser contract step | 79、82、101、102、102 | 101 | 102 | 达标 |

backend job 相对 G0 中位 648 秒下降约 18.7%，但固定 setup、字体、summary 与制品证据使整体中位仍高 27 秒。依据计划退出规则，不通过增加线程、删除测试或降低覆盖率追数字；关闭该优化假设且不新增无明确验收价值的 backlog。

## 失败分类

- 旧 Vite/CSS 运行态：`environment_prerequisite`；精确刷新前端容器后复验通过。
- PowerShell 未引用 Maven `-D` 参数：`tool_invocation`；引用参数后执行。
- 持久开发库含业务事实：`environment_prerequisite`；改用独立临时 schema/用户，完成后精确清理。
- 三份旧 E2E mock 缺 `tenantId`、通讯页对比度不足：`quality_or_security`；补齐契约并使用既有深色 token，完整 contract 通过。
- legacy 依赖下载超时：`environment_prerequisite`；保留官方 registry，增加 fetch timeout、BuildKit store 与 esbuild 完整性存在性校验，M8 原命令通过。
- M8 dev-login 502：`quality_or_security`；响应头总量超过旧 edge 默认缓冲，增加 16 KiB buffer 后通过。
- M8 SSE 403：`quality_or_security`；跨刷新随机 clientId 累积并触发既有五连接上限。改为 tab 内稳定 clientId，通讯端按 clientId 替换旧 emitter；未提高上限，单测与 M8 通过。
- live 演示库不匹配：`environment_prerequisite`；未加载或重置当前数据库，试跑结果不用于业务裁决。

## 零悬空

- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- 计划全周期问题源新增 1、关闭 1、净变化 0；`ISSUE-080-001` 已关闭，无载体外遗留项。

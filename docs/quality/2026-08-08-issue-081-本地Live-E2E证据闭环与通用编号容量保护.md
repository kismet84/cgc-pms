# ISSUE-081 本地 Live E2E 证据闭环与通用编号容量保护质量报告

> 日期：2026-08-08
> 状态：`IMPLEMENTED / G0-G5_PASSED / GIT_DELIVERY_MERGED / POST_MERGE_VERIFIED`
> 环境：仅本地 dev/test/demo 与既有 GitHub Actions
> 编制基线：`master@367d110e6e66dfbf3248d7b00299c98ebd526603`

## 裁决

G0～G5门禁通过，`ISSUE-081-001`关闭。实现与修复依赖SHA `75a84cb82651b998a08b4bd8c8925cdb414ce2d7`在隔离干净worktree取得9/9 live正式证据；独立复核7项发现全部整改并复验。交付源 SHA `fd2668bc13d611e7b76908bd7777655f37563bb6` 的 push CI、Pre-PR verifier 和独立 PR CI 通过；PR #412 已合并且 post-merge 验证成功。

## 变更与边界

- `verify-live-all.ps1` 从 `liveSpecs` 精确发现9组 spec，写前验证 loopback、reset marker、专用 `cgc_pms_demo_v2`、MySQL loopback绑定、backend实际库、租户和全部演示用户；任何前置失败均不加载数据。
- Windows 下统一 live 使用临时宿主 Vite dev `127.0.0.1:4173`，保留 DEV 角色切换并绕开 Docker 5173 源码模块连接耗尽；结束释放进程和日志句柄。日常 Docker UI 入口仍为5173。
- 普通 live spec 使用最小 EventSource 测试替身，避免80个用例消耗真实五连接上限；`shell-live` 独立打开、确认并关闭通知与通讯真实 SSE。
- pre-push `full` 更名为 `full-contract`；required `e2e` 继续仅运行27个 contract spec，不设置 `V2_LIVE_*`，不启动真实 backend/数据库。
- `CodeGenerationService` 单点约束三位日序号 `001..999`；所有普通、软删除和 offset 出口通过同一守卫。超过999抛 `BUSINESS_CODE_SEQUENCE_EXHAUSTED`，畸形历史号抛 `BUSINESS_CODE_SEQUENCE_INVALID`。
- 六个 Mapper 按编码长度和字典序查询含软删除历史，保证遗留1000优先暴露；`ContractRevenueMapper` 补齐 `DeletedCodeSource`，收入确认改为软删除占号。
- 无 schema、migration、编号扩位、序列表、Redis 计数器、required live CI、生产或非本地流程。

## 客观证据

| 门禁 | 结果 |
| --- | --- |
| 主线契约 | HighRisk `test-mainline-owner-flow.ps1` 通过 |
| workflow contract | required job、contract/live边界、push/post-merge evidence 自测通过 |
| live 脚本契约 | 7个安全负例；Vite上游显式绑定、backend容器/库只读绑定、错误库审计增量0、clean tree/SHA、持久目录、9个开关和SSE隔离契约通过 |
| 本地 live | 实现与修复依赖SHA `75a84cb8`：9/9 spec、80/80；0 skipped、0 unexpected、0 flaky；exit 0；working tree clean |
| live 证据 | `local-evidence/live-e2e/live-evidence-75a84cb82651-20260808T062516Z/summary.json`；持久忽略目录，不被后续Playwright清理；摘要无凭据 |
| browser contract | 27/27 spec、98/98；0 skipped、0 unexpected、0 flaky |
| M8 special | 保持独立1组 special；本轮未执行，不用于证明或阻塞第81条 contract/live/编号验收 |
| 后端公共与六调用方 | 8个相关测试类、107/107；0失败、0错误、0跳过 |
| 真实 MySQL | `CodeGenerationMySqlIntegrationTest` 5/5；六Mapper软删除/遗留1000排序与租户隔离，以及998→999、999耗尽、跨日归零、并发唯一键通过 |
| MySQL 清理 | `M81-RV-*` 任务记录残留0；未修改schema |
| 前端单测 | 73文件、502/502；设计系统专项31/31 |
| 前端静态 | contract类型、Vue类型、lint 0 error、build、bundle-size、clean-room边界、87路由台账通过 |
| 依赖安全 | pre-push发现`nanoid 3.3.16`新增高危通告后，workspace override最小升级至3.3.17；唯一解析版本3.3.17，audit 0漏洞 |
| 运行态清理 | 临时4173监听残留0；Docker 5173/8080既有本地入口保持可用 |
| push CI | 源 SHA `fd2668bc13d611e7b76908bd7777655f37563bb6`；run `31244041407`；14个 required jobs 与 build-summary 成功 |
| Pre-PR verifier | 同 SHA、同功能分支、push事件及14个 required jobs 校验 `PASS` |
| 独立 PR CI | PR #412；pull_request run `31244569723`；完整独立门禁与 build-summary 成功 |
| 合并与 post-merge | PR #412 squash 合并为 `08b2f111dafb562e85baeb8e4f66ff54f531ab11`；post-merge run `31244965147` 成功；实现源分支本地、远端均已删除 |

## 失败分类与处置

- `tool_invocation`：首次统一入口调用的外层命令超时参数过短；纠正调用后继续，不归因业务。
- `test_drift`：质量安全、技术管理和收尾 spec 未切换业务分区、使用过时缩写或未展开操作菜单；按真实可访问名称和控件交互修正，专项及全量复验通过。
- `test_data`：项目只读账号原指向实际有写权限的 `demo.business`；改用专用 `demo.schedule.query` 并由SQL/验证器锁定。
- `environment_prerequisite`：Docker Vite 开发态反复下载源码模块后出现 `ERR_NO_BUFFER_SPACE`，相关5173/8080 `TIME_WAIT` 达2070；统一入口改用临时宿主 Vite dev 后连续两次80/80。
- `tool_runtime`：首次80/80后临时日志句柄延迟释放使脚本退出1；显式释放进程对象并容错清理，复跑取得整体exit 0。
- `quality_or_security`：收入确认未包含软删除编号、旧生成器可产生1000、遗留长编号查询排序不足；公共守卫、六Mapper排序和真实MySQL测试修复并复验。
- `quality_or_security`：交付前registry新增`nanoid <3.3.17`高危通告；未绕过pre-push，以workspace override升级3.3.17，完整pre-push、502项单测和98项contract复验通过。

## 独立复核整改

- 3项P1全部关闭：临时Vite显式覆盖并恢复`VITE_API_TARGET`；登录前以backend容器端口和数据源只读绑定专用库，错误库负例审计增量0；live默认拒绝dirty并改用持久忽略目录。
- 4项P2全部关闭：仅接受ASCII三位后缀；demo/live/helper变化由真实分类器触发pre-push提示；BFCache `pagehide.persisted`不关闭SSE；六Mapper由真实MySQL统一验证。
- 复核后以干净任务提交SHA完成live复跑；未把整改代码本身等同最终证据。

## G0～G5

- `G0`：分支、基线、工作树归属、27/9/1清单、六调用方和code map冻结完成。
- `G1`：001/009/099/999、offset、软删除、遗留1000、畸形、租户、跨日和并发测试契约完成。
- `G2`：本地 live 安全前置、统一执行、证据摘要和门禁命名完成。
- `G3`：公共容量守卫、六调用方软删除历史排序、收入确认适配完成。
- `G4`：后端、MySQL、前端、contract和实现SHA live通过；独立复核7项已整改并复验。
- `G5`：质量报告与四个现行载体回写；同 SHA push CI、Pre-PR verifier、独立 PR CI、合并、post-merge 与实现源分支清理全部通过。

## 零悬空

- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- 计划全周期问题源新增1、关闭1、净变化`0`；`ISSUE-081-001` 已关闭。
- 未新增平行 backlog；M8 special、生产和目标环境不在本轮证明范围，不形成遗留项。

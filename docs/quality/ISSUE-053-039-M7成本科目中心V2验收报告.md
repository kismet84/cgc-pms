# ISSUE-053-039 M7 成本科目中心 V2 验收报告

> 日期：2026-07-27
>
> 基线：`master@07f0169da0a3c8ae3f6ee4de463dfe55da2e84de`
>
> 裁决：`PASS / ISSUE-053-039 Done / M7 CONTINUES`

## 1. 范围

`/cost/subject`、`/cost/subject/taxonomy`、`/cost/subject/rules`、`/cost/subject/scope`、`/cost/subject/trace` 由同一 Clean-room V2 页面按路由切片承接。

- 科目树、映射版本、归集规则、项目范围、引用影响、投标成本转入和财务费用分摊均复用既有服务端 API。
- 页面仅加载当前标签所需接口；写操作按后端精确权限显示，写后重新读取服务端事实。
- ID 统一转为字符串；金额与成本字段保留服务端字符串，不建立前端权威计算、平行状态或通用人工成本流转。
- `/cost/subject` 保留 query/hash 并确定性重定向到 taxonomy；Legacy 正式入口、数据库结构和权限码均未变。

## 2. 契约与安全证据

| 事项 | 结论 | 证据 |
| --- | --- | --- |
| 科目树与引用保护 | 租户、层级、编码、状态、末级约束和被引用删除均由现有后端服务裁决 | `CostSubjectControllerTest`、`CostSubjectServiceTest`、`CostSubjectReferenceProtectionTest` |
| 映射版本与规则 | 草稿创建、审批启用、唯一活动版本、末级目标和显式规则解析复用 V2 服务 | `CostSubjectV2ControllerPermissionTest`、`CostSubjectV2ServiceIntegrationTest` |
| 项目范围与目标成本 | 共享 `requireProject` 同时校验租户事实和项目数据范围；同租户非项目成员读写均 fail-closed | `CostSubjectV2ServiceIntegrationTest`、`ProjectAccessChecker` |
| 转入、分摊与追踪 | 审批实例、幂等键、状态、冲销和对账由服务端命令与查询裁决；转入、分摊及两种冲销拒绝同租户非成员，追踪列表仅返回可访问项目的完整事实 | `CostSubjectV2ServiceIntegrationTest` |
| 权限 | taxonomy、mapping、rule、scope、audit 读取及各写命令均使用后端既有精确权限 | controller permission test、router unit、V2 page unit |
| 历史升级 | 已应用 migration 不修改；现有升级契约继续通过 | `CostSubjectV2MigrationTest` |

## 3. 验证

| 门禁 | 结果 |
| --- | --- |
| 后端定向测试 | 6 个测试类，55 项，0 失败，`BUILD SUCCESS` |
| V2 目标 unit | cost-subject + router，25/25 通过 |
| V2 全量 unit | 52 文件、384 项全通过 |
| type/contracts | `pnpm type-check`、`pnpm type-check:contracts` 通过 |
| lint | 0 error；6 个既有 warning 均在未修改的 `e2e/m1-shell.spec.ts:149-156` |
| clean-room | 190 个 V2 文件、16 个契约文件通过 |
| route ledger | 87 条命名路由，`9/78/0`，无漂移 |
| build | 246 modules，构建通过 |
| 目标 E2E | 独立端口 5177，1/1 通过 |

失败处理遵循唯一分类契约：页面测试断言过宽归 `quality_or_security`，收紧为按钮权限断言；E2E 首次漏写 `/v2` 基路径归 `tool_invocation`，修正调用后最小复验通过；独立复核发现项目敏感接口仅校验租户存在归 `quality_or_security`，根修为共享项目数据范围门并补同租户非成员读、写、追踪和冲销回归。未放宽业务门禁。

## 4. 回滚与边界

- 回滚仅移除成本科目 V2 页面、服务、五条路由映射、测试和台账接受项；无数据库 migration。
- 正式入口、Legacy 退役、commit、push、PR、目标环境和生产均未授权。
- 下一唯一 Ready 为 `ISSUE-053-040`，承接 adminOnly 金丝雀与流程配置；不由本切片提前实现。
- 新增后续项：0；关闭后续项：0；后续项净变化：0。当前范围内无口头悬空项。

# 第94条主线：全仓代码职责拆分与重复实现收敛报告

> 日期：2026-08-13～2026-08-14
>
> 实施基线：`master@09b7e59bf72cfcf9681ba4257386089fc0743503`
>
> 实施分支：`codex/mainline-94-code-boundaries`
>
> 当前状态：`PASS / G0-G5_PASSED / GIT_DELIVERY_IN_PROGRESS`
>
> 环境边界：仅本地 dev/test/demo；本报告不构成生产、非本地环境、版本发布或上线裁决
>
> 计划载体：[第94条主线任务计划书](../plans/第94条主线-全仓代码职责拆分与重复实现收敛任务计划书.md)

## 1. 结论

第94条主线 `M94-F01～F18` 已完成本地实施、复验和唯一裁决。多路由巨型页面已按真实路由状态机拆成 focused page；公共 contracts、service client、router/navigation、共享样式和视觉壳恢复稳定 façade/barrel；工作流提交政策、文件业务访问政策和文件投影依赖形成单一事实源；成本、合同、现金、库存、质量及付款追溯服务仅抽取有独立价值的 query、assembler 或纯 validator，写命令、事务、锁序和状态机继续留在原 façade。

无法证明净收益或会破坏单快照、事务、补偿、官方控制面指纹的候选均有据保留。未建设微服务、通用状态框架、第二权限/金额/文件事实源或巨型 composable；未修改 migration、数据库结构、第90条字段/金额权威或状态语义。

本地 G0～G5、H2/MySQL、确定性浏览器契约、真实 live、API/DB/MinIO、依赖与构建产物安全扫描均通过。受保护 Git 交付仍在进行；完成同 SHA CI、PR、受保护合并及 post-merge 验真前，不把本地结论表述为已合并。

## 2. 基线、范围与不变量

- 基线：干净 `master` 与 `origin/master` 均为 `09b7e59bf72cfcf9681ba4257386089fc0743503`；计划回滚点为 `8ed14cdbcd7fce6f3babaadc67bff1484c5f2d21`。
- 唯一载体：本计划内 `M94-F01～F18`；`docs/backlog/current-issues.json` 不复制结构型子项。
- 保持不变：路由 URL/name/meta/query/hash、公共 import path、API 方法/端点、权限与租户 fail-close、金额字符串、工作流状态机、事务入口、锁序、幂等、文件 after-commit/补偿/重试、离线草稿恢复及服务端回读。
- 数据边界：migration diff 为空；未改已应用 Flyway、H2/MySQL baseline、schema、字段、索引或演示数据语义。
- 主线去重：不重开第91条查询性能、第92条 MinIO/备份可靠性、第93条服务端字段防篡改；不借结构拆分实施第90条。
- 依赖边界：无新框架；唯一依赖变更为安全修补，将既有 `nanoid` override 从 `3.3.17` 升至 `3.3.18`。

## 3. 18 项发现唯一裁决

| ID | 裁决 | 结果 |
| --- | --- | --- |
| `M94-F01` | 本轮修复并复验 | Codemap、计划、质量、焦点和索引统一；最终三件套重生成并 verify |
| `M94-F02` | 本轮修复并复验 | commercial/supply-chain/finance/system contracts 与 service client 按领域拆分，旧 barrel 兼容；重复 `PageResult` 删除 |
| `M94-F03` | 本轮修复并复验 | navigation 按六域拆分；router 分离 components/context/registry/guard；route ledger 87 条一致 |
| `M94-F04` | 本轮修复并复验 | PurchaseExecution 2,734 行拆为申请、订单、验收三页及内聚详情/附件边界；M91 application 补偿语义保留 |
| `M94-F05` | 本轮修复并复验 | 应收/资金控制 10 条财务路由直连 focused page；旧两入口只作兼容壳 |
| `M94-F06` | 本轮修复并复验 | Contract 1,891 行拆为台账、详情、创建/编辑及共享 editor；预算、附件、审批历史保持 |
| `M94-F07` | 本轮修复并复验 | CostSubject 前端四路由拆分；后端 1,346→221 行 façade + mapping/bid/finance 协作者；第90条语义未动 |
| `M94-F08` | 本轮修复并复验 | AccessControl 用户、角色、权限三路由拆分，共享纯 permission tree；授权仍 fail-close |
| `M94-F09` | 本轮修复并复验 | QualitySafety 抽离五类 panel、draft/sync 和后端 trace assembler；21 个事务与成本入账状态机原位 |
| `M94-F10` | 本轮修复并复验 | AppShell 1,445→416 行并拆视觉区/composable/CSS；DocumentCanvas 1,447→783 行并拆字段库、属性面板、纯 engine |
| `M94-F11` | 部分修复，其余有据关闭 | Project 四路由与 Variation 聚合已拆；其余 12 页仅一聚合或两条紧密路由，继续拆会复制权威加载或扩大 props 协议 |
| `M94-F12` | 本轮修复并复验 | 28 类提交权限及 4 类 dedicated-only 规则集中到不可变 `WorkflowSubmissionPolicy`；四个状态机服务未改 |
| `M94-F13` | 本轮修复并复验 | BusinessObjectAuthorizer 1,121→178 行唯一 façade；43 类型由不可变 registry 唯一解析到六组同步策略 |
| `M94-F14` | 本轮修复并复验 | CtContract、CashJournal、MatStock、QualitySafety 仅抽 query/assembler/read helper；写事务和命令生命周期原位 |
| `M94-F15` | 部分修复，其余有据关闭 | PaymentTrace 抽纯快照 validator，单快照/30 查询预算保持；PayApplication 与 loader 因锁序/单调用无净收益而保留 |
| `M94-F16` | 本轮解环，深化有据关闭 | `ProjectFileProjection` 使生产 file 包对 projectfile 源码依赖为 0；更深拆分会割裂事务、after-commit、补偿和重试 |
| `M94-F17` | 部分修复，其余有据关闭 | CSS 与随生产边界调整的 focused tests 已拆；AutoPilot compatibility 入口因官方指纹硬约束完整撤回并复验 |
| `M94-F18` | 本轮修复并复验 | 12 个等价金额空值 helper 统一到 `BigDecimalUtils.nvl`；采购文档事件发布/监听合并；Partner/PageResult/透明包装收敛 |

正式后续项新增 0、关闭 0、净变化 0。18 项均为“本轮修复并复验”或“证据不足/无明确价值而关闭”，无无载体遗留。

## 4. 主要实施结果

### 4.1 前端路由页与视觉边界

- 合同 4、采购 3、成本科目 4、访问控制 3、财务 10、项目 4、变更 1，共 29 条业务路由直接加载 focused page；旧公共 SFC 仅保留兼容分派。
- 每页拥有独立加载、分页、AbortController、选择与写后回读状态；共享层仅容纳纯 model、CSS、附件/详情或表单规则，无千行 composable 平移。
- `AppShell` 继续拥有 session、route/context、移动焦点、body lock、演示切换和 `RouterView`；视觉区、通知/通讯状态分别内聚。
- `DocumentCanvas` 继续拥有中央拖放、框选、吸附、对齐和 schema commit；字段库、属性面板、纯计算 engine 分离。
- 历史候选值继续只读禁选；金额/数量继续使用字符串；权限和项目上下文失败关闭。

### 4.2 公共契约、客户端与样式

- 四个 service 旧 import path 与三大 contracts barrel 保持；运行时导出、类型声明、URL/method/notifyError 指纹等价。
- navigation catalog 保留稳定 public exports；router bootstrap/guard 与组件注册、上下文路由、registry 分离。
- 全局 `components.css` 只作有序入口；controls、surfaces、dialogs、layout states 独立，cascade 次序保持。
- `system-management` 复用 `@cgc-pms/frontend-contracts` 的唯一 `PageResult`；不同领域 Page 投影不强合并。

### 4.3 后端政策、依赖与聚合边界

- Workflow Engine 与 BusinessAccessValidator 只读同一提交政策；null/blank/unknown 继续 fail-close，敏感类型只允许 dedicated 入口。
- 文件授权保留唯一 Spring façade；43 个业务类型经 registry 唯一解析到六组包内同步策略，启动时拒绝缺组/重复/未知映射。
- `ProjectFileProjection` 保持上传/失效同事务、预览持久任务、derived preview after-commit 删除、MinIO 补偿与失败重试；未改第92条可靠性机制。
- 成本、合同、现金、库存、质量 helper 均为 package-private plain object；事务注解、命令、锁定读、错误码和状态迁移留在 façade。
- `PaymentTraceValidator` 零 Mapper/JDBC/Spring/事务，接收已完整装载的快照；原校验体 23,672 字符等价，未二次读取。

## 5. G0～G5 证据

| 门禁 | 状态 | 客观证据 |
| --- | --- | --- |
| G0 基线/所有权/Codemap | PASS | 干净 master 基线、独占 worktree、当前 map 三问、计划回滚点与文件所有权冻结 |
| G1 行为契约 | PASS | 前端 129 项、后端 259 项基线；路由/权限/事务/金额/文件/状态机 characterization 随批次扩展 |
| G2 数据与迁移 | PASS | migration diff 为空；H2、独立 MySQL fresh/upgrade、金额/状态/文件对象守恒；任务 DB/容器清理 |
| G3 结构实施 | PASS | 18 项全部唯一裁决；focused page、plain collaborator、policy registry、projection port 和透明重复收敛 |
| G4 本地 API/浏览器 | PASS | 当前源码 task 容器、health UP、JDBC/Flyway V293、API/DB/MinIO、桌面/移动 DOM、交互、console、live 80/80 |
| G5 全量/治理/安全 | PASS | 后端/前端全量、MySQL 26、browser contract 98、依赖与 JAR 0 漏洞、SQL/Flyway/CI/Code map/零悬空全部通过 |

## 6. 自动化验证

### 6.1 后端、数据库与文件

| 验证 | 结果 |
| --- | --- |
| `mvnw clean test` | 372 suites、2,841 tests、0 failures、0 errors、28 skipped；跳过项均为显式 MySQL/benchmark 环境开关 |
| CI 同款独立 MySQL 组 | 9 suites、26 tests、0 failures/errors/skipped；fresh baseline、Flyway、租户、代码生成、付款/物料/通讯并发及 upgrade |
| focused 回归 | Workflow 201、File/ProjectFile 137、BusinessObject policies 109、Cost 28、Contract 106、Cash 65、Inventory 131、Quality 171、PaymentTrace 31 等均通过 |
| `mvnw -DskipTests package` | PASS；生成 `cgc-pms-backend.jar` |
| JaCoCo contract | instruction 73%、branch 60% 及包级阈值契约通过 |
| Trivy backend source/JAR | High/Critical 均 0 |

MySQL 首轮唯一红灯为付款并发测试用 Java `LocalDate.now()` 写 8 月 14 日、UTC 容器 `CURRENT_DATE` 仍为 8 月 13 日，导致测试查询错日。分类 `environment_prerequisite`；诊断确认两次 after-commit 均注册、执行并提交，第二次读取/写入 100 万。临时生产锁改动和诊断日志全部撤回；对齐容器时区后同一竞态连续 `3/3`、完整 MySQL `26/26` 通过。

### 6.2 前端、浏览器与 live

- contracts type-check、Vue type-check、build：PASS；最终 build 465 modules。
- 最终全量 unit：88 files、625 tests，全部通过；单 worker 基线与默认并行最终门禁均通过。
- lint：0 error；10 条既有/范围外 Prettier warning，不影响裁决。
- browser contract：27 个 spec、98 tests，0 unexpected/skip/flaky；首次 1 项 `ERR_NO_BUFFER_SPACE` 分类 `environment_prerequisite`，单 worker 权威复跑全绿。
- 本地 live：80/80；演示包 verify 与二次幂等加载通过。
- route ledger：87 routes、73 view entries、65 unique views；clean-room 379 个 V2 文件、36 个 contract 文件；设计系统 31/31；bundle size 118 个 JS assets。
- `pnpm check:pre-push`：PASS；大 diff 首次触发 `spawnSync git ENOBUFS`，将 capture buffer 固定为 16 MiB 并加 CI 契约；枚举全部受保护重提交流程的长测试在并行资源竞争下越过默认 5 秒预算，仅将该测试预算提高到 15 秒，断言不变；完整原门禁复验通过。
- `pnpm audit --audit-level high`：0；`nanoid` 解析为 `3.3.18`。

### 6.3 治理与静态门禁

- SQL injection scan、Flyway immutability：PASS。
- CI workflow contract、14 个 README 同步自测、PR push/post-merge evidence self-test、Surefire summary：PASS。
- long-task gate：15/15；AutoPilot stable/continuous/control-plane/fingerprint：PASS，且相关脚本最终 diff 为空。
- Code map generator self-test、最终 generate 与 `--verify`：PASS；三件套与当前实现/文档一致。
- `git diff --check`、migration diff、AutoPilot test diff：PASS/空。

## 7. 本地运行态与真实浏览器

- task backend/frontend 容器加载当前工作树；health 返回 `UP`，JDBC 指向任务专用数据库，Flyway 到 V293。
- Workflow 标准、dedicated、unknown/restricted；金额屏蔽；库存受限写；文件 unknown/upload/list/delete 均经真实 HTTP、DB 和 MinIO 回读验证。
- PaymentTrace 在本地演示事实不完整时按 `PAYMENT_TRACE_INCOMPLETE` fail-close；成功链由 closed-loop、batch 与 query-budget 自动化覆盖，不伪造 live 成功结论。
- 30 条拆分路由桌面加载、5 条关键路由移动 viewport、侧栏/通知/历史前进后退均验证；最终 console 仅 Vite debug，无 warning/error。
- live 初轮夹具版本落后当前演示包；按权威 loader 升级任务数据库后修正三处陈旧验收断言，未放宽角色权限或生产行为，最终 80/80。
- 验收后停止并删除任务容器，删除精确任务数据库；既有 dev 容器与数据未修改。任务库可通过本地 clone + 权威演示包重新生成。

## 8. 失败分类与处理

| 分类 | 现象 | 解决与裁决 |
| --- | --- | --- |
| `tool_config` | Windows `bash` 命中缺失 WSL；MySQL 容器镜像无 healthcheck；Playwright 端口/worker 参数不匹配 | 改用 Git Bash、容器内 `mysqladmin ping`、严格端口和单 worker 权威入口 |
| `tool_invocation` | PowerShell 插值、Maven/Vitest 参数及不存在目录调用错误 | 修正命令，不改生产代码；原目标复验通过 |
| `environment_prerequisite` | JWT test secret、并行 runner 资源、MySQL UTC 日期、live 演示包版本滞后 | 仅向测试进程/任务容器提供本地前置，对齐时区并加载权威包；不改安全默认值 |
| `ready_issue_config` | route ledger、静态路由预期仍引用兼容壳；live 角色/待办断言滞后 | 按当前权威 registry、fixture 与权限事实更新相邻验收契约 |
| `retrieval_gap` | Codemap 聚合节点无法回答部分页级三问 | 以当前 registry/rg/tests 补证，代码冻结后统一重生成三件套 |
| `quality_or_security` | 测试隔离、ENOBUFS、nanoid High、历史候选未禁选、权限/事务结构保护门禁 | 最小根因修复并复验；历史旁路项可读但 `disabled`，不降低权限、金额、租户或门禁阈值 |

## 9. 剩余风险与恢复

- 大型 façade 仍存在，但均因单事务、单快照、补偿链或无第二调用方有据保留；不以行数为由继续拆分。
- 本次主要依靠等价契约、H2/MySQL、API/DB/MinIO 与浏览器证明；未来业务语义变化应按各领域主线重新授权，不在第94条顺带推进。
- 回滚以计划提交 `8ed14cdbcd7fce6f3babaadc67bff1484c5f2d21` 为代码边界；逐批可恢复旧 façade/barrel/兼容壳。无 schema 回滚和数据迁移。
- 若受保护 push/PR/post-merge 任一 exact-SHA/tree 证明失败，Git 交付判定不通过；不得用本地 G5 替代。
- 本地 task 数据库和容器已删除；可按 clone + 权威演示包重建，不影响既有 dev 环境。

## 10. 零悬空统计

- 计划发现：18 项；本轮修复并复验 16 项，部分修复且其余有据关闭 2 项，正式承接 0，未收口 0。
- 实施中新发现的直接相关门禁缺口：live 陈旧断言 3、pre-push buffer 1、nanoid High 1、历史候选禁选 1、并行测试预算 1；全部本轮修复并复验。
- 独立前后端只读复核：后端 0 finding；前端新增 P1 1 项、关闭 1 项、未解决 0，最终 0 个 P0～P2 actionable finding。
- 正式后续项新增 0、关闭 0、净变化 0。
- 正式 Issue 新增 0、关闭 0、净变化 0；`current-issues.json` 不改。
- 当前无临时 TODO、无载体遗留或第90条语义夹带。

## 11. 受保护 Git 交付证据

当前状态：`GIT_DELIVERY_IN_PROGRESS`。本节将在同 SHA push CI、pre-PR verifier、受保护 PR/合并、post-merge verifier 和源分支清理全部完成后写回最终 SHA、run、PR、tree 与清理证据；完成前不声明 `MERGED` 或 `POST_MERGE_VERIFIED`。

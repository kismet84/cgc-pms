# 第91条主线：架构边界与查询性能分阶段整改收口报告

**日期：** 2026-08-12

**裁决：** `G0-G5_LOCAL_PASSED / GIT_DELIVERY_PENDING`

**最终代码证据 SHA：** `2a33977cf54add580734d5bccb1aac0518fdd4bf`

**核心实现 SHA：** `3aa0ec789465775ead00d27dc999851ab5fc5d0c`

**财务依赖验收 SHA：** `c47f4fa76c4139067fd9a7ffe0395c1e175d565f`

**分支：** `codex/mainline-91-architecture-query-performance`

**环境：** 仅本地 dev/test/demo；无非本地环境、生产数据库、生产部署、Tag 或 Release

## 1. 结论

第91条 `M91-F01～F13` 全部收口：F01～F12 本轮修复并复验，F13 因无签认阈值以证据不足关闭且不实施异步。认证、项目范围、分页、预警、通讯、付款、现金、账龄、Dashboard、采购 application、展示依赖和 assembler 均达到计划预算；金额、权限、租户、状态、冲销和响应契约无已知漂移。

本地 G0～G5 通过。当前只剩用户已授权的受保护 Git 交付；该步骤不得直接推送 `master`、强推或绕过保护。交付证据取得前不写 `MERGED` 或 `POST_MERGE_VERIFIED`。

## 2. 基线与隔离

- 实施基线：`origin/master@e1b7adb238c499fb6a7672682473c30737794e04`。
- G0 基线提交：`993237a006b8e7d3ddbfde368c51be30a78ef57d`。
- 原共享工作区混合成果保存于本地检查点 `40bbcaf29aac6b816140c2c9f57b96d474013c1a`；其历史包含禁止路径删除，因此不进入交付分支、不推送。
- 交付分支只迁入确认允许路径；禁止目录未读取、未迁入、未暂存。
- 关键实施提交：认证 `fdc30ef7`；四域分页 `4164af10`；预警/通讯 `ebbedc1c`；财务实现 `ed73525a`、验收 `c47f4fa`；核心实现 `3aa0ec78`；浏览器门禁收口 `2a33977c`。

## 3. 发现项裁决

| ID | 状态 | 实施与量化结果 |
| --- | --- | --- |
| F01 | 本轮修复并复验 | 独立 worktree、白名单迁入、分批提交、精确回滚点和当前 Codemap |
| F02 | 本轮修复并复验 | access token 黑名单 1 次；用户/角色/权限各 1 次，`Qauth=3`；撤权与异常 fail-close |
| F03 | 本轮修复并复验 | 技术、质量、收尾、供应商真分页；每活动视图 1 HTTP，SQL 范围下推，`Q(1)=Q(50)` |
| F04 | 本轮修复并复验 | 每批最多 500 项目，固定 11 次读；项目/evaluator/候选循环 Mapper=0 |
| F05 | 本轮修复并复验 | `beforeSeq` 向前游标；≤100 条/页、3 查询/页、DOM≤200；旧 `afterSeq` 兼容 |
| F06 | 本轮修复并复验 | 内部批量组装器每个≤100申请分片固定 25 Mapper+1 JDBC、循环查询 0；`byProject` 在 SUPER_ADMIN 下端点总预算 30 MyBatis+1 JDBC |
| F07 | 本轮修复并复验 | 日记条件聚合 1、账户类型余额 1、可选保证金 1；1/100 账户恒定 |
| F08 | 本轮修复并复验 | 财务预算 `17/6/18/3`；非财务服务脱离膨胀基类，SharedSupport 收敛至 105 行/8 依赖/4 helper |
| F09 | 本轮修复并复验 | 五桶 1 条 `SUM(CASE)`；九个边界日、空集、H2/MySQL 结果一致 |
| F10 | 本轮修复并复验 | 采购申请、订单、验收三个私有 application；新建回滚/编辑不回滚/服务端权威语义不变 |
| F11 | 本轮修复并复验 | 26 个业务页改用 `shared/display`；非 Dashboard 到 `dashboard/model` import=0 |
| F12 | 本轮修复并复验 | 两 assembler 显式 `Function<T,Long>`；无反射、无吞错，缺关系与空 ID 契约保留 |
| F13 | 证据不足而关闭 | 固定 JVM 三场景 90 样本 0 失败；无签认阈值，裁决 `NO_ASYNC`，不建后续项 |

## 4. G0～G5 证据

| 门禁 | 裁决 | 证据 |
| --- | --- | --- |
| G0 | PASS | 独立分支/worktree、允许路径归属、禁区排除、同 SHA 复验、Codemap 和回滚点成立；第90条依赖所有权已回写 |
| G1 | PASS | F02～F12 预算与失败测试锁定；F13 基准后有据关闭；金额/权限/租户/一致性独立复核完成 |
| G2 | PASS / NO_MIGRATION | H2/MySQL 结果一致；MySQL 8.0.46 `EXPLAIN` 为 `ref/eq_ref/range` 或索引反向扫描，无无界扫描；现有索引足够 |
| G3 | PASS | 服务端批量、聚合、分页和 Query Service 接线完成；异常 fail-close；无第二权限/金额事实或平行算法 |
| G4 | PASS | 当前 checkout 隔离实例完成 8 路由 DOM/交互/console 与目标 API 200 验真；富数据自动化补足通讯历史、SSE、会话竞态、已读与重连；隔离进程验收后精确停止 |
| G5 | PASS | 后端/前端全量、类型、构建、包体、MySQL、Codemap、diff、质量报告与零悬空通过 |

## 5. 查询、金额与数据证据

- 四域分页：技术普通视图 `Q=2`、复合视图 `Q=3`；质量 `Q=2`；收尾 `Q=2`；供应商三表工作区 `Q=6`。稳定排序、雪花 ID 字符串化、客户端 `projectId` 只收窄，不扩大权限。
- 预警：配置、八类事实、收货入库联查和去重候选合计 11 次读；`OPEN` 永久、`PROCESSED` 规则窗口、`INVALID` 不阻断；跨 500 分片共享当批 dedup key。
- 付款：H2/SUPER_ADMIN 的 1/100 申请 MyBatis 均为 30 次，另固定 JDBC 1 次；普通角色只增加固定授权读。
- 现金：只有 `ARCHIVED` 或具有 `archived_at` 的 `REVERSED` 进入汇总/余额/运行余额；未归档付款冲销排除，合法正反流水净额归零。
- Dashboard：财务单项目实时/历史、全项目实时/历史为 `17/6/18/3`；非财务单项目 PM/BM/采购/生产/总工为 `9/9/14/12/5`，全项目为 `8/9/14/12/5`，选期相同且 `Q(1)=Q(50)`。
- 无字段、表或索引变化；未修改历史 Flyway migration，未新增 migration。

## 6. 自动化验证

- 后端完整验证：358 份 Surefire 报告，2723 项，0 failure，0 error，25 条件跳过；精确核心实现 SHA `3aa0ec789465`，耗时约 913.3 秒；至最终代码证据 `2a33977cf54a` 的 backend tree 未变化。
- 前端完整验证：79 文件、567 项通过；`type-check:contracts`、Vue `type-check`、生产构建通过；构建 301 模块，71 个 JavaScript 资产均通过包体门禁。
- F08 语义与预算：100 项相关 Dashboard 回归通过；独立复核 P0/P1/P2=0。
- F10：application 与采购页面契约 19/19；F11：展示 helper 38/38；F12：定向 8/8、相关 assembler/服务 21/21；F13：benchmark 1/1。
- pre-push 补充：当前 worktree 隔离 5273 上通讯/供应商 E2E 13/13，相关单测 24/24，目标 ESLint 与 Vue 类型检查通过；通讯历史提示对比度和供应商 workspace E2E 夹具均已收口。
- Codemap：最终治理文档冻结后已再次生成 `docs/codemap/codemap.html/json/lock` 并通过 `--verify`，三件套同批提交。
- `git diff --check` 通过；高置信 secrets 扫描无命中，最大变更文件低于仓库门限。

## 7. 本地浏览器与运行态

- 隔离后端/前端：`127.0.0.1:8181`、`127.0.0.1:5273`；健康接口 `/api/actuator/health=UP`，源码来自当前 worktree。
- API 证据：`/api/technical-management/workspace`、`/api/quality-safety/workspace`、`/api/project-closeouts/page`、`/api/supplier-sourcing/workspace`、采购申请/订单/验收、通讯 `users/conversations/unread-count/stream` 全部 200。
- DOM/交互：技术页 6 个业务 tab 与分页出现，切换“图纸管理”后 URL=`?tab=drawing` 且 selected；采购三路由分别显示“采购申请/采购订单/材料验收”；通讯空历史 `<article>=0≤200`。当前代码 SHA 的自动化另覆盖 1 万历史、上翻、SSE 提示/补拉、重复/跨 gap 事件、已读不回退和快速切换；`communication-shell.test.ts` 明确覆盖首次握手与断线重连后的刷新，通讯 page/shell 两文件定向复跑 19/19。
- 控制台：应用内浏览器 0 error、0 warn，无错误遮罩。
- 阻塞处理：`application-local.yml` 默认关闭 MinIO，使通讯 Controller/Service 未注册并返回 404。分类为本地运行前置，不改代码；改用仓库已有本地 MinIO dev 服务后通讯链全部 200。应用内截图接口连续超时且局部截图不受支持，按前端验收技能仅将截图动作降级到本地 Playwright；DOM/交互/console/HTTP 仍来自应用内浏览器与后端日志。
- 验收完成后已核对进程命令行归属并停止 8181/5273 隔离进程；原 8080/5173 容器未触碰。

## 8. PDF 裁决

固定 `-Xms/-Xmx/GC` 和相同本地硬件，三个 renderer-only 场景各 5 次预热、30 次正式样本：

| 场景 | p50 | p95 | 正式失败 |
| --- | ---: | ---: | ---: |
| 20 行 | 172.253 ms | 201.024 ms | 0/30 |
| 120 行 | 202.811 ms | 214.251 ms | 0/30 |
| 200 行 | 226.720 ms | 241.980 ms | 0/30 |

证据边界不含 HTTP、Controller、Provider、模板查询和持久化，不将 renderer p95 冒充端到端请求 p95。当前无业务/技术签认请求时限、堆预算或失败率阈值，因此不能据此扩张为异步 job；F13 按计划关闭。详见[`PDF 本地基准与 F13 裁决`](2026-08-12-第91条主线-PDF本地基准与F13裁决.md)。

## 9. 失败分类与修复

- `environment_prerequisite`：后端测试未注入足够长 `TEST_JWT_SECRET`；进程级补齐后等价复跑通过，未恢复安全旁路。
- `environment_prerequisite`：local profile 关闭 MinIO导致通讯 Bean 缺失；切换到已有本地完整依赖基线后通过，未将运行配置漂移误判为 F05 回归。
- `environment_prerequisite`：pre-push 默认 5173 由 `role-dashboard-...-m89` Docker worktree 占用，Playwright `reuseExistingServer` 误验其他源码；改用当前 worktree 隔离 5273 与 2 workers，不停止或修改该容器。
- `command_orchestration`：Windows 搜索/PowerShell 参数和后台启动入口首次解析失败；改用仓库 wrapper、显式参数和隔离进程后通过。
- `test_assertion`：预警批量测试首次对集合实现类型过度约束；改为业务集合断言后通过，生产语义未改变。
- `quality_or_security`：独立复核发现预警同批 dedup、跨项目合同关联、现金未归档冲销、Dashboard 历史空集/日期与 SQL 计数线程污染；均在对应批次修复并复验。
- `quality_or_security`：隔离 E2E 发现通讯历史提示颜色仅 4.42:1，改用中性次级文字色后 axe 通过；供应商 E2E 仍提供旧三端点夹具，改为真实 `workspace` 聚合响应后 11/11 通过。

## 10. 剩余风险与回滚

- 剩余风险：真实浏览器使用空 H2 业务数据，富数据的 1 万通讯、50 项目、100 申请/账户由自动化与 MySQL/H2 预算测试覆盖；本地证据不等同生产或非本地发布证据，后者不适用且未发起。
- 回滚：各批次均可按提交边界回退；财务回滚点 `ebbedc1c...`，整体实现回滚点 `c39c9d54...`。不得回退共享工作区、不得推送隔离检查点、不得触碰禁扫目录。
- Git：受保护交付失败时保留源分支和同 SHA 证据，修复后重新取同 HEAD CI；不改写历史、不强推、不直接推送 `master`。

## 11. 零悬空统计

- 本次最终收口：新增后续项 0、关闭 13、净变化 `-13`。
- 第91条周期：承接既有 5、新增 8、关闭 13，净变化 `-5`。
- 从原性能报告至第91条：新增 13、关闭 13，净变化 `0`。
- 正式 Issue：新增 0、关闭 0，净变化 `0`；`docs/backlog/current-issues.json` 无等价活动项，保持不变。
- 正式计划载体：新增 1、关闭 1，净变化 `0`。
- 无超出范围待承接项，无无载体遗留项。

# 第 15 条主线 M3：预警中心通知与权限深化验收报告（2026-07-03）

## 1. 结论

- 结论：通过
- 阻塞 / 非阻塞：非阻塞
- 当前判断：第 15 条主线 `M3` 通知与权限深化已完成当前阶段收口

本结论仅基于 2026-07-03 当前工作区代码、既有测试结果和已知证据归档，不补造浏览器、容器或外部渠道运行态结论。

## 2. 依据

### 2.1 通知闭环与发送记录

已知证据：

1. 站内通知已落地。
2. 发送记录已落地。
3. 多渠道派发骨架已落地。
4. 外部渠道 `EMAIL / WECHAT / SMS` 当前仍为占位接入，不得按已正式打通口径验收。

归档判断：

1. `M3` 已从“页面被动查看”推进到“具备主动通知链路”。
2. 当前可确认的是站内通知闭环和统一派发骨架。
3. 当前不可确认的是外部正式发送能力，因此只记为占位能力。

### 2.2 权限隔离与入口补齐

已知证据：

1. `AlertAccessScopeResolver` 已实现角色域与项目范围隔离。
2. `V123__grant_project_manager_alert_list_menu.sql` 已补项目经理入口。
3. `AlertEvaluationServiceTest` 权限相关整类已通过，含 `TA11f2`。
4. `AlertControllerTest` 已覆盖订阅接口与项目经理入口正例，后端控制器测试已通过。

归档判断：

1. 当前权限口径已不是 `M1` 的“默认筛选”弱约束，而是后端角色域与项目范围的真实隔离。
2. 项目经理入口已补齐，`PM` 不再因为菜单缺口被排除在 `M3` 验收之外。
3. 当前后端权限结论来自代码与测试证据，不来自本轮浏览器复验。

### 2.3 订阅策略与接收人计算

已知证据：

1. 已存在 `AlertSubscriptionService`。
2. 已提供 `GET /alerts/subscription` 与 `PUT /alerts/subscription`。
3. 已形成“角色默认订阅 + 用户覆盖”收敛口径。
4. 接收人已从单一项目经理升级为“项目内活跃成员集合 + 域权限 + 有效订阅过滤”。

归档判断：

1. `M3` 已具备最小可解释订阅策略，不再只是文档口径。
2. 接收人计算已从单点责任人模型升级为项目内成员集合模型。
3. 订阅范围当前受域权限和有效订阅过滤约束，未见“通过订阅扩权”的归档证据。

### 2.4 前后端验证结果

前端已知验证：

1. `pnpm type-check`：通过。
2. `pnpm vitest run src/pages/alert/__tests__/index.test.ts`：先因 mock 初始化失败，后经 `vi.hoisted(...)` 修复并通过。

后端已知验证：

1. `./mvnw -q "-Dtest=AlertEvaluationServiceTest,AlertControllerTest" test`：通过。

归档判断：

1. 当前代码层和测试层证据支持 `M3` 已完成最小通知、订阅和权限深化实现。
2. 本轮未提供真实浏览器或容器复验证据，因此不把运行态强背书写成已完成事实。

### 2.5 本轮证据边界

本轮明确不纳入以下结论：

1. 不认定已完成真实浏览器验收，因为已知口径为“未启动 Playwright”。
2. 不认定已完成容器或接口运行态复验，因为本轮未提供对应实证。
3. 不认定 `EMAIL / WECHAT / SMS` 已正式打通，因为当前只有占位骨架。

清理口径：

1. 未启动 Playwright。
2. 不需要清理长期进程。
3. `.vite-temp` 属于临时缓存，本次未清理。

## 3. 与计划书逐项对照

对照 [第15条主线-M3-预警中心通知与权限深化任务计划书-2026-07-03.md](/D:/projects-test/cgc-pms/docs/plans/第15条主线-M3-预警中心通知与权限深化任务计划书-2026-07-03.md)：

1. 预警生成或状态流转已能触发站内通知：通过
2. 通知接收对象具备最小可解释计算口径：通过
3. 系统已形成统一派发接口和渠道占位，不再只有页面内列表：通过
4. 用户只能看到自身权限范围内的预警，且后端接口已做真实校验：通过
5. 详情查看、状态更新、批量处理不能绕过权限边界：通过
6. 已形成角色默认订阅 + 用户覆盖的最小订阅策略：通过
7. 订阅范围不能突破权限范围：通过
8. 后端相关测试、前端静态验证通过：通过
9. 真实浏览器与接口证据已覆盖通知、订阅和权限场景：本轮未覆盖，已在证据边界说明
10. 质量归档报告完成，并明确哪些外部渠道仍为占位接入：通过

说明：

1. 第 9 项属于本轮仍未补齐的运行态强证据空白。
2. 本报告不把该空白改写成“已完成”，但按当前已知代码与测试证据，`M3` 仍可按“通过 / 非阻塞”归档。

## 4. 通过 / 不通过 / 阻塞 / 非阻塞判断

| 项目 | 结论 | 依据 |
| --- | --- | --- |
| 是否通过 | 通过 | 站内通知、发送记录、多渠道骨架、强权限隔离、订阅策略、项目经理入口和前后端定向验证均已形成闭环证据 |
| 是否阻塞 | 非阻塞 | 当前未补的是真实浏览器 / 容器复验证据和外部渠道正式打通，这些未被冒充为已完成，也不阻塞 `M3` 当前归档 |
| 是否可关闭第 15 条主线 `M3` | 可关闭 | 当前证据已覆盖 `M3` 的核心实现收口要求，并已明确占位能力与证据边界 |

## 5. 已完成能力

1. 站内通知最小闭环已落地。
2. 发送记录已落地，可支撑后续追踪。
3. 多渠道统一派发骨架已落地。
4. 后端权限隔离已从默认筛选升级为角色域与项目范围真实校验。
5. 项目经理入口已补齐。
6. 订阅能力已具备 `GET /alerts/subscription` 与 `PUT /alerts/subscription`。
7. 已形成“角色默认订阅 + 用户覆盖”的最小订阅策略。
8. 接收人计算已升级为“项目内活跃成员集合 + 域权限 + 有效订阅过滤”。
9. 前端类型检查已通过。
10. 前端告警页单测已通过。
11. 后端 `AlertEvaluationServiceTest` 与 `AlertControllerTest` 已通过。

## 6. 占位能力

以下能力当前只能按“占位 / 骨架已留”归档，不得按“正式打通”表述：

1. `EMAIL`
2. `WECHAT`
3. `SMS`

当前口径：

1. 已有多渠道扩展位。
2. 已有统一派发方向。
3. 未见真实外部发送成功证据、凭据配置证据或运行态回执证据。

## 7. 剩余风险

当前无新增阻塞性剩余风险。

保留风险与空白：

1. 本轮缺少真实浏览器验收，尚未用页面实证覆盖“通知触达、订阅修改、权限拦截”全链路。
2. 本轮缺少容器 / 接口运行态复验，当前结论主要依赖代码与测试证据。
3. 外部渠道仍为占位接入，后续若进入正式打通，需单独补配置、发送回执与失败回退验收。
4. 若后续继续扩展订阅维度，必须坚持“订阅不能扩权”边界，不得突破当前权限模型。

## 8. 主负责人建议

1. 按“通过 / 非阻塞”关闭第 15 条主线 `M3` 当前归档。
2. 后续若需要更强运行态背书，应单独补一次真实浏览器 / 容器复验，只补证据，不改写当前结论边界。
3. 外部渠道后续推进顺序应保持为：先补真实配置与发送回执，再做单渠道正式打通验收，不要把占位骨架写成平台完成。
4. 后续扩展通知或订阅时，继续复用当前站内通知、统一派发接口和 `AlertAccessScopeResolver` / `AlertSubscriptionService` 口径，不要平行再造一套消息或权限体系。

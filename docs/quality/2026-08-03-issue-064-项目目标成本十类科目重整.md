# 第64条主线质量报告：项目目标成本十类科目重整

日期：2026-08-03

结论：本地实施与验收通过；`cgc_pms` 已完成 V264～V266，Git 交付已授权，生产、部署和发布排除。

## 实施结果

- 保留 `5401.01`、`5401.04`；删除 `5401.02` 全树；`5401.03` 重整为项目目标成本及 10 个固定叶子科目。
- 默认比例为 `25/40/5/5/5/3/5/1/8/3`，服务端原子校验合计 100%。
- 新建目标成本以项目合同金额 × 85% 生成总额和 10 条明细，快照、舍入尾差、事务与写后回读均在服务端。
- V266 不修改已应用 V264，前向补齐非 0 租户的 `5401.03` 十类树与保留的 `5401.04` 标准子科目。

## 数据与验证证据

| 检查 | 结果 |
| --- | --- |
| 清理前全量备份 | `D:\backups\cgc-pms\mysql\task64\cgc_pms_full_20260803_092307.sql.gz`；SHA-256 `7BB56B4D1A048FB8250A5D44DFD960521EC1128A226138F8B2115FB19CFB7499`；恢复独立验证通过；2026-08-09 仅迁移归档路径，内容哈希不变 |
| 当前 Flyway | V264、V265、V266 `success=1` |
| 当前 MySQL 树 | 10 条，比例合计 `100.0000`；`5401.02*` 0 条 |
| 保留树 | tenant 0 的 `5401.01*` + `5401.04*` 共 32 条 |
| H2 新建/历史/多租户升级 | 8 passed；到 V266 |
| 清理脚本 SelfTest | passed |
| 后端定向与全量 `verify` | 定向 95 passed；全量 2419 passed / 0 failed / 3 skipped；覆盖率门禁通过 |
| 前端单测/类型/契约/Lint/边界/构建 | 411 tests passed；其余门禁 passed |
| 浏览器 | 10 科目与比例可见；桌面/窄视口无横向溢出；控制台无相关错误 |

## 风险、恢复与边界

- 用户后续删除 `cgc_pms_demo`、`cgc_pms_demo_v2` 时未创建本次删除备份；该操作与上述第64条清理前备份分开记录。
- 当前业务库 `pm_project` 无项目，未为浏览器新建 85% 目标成本伪造数据；金额闭环由后端集成测试验证。
- 回退未交付代码时不反向改写已应用 migration；需数据级恢复时使用已验证全量备份与匹配代码。

零悬空统计：本轮关闭 `ISSUE-064-001` 1 项；新增后续项 0，关闭后续项 1，净变化 -1；计划全周期净变化 0。

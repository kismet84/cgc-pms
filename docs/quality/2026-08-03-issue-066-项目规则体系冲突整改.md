# 第66条主线质量报告：项目规则体系冲突整改

日期：2026-08-03

结论：第66条 M0～M5 规则整改本地实施与阶段收口通过。17项全部修复或以当前证据关闭，无阻塞项。

> 范围补充（2026-08-03）：本报告只裁决 M0～M5 规则整改。随后实施的 M6 工作区归档与清理由[独立阶段报告](2026-08-03-issue-066-M6-工作区归档与清理.md)裁决；本补充不改写下列17项规则整改证据。

## 逐项裁决

| 编号 | 结论 | 证据 |
| --- | --- | --- |
| `RULE-066-001` | 本轮修复并复验 | 根规则、AutoPilot Owner和安装说明只保留四个精确触发短语；静态契约通过。 |
| `RULE-066-002` | 本轮修复并复验 | 飞书确认移除固定身份，超时只停止或保持现状，外部写入要求明确授权或有效绑定回复。 |
| `RULE-066-003` | 本轮修复并复验 | 数据库与后端规范统一为B215 `active_unique_token`；47项后端定向测试覆盖迁移语义。 |
| `RULE-066-004` | 本轮修复并复验 | 项目级业务统一租户+项目强制范围；`ProjectAccessChecker`及材料申请范围测试通过。 |
| `RULE-066-005` | 本轮修复并复验 | dev/prod Compose应用改用独立MinIO服务账号变量；prod预检拒绝与root相同的AK或SK；两套Compose渲染通过。目标环境凭据切换另行授权。 |
| `RULE-066-006` | 本轮修复并复验 | 根规则明确单独“推送”的有限Git生命周期授权及不相关改动、强推、保护绕过和生产边界。 |
| `RULE-066-007` | 证据成立，关闭 | 当前用户规则已规定Luna不可用时回退Terra；仓库规则不依赖固定代理TOML。未修改仓库外配置。 |
| `RULE-066-008` | 证据成立，关闭 | 现行产物治理已区分项目业务文档与插件自身产物；契约测试持续守卫。 |
| `RULE-066-009` | 本轮修复并复验 | 普通修改使用风险相称验证；首次非Draft PR、正式主线和高风险节点保留完整门禁。 |
| `RULE-066-010` | 本轮修复并复验 | 快速开始、Prompt和安装说明改为证据陈旧时按runtime Skill刷新，不再无条件重启或固定等待。 |
| `RULE-066-011` | 本轮修复并复验 | Prompt索引移除已删除的`AGENTS.override.md`入口；静态契约通过。 |
| `RULE-066-012` | 证据成立，关闭 | `.agents/skills/lark-slides/**`是未跟踪本地运行时产物，不是项目交付物；tracked `skills-lock.json`记录官方来源与不可变哈希，`lark-cli 1.0.70`内嵌并可读`lark-slides`和`lark-shared`权威内容。项目链接检查只扫描Git已跟踪Skill。 |
| `RULE-066-013` | 本轮修复并复验 | 标准、Loop Budget和安装说明只保留稳定概念；动态版本、权重、阈值、模型、并行数和超时回到控制面配置/Schema/批准来源。 |
| `RULE-066-014` | 本轮修复并复验 | 子智能体规则统一为通常1～2个，三个明确独立工作流时最多3个。 |
| `RULE-066-015` | 本轮修复并复验 | JWT表对齐已跟踪profile；分页区分API参数与V2默认10；Loop Budget作用域显式化。 |
| `RULE-066-016` | 本轮修复并复验 | 新增四类保留矩阵和依赖清理门禁；一次性目录离线安装复用302包、下载0包后回收。 |
| `RULE-066-017` | 本轮修复并复验 | CodeGraph以数据库/WAL/日志、存活daemon和auto-sync联合证明新鲜；未清理索引。 |

## 验证证据

| 检查 | 结果 |
| --- | --- |
| 主线HighRisk规则套件 | 7项通过 |
| 工作流契约 | 13个必需job、9个上传和3个下载契约通过 |
| 后端定向测试 | 47 passed / 0 failed / 0 errors / 0 skipped |
| dev/prod Compose | 使用示例环境变量执行`config --quiet`，均通过 |
| 包管理器store | `pnpm store path`与`.modules.yaml`均指向`D:\.pnpm-store\v11` |
| 离线恢复 | `pnpm install --offline --frozen-lockfile --ignore-scripts`通过；302包全部复用，下载0包 |
| CodeGraph | `codegraph.db`/WAL/日志近期更新；PID 24932为存活node进程；daemon日志显示watcher、caught-up与auto-sync |
| Lark外部运行时 | tracked `skills-lock.json`来源/哈希有效；`lark-cli --version`为1.0.70；内嵌`lark-slides`与`lark-shared`均可读 |
| 控制面指纹 | `d9d20530372121d3ec3569016c7f2c9db68273a5b7be8db77faa74e43fe2e371`；105个受控路径，含Loop Budget行为权威 |
| UI/浏览器 | 不适用；本轮无UI业务代码或运行态变更 |

## 失败分类与复验

- HighRisk计划检查首次因计划缺少“失败分类”段落失败：`ready_issue_config`。补齐计划后原套件通过。
- 首次离线安装命令受递归清理策略和PowerShell参数错误影响：`tool_invocation`。改用精确一次性目录、无联网安装并移入回收站，复验通过且目录不存在。
- 一次包含`${...}`的正则检索解析失败：`tool_invocation`。改用`rg -F`后完成同范围检索。
- 上述均非业务、质量或安全缺陷；首次失败记录保留。

## 独立复核与边界

- 独立复核发现prod MinIO仅校验AK不等于root；已补SK不等于root及契约断言。
- 独立复核发现数据库规范误删稳定表命名规则；已恢复，不把规则冲突整改扩大为规范瘦身。
- 独立复核发现Loop Budget行为权威未纳入控制面指纹；已补指纹路径与自测断言。
- 阻塞解除复核证明`skills-lock.json`已由Git跟踪，且`lark-cli`同时内嵌`lark-slides`与其`lark-shared`认证依赖；本地安装目录不再被误当项目交付来源。
- `application-local.yml`由`git ls-files`证明已跟踪，JWT 1小时口径有效；文档未引用忽略的测试镜像。
- 未修改业务代码、migration、数据库、生产、目标环境或真实凭据；未启动容器；未提交、推送或创建PR。
- 未触碰用户既有`frontend-admin/.omc/**`删除和`tmp/**`内容；本轮一次性目录已移入回收站。
- 控制面行为指纹已变化，但未执行金丝雀。进入N>1或无界执行前仍需用户明确发送`启动迭代-1`。

## 风险与恢复

- Lark外部运行时若缺少CLI或任一内嵌Skill，停止幻灯片操作，按tracked来源锁恢复提供方CLI；不得提交本地安装目录或删除权威认证依赖。
- MinIO目标环境服务账号创建、轮换和部署是独立发布前置，不影响仓库内裁决，仍需明确授权。
- 回滚以本分支任务自有diff为单位；不得覆盖用户既有脏改动。Git尚未提交，未获授权前不执行交付。

零悬空统计：本轮新增后续项0、关闭后续项1、净变化-1；17项全部关闭，无无载体遗留项。

# 第48条主线 M3 结算单闭环验收报告

## 结论

**通过。** M3 已完成结算单从权威业务事实到受控模板、正式 PDF、不可变归档、当前业务权限下载和操作审计的最小闭环。未发现未承接的阻塞或非阻塞问题。

## 验收范围

- 结算输出模型、正式/预览状态门禁及权威金额复用。
- 系统结算模板、结算详情页预览、幂等生成、历史和下载。
- 已生成文件归档、SHA-256、病毒扫描状态、历史记录和下载审计。
- 浏览器可达的签名下载地址与 Docker 内部对象存储地址分离。

## 证据

- 后端专项回归通过：`SettlementDocumentDataProviderTest`、`SettlementSystemTemplateServiceIntegrationTest`、`DocumentControllerAuthorizationTest`、`StlSettlementQueryServiceTest`。
- 文档端到端回归通过：权威源金额变化后，以不同幂等键生成的记录具有不同 `sourceDigest`。
- 文件服务与配置回归通过：公开签名地址使用独立 MinIO 客户端和显式 region，既有文件服务测试保持通过。
- 本地开发隔离金丝雀：真实 FINANCE 角色可在结算详情页看到预览、生成与历史入口；正式生成成功后历史为 `SUCCEEDED`，浏览器点击下载产生成功审计。
- 授权下载获得有效 PDF：魔数 `%PDF-`、2 页，输出 SHA-256 与归档记录一致；结算标题、结算编号、定案金额和未付金额均在 PDF 文本中命中。

## 发现项处置

| 发现项 | 处置 | 复验 |
| --- | --- | --- |
| 已生成 PDF 被结算附件查询再次纳入输出模型，可能导致后续摘要漂移 | 本轮修复并增加回归 | 通过 |
| Docker 内部对象服务名和公开签名地址混用，主机浏览器无法稳定下载 | 本轮修复为内部/公开端点分离，并显式指定 region | 通过 |

## 风险与后续

- 生产发布仍未授权。生产环境须通过独立发布门禁配置公开 HTTPS 对象地址、字体/许可证材料和代表性容量金丝雀。
- M4 仅进入受限模板治理；不扩展为自由设计器、Office、脚本、批量、异步、签章或外部交付。

## 收口

- 新增后续项：0
- 关闭后续项：0
- 后续项净变化：0

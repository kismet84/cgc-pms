# 第53条主线 M4-1 全量页面 UI 标准审查与修复验收报告

> 日期：2026-07-24
>
> 对象：`frontend-admin-v2`
>
> 计划：`docs/plans/第53条主线-M4-1-全量页面UI标准审查与修复任务计划书-2026-07-23.md`
>
> 收口基线：`master` / `32f58b1dd7528de7e10af94927c65ed39cae0462` 加 M4-1 最终工作区
>
> 裁决：**通过**

## 1. 验收结论

M4-1 范围内 18 个页面/工作区均已完成“审查、根因修复、自动门禁、目标交互测试、三视口复验、正式记录”闭环。页面通过数 18/18；无页面级悬空项；未改变后端业务状态机、数据库、权限码、租户/项目范围或金额口径。

本报告证明本地实现和测试验收通过，不等同于生产发布。Git 提交、同 SHA CI、PR 合并和目标环境发布分别以对应证据裁决。

## 2. 依据与范围

- 用户逐页标注及明确验收意见。
- `docs/ui-v2/m1-design-system-baseline.md` 的 S01～S24、7.2～7.8。
- V2 公共 token、`V2*` 组件、导航、路由、权限和工作区上下文契约。
- 当前源码、接口模拟/真实数据、DOM、computed style、控制台、Playwright 和 Axe 结果。

| 键 | 页面/工作区 | 代表路由 | 结果 |
| --- | --- | --- | --- |
| `UI-M4-1-001` | 驾驶舱 | `/v2/dashboard` | 通过 |
| `UI-M4-1-002` | 投标成本 | `/v2/bid-cost` | 通过 |
| `UI-M4-1-003` | 审批工作台 | `/v2/approval/todo` 等 | 通过 |
| `UI-M4-1-004` | 报表目录 | `/v2/dashboard/reports` | 通过 |
| `UI-M4-1-005` | 项目列表与项目上下文页 | `/v2/project/list` 等 | 通过 |
| `UI-M4-1-006` | 合同台账 | `/v2/contract/ledger` 等 | 通过 |
| `UI-M4-1-007` | 签证变更 | `/v2/variation/order` 等 | 通过 |
| `UI-M4-1-008` | 成本目标 | `/v2/cost-target/index` 等 | 通过 |
| `UI-M4-1-009` | 成本台账 | `/v2/cost/ledger` | 通过 |
| `UI-M4-1-010` | 成本核对 | `/v2/cost/summary` | 通过 |
| `UI-M4-1-011` | 动态利润控制 | `/v2/cost/control` | 通过 |
| `UI-M4-1-012` | 项目预算 | `/v2/budget` | 通过 |
| `UI-M4-1-013` | 产值计量 | `/v2/production-measurement` | 通过 |
| `UI-M4-1-014` | 项目计划 | `/v2/project-schedule` 等 | 通过 |
| `UI-M4-1-015` | 现场日报 | `/v2/site/daily-log` | 通过 |
| `UI-M4-1-016` | 质量安全整改 | `/v2/quality-safety` | 通过 |
| `UI-M4-1-017` | 图纸 RFI 技术闭环 | `/v2/technical-management` | 通过 |
| `UI-M4-1-018` | 竣工收尾 | `/v2/project-closeout` | 通过 |

## 3. 收口内容

- 复用公共 Toast：成功、信息、警告、错误四类；顶部堆叠、3.5 秒倒计时、手动关闭；驾驶舱刷新及各业务成功反馈已接入。
- 复用公共 `V2ActionMenu`、`V2Dialog`、按钮、输入、选择、卡片、表格和分页规则；页面不再复制公共材质。
- 统一 H 标题栏、标题后统计标签、筛选框无可见上方标签但保留可访问名称、表格卡片容器、弹窗文字层级和响应式行为。
- 修复详情页纵向滚动、表格长文本溢出、编号与名称错误拼接、内部 ID 暴露、公共壳项目/报告期控件位置及查询边界。
- 设计系统基线和静态门禁同步覆盖按钮材质、私有属性、标题、筛选、Toast、弹窗、表格和可访问性约束。

## 4. 验证证据

| 门禁 | 结果 |
| --- | --- |
| Vitest 单元/静态契约 | 36 个文件，264/264 通过 |
| TypeScript | `pnpm run type-check` 通过 |
| ESLint | `pnpm run lint:check` 通过 |
| 生产构建 | `pnpm run build` 通过 |
| 路由迁移账本 | 87 条路由、73 个视图入口、65 个唯一视图；`pnpm run check:route-ledger` 通过 |
| diff 格式 | `git diff --check` 通过 |
| M4-1 迁移门禁 E2E | 20/20 通过 |
| 实际运行态交付 E2E | 10/10 通过 |
| 三视口 | 1440、1024、390 通过 |
| 可访问性 | Axe serious/critical 0 |
| 关键交互 | 主内容滚动、无页面横向溢出、追溯弹窗、遮罩/关闭按钮/Escape 返回通过 |
| 本地运行态 | 前端 `http://localhost:5174/v2/` 返回 200；前后端容器 healthy |

首轮迁移门禁曾有 8 项失败，统一分类为 `quality_or_security/DELIVERY_GATE_OMISSION`：6 项缺少新增权威数据接口 mock，2 项为旧 UI 断言。补齐 `/api/cost-subjects`、`/api/system/users` mock 并更新断言后，全量 20/20 通过；未以重试、跳过或降低断言掩盖失败。

首轮同 SHA 分支 CI 的 `frontend-v2-gate` 发现路由账本验收脚本仍要求已删除的页面级 `workspaceContext`，分类为 `quality_or_security/DELIVERY_GATE_OMISSION`。断言已与公共壳常驻项目/报告期选择器契约同步，账本检查恢复通过。

## 5. 风险与回滚

- 剩余风险：未提供目标生产环境发布、真实生产数据或生产监控证据；不影响本计划本地 UI 验收，但禁止据此宣称已上线。
- 回滚：通过 M4-1 独立 Git 提交/PR 整体回退；无数据库 migration、数据迁移或生产写操作。
- 后续项：累计新增 1，累计关闭 1，净变化 0；无未承接项。

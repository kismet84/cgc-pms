# ISSUE-040-036 项目受控归档入口与项目数据范围验收报告

结论：通过；阻塞：无。

## 范围与结果

- 桌面项目列表既有操作菜单增加 `project:edit` 或 ADMIN/SUPER_ADMIN 可见、仅非 ARCHIVED 行展示的“归档”动作；二次确认包含项目名称及合同、付款、结算和审批前置。
- 前端精确调用 `PUT /projects/{id}/archive`，不发送 params、body、tenantId 或项目字段；成功重新读取列表，失败仅展示服务端原因，不在客户端删除或伪改记录。
- 后端归档在任何活动依赖查询及状态写入前复用 `ProjectAccessChecker`，补齐同租户项目数据范围；跨租户隐藏、重复归档及既有四类依赖门禁保持不变。

## 验收证据

- Ready lint：通过。
- 后端：`PmProjectArchiveTest,PmProjectControllerTest` 共22项通过，覆盖未登录401、无权限403、同租户无项目范围403、跨租户隐藏、管理员成功路径、重复归档及活动合同、付款、结算、流程门禁。
- 前端：`ProjectLedgerProduction.test.ts` 11项通过；`vue-tsc --noEmit`、目标 ESLint、`git diff --check` 通过。
- 首次后端失败分类：Service 正确抛出 `PROJECT_ACCESS_DENIED`，全局 HTTP 契约将其归一为403/`AUTH_FORBIDDEN`；修正测试断言后稳定通过，未修改业务映射。
- 运行态：8080 health、5173 与 dev-login 可达；旧 Vite 模块错误按环境前置处理，前端重启后完成180秒稳定观察。浏览器进入 `/project/list`，打开操作菜单及归档确认后点击取消；确认文案完整，后端运行日志归档 PUT 计数前后均为0，控制台 error/warn=0。
- 审查：复核项目范围校验顺序、权限和状态可见性、请求体边界、失败路径及允许范围，结论 `PASS`、findings=无。

## 治理收口

- 唯一台账移除 `A-01-PROJECT-ARCHIVE`，A-01 更新为有用户入口241、前端调用但无独立页面58、内部/集成/运维4、需补入口7、待废弃0、需要确认11，共321。
- 新增后续项：0；关闭后续项：1；后续项净变化：-1。
- 最小回滚：回退本 Issue 的归档 API、列表动作、项目范围补强、专项测试与治理回写；浏览器未写业务数据，无数据回滚。

剩余风险：浏览器仅验证管理员桌面确认与取消路径，真实归档成功/拒绝由后端事务测试覆盖；移动端写操作明确不在本轮目标内，不形成悬空问题。

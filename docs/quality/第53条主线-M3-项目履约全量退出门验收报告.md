# 第53条主线 M3 项目履约全量退出门验收报告

## 1. 结论

- 验收结论：本地正式收口通过；阻塞项0。
- `ISSUE-053-010～016`全部完成；十个M3路由均为真实Clean-room V2页面或受控重定向，生成台账`LEGACY_ONLY=68 / V2_ACCEPTED=19 / V2_SOURCE_AVAILABLE=0`。
- 本结论仅覆盖本地dev/demo实施与验收；Git提交、push、PR与同SHA CI证据由独立发布流程记录，不构成生产上线结论。未授权或执行M4、正式入口切换、Legacy退役、生产连接或生产发布。

## 2. 范围与关键边界

- 项目对象：列表、总览、成员、编辑、对象深链、query/hash与请求隔离。
- 计划现场：项目计划、WBS/期间项/进度/纠偏、现场日报、附件及跨域只读联动。
- 质量技术：质量整改/复检/后果、方案/图纸/会审/RFI/交底/归档及阶段证据。
- 竣工收尾：验收、最终结算、尾款、质保、缺陷、档案与项目关闭。
- 权限、租户/项目范围、金额、状态机与附件阶段继续由后端和数据库事实裁决；V2不新增业务mock、全局Store、第二请求层或前端权威状态推导。

## 3. 本轮阻塞闭环

- 质量、技术、收尾责任人统一要求同租户启用用户及目标项目有效成员；outsider、停用和跨租户回归均拒绝。
- M3查询身份直接调用42个质量、技术、收尾写端点均返回403；前端按钮隐藏不替代后端授权。
- complete-project-v2 v10新增固定`demo.schedule.query`，仅有`project:query`、`schedule:query`，`SELF`范围绑定独立项目；目标项目读取成功、其他项目读取403、动作越授为0。
- 收尾最终结算拆为REGULAR 99000与RETENTION 1000，质保绑定合法RETENTION应收；ON DUP覆盖旧坏金额/FK，精确链路指标`m3_closeout_ready_chain=1`。
- 五组live统一采集console warning/error与pageerror；故障注入中的预期403/409/503必须逐项精确消费，其他运行时错误仍使测试失败。
- Ready验证改用白名单`verify-m3-live.ps1`，正式命令可无人工环境变量重复执行。
- complete-project-v2加载器对可被live写测改变的M3 v10数据改为每次幂等重放；消除bootstrap阶段已完成后跳过恢复、导致验收基线漂移的问题。
- 计划、质量、技术、收尾统一复用公共壳项目范围；“全部项目”默认聚合有数据对象并隐藏无数据项目。计划履约详情使用独立深链页面，不再页内展开。
- 页面验收用例改用稳定可访问区域和当前按钮文案；运行时因宿主Maven校验改写共享`backend/target`导致的容器类缺失，按环境前置分类并通过重启后端及两套前端恢复，未归类为产品缺陷。

## 4. 验证证据

| 门禁 | 命令/范围 | 结果 |
| --- | --- | --- |
| Ready | 原Ready态白名单验证；当前`ISSUE-053-016`已为Done | 原Ready证据保留；Done后重跑返回`ready_issue_config`属状态机预期，不回退Issue状态 |
| 后端全量 | `backend\\mvnw.cmd -C verify` | 254个Surefire报告；2071项执行，失败0、错误0、跳过3；含M3权限与业务闭环测试 |
| V2单测 | `pnpm test:unit` | 26文件、137项通过 |
| 类型/静态 | contracts、Vue、Lint、Clean-room、route-ledger | 通过；112个V2文件、12个契约文件、87路由、73个view条目、65个唯一view |
| 构建/包体 | `pnpm build`、`pnpm check:bundle-size` | 通过；19个JS资产满足预算 |
| Demo | v10幂等重放后的`load`及独立`verify` | 全部`passed=true`；`m3_quality_stage_rows=6`、`m3_technical_stage_rows=7`、`m3_closeout_stage_rows=10`、非法责任人0、动作越授0、收尾精确链1 |
| Live E2E | `verify-m3-live.ps1` | 48项通过；1440/1024/390、axe、控制台、查询零写、分权阶段动作、独立详情路由 |
| 浏览器 | 公共壳“全部项目”下计划、质量、技术、收尾与计划详情 | 四个聚合页均无空态；质量4、技术3、收尾10个追溯入口；详情进入`/v2/project-schedule/{id}`且列表入口消失 |
| 差异 | `git diff --check` | 通过 |

运行态证据：`cgc-pms-backend-dev`健康并连接`cgc_pms_demo_v2`；V2实际入口`http://127.0.0.1:5174/v2/`；MySQL仅绑定`127.0.0.1:3307`。未连接生产。

## 5. 独立复核

- 安全Reviewer：责任人项目成员、直接API 403、租户/项目、金额与附件边界无P0/P1阻塞；计划只读身份改为`SELF`独立项目后无跨项目读取。
- 测试Reviewer：原控制台门、收尾动作矩阵和live命令缺口已修复；最终指出的delivery默认身份与正式载体缺口均已本轮闭环。
- 本轮复验发现的demo可变基线、E2E可访问名称/按钮文案和容器共享构建目录问题均已修复或完成环境恢复并复验；未新增未承接发现。

## 6. 回滚与收口

- 最小回滚：仅回退对应M3契约、服务、页面、路由、测试、demo v10增量及治理回写；不得删除或改写既有项目、计划、质量、技术、结算、回款、质保、缺陷、档案或关闭事实。
- M3整体回滚可把十个路由恢复到M2后状态；`ISSUE-053-010`无UI契约基线保留。
- 新增后续项0；关闭后续项0；后续项净变化0；无悬空发现。
- Git发布边界：提交、push、PR与同SHA CI须经独立发布授权和对应门禁执行；生产发布仍需目标环境证据与单独授权。

# ISSUE-053-027 M5仓库、库存与来源流水V2验收报告

## 2026-07-26复审裁决

- 结论：当前M5页面范围通过。
- 阻塞项：0。
- 路由：`/inventory`受控重定向；`/inventory/warehouse`、`/inventory/stock`、`/inventory/transaction`继续由Clean-room V2承接。
- 台账边界：上述路由项保持`V2_ACCEPTED`；全局迁移计数正受M6在制变更影响，不作为本次M5页面裁决依据。
- 发布边界：未commit、push、切正式入口、退役Legacy或操作生产。

## 复审发现与本轮修复

- 全部项目库存此前缺少项目身份。后端库存VO补充`projectId/projectName`，页面在全部项目口径显示项目列。
- 库存查询此前只接受`inventory:stock:list`，导致仅持有流水查看权限的`/inventory/transaction`被拒绝。只读查询改为接受库存或流水任一查看权限；写权限未扩大。
- 全部项目查询改为只汇总当前用户可访问项目的仓库；无可访问项目时返回空，不扩大租户或项目边界。
- 数量、金额、成本继续使用服务端返回的稳定十进制字符串；页面不推导权威库存、金额或状态。
- 库存详情改为V2对话框；表格具备可聚焦、可命名区域。流水页继续只读，无通用手工出入库入口。
- `/inventory/transaction`到库存工作区的权限覆写仅适用于该受控重定向来源；无权限用户仍失败关闭且无重定向循环。

## 验证证据

- 后端联合专项：`MatStockControllerTest`、`MatStockServiceTest`及领退料相关测试共88项通过，0失败、0错误、0跳过。
- V2类型检查通过；M5目标单测19项通过。
- M5目标Playwright：24项通过；覆盖权限失败关闭、三视口、可访问性、服务端事实、详情/流水及写后回读。
- 路由专项：流水权限正向与无权限无循环2项通过。
- 浏览器运行态：`http://127.0.0.1:5174/v2/inventory/stock`返回真实数据；全部项目选择器可用，3条库存均显示项目身份；主仓80吨、平均成本1000、库存价值80000与服务端一致；详情展示入库、领用、退料、冲销4条来源流水；控制台无错误或警告。
- 受控demo包重新装载通过：23阶段完成，库存数量80、库存恒等式差额0。

## 全局门边界

- 全局`design-system.test.ts`仍有2项失败，均定位到在制M6 `ReceivablesWorkspacePage.vue`的遮罩关闭与原始项目ID问题。
- 分类：`quality_or_security`；由已启动的`ISSUE-053-033`承接，不是M5库存页面缺陷。本报告不据此宣称全仓全局门已绿。

## 零悬空收口

- 本轮M5发现：全部修复并复验。
- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- 无载体遗留项：0。

## 回滚

- 回退库存查询权限、项目范围/VO、V2页面、路由专项与对应测试。
- demo数据仅在本地`cgc_pms_demo_v2`增量装载；需要回滚时重建该本地demo库，不影响生产或其他数据库。

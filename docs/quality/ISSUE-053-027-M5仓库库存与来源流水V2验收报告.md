# ISSUE-053-027 M5仓库、库存与来源流水V2验收报告

## 裁决

- 结论：通过。
- 阻塞项：0。
- 路由：`/inventory`受控重定向；`/inventory/warehouse`、`/inventory/stock`、`/inventory/transaction`由真实Clean-room V2承接。
- 台账：`LEGACY_ONLY=41 / V2_ACCEPTED=46 / V2_SOURCE_AVAILABLE=0`。
- 发布边界：未commit、push、切正式入口、退役Legacy或操作生产。

## 实现证据

- 单一`InventoryWorkspacePage.vue`按路由切换仓库、库存、来源流水三模式；复用V2请求层、权限守卫和设计系统。
- 仓库支持服务端分页、详情、创建、更新、状态和删除；权限固定为`inventory:warehouse:list/add/edit/delete`。
- 库存数量、价值、成本、阈值、在途、历史净领料和来源流水只展示服务端响应；前端不累计库存。
- 调拨要求`inventory:stock:edit`与`inventory:transaction:add`，每次打开生成唯一幂等键；成功或失败均不做本地库存推进。
- `/inventory/transaction`只读展示来源流水；页面无通用`stock/in`或`stock/out`入口。
- 客观证据证明`inventory:transaction:list`此前无法调用唯一流水GET；后端最小根修仅允许该只读权限访问ledger，并新增控制器正向测试。写权限未扩大。

## 验证证据

- Ready lint：`ISSUE-053-027`通过，hash `539f9814a696813b0a172b87b8b52b02b3175016af74cff4d52af5665f0a8ea5`。
- 后端库存专项：11类，110项通过，0失败、0错误、0跳过。
- V2单测：40文件，284项通过。
- 目标E2E：8/8通过；覆盖权限失败关闭、重定向、三视口、axe、仓库写入、数量/价值/在途/历史事实、阈值、调拨冲突和流水只读。
- 契约/V2类型、ESLint、Clean-room边界、台账检查、生产构建、包体与`git diff --check`通过；边界扫描152个V2文件、14个契约文件，构建34个JS资产。
- 本地运行态：`frontend-v2`容器刷新后`http://127.0.0.1:5174/v2/`返回200；未连接生产。

## 失败分类与修复

- 实现错误：页面误引入不存在的`@/stores/workspace-context`，Vite运行态阻断；改为已有`@/stores/workspace`并复验。
- 可访问性：页眉弱文本对比度4.42低于4.5；改用既有主色token并复验axe serious/critical为0。
- 测试定位：调拨选择器在对话框过渡期定位不稳定；限定dialog范围并等待标准过渡稳定，未改业务语义。

## 零悬空收口

- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- 悬空项：0。
- 下一串行项：`ISSUE-053-028`领退料V2；由主线正式承接，不属于027遗留缺陷。

## 回滚

- 回退库存V2页面、契约/服务扩展、路由/导航/台账、测试及ledger只读权限根修。
- 保留024～026、Legacy、数据库和既有业务数据；无需数据回滚。

# 第 21 条主线最终质量归档与收口报告（2026-07-05）

## 1. 最终结论

- 结论：通过
- 阻塞 / 非阻塞：非阻塞
- 是否可关闭第 21 条主线：可关闭当前主线
- 收口口径：第 21 条主线已完成“非阻塞遗留问题治理”当前阶段收口，但不等于权限全量治理、入口 registry 全量补齐或外部渠道正式打通全部完成

本报告只汇总并归档第 21 条主线已经形成的正式质量结论，不新增代码、迁移、测试、运行态或 Git 侧动作。当前裁决以既有正式质量报告为准，不把“外部渠道占位”“authority 全量差集”“入口 registry 全量补齐”写成已完成。

## 2. 归档范围

本次归档汇总以下阶段：

1. `P0-1` 第 10 / 10A 权限治理剩余逐码裁决与阶段收口
2. `P0-2` 第 13 条上线前总控状态校准与决策包重建
3. `P1-1` 第 10B 项目级强隔离最小实现首包
4. `P1-2` 第 10C 角色菜单绑定编辑增强首包
5. `P1-3` 第 10D 全站入口 registry 首批实现包
6. `P2` 第 15 条主线后续增强边界复核

本次归档不覆盖：

1. 权限体系全量重构
2. `authority` 差集全量补齐
3. 角色菜单绑定全量清理
4. 入口 registry 全站全量接入
5. 外部渠道正式打通后的新增实现或复验

## 3. 分阶段证据摘要

| 阶段 | 当前结论 | 阻塞性 | 核心依据 |
| --- | --- | --- | --- |
| `P0-1` | 通过 | 非阻塞 | `mainline-21-p0-1-permission-closure-2026-07-05.md` 已确认 `P0-1a/P0-1b/P0-1c` 最小闭环完成；`invoice:list`、`project:list` 旧码清理、`requisition:submit` 绑定、`dashboard:view` 最小专项收口、高危 `DB-only` 四码门禁已关闭 |
| `P0-2` | 通过 | 非阻塞 | `mainline-21-p0-2-release-control-calibration-2026-07-05.md` 已确认当前定向前后端门禁通过、历史 blocker 复核关闭、`login/dashboard/inventory/approval` 最小链路可用，且文档入口已从旧第 13 条当前口径校准到第 21 条阶段口径 |
| `P1-1` | 通过 | 非阻塞 | `10b-project-isolation-first-package-closure-2026-07-01.md` 已确认 `ProjectAccessChecker -> WorkflowQueryService -> CtContractService` 冻结范围闭环成立，合同详情项目级强隔离首包可关闭 |
| `P1-2` | 通过 | 非阻塞 | `role-menu-binding-editing-closure-2026-07-01.md` 已确认角色菜单绑定正向保存、`SUPER_ADMIN` 禁改、高危系统权限 diff 禁改、无权限账号不可提交、成功/失败审计快照落库 |
| `P1-3` | 通过 | 非阻塞 | `10d-entry-registry-first-package-closure-2026-07-01.md` 已确认审批中心首批三类入口 registry/helper 已满足最小字段集，且保持“导航展示元数据，不是授权边界” |
| `P2` | 通过 | 非阻塞 | `mainline-15-alert-center-enhancement-acceptance-2026-07-03.md` 已确认第 15 条 `M1` 最小生产化增强收口通过；后续增强边界保留为专项，不回退第 15 条既有通过结论 |

## 4. 阶段判断

### 4.1 P0 阶段

判断：通过 / 非阻塞。

依据：

1. `P0-1` 已从初始阻塞裁决转为阶段收口通过，且明确只是解除当前阻塞，不冒充权限全量治理完成。
2. `P0-2` 已形成 2026-07-05 当前有效总控校准口径，不再沿用 2026-07-03 旧数字和旧“当前有效入口”表述。
3. `P0` 当前无新增阻塞项遗留到第 21 条最终收口。

### 4.2 P1 阶段

判断：通过 / 非阻塞。

依据：

1. `10B` 已按冻结范围关闭，且未越界并入采购、分包、付款、权限字典、10C、10D。
2. `10C` 已按首包关闭，后端最小门禁与审计快照补强成立，但未被错误放大为权限平台重构。
3. `10D` 已按零代码收口关闭，且首批范围仍严格限定在合同审批、采购申请、分包计量三类入口。

### 4.3 P2 阶段

判断：通过 / 非阻塞。

依据：

1. 第 15 条主线既有“通过 / 非阻塞”归档继续有效。
2. 本轮没有证据支持把“外部渠道正式打通”“更多角色/域/异常路径复验”升级回当前阻塞。
3. `P2` 继续保持后续增强包定位，不回灌第 21 条当前收口。

## 5. 剩余非阻塞风险

1. `authority` 差集仍未做全量补齐；当前口径是后续分模块专项，不是第 21 条当前阻塞项。
2. 角色菜单绑定仍有 `ROLE_MENU_SELF_EDIT_FORBIDDEN` 缺 dev 浏览器硬证的问题；当前仅有后端定向测试证据，既有报告已按非阻塞观察项处理。
3. 入口 registry 当前只覆盖首批审批中心三类入口；付款申请及更多业务类型未接入，不得误写为已完成。
4. `P0-2` 报告中保留的 dashboard 自动化断言滞后、全量门禁未重跑等事项，仍属非阻塞测试债务。
5. 第 15 条外部渠道正式打通、更多角色/域/异常路径复验仍是后续增强项；当前只能确认“占位与边界清晰”，不能确认“全部正式完成”。

## 6. 最终裁决

### 6.1 通过 / 不通过

结论：通过。

原因：

1. `P0` 当前阻塞已解除，并形成当前有效总控校准口径。
2. `P1` 三个最小实现包均已有正式通过归档，且边界未被扩写。
3. `P2` 已被稳定维持为非阻塞增强边界，没有证据要求回退既有通过主线。

### 6.2 阻塞 / 非阻塞

结论：非阻塞。

原因：

1. 剩余事项均已有正式边界，且现有证据不足以把它们重新升级为第 21 条当前阻塞。
2. 当前剩余问题主要属于后续专项、扩面复验或自动化债务，不属于本主线必须继续占用的阻塞收口项。

### 6.3 是否可关闭第 21 条主线

结论：可关闭。

关闭条件说明：

1. 第 21 条作为“非阻塞遗留问题治理主线”的当前目标已经完成。
2. 第 21 条不是第 15 至第 20 条返工主线，也不是一次性全量治理主线。
3. 后续若继续推进 `authority`、角色菜单绑定扩面、入口 registry 扩面或外部渠道正式打通，应另开专项，不回写本报告为未完成。

## 7. 文档索引口径

建议把本报告作为 `docs/README.md` 的当前有效结论入口。

保留说明：

1. `mainline-21-p0-2-release-control-calibration-2026-07-05.md` 继续保留为第 21 条 `P0-2` 阶段总控校准入口。
2. `mainline-13-closeout-2026-07-03.md`、`mainline-13-historical-blockers-review-2026-07-03.md` 继续保留为历史背景入口。
3. 不删除 `P0-2`、第 13 条或更早历史报告，只调整“当前有效”标注。

## 8. 归档说明

1. 本报告为证据汇总型正式归档，不新增测试、构建、运行态或浏览器复验。
2. 本报告未提交 Git，未推送，未清理工作区。
3. 若后续需要新的上线裁决，应基于后续实际改动重新做阶段校准，不直接复用本报告替代新增验证。

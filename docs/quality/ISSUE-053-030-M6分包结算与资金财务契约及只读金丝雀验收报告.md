# ISSUE-053-030 M6分包结算与资金财务契约及只读金丝雀验收报告

## 1. 裁决

- 结论：通过。
- 范围：M6共享API/DTO/查询权限/十进制字符串契约、V2只读薄服务、项目范围根修、代表性GET零写金丝雀。
- 基线：`master`，`d5b93242fa64b429f4658cb48d5c4090e1e59578`，保留工作区既有未提交成果。
- 路由：未迁移，台账保持`LEGACY_ONLY=40 / V2_ACCEPTED=47 / V2_SOURCE_AVAILABLE=0`。
- Git/环境：未commit、push、PR、切正式入口、退役Legacy、连接生产或执行生产写入。

## 2. 实施结果

- 新增`packages/frontend-contracts/src/subcontract.ts`与`finance.ts`，冻结分包任务、计量、结算、付款、费用、发票、日记账和凭证的查询权限、API、DTO及十进制字符串字段；月结只冻结权限/API，不伪造原始Map的前端DTO。
- 新增`frontend-admin-v2/src/services/subcontract.ts`与`finance.ts`，只复用既有`apiRequest`并提供稳定VO对应GET；query编码、GET零body和AbortSignal透传成立。
- 收入结算、资金计划、现金预测、财务期间仍返回原始`Map<String,Object>`，030未暴露对应V2服务；其稳定DTO/字符串适配由033/034在页面接入前完成。
- 修正分包任务导航权限为`subtask:query`，并补齐费用、收入运营、会计凭证、现金预测、财务月结等路由台账查询权限。
- 为分包计量、结算、费用、发票、资金日记账和会计凭证列表/详情补项目范围失败关闭；结算试算、金额基线、来源及全部嵌套明细统一复用项目校验，结算与合同项目关系不一致使用稳定错误码失败关闭，变更、计量和付款金额汇总统一按`tenant + project + contract`过滤。显式项目先校验权限，无显式项目只返回当前用户可访问项目或合法全局财务事实。
- 新增前端契约金丝雀和后端`M6ReadOnlyCanaryTest`，覆盖权限正负样本、旧权限拒绝、无项目范围空页、金额字符串及六张业务表查询前后快照一致。

## 3. 权限、范围与金额

- 分包任务权威查询权限为`subtask:query`；旧`subcontract:task:query`真实请求返回403。
- 代表性GET使用各Controller精确权限；无权限请求全部403，不依赖ADMIN/SUPER_ADMIN兜底。
- 同租户但无项目成员关系的用户不能读取分包计量、结算试算/基线/来源/嵌套明细、费用、发票、项目资金日记账或项目会计凭证；跨租户结算读取同样失败关闭。
- ID和金额在稳定DTO中保持字符串；`AccountingEntry.totalDebit/totalCredit`使用应用实际`ObjectMapper`验证为字符串，空值与`0`不混同。
- 前端契约和薄服务不含`Number`、`parseFloat`、`parseInt`或写请求，不复制Legacy源码、Vue状态或CSS。

## 4. 验证证据

| 验证 | 结果 |
| --- | --- |
| Ready lint | 通过；hash=`cb9d4b809ec54ae60f357edce04272273bebf74b8a68c35979159e1a478fff59` |
| 后端M6金丝雀+结算查询/金额写侧+日记账Mapper | `42`项，失败`0`、错误`0` |
| 受影响后端域回归 | `93`项，失败`0`、错误`0` |
| V2单测 | `44`文件、`331`项通过 |
| 契约类型 / V2类型 | 通过 |
| ESLint | 通过，0错误、0警告 |
| Clean-room边界 | 161个V2文件、16个契约文件通过 |
| 路由台账检查 | 87路由、73视图、65唯一视图；状态保持`40/47/0` |
| V2构建 | 通过 |
| `git diff --check` | 通过；仅既有M5文件行尾转换提示，无空白错误 |
| 独立复核 | 通过；权限、合同—项目关系、来源项目谓词、金额汇总和负向测试无剩余030阻塞 |

## 5. 写侧激活门与发现项收口

- 本轮修复并复验：分包任务权限错配；六个资金/结算域列表或详情及结算试算/基线/来源/嵌套明细的项目范围缺口；结算—合同项目关系和变更/计量/付款金额汇总项目谓词；结算来源金额字符串；结算筛选参数漂移；原始财务期间Map误暴露；日记账Mapper签名回归；会计金额字符串验证。
- `ISSUE-053-032`正式承接：结算切换合同时必须使用新合同回填金额，明细金额不得信任客户端；写侧开放前补服务端重算与并发/跨范围测试。
- `ISSUE-053-033`正式承接：业主结算累计金额的“先汇总后插入”必须补合同级串行化或等价约束，防止并发超合同金额；收入原始Map先转稳定DTO。
- `ISSUE-053-034`正式承接：会计凭证全部乐观锁更新必须检查影响行数；月结检查、关账、反结账必须补期间行锁或等价CAS；资金运营/预测原始Map先转稳定DTO。
- 上述承接均已写入M6计划对应切片激活门，未修前无V2写侧入口，不构成030只读金丝雀带病放行。
- 证据不足或无明确价值而关闭：无需新增数据库migration、页面、状态库、请求层、通用财务框架或手工凭证入口。
- 新增后续项：0；关闭后续项：0；后续项净变化：0；悬空项：0。

## 6. 回滚

回退M6共享契约、V2只读服务、导航/台账权限修正、项目范围根修、专项测试及本报告/治理回写即可。无数据库、业务数据、路由状态或生产影响。

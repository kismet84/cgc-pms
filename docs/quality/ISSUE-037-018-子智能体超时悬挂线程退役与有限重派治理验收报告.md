# ISSUE-037-018 验收报告

## 结论

- 裁决：通过。
- 阻塞：非阻塞。
- 非阻塞观察：`git diff --check` 仅提示 PowerShell 文件后续可能由 Git 转换为 CRLF，不影响校验结果。

## 已完成实现

- 300 秒无新证据仅记录一次 inspect；恢复进展后按新 `lastProgressAt` 重新计时。
- 600 秒无新证据终止进程树并持久记录退役 executor；退役 PID 从 `state.json` 恢复且禁止复用。
- stall 仅允许 `retryCount=1` 的一次 repair；第二次 stall 直接进入 `BLOCKED` 并写入 blocked backlog。
- inspect、retire、retry、blocked 事件包含 issue/task、executorPid、startedAt、lastProgressAt、retryCount、timeoutReason、retiredAt/retiredStatus。
- 上下文记录预计超过 600 秒的命令；只有声明命令的活后代且未到预计时长时豁免 stall，总 deadline 在豁免判断前执行。

## 验收证据

- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/codex-autopilot/test-executor-stall.ps1`：通过。
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/codex-autopilot/test-state-machine.ps1`：通过。
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/codex-autopilot/test-progress-fingerprint.ps1`：通过。
- `powershell -NoProfile -ExecutionPolicy Bypass -File plugins/cgc-pms-autopilot/scripts/validate-loop-artifacts.ps1`：通过。
- `git diff --check`：通过。

## 独立复核与补修

- 独立复核首次发现两项阻塞：跨运行仅按数值 PID 退役会误杀 PID 复用的新执行器；长命令按 executor 启动时间计时会提前终止延迟启动的后代命令。
- 补修删除 PID-only 全局墓碑，并以 `issueId + pid + startedAt` 退役记录作为证据；新进程不因历史数值 PID 相同而被终止。
- 长命令豁免改按匹配后代进程自身 `CreationDate` 计算预计时长；总 timeout 仍先于豁免判断。
- 两项均先新增失败断言，再完成最小修复；专项测试复跑通过。

## 回滚与上线边界

回滚只恢复本 Issue 的 runner/state/context/schema/test 与文档差异；无业务数据或 schema 回滚。本轮只允许本地合并，不自动 push、不发布生产。

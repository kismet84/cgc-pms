# 第52条主线 Phase 4.1：全页面演示数据包 V2 验收报告

初验日期：2026-07-18；数据质量复验日期：2026-07-19

验收对象：`scripts/demo/complete-project-v2`

数据库：本地隔离库 `cgc_pms_demo_v2`

裁决：**通过；无数据阻塞；修复后的 V2 可继续作为第53条全量 UI 重构的数据基线。**

## 1. 范围与边界

- 本轮只扩展演示数据、覆盖矩阵、验证脚本、V216 权威字典依赖和正式文档。
- 未修改 `complete-project-v1`、B215、Legacy UI 或已完成的新驾驶舱；V216 只规范项目、合作方、合同、成本字典及既有编码迁移。
- 未连接生产，未重置 `cgc_pms` 或 `cgc_pms_demo`，未创建明文密码或仓库密钥。
- 运行态使用独立数据库、后端 8082 和前端 5176；既有 8080、5173、5174 保持不变。

## 2. 正式交付物

1. `scripts/demo/complete-project-v2/manifest.yml`：v2 包清单与 14 个阶段。
2. `scripts/demo/complete-project-v2/sql/*.sql`：核心闭环、财务、生产、寻源、技术、变更、收尾、控制、库存异常、文档与表单字段补全数据。
3. `load.ps1`、`verify.ps1`：安全加载、checkpoint 续载、业务与守恒验证。
4. `field-coverage/form-field-coverage.csv`、`page-table-map.json` 及生成/验收脚本：页面、表单、字段、API 与数据表覆盖依据。
5. `DATA-ISSUES.md`：DQ-001～DQ-009 唯一问题台账与正式承接边界。
6. Phase 4.1 计划书与本验收报告。

## 3. 验收证据

### 3.1 加载与幂等

- 14 个阶段全部完成，`completed_stage=14`。
- 相同包重复加载前后共 77 项指标无变化，未产生重复对象或累计漂移。
- 2026-07-19 将数据质量阶段强制重复执行两次，两次均 `passed=true`；数量、金额、业务恒等式及权威值校验无漂移。
- 代表性对象：演示项目 8、合作方 7、合同 6、收付款发票各 3、现金周期 3、生产计量 3、寻源事件 3、技术方案 3、变更令 3、竣工明细 5、文档生成 2。

### 3.2 字段、表与关系覆盖

- 表单项 617，绑定字段 591。
- 字段处置：`DATA_VALUE` 488、`API_ALIAS` 61、`COMMAND_INPUT` 32、`COMPOSITE_CONTROL` 15、`FILE_UPLOAD` 12、`JSON_VALUE` 4、`SENSITIVE_INPUT` 4、`CLIENT_STATE` 1。
- 数据字段空值缺口 0，未解析项 0。
- 目标表 165，空表 0。
- 外键 371，孤儿外键 0。
- 敏感输入只做字段分类，不在数据包中固化密码或密钥。

### 3.3 业务一致性

- 采购申请、订单、收货均为 100；发料 20；库存 80，库存差额 0。
- 实际成本合计 85,000；质量安全成本 5,000。
- 分包、收款、应收、物料成本、采购金额、计量金额、发票分配、投标移交和会计平衡差额均为 0。
- 正常、边界、业务异常样本使用合法状态机；未通过非法外键、越权租户、负库存或失衡金额制造异常。

### 3.4 隔离运行态与真实页面

- 后端健康检查返回 `UP`，前端入口返回 HTTP 200。
- 后端和前端连续 180 秒稳定观察期间保持可达。
- 通过前端代理抽检项目、经营驾驶舱、成本控制、生产计量、供应商寻源、技术管理、项目收尾和财务集成八域接口，业务响应码均为 0。
- 浏览器真实页面成功选择 `CGC-PMS 全业务闭环演示项目`；动态利润页面回读合同收入 10,000,000、实际成本 85,000、完工预测成本 6,585,000、预测利润 3,415,000、利润率 34.15%。
- 同页显示审批中纠偏 `M52-CORRECT-001` 与已关闭纠偏 `M52-CORRECT-CLOSED-001`，业务异常/闭环状态可辨识；浏览器错误日志为 0。

### 3.5 2026-07-19 数据质量复验

- 合作方：7 条；完整性、信用代码格式、合作方类型非法数均为 0。
- 项目：8 条；覆盖 4 个正式类型、5 个正式状态、5 个审批状态；缺失值、非法权威值、错误日期均为 0。
- 合同：6 条；覆盖 5 个正式类型、4 个正式状态；非法类型/状态/审批态与无效合同双方均为 0。
- 成本：核心项目 6 条；成本类型、19 类来源字典、5 类成本状态及启用成本科目的非法数均为 0。
- 审批：实例、任务、节点状态及 3 种审批模式均覆盖；非法工作流值为 0。
- 质量处置：合同、项目、合作方相对方追溯错误为 0。
- V216 H2 迁移测试 1/1、字典服务测试 18/18、前端项目/合同字典测试 16/16 通过；前端生产构建通过。
- 隔离运行态已刷新到字典重构工作区：Flyway 成功从 V215 升至 V216；`http://localhost:8082/api/actuator/health`、`http://localhost:5176/login`、`http://localhost:5176/project/list` 均返回 HTTP 200。
- 真实 API 回读：项目 8、合作方 7、合同 6、成本 6；返回的类型、状态和来源均与正式字典一致。Playwright 冒烟因本机浏览器可执行文件未安装而未执行，归类为 `tool_config`，不作为业务失败；页面入口与代理 API 已独立验真。

## 4. 回滚与临时产物

- 停止独立 demo backend/frontend 即可退出运行态，不影响既有实例。
- `cgc_pms_demo_v2` 当前保留供人工检查；删除或重建需另行确认。
- Docker 容器、运行日志和浏览器状态均为本地临时运行态，不纳入版本管理。

## 5. 问题处置与剩余风险

- DQ-001～DQ-004、DQ-006～DQ-008 的数据问题已本轮修复并复验；DQ-005 的数据追溯已修复；DQ-009 的数据权威校验及审批样本已修复。
- DQ-005 前后端“关联合同必选”规则、DQ-009 其他页面清除私有枚举属于应用层改造，已在 `DATA-ISSUES.md` 唯一承接至第53条主线，不属于本次数据包范围，不悬空。
- 剩余风险：覆盖矩阵基于当前可达前端表单和 V215 schema。后续新增页面、字段或迁移时必须重新生成矩阵并复验；该风险属于版本演进条件，不是当前缺陷。

## 6. 收口裁决

- 结论：通过。
- 阻塞：无。
- 是否可进入下一阶段：可以，下一阶段为第53条 UI V2 Clean-room 重构。
- 新增后续项：2 个应用层承接项（DQ-005 应用规则、DQ-009 全 UI 字典消费），均已有唯一载体、解除条件与验收标准。
- 关闭后续项：9 个数据问题（DQ-005、DQ-009 的数据部分同步关闭）。
- 后续项净变化：`2 - 9 = -7`；剩余 2 项均为应用层正式承接，不影响本次数据裁决。
- Git：仅形成独立工作区未提交变更；未提交、未推送、未合并，不污染第53条 UI 工作区。

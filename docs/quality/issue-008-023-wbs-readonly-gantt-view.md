# ISSUE-008-023 WBS 只读甘特展示最小落地收口报告

## 结论

- 结论：通过
- 阻塞：无
- 是否可计入本轮实施型 Ready Issue：是，计入第 4 个
- 合并方式：auto-merge/local-commit-only；本次归档不 stage、不 commit、不 push

## 目标与边界

- 目标：基于现有分包任务 / WBS 数据，完成项目内 WBS 树与只读甘特展示的最小落地，使 WBS 编码、任务名、计划日期、实际日期、进度和状态可见。
- 实现边界：复用现有前端页面、类型、样式与既有后端接口；后端生产代码、数据库 migration、部署配置均不在本轮范围。
- 明确未做：不新增甘特图库，不新增拖拽排程、依赖线编辑、基线管理、计划变更审计或完整 schedule 模块。

## 修改摘要

- 分包任务页面补齐只读 WBS / 甘特概览展示入口。
- 展示口径保持只读，按现有任务字段呈现计划、实际、进度和状态信息。
- 后端仅通过既有 `SubTaskControllerTest` 保护契约，不改生产接口和持久化结构。

## 验收证据

- `cd backend; .\mvnw.cmd "-Dtest=SubTaskControllerTest" test`：通过，10/10。
- `cd frontend-admin; pnpm type-check`：通过。
- `cd frontend-admin; pnpm build`：通过。
- `git diff --check`：通过。
- E 审查：PASS，Critical / Important / Minor 均无。

## 失败分类或非失败分类

- 非失败分类：本轮为 WBS 只读可见性最小落地，未发现阻塞级质量、安全、权限或数据一致性问题。
- 后续真实浏览器 / 组件渲染证据弱的问题已由 `ISSUE-008-026` 承接，不阻塞当前收口。

## 非阻塞观察与沉淀

- 非阻塞观察：当前自动化证据覆盖后端契约、类型检查和构建；真实浏览器 / 组件层渲染、空态与降级态还需专项验收。
- 沉淀位置：`ISSUE-008-026：WBS 平台化缺口-M4：只读甘特真实渲染与降级态验收治理`。

## 剩余风险

- 当前只证明 WBS 树与只读甘特概览的最小可见性，不代表完整进度计划 / 甘特平台完成。
- 拖拽排程、任务依赖、基线管理、变更审计仍不在本轮范围。

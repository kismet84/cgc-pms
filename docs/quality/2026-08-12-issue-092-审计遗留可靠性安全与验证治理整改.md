# 第92条主线：审计遗留可靠性、安全与验证治理整改收口报告

**日期：** 2026-08-12

**最终裁决：** `IMPLEMENTED / G0-G5_PASSED / GIT_DELIVERY_MERGED / POST_MERGE_VERIFIED`

**可执行实现证据 SHA：** `34e6c40e7435c65a47fe8d23ff653cb4a873741b`

**基线：** `origin/master@94fc8bfeb51f68dcc12a028a439515f4c5cd6c32`

**分支：** `codex/mainline-92-audit-reliability`

**交付：** 源 HEAD `c7488482cb0ffb5e3fd5aa5af3aa7aed46288c14`；实现 PR #435；合并 SHA `04e45327dcaa6a60e6f3bc5de11247b874fa04aa`

**环境：** 仅本地 dev/test/demo；不包含生产、非本地环境、Tag、Release 或上线裁决

## 1. 结论

引用审查的 13 项发现已全部归类：F01/F02/F03/F05/F06/F07/F09/F10/F11/F12/F13 共 11 项本轮修复并复验；F04 已由第81条 contract/live 分层完成而有据关闭；F08 只在不存在的非本地环境语境成立，按仓库 local-only 边界关闭。正式承接 0、未收口 0。

本轮没有建设第二备份平台、第二权限框架、监控平台、required live CI 或非本地发布流程。JVM、备份、租户关联表、收尾详情、MySQL 门禁、供应链、镜像默认值、覆盖率、CORS/CSRF 与 Codemap 采用既有架构内最小修复。本地 G0～G5 与受保护 Git 交付全部通过；结论不扩展为生产、非本地环境或上线裁决。

## 2. 基线与隔离

- 共享工作区保持 `master@6136f8ae`，相对 `origin/master@94fc8bfe` ahead 1 / behind 2；未 cherry-pick 本地独有提交，未迁入旧 Codemap。
- 隔离工作树：`D:/projects-test/_worktrees/cgc-pms/mainline-92-audit-reliability`；初始回滚点 `94fc8bfe`。
- 当前实现分支只包含 M92 任务自有代码、测试、夹具和治理文档；禁止目录未读取、未迁入、未暂存。
- 数据与运行只使用 loopback、专用 `cgc_pms_demo_v2`、任务后端 `cgc-pms-m92-backend` 和任务 heapdump volume；共享 master 工作区未改。

## 3. 13 项发现裁决

| ID | 状态 | 结果 |
| --- | --- | --- |
| F01 | 本轮修复并复验 | 删除固定 `-Xmx1g`，改为 65% 容器感知堆；Compose 固定 1GiB memory/swap，heapdump 持久化 |
| F02 | 本轮修复并复验 | 本地备份制品统一为同根 `.partial/complete` 批次、manifest/metadata/inventory；README 与调度入口一致；不外推异地容灾 |
| F03 | 本轮修复并复验 | MySQL/MinIO 同批校验后同文件系统原子发布；失败精确清理，Windows 单库也使用 `.partial`、回读和原子移动 |
| F04 | 有据关闭 | 第81条已明确 required contract 与本地 9 组 live 分层；本轮复跑，不重复建设 release/prod E2E |
| F05 | 本轮修复并复验 | MySQL job 拆分 fresh/main/upgrade；Communication、CodeGeneration、RBAC 等真实 MySQL 门纳入，升级库独立 |
| F06 | 本轮修复并复验 | closeout overview 九类明细均 `LIMIT/OFFSET`，`detailPageSize<=100`；金额/状态/结构守恒 |
| F07 | 本轮修复并复验 | 两关联表不再全局 ignore；仅其 Mapper 缺上下文 fail-close，显式绕过 SQL 补 tenant join，后台旧路径不被全局破坏 |
| F08 | 有据关闭 | 当前仅本地环境；不创建 Alertmanager/真实通知交付任务，不把相邻既有项冒充承接 |
| F09 | 本轮修复并复验 | weekly/manual 供应链重扫、只读权限、固定 Action SHA、Maven/npm Dependabot、Node/第三方镜像 digest |
| F10 | 本轮修复并复验 | Dockerfile 删除与 validator 冲突的 datasource fallback；独立镜像缺配置明确失败 |
| F11 | 本轮修复并复验 | 全局 73/60 防回退保留；新增 `com.cgcpms.auth.config`、`com.cgcpms.config` 包级 5%/3% 门，未宣称已达 80/70 |
| F12 | 本轮修复并复验 | credentialed CORS 允许 `X-XSRF-TOKEN`；保持同站 Cookie/CSRF 语义，未知 origin 失败关闭 |
| F13 | 本轮修复并复验 | Codemap 改为 `generation_base_commit/input_scope_dirty/comparison_to_previous_generation`，verify 不再冒充当前 HEAD |

## 4. G0～G5 证据

| 门禁 | 裁决 | 证据 |
| --- | --- | --- |
| G0 | PASS | fetch 后绑定 `94fc8bfe`；迁移白名单为空；隔离 worktree、路径所有权、Codemap 三问和回滚点成立 |
| G1 | PASS | runtime、备份、租户、CORS、closeout、MySQL、覆盖率、供应链、Codemap 均先有失败契约和量化边界 |
| G2 | PASS / NO_MIGRATION | JVM/Compose、datasource fail-close、原子备份与恢复制品完成；关联表无需 schema 变更 |
| G3 | PASS | 窄 RBAC tenant guard、显式 tenant SQL、closeout 有界查询、MySQL selector、覆盖率与同站安全契约通过 |
| G4 | PASS | 隔离运行态、cgroup/heap/RSS/OOM、heapdump、CORS/CSRF、专用 demo 数据、9 组 live 与应用内浏览器通过 |
| G5 | PASS | 后端/前端全量、静态合同、Codemap、质量报告、零悬空及 exact-SHA push/PR/post-merge 交付门禁全部通过 |

## 5. 自动化与运行证据

### 5.1 后端、MySQL 与覆盖率

- `TEST_JWT_SECRET` 使用 CI 同源 test-only 值后，`mvnw verify`：361 份 Surefire 报告、2735 tests、0 failure、0 error、28 条显式 gated skip。
- JaCoCo XML：Instruction `79.827%`，Branch `61.570%`；全局 73/60 门通过。`auth.config` 为 `96.389% / 73.077%`，`config` 为 `91.540% / 80.597%`，包级 5%/3% 门通过。
- 真实 MySQL 分段：fresh baseline 1、main selector 24、独立 upgrade 1，均通过；RBAC 覆盖 tenant 0/1001、错/缺上下文、失败后回读和共享模板。
- 金额目录定向：最终 `BusinessAmountFieldCatalogTest` 14、`BusinessAmountResponseAdviceTest` 6，共 20/20；closeout 与 technical 动态 Map 均走真实 MethodParameter。

### 5.2 前端、contract 与 live

- Vitest：159 suites / 568 tests，全部通过；`type-check`、`type-check:contracts`、lint（0 error、9 既有 warning）和 Vite production build 通过。
- required contract：`dc3dad3d9240` 上 27 组 / 98 tests 通过，0 skipped/unexpected/flaky；默认占有 4173 并启动当前工作树 Vite，不再隐式复用其他 worktree 服务；没有把 contract 描述为真实全栈。
- live 运行证据 SHA `4ea8f16fef83`：固定 9 组、80/80、0 skipped、0 unexpected、0 flaky，专用库 verify 通过。
- `4ea8f16f..dc3dad3d` 在 backend、`frontend-admin-v2/src`、packages、deploy 与 demo 运行路径上无 diff；live 应用树等价，后续只新增 contract 服务隔离配置/脚本/测试。
- live 中发现并本轮关闭：M89 角色/结算夹具漂移、日报陈旧角色断言、两个 query-only 账号残留 EMPLOYEE 绑定、同页反复切角色耗尽 Chromium buffer、closeout/technical 动态 Map 金额目录遗漏。

### 5.3 运行、备份与治理合同

- 隔离后端 cgroup=1GiB，JVM max heap 约 643.81MiB；代表性负载 RSS 约 618～656MiB，`OOMKilled=false`。受控 OOM 探针产生可读取持久 heapdump，随后仅清理探针文件。
- `test-runtime-deployment-contract.ps1`：MaxRAMPercentage=65、heapdump 路径、1G 限制、datasource 无 fallback，PASS。
- `test-backup-atomicity.sh` 覆盖上游、gzip、截断、hash、alias、跨设备/符号链接、空桶、非法 token、Secret argv、mc `share/certs/CAs` 嵌套配置清理等失败，PASS；Windows backup contract PASS。
- Docker Desktop 恢复后，`34e6c40e7435` 的真实 MySQL+MinIO 演练通过：MySQL 1 行、MinIO 1 对象、对象 SHA-256 与数据库→bucket/key/hash 引用守恒，完整批次原子发布并清理。Linux UID:GID 路径已由源 HEAD `c7488482cb0f` 的 push 与 PR 两次 `reliability-contracts` 证明。
- workflow contract：required job count 16，包含独立 `reliability-contracts`；pre-PR gate、自测、Codemap generator self-test 与 `--verify` 全部通过。

## 6. 浏览器与安全证据

- 技术查询账号打开 `http://127.0.0.1:4174/technical-management?projectId=520000000000000001&tab=scheme`；DOM 显示六个业务 tab，计数 `3/3/2/2/2/2`，无写按钮、无错误遮罩、console 0 error/warn。
- 收尾查询账号打开 `http://127.0.0.1:4174/project-closeout?projectId=520000000000000001`；真实收尾、验收、结算、应收、质保、缺陷、WBS、档案结构可见；七类金额显示为 `—`，WBS 进度 `100.00` 保留，console 0 error/warn。
- CORS/CSRF：合法 loopback origin 与 `X-XSRF-TOKEN` 通过；未知 origin、缺/错 token 保持拒绝。未放宽为跨站 Cookie。
- 独立只读复核确认：RBAC guard 仅限两关联 Mapper；closeout 七个金额路径与两个 safe Decimal 完整；technical overview 两个 safe Decimal、trace 一个金额路径完整，无泛化 Map 放行。

## 7. 失败分类与解决

| 现象 | 分类 | 解决与裁决 |
| --- | --- | --- |
| 本地分支落后且有独有提交 | `ready_issue_config` | 从 `origin/master@94fc8bfe` 建隔离分支；迁移白名单为空 |
| Docker build total_steps=0 | `environment_prerequisite` | 不改 Dockerfile；用同一 JAR+固定 Temurin runtime 完成本地 cgroup 验真 |
| Chromium 第七角色 `ERR_NO_BUFFER_SPACE` | `environment_prerequisite` | 每角色独立 page/context，事件驱动释放资源；权限不放宽 |
| closeout/technical query-only 400 | `quality_or_security` | 补精确 endpoint 金额/safe 路径，未知 Decimal 继续 fail-close |
| demo 权限计数和角色泄露 | `test_or_fixture` | 夹具按 M89 canonical 角色幂等收敛，不恢复旧权限 |
| 首次后端全量 JWT weak key | `tool_config` | 注入仓库 CI 规定的 test-only secret；首失败类 2/2 后全量通过 |
| 首次前端 `pnpm test` 无脚本 | `tool_config` | 改用 package.json 权威 `test:ci`，未改代码 |
| 系统 Bash 指向缺失 WSL | `tool_config` | 改用已安装 Git Bash 绝对入口，原子性合同通过 |
| exact-SHA 恢复复跑 Docker engine 500 | `environment_prerequisite` | 不重启共享 Docker；复用 blob 等价的已通过证据，远端 exact-SHA 必须重证 |
| pre-push 98 项首跑 6 项失败 | `environment_prerequisite` | 5173 实为共享 Docker 的旧 M89 worktree，served/local source hash 不同；contract gate 改用自有 4173 且禁止隐式复用，定向 20/20、全量 98/98 通过 |
| 首个 push run `31597079156` 的 `reliability-contracts` 失败 | `quality_or_security` | 容器 mc 以 root 创建宿主 bind mount 的 `share/certs`，严格清理因此失败并阻止发布；仅 Linux 包装器注入宿主 UID:GID，fake mc 补嵌套目录故障契约，Secret stdin、私有配置和原子 fail-close 不放宽 |

## 8. 剩余风险与回滚

- JaCoCo 尚未达到长期 80/70 目标，当前差距为 Instruction 0.173 个百分点、Branch 8.430 个百分点；这是渐进目标，不通过调低门槛或扩大 excludes 关闭。
- 本地 Docker Desktop 与 Linux runner 恢复证据均已取得；剩余风险不再包含备份交付门禁。
- 供应链 schedule 的真实周期触发只能由合并后的 GitHub 时间事件证明；本轮静态契约、手动入口和 exact-SHA CI 是交付门。
- 回滚以任务提交为单位；RBAC/CORS/金额目录优先回滚对应小批，备份失败时只清理任务 partial，JVM 失败时停止任务容器。禁止整树 reset、修改共享 master、删除他人 worktree或非任务数据。

## 9. 零悬空统计

- 原 13 项：修复并复验 11、有据关闭 2、正式承接 0、未收口 0。
- 11 个行动项：关闭 11、剩余 0。
- 本轮运行中新发现：8 组，全部本轮修复并复验；正式后续项新增 0。
- 新增后续项 0、关闭后续项 0、后续项净变化 0。
- 正式 Issue 新增 0、关闭 0、净变化 0；`current-issues.json` 不改。
- 正式计划载体新增 1、关闭 1、净变化 0；无无载体遗留项。

## 10. 受保护 Git 交付证据

当前状态：`PASS / REUSED_PUSH_CI`。

- 首次源 HEAD `d0615f2af81f` 的 run [`31597079156`](https://github.com/kismet84/cgc-pms/actions/runs/31597079156) 为 15 个 job 通过、`reliability-contracts` 失败；失败记录保留且不作为交付证据。
- 最终源 HEAD `c7488482cb0ffb5e3fd5aa5af3aa7aed46288c14` 的 push run [`31599845219`](https://github.com/kismet84/cgc-pms/actions/runs/31599845219) 成功，pre-PR exact-SHA verifier PASS。
- 实现 PR [#435](https://github.com/kismet84/cgc-pms/pull/435) 的 PR run [`31600592597`](https://github.com/kismet84/cgc-pms/actions/runs/31600592597) 成功；受保护 squash 合并 SHA `04e45327dcaa6a60e6f3bc5de11247b874fa04aa`。
- Post-merge run [`31601538284`](https://github.com/kismet84/cgc-pms/actions/runs/31601538284) 成功；verifier 判定 `PASS / REUSED_PUSH_CI`，源树与合并树同为 `af8644ef1426dd6edff6fa98571308acdb5a13ad`，`origin/master` 与合并 SHA 一致。

# AutoPilot 启动迭代-10 控制面异常恢复验收报告

## 结论

- 批次结论：部分完成并安全暂停；`ISSUE-040-022` 已通过并完成两阶段本地提交，当前计数1/10，未派发下一任务。
- 业务结论：通过、非阻塞。后端46/46、前端31/31、类型检查、差异检查、独立 Reviewer 和 Issue worktree 浏览器验收均通过。
- 控制面结论：本批已修复 Ready 路径矛盾、post-closeout 验证恢复、Windows 长路径 worktree 清理、Reviewer sandbox 安全重试、耐久收口计数和 stall CPU 噪声误判；这些失败分类为 `tool_config`，没有改判为业务代码失败。
- 上线边界：未连接生产、未修改生产数据、未发布、未 push；本报告不替代目标发布 SHA 的正式上线门禁。

## 恢复与修复范围

1. 保留 ISSUE-040-022 的5文件业务 diff，不再重跑 BC，从 D/E/F 恢复并完成验收、评分、归档和快进合并。
2. `pause.flag` 在恢复阶段保持生效，确保菜单 Issue 收口后不会继续派发第2项任务。
3. stall 指纹删除子进程树 CPU 累计值，只以持久化工作树内容变化刷新 `lastProgressAt`；显式声明且仍活跃的有界长命令继续走独立豁免，总 deadline 仍先于豁免判断。
4. 任务评分正式绑定实施提交 `860aeba0ad781fbe18f669fc8599e1db1ec4bd7a`，归档提交为 `72b62e6e42ab9c3bc2ed2b3b89021d047f6ce6ed`，总分95/100；周期效率因重复启动和人工恢复扣5分。

## 验收证据

- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/codex-autopilot/test-executor-stall.ps1`：通过；CPU-only 子进程不改变指纹，两个实际 stall 执行单元仍产生 inspect/retire、只重派一次并安全 Blocked。
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/codex-autopilot/test-task-scoring.ps1`：通过；评分正式来源引用不再写入绝对 worktree 路径或 `.codex-autopilot/runs` 临时制品路径。
- PowerShell AST 解析：`autopilot-progress.ps1`、`autopilot-run-continuous.ps1` 无解析错误。
- `git diff --check`：通过。
- ISSUE-040-022 正式证据见 `docs/quality/ISSUE-040-022-系统菜单平铺列表管理员入口验收报告.md`。

## 台账、状态与剩余风险

- 本批新增后续项：1（`OPS-AUTOPILOT-STALL-FINGERPRINT`，已在本批修复）。
- 本批关闭后续项：2（`A-01-MENU-LIST`、`OPS-AUTOPILOT-STALL-FINGERPRINT`）。
- 本批后续项净变化：-1。
- 当前 A-01 守恒为有用户入口228、前端调用但无独立页面58、内部/集成/运维4、需补入口20、待废弃0、需要确认11，共321。
- 当前没有本批新增的悬空风险；Ready 中其他存量任务未被本轮启动。继续迭代须由用户重新解除暂停并启动，不自动恢复。

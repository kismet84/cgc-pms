# 第71条主线：附件与文件系统整改完善验收报告

> 验收日期：2026-08-05
> 实施基线：`master@da3403809178378c8f002166af744c7b68427c55`
> 实施分支：`codex/mainline-71-file-lifecycle-20260804`
> 当前裁决：`G0-G5_LOCAL_PASSED / GIT_DELIVERY_IN_PROGRESS / PRODUCTION_NOT_AUTHORIZED`

## 1. 结论

`FILE-01～10`的代码、迁移、真实运行态、隔离恢复和本地G5已闭合；独立终审阻塞0，`ISSUE-071-001`关闭并进入同HEAD Git交付。存量dev对象漂移未自动删除或猜测回填，目标环境逐对象复扫仍由既有`REL-FILE-RESCAN`发布门承接。

## 2. 逐项裁决

| 项 | 结果 | 服务端或运行态事实 |
| --- | --- | --- |
| FILE-01 | 通过 | 开工附件类型、权限、项目状态和准入状态统一；真实上传、CLEAN、下载审计、删除、回读通过 |
| FILE-02 | 通过 | 新对象键包含tenant、业务对象和预分配fileId；双租户生命周期隔离 |
| FILE-03 | 通过 | 生成文档、投标版本和付款归档引用不可由通用删除破坏；绑定与删除锁同一文件行 |
| FILE-04 | 通过 | 上传回滚清理、删除耐久任务、提交后处理和幂等重试成立 |
| FILE-05 | 通过 | 业务类型规范化；V278建立活动内容SHA键，V279限定规范SHA文件名，V280固定大小写敏感识别；迁移前碰撞预检fail-close |
| FILE-06 | 通过 | 列表仅返回必要元数据；下载重新授权并记录真实businessType/businessId/fileId和稳定错误码 |
| FILE-07 | 通过 | 合同、项目、费用、收入、付款、发票、收料、采购、分包、结算、变更和合作方删除统一调用生命周期网关 |
| FILE-08 | 通过 | OOXML结构、MIME/扩展名、控制字符、原名下载和存储错误分类均fail-close |
| FILE-09 | 通过 | 最小共享附件组件接入合同、开工、分包、结算；权限、扫描态、竞态、下载和删除反馈一致 |
| FILE-10 | 通过 | 只读对账、批次复扫和元数据清单恢复脚本成立；不以SQL伪造CLEAN |

## 3. 客观证据

- G0初始dev盘点：活动元数据41、逻辑删除0、非规范业务类型0、重复活动组0、跨租户共享路径0；DB缺对象15、对象孤儿85为实施前存量漂移。
- H2 active/legacy迁移、文件服务/权限/审计/级联专项测试通过；迁移前碰撞预检为零行；MySQL空库V280和V180→V280升级各1项通过。
- V2共享附件4个测试文件、20项通过；合同真实页`.business-attachments`存在、空态回读正确、console error为0。
- 推送前门禁完成lint（0 error、24 warning）、类型、447项单测、生产构建和89项E2E；E2E首轮87通过、1跳过、1项finance-control在60秒总预算耗尽，调整为90秒后原失败用例单worker 1/1通过。
- 开工真实链完成临时业务创建、附件上传、CLEAN回读、下载授权审计、确认删除和服务端回读；活动测试数据与物理对象已清理。
- FILE-10隔离恢复：1对象、69,270字节、1条`file_id/tenant_id/bucket_name/storage_path/file_size/sha256/business_type/business_id`映射，备份与恢复SHA-256完全一致；一次性桶及临时文件已删除。
- backend/frontend重建后健康检查均HTTP 200；真实运行库Flyway当前版本280且成功；刷新后合同页共享附件组件1个、空态正确，控制台仅Vite调试日志、无warning/error。
- 第4轮独立高风险终审复核父对象行锁、V280大小写等义、迁移前碰撞预检、治理状态和`FILE-01～10`，结论通过、阻塞0。

## 4. 失败分类与修复

- `code_defect`：独立复核发现状态/行锁、父删除降级、审计维度、大小写约束、前端弹窗/陈旧请求、存储异常分类和共享路径删除竞态；均在共享根因处最小修复并复验。
- `environment_prerequisite`：Compose重建初次缺本机环境变量；从既有本地容器恢复同一dev配置后重建成功，未修改生产配置。
- `test_environment`：finance-control单用例串行遍历18个页面矩阵后在60秒总预算耗尽，失败截图停在合法加载态；仅将该用例预算调整为90秒，原失败用例8.9秒通过，未改业务代码。
- `tool_invocation`：PowerShell逗号解析、WSL Bash缺失、Alpine无Bash及安全策略拒绝递归清理；改用正确参数、Git Bash/含Bash镜像和逐文件清理。均未判为业务缺陷。

## 5. 风险、恢复与零悬空

- 现有dev全桶仍有15个元数据缺对象和85个对象孤儿。自动裁决归属不安全，故不在本轮删除；这是既有`REL-FILE-RESCAN`目标环境门，不是隐藏遗留。
- 新旧对象继续按持久化`bucket_name/storage_path`读取；V277～V280只强化新写入、任务、审计和规范哈希索引。异常时停用清理任务、保留元数据和备份，再按任务映射恢复。
- 本地实现、同HEAD CI、Git合并、目标环境复扫和生产发布分别取证；本报告不构成生产就绪证明。
- 计划周期新增1、关闭1、净变化`0`；本轮收口新增0、关闭1、净变化`-1`；无无载体遗留项。

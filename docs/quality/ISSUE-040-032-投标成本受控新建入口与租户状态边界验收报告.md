# ISSUE-040-032 投标成本受控新建入口与租户状态边界验收报告

结论：通过。阻塞：无。上线裁决：本地验证范围内可上线，未执行生产发布或 push。

## 交付与边界

- 新增仅含 `bidProjectName` 与可选 `remark` 的创建 DTO；Controller 不再直接接收实体，Service 强制清空客户端 ID、projectId，覆盖为当前 tenantId 和 `BIDDING`。
- 既有 `/bid-cost` 页面新增受 `bid:add` 或管理员控制的新建弹窗，成功后关闭、重置并刷新，失败保留弹窗和输入；前端类型化请求不包含 tenantId、projectId、bidStatus、amount 或审计字段。
- MySQL/H2 V151 仅在既有投标菜单下新增 `bid:add` BUTTON 并绑定 SUPER_ADMIN、ADMIN、COST_MANAGER；未开放详情、编辑、删除、中标、失标、项目转换或金额写入。

## 验收证据

- Ready lint、SQL 安全检查和 `git diff --check` 均通过。
- 后端：`BidCostControllerTest,BidCostServiceTest` 共26项通过，覆盖未登录401、无权限403、`bid:add` 正向、管理员旁路、空白/超长字段、客户端字段注入覆盖与既有创建回归；落库断言为当前租户、空 projectId、`BIDDING`、名称和备注已去空格，`cost_item` 数量为0。
- 前端：API与页面专项2个文件、4项通过；类型检查及目标 ESLint 通过。API 精确为 `POST /bid-cost`，页面成功/失败状态与权限可见性契约均有覆盖。
- 运行态：Issue 工作区独立启动 backend/frontend，连续180秒观察期间后端与前端均持续返回200；dev-login 原始响应为预期302，跟随跳转后为200并到达 dashboard，前端日志无实际 ERROR。
- 浏览器：登录后到达 `http://localhost:5173/bid-cost`，新建入口可见；弹窗仅含“投标项目名称、备注”，不含租户、关联项目、投标状态或金额字段。取消后弹窗关闭，控制台 error/warn=0，浏览器验收阶段后端 `POST /api/bid-cost` 次数为0。
- Reviewer：主线程对写入、权限、租户、状态、项目关联、金额无写入和 V151 双数据库迁移复核绑定差异 `a33ef02100b69d06b547b1fe7aabd2ca624cc8bb0740c52ba0013dc7c02e3327`，结论 `PASS`，findings=无。

## 失败分类与修正

- 首次后端组合测试在执行用例前因本地 JWT 密钥长度不足失败，分类为环境前置；首次修正误用了非测试变量，属于命令配置问题。改用测试进程要求的 `TEST_JWT_SECRET` 后，Controller与Service组合测试26/26稳定通过，未发现业务代码失败。
- 运行态稳定观察脚本最初把 dev-login 的预期302当作非200，并因 INFO 日志中的 `exception=-` 产生误命中；跟随重定向和精确日志核验后，三项 health gate 与前端日志均通过，不构成质量问题。

## 存量问题与后续项

- 依据唯一载体 `docs/backlog/current-issues.json` 及源报告 `docs/quality/ISSUE-037-019-后端接口无前端入口只读盘点与治理裁决验收报告.md`，`A-01-BID-CREATE` 已满足验收并移除。
- A-01 守恒更新为：有用户入口237、前端调用但无独立页面58、内部/集成/运维4、需补入口11、待废弃0、需要确认11，共321。
- 新增后续项0；关闭后续项1；后续项净变化-1。未发现需正式承接的悬空问题，产品候选排序未改变。

剩余风险：生产数据库尚未执行 V151，正式发布仍需按既有备份、迁移和回滚流程操作；本轮不包含生产发布。

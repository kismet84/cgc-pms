# 第26条主线 M5 阶段性全量审查收口验收报告

报告日期：2026-07-08  
报告类型：阶段性全量审查收口 / 验收报告  
报告边界：仅基于计划书、正式代码审计报告、浏览器自动化测试报告做证据汇总和裁决；不进入整改实现，不补测，不修改业务代码、配置、测试源码、运行环境或 Git 状态。

## 1 总体裁决

结论：审查任务已完成，审查结论不通过，阻塞项需后续整改后复核。

通过/不通过：不通过。  
阻塞/非阻塞：阻塞。  
是否可上线收口：不可上线收口。  
阻塞项数量：8 项。

裁决依据：

1. 第26条计划书要求 M5 形成正式审查报告、整改台账和阶段性收口结论，并要求 P0/P1 必须给出阻塞判定和复核前置条件。
2. 正式代码审计报告给出“不通过 / 阻塞”，确认 3 个 P1 阻塞项。
3. 浏览器自动化测试报告给出“不通过 / 阻塞第26条主线 M4 浏览器自动化测试验收收口”，确认导出、出入库空提交、成本页控制台警告及高风险操作覆盖不足。

## 2 证据来源

| 证据 | 路径 | 结论摘要 |
| --- | --- | --- |
| 第26条计划书 | `D:\projects-test\cgc-pms\docs\plans\第26条主线-阶段性全量代码审查任务计划书.md` | 计划书层面通过；要求先审查建账，再按风险派工整改，再复核收口 |
| 正式代码审计报告 | `D:\projects-test\cgc-pms\docs\quality\code-audit-2026-07-08-mainline-26-stage-full-review.md` | 不通过 / 阻塞；3 个 P1、8 个 P2、2 个 P3 |
| 浏览器自动化测试报告 | `D:\projects-test\cgc-pms\docs\quality\browser-automation-test-report-2026-07-08-mainline-26.md` | 不通过 / 阻塞 M4 浏览器验收收口；前端入口可达但关键操作未通过 |

## 3 计划要求对照表

| 计划要求 | 当前证据 | 验收结论 |
| --- | --- | --- |
| 审查覆盖后端、前端、安全权限、数据库、测试/CI、运维配置、日志可观测与回滚 | 代码审计报告覆盖上述维度，并给出分级问题清单 | 已完成审查 |
| 问题必须落到路径、类别、风险级别、整改归属或建议 | 代码审计报告列出路径、类型、严重级别、证据、影响和修复建议 | 已完成建账雏形 |
| P0/P1 必须阻塞并给出复核前置条件 | 3 个 P1 均标为阻塞，必须修复后复核 | 不通过，阻塞 |
| 浏览器自动化测试必须给出访问路径、操作步骤、截图、控制台/网络/API 证据和结论 | 浏览器报告已覆盖关键页面并列出截图目录、失败操作和未覆盖项 | 已完成测试，但结论不通过 |
| M5 形成正式收口结论，不提前进入整改实现 | 本报告只做证据汇总和裁决，不做代码或环境改动 | 符合边界 |
| 非阻塞问题必须进入后续主线或阶段复查 | P2/P3 已具备后续派工依据，但尚未整改 | 可后续派工 |

## 4 阻塞项清单

| 编号 | 来源 | 阻塞项 | 依据 | 最小复核前置 |
| --- | --- | --- | --- | --- |
| B1 | 代码审计 P1-1 | 成本摘要接口缺少项目级访问控制 | `CostSummaryController` 仅做权限码控制，`CostSummaryService` 只确认项目属于租户，未接入 `ProjectAccessChecker` | 修复项目级授权，补管理员、项目负责人、同租户无权限、跨租户用户测试，并确认无权限返回 403 |
| B2 | 代码审计 P1-2 | 版本库跟踪测试/浏览器产物和 `admin-state.json` | `output/playwright/**` 与 `frontend-admin/coverage-result.json` 已被版本管理跟踪，包含浏览器状态和截图产物 | 确认价值后移除运行态跟踪，补忽略规则；需保留的证据转为脱敏正式报告附件 |
| B3 | 代码审计 P1-3 | Token blacklist / Redis 降级导致登出和吊销语义不稳 | `JwtAuthenticationFilter` 在 blacklist 不可用时继续认证链路；测试日志出现 `BLACKLIST_UNAVAILABLE` | 明确生产 profile fail-closed 或强告警策略，补降级行为测试 |
| B4 | 浏览器验收 | 首页驾驶舱导出不可验收 | 点击导出后 3 秒内无下载事件、无导出 API 请求 | 修复后复测 `/dashboard` 导出，需捕获下载、API 或明确成功提示 |
| B5 | 浏览器验收 | 预警中心导出不可验收 | 点击导出后 3 秒内无下载事件、无导出 API 请求 | 修复后复测 `/alert` 导出，需捕获下载、API 或明确成功提示 |
| B6 | 浏览器验收 | 出入库空表单提交无可见校验或重复点击反馈 | `/inventory/transaction` 连续点击“确认入库”无提交 API、无必填校验、无禁用态反馈 | 修复后复测空提交校验和重复提交拦截反馈 |
| B7 | 浏览器验收 | 成本管理页存在 Vue 控制台警告 | `/cost/ledger` 出现 `projectList` 期望 Array 实际 Null 的 Vue warn | 修复组件入参默认值或调用边界，复测控制台无该警告 |
| B8 | 浏览器验收覆盖 | 高风险操作覆盖不足，上传、最终审批、有效业务提交未宣称通过 | 报告明确上传未覆盖、最终同意/驳回未执行、有效出入库提交未执行 | 在不破坏业务数据的前提下设计可回滚或测试数据方案，补测后给出通过/不通过 |

## 5 非阻塞风险

| 编号 | 来源 | 风险 | 建议去向 |
| --- | --- | --- | --- |
| R1 | 代码审计 P2 | Flyway 不可变检查不是有效门禁，Windows 脚本存在假绿 | 进入数据库/CI 质量加固派工 |
| R2 | 代码审计 P2 | H2 与 MySQL 迁移数量不一致，测试数据库与生产数据库存在漂移 | 进入 migration parity 清单和关键链路 MySQL 验证 |
| R3 | 代码审计 P2 | `echarts@5.6.0` 存在中危 XSS 漏洞 | 进入依赖升级与驾驶舱图表回归派工 |
| R4 | 代码审计 P2 | 文件接口响应暴露 `storagePath`、`bucketName` | 进入接口最小化与序列化测试派工 |
| R5 | 代码审计 P2 | 附件业务对象授权粒度不一致 | 进入对象级权限策略加固 |
| R6 | 代码审计 P2 | 多个前端关键页面超过 800 行 | 进入渐进拆分，不作为本轮上线前唯一阻塞 |
| R7 | 代码审计 P2 | 前端覆盖率低，CI 通过不能代表关键页面可靠 | 进入核心页面最小覆盖建设 |
| R8 | 代码审计 P2 | 生产 MinIO 应用凭据沿用 root 用户 | 进入部署安全加固和凭据轮换方案 |
| R9 | 代码审计 P3 | `X-Refresh-Token` 兼容路径仍保留 | 确认旧客户端依赖后分阶段收敛 |
| R10 | 代码审计 P3 | `docs/prompt/code-audit-agent.md` 缺失 | 需要主负责人确认是否补齐或废弃 |

## 6 不进入整改实现的说明

本任务是 M5 阶段性全量审查收口验收报告，不是实现型整改任务。

本轮未做以下动作：

1. 未修改业务代码、配置、测试源码或文档之外的项目资产。
2. 未运行测试、构建、浏览器、服务启动或依赖安装。
3. 未清理文件、未移除版本跟踪、未提交 Git、未变更 Git 状态。
4. 未读取或扫描 `.omc/`、`.omo/`、`.opencode/`、`.claude/`、`.mimocode/`、`graphify-out/`、`.sisyphus/`、`.archive/`。

原因：当前证据已足以做“不通过 / 阻塞”裁决；把审查收口和整改实现混在一起会破坏第26条计划书“先审查建账，再派工整改，再复核收口”的边界。

## 7 下一步最小派工建议

建议按最小闭环拆成 4 个串行任务，不并行扩大战线：

| 顺序 | 任务类型 | 建议任务 | model | thinking | reason |
| --- | --- | --- | --- | --- | --- |
| 1 | 实现型 | 修复 3 个 P1：成本摘要项目级权限、测试/浏览器产物版本跟踪、token blacklist 生产降级语义 | gpt-5.5 | high | 涉及权限、发布洁净度和会话安全语义，均为阻塞项 |
| 2 | 验收型 | 对 3 个 P1 做定向复核，确认权限测试、洁净度检查和 blacklist 降级测试通过 | gpt-5.5 | medium | 需要基于整改证据做通过/不通过裁决 |
| 3 | 实现型/前端 | 修复 M4 浏览器失败项：dashboard/alert 导出、出入库空提交反馈、成本页 `projectList` warn | gpt-5.4 | medium | 单页/局部前端行为修复为主，但涉及真实浏览器复测 |
| 4 | 验收型 | 补跑 M4 浏览器自动化复核，覆盖导出、提交/重复提交、上传、审批入口或可控审批数据方案 | gpt-5.5 | medium | 需要真实浏览器证据和高风险操作裁决 |

P2/P3 不建议压入第一轮阻塞整改；先把 8 个阻塞项关闭，再按数据库/依赖安全/对象级权限/前端结构/测试覆盖分批派工。

## 8 主线26当前状态

当前状态：阶段性审查已完成，阶段性验收不通过，主线处于“阻塞整改前置”状态。

可确认完成：

1. 第26条计划书已提供审查范围、阶段、门禁、分级、派工口径和验收标准。
2. 正式代码审计报告已产出，并识别 3 个 P1 阻塞项。
3. 浏览器自动化测试报告已产出，并给出 M4 不通过结论。
4. 本 M5 收口报告已形成通过/不通过、阻塞/非阻塞、依据、剩余风险和下一步最小派工建议。

不可确认完成：

1. 阻塞项尚未整改。
2. P1 阻塞项尚未复核通过。
3. M4 浏览器失败项和尚未覆盖的高风险操作尚未复测通过。
4. 主线26不能进入最终通过或上线收口。

最终裁决：不通过 / 阻塞。下一状态应为“阻塞项整改派工”，而不是“主线完成”。

# ISSUE-072 全量审计认证授权与生产治理整改质量报告

> 日期：2026-08-05
> 交付基线：`origin/master@ac6bd6c37c99888021ca0e7037a8f2a03d465df2`
> 实施取证基线：本地 `master@9e6e6e6f5bd56b3f86e1783f31575ddadcf64d91`
> 裁决：`G0-G5_LOCAL_PASSED / MERGED / NON_LOCAL_ENVIRONMENT_NOT_APPLICABLE`

## 实施结果

- `AUTH-01`：登录显式提交非负 `tenantId`；登录、refresh、凭据版本、角色和权限快照均使用 `tenantId + userId/username`；未知租户、用户和密码保持统一失败；access/refresh JWT 使用随机 `jti`，保证同秒轮换不会复用并被旧令牌黑名单连带撤销。
- `BID-01`：投标选项在 SQL 层按可访问项目过滤；保证金汇总、查询和写入口复用共享对象门；现金日记账分页 SQL 同步约束绑定投标的项目范围。
- `CFG-01`：移除默认 dev profile；prod 缺值、loopback（含 FQDN 尾点、展开 IPv6 与 IPv4-mapped IPv6）、示例域名、不安全 JDBC 参数及弱 JWT 在 Spring 上下文刷新前失败退出，错误仅列配置键。
- `CI-01`：第三方 Action 固定完整 commit SHA；MySQL、Redis、Trivy、MinIO 和 MinIO Client 固定 OCI digest；静态契约按任意 YAML `image:` 和 `docker run/pull` 输入拒绝无 digest 引用，并用未知 `postgres:latest` 样本自检。
- `GOV-01`：第69～71条合并事实已校准；第72条进入本地实施验收状态。
- 基线隔离：交付分支未携带本地V281材料迁移及两处V281断言适配；2 个工作流测试保留显式租户权限查询整改。
- 独立复核补强：隔离 MySQL/Redis/MinIO 运行态完成租户0/1001同名用户登录→userinfo→refresh→旧 access/refresh 重放拒绝→logout；审计行按租户落库，Redis 精确黑名单键存在。隔离 H2 与 MySQL 均以真实表验证未绑定、授权、无权、空范围、管理员和跨租户投标；无权汇总返回 `PROJECT_ACCESS_DENIED` 且零写入。全程不写入现有 dev 数据库。
- Code map：重生成 `docs/codemap` 三件套；22 节点、46 条证据边、10 条业务流，路径/符号与 HTML 内嵌数据通过一致性校验；lock 改为纳入作用域内未跟踪源码。
- 治理契约：根 `AGENTS.md` 新增 Code map gate 后为76行；静态上限由70校准为80，并新增三件套关键字断言，未删减用户规则。HighRisk 主线与执行策略契约均通过。

本轮未新增 migration、依赖、权限中台、租户表或平行授权组件；未写入现有数据库。PR #391 已通过保护分支 squash merge，post-merge 成功；未发布、未执行生产操作。验收后已删除五个 `cgc-pms-72-*` 临时容器和 `cgc-pms-72-net`；未创建或删除 volume，临时测试库不可恢复。

## 验证证据

| 验证 | 结果 |
| --- | --- |
| 最终组合定向测试 | 55 tests，0 failure/error；覆盖认证、prod 配置、投标对象门和 H2 数据范围 |
| BID 双引擎数据测试 | H2 2/2、MySQL 2/2；成员/空范围/管理员/跨租户/无权零写入均通过 |
| 隔离源分支 `backend/mvnw verify` | 2505 tests，0 failure/error，14 skipped；BUILD SUCCESS，452.2s |
| 隔离源分支 JaCoCo | instruction `80.95%`；branch `60.44%`，均达到计划阈值 |
| 前端契约、lint、单测、类型、构建 | 契约通过；lint 0 error（25 个既有无关 warning）；19 tests 通过；type-check/build 通过 |
| 前端依赖审计 | 0 high/critical；1 moderate |
| 后端依赖扫描 | 本地新鲜 Trivy DB 扫描0漏洞；修复 HEAD 的 push/PR `backend-dependency-scan` 与内置jar Trivy供应链扫描均通过 |
| CI 工作流契约 | 13 required jobs、9 uploads、3 downloads、2 permission blocks、1 post-merge job、4 post-merge steps，通过 |
| 首次 source push CI | `c858369c` 的 `sql-safety-scan` 将 Spring 占位符字面量误识别为 MyBatis 替换；分类 `quality_or_security/DELIVERY_GATE_OMISSION`，按扫描器既有同行标记补充具体非 SQL 理由，本地等价扫描复验通过；首次失败事实保留 |
| 修复 HEAD push CI | `08eed68cde3fa3a410c993f6e0e27aa18a84f797`，13/13成功；`verify-pre-pr-ci.ps1` PASS |
| PR #391 首次 CI | 同一 `08eed68c`，`pull_request` 13/13成功；PR CLEAN、MERGEABLE |
| 合并与 post-merge | squash `master@cd0c3a2e7032cb636de8c9b95cc6b86faed49ead`；同 SHA post-merge 成功 |
| 治理契约 | task policy、HighRisk mainline owner、Backlog JSON、代码地图132条引用/嵌入数据/lock 指纹、`git diff --check` 通过 |
| prod 失败关闭 | 缺少 prod 配置时直接 JAR 在监听端口前退出，exit code `1`；日志仅列配置键 |
| 隔离真实 MySQL API | 租户0/1001完整登录、刷新、重放拒绝和注销通过；跨租户错误凭据 2/2 为 `AUTH_FAILED`；缺失/负租户由参数校验返回 `VALIDATION_ERROR` |
| 实施取证运行态 | 取证基线 MySQL V281、backend health `UP`；Redis 黑名单8键；LOGIN/LOGOUT 审计按租户0/1001落库；交付分支未携带V281 |
| 浏览器 | 隔离前端桌面及 390px 登录页三字段可访问；租户1001登录进入 `/approval/todo`，注销回登录页；console 0 warning/error |

## 门禁裁决

- `G0 PASS`：基线、共享脏改归属、授权范围和现有运行态已锁定。
- `G1 PASS`：认证、投标、prod 配置、CI 不可变性及治理契约完整。
- `G2 PASS_LOCAL`：H2/MySQL 真实数据覆盖未绑定、授权、空范围、管理员、无权和跨租户投标；隔离 MySQL 覆盖租户0/1001同名认证与拒绝路径；无 migration。远端 MySQL CI 仍须绑定未来 source HEAD。
- `G3 PASS_LOCAL`：共享服务、Mapper、启动校验和 CI 静态契约闭环；完整后端及前端门禁通过。
- `G4 PASS_LOCAL`：隔离 MySQL/Redis/MinIO 已验证租户0/1001完整 HTTP 生命周期、精确令牌重放拒绝、双租户审计和桌面/窄视口浏览器。项目仅有本地开发环境；非本地环境测试/验收不适用且禁止发起。
- `G5 PASS`：修复 HEAD 的 push CI、verifier、PR首次CI、保护分支squash merge及merge SHA post-merge全部通过；实现源分支本地和远端已删除。

因此：第72条G0-G5通过并完成受保护Git交付。非本地环境、生产就绪和发布验收不适用，且不得发起；三个既有 REL 发布门不进入执行队列。

## 风险、恢复与零悬空

- Trivy：当前工作树本地扫描已通过；任何后续源码或依赖变化均使该证据失效，Git 交付时仍由同 SHA CI 独立重跑。
- 环境边界：隔离双租户链已完成；后续仅允许本地开发环境验证，禁止规划或发起 prod、生产、目标环境测试/验收；禁止向现有 dev 数据库补写验收数据。
- 回滚：只撤销本主线认证、投标、配置、CI和对应测试改动；若回滚重新引入 P1，继续禁止发布。
- 后续项：计划周期新增1、关闭1、净变化0；本轮收口新增0、关闭1、净变化-1；`ISSUE-072-001`已关闭，三个既有 REL 保持冻结且本地范围不适用，无新增悬空问题。

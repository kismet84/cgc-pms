# 第67条主线：工程投标资料与成本日记账闭环验收报告

> 验收日期：2026-08-04
> 实施基线：`master@1eadfedf4aaa7a3974db59cc66bd24fd43136a76`
> 裁决：`LOCAL_ACCEPTANCE_PASSED / GIT_DELIVERY_IN_PROGRESS / TARGET_RELEASE_NOT_EVALUATED`

## 1. 结论

本地主线通过。工程投标只保留投标记录、投标成本两个菜单；文件版本、投标状态、中标建项、现金流水和角色权限均以服务端事实闭环。未建立审批流、第二套现金账或前端权威余额。

## 2. 关键证据

| 门禁 | 结果 | 证据 |
| --- | --- | --- |
| 数据 | 通过 | V269扩展投标/文件/现金/角色；V271对齐约束；V272补齐项目计划日期；MySQL/H2迁移等义测试通过 |
| 状态 | 通过 | 统一状态CAS；`WON`校验有效中标通知书和项目必填信息；重复请求回读唯一项目 |
| 文件 | 通过 | 版本递增、FINAL不可覆盖、SUPERSEDED/VOID保留历史；下载走通用文件授权 |
| 现金 | 通过 | `cash_journal_entry`显式绑定投标与成本科目；汇总只返回五项投标指标，不泄露账户全局余额 |
| 权限 | 通过 | 仅`MANAGEMENT`/`SUPER_ADMIN`获投标权限模板；普通角色前端直达拒绝、后端403；迁移不自动分配用户角色 |
| 前端 | 通过 | 旧`/bid-cost`重定向；列表、独立详情四页签、成本页、桌面和390px窄视口成立 |

## 3. 自动化与运行态

- 后端相关回归：90项，失败0、错误0。
- 后端全量门禁：`mvnw.cmd verify`共2427项，失败0、错误0、跳过3；JaCoCo分支覆盖率0.601994（覆盖11052、遗漏7307），全部覆盖率门槛满足，`BUILD SUCCESS`。
- 前端相关单测：66项通过；前端全量单测422项通过；`type-check`、契约类型检查、生产构建、87路由台账通过。
- Playwright：`engineering-tender.spec.ts` 1项通过，覆盖管理层正样本、普通角色负样本、桌面/窄视口及严重无障碍问题。
- 首次E2E因复用旧Vite模块图出现空白页，分类为`environment_runtime_stale`；重启既有前端容器后同用例通过，非业务缺陷。
- 真实本地账号：`ui26.mgmt01`可见投标记录、实际记录和投标成本，390px窄视口通过；`ui26.staff01`直达记录页进入403，投标现金接口返回403；浏览器错误0。
- 真实记录返回数值型旧`bidExpense`触发共享金额格式器异常；根因修复为兼容服务端数值/字符串输入，并增加数值回归样例后复验通过。
- 首轮全量门禁暴露迁移版本断言仍停在268及新增原始`SELECT *`违反数据库治理契约，分类为`test_contract`并修复；次轮分支覆盖率为0.599542，分类为`coverage_gate`，通过补充请求归一化分支回归提升覆盖率，未降低门槛。
- 推送前门禁发现V2页面治理、导航域旧断言及旧M4 E2E mock与新投标入口不一致，分类为`test_contract`；页面改用既有H1、业务标签和失败/空态契约，导航及E2E对齐新双菜单、负责人选项和旧路由重定向后全量通过。
- 首次远端push CI中，MySQL基线测试仍断言最高版本268，分类为`test_contract`并对齐272；npm新高危公告命中间接依赖`brace-expansion 5.0.8`，分类为`dependency_vulnerability`，前滚至5.0.9并保留最小模块导出兼容补丁后，高危审计通过。
- 首次远端push CI分支覆盖率为0.599052，分类为`coverage_gate`；补充文件版本首次追加、取代、定稿、作废及非法文件/摘要回归后，本地全量提升至0.601994，门槛未调整。
- 本地数据库操作前已生成可恢复全备份；生产数据库和生产部署未操作。

## 4. 边界与零悬空

- 不做报价组价、外部平台同步、电子签章、自动合同、自动目标成本批准。
- 本地主线通过不等于同SHA CI、Git合并或目标环境发布通过。
- 新增后续项0、关闭后续项0、净变化0；无无载体遗留项。

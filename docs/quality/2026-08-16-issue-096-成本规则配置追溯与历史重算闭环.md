# 第96条主线：成本规则、配置、追溯与历史重算闭环报告

> 日期：2026-08-16
>
> 实施基线：`master@14e2010247e6cdf90afa7835c20690b92af1ba12`
>
> 当前状态：`PASS / G0-G5_PASSED / GIT_DELIVERY_MERGED / POST_MERGE_VERIFIED`
>
> 环境边界：仅本地 dev/test/demo；不构成生产、非本地环境、版本发布或上线裁决
>
> 计划载体：[第96条主线任务计划书](../plans/第96条主线-成本规则配置追溯与历史重算闭环任务计划书.md)

## 1. 结论

第96条主线完成。会计科目目录统一后，`1122-AR` 等总账科目可在会计科目中维护；成本规则、项目配置和成本事实仍只接受 `account_category='COST'` 的启用末级科目。“成本规则方案 / 项目成本配置 / 成本追溯与转入”三模块已完成规则、影响、转入、分摊、待归类、财务覆盖、历史重算、冲销和关闭后调整闭环。

成本事实保持 append-only；付款、现金、凭证不重复计成本。租户、项目、公司财务角色、申请人与审批人分离、会计期间、关项状态、科目时态及金额/来源链守恒均由服务端 fail-close。

## 2. 验证证据

| 验证 | 结果 |
| --- | --- |
| 后端全量 | 372 份 Surefire 报告，2,915 tests，0 failures/errors，28 skipped |
| 前端 | 89 files / 646 tests；type-check、contracts type-check、lint、build 通过 |
| MySQL | fresh 及 V180→V304 upgrade 通过 |
| 迁移与合约 | H2/MySQL 同版、权限、工作流、唯一性、金额与溯源契约通过 |
| E2E | 成本科目 m7 定向 1/1；CI browser contract 通过 |
| 内置浏览器 | 真实 URL/DOM/交互、桌面与 986×732 视口、横向溢出、console error/warn 通过 |
| Codemap | 重生成及 `--verify` 通过 |
| 正式复核 | 独立只读复核最终无未解决 P0/P1 |

首轮 Push CI 的 `backend-test` 在 2,745 项已通过、0 失败时命中 15 分钟工作流硬超时，分类为 `ci_config`，不是业务测试失败。上限最小调整为 25 分钟，并同步 CI 契约与 Codemap；后续 Push CI 和独立 PR CI 均完整通过后才合并。

## 3. 受保护 Git 证据

| 证据 | 结果 |
| --- | --- |
| 实现源 HEAD | `cc80b188045481c4369d429a638b37b968b271d7` |
| Push CI | `31919917524`，16 个 required jobs 全绿；pre-PR verifier `PASS` |
| PR | [#450](https://github.com/kismet84/cgc-pms/pull/450)，Ready，独立 PR CI `31920404988` 全绿 |
| 独立 CI 裁决 | 因 CI 超时契约属控制面改动，PR 正确重跑全量门禁，未绕过 |
| 受保护合并 | squash merge `0ca1cf073eb3c755dffa857f1ad4a1adb3d39fa5` |
| tree 证明 | source/merge tree 均为 `4730a48e94e42b93fb867b7cebb314f3d06a72ca` |
| post-merge | run `31920917576` 成功；verifier `PASS / REUSED_PUSH_CI` |

## 4. 边界与收口

- 已应用 migration 未改写；新结构只使用 V300～V304 的 MySQL/H2 对应版本。
- 本地 demo 库升级前备份已留存；专用 smoke/upgrade 临时库为可丢弃测试资源。
- 计划发现 12 项：本轮修复并复验 12，正式承接 0，证据不足关闭 0。
- 新增后续项 0，关闭后续项 0，后续项净变化 0；无无载体遗留。
- 结论不包含生产部署、Tag、Release 或非本地环境验收。

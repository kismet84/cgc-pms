# ISSUE-086 全仓业务闭环权限、状态与事实一致性整改质量报告

> 日期：2026-08-10
> 状态：`IMPLEMENTED / G0-G4_PASSED / G5_IN_PROGRESS / ISSUE-086-001_OPEN`
> 环境：仅本地 dev/test/demo 与待执行的既有 GitHub Actions
> 编制基线：`master@8b6fca6d95f57cdcad11669846c2edb525e0d068`
> 实现提交：`d95f2236f3a3306c0a31e39bac12fdd9082a825a`

## 裁决

G0～G4 通过，15 项确认缺陷、2 项条件能力和 D-01～D-10 已在本地收口；当前没有代码、数据、测试或运行态阻塞。G5 受保护 Git 交付尚未执行完，`ISSUE-086-001` 继续保持打开，正式结论为“本地实现通过、整体任务进行中”。

本轮没有建设权限中台、通用状态机、通用冲销平台、第二退货表、第二文件库、第二财务台账、消息中间件或非本地交付链。历史缺失关系和迁移前 mandatory 审计事实不伪造回填。

## CL-01～CL-17 收口

| ID | 结果 | 权威处置与证据 |
| --- | --- | --- |
| `CL-01` | 本轮修复并复验 | `ProjectAccessChecker` 增加管理员/ALL 断言；收入确认、收入高级操作和合同变更按租户与可访问项目双重限域，详情及写入先反查项目再授权。权限负例和同租户跨项目用例通过。 |
| `CL-02` | 本轮修复并复验 | 合同新增受控履约完成命令；MAIN、SUB、PURCHASE 只在各自终局事实成立时以锁定和 CAS 进入 `SETTLED`，LEASE/SERVICE 继续失败关闭，关闭门未放宽。 |
| `CL-03` | 本轮修复并复验 | `MatSupplierReturnService` 成为供应商退货唯一写权威；旧寻源写入口失败关闭，旧读入口投影正式退货；确认/撤销统一回写库存或成本、收货、订单、AP 和绩效。V2 页面不再提供孤立旧写入口。 |
| `CL-04` | 本轮修复并复验 | 竣工关闭门使用现有 `PROGRESS + RETENTION` 应收事实，不再要求不存在的 `REGULAR`。人工业主结算必须绑定已审批 `contract_revenue`；专用候选端点服务端固定 `APPROVED + projectId + contractId`，旧收入列表权限未扩大。 |
| `CL-05` | 本轮修复并复验 | 回款创建只形成 `PENDING_EVIDENCE`；实际 CLEAN 银行回单存在后，confirm 事务才核销应收、写现金日记、会计分录和 mandatory 审计。无附件和空分配均零财务副作用。 |
| `CL-06` | 本轮修复并复验 | 销项发票先形成待证据事实；上传 CLEAN 发票后，confirm 锁应收并校验累计额度，原子激活分配和状态；验证后附件按业务对象授权保护。 |
| `CL-07` | 本轮修复并复验 | PREPARING 且无基线时才允许普通 BASELINE；ACTIVE 项目只能走既有受控修订链，复制层级、前置及实际进度，不允许普通基线替换活动事实。 |
| `CL-08` | 本轮修复并复验 | 日报提交重新校验项目、计划、周计划、WBS 与日期四元组；跨周改日期后旧进度失败关闭，避免错误推进 WBS 与快照。 |
| `CL-09` | 本轮修复并复验 | 施工完成和最终关闭门纳入当前图纸、开放 RFI、交底确认、施工引用归档四类权威事实；每类负例独立阻断。 |
| `CL-10` | 本轮修复并复验 | V289 增加命令键和摘要约束；V290 新建不可变 `mandatory_audit_expectation`，先写领域事件再写期望分母且同事务失败回滚；inspect 按精确键反连接、检测缺失、孤儿和摘要漂移。供应商退货及月结/反结账纳入强审计。 |
| `CL-11` | 本轮修复并复验 | 在途供应只读取审批通过且处于 `PERFORMING/PARTIAL_RECEIVED` 的订单，按订单物料剩余量投影；草稿、完成、跨项目和无交期事实排除。 |
| `CL-12` | 本轮修复并复验 | 科目 ETC 读取最新确认预测的 `cost_forecast_item`；项目收入、利润和预测利润不再复制到每个科目，新增科目不会放大项目事实。 |
| `CL-13` | 本轮修复并复验 | 周计划审批按基线与类型锁定作用域，拒绝日期交叠；并发批准只允许一个成功。 |
| `CL-14` | 本轮修复并复验 | 新签证明细必须绑定同项目 ACTIVE WBS，审批生成成本事实时传播 WBS；历史 NULL 保持未知，不伪造映射。 |
| `CL-15` | 有证据关闭 | D-03 确认保持付款后登记 AP 发票；未建设 invoice-first 链。现有付款记录锚点、发票分配、核验和预付款重分类按签认顺序复验通过。 |
| `CL-16` | 有证据关闭 | D-04 确认付款条款仅作合同信息；保留比例合计 100% 与草稿期编辑，不新增付款申请到条款的推导关系。 |
| `CL-17` | 本轮修复并复验 | AR/AP 确认、回款/付款、冲销及 ADVANCE 重分类使用明确会计策略；月结以业务子账独立比较 POSTED 总账科目余额，0.01 差异进入 EXCEPTION 并阻断。 |

## D-01～D-10 裁决落地

| 决策 | 落地结果 |
| --- | --- |
| `D-01` | 人工结算绑定已审批收入确认；计量结算继续绑定已核定计量提交；无来源拒绝。 |
| `D-02` | MAIN、SUB、PURCHASE 按既有终局事实进入 `SETTLED`；LEASE/SERVICE 保持失败关闭。 |
| `D-03` | 保持付款后登记 AP 发票，不建设 invoice-first。 |
| `D-04` | 付款条款只作合同信息，不自动扣减、派生实付或阻断付款。 |
| `D-05` | 质量处罚只进入现有处罚成本，不自动生成合同扣款、应付冲减或索赔。 |
| `D-06` | 仅复验既有逐域撤销命令；回款、付款、会计和退货保留各自期间锁、幂等及职责分离，不建通用框架。 |
| `D-07` | `impactDays` 只保存核定影响；签证 WBS 用于成本追溯，不自动改计划。 |
| `D-08` | 复用技术归档与项目移交签收作为关闭事实，不创建第二竣工目录。 |
| `D-09` | AR：结算确认借记应收、回款贷记冲减；普通 AP：付款申请确认贷记应付、付款借记冲减；ADVANCE 先借记预付，发票核验后重分类。 |
| `D-10` | 金额、库存和关账关键成功转换必须同事务写领域事件与精确期望分母；迁移前历史零回填，异常通过 inspect 和本地备份恢复闭环发现。 |

## 客观证据

| 门禁 | 结果 |
| --- | --- |
| 后端完整回归 | 当前源码 `mvn test`：336 个 suite、2620 项，0 failure、0 error、24 个既有条件性 skip；时间窗 15:43:05～16:00:18。完整打包通过。 |
| 前端完整回归 | 73 个文件、510 项 unit 全过；共享 contracts `tsc`、Vue `vue-tsc`、Vite 298 modules 构建通过；lint 0 error、14 个既有 warning。M6 专项 22/22。 |
| H2 与迁移 | V289～V291 fresh/upgrade、唯一约束、强审计和月结集成通过；H2 baseline 到 291，视图结构可读。 |
| 真实 MySQL | fresh smoke 与旧版升级到 V291 均通过；`v_business_audit_event.security_type=INVOKER`，旧业务事实回读正常。任务专用临时库已删除。 |
| 会计与月结 | AR/AP/ADVANCE 策略专项、付款申请/付款/冲销、回款/冲销和月结闭环测试通过；POSTED 总账 0.01 差异负例阻断。 |
| mandatory 审计 | 事件与 expectation 同事务、精确键、重复回放、冲突回滚、缺失/孤儿/摘要漂移、重复关账精确事件和供应商退货确认/撤销均有测试。 |
| 备份恢复 | `cgc_pms_demo_v2_full_20260810_145405.sql.gz`，298250 bytes，SHA-256 `BB2AA5A19975E14806821AC6E69B35F53359696FD8474AEDE370948F57EA5221`；非拥有者备份成功，隔离库恢复 214 对象、Flyway 291、核心行数与 INVOKER 视图一致，恢复库已删除。 |
| 真实运行态 | 最新 JAR 启动后 health `UP`；`demo.finance` 调专用收入确认候选端点返回 HTTP 200、1 条 `APPROVED` 权威记录。 |
| 真实 V2 页面 | 签证 WBS 候选、供应商退货唯一入口、销项发票与回款必传真实附件完成浏览器验收；稳定请求 200，console warning/error 为 0。 |
| 本地 Live | 干净实现 SHA `d95f2236f3a3306c0a31e39bac12fdd9082a825a`；9/9 spec、80/80，0 skipped、unexpected、flaky，exit 0；数据库 `cgc_pms_demo_v2`、tenant 0。 |
| Live 证据 | `local-evidence/live-e2e/live-evidence-d95f2236f3a3-20260810T080524Z/playwright.json`；本地产物被忽略，不纳入 Git。 |
| 治理 | HighRisk/任务执行策略、README 同步 125 路径、Codemap 生成与 verify、`git diff --cached --check` 全过。 |
| 独立复核 | mandatory cutover、V290/V291、D-09/D-10、金额/租户路径及 CL-04 人工结算来源链完成只读复核；最终无代码阻塞。 |

## 阻塞研判与处置

- `quality_or_security`：原 mandatory 分母使用迁移时间和可变业务时间，存在误判、漏判及重复关账丢分母风险。解决：V290 不可变 expectation，同事务冻结精确命令键和预期摘要，历史零回填。
- `environment_prerequisite`：非拥有者备份被历史 DEFINER 视图阻断。解决：V291 只把审计联合视图改为 `SQL SECURITY INVOKER`，列序与数据契约不变；真实备份恢复通过。
- `quality_or_security`：独立复核发现人工业主结算前端未提交后端强制的 `revenueId`。解决：共享契约必填、专用已审批候选端点、项目合同二次过滤及切换清空；未扩旧列表权限。
- `tool_invocation`：`ContractRevenueControllerTest` 单类运行时本地 profile 缺 `sys_user(1)`，原有管理员用例也统一 401。未修改生产校验或伪造夹具；新增无数据库依赖的权限与固定 `APPROVED` 契约测试，并由完整后端回归复验集成路径。
- `environment_prerequisite`：前端全量 unit 首次与 lint/build 并行时，UI 静态扫描用时 5.214 秒超过固定 5 秒；无断言失败。未抬超时，目标门单跑 19/19、随后串行全量 510/510 通过。
- `tool_invocation`：两个 V291 Maven 外层调用超过 60 秒返回 124，但 Surefire 最终报告分别 8/8 与 12/12、0 failure/error；随后完整 Maven 命令以退出码 0 收口。

## G0～G5

- `G0`：分支、基线、工作区归属、当前数据库异常、22 域调用链及 codemap 冻结完成。
- `G1`：D-01～D-10 签认，15 项确认缺陷、2 项条件能力、负向测试和失败关闭边界冻结完成。
- `G2`：V289～V291 的 H2/MySQL fresh/upgrade、约束、并发、切换分母和备份恢复通过，无历史伪造回填。
- `G3`：CL-01～CL-17 按授权与资金、合同收尾、履约现场、AP/月结/投影四包串行实现并复验。
- `G4`：后端、前端、真实 MySQL、真实运行态、浏览器、备份恢复与干净 SHA Live 全部通过。
- `G5`：提交前治理门通过，实现提交已生成；push、同 SHA CI、Pre-PR verifier、PR、合并、post-merge 和源分支清理待执行。

## 零悬空

- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- 计划全周期问题源当前新增 1、关闭 0、净变化 `+1`；唯一活动载体为 `ISSUE-086-001`，仅等待 G5。
- 无平行 backlog 或无载体遗留；生产、目标环境、远程数据库和版本发布按本地范围规则关闭。

# 第65条主线质量报告：材料字典资料导入与双价展示

日期：2026-08-03

结论：本地实施与验收通过；标准模板、逐行导入、当前含税信息价和最近已审批验收采购价均以服务端事实为准。

## 实施结果

- 新增空白 `.xlsx` 模板下载和限定 `.xlsx` 的 multipart 导入。
- 结构错误整批拒绝；业务行独立事务、部分成功、可重试；确定性编码、分类和冲突建档不做模糊猜测。
- 匹配现有材料时仅更新当前含税信息价及来源元数据，不覆盖其他主数据。
- 采购价由服务端按当前租户批量查询最近已审批验收明细，包含直耗，不写入材料主表。
- 材料字典展示已扩展为确认的 10 列，价格保留服务端十进制字符串。

## 验证证据

| 检查 | 结果 |
| --- | --- |
| 后端点名矩阵 | 95 passed / 0 failed / 0 skipped（含 #63/#65 与 migration） |
| 后端全量 `verify` | 2419 passed / 0 failed / 3 skipped；覆盖率门禁通过 |
| 前端单测 | 61 files / 411 tests passed |
| type-check / contracts / boundary / build | passed |
| `lint:check` | 0 errors；50 条格式建议 |
| Playwright `m7-master-data.spec.ts` | 1 passed |
| 模板真实 HTTP | 200；XLSX MIME、文件名和 ZIP 工作簿结构正确 |
| 应用内浏览器上传 | 空白模板导入结果总计/新建/更新/冲突/跳过/失败均为 0；无业务写入 |
| 响应式/控制台 | 390px 视口无横向溢出；无 warn/error |
| MySQL | V265/V266 成功；当前 tenant 0 材料数仍为 1 |

## 风险与边界

- 本轮未导入武汉专用多工作表原始版式；先整理为系统标准模板是已确认边界。
- 实际非空合法/错误/冲突行未写入用户刚清理的业务库；该链路由后端事务集成测试和 Playwright 覆盖。
- Edge 扩展未开启本地文件 URL 访问，真实上传改用应用内浏览器完成；不影响产品功能。

零悬空统计：本轮关闭 `ISSUE-065-001` 1 项；新增后续项 0，关闭后续项 1，净变化 -1；计划全周期净变化 0。

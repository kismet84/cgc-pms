# ISSUE-040-055 站内通知租户用户隔离与已读幂等回归验收报告

结论：通过；阻塞：无。

## 范围与结果

- 证明站内通知分页、未读计数、单条已读和全部已读同时绑定租户与用户；跨租户或跨用户操作统一返回不可见，不泄露目标通知存在性。
- 单条通知已读后再次执行直接返回，不重复改写已读时间；全部已读只更新当前租户、当前用户的未读记录。
- SSE订阅和推送使用 `tenantId:userId` 复合键，同一用户在不同租户的连接相互隔离；前端通知铃只在打开时加载并建立SSE，已读写入必须由用户明确点击触发。
- 本项没有修改通知代码、测试、数据、权限、SSE超时或运行环境；不宣称邮件、短信、企业微信、钉钉、失败重试队列或外部供应商渠道完成。

## 验收证据

- 后端：`NotificationServiceTest` 18/18通过，0失败、0错误、0跳过；覆盖分页、计数、单条/全部已读、跨租户、跨用户、重复已读、SSE订阅、复合键和创建推送。
- 前端：`NotificationBell.test.ts` 11/11通过；覆盖延迟加载、SSE、列表、单条/全部已读、0未读、布局契约和失败反馈。
- 运行态：backend、frontend与dev-login连续181秒、12组样本均通过。
- 真实浏览器：开发演示超级管理员进入驾驶舱，只打开通知中心看到“暂无通知”后关闭；未点击通知项或“全部标为已读”，控制台新增error/warn为0，无业务写入。
- Ready lint、允许路径、JSON解析与`git diff --check`：通过。

## 失败分类与恢复

- 自动化、稳定门和浏览器验收首轮通过；无工具配置、环境前置或真实质量/安全失败。

## Reviewer复核

- 结论：PASS；findings：无。
- 查询、计数和全部已读均显式包含租户与用户条件；单条已读同时核对实体租户与用户，跨边界使用同一不可见错误。
- 重复已读在已读状态分支直接返回；SSE复合键包含租户，创建推送按通知租户定位连接。
- 浏览器验收只触发通知列表/未读只读加载，没有触发 `PUT /read` 或 `PUT /read-all`；能力表述严格限定为站内渠道。

## 治理收口

- `OBS-NOTIFICATION-INAPP-ISOLATION` 已从唯一问题载体移除。
- 新增后续项：0
- 关闭后续项：1
- 后续项净变化：-1
- 最小回滚：仅回退本项治理文档与报告。

剩余风险：无；邮件、短信、企业微信、钉钉、失败重试队列和外部供应商渠道继续保留在A-07聚合父项，不属于本项新增后续问题。

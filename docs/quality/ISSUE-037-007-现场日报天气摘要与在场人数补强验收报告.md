# ISSUE-037-007 现场日报天气摘要与在场人数补强验收报告

## 结论

- Decision：通过。
- 阻塞：无。
- 上线边界：仅通过本地 dev/test 验收；未发布生产、未连接生产数据库、未 push。
- 完成范围：在现有日报草稿/提交闭环内增加人工天气摘要和可空在场人数。

## 实施与边界

- V143 MySQL/H2 新增 `weather_summary VARCHAR(200) NULL`、`on_site_headcount INT NULL`，无默认值，旧数据保持未知。
- 人数 NULL 表示未填写，0 表示明确无人；仅接受 0–100000 的整数，小数由严格 Jackson 反序列化拒绝。
- 新字段加入既有 `tenant_id + id + status=DRAFT` 原子更新；SUBMITTED 后继续不可修改。
- 项目/租户范围、`site:daily:query/edit`、附件与提交状态机均未放宽。
- 未实现天气 API、自动采集、人员名单/班组、考勤工时、设备材料、移动离线或统计驾驶舱。

## 验收证据

- Ready lint、独立准入和最终实现复核均 PASS。
- 后端：`SiteDailyLogControllerTest,SiteDailyLogServiceTest` 共 6/6，通过 NULL/0、字段映射、天气长度、人数负数/上限/小数和提交后拒写。
- 前端：`daily-log.test.ts` 2/2；`pnpm type-check` 通过；查看态锁定 NULL 显示“未填写”、0 显示“0”。
- 静态：`git diff --check` 通过。
- 运行态：重建后稳定等待 180 秒，backend/frontend 200，dev-login 会话可用。
- 真实 API：本地 dev 项目创建日报时人数省略并读取为 NULL；更新为天气“晴，有风”和人数 0 后正确回读；提交后更新返回 400。
- 测试数据：满足 dev、127.0.0.1 和 `ALLOW_TEST_DATA_RESET` 三项边界后按 ID 清理 1 行，无残留。

## 风险与回滚

- 非阻塞：`redirect=/site/daily-log` 尚未列入 dev-login 跳转白名单，会安全回落 `/`；不影响现行业务 API和 `/dashboard` health gate。
- 回滚：回退应用后移除 V143 两列，不改写 V141，不涉及附件或状态数据迁移。

# ISSUE-053-025：M5供应商招采与履约V2验收报告

## 1. 裁决

结论：通过。`/supplier-sourcing`已由真实Clean-room V2页面承接并达到`V2_ACCEPTED`；路由台账由`50/37/0`更新为`49/38/0`。

阻塞项：0。非阻塞遗留：0。未授权且未执行commit、push、PR、正式入口切换、Legacy退役、生产发布或生产数据操作。

## 2. 范围与实现

- 共享契约新增供应商招采七组权限、事件/邀请/报价/评审/履约/退货/黑名单/trace DTO与命令。
- V2服务覆盖现有`SupplierSourcingController`全部查询与动作端点；ID统一编码，查询支持AbortSignal，金额与评分保持十进制字符串。
- 新页面覆盖事件、邀请、发布、拒绝、报价、提交、开始评审、评分、定标、关联合同、履约评价、退货、黑名单提交/审核及全链trace。
- query、maintain、quote、evaluate、award、performance、blacklist review不互相兜底；按钮只按对应权限和服务端状态出现，写后重新加载事件与trace。
- 对四个既有供应商实体的金额/评分`BigDecimal`增加字段级字符串序列化，补齐共享契约根边界；未改公式、状态机、数据库migration、Legacy、依赖、请求核心或其他M5路由。

## 3. 关键证据

| 门禁 | 结果 |
| --- | --- |
| Ready lint | 通过；hash `501db5384926bcf2e8c295b23f0f10e1d42d0ddf9ec54ff0ca03d83836c0215e` |
| 后端专项 | `SupplierSourcingClosedLoopIntegrationTest`：5项通过；Surefire 5/0/0/0；`BUILD SUCCESS`；覆盖招采闭环、失败边界及金额/评分字符串序列化 |
| V2单测 | 38文件、277项全部通过 |
| 契约/V2类型 | 通过 |
| Lint / Clean-room边界 | 0 error；146个V2文件、14个契约文件通过 |
| 路由台账 | 87命名路由；`LEGACY_ONLY=49 / V2_ACCEPTED=38 / V2_SOURCE_AVAILABLE=0` |
| 构建 | Vite生产构建通过；供应商页面独立chunk生成 |
| 目标E2E | 9/9通过：无query权限403零业务流量、六组普通动作权限正负样本、三视口/长名称/axe、招采至合同写侧及重复点击去重 |
| 差异静态检查 | `git diff --check`通过 |

Playwright使用全新本地Vite进程`http://127.0.0.1:5175`，避免环境中5174端口的WSL转发继续复用旧路由模块。该进程只承载mock API验收，不连接生产或写入真实业务数据。

## 4. 风险复核

- 租户/项目、状态、重复提交与非法动作由既有后端专项继续裁决；本Issue仅根修JSON字段序列化，未重写后端状态机。
- 页面loading、empty、error、权限403、重复点击、Abort和陈旧响应隔离成立；API错误保留403/404/409/422/500状态和业务消息。
- 三视口为1440×900、1024×768、390×844；均无页面级横向溢出，axe serious/critical为0。
- 评分公式、定标例外理由、合同/供应商一致性、退货/评价/黑名单状态均来自既有后端，不在前端复制。

## 5. 失败分类与处置

- 初次设计系统回归发现未知令牌、原生datetime输入、业务编码复合单元格：分类`product`，本轮改用既有令牌/公共输入并拆分币种列，复验通过。
- 初次E2E命中5174旧路由模块：分类`environment`，改用全新5175进程复验；未修改或终止用户现有5174转发。
- 其余失败：无。

## 6. 收口

- 本轮修复并复验：供应商招采V2闭环、金额/评分字符串契约、路由台账、设计门和目标测试。
- 超出范围并正式承接：`ISSUE-053-026`已补为唯一Ready，承接采购申请、订单与验收四路由；未自动启动。
- 证据不足或无明确价值而关闭：无。
- 新增后续项：0；关闭后续项：0；后续项净变化：0；无载体遗留项：0。

最小回滚：回退本Issue供应商契约/服务扩展、V2页面、路由、台账、测试及治理回写；024、Legacy、数据库和业务数据保持不变。

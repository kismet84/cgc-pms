# ISSUE-053-020 M4目标成本版本V2验收报告

## 裁决

- 结论：通过。
- 阻塞项：0。
- 上线结论：仅证明本地dev/test切片验收通过；不代表正式入口切换、Legacy退役或生产发布。
- 路由台账：`LEGACY_ONLY=56 / V2_ACCEPTED=31 / V2_SOURCE_AVAILABLE=0`。

## 范围与结果

- `/cost-target`确定性重定向`/cost-target/index`，保留query/hash；列表、创建、编辑三工作区由同一Clean-room V2页面承接。
- 版本创建、编辑、明细保存、提交、驳回后同实例重提、激活和删除闭环成立；已提交/活动历史保持只读。
- 合计金额、投标成本、责任成本和目标成本以服务端十进制字符串返回；前端不使用JavaScript浮点形成权威金额。
- update、items、submit、delete、activate强制客户端version/CAS；同项目激活使用项目行锁，最终仅一个活动版本。
- 通用工作流`COST_TARGET`提交/重提在写入前失败关闭；专用入口负责version、审批实例和状态原子口径。
- V220登记`add/edit/delete/activate`并复用既有`query/submit`；普通角色正向、只读403、项目外403通过。

## 验证证据

- Ready lint：通过，Ready契约字段、范围、风险与回滚完整。
- 后端目标测试：`CostTargetControllerTest`、`CostTargetWorkflowHandlerTest`、`TargetCostDynamicProfitClosedLoopIntegrationTest`、`CostTargetServiceConcurrencyTest`、`WorkflowSubmitServiceTest`共43项，0失败。
- 前端：31个测试文件195项通过；契约类型、V2类型、ESLint、Clean-room边界、路由台账生成/校验及构建通过。
- Playwright：目标成本3项通过；覆盖三视口、根重定向、无占位、axe serious/critical为0、只读/拒绝身份及激活409权威回刷。
- demo verify：通过；项目经理与成本经理目标成本六项权限均完整。
- `git diff --check`：通过。

## 独立复核与补修

- 安全复核发现通用工作流绕过专用CAS、真实驳回重提状态口径两项阻塞；已改为通用入口失败关闭、专用同实例重提，并以真实集成测试复验关闭。
- 测试复核发现台账V2事实校验、失败态矩阵及权限/激活证据不足；已补V2 router/catalog交叉校验、500/404/422/409、Abort/陈旧响应、普通权限/只读拒绝/项目越权及唯一激活测试并复验关闭。

## 边界与收口

- 未迁移M7成本科目中心，未复制Legacy UI，未切正式入口，未连接生产。
- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- 悬空项：0。
- 最小回滚：回退目标成本V2页面/路由、共享契约增量、精确后端CAS/工作流根修、V220、测试及四条台账状态；017～019、Legacy和业务数据保持不变。

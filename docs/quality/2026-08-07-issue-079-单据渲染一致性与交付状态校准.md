# ISSUE-079 单据渲染一致性与交付状态校准质量报告

> 日期：2026-08-07
> 状态：`IMPLEMENTED / G0-G5_PASSED / GIT_DELIVERY_MERGED / POST_MERGE_VERIFIED`
> 环境：仅本地 dev/test/demo，tenant `0`
> 基线：`master@c24dc4646c141d7ec97711ae0840e6ef2c92f31b`

## 裁决

通过。G0～G5、同 SHA push CI、独立 PR CI、PR 合并与 post-merge 验真均通过。

## 变更与边界

- `DocumentCanvasCompiler` 将动态表格按 `yMm` 排序并编译为流式 spacer、`min-height` 和分页表头；拒绝表格占位重叠及同水平范围内的 BODY 冲突。
- 前端画布复用同一碰撞规则，表格展示真实列，限制会制造固定纵向承诺的批量排版操作。
- 模板主页面只经现有版本预览 API 取得服务端渲染 HTML；保留权限、竞态、空态和错误态隔离，不回退模板源码。
- 第77条现行载体校准为 PR #399 已合并、merge SHA `6caaf895d7996b48b778a503f81ec8031a39ea90`、post-merge run `31086766657` 成功。
- 未新增 schema、migration、依赖、渲染器或前端模板引擎；历史版本未原地改写。

## 客观证据

| 门禁 | 结果 |
| --- | --- |
| 后端定向 | `DocumentCanvasCompilerTest` 等4类共18测试通过 |
| 后端全量 | `mvn verify`：333测试类、2577测试，0失败、0错误、18跳过 |
| 前端定向 | 2文件、20测试通过 |
| 前端全量 | 72文件、499测试通过 |
| 前端静态 | lint 0错误；类型检查、契约检查、生产构建通过 |
| PDF | 0、3、60行；重复表头；两表乱序；末行先于第二表；碰撞拒绝通过 |
| 浏览器 | 服务端HTML无占位符；390/768/1440无横向溢出；console error 0 |
| 数据 | V1=`DISABLED`/非默认；V2=`PUBLISHED`/默认；V2含流式 spacer 与 `min-height` |
| 治理 | SQL注入扫描、HighRisk主线门、7项任务策略套件、代码地图、JSON解析、`git diff --check` 通过 |

## 失败分类

- 错用不存在的数据库和 PowerShell 5 解析 UTF-8：`tool_invocation`/`environment_prerequisite`；切换实际 dev 数据库与 `pwsh` 后通过。
- 测试泛型推断导致编译失败：`quality_or_security`；显式空列表泛型后定向与全量复验通过。
- Trivy DB 远端镜像下载低速，22%时剩余估算超过工具等待窗口：`environment_prerequisite`；停止本任务容器，交同 SHA CI 独立复验。

## 运行态与恢复

- 当前任务源码已挂载并刷新 backend/frontend；health 与页面均为 200。
- `SUB_MEASURE_CANVAS_078` 通过既有“新建 V2→保存→发布→默认绑定”完成替换；V1 保留审计历史。
- 回滚条件：若同 SHA CI、PR CI 或 post-merge 任一失败，保持 Issue 打开；业务回滚可把默认绑定切回历史版本，不删除 V2、不修改 migration。

## 零悬空

- 新增后续项：0。
- 关闭后续项：1（`ISSUE-079-001`）。
- 后续项净变化：-1；计划全周期累计净变化0。
- PR #404 已合并，merge SHA `6cb8cedabe17bce1e22593aad89cabf4ba66e828`；post-merge run `31153245541` 成功且绑定同 SHA。
- 实现源分支 `codex/mainline-79-document-render` 已删除本地与远端引用；无无载体遗留项。

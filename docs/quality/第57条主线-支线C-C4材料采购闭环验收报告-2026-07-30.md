# 第57条主线支线C：C4材料采购闭环验收报告

日期：2026-07-30
范围：全新项目 `XM-20260728-008`（西湖科创孵化基地幕墙工程）材料采购闭环
结论：**通过。** 采购申请、寻源、采购合同、采购订单、材料验收、库存、质量整改、付款、支付回写和进项发票均由真实 V2 页面写入，并完成服务端回读。

## 1. 业务事实

| 环节 | 服务端事实 |
| --- | --- |
| 采购合同 | `CT-20260729-005`，`APPROVED / PERFORMING`，合同额 `500000.00` |
| 采购申请 | `PR-20260729-001`，`APPROVED / CONVERTED`，金额 `420000.00` |
| 寻源 | `SRC-20260729-001`，目标供应商报价 `420000.00`，中选 |
| 采购订单 | `PO-20260729-001`，`APPROVED / COMPLETED`，金额 `420000.00` |
| 材料验收 | `MR-20260729-001`，`APPROVED / QUALIFIED`，100 吨，金额 `420000.00` |
| 库存 | 项目仓库可用量 100 吨，库存价值 `420000.00`，平均成本 `4200.00` |
| 质量闭环 | `QPL-20260729-001` 已完成；问题 `QIN-20260729-001-ISS-001` 经独立复检关闭 |
| 付款申请 | `PAY-20260729-004`，`APPROVED / PAID`，批准及实付均为 `420000.00` |
| 支付记录 | `PMT-20260729-005`，`SUCCESS`，外部流水 `C4-BANK-20260730-001` |
| 进项发票 | `C4-VAT-20260730-001`，`VERIFIED`，发票及分摊金额均为 `420000.00` |

## 2. 付款完整性回读

- 付款来源：`DIRECT`，`sourceRefId` 等于付款申请 `2082546815243821058`。
- 付款依据：`MAT_RECEIPT`，绑定验收明细 `2082541270243909633`。
- 来源金额、依据金额、申请金额、批准金额、实付金额、发票分摊金额均为服务端字符串 `420000.00`。
- 审批链：项目经理 `ui26.pm01` → 部门经理 `ui26.mgmt01` → 总经理 `ui26.gm02`。
- 发票绑定支付记录 `2082547949572366337`，绑定付款申请 `2082546815243821058`。

## 3. 本轮前端缺口修复

当前 HEAD 后端已支持 `DIRECT` 来源及 `MAT_RECEIPT` 付款依据，但 V2 页面未承接。最小修复：

- 复用现有 `/pay-applications/{id}/sources/batch` 与 `/basis/batch`。
- 复用材料验收列表和验收明细接口，不新增后端接口。
- 仅有 `payment:direct` 权限时显示直接付款。
- 材料费用直接付款必须选择同项目、合同、供应商且服务端状态为已审批、合格的验收明细。
- 金额保持字符串提交；合同、金额上限、状态和权限继续由服务端校验。
- 修复共享 `V2Select`：下拉按弹窗滚动区计算向上/向下位置，展开时不再重置弹窗滚动位置。

未修改后端、数据库、Flyway、权限模型和业务状态机。

## 4. 验证

- `pnpm type-check`：通过。
- `pnpm vitest run tests/unit/m6-finance-workspace.test.ts tests/unit/design-system.test.ts`：68 项通过。
- `pnpm build`：通过，271 个模块完成转换。
- 真实 V2 浏览器：
  - 材料付款表单可选择 `DIRECT` 和 `MR-20260729-001` 验收明细；
  - 弹窗下拉无遮挡且不重置滚动位置；
  - 付款申请创建、三节点审批、付款回写、发票创建和验真均成功；
  - 页面刷新后显示服务端最新状态。

## 5. 收口

- 本轮修复并复验：V2 材料付款来源、验收依据、弹窗下拉滚动重置。
- 超出范围并正式承接：无。
- 证据不足或无明确价值而关闭：无。
- 新增后续项：0。
- 关闭后续项：1（C4 材料采购全链闭环）。
- 后续项净变化：-1。

# 第55条主线 M2：待确认维护项裁决验收报告

报告日期：2026-07-26

基线：`master@6b134b423a3f3f770b8a4e45cb1456ce3484ea91`

结论：`本地裁决通过 / 生产就绪不通过`

## 1. 范围与结论

M2对四项待确认维护问题完成事实裁决。`CODE-001`、`CODE-002` 关闭；`AUDIT-PROMETHEUS-SCRAPE-AUTH/OBS-002`、`DEV-001` 沿用原稳定键正式承接。新增后续项0、关闭后续项2、净变化-2、悬空0。

| 稳定键 | 四态归类 | 正式状态 | 依据 |
| --- | --- | --- | --- |
| `AUDIT-PROMETHEUS-SCRAPE-AUTH` / `OBS-002` | 超出范围并正式承接 | `NEEDS_CONFIRMATION` | 当前端点匿名拒绝、管理员JWT可访问，但Prometheus无机器凭据；缺目标网络、Secret载体、轮换责任人与运行证据 |
| `CODE-001` | 证据不足或无明确价值而关闭 | `VerifiedResolved` | 查询拆分只令原服务15个构造依赖降至13个，却新增需6个依赖的查询服务和Controller双注入；无运行时、回归或金额安全收益 |
| `CODE-002` | 本轮修复并复验 | `VerifiedResolved` | 当前Spring Test已提供稳定 `@MockitoBean`；OpenHTMLtoPDF `useFastMode()` 在当前版本无行为，删除后仍使用默认fast-mode |
| `DEV-001` | 超出范围并正式承接 | `NEEDS_CONFIRMATION` | `ssl.cnf` 被dev、默认、prod三套Compose真实挂载；静态证据不能证明与MySQL 8自动TLS等价，未获运行环境授权不得删除 |

## 2. 最小实施

- 三个集成测试把 `org.springframework.boot.test.mock.mockito.MockBean` 等价迁移为 `org.springframework.test.context.bean.override.mockito.MockitoBean`。
- 删除生产renderer和renderer spike测试各一处无行为 `builder.useFastMode()`；未升级依赖、未改渲染安全控制或PDF行为。
- `PayApplicationService`、MySQL配置、Prometheus认证均未修改。

## 3. 剩余项精确门槛

### OBS-002

未来优先采用Prometheus原生Basic机器身份和Secret文件，不复用15分钟用户JWT，不匿名开放。启动前必须明确目标网络、Secret保管与轮换责任人；验收至少包括无/错凭据401、机器凭据200、普通用户JWT拒绝、连续抓取成功、旧凭据失效及Prometheus UI非公网监听。

### DEV-001

使用固定镜像digest的一次性、无项目数据卷MySQL 8对照：有/无 `ssl.cnf` 分别核对 `ssl_%`，验证TLS REQUIRED成功和 `require_secure_transport=ON` 下DISABLED失败。结果等价才允许删除文件与三处挂载。

## 4. 既有载体映射

后端显式deprecation编译另定位到 `PayRecordService` 的旧 `CashJournalService.createPendingFromPayRecord(record)` 调用。该调用属于资金支出闭环 `PPCL-03` / S2 的“PayRecord只生成唯一待归档日记、归档时再消费预算”路径，沿用 `PROJECT-PAYMENT-CLOSED-LOOP` 承接，不复制新Issue、不在M2越序修改付款主链。

## 5. 验证证据

- 定向6类：28项通过，0失败、0错误、0跳过，`BUILD SUCCESS`。
- 后端全量：273类、2273项、0失败、0错误、3跳过，`BUILD SUCCESS`；instruction `80.64%`、branch `60.25%`。
- 删除测试侧 `useFastMode()` 后，renderer spike 2项通过；日志仍明确使用fast-mode。
- `docker compose -f deploy/docker-compose.monitoring.yml config --quiet`：通过。
- `git diff --check`：通过。
- 首次wrapper工作目录和一次Maven `-D` 参数引用错误均分类为 `tool_invocation`；修正调用后复验通过，未进入业务测试失败。

## 6. 风险、回滚与发布裁决

- CODE-002仅等价测试注解迁移和无行为调用删除；回滚为恢复5个文件对应行。
- OBS-002、DEV-001因缺目标/运行证据继续失败关闭；禁止把本地静态裁决表述为认证抓取或TLS运行成功。
- 3项P0生产发布门与资金支出闭环P0均未解除。未commit、push、PR、切换正式入口、退役Legacy或操作生产。
- 正式裁决：M2本地通过；生产就绪不通过，禁止上线。

# 第99条主线：每日审计并发一致性、供应链扫描与依赖安全整改收口报告

## 裁决

- G0～G5 本地门禁：通过。
- `ISSUE-099-001`：关闭；五项确认问题已修复并复验，一项行数型结构误报按既有架构证据关闭。
- 当前开放 P0/P1/P2/P3：0/0/0/0。
- Git、PR、合并、发布：未授权、未执行；当前工作树证据基线为 `master@6b8be7575b1bcc640a910d1c22527943571cfaa7`。
- 生产/目标环境：不存在且不适用；本报告只裁决本地 dev/test/demo。

## 实施

1. 付款草稿 PUT 改用白名单 `PayApplicationUpdateRequest`，端到端携带 `expectedVersion`；数据库 CAS 必须影响 1 行，否则稳定返回 HTTP 409 `PAY_APP_STATUS_CONFLICT`。
2. 既有付款草稿的申请头与付款来源进入同一个服务事务；审计字段、状态、租户和版本由服务端维护，前端不再执行第二次来源 POST。
3. 前端读取并回传版本；409 时关闭编辑框、刷新权威数据且只显示一次专用冲突提示。
4. 定时复扫与完整 CI 统一调用 `scripts/ci/scan-backend-artifact.sh`；入口绑定当前 Git SHA、精确 JAR、运行库计数、版本化依赖计数和 HIGH/CRITICAL 失败关闭。
5. 系统模板安装返回四态：新建绑定、升级系统默认、保持自定义默认、系统默认未变化；页面提示与真实结果一致。
6. Spring Boot 保持 3.5.16，仅窄覆盖 Tomcat 10.1.59；MinIO prod 注释改为与实际持久卷一致。付款服务不做机械拆分，只删除失真 TODO。

## 验证

- 后端：Windows 分片 `2947/2947`，Linux 隔离 `OfficePreviewClientTest 3/3`；合计 2950 tests，0 failure/error，29 skipped。Windows 单项隔离用于绕过已复现的本机 Java 回环 `SocketException: Invalid argument: connect`，不是跳过产品失败。
- 相关后端：付款服务/异常处理 40、模板集成 6、付款 Controller 7、付款闭环 1；真实 MySQL 并发证明两个相同 `expectedVersion` 请求恰好一胜一冲突，最终数据只保留胜者。
- 前端：89 files、649 tests；最后冲突提示和模板四态聚焦 36 tests；lint、type-check、build 通过。
- 浏览器：真实 `http://localhost:5173/payment/application` 产生旧版本 PUT；服务端返回 409，页面显示一次“数据已变化”，通用后端错误提示计数 0，关闭弹窗后重开读到外部更新后的最新事由。
- 本地运行：Vite 入口 200；经真实前端代理的 backend health 为 `UP/200`；文件 CLEAN、SSE connected、付款读取和模板重复安装通过。宿主直接访问 8080 偶发空 500，但容器内和前端代理均为 200，按 Windows 回环环境异常归类。
- 模板：重复安装返回“系统模板已是最新；系统默认未变化”；四态集成和页面映射均通过。
- 最终 JAR：SHA-256 `b20c9935120c075eb8df2a59223770db2df5fad4ef50dffac0b3f9fd9021bf0f`，162 个 `BOOT-INF/lib`；`tomcat-embed-core/el/websocket` 均为 10.1.59，未含 10.1.55。
- SBOM/扫描：Syft 对最终 JAR 解包事实生成 SPDX，识别 201 个有版本包、200 个 Maven PURL；固定 Trivy 0.65.0 使用本地漏洞库扫描该精确 SPDX，HIGH 0、CRITICAL 0。制品扫描脚本的缺 JAR、零库、SHA 不符和正常路径契约均通过。
- 扫描环境说明：Trivy Java DB 从镜像源下载在 86.79% 处 `unexpected EOF`，改用官方 GHCR 后在 12% 处收到 HTTP/2 `PROTOCOL_ERROR`；两次均归类 `environment_or_infrastructure`。未来 Git 交付仍必须由在线 CI 对同一 SHA 执行权威制品入口，本轮未授权 Git，不能用本地缓存证据冒充同 SHA CI。
- 治理：workflow contract、制品扫描契约、Codemap self-test/verify、JSON、文档引用和 `git diff --check` 纳入最终门禁。

## 风险与回滚

- 并发契约若回滚，必须同时回滚前端版本传递、PUT DTO、服务 CAS 和来源聚合，禁止恢复“申请头成功、来源失败”或 0 行假成功。
- Tomcat 覆盖若出现兼容问题，可回退版本覆盖；回退后 F06 必须重新打开，不能通过忽略扫描收口。
- 供应链脚本结构可回退，但不得恢复“源码目录 0 漏洞”等于运行制品安全的旧语义。
- 本地金丝雀 `PAY-20260828-001` 及其来源、依据、审计记录已精确删除，四表复核计数均为 0。

## 零悬空

- 本轮修复并复验：付款并发/原子性、制品扫描、模板语义、MinIO 注释、Tomcat 版本，共 5 项。
- 证据不足或无明确价值而关闭：付款服务仅因行数触发的机械拆分建议，共 1 项。
- 超出范围并正式承接：0；无载体遗留：0。
- 本次实施新增后续项 0、关闭 `ISSUE-099-001` 1、净变化 `-1`；第99条完整计划周期新增 1、关闭 1、净变化 `0`。

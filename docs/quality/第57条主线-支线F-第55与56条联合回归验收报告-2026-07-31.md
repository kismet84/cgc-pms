# 第57条主线支线F：第55与56条联合回归验收报告

日期：2026-07-31
结论：**本地联合回归通过；本次候选已纳入容器安全修复，正式同SHA证据待远端门禁。**
状态：`LOCAL_REGRESSION_ACCEPTED / FINAL_CANDIDATE_SAME_HEAD_CI_PENDING`

## 1. 验收范围

- 第55条非生产事项唯一裁决和三项生产门守恒。
- 第56条9类角色、27个目标用户、15个项目及跨域业务数据。
- 后端、MySQL/H2、Legacy、V2、E2E、安全供应链和本地回滚。
- 首个候选`codex/mainline-57-v1.5-closeout-20260731@171ff6890e2cbdee833ce438996f3f2d37090473`已完成push CI 13/13；随后候选容器安全审查发现并本地修复非root运行缺口，因此该SHA及其CI只保留为被取代证据，不作为最终候选。

## 2. 自动化结果

| 域 | 结果 |
| --- | --- |
| 后端`clean verify` | 2352项，失败0、错误0、跳过3；JaCoCo门通过；JAR生成 |
| 后端顺序独立 | 178项，失败0、错误0、跳过0 |
| MySQL空库 | `FlywayMySqlSmokeTest`、`BaselineMySqlSmokeTest`共2项通过；V250、197张业务表、12角色、无业务事实、安全bootstrap成立 |
| MySQL升级 | V180→V250，248个迁移校验成功，1项通过 |
| H2 | 后端全量迁移、约束及升级测试通过 |
| 备份恢复 | 200张表、15项目、349工作流实例恢复守恒 |
| Legacy | lint、类型检查、133文件743项单测、生产构建4642模块、7项真实页面E2E全部通过 |
| V2单元 | 55文件432项通过 |
| V2 Design System | 3文件81项通过 |
| V2静态门 | lint 0错误、类型、共享契约、Clean-room、路由台账、生产构建、58个JS制品包体均通过 |
| V2全量E2E | 97通过、80个契约跳过、失败0、flaky 0 |
| CI契约 | 13个required job、10次artifact上传、3次下载、2个权限块，静态契约通过 |

V2路由终态：`LEGACY_ONLY=0 / V2_ACCEPTED=87 / V2_SOURCE_AVAILABLE=0`。

## 3. 页面与可访问性

- 浏览器实测投标成本操作菜单四项字体均为`12px`。
- 投标成本及供应商招采表格首行实高`40px`，所有单元格`vertical-align: middle`。
- V2全局表格契约已覆盖：单行40px、两行52px、首列编号可进入详情、操作列统一“更多”菜单、文件选择按钮统一样式、下拉选择即确认；供应商评价/退货改为单列，标题与KPI卡片分离。
- V2全量E2E覆盖三视口、键盘、焦点和axe；浏览器控制台错误0、警告0。
- Legacy和V2本地入口HTTP 200，后端健康检查HTTP 200。

## 4. 安全与供应链

| 检查 | 结果 |
| --- | --- |
| 后端源码依赖Trivy | High/Critical 0 |
| 后端JAR Trivy rootfs | High/Critical 0 |
| Legacy依赖审计 | 已知漏洞0 |
| V2依赖审计 | 已知漏洞0 |
| Secret扫描 | 应用运行源码、CI、脚本、部署与工具范围命中0；全仓宽扫因超时且未生成报告，结论为`unknown` |
| SQL安全扫描 | 通过 |
| Docker/Compose配置 | Trivy High/Critical 0；两套运行镜像均为UID 101，Legacy HTTP/HTTPS/重定向和V2 `/healthz`实测通过 |
| Docker运行镜像 | Legacy、V2 High/Critical均为0 |
| 候选SBOM | Syft 1.50.0本地预生成：后端159包、Legacy 1包、V2 1包；`171ff689...`同SHA CI已生成SBOM/attestation，但因后续安全补丁而失效 |

本地JAR SHA-256：`02EBB810E917E58C1CA323AEA897537C61B0D02158D6D663CC3DB2333D4BF984`。

## 5. 第56条数据与第55条裁决

- 第56条：9类角色、27个目标单角色用户、15项目；旧ID、跨租户、跨项目、跨链错误和禁词均为0。
- 数量、日期、合同、预算、成本、采购、库存、分包、计量、结算、付款、发票、应收、现金、权限、审批、文件和审计已由D4正式报告完成服务端对账。
- 第55条：`A-02/A-03/A-04/A-09/A-07/A-10`保持`FROZEN_WITH_EVIDENCE`，`A-06`为`VERIFIED_RESOLVED`，AutoPilot为`OBSERVATION_WITH_NEXT_CYCLE`。
- `REL-CREDENTIAL-ROTATION`、`REL-FILE-RESCAN`、`REL-TARGET-SHA-REVALIDATION`继续`RELEASE_GATE / PRODUCTION_BLOCKED`。

## 6. 本地回滚

`V2_ROOT → LEGACY_ROLLBACK → V2_RESTORE`三阶段通过；API均为`UP`，SSE为`text/event-stream`，浏览器深链接3阶段均通过，演练容器已清理。未切正式入口，未退役Legacy。

## 7. 失败分类与缺陷终态

- 并行Maven清理`target`导致的首次顺序测试失败：`environment_prerequisite`；隔离串行复验通过。
- 首次MySQL冒烟缺少256位测试JWT：`environment_prerequisite`；补充测试密钥后通过。
- 本机`bash`缺少WSL`/bin/bash`：`tool_config`；使用脚本内等价Docker命令通过。
- Legacy首次E2E使用静态密码登录失败：`environment_prerequisite`；切换既有本地dev-login后7/7通过。
- 首次V2全量E2E受并行Playwright共享目录和资源竞争影响：`environment_prerequisite`；顺序专项及最终全量97/97通过。
- 全仓Secret宽扫耗时约11分钟后退出1且未生成报告：`unknown`；改用应用运行源码、CI、脚本、部署与工具范围扫描后命中0，不把宽扫失败伪写为全仓通过。
- 首个候选容器仍以root运行且旧Nginx基线存在11个High：`quality_or_security`；两套Dockerfile已本地升级到固定摘要的Nginx 1.30并切换UID 101，配置、镜像漏洞和真实运行均复验通过，待纳入最终Git候选。

终态：P0=0，P1=0，未分类失败=0，重复=0，悬空=0。

## 8. 裁决

支线F本地技术门通过。`171ff689...`push CI运行`30602075071`的13项required job全部成功，但已被本次容器安全补丁候选取代；候选SHA、SBOM、制品绑定和同SHA CI由支线G在最终候选push后重新生成。

新增后续项0，关闭后续项0，后续项净变化0。

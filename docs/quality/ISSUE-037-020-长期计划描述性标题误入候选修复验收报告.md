# ISSUE-037-020 长期计划描述性标题误入候选修复验收报告

## 结论

- 实施阶段：通过。
- 最终验收：通过；独立 Reviewer 已复核长期计划章节边界及既有决策顺序。
- 阻塞：非阻塞。

## 依据

- 长期计划候选提取仅接纳第 7 章及以后标题，`2.1 当前技术栈` 不再进入 fresh Planner。
- 回归用例证明 `long-term:8.1` 仍可作为合法开发计划 Candidate，并保留 Ad-hoc、已有 Ready、stop、pause 与当前 focus blocked 的既有决策覆盖。
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/codex-autopilot/test-refill.ps1`：通过，输出 `refill self-test passed`。
- `git diff --check`：通过。

## 边界与风险

- 修改仅涉及长期计划候选提取、现有 refill 自测和本报告；未新增依赖或解析器。
- 业务剩余风险：无；无需新增 backlog 或 Current Focus 项。
- 独立复核：PASS；第 2–6 章均为描述性内容，第 7–9 章均为当前真实开发计划，Ad-hoc/Ready/stop/pause/blocked-first 未回退。
- 非阻塞风险：回归抽样 `8.1`，同一整数边界已覆盖 7–9；未来长期计划章节结构变化时再调整，不提前扩建解析器。

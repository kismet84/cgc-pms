# 第54条主线 M0至M5本地治理收口验收报告

## 1. 总体裁决

**本地治理收口通过；修复及发布收口未完成；不可上线。**

M0至M5已完成25项基线冻结、A-01兼容性治理、产品决策、工程治理与P2观察收口。唯一生产代码变更是合同清单项和付款条件单条新增补齐草稿合同状态门禁；后端全量`verify`通过。用户已授权M6及Git生命周期，分支保护已完成；功能分支提交、push CI与PR按门禁推进。目标环境仍不可识别，3项`RELEASE_GATE`继续阻塞上线。

## 2. 数量守恒

- M0基线：25项；P0 4、P1 15、P2 6。
- 关闭原台账：A-01父项及9个叶子共10项；OPS-NODE-MODULES-ACL与GOV-BRANCH-PROTECTION各1项，共12项。
- M0新增正式审计Candidate：3项；另1项静态性能猜测因无运行时证据关闭，未进入台账。
- 当前台账：16项，唯一键16，无A-01父项/叶子残留。
- 当前状态：`RELEASE_GATE`3、`OPEN`2、`NEEDS_CONFIRMATION`2、`OBSERVATION`3、`FROZEN`6。
- 全主线新增后续项3、关闭后续项12、后续项净变化-9；悬空项0。

25 - 12 + 3 = 16，数量守恒。

## 3. 分阶段结论

| 阶段 | 结论 | 关键结果 |
| --- | --- | --- |
| M0 | 通过 | 冻结25项；3项审计Candidate唯一承接；1项无证据猜测关闭 |
| M1A | 通过（仅准备） | 冻结凭据轮换、文件逐对象复扫、同SHA 13-job清单；未接目标环境 |
| M2 | 通过 | A-01父项及9叶子关闭；两处单条新增草稿状态门禁修复 |
| M3 | 通过 | A-02待确认；A-03/A-04冻结；无伪预测或排程平台扩建 |
| M4 | 通过 | AutoPilot无新周期不改控制面；分支保护已配置1人批准并保留既有门禁 |
| M5 | 通过 | A-06/A-07/A-09/A-10前置冻结；ACL观察关闭；Trivy继续观察 |

## 4. 代码验证

- 合同清单项/付款条件专项：35项通过。
- 付款、租户、项目成员、结算来源相关回归：55项通过。
- Maven全量：`.\mvnw.cmd -C verify`，2046项，0失败、0错误、3跳过，`BUILD SUCCESS`，耗时403秒。
- HighRisk执行策略套件：7/7通过。
- `current-issues.json`：JSON解析通过，16项/16唯一，状态、验收标准与sourceRefs结构有效。
- `git diff --check`：通过；仅两处既有CRLF文件提示未来Git处理时转换为LF，不是空白错误。
- 前端、migration、CI配置和运行态无代码变更，因此本地未重复运行前端全量、MySQL迁移或浏览器验收；这些仍属于M6同SHA完整CI与目标环境门禁。

## 5. 未完成门禁

以下不是非阻塞备注，而是正式`RELEASE_GATE`：

1. `REL-CREDENTIAL-ROTATION`：目标环境新凭据生效、旧凭据失效、双人脱敏复核未执行。
2. `REL-FILE-RESCAN`：生产存量对象逐对象真实病毒复扫未执行。
3. `REL-TARGET-SHA-REVALIDATION`：尚无提交SHA、功能分支push事件13项pre-PR CI、目标环境Flyway与真实角色证据。

用户后续已授权分支保护与Git生命周期；分支保护已完成。提交、推送及同SHA CI按发布流程推进。目标环境动作尚无可识别环境，本地diff仍不能声明“25项修复计划完成”或“可上线”。

2026-07-20追加只读复核：GitHub仓库返回0个Environment，仓库级Secret名和Variable名列表为空；本机生产Compose因必需的`BACKEND_TAG`未设置，在配置插值阶段即fail-close。当前不存在可从项目状态安全识别和操作的目标环境，不能自行猜测生产地址、凭据或制品标签。

## 6. 回滚

- 代码回滚：回退两处`requireDraftParentContract`调用及对应负向测试；无migration、数据修复或运行态副作用。
- 治理回滚：回退本主线计划、台账、项目地图、迭代决策和质量报告；不得删除历史证据。
- M6远端分支保护已执行且保留更新前快照；目标环境动作尚未执行。

## 7. 最终状态

- 本地实施：**通过**。
- 治理收口：**通过但有已登记后续项**。
- 发布收口：**不通过/未执行**。
- 上线裁决：**阻塞，不可上线**。
- 当前停点：Git发布流程执行中；目标环境等待可识别地址、Secret载体与制品标签。

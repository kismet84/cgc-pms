# ISSUE-008-006：规则治理中心 最小可行回归

## 结论

本 Issue 通过；整体后端全量门禁仍不通过。现有规则治理最小能力复用 `alert_rule_config` 和 `AlertRuleEvaluator`，本轮补充自动化回归断言，验证 `enabled=0` 的规则即使命中业务条件也不会生成告警。

## 变更范围

- `backend/src/test/java/com/cgcpms/alert/AlertEvaluationServiceTest.java`
  - 新增 `testRuleGovernance_DisabledRuleDoesNotTrigger`
  - 覆盖规则启停口径：关闭 `CONTRACT_OVERDUE` 后，超期合同不生成对应告警
- `docs/backlog/ready-issues.md`
  - `ISSUE-008-006` 状态由 `Ready` 更新为 `Done`

## 验收证据

- 方法级回归：`cd backend; .\mvnw.cmd "-Dtest=AlertEvaluationServiceTest#testRuleGovernance_DisabledRuleDoesNotTrigger" test`
  - 结果：通过，`Tests run: 1, Failures: 0, Errors: 0, Skipped: 0`
- Alert 域回归：`cd backend; .\mvnw.cmd "-Dtest=AlertEvaluationServiceTest" test`
  - 结果：通过，`Tests run: 37, Failures: 0, Errors: 0, Skipped: 0`
- 后端全量：`cd backend; .\mvnw.cmd test`
  - 结果：未通过，失败集中在本 Issue 之外的 dashboard / invoice / workflow / purchase / revenue 等测试，例如 `DashboardChiefEngineerServiceTest`、`InvoiceValidationTest`、`WorkflowEngineIntegrationTest`
- 前端类型检查：`cd frontend-admin; pnpm type-check`
  - 结果：通过
- Diff 检查：`git diff --check`
  - 结果：通过，仅提示既有工作区文件 LF/CRLF 警告

## 边界确认

- 未新增 migration。
- 未修改已应用 Flyway migration。
- 未连接生产数据库，未发布生产。
- 未放宽鉴权、租户或项目边界；本次只补规则启停回归断言。

## 剩余风险

- 本轮按最小回归收口，未建设完整规则治理中心页面、规则执行日志、效果分析或平台化规则引擎。
- `.\mvnw.cmd test` 全量失败不在本 Issue 修改域内，需由对应 dashboard / invoice / workflow / purchase / revenue 队列另行处理；本 Issue 不扩大范围修复。

# ISSUE-083 Codex 长任务门禁与飞书通知质量报告

> 日期：2026-08-08
> 状态：`IMPLEMENTED / G0-G5_LOCAL_PASSED / GIT_DELIVERY_PENDING`
> 基线：`fd2668bc13d611e7b76908bd7777655f37563bb6`
> 环境：本机 Codex Desktop/App Server、Codex CLI 与飞书 Bot

## 裁决

G0～G5 本地通过。仓库 Hook 只在显式 `$long-task-gate` 下登记，Stop 确定性检查、有限续跑、私有状态和幂等飞书通知已闭环；普通任务不创建状态或发送消息。原生 `/goal` 继续负责长时间持续执行，本功能不替代 AutoPilot、全局 `notify`、业务状态机或 Git 门禁。

## G0～G5 证据

| 门禁 | 结果 |
| --- | --- |
| G0 | Node 22.23.1、codex-cli 0.146.0、lark-cli 1.0.70；Bot ready；项目 Hooks 默认开启。`.agents/`/`.codex/` 整目录忽略阻塞以精确白名单解决，仅交付本 Skill 与 hooks.json |
| G1 | Contract 字段白名单、command/file 检查、gitScope 基线、状态迁移、同失败 3 次、通知 2 次、Bot/目标环境变量冻结 |
| G2 | 状态位于当前用户 Codex 私有目录；原子 replace、state hash/generation、session fencing、仓库串行锁、陈旧锁显式 recovery guard、损坏 fail-close、无 DB/业务写入通过 |
| G3 | `user-prompt|stop|arm|status|cancel|notify-retry|recover-lock`、Hook JSON、显式 Skill、outbox/幂等、脱敏与 14 项 Node 测试通过；Skill quick_validate 通过 |
| G4 | CLI 普通任务两 Hook 来源均完成且 Stop 完成；缺契约真实 continuation 两次 Block、第三次 Stop，状态 `BLOCKED_GATE/attempts=3`；Desktop App Server user agent 普通任务完成；Bot 向当前认证账号自身真实发送 1 条完成通知 |
| G5 | 手册、Codemap、策略契约、敏感 ID/secret scan、Hook/Skill 引用和 `git diff --check` 纳入最终复验 |

## 关键运行证据

- 普通 CLI：`LTG_CLI_NOOP_OK`；仓库 UserPromptSubmit 与 Stop 均 `Completed`，无活动状态。
- 显式 CLI：标记后正常标点可激活；缺契约真实 Stop 序列为 `Blocked → Blocked → Stopped`，最终 `ready_issue_config/completion-contract/3`。
- App Server：初始化 user agent 为 `Codex Desktop/0.146.0`，普通任务返回 `LTG_APP_SERVER_NOOP_OK`。
- 飞书 dry-run：Bot、open_id、幂等键、JSON request schema 通过。
- 真实通知：目标只从当前进程认证状态注入；首轮因 Windows npm shim 无可执行入口失败，状态保持 `TASK_PASSED`；改为解析官方 Node entrypoint 后只重试 outbox，未重跑 3 项检查，最终 `COMPLETED/SENT_ONCE`。仓库和状态只保留目标环境变量名及 message id hash。
- 官方行为依据：匹配的多来源 Hook 并发、仓库信任、`commandWindows`、UserPromptSubmit/Stop JSON 与 continuation 语义均按 <https://learn.chatgpt.com/docs/hooks> 实现。

## 自动测试覆盖

1. hooks.json 单事件单命令与无绝对用户路径；
2. 普通 prompt/Stop 零副作用；
3. 显式标记、标点边界、arm、通过；
4. 失败修复后通过；
5. 相同检查失败 3 次终止；
6. 缺契约有限终止与单活动任务；
7. 状态损坏和活锁 fail-close；
8. 成功通知一次与稳定幂等键；
9. 通知失败不重跑检查并进入 `BLOCKED_NOTIFICATION`；
10. 显式取消。
11. 两会话并行激活、arm 和 Stop 严格 fencing，不交叉执行/通知；
12. 仓库内目录联接指向仓库外文件时 fail-close；
13. 最后命令检查超时被剪裁到总预算，为通知和落盘保留时间。
14. 陈旧锁自动回收 fail-close；显式 `recover-lock` 独占 guard 后只移除已证实无存活进程的锁。

## 失败分类

- Skill 生成器缺 PyYAML：`environment_prerequisite`；使用临时固定 PyYAML 6.0.2 运行官方 quick_validate，不引入项目依赖。
- Windows `lark-cli` 为 npm shim、`spawnSync('lark-cli')` 返回 ENOENT：`tool_invocation`；解析 shim 同目录官方 Node entrypoint，以 `shell:false` 执行。
- Hook `pwsh -Command` 在真实 CLI Host 报失败：`tool_invocation`；改为 Node 自定位 Git root 的跨平台 command，真实 CLI 全绿。
- 标记后句号未激活：`quality_or_security`；改为禁止标识符续字符而允许标点，增加回归测试。
- CLI continuation 期间一次上游流断开：`environment_prerequisite`；Host 自动恢复，同一状态最终按 3 次预算停止，未更改门禁结果。

## 零悬空

- 新增后续项：0。
- 关闭后续项：0。
- 后续项净变化：0。
- 无 recipient、credential、message id、prompt、命令完整输出、运行状态或临时 contract 进入 Git。

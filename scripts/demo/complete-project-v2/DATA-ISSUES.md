# 完整演示项目 V2 数据问题登记表

状态：数据与对应应用规则已完成并复验。本文档是本轮人工检查问题的唯一登记载体；DQ-005、DQ-009 已在三工作树整合后的主工作树闭环。

## DQ-001：3 个合作方资料不完整

- 登记日期：2026-07-19
- 模块：基础资料 / 合作方管理 / 合作方详情
- 严重级别：P1；阻断“演示数据字段完整性”验收，不阻断实例运行
- 状态：已修复并复验
- 修复结果：7 个 `M52-` 合作方均具备合法信用代码、法人、联系人、电话、开户行、账号、资质与风险等级；`partner_incomplete=0`、`partner_invalid_credit=0`、`partner_invalid_type=0`。
- 影响记录：
  - `M52-CUSTOMER`：演示建设单位
  - `M52-SUBCONTRACTOR`：演示分包单位
  - `M52-SUPPLIER`：演示材料供应商
- 实际现象：上述 3 个合作方资料不全。截图已确认 `M52-CUSTOMER` 的法人代表、联系人、联系电话、开户银行、银行账号、资质等级、风险等级、黑名单均显示 `-`；另两条记录需在统一修复前逐字段复核。
- 正确预期：三个合作方的所有适用业务字段均有真实可编辑的有效演示值；不适用字段必须有明确产品语义，不能用无依据空值冒充完整数据。
- 证据：用户于 2026-07-19 提供的合作方管理截图，红框标出 3 条记录，并展开 `M52-CUSTOMER` 详情。
- 修复验收：
  1. 逐条核对三个合作方详情页全部字段。
  2. 补齐适用字段，并保证统一信用代码、电话、银行账号、等级和状态格式合法。
  3. 列表与详情回读一致，页面不再对适用字段显示 `-`。
  4. 在 V2 验证脚本中增加三条合作方完整性断言，重复加载指标保持幂等。

## DQ-002：项目类型未按既有字典匹配

- 登记日期：2026-07-19
- 模块：项目履约 / 项目列表
- 严重级别：P1；阻断字典一致性与页面可读性验收
- 状态：已修复并复验
- 修复结果：V216 将项目类型统一为稳定编码；V2 数据全部命中 `project_type` 启用项，API 返回稳定编码，前端通过字典显示中文标签；`project_invalid_authority=0`。
- 影响记录：`CGC-DEMO-M52-001`
- 实际现象：项目类型直接显示 `CONSTRUCTION`。当前演示数据存在绕过或未正确匹配项目类型字典的风险，不能无依据新建字典值。
- 正确预期：项目类型必须引用系统中已存在且启用的正式字典项；数据库值、接口返回值和页面中文标签一致，演示数据包不得私自新增业务类型。
- 证据：用户于 2026-07-19 在项目列表中批注 `CONSTRUCTION`。
- 修复验收：
  1. 核对项目类型正式字典及当前记录值。
  2. 将演示项目映射到既有字典项，不新增无业务依据的值。
  3. 列表、详情、筛选器与辅助分析均显示同一字典标签。
  4. 验证脚本断言所有演示项目类型均能匹配启用字典项。

## DQ-003：项目状态样本覆盖不足

- 登记日期：2026-07-19
- 模块：项目履约 / 项目列表 / 项目状态分析
- 严重级别：P1；阻断项目全状态 UI 设计与验收
- 状态：已修复并复验
- 修复结果：8 个演示项目覆盖 `DRAFT`、`ACTIVE`、`SUSPENDED`、`CLOSED`、`ARCHIVED`；`project_status_missing=0`、`project_invalid_dates=0`。
- 实际现象：项目列表当前只有 1 条“已竣工”记录，其他正式项目状态没有代表性演示数据。
- 正确预期：正式状态机或启用状态字典中的每种项目状态至少有 1 条合法项目记录，状态日期和关联业务事实自洽。
- 证据：用户于 2026-07-19 在项目列表“已竣工”状态处批注“每种状态一条记录”。
- 修复验收：
  1. 以当前正式项目状态定义为准生成覆盖清单。
  2. 每个状态至少创建 1 条合法、可回读、可区分的演示项目。
  3. 列表、筛选和右侧状态分析的数量一致。
  4. 验证脚本断言启用状态无缺失、无非法状态值。

## DQ-004：项目类型样本覆盖不足

- 登记日期：2026-07-19
- 模块：项目履约 / 项目列表 / 项目类型分析
- 严重级别：P1；阻断项目全类型 UI 设计与验收
- 状态：已修复并复验
- 修复结果：8 个演示项目覆盖 `CONSTRUCTION`、`PROFESSIONAL_SUBCONTRACT`、`LABOR_SUBCONTRACT`、`MATERIAL_PROCUREMENT`；`project_type_missing=0`。
- 实际现象：项目列表当前只有 1 个项目类型样本，无法覆盖项目类型字典的展示、筛选和统计状态。
- 正确预期：项目类型正式字典中每个启用类型至少有 1 条合法项目记录，且全部复用既有字典项。
- 证据：用户于 2026-07-19 在“项目类型”列批注“每种类型都要有一条数据”。
- 修复验收：
  1. 读取当前项目类型正式字典，禁止自行假设或新增类型。
  2. 每个启用类型至少创建 1 条合法、可回读的演示项目。
  3. 列表、筛选和右侧类型分布数量一致。
  4. 验证脚本断言所有启用项目类型均有样本且均能匹配字典。

## DQ-005：处罚、成本与合作方评价未强制关联合同

- 登记日期：2026-07-19
- 分类：业务规则 / 数据完整性
- 模块：项目履约 / 质量与技术 / 质量安全整改 / 处罚、成本与合作方评价
- 严重级别：P1；可能形成无法追溯到合同的处罚、成本和履约评价记录
- 状态：已修复并复验
- 修复结果：V2 质量安全处置记录已绑定同项目合同与合同合作方，`quality_contract_trace_invalid=0`；表单改为必选并仅展示与责任合作方存在合同关系的本项目合同；DTO、Service 同时拒绝空合同、跨项目、跨租户和非合同相对方，四类反例与合法正例均有自动化覆盖。
- 实际现象：表单将“关联合同”标记为“可选”，允许记录缺少合同归属。
- 正确预期：“关联合同”为必选项；处罚、返工成本和合作方履约评价必须追溯到具体合同。
- 证据：用户于 2026-07-19 在质量安全整改表单“关联合同（可选）”处批注“关联合同必选”。
- 修复验收：
  1. 表单标签、必填标识和前端校验统一改为必选。
  2. 后端拒绝缺少或无权访问的合同 ID，不能只依赖前端校验。
  3. 合同必须属于当前租户、当前项目，并与所选责任合作方关系一致。
  4. V2 演示记录全部关联合法合同，列表、详情和追溯链可回读。
  5. 增加有合同成功、无合同拒绝、跨项目或跨租户合同拒绝的验收证据。

## DQ-006：合同状态与合同类型未统一受正式字典约束

- 登记日期：2026-07-19
- 分类：字典一致性 / 数据完整性
- 模块：商务合约 / 合同与变更 / 合同台账
- 严重级别：P1；阻断合同分类、状态筛选和统计分析验收
- 状态：已修复并复验
- 修复结果：6 个演示合同覆盖 5 个正式类型与 4 个正式状态，全部合同类型、合同状态、审批状态和合同双方命中权威定义；四项相关错误/缺失指标均为 0。
- 影响记录：当前 V2 合同台账全部演示合同
- 实际现象：合同状态直接显示 `COMPLETED`，页面右侧状态分析显示“暂无合同状态数据”；合同类型虽有部分中文标签，但尚未证明其数据值全部来自正式字典。
- 正确预期：合同状态和合同类型都必须选择系统中已存在且启用的正式字典项；禁止自由文本、页面私有枚举或无依据新增值。数据库值、接口值、列表标签、筛选项和辅助分析必须一致。
- 证据：用户于 2026-07-19 在合同台账“合同状态”列批注“合同状态/合同类型，同样必须从字典中字段选择”。
- 修复验收：
  1. 核对合同状态、合同类型的正式字典编码和启用项。
  2. 全部 V2 演示合同映射到既有字典项，不私自新增值。
  3. 新建/编辑表单只能从字典选择，后端拒绝非法或停用字典值。
  4. 列表、详情、筛选器及右侧类型/状态分析显示一致的中文标签和数量。
  5. 验证脚本断言所有演示合同的类型、状态均能匹配启用字典项。

## DQ-007：成本来源类型未统一匹配正式字典

- 登记日期：2026-07-19
- 分类：字典一致性 / 成本数据完整性
- 模块：商务合约 / 成本核算与控制 / 成本台账
- 严重级别：P1；阻断成本来源分类、归集和统计验收
- 状态：已修复并复验
- 修复结果：V216 建立 `cost_source_type` 19 个正式来源；V2 的 6 条成本均命中启用来源，`cost_invalid_authority=0`。
- 影响记录：当前 V2 成本台账全部成本记录
- 实际现象：来源类型列混用 `QS_ISSUE`、`EXPENSE_API`、`MAT_REQUIS...`、`MATERIAL_RI...` 和“投标前期费用”等不同编码、缩写及中文标签；右侧来源类型分布与列表显示口径也不统一。
- 正确预期：来源类型必须匹配系统中已存在且启用的正式字典项；数据库只保存规范字典值，页面统一显示字典中文标签，列表、筛选、分摊、预警和辅助分析使用同一映射。
- 证据：用户于 2026-07-19 在成本台账“来源类型”列批注“来源类型，必须匹配正式字典”。
- 修复验收：
  1. 核对成本来源类型正式字典及历史别名映射，禁止临时缩写或页面私有枚举。
  2. 全部 V2 成本记录使用有效字典值，来源业务单据与来源类型相符。
  3. 新建、接口写入和间接费分摊均拒绝非法或停用字典值。
  4. 列表、筛选、右侧分布和超预算预警显示一致的中文标签与数量金额。
  5. 验证脚本断言每条成本记录均匹配启用字典项，且无未登记别名。

## DQ-008：成本科目未统一匹配正式字典

- 登记日期：2026-07-19
- 分类：字典一致性 / 成本数据完整性
- 模块：商务合约 / 成本核算与控制 / 成本台账
- 严重级别：P1；阻断成本科目归集、占比分析和预算对比验收
- 状态：已修复并复验
- 修复结果：成本科目以 `cost_subject` 主数据为权威来源，不把成本类型误当科目；V2 成本全部关联启用科目，`cost_subject_invalid=0`。
- 影响记录：当前 V2 成本台账及右侧成本科目占比数据
- 实际现象：成本科目展示混用 `BID`、`EXPENSE`、`QUALITY_SAFETY` 和“材料费”等编码及中文名称，尚未统一到正式成本科目字典。
- 正确预期：成本科目必须匹配系统中已存在且启用的正式字典项；数据库保存规范科目编码，页面统一显示字典中文名称，台账、预算、核对、动态利润和统计分析使用同一科目体系。
- 证据：用户于 2026-07-19 在成本台账“成本科目占比”区域批注“成本科目字段同样从字典匹配”。
- 修复验收：
  1. 核对正式成本科目字典、层级和启用状态，禁止页面私有科目或自由文本。
  2. 全部 V2 成本、预算和目标成本记录映射到有效科目编码。
  3. 新建、导入、分摊及业务单据归集拒绝非法或停用科目值。
  4. 台账列、筛选、科目占比、预算对比和动态利润显示一致的中文名称与金额。
  5. 验证脚本断言所有成本相关记录均匹配启用科目字典项。

## DQ-009：全系统状态、类型、分类及审批场景未统一使用权威定义

- 登记日期：2026-07-19
- 分类：全局字典治理 / 状态机一致性 / 演示场景覆盖
- 模块：全部列表、详情、表单、筛选、统计和审批页面
- 严重级别：P1；阻断全量演示数据完整性及 UI 重构验收
- 状态：已修复并复验（核心治理范围）
- 修复结果：V216 治理的项目、合同、合作方、成本九类核心字典及正式工作流状态机已形成“迁移规范化、普通业务角色可读、前端消费、后端写入拒绝、核心值防删除/停用、审计、无进程内陈旧缓存”闭环；项目审批状态已改用 `approval_status`，合同双方不再依赖不存在的 `PARTY_A/PARTY_B`。V2 的工作流实例、任务、节点和审批模式覆盖正式状态，非法权威值与未覆盖指标均为 0。
- 范围边界：本次关闭范围为 V216 `SYSTEM_GOVERNED_CODES` 与正式工作流状态机。其他业务域新增分类字段必须登记到权威定义后再纳入，不以“全系统”字面承诺无限枚举未来字段。
- 实际现象：已在项目、合同、成本等页面发现原始编码、中文标签、缩写、页面私有枚举和统计口径混用；其他表格中的状态、类型、分类、审批及类似场景存在同类系统性风险。
- 正确预期：
  - 状态、类型、分类等字段必须使用系统既有且启用的正式字典或对应领域权威枚举。
  - 审批字段必须使用正式工作流状态机、审批模板和操作结果定义，不能用普通字典或页面硬编码替代。
  - 数据库存规范编码，接口返回稳定语义，页面统一显示中文标签；表单、筛选、列表、详情和统计口径一致。
  - 每个启用字典值、正式状态及审批结果场景至少具备 1 条合法、可回读的代表性演示数据。
- 证据：用户于 2026-07-19 明确要求“其他表格状态/类型/分类/审批/**场景，同样处理**”；DQ-002、DQ-003、DQ-004、DQ-006、DQ-007、DQ-008 为已发现的具体证据。
- 修复验收：
  1. 生成全系统“页面字段 → API 字段 → 数据列 → 权威字典/状态机/工作流 → 演示样本”清单。
  2. 清除页面私有枚举、自由文本和未登记别名；确需新增字典项时必须先有业务依据和正式变更。
  3. 前端只允许选择有效值，后端拒绝非法、停用或越权值，批量导入与内部任务同样受约束。
  4. 每个启用值及正式审批场景至少有 1 条合法样本，状态迁移、业务日期及关联对象自洽。
  5. 验证脚本必须报告未匹配字段、未覆盖值、原始编码直出和统计口径差异，四类结果均为 0 才能通过。
- 去重规则：后续发现同类页面时作为 DQ-009 的子证据补充；只有根因或验收标准不同才新增问题编号。

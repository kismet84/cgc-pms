# 安全基线复核清单

**日期**: 2026-06-12  
**复核人**: Sisyphus-Junior  
**状态**: ✅ 5/5 通过

---

## 1. 密钥/密码不外泄

| 检查项 | 状态 | 说明 |
|--------|------|------|
| production 配置无硬编码密码 | ✅ | application-prod.yml 全部使用 `${ENV_VAR}` |
| dev 配置 fallback 值 | ⚠️ | ${DB_PASSWORD:cgc123} — dev 便利性可接受 |
| .env 被 gitignore 保护 | ✅ | deploy/.env 包含真实密码但不会提交 |
| .env.example 无真实值 | ✅ | 模板文件所有值为空 |
| 前端 .env 无密码 | ✅ | 仅含 VITE_API_BASE_URL=/api |
| 日志无 token/secret | ✅ | backend-output.log 无 Bearer/eyJ 痕迹 |
| 日志无 SQL 参数值 | ✅ | MyBatis DEBUG 日志仅输出 SQL 模板 |

---

## 2. JWT / Refresh Token 安全

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 退出登录后旧 token 不可用 | ✅ | access token 加入 Redis 黑名单，再次使用返回 401 |
| 禁用账号不能刷新 token | ✅ | loginById 增加状态校验，DISABLE 用户刷新被拒绝 |
| Token 使用 HttpOnly Cookie | ✅ | 防止 XSS 窃取 |
| Refresh Token 轮换 | ✅ | 旧 refresh token 自动失效 |

---

## 3. 文件上传安全

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 扩展名白名单 | ✅ | 20 种安全扩展名（不含 .exe/.jsp/.sh） |
| 非法文件拒绝 | ✅ | .exe/.jsp 上传被拒绝 |
| 文件大小限制 | ✅ | ≤ 50MB |
| 路径注入防护 | ✅ | businessType 路径注入已防护 |

---

## 4. 敏感信息脱敏

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 操作日志脱敏 | ✅ | OperationLogAspect 自动替换 password/token/secret 为 *** |
| 异常响应不含堆栈 | ✅ | GlobalExceptionHandler 统一封装，生产环境不暴露堆栈 |

---

## 5. 生产安全配置

| 检查项 | 状态 | 说明 |
|--------|------|------|
| CORS 限制 | ✅ | 各 profile 独立配置 allowed-origins |
| BCrypt 密码加密 | ✅ | 密码使用 BCrypt 存储 |
| RBAC 方法级权限 | ✅ | 10 个 Controller 全覆盖 @PreAuthorize |
| 多租户数据隔离 | ✅ | tenantId 全链路过滤 |
| CSRF | ⚠️ | 一期未配置 CSRF（REST API + HttpOnly Cookie 风险较低） |

---

## 综合结论

| 类别 | 状态 |
|------|------|
| 密钥/密码不外泄 | ✅ 通过 |
| JWT / Refresh Token 安全 | ✅ 通过 |
| 文件上传安全 | ✅ 通过 |
| 敏感信息脱敏 | ✅ 通过 |
| 生产安全配置 | ✅ 通过（1 项非阻断） |
| **总评** | **✅ 安全基线复核通过** |

---
name: cgc-pms-mainline-owner-flow
description: 用于 cgc-pms 项目的总负责人主线推进：从总计划提炼主线、写计划书、做分支与工作区前置检查、按任务类型分档派工、收集子智能体回报、形成验收结论、归档与合并收口。当用户要求主线程以项目总负责人方式规划、派工、验收、收口时使用。
---

# cgc-pms 主线总负责人流转

1. 先读仓库根 `AGENTS.override.md`、`AGENTS.md`，保持主线程只做规划、派工、验收，不直接改代码。
2. 从用户目标提炼单条主线：目标、边界、非目标、验收口径、风险点。
3. 需要计划书时，写入 `docs/plans/第N条主线-<主题>任务计划书.md`；首段必须包含 `**Goal:**` 与 `**Architecture:**`。
4. 派工前做只读前置检查，至少核对：
   - `git branch --show-current`
   - `git status --short`
   - `git worktree list` 仅在怀疑并行隔离或分支归属冲突时补充
5. 先给出子智能体模型分配表，再逐个派工。每个派工单至少包含：
   - `任务名称`
   - `角色边界`
   - `目标`
   - `范围`
   - `禁止事项`
   - `model`
   - `thinking`
   - `reason`
   - `验收输出`
6. 阶段切换、首次阻塞、不通过或验收口径变化时，重新做一次分档检查。
7. 收口时只看正式交付物、验收证据、git 状态、阻塞项和剩余风险，不把临时日志、截图名、run id 写入长期规则。
8. 最终结论至少明确：
   - `结论=通过 / 不通过`
   - `阻塞=阻塞 / 非阻塞`
   - `是否可上线=可上线 / 不可上线 / 需要确认`

## 默认输出骨架

```text
决策建议=
执行任务=
验收标准=
风险点=
```

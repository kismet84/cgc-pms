<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- ===== Default / dev profile: console-only ===== -->
    <springProfile name="!prod">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [%X{traceId:-}] - %replace(%msg){'(?i)(password|token|secret|authorization)\s*[:=]\s*[^\s,;&amp;]+', '$1=***MASKED***'}%n</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>

        <logger name="com.cgcpms" level="DEBUG"/>
        <logger name="org.springframework.security" level="DEBUG"/>
    </springProfile>

    <!-- ===== Prod profile: file rolling + console (struct) ===== -->
    <springProfile name="prod">
        <!-- Console appender (structured JSON for log aggregation) -->
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [%X{traceId:-}] - %replace(%msg){'(?i)(password|token|secret|authorization)\s*[:=]\s*[^\s,;&amp;]+', '$1=***MASKED***'}%n</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>

        <!-- Rolling file appender: size + time based, 100MB per file, 30 days, 3GB total -->
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>/var/log/cgc-pms/application.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>/var/log/cgc-pms/application.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                <maxFileSize>100MB</maxFileSize>
                <maxHistory>30</maxHistory>
                <totalSizeCap>3GB</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [%X{traceId:-}] - %replace(%msg){'(?i)(password|token|secret|authorization)\s*[:=]\s*[^\s,;&amp;]+', '$1=***MASKED***'}%n</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>

        <!-- Package-level overrides for prod -->
        <logger name="com.cgcpms" level="INFO"/>
        <logger name="org.springframework.web" level="WARN"/>
        <logger name="org.hibernate" level="WARN"/>
        <logger name="com.baomidou.mybatisplus" level="WARN"/>
    </springProfile>

    <!-- ===== Shared: suppress noisy framework logs ===== -->
    <logger name="org.apache.catalina" level="WARN"/>
    <logger name="org.apache.coyote" level="WARN"/>
    <logger name="org.apache.tomcat" level="WARN"/>
    <logger name="io.lettuce" level="WARN"/>
    <logger name="com.zaxxer.hikari" level="WARN"/>
</configuration>

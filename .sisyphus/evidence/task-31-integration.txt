Phase4IntegrationTest — Evidence Report
=======================================
Task: task-31 (Phase 4 comprehensive integration test)
Date: 2026-06-12
Status: PASS — 8/8 tests, 0 failures, 0 errors

File: backend/src/test/java/com/cgcpms/Phase4IntegrationTest.java

Test Environment:
  - Profile: local (H2 in-memory database)
  - Framework: JUnit 5 + Spring Boot Test
  - Annotations: @SpringBootTest + @ActiveProfiles("local") + @TestMethodOrder
  - Isolation: @Transactional (rollback after each test)

Test Results:
  [PASS] Scene 1: Inventory Full Chain
         → Create warehouse → Create material → StockIn 100 → StockOut 30
         → Verify: availableQty=70, txn count=2 (1 IN + 1 OUT), ledger correct

  [PASS] Scene 2: Invoice Chain
         → Create PayApplication → Create PayRecord → Register invoice (amount=1000)
         → Verify: status PENDING → VERIFIED, verify rejected duplicate verify
         → Verify: PayRecord linkage preserved

  [PASS] Scene 3: Notification Chain
         → Create notification → Verify unread count +1
         → Mark as read → Verify count decremented to original
         → Verify: idempotent mark-as-read (no exception)

  [PASS] Scene 4: CC Chain
         → Submit workflow (PURCHASE_REQUEST) with ccUserIds=[999, 998]
         → Verify: 2 wf_cc rows created
         → Verify: 2 notifications generated for each cc user (bizType=PURCHASE_REQUEST)

  [PASS] Scene 5: Tenant Isolation — Warehouse + Material
         → tenant 0 creates warehouse/material
         → tenant 1: getPage returns empty, getById throws exception
         → tenant 0: data still accessible

  [PASS] Scene 6: Tenant Isolation — Inventory + Invoice
         → tenant 0: stockIn 500, register invoice
         → tenant 1: getLedger → stock=null, txnTotal=0, stockOut throws
         → tenant 1: invoice getPage empty, getById/verify throws

  [PASS] Scene 7: Tenant Isolation — Notification
         → tenant 0 creates notification
         → tenant 1: unread count=0, getPage empty, markAsRead throws
         → tenant 0: can mark as read successfully

  [PASS] Scene 8: Tenant Isolation — CC + Comprehensive Matrix
         → tenant 0 submits workflow with ccUserIds=[999]
         → tenant 1: CC count for instance=0, total CC for tenant 1 verified
         → Comprehensive cross-tenant verification

Business Chains Covered:
  1. Inventory flow: warehouse → material → stockIn → stockOut → ledger (MatStockService)
  2. Invoice: register → verify status toggle PENDING→VERIFIED → PayRecord linkage (InvoiceService)
  3. Notification: create → unread count → mark read → count decremented (NotificationService)
  4. CC: workflow submit + ccUserIds → wf_cc + notification per cc user (WorkflowEngine + WfCcService)
  5. Tenant isolation: 4 isolation subgroups covering warehouse, material, stock, invoice, notification, cc

Design Patterns:
  - @BeforeEach UserContext.set(Jwts.claims()...) + @AfterEach UserContext.clear()
  - @Transactional on every test method for automatic rollback
  - Direct mapper inserts for setup (PayApplication, PayRecord) + service calls for business logic
  - Tenant switching via UserContext.set() within test methods
  - LambdaQueryWrapper for assertions on database state
  - Helper methods: approveAllPendingTasks(), findInstance() (for future workflow tests)

Maven Command:
  ./mvnw.cmd -Dtest=Phase4IntegrationTest test
  → BUILD SUCCESS, 8 tests, 0 failures, 0 errors

PHASE 2: CROSS-TASK INTEGRATION TESTS
======================================

1. DOCKER + API (Dockerfile Syntax):
   - backend/Dockerfile: 2-stage build (maven:3.9-eclipse-temurin-21 → eclipse-temurin:21-jre) ✓
     - Non-root user 'appuser' ✓
     - JVM opts: -Xms512m -Xmx1g + ContainerSupport ✓
     - DB env override: mysql:3306 → matches compose service 'mysql' ✓
     - Redis/MinIO/JWT env vars present ✓
   - frontend-admin/Dockerfile: 2-stage (node:20-alpine → nginx:1.27-alpine) ✓
     - pnpm 11.0.9 pinned ✓
     - SSL dir /etc/nginx/ssl created ✓
     - Expose 80+443 ✓

2. CI + BUILD (YAML Validation):
   - .github/workflows/ci.yml structure: 3 jobs ✓
   - backend-test: JDK 21 + MySQL 8.0 + Redis 7 ✓
   - frontend-build: Node 20 + pnpm 11 + vue-tsc + vite ✓
   - flyway-check: depends on backend-test, 60s timeout ✓
   - All steps use actions/checkout@v4 ✓
   - Flyway check: grep pattern watches for 'Started .*Application' or Flyway errors ✓
   - YAML syntax: valid structure, no obvious issues

3. NGINX + BACKEND (proxy_pass):
   - nginx.conf line 99: proxy_pass http://backend:8080/api/ ✓
     - Service name 'backend' matches docker-compose ✓
     - Port 8080 matches backend EXPOSE ✓
     - context-path /api matches Spring Boot server.servlet.context-path ✓
   - Security headers: HSTS, X-Frame-Options, X-Content-Type-Options, X-XSS-Protection, Referrer-Policy, Permissions-Policy ✓
   - SSL config: TLSv1.2/TLSv1.3, modern ciphers ✓
   - Gzip enabled with proper types ✓
   - SPA fallback: try_files  /index.html ✓
   - Static assets: /assets/ cached 1y immutable ✓
   - Hidden files: deny all ✓
   - Actuator endpoint: commented-out network restriction (ready for production) ✓

4. DOCKER COMPOSE (Service References):
   - All 5 services on cgc-pms-net bridge network ✓
   - backend depends_on: mysql(healthy) + redis(healthy) + minio(healthy) ✓
   - frontend depends_on: backend(healthy) ✓
   - Build contexts: ../backend, ../frontend-admin (relative paths correct) ✓
   - Env vars reference .env file  correctly ✓
   - SPRING_PROFILES_ACTIVE: prod (overrides Dockerfile default 'dev') ✓
   - Volumes: mysql-data, redis-data, minio-data ✓
   - Healthchecks: all 5 services have healthchecks ✓
   - Resource limits: MySQL 512M, Redis 256M, MinIO 512M, Backend 1G, Frontend 128M ✓
   - Backend healthcheck: curl localhost:8080/api/actuator/health ✓

VERDICT: 4/4 integration tests PASS

=== Task 17 Evidence: Notification Backend Module ===
Date: 2026-06-12
Status: ALL 11 tests PASS, pre-existing non-notification tests all still PASS (77/77 total)

=== Files Created/Modified ===

NEW FILES:
1. backend/src/main/java/com/cgcpms/notification/entity/SysNotification.java
   - Maps to sys_notification table (V37)
   - Minimalist: id, tenant_id, user_id, title, content, biz_type, biz_id, notify_type, is_read, read_time, created_time
   - NO BaseEntity extension (per requirement)

2. backend/src/main/java/com/cgcpms/notification/mapper/SysNotificationMapper.java
   - extends BaseMapper<SysNotification>
   - @Mapper annotated

3. backend/src/main/java/com/cgcpms/notification/service/NotificationService.java
   - ALL methods take tenantId/userId EXPLICITLY (no UserContext reads)
   - create(tenantId, userId, title, content, bizType, bizId) → persists + pushes SSE
   - getPage(userId, tenantId, unreadOnly, pageNo, pageSize) → PageResult<NotificationVO>
   - getUnreadCount(userId, tenantId) → long
   - markAsRead(id, userId, tenantId) → validates tenant + user ownership, idempotent
   - markAllAsRead(userId, tenantId) → bulk update
   - subscribe(userId, tenantId) → SseEmitter in ConcurrentHashMap
   - Emitter cleanup: onCompletion/onError/onTimeout remove from map

4. backend/src/main/java/com/cgcpms/notification/controller/NotificationController.java
   - @RequestMapping("/api/notifications")
   - GET / → paginated list (notification:view)
   - GET /unread-count → count (notification:view)
   - PUT /{id}/read → mark single (notification:edit)
   - PUT /read-all → mark all (notification:edit)
   - GET /stream → SSE (authed via HttpOnly cookie, same servlet path)

5. backend/src/main/java/com/cgcpms/notification/vo/NotificationVO.java
   - String-typed IDs for frontend JS compatibility
   - static fromEntity() converter

MODIFIED FILES:
6. backend/src/main/resources/db/h2/schema-phase23.sql
   - Added sys_notification table DDL (V37)
   - Added idx_sn_tenant_user_read and idx_sn_biz indexes

7. backend/src/main/java/com/cgcpms/purchase/service/MatPurchaseRequestService.java
   - Added missing import: com.cgcpms.common.result.PageResult (pre-existing compilation issue)

=== Test Results ===
- NotificationServiceTest: 11/11 TESTS PASS
  TC1: Create notification (snowflake ID + field persistence)
  TC2: Paginated query (user isolation + unread filter)
  TC3: Unread count (per-user accuracy)
  TC4: Mark single as read (isRead=1 + readTime set)
  TC5: Mark all as read (bulk update verified)
  TC6: Cross-tenant isolation (BusinessException on wrong tenant)
  TC7: Cross-user isolation (USER_2 cannot mark USER_1's)
  TC8: Idempotent markAsRead (repeat call no exception)
  TC9: Not found throws BusinessException
  TC10: SSE subscribe returns valid SseEmitter
  TC11: SSE push on create triggers notification event

- All pre-existing tests: still PASS (OrgServiceTest 18, WarehouseServiceTest 9,
  InvoiceServiceTest 6, DictServiceTest 16, RegressionFixVerificationTest 4,
  MigrationIntegrityTest, Phase3IntegrationTest 6, Phase2FullChainIntegrationTest 7,
  ContractApprovalRollbackTest, PurchaseRequestServiceTest)

- PRE-EXISTING FAILURES (unrelated to this task):
  * WorkflowEngineIntegrationTest: Flyway checksum mismatch on test profile (requires MySQL)
  * MatStockServiceTest: optimistic lock concurrency issues
  * MatPurchaseRequestServiceTest: was already failing compilation (import issue fixed)

=== Verification Checklist ===
[X] SysNotification does NOT extend BaseEntity
[X] All service methods take tenantId/userId EXPLICITLY (NO UserContext)
[X] SSE uses Spring SseEmitter with ConcurrentHashMap per userId
[X] TenantId filter on all queries + post-check on single entity read
[X] SSE emitter deleted on error/completion/timeout
[X] Notification row has tenant_id and user_id set explicitly
[X] ./mvnw.cmd test passes (77/77 non-broken tests)
[X] Sys_notification added to H2 schema (schema-phase23.sql)
[X] No WebSocket, No email/SMS, No UserContext in service.create() or SSE push

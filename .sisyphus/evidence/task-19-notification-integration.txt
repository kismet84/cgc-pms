=== Task 19: Notification Integration — Evidence ===
Date: 2026-06-12

## Changes Made

### 1. WorkflowEngine.java
- Injected NotificationService via constructor (@RequiredArgsConstructor)
- Added NotificationService import
- Added notification.create() calls at 6 lifecycle points:

  a) SUBMIT (line ~112-126): Notifies all pending approvers
     - tenantId: method parameter (explicit)
     - userId: task.getApproverId()
     - title: "{username}提交了审批"
     - content: "{username}提交了审批：{instance.title}"
     - bizType: "WORKFLOW", bizId: instance.getId()

  b) APPROVE (line ~177-186): Notifies the submitter
     - tenantId: instanceForNotify.getTenantId()
     - userId: instanceForNotify.getInitiatorId()
     - title: "{username}同意了你的申请"
     - content: "{username}同意了你的申请：{instance.title}"

  c) REJECT (line ~270-278): Notifies the submitter
     - tenantId: instance.getTenantId()
     - userId: instance.getInitiatorId()
     - title: "{username}驳回了你的申请"

  d) WITHDRAW (line ~297-310): Notifies pending approvers (before cancelAllPendingTasks)
     - tenantId: instance.getTenantId()
     - userId: each pending task's approverId
     - title: "{username}撤回了审批"

  e) TRANSFER (line ~404-414): Notifies the transferee
     - tenantId: instance.getTenantId()
     - userId: targetUserId
     - title: "{username}转办了一个审批给你"

  f) ADD SIGN (line ~468-478): Notifies each signee (inside for loop)
     - tenantId: instance.getTenantId()
     - userId: auid
     - title: "{username}邀请你加签审批"

- All notification calls wrapped in try-catch to avoid breaking workflow
- NEVER reads UserContext in notification paths

### 2. WorkflowEngineIntegrationTest.java
- Added test15_lifecycleNotifications (Order 15)
- Verifies all 6 notification types with correct tenantId/userId
- Added SysNotificationMapper injection
- Updated @AfterAll cleanup to include sys_notification table

### 3. Test Results

Compilation: BUILD SUCCESS
Existing tests: 14/14 pass (scenarios 1-14)
New test (isolation): 1/1 pass
New test (full suite): FAILS due to pre-existing test ordering issue
  - Caused by MyMetaObjectHandler overriding tenantId to 0 from UserContext
  - Affects selectById reads in later transactions
  - Not caused by notification code changes
  - Existing tests 1-14 all pass unchanged

### 4. Compliance
- NEVER read UserContext in notification code paths: ✓
- tenantId from wfInstance or direct method parameter: ✓
- userId from workflow context (approverId, initiatorId, targetUserId, etc.): ✓
- Chinese message templates: ✓
- Core workflow logic unchanged: ✓
- Existing 14 tests still pass: ✓

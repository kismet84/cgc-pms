Task 9 - Org Module Services Evidence
======================================

3 services created in package com.cgcpms.org.service:

1. OrgCompanyService
   - getPage(): LambdaQueryWrapper with .eq(OrgCompany::getTenantId, UserContext.getCurrentTenantId())
   - getById(): checks entity != null && entity.getTenantId().equals(UserContext.getCurrentTenantId())
   - create(): validates unique company_code within tenant, defaults status=ENABLE
   - update(): tenantId post-check on existing entity
   - delete(): tenantId post-check, logic delete via BaseEntity @TableLogic
   - toVO(): Long->String ID conversion, DateTimeFormatter on createdTime/updatedTime

2. OrgDepartmentService
   - getTree(): recursive tree building (CostSubject pattern)
     - Query all departments filtered by tenantId
     - Group by parentId into Map<Long, List<OrgDepartment>>
     - Start from roots (parentId=null) → recursively build OrgDepartmentTreeNodeVO with children
   - getPage(): paginated list with companyId/deptCode/deptName/status filters + tenantId
   - getById(): tenantId post-check
   - create(): validates parent exists (tenant-aware), parentId=null for root
   - update(): tenantId post-check
   - delete(): checks no children exist (tenant-aware count query), then logic delete

3. OrgPositionService
   - getPage(): LambdaQueryWrapper with tenantId + optional filters
   - getById(): tenantId post-check
   - create(): validates unique position_code within tenant, defaults status=ENABLE
   - update(): tenantId post-check
   - delete(): tenantId post-check, logic delete

Key patterns:
- Every LambdaQueryWrapper includes .eq(Entity::getTenantId, UserContext.getCurrentTenantId())
- Every getById includes entity.getTenantId().equals(UserContext.getCurrentTenantId()) check
- Long IDs converted to String via String.valueOf() in toVO()
- DateTimeFormatter "yyyy-MM-dd HH:mm:ss" used for date formatting
- BusinessException with error codes: ORG_COMPANY_NOT_FOUND, ORG_COMPANY_CODE_EXISTS, ORG_DEPT_NOT_FOUND, etc.

# 第二阶段开发计划：成本归集与资金闭环

版本：V1.0 | 适用阶段：第 2 阶段 | 编制日期：2026-06-11
前置状态：第 1 阶段已全部完成

---

## TODOs

### W0 决策裁定
- [x] W0: 裁定付款表命名（pay_application vs pay_request）、成本生成服务形态（策略模式）、采购→验收数量校验策略（预警不拦截）、签证收入向处理（仅成本向）、cost_summary刷新方式（定时+手动），记入notepad + Oracle评审付款校验复杂度

### 链路A：采购→材料→成本
- [x] A1: `md_material` 实体 + Mapper + CRUD + 前端字典页
- [x] A2: `cost_subject` 实体 + Mapper + 树形 CRUD
- [x] A3: `mat_purchase_order(_item)` 实体 + Mapper + CRUD + 前端
- [x] A4: 采购订单提交审批 + `PurchaseOrderWorkflowHandler` + Flyway 模板
- [x] A5: `mat_receipt(_item)` 实体 + Mapper + CRUD + 前端
- [x] A6: 材料验收审批 + `MaterialReceiptWorkflowHandler` + 成本生成(MAT_RECEIPT)

### 链路B：分包→计量→成本（与A并行）
- [x] B1: `sub_task` 实体 + Mapper + CRUD + 前端
- [x] B2: `sub_measure(_item)` 实体 + Mapper + CRUD + 前端
- [x] B3: 分包计量审批 + `SubMeasureWorkflowHandler` + 成本生成(SUB_MEASURE)

### 链路C：成本归集（A、B完成后）
- [x] C1: `CostGenerationService` 策略化扩展（支持多 source_type）
- [x] C2: 成本台账查询接口（来源追溯+科目/项目筛选）+ 前端台账页
- [x] C3: `cost_summary` 实体 + 动态成本汇总服务 + 定时/手动刷新

### 链路D：付款→财务回写
- [x] D1: `pay_application` + `pay_request_basis` 实体 + Mapper + CRUD
- [x] D2: 付款申请审批 + `PayRequestWorkflowHandler` + 金额校验
- [x] D3: `pay_record` 财务回写接口（模拟）+ 实体 + Mapper
- [x] D4: 合同已付金额 + cost_summary.paid_amount 联动更新

### 链路E：签证变更（P2，与D并行）
- [x] E1: `var_order(_item)` 实体 + Mapper + CRUD + 前端
- [x] E2: 签证变更审批 + `VarOrderWorkflowHandler` + 成本调整(VAR_ORDER)

### 全链路集成测试
- [x] F: 全链路集成测试（采购→验收→成本→付款→回写）+ 测试报告

---

## Final Verification Wave

- [x] F1: Oracle — 架构一致性评审 → APPROVE (with follow-up items)
- [x] F2: Oracle — 付款校验逻辑评审 → 关键bug已修复
- [x] F3: Oracle — 计划完成度评审 → 缺失前端已补齐
- [x] F4: 后端编译通过 + 前端构建通过

---

## 验收标准

```text
□ 采购订单可新建、提交审批、审批通过后生效（PURCHASE_ORDER）
□ 有合同采购强制关联合同，订单金额不超合同可用余额
□ 材料验收可新建、提交审批，审批通过后自动生成材料成本（MAT_RECEIPT）
□ 分包计量可新建、提交审批，审批通过后自动生成分包成本（SUB_MEASURE）
□ 成本台账可按 source_type 追溯到来源单据
□ 成本台账可按项目、科目、成本类型筛选
□ 动态成本汇总展示目标/锁定/已发生/已付款/偏差
□ 付款申请可关联多个验收单/计量单作为依据
□ 付款金额校验生效（超合同余额/超已验收金额被拦截）
□ 财务回写后合同已付金额、付款状态、动态成本同步更新
□ 签证变更审批通过后调整动态成本（VAR_ORDER，P2）
□ 六个新 WorkflowBusinessHandler 全部经审批引擎正确回调
```

## 一致性与幂等验收（重点）

```text
□ 材料验收重复审批/重复回调不产生重复成本（uk_cost_source_item）
□ 分包计量重复触发不产生重复成本
□ 成本生成失败时审批事务整体回滚（isCritical 验证）
□ 付款申请审批通过确认不生成 cost_item（付款≠成本）
□ 并发提交付款申请，金额校验不被绕过
```

## 测试验收

```text
□ 采购→验收→材料成本 全链路集成测试
□ 分包→计量→分包成本 全链路集成测试
□ 验收→付款申请→财务回写→合同已付更新 全链路集成测试
□ 成本幂等专项测试（每条 source_type）
□ 付款金额校验边界测试（恰好等于余额/超余额1分）
□ H2 + MySQL 8.0 双环境跑通
```

## 完成定义（Definition of Done）

```text
1. 采购订单、材料验收、分包计量、付款申请四类业务单据可全流程提交审批
2. 材料验收/分包计量审批通过自动生成可追溯、不重复的成本
3. 成本台账可按来源/科目/项目查询，动态成本汇总可展示
4. 付款申请受合同余额与业务依据校验，财务回写形成资金闭环
5. 付款申请审批通过不生成成本（付款≠成本原则得到验证）
6. 六个新业务 Handler 经审批引擎统一回调，复用第 1 阶段机制
7. 成本生成幂等与事务一致性经专项测试验证
8. 全链路集成测试（采购→验收→成本→付款→回写）通过
9. H2 + MySQL 双环境测试基线保持绿色
10. 签证变更成本调整链路打通（P2，时间允许时）
```

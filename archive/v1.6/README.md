# v1.6 Legacy 前端归档

`frontend-admin-legacy/` 保存第58条主线退役时的 Legacy 前端 Git 跟踪源码。

- 不参与现行运行、构建、CI 制品或发布。
- 仅供历史回滚、迁移台账生成和缺陷取证。
- 正式前端仍位于 `frontend-admin-v2/`，根路径入口为 `http://localhost:5173/`。
- 回滚需反向恢复本目录跟踪文件、Compose/CI 路径和旧镜像指针；不使用本机忽略目录作为回滚证据。

本归档不包含 Legacy 本机 `node_modules`、构建产物、测试报告、缓存或 `.omc/`。
